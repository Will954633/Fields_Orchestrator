# <ADDRESS> — approved campaign copy
<Marketplace listing + group variants + ad primary text. Attribute the listing agent on every public
asset. Run claim_gate.py --slug <slug> --file over every $-claim / "only"/"every" before publishing.>
