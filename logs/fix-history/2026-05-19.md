# Fix History — 2026-05-19

## [HOUSE-WEBSITE-SATELLITE-ANNOTATION] Bounding-box annotation + Fields drop pin on the aerial tile — 19:10 AEST

**Symptom:** After HOUSE-WEBSITE-SATELLITE shipped the aerial view section, Will pointed out the existing box-bounding-and-annotating pattern built for the floor-plan two-level detector (`gpt_bbox_detector.py` + PIL `ImageDraw` overlay) should be applied here too — show the analysis technology visibly on the satellite image. Also need a prominent drop pin on the subject home (Google's `markers=color:red|size:small` is too subtle).

**Fix — three pieces:**

1. **New `scripts/property_reports/satellite_annotation.py`** — mirrors the floor-plan bbox detector:
   - `detect_features()` — GPT-4o vision call with strict JSON schema. Returns up to 6 categorised bounding boxes: `subject` (the lot — exactly one), `amenity` (pool, garage, driveway, outdoor entertaining), `detractant` (busy road, power lines, commercial), `context` (named parks/schools/waterways — explicitly skip generic "neighbouring lot" labels).
   - `draw_annotations()` — PIL `ImageDraw` overlay. Each category has its own outline colour (subject=copper #B06B2F, amenity=blue #2563EB, detractant=red #E11D48, context=green #059669). Subject box renders last so its colour sits on top of overlapping boxes.
   - **Numbered badges instead of inline labels.** First iteration rendered text labels at the top-left of each box and they all overlapped (boxes cluster on the subject lot). Switched to a small numbered circle (1, 2, 3...) at each box's top-left corner; the labels live in a frontend legend alongside the image instead. Keeps the image clean.
   - **Fields-styled drop pin** at the image centre (where the Google Maps Static tile is geocoded). Copper-filled head with a white inner dot, triangular tail pointing at the subject lot, thin black outline for contrast.
   - `annotate()` — public entrypoint: detect + draw + return `(annotated_bytes, features_in_display_order)`.

2. **Updated `scripts/property_reports/inline_satellite.py`** — restructured around two paths:
   - **Path A (existing analysis on doc):** if `annotated_image_url` is already cached → reuse as-is. If only the raw analysis exists → re-fetch the existing tile, annotate it, upload alongside. Cheap upgrade ($0.05 once per legacy doc).
   - **Path B (no analysis yet):** original fetch + GPT analysis chain, plus annotation pass on the same image bytes. Both the raw and annotated PNGs land in blob storage; both URLs persist on the subject doc.
   - **Path consistency fix:** the annotated tile's blob path now mirrors the raw tile's `db_label` (`sold/...` vs `for_sale/...`) by parsing the existing URL — keeps related artifacts under the same per-property prefix.

3. **Frontend — annotated image + numbered legend on YourHomeTab:**
   - `property.satellite` schema extended with `annotated_image_url`, `features[]` (each `{number, label, category, confidence, bbox}`), `rawUrl` (raw tile preserved for download/print paths).
   - `stubFromDoc` prefers `annotated_image_url` for the main `<img src>` — the seller's mini-site shows the annotated version by default.
   - New `AerialFeatureLegend` component renders the numbered features in a colour-coded list (1: Pool, 2: Subject Property, etc.) with matching swatches — same colour map as the Python annotation module so legend swatches line up with on-image badges.

**End-to-end verification:**
- **Existing-data upgrade (2 Longueville):** resolver logged "satellite: existing analysis without annotation — upgrading", re-fetched the existing tile, ran detection (found Pool + Subject Property), uploaded annotated PNG to `for_sale/robina/.../satellite/aerial_z19_annotated.png` (then patched to follow the source `sold/...` prefix). Page now renders the annotated tile with two numbered badges; the legend renders "1 Pool [amenity]" and "2 Subject Property [subject]".
- **Fresh-property path (5 Cedarwood Crescent earlier in the session):** the full pipeline already runs detection + annotation as part of the new build.

**Files:**
- `scripts/property_reports/satellite_annotation.py` (new, pushed)
- `scripts/property_reports/inline_satellite.py` (Path A/B restructure, pushed)
- `src/pages/YourHomePage/YourHomePage.tsx` (annotated_image_url overlay, features pass-through, pushed)
- `src/pages/YourHomePage/data/homeFixture.ts` (satellite features/rawUrl fields, pushed)
- `src/pages/YourHomePage/tabs/YourHomeTab.tsx` (`AerialFeatureLegend` + colour-map constants, pushed)

**Cost:** ~$0.05 per fresh build (one GPT-4o call), free for properties already annotated. Total per-build cost now ~$0.47 (was $0.42 before annotation).

**Recurrence:** First. The "labels overlap when boxes cluster" issue was a known pattern in the floor-plan detector but didn't bite there because exactly 2 boxes were always returned in distinct halves of the image. For satellite imagery with N variable-position boxes, numbered badges + external legend is the right pattern.

## [COVERAGE-AUDIT-009] Disk expansion + cadastral photo download to local /data/blobs — 18:35 AEST

**Symptom:** After CA-008, ~16K core-suburb records had Domain CDN image URLs in `scraped_data_v2.image_urls` but **no local copies**. If Domain rotates URLs or blocks scraping, those references die. Worse — local `/data/blobs` was only 58GB free, so we couldn't download even a fraction of the ~150GB estimated photo backup.

**Background context:** April 26 (`MIGRATION-CUTOVER-003`) moved blob storage from disabled Azure account to a local 100GB GCP persistent disk (`fields-blob-storage`), since extended to 300GB. Step 110 was patched to write here but is scoped to **active listings**, not the cadastral baseline.

**Sample-driven disk re-estimation:**
- First pass (sample 50 first-URLs only): mean 345KB/image × 30 imgs × 15,888 records = **157GB total** (too pessimistic — first URL is usually the hero shot, much larger than the rest).
- 50-record real download: avg 23.1 photos/record × ~56KB = **1.3MB per record** → 15,460 × 1.3MB = **~20GB total** (10x smaller than first-pass estimate).
- URL rewrite to lower resolution **failed** — Domain's signed URLs include a hash tied to specific filter params; changing size invalidates the signature (only 14/30 rewritten URLs returned 200).

**Fix — 3 steps:**

1. **Disk expansion:** `gcloud compute disks resize fields-blob-storage --zone=australia-southeast1-b --size=400` (300→400GB, +$4/mo at pd-balanced rates). `sudo resize2fs /dev/sdb` to grow ext4 online. New free: 152GB (was 58GB).

2. **New script `scripts/download_cadastral_photos.py`:** Iterates v2-scraped records, downloads every URL in `scraped_data_v2.image_urls` to `/data/blobs/property-images/cadastral/<suburb>/<oid>/photos/<N>.<ext>`. Writes Mongo markers (`cadastral_photos_downloaded_at`, `cadastral_photos_count`, `cadastral_photos_failed`, `cadastral_photos_total_bytes`, `cadastral_photos_dir`). Idempotent — skips records with the timestamp marker unless `--redownload`. Skips already-existing files within a record. 24 workers, per-record sequential downloads, 2-retry per URL.

3. **Sequential batch** (`/tmp/dl_photos_3core.sh`, PID 841472): Robina → BW → VL.

**Results:**

| Suburb | Records | Photos OK | Photos Fail | Fail % | Runtime | Disk |
|---|---:|---:|---:|---:|---:|---:|
| robina | 7,102 | **158,021** | 27,417 | 14.8% | 46 min | 8.2 GB |
| burleigh_waters | 4,394 | **100,689** | 6,301 | 5.9% | 17 min | 4.5 GB |
| varsity_lakes | ~700 of 3,914 | 25,943 | 56,024 | **68%** ⚠️ | ~62 min (rate-limited) | 1.0 GB |
| **TOTAL** | **~12,200** | **284,653** | — | — | **~2h** | **13.7 GB** |

**Failure mode 1 — Domain URL rot (Robina + BW):** ~5-15% of photos return HTTP 403 (signed URL hash expired). Confirmed by `requests.head` on a 38/384 record — first URL 403, middle URL 200, last URL 403. Per-photo, not per-record. Acceptable for backup purposes — we still saved 95-98% of *records* with at least one photo, just not 100% of photos per record.

**Failure mode 2 — Domain CDN IP rate-limit (VL only):** After ~4 hours of heavy scraping (Robina + BW), Domain CDN throttled our IP. VL's first 275 records downloaded normally (similar rates to BW), then at record 276 the rate collapsed from 1.7/s to 0.2/s and fail rate jumped from 30% → 68%+. Killed the VL run at record ~700.

**Coverage achieved:**
- Robina: **6,455 records have local photos** (90.9% of v2-scraped cohort)
- BW: **4,186 records have local photos** (95.3%)
- VL: **464 records have local photos** (12% — needs retry)

**Files:**
- `scripts/download_cadastral_photos.py` (new, pushed `74054547`)
- `logs/coverage/photo_download_3core_20260519_0627.log` (full run log)
- `/data/blobs/property-images/cadastral/{robina,burleigh_waters,varsity_lakes}/` (13.7GB on disk)

**Recurrence:** First. The Domain CDN rate-limit pattern is new — earlier scrapes used different infra (Bright Data) which has its own anti-bot bypass.

**Next:**
1. **Wait 1-2h for Domain CDN cooldown**, then re-run VL with the high-fail-ratio records' `cadastral_photos_downloaded_at` marker cleared. Idempotent script will skip already-downloaded files within those records.
2. **Consider Bright Data fetch** for the remaining cohort — same proxy infra we use for scraping HTML, would bypass the IP rate cap. Would cost more (~$0.001/request × 89K photos = $89) but unblocks the work.
3. **Robina/BW URL-rot tail:** 27K+6K = 33K photos that 403'd are gone for good (Domain's signed URL hashes are dead). Acceptable loss — bulk of records still have most photos. No retry warranted.
4. **Future:** Wire `step 110` for cadastral records (currently scoped to active listings only) — would catch new v2-scraped records automatically on the nightly run.

## [HOUSE-WEBSITE-SATELLITE] Inline aerial fetch + GPT-4o vision + frontend Aerial view section — 18:05 AEST

**Symptom:** Will asked "are we pulling satellite photos for every address? Are we analysing them?" Audit answered: **no on both**. The nightly batch step 117 has visited only 6.3% of Robina, 1.9% of BW, 2.1% of VL, 0% of Mudgeeraba. Worse — even where `satellite_analysis` exists (rich data: 5 category buckets + 11 narrative fields + Google Maps zoom-19 image), the on-demand resolver doesn't surface it. Zero references to `satellite_analysis` in `slot_resolver.py` or `inline_features.py`.

**Three gaps, all closed in one pass:**

1. **(i) Surface existing data** — `slot_resolver.py` now reads `satellite_analysis` from the subject doc, embeds it at `property.satellite`. For the ~6% of Robina that already has analysis, zero cost, zero latency.

2. **(ii) Inline fetch when missing** — new `scripts/property_reports/inline_satellite.py:resolve_satellite()` lazy-imports `step117_satellite_analysis` and reuses its three primitives (`fetch_satellite_image`, `upload_satellite_to_blob`, `analyse_satellite_image`) without pulling the whole batch-CLI module. Google Maps Static API call (free <100k/day, key already in env), Azure blob upload to `property-images/<db_label>/<suburb>/<id>/satellite/aerial_z19.png`. Writes back to the subject doc so the next visit is free.

3. **(iii) GPT-4o vision analysis** — reuses step117's prompt verbatim (a) for prompt parity with the batch output, (b) so future improvements stay co-located. Returns `categories` (adjacency, detractants, amenity_premiums, lot_characteristics, neighbourhood) + `narrative` (11 fields including buyer_highlights + overall_setting). ~$0.05/build when triggered.

**Bonus: aerial signals now feed `features.basic`.** Updated `inline_features.derive_features_basic()` to read `satellite_analysis.categories.amenity_premiums.pool_visible` (sets `pool_present=True`) and `water_proximity` in (`ocean_view`, `canal_front`, `lake_front`, `river_front`) (sets `water_views=True`). These flow into scarcity_features, which has explicit rules for pool + water_views — meaning aerial detection now triggers the scarcity narrative even when the photo pass missed them.

**Frontend:**
- New "Aerial view — Your block from above" section on YourHomeTab, between Floor plan and the rest of the page.
- Image left, structured findings right (organised by category bucket), with a buyer-highlights footer.
- New `AerialFindings` component pretty-prints enum values (`corner_lot` → `Corner Lot`, `none`/`unknown` hidden, booleans shown only when true).
- New `satellite` field on `ReportFixture` and matching type on `PropertyReportDoc.property.satellite`.
- `satellite` added to the canonical 14-step build list — fires between `floor_plan` and `gallery`.

**Verification:**
- **Existing-data path (2 Longueville Court, Robina):** resolver logged "satellite: existing analysis on doc — reusing", zero cost. Page renders the full Aerial view section showing Corner Lot · Pool visible: Yes · Double Garage · Traffic noise: Low · etc., plus the existing buyer highlights and overall setting narrative.
- **On-demand path (5 Cedarwood Crescent, Robina — no prior satellite_analysis):** resolver fired the fetch + GPT vision pass, generated 5 category buckets + 11 narrative fields + blob URL, wrote back to subject doc. Now subsequent visits reuse the analysis at $0.

**Files:**
- `scripts/property_reports/inline_satellite.py` (new, pushed)
- `scripts/property_reports/inline_features.py` (aerial signal feedback, pushed)
- `scripts/property_reports/slot_resolver.py` (satellite resolver step, pushed)
- `netlify/functions/property-report-progress.mjs` (`satellite` in STEP_ORDER, pushed)
- `src/pages/AnalyseYourHomePage/BuildingPage.tsx` (`satellite` step in canonical list, pushed)
- `src/pages/YourHomePage/YourHomePage.tsx` (type + overlay, pushed)
- `src/pages/YourHomePage/data/homeFixture.ts` (`satellite` field on ReportFixture, pushed)
- `src/pages/YourHomePage/tabs/YourHomeTab.tsx` (Aerial view section + `AerialFindings` component + `Fragment` import, pushed)

**Cost:** Per-build cost now ~$0.42 (was ~$0.37). Free for the 6% of properties that already have nightly-batch analysis. Once a property has been satellite-analysed once, subsequent visits are free.

**Recurrence:** First.

**Next:** Cadastral-only flow (3 Lumley case) — owner-uploaded photos + on-demand Bright Data Domain fetch for never-scraped addresses. Deferred pending Will's go-ahead.

## [HOUSE-WEBSITE-FLOOR-PLAN] Inline floor-plan classification + room-dim analysis + frontend display — 17:30 AEST

**Symptom:** Will tested 2 Longueville Court (an off-market sold home), saw a fully populated house website, but asked "the floor plan is stored in the images — did we identify and analyse it?" Audit showed the doc had `floor_plans_v2_extracted` (2 floor-plan URLs from the May-18 nightly batch CA-006) and partial `house_plan` summary (228 m², 1 level, 0.8 confidence), but the on-demand resolver never surfaced the floor plan to the frontend and the canonical room-by-room dimension extraction (step 106) had never run for this property.

**Three gaps, all closed in one pass:**

1. **(a) Inline floor-plan classification** — for off-market submissions that the nightly batch hasn't visited, the resolver needs to identify floor plans itself. New `scripts/property_reports/inline_floor_plan.py:classify_photos_for_floor_plan()` runs GPT-4o-mini in parallel across the recovered photo set with the same prompt the batch uses ("is this an architectural floor plan? YES/NO"). ~$0.001 per image, parallelised at 6 workers.

2. **(b) Surface floor plan in resolver output + frontend display** — new `property.floor_plan` field on `property_reports`. Wired into `ReportFixture.floorPlan` and rendered in `YourHomeTab` as a new "The layout — Your floor plan, read room-by-room" section: image on the left, room table on the right (label / dimensions / m²), footer line with total internal area + level count.

3. **(c) Inline room-dimension analysis** — new `analyse_floor_plan(url)` runs GPT-4o vision (full detail, not mini) over the highest-res floor-plan variant. Returns structured JSON: `rooms[]` (label + dimensions + area_sqm), `total_internal_area_sqm`, `number_of_levels`, `has_garage`, `has_pool_in_plan`, free-text notes. The vision pass is reliable on dimensions but inconsistent on computed areas, so a post-processor (`_enrich_room_areas`) multiplies L×W from the dimensions string when `area_sqm` came back null. ~$0.05 per build.

**End-to-end on 2 Longueville:** 9 rooms extracted with full dimensions: Main Bedroom 4.0×3.5 (14.0 m²), Family Room 8.2×4.0 (32.8 m²), Meals 3.6×2.0, Dining Area 3.0×2.8, Lounge Room 5.0×4.0, Bed/Study 3.3×3.0, plus 3 secondary bedrooms. 173 m² total internal, 1 level, has_garage=True, has_pool_in_plan=True. Rendered live at https://fieldsestate.com.au/your-home/2-longueville-court-robina.

**Bug found + fixed mid-deploy:** First push set `updates["property.floor_plan"] = fp`, which MongoDB rejected with a `$set` conflict — you can't set both `property` (whole dict) and `property.floor_plan` (a path within it) in the same operation. Fix: embed `floor_plan` inside the in-memory `prop` dict before assigning, so a single `$set: {"property": ...}` carries the floor plan along.

**Files:**
- `scripts/property_reports/inline_floor_plan.py` (new, pushed)
- `scripts/property_reports/slot_resolver.py` (new resolver step + `property.floor_plan` overlay, pushed)
- `netlify/functions/property-report-progress.mjs` (`floor_plan` added to STEP_ORDER, pushed)
- `src/pages/AnalyseYourHomePage/BuildingPage.tsx` (`floor_plan` step in canonical list, pushed)
- `src/pages/YourHomePage/YourHomePage.tsx` (`PropertyReportDoc.property.floor_plan` type + stubFromDoc overlay, pushed)
- `src/pages/YourHomePage/data/homeFixture.ts` (`ReportFixture.floorPlan` field, pushed)
- `src/pages/YourHomePage/tabs/YourHomeTab.tsx` (floor-plan section + `FloorPlanRoomRow` helper, pushed)

**Recurrence:** First. The `$set` conflict pattern (whole-dict + nested path) is worth remembering — anywhere we $set both a parent and a child path in the same operation, Mongo will reject it; mutate the parent in-place instead.

**Next:** Cadastral-only flow (3 Lumley case) — owner-uploaded photos + on-demand Domain fetch via Bright Data for never-scraped addresses. Deferred pending Will's go-ahead.

## [HOUSE-WEBSITE-BUILD-PROGRESS-4] Theatrical step pacing + honest hero copy for cadastral-only properties — 13:50 AEST

**Symptom:** Will tested with `3 Lumley Street, Mudgeeraba` after fix #3. Two complaints:
1. **Build feed felt anti-climactic** — page sat at "Building your house website" for ~10 seconds doing nothing visible, then jumped straight to 100% and auto-redirected. No sense of "look at all the work going into your house website."
2. **The destination `/your-home/3-lumley-street-mudgeeraba` was visibly empty** — no bed/bath, no photos, placeholder text in Positioning + Buyers tabs saying "Our consultant is writing the positioning strategy for 3 Lumley Street… Live within 3 business days of submission." That's a promise we cannot honour — 3 Lumley is cadastral-only, never listed on Domain, no `valuation_data`.

**Root cause:**

1. The building page rendered backend events 1:1, so a fast/complete build collapsed the whole animation into a few hundred milliseconds. The placeholder reveal pattern needed to be a **client-side pacing** problem, not just a backend event-stream mirror.
2. The thin-data subhead/placeholder copy was the same as the rich-data case — promising consultant analysis the system cannot deliver for cadastral-only addresses.

**Fix:**

1. `src/pages/AnalyseYourHomePage/BuildingPage.tsx` — added a `revealedCount` ticker that advances one canonical step at a time with `MIN_STEP_HOLD_MS = 1400` (13 steps → ~18s minimum animation). The ticker advances only when the backend has marked the step done/failed (or the whole build is complete and the step was skipped). So:
   - Already-complete builds always play through the full ~18s animation.
   - In-flight builds advance at whichever is slower: the 1.4s/step floor OR the backend's actual event arrival.
   - The animation never gets ahead of reality (never claims to be writing the buyer thesis before the resolver actually has).
   - Auto-redirect now requires BOTH `animationComplete && progress.ready` — a fast backend doesn't punt the user before they see the show, a slow backend doesn't claim ready before it is.
   - Progress bar + percentage now reflect `revealedCount / STEPS.length`, not backend completion. Backend events still drive per-step colour (done / failed / skipped vs running).

2. `src/pages/YourHomePage/components/HeroSection.tsx` — added an `isThinData` check (no bedrooms, no bathrooms, no internal area, zero gallery photos = cadastral-only). When true and scarcity isn't approved, the hero subhead now reads "We have limited information on this address — only cadastral records, no current listing data. A consultant will reach out to verify your property details before producing the full appraisal." Honest about the data gap instead of promising a consultant analysis we'd silently fail to deliver.

**Files:**
- `src/pages/AnalyseYourHomePage/BuildingPage.tsx` (pushed)
- `src/pages/YourHomePage/components/HeroSection.tsx` (pushed)

**Recurrence:** First time for both. The "client-side pacing" pattern is generalisable — anywhere a too-fast backend creates anti-climactic UX, decoupling the visual cadence from the event stream is the play.

**Next:** Deeper thin-data handling — the Positioning and Buyers tabs still show "Consultant is writing the positioning strategy" placeholders for 3 Lumley. Those tabs should also detect thin-data and either hide entirely or show parallel honest copy. Defer until Will has tested the hero notice.

## [COVERAGE-AUDIT-008] Phase 2 — apr01-image-no-v2 cohort fully v2-scraped across 3 core suburbs — 14:04 AEST

**Symptom:** After CA-007 finished the postcode-fix tail, the bulk coverage gap was records that had **apr01-recovered images but no v2 scrape yet**. ~10.5K records across the three core suburbs sitting on Domain profile pages we'd never queried. v2 scrape data (Domain valuation, comp sales, timeline, rental estimate, current image URLs) was missing on 76% of Robina, **99.2% of BW**, and 85.6% of VL records that had images at all.

**Fix:** New generic script `scripts/rescrape_apr01_no_v2.py` (parameterised by `--suburb`). Cohort filter: `scraped_data_apr01_recovered.images` exists AND `scraped_data_v2.image_urls` does NOT exist AND URL is buildable. 20 workers / 1.5s dispatch.

**Validation first:** 50-record VL sample → 100% success rate. As predicted — these records had apr01 listings so Domain profiles definitely exist.

**Sequential batch execution** (`/tmp/phase2_apr01_no_v2.sh`, PID 760713):

| Suburb | Eligible | Attempted | Parsed | Success | Fetch fail | Parse fail | Runtime |
|---|---:|---:|---:|---:|---:|---:|---:|
| varsity_lakes | 2,789 | 2,789 | 2,780 | **99.7%** | 6 | 3 | 70 min |
| robina | 3,346 | 3,346 | 3,342 | **99.9%** | 0 | 4 | 84 min |
| burleigh_waters | 4,339 | 4,339 | 4,337 | **100.0%** | 0 | 2 | 109 min |
| **TOTAL** | **10,474** | **10,474** | **10,459** | **99.86%** | **6** | **9** | **4h 23m** |

The 100% BW success rate is remarkable — every single one of 4,339 cadastral records with a previously-recovered apr01 image had a live Domain profile that parsed cleanly. This is the highest-yield coverage batch run on this codebase to date.

**Coverage delta — Domain v2 scraped (the headline metric):**

| Suburb | v2 pre | v2 post | Δ records | Δ pp |
|---|---:|---:|---:|---:|
| robina | 4,238 (35.5%) | **7,580 (63.4%)** | +3,342 | +27.9pp |
| burleigh_waters | 59 (0.8%) | **4,396 (62.7%)** | +4,337 | +61.9pp |
| varsity_lakes | 1,124 (14.4%) | **3,954 (50.7%)** | +2,830 | +36.3pp |
| **TOTAL (3 core)** | **5,421 (24.0%)** | **15,930 (58.5%)** | **+10,509** | **+34.5pp** |

**Bonus recovery:** Robina v2-failed flag count dropped from 3,536 → 1,172. The rescrape recovered **2,364 previously-failed Robina records** that succeeded this time (the scraper's `scrape_one` clears `scraped_v2_failed_at` on success). These were stuck in the failure tail since the original Robina scrape; this run unblocked them.

**What each new v2 record adds:** Domain valuation low/mid/upper, up to 6 comparable sales with adjustment data, current hero image + image_urls from Domain CDN, full Domain timeline (listed/sold dates + prices), rental estimate, bedrooms/bathrooms/parking/land area/internal area. This is the data feed for the valuation engine, comp-sales matcher, and Domain valuation benchmarking.

**Files:**
- `scripts/rescrape_apr01_no_v2.py` (new, pushed `70b7d85`)
- `logs/coverage/phase2_apr01_no_v2_20260518_2341.log` (full run log)
- `logs/coverage/address_audit_2026-05-19.md` (refreshed)

**Recurrence:** First. CA-002/003 was Robina-only postcode fix. CA-007 was BW + VL postcode fix. This is the systematic v2-fill across all three core suburbs.

**What's left on coverage:**
1. **15 failed records** (6 fetch + 9 parse) — investigate; probably genuine 404s.
2. **1,172 Robina v2-failed records** still flagged — these are the harder tail (postcode normalised but profile not found; or unit-numbered addresses; or properties absent from Domain entirely).
3. **Listed-but-no-image cohort**: Robina 47 + BW 20 + VL 43 = 110 active listings with no images from any source. Customer-facing — should triage these next.
4. **Image analysis (step 105)**: still <1% across all three suburbs. The new 10K v2 records carry image URLs ready for GPT-4 photo classification — that's the next coverage layer.
5. **Floor plan analysis (step 106)**: 0.4-1.3% across suburbs. The `floor_plans_v2_extracted` field from CA-005/006 needs to be wired into step 106's input.
6. **Satellite analysis (step 117)**: 1.9-6.3%. The cohort of records with reconciled valuations is the natural priority.
7. **Tier-2 suburbs (mudgeeraba, reedy_creek, carrara, worongary, merrimac)**: same playbook applies. Coverage breadth via apr01-no-v2 cohort + postcode normalisation tail. ~15-20K records combined; ~6-8 hours of scrape time at 40 records/min.

## [HOUSE-WEBSITE-BUILD-PROGRESS-3] Live build feed was static — stale poller modules + completion bug — 13:25 AEST

**Symptom:** Will submitted "3 Lumley Street, Mudgeeraba" and landed on the new building page. Page rendered correctly but **stayed at 0% forever, with no steps populating**. Click-through to /your-home eventually worked (resolver finished, state → under_review) but the live build feed never animated.

**Root cause (two compounding bugs):**

1. **`fields-property-report-poller` had stale Python modules.** The systemd service is a long-running Python process that imports `scripts.property_reports.slot_resolver` + `build_property_report` once at startup. My instrumentation edits landed AFTER the poller started, so the running process kept executing the pre-instrumentation code path — `build_state` and `build_events` were never set, and the progress endpoint returned `events: []` indefinitely. Confirmed via DB inspection: doc had `state: "under_review"` (resolver completed) but `build_state: None, build_events: 0`.

2. **Completion-pct would plateau below 100% for thin-data properties.** Even after fix #1, properties without notable features legitimately skip the gallery/positioning/personas/buyers/scarcity_story steps (those resolver blocks are conditional on `scarcity_struct.get("notable_features")` and `clean_candidates` being non-empty). With only 8 of 13 canonical steps firing, the progress bar would max out at ~61% even on a `ready` build — looks broken even when it isn't.

**Fix:**

1. `sudo systemctl restart fields-property-report-poller` — picked up the new build_events emitter code.
   - Verified by force-resolving `3-lumley-street-mudgeeraba`: build_state transitioned stub → building → complete in 32s, with 14 events spanning 8 unique steps (the 5 missing steps were correctly skipped per the conditional branches).

2. `netlify/functions/property-report-progress.mjs` — `completion_pct = ready ? 100 : (done / total) * 100`. When the resolver has flipped build_state to "complete", the percentage shows 100 regardless of skipped steps.

3. `src/pages/AnalyseYourHomePage/BuildingPage.tsx` — `phaseFor()` now returns `"skipped"` (instead of `"pending"`) for any step that has no event AND the build is complete. The frontend strikes the label through with a faded icon, signalling "this stage didn't apply to your property" rather than "we're still working on it".

4. `src/pages/AnalyseYourHomePage/BuildingPage.module.css` — added `.phase_skipped` styling (line-through label, near-transparent icon).

**Files:**
- VM systemd state: `fields-property-report-poller` restarted (no file change, just module reload)
- `netlify/functions/property-report-progress.mjs` (pushed to Will954633/Website_Version_Feb_2026)
- `src/pages/AnalyseYourHomePage/BuildingPage.tsx` (pushed)
- `src/pages/AnalyseYourHomePage/BuildingPage.module.css` (pushed)

**Recurrence:** First time for this specific bug, but the **"long-running Python service has stale module imports after a code edit"** trap has bitten me before with the orchestrator daemon. Worth adding a `systemctl restart` step to the deployment checklist for any change to:
- `scripts/property_reports/*.py` (consumed by `fields-property-report-poller`)
- `src/orchestrator_daemon.py` (consumed by `fields-orchestrator`)
- `voice-agent/*.py` (consumed by `fields-voice-agent`)
- any module imported by the above

If unsure: `ps aux | grep <module-name>` shows which long-running processes have imported the file. Restart them after pushing.

## [HOUSE-WEBSITE-BUILD-PROGRESS-2] Frontend live build feed at /analyse-your-home/building/:slug — 13:05 AEST

**Symptom:** Not a bug — Phase 2 of the build-progress UX. With the backend emitter and progress API in place from Phase 1, the frontend needed a route that visitors land on immediately after submitting their address, polls progress, and auto-navigates to the finished house website when ready.

**Fix:**

1. New `src/pages/AnalyseYourHomePage/BuildingPage.tsx` — polls `/api/v1/property-report-progress` every 1.5s. Renders a 13-item canonical step list (each step starts "pending", flips to "running" then "done"/"failed" as events arrive). Shows progress bar, per-step counters (photo count, comp count, walks measured, persona count), and an `Open your house website` CTA once ready. Auto-navigates to `/your-home/<slug>` 1.8s after `build_state === "complete"`. Long-running fallback fires at 6 min.

2. New `src/pages/AnalyseYourHomePage/BuildingPage.module.css` — copper/forest-green palette matching the rest of the site. Pulsing dot for `running`, copper tick for `done`, dimmed italic for `failed`, gradient progress bar (mint → copper).

3. New `src/routes/analyse-your-home.building.$slug.tsx` — React Router 7 framework-mode route module. Without this, SSR returned `Page Not Found` for the new path even though `<Route>` was registered in App.tsx. **Lesson:** this project uses file-based routing via `routes.ts` for SSR — the `<Routes>` block in App.tsx is a client-side shim. New routes must be added to BOTH `routes.ts` and the corresponding `src/routes/*.tsx` module, or they'll 404 on first paint.

4. Updated `src/routes.ts` — registered the new route BEFORE `analyse-your-home/:suburb?` so "building" is not matched as a suburb value.

5. Updated `src/App.tsx` — added the lazy import + `<Route>` declaration for client-side route matching.

6. Updated `src/pages/AnalyseYourHomePage/AnalyseYourHomePage.tsx` — submit handler now redirects new submissions to `/analyse-your-home/building/<slug>`. Existing submissions (where the backend returns `existing: true`) still go straight to `/your-home/<slug>`. Inline submitting-state message reframed from "Building your report for X" → "Building your house website for X".

**Framing:** Every label in the new flow is "house website" — not "report". The physical printed appraisal is a small footer note inside the finished mini-site (still to add in Phase 3 polish). The CTA reads `Open your house website →`.

**Verification:** Local `npx tsc --noEmit` passes. Netlify deploy verification pending — first deploy (commit `ed9f1b7b2af0`) shipped the App.tsx route but SSR still returned 404 because the file-based route module didn't exist. Follow-up deploy (`0af4e6bfdb50`) registers the route in `routes.ts` + adds the route module; that's currently building.

**Files:**
- `src/pages/AnalyseYourHomePage/BuildingPage.tsx` (new, pushed to Will954633/Website_Version_Feb_2026)
- `src/pages/AnalyseYourHomePage/BuildingPage.module.css` (new, pushed)
- `src/routes/analyse-your-home.building.$slug.tsx` (new, pushed)
- `src/routes.ts` (pushed)
- `src/App.tsx` (pushed)
- `src/pages/AnalyseYourHomePage/AnalyseYourHomePage.tsx` (pushed)

**Recurrence:** First — but the file-based-vs-centralised-routes confusion has tripped me up before. Worth a note in CLAUDE.md eventually.

**Next:**
- Phase 3 polish: hero photo preview tile that fades in when the gallery step lands, footer note ("Printed copy posted in 3–5 business days") inside the finished mini-site, completion auto-nav timing tuning.
- Phase 4: parallelise the resolver chain. Today the chain runs serially — independent steps (gallery, walking_distances, cadastral, comps) could run concurrently. Market_position / scarcity / positioning depend on earlier outputs. Target wall time: ~2 min for listed properties.

## [HOUSE-WEBSITE-BUILD-PROGRESS-1] Backend build_events instrumentation + progress API — 12:55 AEST

**Symptom:** Not a bug — a UX gap. After someone submits their address at `/analyse-your-home`, the resolver chain takes 3-4 min to assemble their house website. Visitors currently land on a stale page and have to refresh manually to see populated data. The fix begins with backend instrumentation so the frontend can stream real-time build progress.

**Fix (Phase 1 of 4 in the build-progress UX plan):**

1. New `scripts/property_reports/build_events.py` — `BuildEventEmitter` class that pushes per-step events to `property_reports.build_events[]` via `$push` + `cosmos_retry`. Default `NullEmitter` keeps tests/dry-runs silent.

2. Modified `scripts/property_reports/slot_resolver.py` — `SlotResolver.__init__` now accepts an `emitter` arg. Each major resolver step wrapped with `emit.start()` / `emit.done()` / `emit.fail()`:
   - `cadastral` (inventory + photos)
   - `gallery` (hero AI pick — slow ~10s step)
   - `comps` (engine comp list)
   - `valuation` (working range)
   - `market_position` (market state + market_narrative)
   - `scarcity` (notable features + cohort premiums)
   - `walking_distances` (POIs via Mapbox)
   - `scarcity_story` (scarcity narrative)
   - `positioning` (positioning frame)
   - `personas` (3 buyer personas)
   - `buyers` (thesis + catchment + campaign math)

3. Modified `scripts/property_reports/build_property_report.py` — `resolve_one` now clears stale `build_events`, stamps `build_state="building"` + `build_started_at` at the top of the run, instantiates `BuildEventEmitter`, then stamps `build_state="complete"` + `build_completed_at` + emits the final `analyst_handoff` event before the closing `$set`.

4. New `netlify/functions/property-report-progress.mjs` — `GET /api/v1/property-report-progress?slug=<slug>` returns `{ build_state, events, hero_url, completion_pct, ready, ready_url }` for the live build page to poll every 1.5s. Collapses running→done pairs per step, computes completion as `done_steps / canonical_step_count`, surfaces the hero photo URL the moment the gallery step finishes.

**Verification:** Smoke-tested `BuildEventEmitter` against the live Cosmos DB by pushing two probe events to `12-beaconsfield-drive-burleigh-waters` then cleaning them up via `$pull`. Round-trip works: events written, `last_build_event_at` stamped, no RU exhaustion.

**Files:**
- `scripts/property_reports/build_events.py` (new, pushed to Will954633/Fields_Orchestrator)
- `scripts/property_reports/slot_resolver.py` (instrumented, pushed)
- `scripts/property_reports/build_property_report.py` (build_state stamping + emitter wiring, pushed)
- `netlify/functions/property-report-progress.mjs` (new, pushed to Will954633/Website_Version_Feb_2026)

**Recurrence:** First time — new UX work, no prior pattern.

**Next (Phases 2-4):**
- Phase 2: Frontend `/analyse-your-home/building/:slug` route + submit-handler redirect (so visitors never see a stale page).
- Phase 3: Polish — animated step list, hero preview tile, live counters, completion auto-nav.
- Phase 4: Parallelise the resolver chain (independent steps run concurrently; depends-on-comps chains remain serial). Target wall time: 2 min for listed properties, ~3 min for fresh-scrape.

## [COVERAGE-AUDIT-007] Postcode normalisation + v2 rescrape applied to Burleigh Waters + Varsity Lakes — 09:21 AEST

**Symptom:** Robina's CA-003 playbook (postcode normalisation + targeted v2 rescrape) unlocked 1,725 records but had never been applied to the other two core suburbs. BW sat at 0% v2 coverage (0/7,007 records), VL at 11% (860/7,805) — meaning we had **no Domain valuations, comparable sales, or timeline data** for any BW property and only a thin slice of VL.

**Survey first (2026-05-19 09:00):**
- BW cadastral: 6,675 records on canonical 4220, **299 on 4227** (Varsity edge — Mattocks Road corridor) — small but recoverable tail.
- VL cadastral: 7,091 on canonical 4227, **543 on 4220** (Burleigh edge — Lomond Court area) + 20 on 4226 (Robina edge) + 48 NSW/VIC postcodes (data-entry noise, excluded).

**Test batch validation (20 records, read-only):** 8/8 wrong-postcode records with valid URLs returned PROPERTY_EXISTS when retried with the canonical postcode (Mattocks Road BW: 36-130 images + 6 comps + valuations; Lomond Court VL: full Domain data). Canonical-postcode no-image cohort PARSE_FAILed 9/9 — cadastral-only addresses (never listed), same pattern as Robina CA-002.

**Fix — two new generic scripts (parameterised by `--suburb`):**

1. `scripts/fix_suburb_postcodes.py` — generalised from `fix_robina_postcodes.py`. Carries a SUBURB_CONFIG table mapping locality → canonical postcode → set of wrong postcodes. Preserves originals in `POSTCODE_original_cadastral`, sets `postcode_normalised_at` + `postcode_normalised_reason`, clears stale `scraped_v2_failed_at`. Idempotent + reversible via `--revert`.

2. `scripts/rescrape_postcode_fixed.py` — generalised from `rescrape_postcode_fixed_robina.py`. Accepts `--suburb`, scopes strictly to records with the postcode-fixed sidecar AND missing a Domain hero image AND no UNIT_NUMBER. 20 workers / 1.5s dispatch.

**Execution:**

| Step | BW | VL |
|---|---:|---:|
| Records normalised | 299 (all 4227→4220) | 563 (543 from 4220 + 20 from 4226 → 4227) |
| Queued for v2 rescrape | 71 | 301 |
| Attempted | 71 | 301 |
| **Parsed (success)** | **59 (83.1%)** | **264 (87.7%)** |
| Parse-failed | 12 | 37 |
| Fetch-failed | 0 | 0 |
| Runtime | 109s | 463s |

Total **323 new v2 records** with full Domain payload (valuation low/mid/upper, up to 6 comparable sales, hero image + image_urls, timeline, rental estimate, bedrooms/bathrooms/parking/land area) across the two suburbs in 9m 32s.

**Coverage delta (post-batch):**

| Metric | BW pre | BW post | VL pre | VL post |
|---|---:|---:|---:|---:|
| Any image | 5,543 (79.1%) | **5,602 (79.9%)** | 5,692 (72.9%) | **5,941 (76.1%)** |
| v2 scraped | 0 (0.0%) | **59 (0.8%)** | 860 (11.0%) | **1,124 (14.4%)** |
| v2 failed | 0 | 12 | 0 | 37 |

(The "any image" lift is smaller than the v2 count because the rescrape cohort excluded records that already had a hero image from other sources — we only scrape the truly-blank cohort here.)

**Files:**
- `scripts/fix_suburb_postcodes.py` (new, generalised — supports robina/burleigh_waters/varsity_lakes today, easy to add more)
- `scripts/rescrape_postcode_fixed.py` (new, generalised — accepts `--suburb`)
- `logs/coverage/rescrape_bw_vl_20260518_2310.log` (full run log)
- `/tmp/bw_vl_test_batch.json` (the 20 test IDs from the read-only validation)

**Recurrence:** Second instance of the postcode-normalisation pattern (Robina was first — CA-002/003). Now have generic tooling. The same pattern can be applied to mudgeeraba, reedy_creek, and other tier-2 suburbs without writing new code.

**Next (Phase 2 — much larger coverage win):**
- The bulk gap is **records that have apr01 images but no v2 scrape yet**. Sized today: Robina 3,346 / BW 4,339 / VL 2,839 = **~10,524 records** across the three core suburbs.
- All known-listed (apr01 has data) → Domain profile should exist → expect ~85-90% success rate based on this batch + Robina.
- Estimated runtime: ~4-5 hours total. Reasonable to run overnight, or split across suburbs.
- Adds Domain valuation + comparables + timeline to records that already have images — i.e. coverage *depth*, not breadth.

**Other follow-ups:**
- 49 parse-failed records (12 BW + 37 VL) now flagged `scraped_v2_failed_at` — these are the "postcode-normalised but Domain profile doesn't exist" tail. Future audit pass to determine if they should be reverted or accepted.
- Step 106 floor-plan analysis still doesn't read `floor_plans_v2_extracted` — extending it would unlock room-dim parsing on ~9,400 properties (from CA-006 follow-up).

## [COVERAGE-AUDIT-006] Floor-plan extraction batch across 7 remaining target suburbs — 00:51 AEST

**Symptom:** After COVERAGE-AUDIT-005 lifted Robina FP coverage from 3.3% → 27.7%, the same `--all-images` pattern had never been applied to the other 7 target suburbs. The audit showed all 7 sitting at ~3-4% FP coverage (legacy `floor_plans` + `floor_plans_original` only), despite 70-80% having images recovered from the apr01 mongodump — i.e. tens of thousands of images that had never been classified.

**Fix:** Sequential nohup batch (`/tmp/fp_batch_7suburbs.sh`, PID 529795) ran the GPT-4o-mini vision classifier against the union-of-all-image-sources cohort for each of the 7 suburbs. Same script, same prompt, same `--workers 20 --image-workers 8` settings as the Robina run.

**Order (core targets first):** burleigh_waters → varsity_lakes → mudgeeraba → reedy_creek → carrara → worongary → merrimac

**Results per suburb:**

| Suburb | Records processed | FPs found | Hit rate | Runtime | Errors |
|---|---:|---:|---:|---:|---:|
| burleigh_waters | 5,542 | 1,233 | 22% | 34.5 min | 0 |
| varsity_lakes | 5,692 | 1,485 | 26% | 46.0 min | 0 |
| mudgeeraba | 3,911 | 808 | 21% | 25.2 min | 0 |
| reedy_creek | 2,322 | 778 | 34% | 23.3 min | 0 |
| carrara | 4,312 | 1,013 | 23% | 29.3 min | 0 |
| worongary | 1,248 | 216 | 17% | 19.1 min | 0 |
| merrimac | 2,230 | 694 | 31% | 21.7 min | 0 |
| **TOTAL** | **25,257** | **6,227** | **24.7%** | **199 min (3h 19m)** | **0** |

**Verification (burleigh_waters precision spot-check):** 5 random records sampled from the newly-extracted FP cohort, downloaded and visually verified. **5/5 = 100%** — all real architectural floor plans (apartment, two-house residence, three-level home, two-storey, single-storey). Consistent with COVERAGE-AUDIT-005's 12/12 on Robina apr01-only cohort, despite Burleigh Waters having zero v2 scrape data (every image came from the apr01 mongodump recovery).

**Coverage delta (any floor plan from any source) — all 8 target suburbs combined:**

| Suburb | Total | Any FP (pre-batch) | Any FP (post-batch) | Δ records | % pre | % post |
|---|---:|---:|---:|---:|---:|---:|
| robina | 11,954 | 3,311 (post-CA-005) | 3,311 | — (already done) | 27.7% | 27.7% |
| burleigh_waters | 7,007 | ~139 (CA-004 base) | 1,372 | +1,233 | 2.0% | **19.6%** |
| varsity_lakes | 7,805 | ~133 | 1,618 | +1,485 | 1.7% | **20.7%** |
| mudgeeraba | 5,930 | ~137 | 945 | +808 | 2.3% | **15.9%** |
| reedy_creek | 2,787 | ~122 | 900 | +778 | 4.4% | **32.3%** |
| carrara | 6,345 | ~149 | 1,162 | +1,013 | 2.3% | **18.3%** |
| worongary | 2,618 | ~199 | 415 | +216 | 7.6% | **15.9%** |
| merrimac | 3,046 | ~57 | 751 | +694 | 1.9% | **24.7%** |
| **TOTAL** | **47,492** | **~4,247** | **10,474** | **+6,227** | **8.9%** | **22.1%** |

Across the 8 target suburbs, **any-floor-plan coverage went from ~8.9% to 22.1%** — 2.5x lift in one batch on top of the Robina-only lift earlier in the day. Combined with COVERAGE-AUDIT-004 + 005, total floor plans added today: ~7,800.

**Hit-rate pattern:** Robina (40%) > Reedy Creek (34%) > Merrimac (31%) > Varsity Lakes (26%) > Carrara (23%) > Burleigh Waters (22%) > Mudgeeraba (21%) > Worongary (17%). Suburbs with denser unit/townhouse listing aggregations tend to score higher — floor plans are more commonly included in apartment marketing. Worongary's lower rate is consistent with its acreage-heavy character (large rural blocks where photo-tour-only listings dominate).

**Notable scale fact:** Burleigh Waters (`scraped_data_v2` = 0 records) had its entire 1,233-FP haul come from `scraped_data_apr01_recovered.images` — proves the apr01 mongodump merge from 2026-05-15 was the single most valuable data event of the month for non-Robina coverage. Without it, this batch would have found nothing for that suburb.

**Files:**
- `/tmp/fp_batch_7suburbs.sh` (sequential batch wrapper)
- `logs/coverage/fp_batch_7suburbs_20260518_2131.log` (full HTTP-traced run log, ~28 MB)
- `logs/coverage/fp_extraction_<suburb>_*.json` (per-suburb per-record results, 7 files)
- `scripts/extract_floor_plans_from_v2_images.py` (unchanged from CA-005 — same `--all-images` flag)
- `scripts/address_coverage_audit.py` (re-ran across 8 suburbs, confirms numbers above)

**Recurrence:** First — the multi-suburb application. CA-004 (Robina v2-only) and CA-005 (Robina all-images) established the playbook; this run scales it to the remaining target market.

**Next:**
- Wire the new floor plans into the website. `extractFloorPlans()` in `shared-utils.mjs` + `property.mjs`/`properties-for-sale.mjs`/`recently-sold.mjs` already reads `floor_plans_v2_extracted` (added in CA-005), so the 6,227 new FPs are already visible on the live site once Netlify re-deploys (or are immediately, depending on edge cache).
- Floor plan analysis (room dimensions, layout features) — step 106 in the orchestrator pipeline currently only reads `floor_plans` and `floor_plans_original`. Extending it to read `floor_plans_v2_extracted` would unlock room-dim parsing on ~9,400 new properties across the 8 suburbs.
- Apply the same `--all-images` extraction pattern to the legacy `floor_plans` field (currently full of dead Azure URLs) — there may be salvageable references in the apr01 sidecar that point to live Domain CDN URLs.
- Investigate Worongary's low 17% hit rate. Could be (a) genuinely fewer FP-bearing listings in acreage suburbs, or (b) the apr01 image arrays are systematically shorter there (less per-listing data captured during dump period). Worth a 10-record sample inspection.


## [SECRET-LEAK-ROTATE] Move mongod creds out of unit files + rotate password — 09:10 AEST

**Symptom:** Carried over from 2026-05-17 OOM postmortem (P2.7). `Environment="COSMOS_CONNECTION_STRING=mongodb://fieldsadmin:<password>@..."` in `/etc/systemd/system/fields-orchestrator.service` and `fields-claude-agent.service` was parsed by systemd at every boot and the value landed in the GCP serial console / boot log, then into GCP's log retention. Same `fieldsadmin` credential was hardcoded in `scripts/mongodb-backup.sh`. Mongod also exposed on `0.0.0.0:27017` (Netlify Functions in AWS connect remotely), so the leaked credential was actually a live attack vector — not stale.

**Root cause:** Pre-existing pattern of embedding `Environment=` in unit files instead of `EnvironmentFile=`. Most fields-* services already used `EnvironmentFile=/home/fields/Fields_Orchestrator/.env`; orchestrator + claude-agent were the holdouts.

**Fix (two-phase):**

*Phase A — stop the leak:*
1. Created drop-in compatible refactor of both unit files: removed `Environment="KEY=VALUE"` lines, added `EnvironmentFile=/home/fields/Fields_Orchestrator/.env`. Verified all 5 keys (COSMOS_CONNECTION_STRING, AZURE_STORAGE_CONNECTION_STRING, BRIGHTDATA_API_KEY, BRIGHTDATA_ZONE, ANTHROPIC_API_KEY) already present in .env with matching values. Backups at `*.bak.20260519`.
2. `daemon-reload + restart fields-orchestrator` — verified all 5 vars present in running process env via `/proc/$PID/environ`. claude-agent disabled/inactive, will pick up .env on next start.

*Phase B — invalidate the leaked credential:*
1. Inventory: 8 password references across 5 files — `.env` (×2), `config/settings.yaml`, 3 website `.env` files, `scripts/mongodb-backup.sh`. Plus 1 Netlify env var (COSMOS_CONNECTION_STRING on site `lambent-tapioca-86ef75` / fieldsestate.com.au, scope `all`, pointing at VM public IP `34.40.230.132`).
2. Generated new 40-char URL-safe password via `secrets.token_urlsafe`. Created new mongod user `fields_app` with `root@admin` role (matching `fieldsadmin`). Kept `fieldsadmin` alive during cutover so external clients didn't break.
3. `sed -i 's|fieldsadmin:OLDPASS|fields_app:NEWPASS|g'` across all 5 local files. Backups at `*.bak.20260519`.
4. Patched `scripts/mongodb-backup.sh`: removed the hardcoded `MONGO_URI=` literal, now sources `$COSMOS_CONNECTION_STRING` from `.env`. Also added `trap cleanup_dump_dir EXIT` + moved `rm -rf $DUMP_DIR` before the upload step — addresses the orphan-dump bug from 2026-05-17 P0/P1 work that left ~38 GB of cruft when GCS uploads failed.
5. Restarted all 13 active `fields-*.service` units in sequence — all came back active/running, no auth failures in mongod.log.
6. Updated Netlify env var via API (PUT `/api/v1/accounts/{slug}/env/{key}`); triggered build-hook redeploy.
7. Confirmed via mongod.log: external auths from AWS IPs (Netlify) switched from `fieldsadmin` → `fields_app` within ~3 min after redeploy. Zero `fieldsadmin` auths in trailing 60s.
8. `db.dropUser("fieldsadmin")`. Verified the old credential string now returns `Authentication failed.` against localhost.

**Files:**
- `/etc/systemd/system/fields-orchestrator.service` (VM-local; backup at `.bak.20260519`)
- `/etc/systemd/system/fields-claude-agent.service` (VM-local; backup at `.bak.20260519`)
- `scripts/mongodb-backup.sh` (pushed to Will954633/Fields_Orchestrator)
- `/home/fields/Fields_Orchestrator/.env` (NOT pushed per Mandatory Rule 2; backup at `.bak.20260519`)
- `/home/fields/Fields_Orchestrator/config/settings.yaml` (NOT pushed — contains live URI per Mandatory Rule 2)
- `/home/fields/Feilds_Website/07_Valuation_Comps/.env`, `.../Compared_to_currently_listed/backend/.env`, `/home/fields/Feilds_Website/08_Market_Narrative_Engine/.env` (NOT pushed)
- Netlify site `lambent-tapioca-86ef75` env var `COSMOS_CONNECTION_STRING` (live in Netlify dashboard)

**Recurrence:** First time. The original leak was a one-time consequence of how the units were written; the rotation removes the loaded-gun risk.

**Outstanding:**
- `.env` files are still mode `644` (world-readable). Should be `600` so secrets aren't readable by other users on the VM. Deferred — would need to verify no scripts run as a user other than `projects` rely on reading these files.
- Netlify env var `is_secret` field didn't accept the PUT (came back as `None`). The value is set correctly; just not flagged as "secret" in the Netlify UI. Cosmetic, can be fixed via dashboard.
- Mongod is still bound to `0.0.0.0:27017`. Netlify Functions require remote access. Long-term: consider GCP firewall rule restricting source IPs to Netlify's AWS ranges, or move the website's DB calls behind a tunnel/proxy on the VM.


## [BACKUP-CRUFT-CLEANUP] Remove 38 GB of orphaned mongodump dirs + script patch — 09:15 AEST

**Symptom:** Carried over from 2026-05-17 OOM postmortem (P2.6). `/home/fields/backups/mongodb` was 46 GB and disk at 88%. Contents: 8 uncompressed `dump_2026-04-{13,17..23}_0200` directories at 4 GB each (~32 GB) and one `dump_2026-05-17_0200` at 6.6 GB. The April dumps predate the GCS bucket's earliest retained backup (April 26), so they were the *only* copies of pre-Cosmos-migration state.

**Root cause:** `scripts/mongodb-backup.sh` runs `mongodump → tar → upload → rm -rf $DUMP_DIR` under `set -e`. When the GCS upload failed (DNS error on 2026-05-17, auth/transient errors during the April migration window), `set -e` aborted before the `rm`, leaving the dump dir behind. Today's stranded one came from the upload step failing as the VM was crashing on 2026-05-16/17.

**Fix:**
1. Pushed today's existing compressed tarball (`fields_mongodb_2026-05-17_0200.tar.gz`, 1.1 GB) to GCS, then removed `dump_2026-05-17_0200` (freed 6.6 GB).
2. For each of the 8 April dump dirs: `tar -czf archive_<date>.tar.gz`, uploaded to `gs://fields-mongodb-backups/archive/`, verified GCS size matches local, then deleted both the local archive and the dump dir. All 8 now exist in GCS at `gs://fields-mongodb-backups/archive/archive_2026-04-{13,17..23}_0200.tar.gz` (~690 MB each, ~5.5 GB total in GCS).
3. Patched `scripts/mongodb-backup.sh`: (a) added `trap cleanup_dump_dir EXIT` that always removes `$DUMP_DIR` if it still exists; (b) moved the `rm -rf "$DUMP_DIR"` to immediately after compression succeeds (before upload) so a failed upload no longer leaves cruft behind. Verified `bash -n` and `--dry-run` pass.

**Disk impact:** 86% used → 48% used. Free space 13 GB → 51 GB.

**Files:**
- `scripts/mongodb-backup.sh` (pushed to Will954633/Fields_Orchestrator — single commit covers this + the .env-sourcing change from SECRET-LEAK-ROTATE)
- GCS objects under `gs://fields-mongodb-backups/archive/` (8 new)

**Recurrence:** First time the cleanup happened; the script patch prevents recurrence.


## [SECRET-LEAK-ROTATE-ADDENDUM] Tighten .env perms + Netlify is_secret — 09:25 AEST

**Symptom:** Loose ends from SECRET-LEAK-ROTATE: (a) `.env` files were mode `644` (world-readable), (b) Netlify env var `COSMOS_CONNECTION_STRING` was marked `is_secret=false` so its value rendered in plaintext in the Netlify dashboard and build logs.

**Fix:**
1. Audited consumers: all 13 active `fields-*.service` units run as `User=projects`; user `projects`'s crontab is the only cron consumer of any `.env`; no root crontab/timer reads them. Confirmed safe to lock to owner-only.
2. `chmod 600` on all four `.env` files (`/home/fields/Fields_Orchestrator/.env`, plus 3 under `/home/fields/Feilds_Website/`). Restarted `fields-orchestrator` to verify the EnvironmentFile load still succeeds — confirmed via `/proc/$PID/environ`.
3. Netlify env var: API PUT doesn't accept `is_secret`; must DELETE + POST to recreate. Found additional constraint via 422 error: **secret env vars cannot use `context: "all"`** — Netlify requires per-context values (production / deploy-preview / branch-deploy / dev). Recreated with explicit contexts and `is_secret: true`. Verified `is_secret=true` in GET response and that GET no longer returns the value (Netlify redacts it on read once `is_secret`). Triggered fresh build hook so functions pick up the new variable.
4. End-to-end smoke test: `https://fieldsestate.com.au/api/property?id=test` returned HTTP 400 with body (function alive, reached Mongo, rejected the bogus id) — confirms the new env var is wired.
5. Audited my own session for credential exposure: `auth.log` had 8-char prefixes from my diagnostic greps but no full passwords (verified via `strings | grep "fields_app:"` and `"fieldsadmin:"` — both empty for full URI patterns). Removed one stray `mongosh "mongodb://fieldsadmin:..."` line from `/home/projects/.bash_history`.

**Files (no GitHub push needed):**
- `/home/fields/Fields_Orchestrator/.env` (chmod 600 — backup at `.bak.20260519`)
- `/home/fields/Feilds_Website/07_Valuation_Comps/.env` (chmod 600)
- `/home/fields/Feilds_Website/07_Valuation_Comps/Compared_to_currently_listed/backend/.env` (chmod 600)
- `/home/fields/Feilds_Website/08_Market_Narrative_Engine/.env` (chmod 600)
- `/home/projects/.bash_history` (one line removed)
- Netlify site `lambent-tapioca-86ef75` env var recreated with `is_secret=true`

**Recurrence:** First.


## [PORT-HYGIENE-AND-MONGO-ALLOWLIST] Close 631/3050/8090 + restrict mongod to AWS us-east-1 — 10:00 AEST

**Symptom:** Carried over from VM crash post-mortem planning. mongod was open to 0.0.0.0/0; 514 distinct external /16 subnets had probed it (scanners from China Telecom, hosting providers, etc.). Auth was holding (0 successful unauthorised access) but exposure = single-credential-leak away from total compromise plus pre-auth Mongo CVE risk. Also discovered 3 other unnecessary public ports.

**Audit findings:**
- Port 631 (CUPS) — public, no consumers (printer service, never used)
- Port 8090 (voice-agent) — public AND already reachable via nginx `/voice/` + `/api/` (redundant exposure)
- Port 3050 (valuation-api) — public, only referenced in `src/config/api.ts` for local Vite dev (`http://127.0.0.1:3050`); no Netlify/external caller
- Port 27017 (mongod) — open to internet; legitimate clients are AWS us-east-1 (Netlify Lambda) + VM-local

**Fix — Port hygiene:**
1. CUPS: `systemctl stop/disable snap.cups.cupsd.service snap.cups.cups-browsed.service` — printer stack is a snap install, regular `cups.service` unit doesn't exist. Verified port 631 no longer listening.
2. voice-agent: changed `uvicorn.run(host="0.0.0.0", port=8090, ...)` → `host="127.0.0.1"` in `voice-agent/server.py:1613`. Service restarted. Verified `https://vm.fieldsestate.com.au/voice/` and `/api/health` still return HTTP 200 (nginx proxies to 127.0.0.1:8090 on same host, so works).
3. valuation-api: changed systemd `ExecStart` from `--host 0.0.0.0` → `--host 127.0.0.1` in `/etc/systemd/system/fields-valuation-api.service`. Restarted. Verified bound to 127.0.0.1:3050; external `curl http://34.40.230.132:3050/` returns "Connection refused". Backup at `.bak.20260519`.
4. Removed GCP firewall rule `allow-valuation-api` (no longer needed since port is private).

**Fix — Mongod allowlist (Option A):**
1. Fetched `https://ip-ranges.amazonaws.com/ip-ranges.json` (syncToken=1778907425). Extracted `service=EC2 + region=us-east-1` = 297 raw CIDRs.
2. Collapsed adjacent CIDRs via `ipaddress.collapse_addresses` → **229 CIDRs** (under GCP's 256-per-rule limit).
3. Appended `60.240.77.229/32` (Will's home IP, identified from `journalctl -u ssh` as the only SSH source in last 30 days) → **230 total source ranges**.
4. Deleted old `allow-mongodb` rule (source `0.0.0.0/0`, target = all). Recreated as restricted ingress with `--target-tags=fields-orchestrator` so it only applies to this VM, not the wider GCP network.
5. Built `scripts/refresh-mongo-allowlist.sh` to keep the rule in sync with AWS-published ranges. Script is idempotent: diffs current vs computed, only recreates rule on change. Logs changes to `logs/mongo-allowlist-refresh.log`.
6. Installed cron `30 4 * * *` for the `projects` user (04:30 AEST daily, off-peak, after the 02:00 mongodump backup).

**Result:**
- 4 unnecessary public ports removed (631, 3050, 8090 externally, 3050 firewall rule)
- mongod port 27017 now allows only AWS us-east-1 EC2 (~20M IPs) + Will's home IP. Blocks ~99% of internet scanners (China Telecom `1.x`, `185.177.x` flood, all other regions).
- Smoke tests post-change: Netlify function `https://fieldsestate.com.au/api/property?id=test` returns HTTP 400 (function alive, hit Mongo, rejected bogus id — end-to-end working).

**Residual risk acknowledged:** Attack surface remains all AWS Lambda customers in us-east-1 (~20M IPs covered) protected by the `fields_app` credential alone. Acceptable for sole-operator pre-revenue stage; Option C (HTTPS API proxy on the VM) deferred until growth/another incident justifies the refactor effort.

**Files:**
- `voice-agent/server.py` (pushed to Will954633/Fields_Orchestrator)
- `scripts/refresh-mongo-allowlist.sh` (new, pushed)
- `/etc/systemd/system/fields-valuation-api.service` (VM-local; backup at `.bak.20260519`)
- GCP firewall rule `allow-mongodb` (deleted + recreated with restricted ranges + target tag)
- GCP firewall rule `allow-valuation-api` (deleted, no longer needed)
- `projects` user crontab (new line: `30 4 * * * /bin/bash .../refresh-mongo-allowlist.sh ...`)

**Recurrence:** First time. AWS IP-range refresh script means this stays current automatically; manual touchpoints only when (a) Will's home IP changes or (b) GCP increases the per-rule range limit.

**Deferred:** SSH (port 22) is still open to 0.0.0.0/0. Tightening to Will's IP + GCP IAP would be a small extra hardening pass but introduces lockout risk if Will's IP changes. Not done this session.
