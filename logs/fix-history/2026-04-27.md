
---

## [WORKER-AGENT-DISABLE] Stop autonomous Worker Agent Telegram brief — 10:50 AEST

**Symptom:** Daily "MORNING BRIEF" Telegram messages still arriving (Will's Day, Growth Engine Status, etc.) from a workflow no longer in use.
**Root cause:** Crontab entry `30 20 * * * cd /home/fields/Fields_Orchestrator && bash worker-agent/run-worker-agent.sh` was still active — launched Claude Opus daily at 06:30 AEST to write deliverables and ping Telegram via `scripts/telegram_notify.py`. Last run: 2026-04-26.
**Fix:** Commented out the cron line (kept the entry for traceability). No active processes were running. Worker-agent script and deliverables left in place — only the schedule is disabled.
**Files:** crontab (user `projects`), backup at `/tmp/crontab.bak.*`.
**Recurrence:** First (related: `[WORKER-AGENT] Replace Codex agents with Claude Opus Worker Agent` was the original install).

---

## [WATCHDOG] Step 106 (Step 106 output (floor_plan_analysis)) — 02:35 AEST

**Failure class:** UPSTREAM_INCOMPLETE
**Cause:** 'floor_plan_analysis' on 36/137 properties (26% vs 55% threshold) — 39 short
**Action taken:** QUEUED process rerun (step=106, metric=floor_plan, id=watchdog_106_1777221328)
**Result:** queued — awaiting repair-agent or Claude

---

## [WATCHDOG] Step 14 (Generate Suburb Statistics) — 07:35 AEST

**Failure class:** UNKNOWN
**Cause:** No recognizable error pattern — check logs manually
**Action taken:** ESCALATED to Claude (id=watchdog_14_1777239349)
**Result:** queued — awaiting repair-agent or Claude

---

## [WATCHDOG] Step 12 (Enrich Property Timeline) — 07:35 AEST

**Failure class:** UNKNOWN
**Cause:** No recognizable error pattern — check logs manually
**Action taken:** ESCALATED to Claude (id=watchdog_12_1777239349)
**Result:** queued — awaiting repair-agent or Claude

---

## [WATCHDOG] Step 117 (Satellite Analysis (Currently Listed)) — 07:35 AEST

**Failure class:** UNKNOWN
**Cause:** No recognizable error pattern — check logs manually
**Action taken:** ESCALATED to Claude (id=watchdog_117_1777239349)
**Result:** queued — awaiting repair-agent or Claude

---

## [SCRAPER-PRICE-PARSE] Domain price extractor returning empty string for "Offers Over" listings — 13:00 AEST

**Symptom:** 173 Ron Penhaligon Way (Robina) showed `price: "Auction"` in DB despite Domain having shown "OFFERS OVER $1,899,000" since 2026-04-12. Step 115 (track_price_changes) ran clean — it just never saw a new price to record. After re-scraping with fix, **86 listings across Robina/Burleigh Waters/Varsity Lakes had stale prices** (price tracker logged 86 changes in one run).
**Root cause:** Domain changed the DOM. The `<div data-testid="listing-details__summary-title">` wrapper now contains an empty `<span style="font-size:0">` spacer as its first child, then the actual price text in a nested `<div data-testid="listing-details__listing-summary-title-name">`. The parser at `html_parser.py:289` did `price_elem.find('span')` which grabbed the empty spacer span and stored `data['price'] = ''`. Then the merge logic at `parse_listing_html:64` (`if key not in property_data and value`) — the key WAS in property_data but the value was empty — fell through silently, and the `price` field was never set, leaving stale DB values untouched. The downstream save logic in `run_curlffi_suburb_scrape.py:642` filters new fields against `skip_fields`, but if `price` isn't in the parsed dict at all, the existing DB value stays.
**Fix:** Replaced the brittle `find('span')` with a lookup for the inner `data-testid="listing-details__listing-summary-title-name"` element, with fallback to the wrapper's full text. Added an `if price_text:` guard so only non-empty extractions are written. After the fix: re-scraped 138 active target listings; 86 prices were corrected; Ron Penhaligon went from `"Auction"` → `"OFFERS OVER $1,899,000"`, with `price_history` correctly appending the change event.
**Files:** `Property_Data_Scraping/07_Undetectable_method/00_Production_System/02_Individual_Property_Google_Search/html_parser.py` (lines 289-299). Pushed to `Will954633/Property_Data_Scraping` as commit `c7ae48b`.
**Recurrence:** First. Related class: prior Domain DOM changes broke scrapers in Feb/Mar 2026 (per backup files `*.backup_chrome_fix_20260212` etc.) — Domain's CSS-Module class names rotate but `data-testid` attributes are usually stable. Lesson: prefer the most-specific testid (`listing-details__listing-summary-title-name`) over generic descendant lookups (`find('span')`) — bare descendant lookups break the moment Domain inserts wrapper elements.

---

## [EDITORIAL-FACTCHECK-RETRY] Varsity Lakes Gammon Drive editorial: failed_factcheck → draft — 12:30 AEST

**Symptom:** Only Varsity Lakes property with attempted editorial (`7/14 Gammon Drive`) was stuck at `ai_analysis.status=failed_factcheck`, blocking VL from appearing on /for-sale-v3 (which gates on `ai_analysis.status=published`).
**Root cause:** Earlier run hit hard fact-check failures (yield calculation, contradictory carspace data between listing and Domain features, mis-located childcare). Editor was unable to find a valid headline angle within the retry budget.
**Fix:** Re-ran `generate_property_ai_analysis.py --slug 7-14-gammon-drive-varsity-lakes --force`. Pipeline took 3 fact-check rounds: Draft 1 → 3 failed claims (yield, "fourteen months ago", price qualifier); Draft 2 → 7 failures including misattributed childcare branch (Robina vs Varsity Lakes), photo-derived material claim ("granite" vs valuation_data "stone"); Draft 3 → 0 failures, accepted. Final headline: "$280K in 2012. $820K in 2024. Now $975K — but with a collapsed contract." Status now `draft` (needs publish action via Editorial Review tab to surface on v3 feed).
**Files:** No code changes. DB doc `Gold_Coast.varsity_lakes._id=690bd7f58b8f54659260be36` updated.
**Recurrence:** First (for this listing). Fact-check loop is functioning as designed — the prior failure exposed real data contradictions, not a bug.

---

## [V2-V4-FUNNEL-LAUNCH] Analyse-Your-Home V2 + For-Sale V4 funnel deployed — 13:10 AEST

**Symptom:** PostHog data showed users entering an address on /analyse-your-home and abandoning before submit. UI consultant diagnosed "commitment before clarity" — the page asks for an email + delivery method without showing any value first.
**Root cause:** Page asks for commitment before showing reciprocity. Users expect "I type → I see something" but got "I type → I submit → I wait". The /for-sale-v3 feed → /analyse-your-home transition also lacked a bridge that pivots from "evaluating these homes" to "evaluate yours".
**Fix:** Built consultant-spec V2 + V4 in parallel (V1 + V3 untouched as control). V2 implements a state machine (initial → preview_loading 1.8s min → preview_ready → unlock_started → submitting → submitted) and shows an indicative buyer range (derived from `valuation_data.confidence.reconciled_valuation` when present, else suburb $/m² × lot_size from `suburb_context.price_positioning`), positioning summary, drivers list, and 3 recent comparable sales — all BEFORE the email field. Email + name + timelines only render after "Unlock full analysis" click. V4 forks V3's hero (adds independent-analysis disclaimer), seller-conversion card ("Compare your home to this kind of property" → V2), sticky CTA ("See where your home sits" → V2), and injects a mid-feed BridgeBlockV4 at ~50% scroll. `lead_variant: 'v2'` propagates to `system_monitor.analyse_leads`/`leads` so V1 vs V2 conversion can be segmented. Tested E2E with "5 Picabeen Close, Robina" — preview rendered $1,373,900–$1,679,212 with 4-bed scarcity driver and 3 sold comparables. Email input correctly gated behind unlock click.
**Files:** Created `src/pages/AnalyseYourHomeV2Page/` (page + CSS), `src/pages/DecisionFeedV4FunnelPage/`, `src/components/AnalyseHomeV2/` (PreviewLoadingStates, ValuationPreview, UnlockReportPanel, ContactDetailsForm, ConfirmationPanel + CSS), `src/components/DecisionFeedV4Funnel/` (HeroV4, StickyCTAV4, SellerConversionV4, BridgeBlockV4, DecisionFeedV4Funnel + CSS), `src/services/analyseHomeV2/buildPreview.ts`, `src/routes/analyse-your-home-v2.tsx`, `src/routes/for-sale-v4.tsx`. Modified `src/routes.ts`, `src/App.tsx`, `netlify/functions/analyse-lead.mjs` (whitelist `lead_variant`). 24 files pushed to `Will954633/Website_Version_Feb_2026` commit `7819a0d`. Netlify deploy ready, both /for-sale-v4 and /analyse-your-home-v2 return live + correct titles.
**Recurrence:** First.
