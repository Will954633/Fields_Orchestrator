## [OFFMARKET-LISTED-DISCOVERY-GAP] Listed home shown as off-market — Domain search schema migration silently broke discovery — 04:50 AEST
**Symptom:** A buyer googled 6 Joy Avenue, Burleigh Waters (listed the day before) and landed on our `/off-market/6-joy-avenue-burleigh-waters` page, which presented a currently-listed home as an off-market opportunity. Root investigation showed the listing existed nowhere in our DB (no `for_sale` doc) — only a bare cadastral off-market record.
**Root cause:** Domain migrated its sale-search page OFF `__NEXT_DATA__`. The scraper's primary listing extractor keyed on a `"listingUrl":"…"` regex inside `__NEXT_DATA__` — now matches ZERO. Discovery silently fell back to anchor-href scraping, which under-renders (4→23 listings per identical fetch). Combined with an early `if not new: break`, this under-discovered ~20-32% of core-suburb listings every night (Burleigh Waters 38/56, Robina 69/95). Undiscovered listings are never scraped → never categorised → their cadastral off-market page keeps serving. The shortfall was logged (`38 (expected 56)`) but the step still passed and no one saw WHICH addresses were missing.
**Fix:** (#3) Rewrote extraction to use the current authoritative source — the search page's `application/ld+json` array (`@type=Residence` = the complete per-page address list, always = pageSize; `@type=Event` + anchors = URLs). `discover()` now RE-FETCHES each page (up to PAGE_REFETCH_MAX) and unions URLs until they cover the Residence count, and only stops on 2 consecutive genuinely-empty pages or reaching Domain's expected count. Result: Burleigh Waters 38→54/54 (100%); 6 Joy Avenue now discovered (listing id 2021035855). (#1) Added `record_coverage()` → `system_monitor.listing_coverage` (per-suburb expected/discovered/in-DB + the SPECIFIC missing addresses) + a self-registered `listing_discovery_coverage` heartbeat, and a `collect_listing_coverage` collector so the Systems Health sheet names the missing addresses (6 Joy Avenue verified surfacing) instead of a silent red cell.
**Files:** `Property_Data_Scraping/03_Gold_Coast/Gold_Coast_Wide_Currently_For_Sale_AND_Recently_Sold/run_curlffi_suburb_scrape.py`, `Fields_Orchestrator/scripts/main_site_health_check.py`. (#2 real-time PropRadar fallback + interim page still pending.)
**Recurrence:** First for the ld+json migration; related to prior [STEP101-LABEL]/[DATA-INTEGRITY] coverage-accuracy work.

## [OFFMARKET-LISTED-PR-GUARD] Real-time PropRadar guard + interim /building page + on-demand build (#2) — 20:40 AEST
**Symptom:** Same incident as [OFFMARKET-LISTED-DISCOVERY-GAP] — a home that lists TODAY has an intra-day window before the nightly scrape captures it, during which its off-market page still renders.
**Fix (#2, real-time defence):** off-market loader now consults PropRadar's independent on_market signal (`GET /properties/search` → flat `{found,on_market,property_id}`; Cosmos-cached in `listing_status_cache`, TTL 6h listed / 7d not-listed, bots skipped) for any home NOT already for_sale in our DB. If PR says on_market: enqueue `property_build_requests` and 302 → `/building/:slug` (interim "we're building coverage" page that polls → forwards to /property when ready, and captures an SMS-when-ready phone number → Cosmos `building_alerts` + Telegram to Will via `building-alert.mjs`). VM `build_listed_property.py` (cron */3min, self-registered `listed_property_builder` heartbeat) drains the queue: scrapes that one Domain listing (creates the for_sale doc → real /property page) + fires editorial DETACHED (nightly step 120 is backstop). PropRadar/Telegram keys set in Netlify env. Verified live: controlled flip of 6 Joy Ave to status=None → off-market URL 302→/building; restore→301→/property; /building renders on-brand (0 console/network errors); building-alert 200 + Cosmos write + Telegram.
**Incident address remediated:** 6 Joy Avenue built on demand → now for_sale (54 imgs, agent, EOI price); its off-market URL 301s to /property.
**Files:** website `src/routes/off-market.$slug.tsx`, `src/routes/building.$slug.tsx`, `src/routes.ts`, `src/lib/db.server.ts`, `netlify/functions/building-alert.mjs`; orchestrator `scripts/build_listed_property.py`; crontab (drainer */3min); Netlify env (PROPRADAR_API_KEY, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID).
**Recurrence:** First; completes the [OFFMARKET-LISTED-DISCOVERY-GAP] chain (#3 discovery + #1 coverage board + #2 real-time guard).

## [QUARTERLY-COVER-ISSUE-QR] Tracked per-issue QR on The Fields Quarterly cover — 08:11 AEST
**Symptom:** The Fields Quarterly cover carried no QR. There was no way to measure whether anyone engaged with a given issue of the report — printed copies and PDF downloads were both analytics-dark.
**Root cause:** Not a bug — missing capability. The existing QR tracking (`/track/scan/<tracking_id>`) is one-code-per-homeowner, which does not fit a mass-produced publication where every copy is identical. The identity that matters there is the ASSET + ISSUE, not the reader, and no endpoint modelled that.
**Fix:**
- New `/a/<asset_code>` route on the tracking server. Resolves the code against a new `system_monitor.print_assets` registry, INSERTS ONE DOC PER SCAN into new `system_monitor.asset_scans`, increments `print_assets.scan_count`, fires PostHog `print_asset_qr_scan` + a Telegram ping, then 302s to the asset's destination with UTM params. Unregistered codes still log and still redirect to the homepage — a printed code can never dead-end.
- `?m=` distinguishes the surface one issue was scanned from (print vs pdf); it lands in `asset_scans.medium` and `utm_content`. This closes the "no per-QR differentiation" gap noted against the appraisal QRs.
- New `pipeline/generate_issue_qr.py` does both halves of the contract in one command — registers the asset AND writes the vector QR — so a QR can never be printed against a code the server cannot resolve. Re-runs are idempotent (`scan_count` is not reset).
- Cover QR placed top-right under the logotype, right-aligned to the same 12.5mm margin. 22mm, untiled: light modules are printed birch (#e9e1d7), not white, so nothing boxes the cover. Grass-on-birch clears ~10:1 contrast against the ~3:1 scanners need.
- `--verify-pdf` decodes the cover out of the rendered PDF at 150/300/600 dpi. This is the check that catches a cover pointing at a stale issue's code — the symbol can be flawless and the attribution still wrong.
**Verified:** endpoint 302s correctly via localhost AND the public `vm.fieldsestate.com.au/track/a/...`; destination page returns 200; `print_assets` + `asset_scans` docs confirmed written then test rows cleared so real data starts clean; QR decodes to the exact scan URL from the final PDF at all three dpi.
**Issue:** `quarterly-q2-2026` → `https://fieldsestate.com.au/market-intelligence/Robina`
**Files:** tracking-server/server.py, 10_Market_Report/pipeline/generate_issue_qr.py (new), 10_Market_Report/pipeline/quarterly/index.html, 10_Market_Report/pipeline/quarterly/assets/img/qr_quarterly-q2-2026.svg (new), 10_Market_Report/pipeline/quarterly/INTEGRATION.md
**Recurrence:** First (for asset/issue-level QR tracking; per-recipient QR tracking is [APPR-V4-BACKCOVER-QR])
**Known risk:** `vm.fieldsestate.com.au` is a single point of failure hard-coded into every printed copy — if the VM or nginx goes down, printed QRs dead-end permanently and cannot be re-pointed. Same exposure as the appraisal QRs; worth moving both behind a Netlify redirect before any large print run.

## [OFFMARKET-DECK-EXPANSION-DRIFT] Nerang off-market URLs indexed but served the OLD deck — 12:55 AEST
**Symptom:** A visitor on `/off-market/141-dugandan-street-nerang` was served the old ladder deck (8 cards, "See what it may be worth") instead of the Discovery deck that went live 2026-07-31. Not a one-off: **985 Nerang off-market URLs were in sitemap.xml with 0 Discovery docs** — every one of them served the old design to organic Google traffic.
**Root cause:** Two systems with independently-maintained suburb lists drifted. `generate-sitemap.mjs getOffMarketUrls()` publishes core suburbs **plus release-gated `EXPANSION_SUBURBS = ['nerang']`** (gate opened 2026-07-29, limit 1000). The deck builder `offmarket_discovery_nightly.py` had its own `CORE = [robina, varsity_lakes, burleigh_waters]` and never saw Nerang. The loader's `if (ld?.discovery)` fallthrough to the classic page is deliberate and silent — no error, no log, so the gap was invisible. The builder's docstring claimed coverage was "EXACTLY the sitemap's criteria… indexed → deck guaranteed", but the sitemap gained Nerang two days *before* that claim was written, so the invariant was already false when it was declared. A **third** copy of the list (`offmarket_intel_poller.TARGET_SUBURBS`, 9 suburbs, no Nerang) meant that even a forced build failed with `subject_not_found`.
**Fix:** (1) Builder now derives expansion suburbs from the KEYS of the `Gold_Coast.offmarket_sitemap_release` config doc — the same doc that gates the sitemap, and a strict superset of what the sitemap can emit (a suburb absent from the config contributes 0 URLs). Builder ⊇ sitemap now holds regardless of which side is edited. (2) Builds the WHOLE expansion suburb, not the released slice, so decks are ready *before* URLs are submitted and we never have to mirror the sitemap's `sort({_id:1}).limit(N)`. (3) `indexed_homes()` now yields the source collection and threads it into `build_one(slug, suburb)` instead of relying on the poller's fallback scan; `nerang` added to that scan too for on-demand paths. (4) **Coverage assertion:** after each run the job diffs the indexed slug SET against built docs (sets, not counts — a count can match while a whole suburb is missing and stale docs fill the hole) and raises if the gap exceeds 25, surfacing as ERROR on the Systems Health Process Registry with the missing count and sample slugs.
**Verified:** dry-run went `indexed=14653` → `indexed=18052 to_build=3399` once the config-derived list took effect. Test-built the reported home (8.7s) and confirmed the LIVE page now returns the Discovery deck ("We found your home" + `build-strategy` CTA); the old "See what it may be worth" marker is gone. Full 3,399-home backfill running in 6 shards.
**Gotcha:** `gc or _gc()` raised `Database objects do not implement truth value testing` inside the config read, whose `except` silently fell back to core-only — the fix looked applied but changed nothing. Caught only because the dry-run count didn't move. pymongo `Database`/`Collection` have no `__bool__`; always `is None`.
**Files:** 15_Off-Market/Page_Redesign_V2/offmarket_discovery_nightly.py, scripts/offmarket_intel_poller.py, Feilds_Website/01_Website/scripts/generate-sitemap.mjs (comment)
**Recurrence:** First for deck-coverage drift. Related: [OFFMARKET-THIN-NEWSUBURB] / [OFFMARKET-COMP-BACKFILL] (2026-07-29) were also Nerang-rollout gaps — that rollout shipped against the ladder deck, and Discovery going live for core-only on 2026-07-31 stranded it.

## [ENGAGEMENT-ACTIVITY-LEDGER] Known-contact site activity now logged to Live Leads Tracker — 13:14 AEST
**Symptom:** A returning visitor we can actually reach (email / phone / a postal address)
could re-visit the site, read a whole suburb's crash-risk page, and leave — and nothing
anywhere recorded it. The "All Leads" tab is insert-once-per-lead, so a repeat visit
produced no row and changed nothing; `returning` existed only as a static tag on
crm_contacts (crm_sync.py:318). No opportunity to follow up was ever surfaced.
**Root cause:** The pieces existed but were never joined. crm_sync.py rebuilds a
per-visitor journey hourly; live_leads_to_sheet.py owns the sheet. Nothing sat between
them turning "this known contact came back and did X" into a row. The only real
return-visit detector (property-report-activity.mjs:98-134) is scoped to a mini-site
owner holding a device_token and drives UI, not a log. The RL-era equivalent
(16_General_Reinforcement_Learning/onsite_signal.py `return_visit`) has been paused
since 2026-07-30.
**Fix:** New `scripts/engagement_activity_to_sheet.py`. Builds the reachable-contact set
from crm_contacts (email / phone / home_confirmed / property_address / probable_address /
offmarket_home), pulls their PostHog events, splits into visits (>30 min gap), keeps
visits where the contact was already known to us beforehand, and writes one verbose row
per visit to a new "Activity" tab on the same spreadsheet. Reuses the existing sheet
auth/tab helpers from live_leads_to_sheet.py and posthog_query/INTERNAL_IDS/BOT_CITIES
from crm_sync.py rather than duplicating them. Dedupe via
system_monitor.engagement_activity_ledger keyed `<contact_id>:<visit_start>`; rows insert
at the top so manual edits shift down intact. Deliberately sends nothing to anyone —
there is no mailer yet; this is the substrate a later trigger reads.
Accuracy decisions worth keeping:
  - Engaged time = max(time_on_page.duration), NOT last-event-minus-first. The milestones
    fire at 10/30/60/120/300s of ACTIVE time and stop at 300, so a tab left open 40 min is
    5 min of reading. Wall-clock span is shown separately and labelled "tab open".
  - Every postal address carries its provenance ("owner-confirmed" vs "inferred — off-market
    lookup, not given to us"), so a row never implies someone handed us their details.
  - A viewed home report is only called "their OWN" when the slug matches that contact's
    own slugs; otherwise "(not their own)" + a different opportunity. Contacts routinely
    read neighbours'/comparables' reports and the naive version asserted the opposite.
  - offmarket_home.address is stored unpunctuated ("13 4 Yodelay Street Varsity Lakes"),
    which is not postable — the slug is reformatted instead ("13/4 Yodelay Street,
    Varsity Lakes").
**Files:** scripts/engagement_activity_to_sheet.py (new), crontab (23:57 daily).
**Verified:** 7-day backfill wrote 21 rows; immediate re-run added 0 (dedupe holds);
job_runs heartbeat `engagement_activity_to_sheet` recorded success (Rule 7).
**Recurrence:** First

## [ENGAGEMENT-ACTIVITY-OWNERSHIP] Activity rows asserted the wrong home address — 14:05 AEST
**Symptom:** Will read two rows and could not tell which address to post mail to. One row
said "opened their OWN home report (70 Burleigh Street)" while its own Reach Via column
said 819 Legend Trail — the same row contradicting itself. Another picked 13/4 Yodelay
Street as "their OWN address" when the visitor had just viewed 7/4 Yodelay Street in the
same session.
**Root cause:** Three defects.
 1. `own_slugs_for()` pooled `probable_address_slugs` into the "own" set. That field is
    every address the person ever opened, not addresses they own — a contact who browsed
    four mini-sites had all four labelled "their OWN home report".
 2. The off-market owner-lookup heuristic (`offmarket_home`) was trusted unconditionally.
    It rests on the documented "94% of off-market visitors view exactly ONE address"
    pattern, so it is only valid when that condition actually holds — and it was never
    checked. `/building/<slug>` views also never write to crm_contacts, so the stored
    fields understate how many addresses a person looked at.
 3. No listing check. 819 Legend Trail and 70 Burleigh Street are BOTH currently
    `for_sale` — so that visitor is either a buyer researching listings or an owner
    already on the market with another agent. Mail was wrong in both readings.
**Fix:** Replaced the flat "own address" notion with an explicit evidence ladder —
T1 CONFIRMED (`home_confirmed.source == user_confirmed`), T2 SUBMITTED (`property_address`),
T3 LOOKUP (`offmarket_home` AND exactly one address across stored fields *and* the current
visit's page slugs), T4 AMBIGUOUS (everything else, incl. `probable_address`). Only T1/T2
count as "own" for labelling. Added a live `listing_status: for_sale` check that
disqualifies any address regardless of tier. New columns: "Safe to mail?", "Evidence",
"All addresses on file" (every candidate with its basis, so ambiguity is visible rather
than hidden behind one confident pick). Added `identity_key()` so duplicate crm_contacts
docs for one human collapse to a single row instead of two mail-outs. Added `--rebuild`.
Also fixed a parameter/local name collision (`seen`) that silently disabled the
multi-address downgrade.
**Files:** scripts/engagement_activity_to_sheet.py, logs/fix-history/2026-08-01.md
**Result:** of 21 visits in the last 7 days, 0 are confidently mailable, 4 are "maybe"
(inferred single-address lookups), 17 are "no". The previous version implied all 21 were
actionable addresses.
**Open:** `819 Legend Trail, Robina, QLD 4213` in crm_contacts vs `QLD 4226` in
Gold_Coast — stored postcode disagrees with the property record; mail would misroute.
Needs an address-normalisation pass against Gold_Coast before any mail-out runs.
**Recurrence:** First

## [ACTIVITY-SELLABILITY-GUARD] PropRadar sale check before any address is marketable — 15:20 AEST
**Symptom:** The Activity tab could recommend posting marketing to an address we cannot
sell — one already listed with another agent, or leased.
**Root cause:** The only sale check was `Gold_Coast.listing_status == "for_sale"`, which
covers three suburbs, has no lease state at all, and can be stale.
**Fix:** New `scripts/propradar/market_status.py` — cached (7d,
`system_monitor.propradar_market_status`) PropRadar lookup per address, plus
`verdict()` returning a plain-English mailability call. Wired into
`engagement_activity_to_sheet.py`, which now blocks on EITHER PropRadar `on_market` OR
our own `for_sale` (neither source is complete alone). `days_on_market >= 75` is flagged
as the Form 6 window and routed to the listing-expiry process rather than mailed.
PropRadar's canonical address also corrects our stored postcodes (819 Legend Trail was
recorded QLD 4213, PropRadar returns 4226).
**Findings that shaped the implementation:**
 1. **PropRadar has NO lease/rental listing data.** `/properties/{id}.rental` is an AVM
    estimate (`confidence: "low"`); `/suburbs/.../listings?listing_type=rent` silently
    IGNORES the param and returns sale listings; `/suburbs/.../rentals` 404s. We have no
    rent source anywhere else either. Lease status is therefore reported as explicitly
    UNKNOWN on every row, never assumed clear.
 2. **`/properties/search` is a LISTING+SOLD index, not a cadastral database.** Measured
    over 180 addresses: 18 found, 17 of those on-market; a real address that sold Mar 2026
    returns found=True/on_market=False; ordinary never-listed houses return found=False.
    So found=False reads as "no current listing". The first cut treated it as
    "unverified" and erred closed, blocking 162 of 180 addresses for the wrong reason.
 3. **`/properties/search` REQUIRES a postcode** (400 invalid_postcode without one) —
    added a suburb→postcode map since slug-derived addresses carry no postcode.
 4. **Slug leading numbers are LOT numbers, not unit numbers.**
    `13-4-yodelay-street-varsity-lakes` is cadastral LOT 13, PLAN GTP4152, STREET_NO_1 4 —
    so "13/4 Yodelay Street" is a guess. `cadastral_address()` now rebuilds addresses from
    the QLD cadastral fields and attaches an explicit unit-number caveat.
**Files:** scripts/propradar/market_status.py (new), scripts/engagement_activity_to_sheet.py
**Result (21 visits, last 7 days):** 8 blocked as currently for sale, 5 blocked as
Form-6-window listings (routed to listing-expiry), 4 blocked on ambiguous ownership,
4 "maybe" (sale-clear, lease unknown). Zero confidently mailable.
**Open:** no lease/rent data source exists — needs a Domain rent-listing scrape before any
mail-out can honestly clear the second disqualifier.
**Recurrence:** First

## [LEASE-GUARD-ONTHEHOUSE] For-lease listings now block marketing — 16:40 AEST
**Symptom:** The sellability guard could only check "is it for sale". Will's second rule —
we can't sell a home the owner is leasing — was unenforceable because no lease data existed
anywhere in the stack.
**Root cause:** PropRadar has no lease data (verified: `.rental` is an AVM estimate,
`listings?listing_type=rent` silently ignores the param, `/rentals` 404s) and
`Gold_Coast.listing_status` only carries for_sale / under_contract / sold / withdrawn.
**Fix:** New `scripts/rental_listings_sync.py` — pulls per-suburb rental indexes from
onthehouse.com.au into `system_monitor.rental_listings` (cron 23:30, before the 23:57
Activity job). Chosen over Domain because it fetches DIRECTLY from this VM (no Bright Data
proxy cost — Domain is Akamai-blocked from our IP), serves a per-SUBURB index rather than
per-address, and embeds structured JSON with real address components, listed date and
managing agency. `market_status.verdict()` now blocks on for-sale OR for-lease.
**Traps handled:**
  - Index pages include SURROUNDING suburbs (querying Burleigh Waters returned 157 records,
    only 11 actually Burleigh Waters). Records are filed under their OWN suburb, and the
    paging stop-rule keys off growth in the TARGET suburb — the naive "any new record"
    rule paged for ~15 min per run.
  - Reconciliation is deferred until all fetches complete, else a listing first seen via a
    neighbouring suburb's page gets deactivated by its own suburb's pass.
  - A failed fetch returns None (not []) and never deactivates — an empty scrape must never
    read as "no rentals here".
  - Join key drops the street TYPE entirely (PL/PLACE, CCT/CIRCUIT); verified 5/5 on real
    pairs including "12-14" number ranges.
**Files:** scripts/rental_listings_sync.py (new), scripts/propradar/market_status.py,
scripts/engagement_activity_to_sheet.py, scripts/ONTHEHOUSE_SCRAPING.md (new), crontab
**Result:** 348 active rentals across 20+ suburbs on first run. Activity guard now reports
16 addresses for sale, 2 for lease. Also removed a stale hardcoded "LEASE STATUS UNKNOWN"
caveat that was still being appended after the lease source went live.
**Recurrence:** First

## [ADDRESS-NORMALISATION] Canonical postable addresses + conflict detection — 17:35 AEST
**Symptom:** Stored contact addresses disagreed with the property record, and off-market
addresses were rebuilt from slugs by guesswork. Either could put mail in a wrong letterbox.
**Root cause:** No canonical address layer. `crm_contacts` stores whatever each capture path
wrote; off-market addresses were reconstructed from a slug with the unit number inferred.
**Fix:** New `scripts/normalize_addresses.py` (cron 23:40, between the rental sync and the
Activity job). Resolves every address a contact references against the QLD cadastral
`complete_address` already on `Gold_Coast` docs — present on ~98% of records and already
correct for unit number, street type and postcode. PropRadar's canonical address is a
fallback, cached-only (never spends API calls). Output goes to
`system_monitor.address_resolution`, NOT onto `crm_contacts` — `crm_sync.py` rebuilds
contacts hourly with `replace_one` and only carries forward an allow-list, so anything
written onto a contact by another process is wiped within the hour.
**Findings:**
  - `UNIT_NUMBER` exists on cadastral records — the earlier "lot vs unit" caveat was
    over-cautious. `13-4-yodelay-street` really is 13/4 (LOT 13 happens to equal UNIT 13),
    and `complete_address` = "13/4 YODELAY STREET VARSITY LAKES QLD 4227" confirms it.
  - 194/196 addresses resolved from cadastral, 1 from PropRadar, 1 unresolved.
  - **2 genuine postcode conflicts**: 819 Legend Trail stored QLD 4213 vs cadastral 4226;
    "27 Andromeda Parade Robina 5814" (5814 isn't a QLD postcode at all).
  - First cut reported 4 conflicts — 3 were FALSE: `\b\d{4}\b` was reading 4-digit UNIT
    numbers ("1604/3 Main Street", "4210/61 Investigator Drive") as postcodes. Postcode
    detection now requires a state token or end-of-string, excluding "1604/3" forms.
  - A postcode conflict now CORRECTS the address rather than blocking it — we hold the
    right answer. Only a wholly unverifiable address blocks.
**Also fixed:** `client.close()` sat inside the `with job_run(...)` block, so the heartbeat
write ran against a closed client. Moved to a `finally` so it closes once, on every path.
**Files:** scripts/normalize_addresses.py (new), scripts/engagement_activity_to_sheet.py, crontab
**Recurrence:** First

## [RENTAL-STALE-LEAK] Rentals in un-queried suburbs never expired — 17:35 AEST
**Symptom:** Second rental sync run showed active 348 -> 267 with **0 ended**.
**Root cause:** Each onthehouse index also returns SURROUNDING suburbs, so we ingest
listings in suburbs we never query directly (Mermaid Beach, Broadbeach, Currumbin Waters).
Deactivation is deliberately restricted to suburbs whose own index we fetched, so those
records could never be reconciled and would stay `active` forever — a stale rental record
silently blocks an address that is no longer leased.
**Fix:** Added a `STALE_AFTER_DAYS = 10` backstop — any active listing not re-seen in 10
days is retired with `ended_reason: "stale"`, regardless of suburb. Reported in the
heartbeat detail. Also tightened the paging stop-rule to key off growth in the TARGET
suburb (the old "any new record" rule paged for ~15 min/run; now ~2 min, same in-suburb
counts: 11 Burleigh Waters, 37 Robina).
**Files:** scripts/rental_listings_sync.py
**Recurrence:** First

## [OFFMARKET-MULTILOT-DUPES] Community-title schemes published as N duplicate standalone-house pages — 15:35 AEST
**Symptom:** 167 off-market slug bases were claimed by more than one property, producing 393 pages of which 226 were surplus near-identical duplicates on an indexed site. Worst case: "2 Scottsdale Drive, Robina" had 8 pages (`-2a52`, `-2a53`, `-2c7c`…), each asserting a standalone house.
**Root cause:** NOT the slug generator — it was working as designed. The cadastre stores one record per TITLED LOT, and a community-titles scheme records every lot under the same street address with no unit number (8 lots on survey plan SP135953 at that address). `url_slug` correctly disambiguates with a 4-hex `_id` suffix, so each lot became its own page. The existing standalone-house filters could not catch these: the address carries no unit number (so `UNIT_ADDR_RE` misses it) and the cadastral `property_type` is a bare "House" (so `NON_HOUSE_TYPES` misses it). 143 of the 167 groups sit on a single plan, i.e. plainly one strata scheme.
**Fix:** New nightly `scripts/flag_multilot_offmarket.py` sets `offmarket_multilot: true` on every eligible record whose slug base collides — the whole group, not just the surplus, because if we cannot uniquely identify which physical home an address refers to we should not publish an address page for it. Mongo cannot express "exclude groups of size > 1" in a find(), and three consumers must agree, so a document flag is the shared signal. Filter added to all three in lockstep: `indexed_query()` (deck builder), `getOffMarketUrls()` (generate-sitemap.mjs), and `meta()`'s `indexable` gate (off-market.$slug.tsx). Idempotent — clears the flag when a collision disappears, so a cadastral correction heals itself. Existing decks were deliberately NOT deleted: with the sitemap exclusion and noindex in place the duplicate content is resolved either way, and deleting only swaps a noindexed deck page for an equally-duplicate noindexed classic page.
**Verification:** flagged 393 records across 4 suburbs; build set fell 14,653 → 14,260 (exactly −393); both `2-scottsdale-drive-robina` and `-2a52` now excluded; coverage gap 0; vite build passes; 4 tsc errors in the route are pre-existing (`process` typing, line 309/340-342) against a 267-error repo baseline.
**Files:** scripts/flag_multilot_offmarket.py (new), 15_Off-Market/Page_Redesign_V2/offmarket_discovery_nightly.py, /home/fields/Feilds_Website/01_Website/scripts/generate-sitemap.mjs, /home/fields/Feilds_Website/01_Website/src/routes/off-market.$slug.tsx, crontab (00:10, ahead of the 00:20 deck build)
**Recurrence:** First

## [OFFMARKET-NERANG-FREEZE] Nerang pulled back to concentrate on the southern core — 15:35 AEST
**Symptom:** Not a failure — a scope decision (Will). Nerang had 3,399 decks built against a released sitemap slice of 1,000, and the release widener was set to keep growing it.
**Root cause:** n/a. The builder deliberately derives its suburb list from the KEYS of the `offmarket_sitemap_release` config doc and builds the WHOLE suburb ahead of release, so simply removing the key would have stopped builds AND dropped the 1,000 already-indexed URLs out of the sitemap, orphaning pages Google already holds.
**Fix:** Added a `frozen: [...]` concept to the release config, distinct from `limits`. A frozen suburb is no longer built and its release can never widen, but `limits` is left in place on purpose so the already-released slice stays in the sitemap and keeps resolving to a real deck. Because the builder no longer covers the whole suburb, "builder ⊇ sitemap" would stop holding by construction — so the coverage assertion now watches `build set ∪ frozen released slice` via a new `frozen_released_slugs()` that mirrors the sitemap's `sort({_id:1}).limit(N)` exactly. Freezing must never become blindness: a frozen suburb losing a deck is the [OFFMARKET-DECK-EXPANSION-DRIFT] failure from earlier today. `offmarket_sitemap_release.py` now refuses to widen a frozen suburb even if its (currently paused) cron is re-enabled. Reversible: drop the name from `frozen`.
**Verification:** build set = core 3 only (14,260); frozen watch = nerang 1,000; watch ⊇ build = True; 0 missing decks; widener run returns "nerang is FROZEN — release held at 1000" and leaves `limits` untouched.
**Files:** 15_Off-Market/Page_Redesign_V2/offmarket_discovery_nightly.py, 15_Off-Market/Reinforcement_Learning/offmarket_sitemap_release.py, Gold_Coast.offmarket_sitemap_release config doc
**Recurrence:** First

## [ONTHEHOUSE-PARALLEL-INGEST] Second data source live — Domain alone saw 71-75% of the market — 17:10 AEST
**Symptom:** Not a crash — a measured blind spot. Domain is our only live listings/sold source and it is incomplete. Six finished seller reports sat in `under_review` for homes listed with other agents (41 Olympus Dr Robina — Harcourts; 157 Christine Ave; 4/44 Frascott Ave; 46 Mornington Tce; 819 Legend Trail; 70 Burleigh St), and 20 addresses in `offmarket_discovery` were actively for sale with none of our sources knowing.
**Root cause:** Single-source coverage. Measured 2026-08-01, core three suburbs, houses only, like-for-like: for-sale Domain 111 / onthehouse 120 / matched 74 (Domain = 71% of union); sold 12-month Domain 508 / onthehouse 619 / matched 449 (Domain = 75% of union). Where they overlap they agree almost perfectly — 539/554 sale prices identical to the dollar, property type 769/769 — so the ~25-29% each misses is genuine coverage, not a join artefact. onthehouse carries Valuer General (`saleSource: "VG"`) settlement records, which is why it reaches sales Domain never had.
**Fix:** New `scripts/onthehouse/` package (client/matching/suburbs) plus three jobs, all houses-only, core three suburbs, direct from this VM with no Bright Data. `onthehouse_listings_sync.py` (nightly 23:35, 17 pages/~50s) → `system_monitor.onthehouse_listings`. `onthehouse_sold_sync.py` (shallow Mon-Sat 23:40, deep Sunday) → `onthehouse_sold`, a 12-MONTH OVERLAY that never overwrites our deeper history. `onthehouse_reconcile.py` (23:55) → coverage board + `onthehouse_conflicts`, which QUARANTINES price disagreements rather than merging (the 3% is the signal; auto-merge would destroy it — 2 caught, both ~1% with contract-vs-settlement date differences). Guard wired in three places: `market_status.verdict()` takes onthehouse as a third sale signal, `engagement_activity_to_sheet` populates it, and `build_property_report.resolve_one` now asks onthehouse before spending a build. Nothing is written to Gold_Coast — Domain stays the system of record for enrichment (floor plans, images, descriptions, withdrawn state, price history, none of which onthehouse has).
**Design invariants:** (1) A fetch failure returns None, never [] — we only expire listings in suburbs we actually fetched, and reconciliation is deferred because an index also returns neighbouring suburbs. (2) ABSENCE IS NOT EVIDENCE — 28% of live Domain listings are missing from onthehouse, so a miss is never proof a home is unlisted; only a hit is decisive. (3) 403/429 raises `Blocked` and aborts with an error heartbeat rather than degrading to "no results". (4) `salePrice: 0` means withheld (24% of sold houses) and is stored as None with `price_withheld`, never as $0. (5) Sold `_id` is address+sale-date so a home selling twice in the window doesn't overwrite itself.
**Address key:** the whole integration risk, isolated in `onthehouse/matching.py`. Two bugs found and fixed during validation — `\b4\d{3}\b` was stripping high-rise UNIT numbers as postcodes (4204/12-14 Executive Dr keyed as 12|14; 4103/61, 4507/61 and 4708/61 Investigator Dr all collapsed onto one key), and a blanket `&.*` cut ate the street name. Also: `suburb=` must be passed from the collection, because many for-sale docs store `street_address` with no suburb — omitting it silently cut a core-suburb join from 111 matches to 4. Both sides of every join now use one function; the fixed key found 9 MORE sold matches than the ad-hoc one used during measurement.
**AVM:** their `guesstimate` is stored from day one but consumed by nothing. Scoring it against past sales gives 4.7% median error, but `calculationDate` post-dates most of those sales so the test is contaminated, and their own confidence labels don't rank (High n=11 at 5.6% is worse than Medium-High at 3.2%). A clean test needs values captured before sale — possible in ~3 months at zero extra cost.
**Verification:** listings sync 140 active houses (120 in-suburb + 20 free spillover), 0 failures; sold deep backfill 647 houses, 126 price-withheld; reconcile reproduces the measured coverage exactly; all six previously-exposed addresses now BLOCKED with correct agency and listing date; control address correctly returns no listing; all four new jobs registering `job_run` heartbeats.
**Scope:** core three only (Will). A 40-suburb southern+central sweep was measured first — 683 requests / 1.08 GB / 16.9 min / zero blocks — and deliberately pulled back; expansion is a later decision.
**Files:** scripts/onthehouse/{__init__,client,matching,suburbs}.py (new), scripts/onthehouse_{listings_sync,sold_sync,reconcile}.py (new), scripts/propradar/market_status.py, scripts/engagement_activity_to_sheet.py, scripts/property_reports/build_property_report.py, crontab
**Recurrence:** First

## [PROPRADAR-MEDIAN-ANCHOR-WRONG] Suburb medians rebased onto a different dwelling population — Burleigh Waters published 12.6% low — 17:07 AEST
**Symptom:** `/market-intelligence/Burleigh-Waters` published a 12-month median of $1,710,000. realestate.com.au published $1,910,000 on 195 house sales over the same window and the same methodology. Robina and Varsity Lakes were also low (−2.8%, −4.4%).
**Root cause:** `scripts/propradar/recalibrate_charts.py` hard-set `rolling_12m_median_price` to PropRadar's published median and scaled the whole `indexed_series` by a factor derived from it. Two compounding errors. (1) The anchor at `:53` was `sum(raws[-4:]) / 4.0` — a MEAN OF FOUR QUARTERLY MEDIANS standing in for a 12-month median; different statistic even on identical data (BW: $1,882,500 vs true $1,905,500). (2) PropRadar's BW house median measures a different population: it reports 240 house sales against REA's 195 at a 10% lower median, and its BW `unit_price` is null despite 75 recorded unit sales — its house/unit split for that suburb is broken, so attached stock drags the median down. Factors: Robina 0.9707, Varsity Lakes 0.9561, Burleigh Waters 0.9084 — BW the outlier, i.e. suburb-specific data fault, not a methodology gap. The workaround existed because `propradar_sold` only covers 2026-01-14→2026-07-16 (six months, 267 records), too shallow to compute a 12-month median from — so we could only anchor to their aggregate. `[ONTHEHOUSE-PARALLEL-INGEST]` (same day, 17:10) removed that constraint by supplying 647 house records across a full 12 months.
**Fix:** New `scripts/precompute_union_prices.py` computes medians from the Domain ∪ onthehouse union (joined on the integration's own `address_key`), house-only via a new `shared/dwelling_type.py`, with bootstrap 90% CIs. Staged to `precomputed_indexed_prices_staging`, promoted by field-level merge — MEDIANS AND CIs ONLY. `transaction_count`, all `*_raw` fields and `in_progress_quarter` are left untouched: `market-insights.mjs:677` does `salesVolume: q.transaction_count || 0`, so nulling pre-union counts would render years of history as a flat line at zero. Volume is a separate change with the frontend. Median anchoring in `recalibrate_charts.py` disabled behind `RECALIBRATE_MEDIAN = False` with a new `_volume_only()` path, so the monthly 05:30 run no longer reverts this; volume calibration is unchanged. `rolling_12m_yoy_pct` recomputed from our own rolling series (was PropRadar's `growth_1y_pct`).
**Result:** Burleigh Waters $1,710,000 → **$1,925,000** (90% CI $1,852,775–$2,000,000, ±3.9%, n=204) — within **0.8%** of REA's $1,910,000 on a sample within 4.6% of theirs. Robina $1,450,000 → $1,490,000 (±2.8%, n=333). Varsity Lakes $1,350,000 → $1,410,000 (±2.8%, n=145). YoY now 6.3 / 8.5 / 11.0% (was PropRadar's 10.9 / 12.2 / 15.5%).
**Two bugs caught in the new code before promotion:** the rolling window was ending on Q3 2026, an in-progress quarter — nine complete months plus a week of sales presented as a 12-month median; and a stale in-progress entry survived the first merge on the OLD basis with no CI, sitting ABOVE Q2 2026 for Robina, which would have rendered as the rolling line ticking up on an artefact. It also silently mis-set YoY by comparing Q3 2025 against Q2 2026.
**Verified:** live API returns 1925000 / 1490000 / 1410000 with matching YoY; `transaction_count` still 44 / 43 / 22 and `*_raw` intact; no mixed-basis entries remain in any rolling series; `recalibrate_charts.py --all --dry-run` reports MEDIAN SKIPPED and VOLUME unchanged.
**Files:** shared/dwelling_type.py (new), scripts/precompute_union_prices.py (new), scripts/propradar/recalibrate_charts.py, 10_Market_Report/working_notes/02_problem_register.md (new)
**Recurrence:** First

## [MARKET-METRICS-DWELLING-FILTER] House medians filtered on the type field alone; 14–50 sold records per suburb dropped silently — 17:07 AEST
**Symptom:** 2.8–9.9% of records inside each suburb's "House" median carried unit-style addresses (`1/10 Cotinga Crescent` $895,000, `6/302 Christine Avenue` $770,000). Separately, sold records with no `property_type` were excluded with no report — Varsity Lakes lost ~15% of its sold records invisibly.
**Root cause:** `precompute_indexed_price_data.py:212-213` filtered on `classified_property_type == 'House'` OR `scraped_data.features.property_type == 'House'` — the type FIELD only. `precompute_valuations.py:254-261` had already solved this correctly for comps, treating a unit-numbered address as the STRONGEST signal with the type field as fallback plus a `misclassified_dwelling` exclusion at `:3053`. The fix existed in our own codebase and was never carried across, so property-page valuations were protected while suburb medians were not.
**Fix:** `shared/dwelling_type.py` — one definition, address-first, with `unknown` as a distinct return value so callers must COUNT rather than silently drop. Falls through `classified_property_type` → `property_type` → `scraped_data.features.property_type` → `scraped_data_v2.property_type`.
**Note on over-flagging:** onthehouse independently types 12/12 of the matched flagged addresses as "House" too, so many are genuinely detached homes on strata/community title (common in QLD). The address is not proof of attachment — it is the best signal the dwelling is not a standard detached house on its own lot. Validation: excluding them moves the BW 12-month median TOWARD realestate.com.au, not away.
**Result:** recovers 105 / 24 / 21 sold records the old filter dropped, while cleanly separating attached (median ~$1,007,500) from detached (~$1,502,500). Median shift +1.9 / +1.1 / +0.8%. The `unknown` bucket proved harmless — 44 / 17 / 18 sold events across the three suburbs, cadastral noise not lost sales.
**Files:** shared/dwelling_type.py (new), scripts/precompute_union_prices.py (new)
**Recurrence:** First

## [MARKET-INSIGHTS-SUBURB-FALLBACK] Hyphenated suburb param silently returns the Gold Coast aggregate — 17:07 AEST
**Symptom:** `market-insights?suburb=Burleigh-Waters` and `?suburb=Varsity-Lakes` return IDENTICAL figures ($1,420,000 / $1,650,000 / 6.4%) belonging to neither suburb. `?suburb=burleigh_waters` and `?suburb=Burleigh%20Waters` both work correctly.
**Root cause:** the normaliser at `market-insights.mjs:37-45` does not expand `-` to `_`, so a hyphenated slug misses and `:196` falls back to `{_id:'gold_coast_average'}` — returning plausible, well-formed, wrong data with HTTP 200 and no warning. Found while verifying the median promotion; my own verification used the hyphenated form and nearly recorded a false regression.
**Impact:** unconfirmed on production surfaces — the page passes an underscored key, so this is a latent trap for any new caller using a URL slug (the route itself is `/market-intelligence/Burleigh-Waters`).
**Fix:** NOT YET APPLIED — logged for the next market-metrics change. The fallback should either normalise `-`→`_` before lookup, or return an explicit error rather than substituting the aggregate.
**Files:** (none yet) netlify/functions/market-insights.mjs
**Recurrence:** First

## [MEDIAN-CI-PUBLISHED] 90% confidence interval published under the suburb median — 17:40 AEST
**Symptom:** Not a fault — the CIs computed by `[PROPRADAR-MEDIAN-ANCHOR-WRONG]` were stored but never served, so the page showed a corrected median with no indication of its precision. Quarterly medians run ±5–9% (Burleigh Waters Q4 2025 at ±16.9%), which means no recent quarter-on-quarter move in any core suburb is distinguishable from noise — and nothing on the page said so.
**Fix:** `market-insights.mjs` now returns `median_price_12m_ci_low/high/margin_pct/sample_n` plus per-quarter `ciLow/ciHigh/ciMarginPct/reliable/basis` on `medianPriceHistory`, and CI fields on `rolling12mMedianSeries`. `db.server.ts` carries the same through SSR so the server render and the client fetch agree. `MedianPriceChart` renders the range under the Current (12m) tile behind a null guard, so suburbs not yet on the union basis render exactly as before. `SsrDataContext` type extended. New `.statRange` style — deliberately quiet, it is context for the figure above it, not a competing number.
**Why it matters commercially:** nobody in this market publishes a median with its confidence interval. For an audience where agents rank third-least-trusted profession in Australia, showing the range is a stronger trust signal than false precision — and it is the only thing that stops us narrating a 3% quarterly move as a trend.
**Verified:** live API returns CI 1,852,775–2,000,000 (n=204) for Burleigh Waters; page renders "Current (12m): $1925k · $1853k – $2000k"; 0 console errors, 0 network errors; `tsc -p tsconfig.app.json` clean on all touched files (bare `tsc` checks zero files — see [[website_verification_gates]]); vite build passes.
**Files:** netlify/functions/market-insights.mjs, src/components/MetricCharts/MedianPriceChart.tsx, src/components/MetricCharts/SsrDataContext.tsx, src/components/MetricCharts/MetricChart.module.css, src/lib/db.server.ts — pushed as ONE commit b6b1d84 via the git trees API (five separate contents PUTs would burn five Netlify deploys and can leave published_at:null)
**Recurrence:** First

## [MARKET-INSIGHTS-SUBURB-FALLBACK] Hyphenated suburb slug silently returned the Gold Coast aggregate — FIXED — 17:40 AEST
**Symptom:** `market-insights?suburb=Burleigh-Waters` and `?suburb=Varsity-Lakes` returned IDENTICAL figures ($1,420,000 / $1,650,000 / 6.4%) belonging to neither suburb, with HTTP 200 and no warning.
**Root cause:** `normalizeSuburbName` converted whitespace to `_` but then STRIPPED hyphens via `[^a-z0-9_]`, so "Burleigh-Waters" became "burleighwaters", missed the `_id` lookup, and fell through to `{_id:'gold_coast_average'}` at `:196`. The route is `/market-intelligence/:suburb` with a hyphenated slug, so any caller passing the URL segment straight through got plausible, well-formed, wrong data.
**Fix:** `.replace(/[-\s]+/g, '_')` before the strip. Superseded the earlier entry today which logged this as NOT YET APPLIED.
**Verified live before/after in consecutive polls 50s apart:** `Gold Coast Average | 1420000 | CI None` → `Burleigh Waters | 1925000 | CI 1852775-2000000 | n 204`.
**Files:** netlify/functions/market-insights.mjs (in commit b6b1d84)
**Recurrence:** Second entry today — first logged as unapplied, now fixed.

## [UNION-PRECOMPUTE-INPROGRESS-LEAK] In-progress quarter promoted into indexed_series became the published median — 17:36 AEST
**Symptom:** After promoting the union medians, the live FAQ and JSON-LD read "The median house price in Burleigh Waters is **$2,125,000 (Q3 2026)**" — a part-quarter with 14 sales, on 1 August.
**Root cause:** MY REGRESSION, introduced an hour earlier. `promote_medians()` appended union-discovered quarters the old Domain-only series lacked, including the current in-progress quarter. Both `db.server.ts:552` and `market-insights.mjs:213` take `indexed_series[length - 1]` as THE median house price and feed it to the FAQ text and structured data. The in-progress quarter has always belonged in the separate `in_progress_quarter` field, which is where those consumers expect it.
**Why the gates missed it:** `tsc` and `vite build` both passed — this was a data-shape assumption, not a type error. It was caught only by screenshotting the rendered page and reading the text, i.e. mandatory rule 4 step 4 doing exactly its job.
**Fix:** `promote_medians()` now skips `is_in_progress` quarters when appending new periods; the already-promoted Q3 2026 rows were stripped from all three suburbs and the precompute re-run to confirm it no longer re-adds them.
**Verified:** all three suburbs report `latest_quarter Q2 2026`; live API `median_price` = 1,855,550 / 1,480,000 / 1,415,000; `in_progress_quarter` still present and untouched; re-screenshot shows "The median house price in Burleigh Waters is $1,855,550 (Q2 2026)".
**Files:** scripts/precompute_union_prices.py
**Recurrence:** First

## [SCRAPER-MIGRATION-NOT-THE-CAUSE] curl_cffi migration cleared as a volume discontinuity; two real composition shifts found instead — 17:33 AEST
**Symptom:** Hypothesis under test — Domain house-sale counts drop sharply from Q1 2026, and the pipeline migrated Selenium→curl_cffi on 2026-03-13. If the scraper change cut capture, the historical volume series carries a technology discontinuity where we would be reading a market signal.
**Result: NOT SUPPORTED.** Splitting Domain sales by source path (property-timeline events vs sold-listing records) shows no step at the migration. Burleigh Waters sold-listing counts RISE straight through it (Q4 2025: 32 → Q1 2026: 36 → Q2 2026: 47). Robina and Varsity Lakes decline gradually, consistent with settlement lag rather than a step.
**What the test found instead — two composition shifts, neither a market event:** (1) ~Q4 2024 the sold-listing feed comes online (Robina and Burleigh Waters are ZERO before it). (2) ~Q4 2025 property timelines stop covering recent sales and their contribution roughly halves (Robina 48→21, Burleigh Waters 42→18, and flat-to-down since) — timelines only hold history up to whenever each property was last scraped. So historical volume is stitched from two sources whose MIX changes twice.
**Implication:** strengthens the decision not to publish absolute historical sale counts. Any long-run volume series crosses both shifts. Feeds the pending volume-truncation change.
**Files:** (analysis only — no code changed)
**Recurrence:** First

## [POSTHOG-QUERY-SILENT-EMPTY] Lead pipeline silently dropped every off-market lead on any PostHog error — 3 nights lost — 17:40 AEST
**Symptom:** Asked whether one visitor's session (distinct_id `019fbc32-008b-71ae-92c7-f1838667808a`, Sydney, 157 Dunlin Drive Burleigh Waters, 17:20 AEST) would reach the Live Leads Tracker. It passed every filter — but investigating showed the last THREE nightly runs (Jul 29/30/31) added **zero** Off-Market rows while the Jul 28 run added 18. 12 genuine off-market leads were sitting undelivered. `logs/live-leads-sheet.log` showed a PostHog 504/503 immediately before each of those runs; `logs/crm-sync.log` had **56** of the same errors.
**Root cause:** `crm_sync.posthog_query()` caught `HTTPError` and `return []`. Every caller reads an empty result as "no such data", so a transient PostHog 504 ("query hit max execution time") or 503 ("queries are a little too busy") silently deleted an entire lead channel from that night's worklist AND sheet, with no error, no alert, nothing on the health board. Two downstream callers made it worse by re-swallowing: `lead_intelligence.py` caught and skipped off-market views, `seller_intent.py` degraded to zero behavioral signals (every lead then scores `no_cross_signal` — indistinguishable from a genuinely cold lead). Reproduced live: the crm_sync pageviews query 504s on first attempt and **succeeds on retry**, so these were transient all along.
**Recurrence note:** this is the *same* failure mode fixed for Brain 2 on 2026-07-19 (`[BRAIN2-504]` → `hog_retry()`). That fix hardened `scripts/brain2/brain2_util.py` only; the sibling helper `crm_sync.posthog_query`, which the entire lead pipeline uses, was left as-is. Fixing one PostHog helper and not the other is what let this run for another 6 weeks.
**Fix:**
- `posthog_query()` now retries transient failures (408/425/429/5xx, timeouts, URLError, bad JSON) with 5s/20s/60s backoff, raises `PostHogQueryError` on exhaustion, and raises immediately on 4xx (a malformed query used to return `[]` too). Added an explicit `soft=True` opt-out for any caller where empty genuinely is acceptable — nothing uses it today.
- Removed the two swallow sites (`lead_intelligence.py` off-market query, `seller_intent.py` behavioral sweep) so a real outage fails the run loudly instead of writing story-less/lead-less data over good data.
- Data was delayed, never lost: the 180-day lookback + `live_leads_sheet_ledger` dedupe mean a recovered run backfills everything missed. Ran the chain — all 12 backlogged leads (Jul 29 → today, incl. 157 Dunlin Drive) are now on the sheet.
**Files:** scripts/crm_sync.py, scripts/samantha/lead_intelligence.py, scripts/samantha/seller_intent.py
**Recurrence:** Second (first: [BRAIN2-504] 2026-07-19, same 504-handling class, different helper)

## [LEAD-CHAIN-ORDERING] Lead pipeline ran out of dependency order — ~30h before a lead was fully actionable — 17:40 AEST
**Symptom:** A lead captured today hit the Live Leads Tracker at 23:55 with a **blank Situation** column, only entered `lead_worklist` at 02:00, and only got its Situation text on the NEXT night's 23:55 run — roughly 30 hours from capture to fully-enriched row.
**Root cause:** five scripts with a genuine dependency chain were five independent cron entries at hand-picked minutes, and two were ordered backwards relative to each other. The sheet's Situation column reads `lead_worklist.seller_intent.story`, but `live_leads_to_sheet` (23:55) ran *before* `lead_intelligence` + `seller_intent` (02:00) that produce it. Nothing enforced or even declared the ordering — the offsets were guesses.
**Fix:** new `scripts/nightly_lead_chain.py` runs the five in declared dependency order, once, at **00:15 AEST** — late enough that the full calendar day is captured and the 23:30–23:55 listing/property syncs the enrichment reads have finished: `crm_sync` → `lead_intelligence` → `seller_intent` → `live_leads_to_sheet` → `engagement_activity_to_sheet`. Each step declares what it `needs`; a failed step skips its dependents (rather than writing rows built on stale data), lets independents run, then the chain exits non-zero. Wrapped in `job_run("nightly_lead_chain", cadence_hours=24)` per Rule 7 with per-step status in `metrics`, plus a Telegram alert naming the failed step. Sequencing also removes self-inflicted PostHog contention from concurrent HogQL jobs. Old three cron entries commented out in place (crontab), not deleted.
**Verified:** ran end-to-end (41s, 5/5 ok); heartbeat present and self-registered; all 12 newly-inserted rows carry a populated Situation **on first insert** ("hot 2 — Sydney-based owner of 157 Dunlin Drive…"), vs the terse fallback on rows added the old way.
**Files:** scripts/nightly_lead_chain.py (new), scripts/samantha/run_lead_intel.sh (no longer the cron entrypoint), crontab
**Recurrence:** First

## [UNION-MEDIANS-NOT-DURABLE] Corrected medians would have silently reverted on 1 September — 17:58 AEST
**Symptom:** Not yet visible — a latent reversion. Today's corrected medians and CIs were promoted by a script that ran ONLY by hand. It was not in crontab and had no heartbeat.
**Root cause:** `precompute_indexed_price_data.py` (cron `0 5 1 * *`) does a FULL `replace_one({'_id': ...}, data, upsert=True)` on `precomputed_indexed_prices`. At 05:00 on 1 September it would have rebuilt the doc from Domain-only sales with the old type-field filter, wiping every corrected median, every CI field, `median_source`, `union_from`, `coverage` and `method` — reverting Burleigh Waters to ~$1,710,000 with nothing to indicate it had happened. Disabling the PropRadar median anchor earlier today stopped the thing that DISTORTS the medians but not the thing that REGENERATES them.
**Fix:** (1) `job_run("market_metrics_union_medians", cadence_hours=744)` wrapper added — this was a mandatory-rule-7 violation from the moment the script was written, and rule 7 exists for exactly this class. `main()` split into `main()`/`run(args, beat)` so the beat carries per-suburb medians, CI margins and sample sizes. (2) Cron installed at `40 5 1 * *` — deliberately AFTER `precompute_indexed_price_data.py` (:00) and `recalibrate_charts.py` (:30), so it is the last writer of the month.
**Second bug caught during install:** the first crontab line I wrote had no `cd` and used relative paths for `.env` and the script, unlike every other entry — it would have failed silently every month. Rewrote with absolute paths and tested the EXACT crontab string under `env -i` from a neutral cwd. (An intermediate `/bin/sh` test failed on `source: not found`, but the crontab sets `SHELL=/bin/bash`, so that was the wrong shell to test with — re-tested under bash: exit 0, log written, promotion performed.)
**Verified:** heartbeat `success` at 07:57 UTC, `self_registered=True`, cadence 744h, detail carries all three medians; data intact after the simulated cron run (1,490,000 / 1,925,000 / 1,410,000, series ending Q2 2026, `median_source=domain_union_onthehouse`).
**Files:** scripts/precompute_union_prices.py, crontab
**Recurrence:** First

## [POSTHOG-DEFAULT-LIMIT-100] 45% of off-market leads invisible — HogQL silently caps unlimited queries at 100 rows — 17:59 AEST
**Symptom:** After splitting the hotness display, 10 sheet rows refused to update to the new format. Their `lead_worklist` docs existed and their PostHog events existed, but `offmarket_rows()` was no longer emitting them, so the in-place Situation refresh could never reach them.
**Root cause:** **HogQL applies a default `LIMIT 100` when a query does not specify one.** No error, no marker on the response — a truncated result is indistinguishable from a complete one. The off-market view query had no LIMIT, so it saw **100 of 183** distinct viewers (verified: same query with `LIMIT 100000` returns 183). 83 genuine off-market leads — 45% — had never been eligible for the sheet or the worklist at all, and which 100 you get is arbitrary, so the set silently churned run to run. `crm_sync`'s property-views query was also returning exactly 100. Related to the known BRAIN2 HogQL LIMIT-100 gotcha; same trap, different helper.
**Fix:** `posthog_query()` now injects `LIMIT 50000` when the query has no trailing LIMIT of its own, and prints a loud WARNING whenever a result lands exactly on its limit (almost always truncation). The regex anchors to the end of the query, so a LIMIT inside a subquery — `... IN (SELECT ... LIMIT 10)` — correctly does not count. Fixes every current and future caller at once rather than query by query.
**Verified:** the query that returned 100 now returns 183; caller-supplied LIMITs still respected; subquery-LIMIT case confirmed not fooled. Re-ran the chain — **19 more previously-invisible off-market leads** added (Jul 27 → today). Combined with [POSTHOG-QUERY-SILENT-EMPTY], **31 real leads recovered today**.
**Honest note:** I first "diagnosed" these 10 rows as having zero PostHog events — because I had truncated the distinct_ids in my own debug print and then queried the truncated strings. Verify the input before trusting the finding.
**Files:** scripts/crm_sync.py
**Recurrence:** Second (BRAIN2 HogQL LIMIT-100 gotcha previously; never applied to `crm_sync.posthog_query`)

## [HOTNESS-SPLIT] "hot N" mixed behaviour with property status into one meaningless number — 17:59 AEST
**Symptom:** Will asked what the "2" in "hot 2" meant. It isn't a warmth grade — `hotness = behavioral_score + listing_bonus`, an unbounded tally. "hot 2" means literally one session + one page view (the floor), but reads like "slightly warm". 100 of 259 scored leads sat at exactly 2 and 180 were <= 2, on a scale running to 164, so the number carried almost no information on the off-market channel — and a lead with an expiring listing and ZERO engagement (+22) outranked anyone with real behaviour below 22.
**Root cause:** two unrelated inputs — what the person DID (sessions, valuations built, price alerts, address searches) vs what is happening to their PROPERTY (+22 expiring, +14 withdrawn, +12 stale, -6 freshly listed) — summed into one figure where they can't be told apart.
**Fix:** `seller_intent` now stores `listing_bonus` alongside the existing `behavioral_score`; new `hotness_label()` in `live_leads_to_sheet.py` renders `intent 45 · listing +22`, omitting a component that is zero. `hotness` is kept in the document for continuity but is no longer displayed anywhere on its own. Console output split to match. Docs scored before today have no `listing_bonus`, but `hotness - behavioral_score` recovers it exactly, so old rows render correctly too.
**Verified:** 9 render cases unit-checked incl. both back-compat paths; all **250** sheet rows with a Situation now show the split (139 intent-only, 29 listing-only, 9 both), zero "hot N" remaining. Immediately surfaced a lead the old display hid: 4/44 Frascott Avenue read "hot 164" (top of the scale) but is actually `intent 170 · listing -6` — freshly listed with a competitor, i.e. explicitly NOT a lead right now.
**Files:** scripts/samantha/seller_intent.py, scripts/live_leads_to_sheet.py
**Recurrence:** First

## [UNION-DEDUP-CONTRACT-SETTLEMENT] Union double-counted every sale recorded at both contract and settlement date — 18:20 AEST
**Symptom:** Union volume counts looked wrong. Two ad-hoc extractions of the same suburb disagreed in DIRECTION — Burleigh Waters Q4 2025→Q2 2026 read 52→41 one way and 47→53 the other.
**Root cause:** MY BUG in `precompute_union_prices.py`. The union deduped on `(address_key, EXACT date)`. Domain records a contract date and onthehouse a settlement date, so one transaction appears twice, 7-45 days apart, at an identical price — `|42|aruma|burleigh waters 2025-08-14 $1,925,000` and `2025-08-21 $1,925,000`. Burleigh Waters alone had 14 such pairs in a 12-month window. The onthehouse integration had already documented this drift when it quarantined price conflicts; I did not carry the lesson into the join.
**Fix:** `dedupe_sales()` — same address + price within 1% + dates within 120 days collapse to one sale, dated EARLIEST (contract-like; settlement lags by an arbitrary conveyancing period and would smear sales into the following quarter). Deduping is done across BOTH sources together, since the pair usually has one leg in each. Removed 267 / 266 / 234 duplicate events per suburb.
**Second issue fixed alongside:** the volume window started at `union_from` (365 days back), but onthehouse's sold index is a rolling window whose OLDEST quarter is always partial — its data starts 3 Aug 2025, so Q3 2025 had Domain for three months and onthehouse for two. Varsity Lakes read 18 against 45 the next quarter, which is coverage, not the market. New `first_full_quarter()` starts the volume series at the first quarter onthehouse covers end to end (Q4 2025). Counts need complete coverage in a way medians do not.
**Effect on medians: negligible, which is the point.** Burleigh Waters stayed $1,925,000 and Robina $1,490,000 despite ~20% of events being removed; only Varsity Lakes moved, $1,410,000→$1,400,000. A median is robust to duplicates — that robustness is exactly why the defect was invisible in the price series and obvious in the counts.
**Count now reconciles better against the external benchmark:** BW 167 priced + 32 price-withheld = ~199 against realestate.com.au's 195. The earlier n=204 was inflated by duplicates and landed near 195 by luck; the median agreement (0.8%) held through both, which is the validation that actually mattered.
**Union volume, first honest read (Q4 2025 / Q1 2026 / Q2 2026):** Robina 71 / 71 / 51 · Burleigh Waters 41 / 44 / 42 (FLAT) · Varsity Lakes 45 / 31 / 17. Published figures claim Robina −67% and Burleigh Waters −30% over the same span. Q2 2026 is still filling in as settlements register, so these are floors.
**Files:** scripts/precompute_union_prices.py
**Recurrence:** First

## [VOLUME-SERIES-CONTRADICTION] Two collections gave opposite volume directions for the same quarter — 18:20 AEST
**Symptom:** The sales-volume chart and the insights strip disagree on the same page. Burleigh Waters Q2 2026: chart 70 sales, strip 44. Raw, before calibration: chart 33→36→47 (RISING through 2026), indexed_series 37→26→26 (FALLING).
**Root cause:** `precomputed_market_charts.{suburb}_sales_volume` counts sold listings; `precomputed_indexed_prices.indexed_series.transaction_count` counts property-timeline sale events. Different populations, each then scaled by its own PropRadar factor against a different raw base (trailing-4q 129 vs 142 for BW). Predates today's work.
**Fix (partial — backend only):** `write_union_volume()` now writes a `union_timeline` + `union_from` + `union_basis` onto the sales-volume doc, union window only. Written as a SEPARATE field rather than overwriting `timeline`, because `market-insights.mjs:677` coerces a missing count to 0 and any consumer not yet updated must keep seeing the old shape rather than a cliff to zero.
**NOT YET DONE:** `market-narrative.mjs` does not expose `union_timeline` and `SalesVolumeChart` does not consume it, so the chart still renders the legacy series and the contradiction remains live. Next change.
**Files:** scripts/precompute_union_prices.py
**Recurrence:** First

## [SHEET-LISTED-ORANGE + POSTHOG-LINK-COL] Orange row when a lead lists with another agent; PostHog person link column — 18:13 AEST
**Request (Will):** (1) "if they have listed and that's the update then highlight that row in orange"; (2) put the PostHog person id in its own column, hyperlinked to that person's events page.
**Built:**
- **Orange row highlight.** Keyed on the `on_market_fresh` STATE, deliberately NOT on seller_intent's `just_listed` flag — `just_listed` is a one-run TRANSITION (not-listed last run → listed this run), so painting on it would flash the row orange for a single night and clear itself before anyone read the sheet. `on_market_fresh` means "listed <69 days ago, inside the competitor's ~90-day exclusive term — NOT a lead yet, don't approach now", and persists as long as that's true. The highlight therefore **clears itself** when the listing ages into `on_market_stale`/`on_market_expiring` — precisely when they become a prime lead again. Orange appearing = stop calling; orange disappearing = start calling.
- **Manual-edit safety:** only paints rows whose background is currently blank (never overwrites a colour Will chose), and only clears fills that match our exact orange (any other colour is left alone).
- **PostHog column (O).** `=HYPERLINK("https://us.posthog.com/project/348370/person/<distinct_id>", "<distinct_id>")` — opens that person's event feed. Visible text is the full distinct_id so it can be copied out of the sheet, not just clicked (this whole thread started from Will pasting one). FB Lead Ads have no on-site session → cell left blank rather than linking somewhere useless. Written in a SEPARATE `USER_ENTERED` pass because the main row insert is `RAW` — `RAW` would store the formula as literal text, but switching the whole row to `USER_ENTERED` would reinterpret unit addresses like "1/35 Thornleigh Crescent" as dates. Confining formula parsing to the one column that only ever holds our formula avoids both.
- **--rebuild path fixed:** `values().clear()` wipes values but NOT formatting, and a rebuild re-orders every row — stale orange would have landed on whichever lead now occupied that row number. Rebuild now strips all backgrounds first, then re-derives links + highlight from scratch.
**Verified:** 150 person links written (formulas stored as formulas, confirmed via `valueRenderOption=FORMULA`); 8 rows painted across all 15 columns, and every painted row independently shows `listing -6` in its Situation — the two mechanisms agree. Re-ran: "Nothing new" — idempotent, no repaint/re-link churn. Rebuild dry-run writes nothing.
**Files:** scripts/live_leads_to_sheet.py
**Recurrence:** First

## [BRAIN-VERIFIER-COVERAGE-INVALID] Brain 1 quote verifier was not a containment test — fidelity metric was measurement noise — 19:55 AEST
**Symptom:** A real Brain 1 run reported `49 quotes | 44 verified | 5 MISATTRIBUTED | 0 NOT_FOUND | 89.8% fidelity`, with confident verdicts like `cited u0202 -> actually u0001 (cov 1.0)`. This was taken as evidence that map-reduce sharding was detaching quotes from unit ids (the wrong ids were repeatedly u0202/u0449, read as "anchoring on salient ids"). A handoff doc was written proposing a shard-boundary fix built on that reading.

**Root cause:** `brain1_verify.coverage()` summed EVERY `SequenceMatcher` matching block, including 1- and 2-character ones. Over a long unit blob those scattered fragments reassemble almost any short needle, so the function was not measuring containment at all. Measured: `"substantial = authority"` (21 normalized chars) scored ≥0.90 against **5,621 of 9,525 units**; `"14-day taper"` against 2,549. Consequences in both directions:
- **False verifications inflated the headline number** — a quote could score ≥0.85 against its cited unit by noise and be marked VERIFIED. The real figure was lower than reported.
- **NOT_FOUND could never fire** — the true-source scan always found *something* at cov 1.0, so genuinely fabricated quotes were reported as misattributions to an arbitrary unit. The run reported 0 fabrications; there were 4.
- **The `-> actually uXXXX` verdicts were arbitrary** — whichever of thousands of spurious matches was hit first. The u0202/u0449 pattern that the shard hypothesis rested on was an artifact of the verifier grabbing whatever id sat on the same line, not a synthesis behaviour.

Two further defects found in the same pass:
- `fix_citations()` (already present, never wired in) anchored to "the first unit id within 60 chars AFTER the quote", `count=1`, over the whole document. On a comparison-table row holding two quotes and two ids it rewrote the wrong id, breaking a correct attribution; where the id preceded the quote it silently matched nothing. Wired in as-is it reported "corrected 4" while fidelity did not move at all (90.7% → 90.7%) — a false success signal.
- The greedy `re.search(r"\[.*\]", out, re.S)` in `brain1_deep.decompose()`/`_judge_chunk()` spans first `[` to LAST `]`, so any trailing prose or a second array makes the span invalid JSON. Reproduced the exact production error `[judge] FAIL-OPEN (kept all 18): Extra data: line 7 column 1 (char 47)`. The same defect class existed with a different hand-rolled implementation in `brain3_annotate.extract_json_array` and `brain2/ad_annotate.extract_json_object` (outermost-span slice).

**Fix:**
- `coverage()` rewritten: exact containment short-circuits to 1.0; fuzzy matching counts only blocks ≥8 chars. A real quote with a transcription slip still scores ~1.0; a short needle assembled from noise scores 0.0. `"substantial = authority"` now matches 0 units, `"beautiful glossy booklet"` exactly 1 (u0645 — the genuine source).
- `verify_text()` now returns a FOURTH verdict, UNVERIFIABLE: a span under 30 normalized chars that appears verbatim nowhere. These are the synthesis's own coined labels in quote marks ("substantial = authority", "14-day taper"), not citations. Reported separately with an advisory, excluded from the fidelity denominator so the metric no longer depends on the model's punctuation habits.
- True-source claims now name every holding unit and mark the verdict ambiguous when several qualify, instead of asserting one.
- `fix_citations()` rewritten: rewrites only UNAMBIGUOUS bindings (line carries exactly one quoted span and exactly one flagged id) and only when exactly one unit holds the quote; re-verifies and **rolls back if the verified count did not increase**, so the repair count can never overstate what was achieved.
- New `scripts/samantha/brain_json.py`: direct parse → fenced block → balanced-bracket scan (string/escape aware, so it cannot over-capture), plus one retry with an explicit format reminder. Wired into all four hand-rolled sites. 11 regression cases, all passing, including the exact production failure.
- New shared `brain1_verify.audit()` — id-shape guard + id membership + quote verification + repair, one contract. Wired into `brain1_deep.py` (replacing its inline block, now repairing BEFORE output) and into `brain1_query.py`, which previously had **no citation verification at all**. The id-shape guard derives valid shapes from the package and flags `u5850349667` (10 digits where real ids have 4) as MALFORMED rather than merely absent.
- `entities` now carried through `brain1_graph.py` into the unit doc, into the retrieval blob (`brain1_query.score_units`, with a rank bonus on entity hits) and into `compact()` so the LLM sees them. Extracted at annotate time since the corpus was built, they had never reached the package.
- Facets cached per (package, n, question) with `--refresh-facets`, so a repeat query is reproducible.
- Source concentration measured and printed; when one library exceeds 50% of carried evidence the synthesis prompt now requires the brief to say so.

**Measured result** (same brief, `10_Market_Report/research/brain1_printed_report_evidence.md`):
| | reported before | actual |
|---|---|---|
| fidelity | 89.8% | **84.4%** (86.7% after repair) |
| misattributed | 5 | 3 → 2 after repair |
| fabricated (NOT_FOUND) | 0 | **4** |
| unverifiable coined labels | not a category | 9 |

Name-anchored retrieval, top-45 units genuinely about the person: Tom Panos **6 → 32**, Alex Jordan (top-150) **9 → 17 of 25**.

**Not done, deliberately:** the map-reduce shard-boundary experiment the handoff put second in priority. Its premise was the 5-misattribution pattern, which is now known to be mostly measurement error; the genuine misattributions are all one confusion (u0449 vs u0645 — two units about the same quarterly booklet), which is too thin to design a sharding change on. `--save-relevant/--load-relevant/--force-path/--shard-n` were added so the experiment can be run properly later, holding the evidence constant across both synthesis paths, once the corrected verifier has produced a few trustworthy runs.

**Files:** scripts/samantha/brain_json.py (new), scripts/samantha/brain1_verify.py, scripts/samantha/brain1_deep.py, scripts/samantha/brain1_query.py, scripts/samantha/brain1_graph.py, scripts/samantha/brain3_annotate.py, scripts/brain2/ad_annotate.py, scripts/samantha/BRAIN_FIXES_HANDOFF.md
**Recurrence:** First

## [BRAIN-VERIFY-APOSTROPHE-FALSE-NEGATIVE] Fixed coverage() reports genuine verbatim quotes as NOT_FOUND when they contain an apostrophe — 20:20 AEST
**Symptom:** Re-verifying 11 cadence claims used in a live strategy discussion, 10 verified and one returned NOT_FOUND: `"There's a picture of their house on the covering letter"`. The quote is present **verbatim** in u0048 — confirmed by plain substring search.
**Measured:**
```
coverage("There's a picture of their house on the covering letter", u0048_quote) = 0.891   <- below the 0.90 gate
coverage("a picture of their house on the covering letter",        u0048_quote) = 1.000   <- same string, apostrophe removed
```
Both sides use the SAME straight apostrophe, so this is not a smart-quote mismatch. The rewritten `coverage()` is documented to short-circuit exact containment to 1.0; for a true substring containing an apostrophe that short-circuit does not fire, and the fuzzy path (blocks ≥8 chars only) then scores it 0.891 because the apostrophe splits the span into `There` + `s a picture…`, costing enough matched length to drop under the gate.
**Why this matters more than it looks:** NOT_FOUND only started firing in the same change that introduced this (`[BRAIN-VERIFIER-COVERAGE-INVALID]`, 19:55). That change reported **4 fabricated quotes** as its headline remaining defect, and open item §4.2 of the handoff proposes attacking the map-reduce map-step contract because of them. **Any of those 4 containing an apostrophe is a false positive.** English coaching-transcript quotes are dense with contractions ("they're", "it's", "you'll", "don't"), so the false-negative rate on this corpus will be high. Chasing a map-reduce fidelity defect that is partly a normalisation bug would be the second time in one day a fix was designed on top of a broken measurement.
**Fix:** NOT YET APPLIED — flagged for the brain session. Normalise apostrophes/contractions (and ideally all non-alphanumerics) consistently on BOTH sides before the containment test, then re-run the 4 NOT_FOUND quotes before treating any as fabrications. Re-verify the 11-claim panel afterwards as a regression check.
**Verification panel (useful as a fixture — 10/11 verified under the current build):** 5,000 letterbox/week u0199 · old-stuff-again u1002/1003/1009 · 5th-to-12th contact u1229 · 14-day taper u1468/u2267 · dinner parties u0695 · eight-page glossy booklet u0645 · 50% from database u0370/u0645 · secret weapon u0048 · no competition u0048 · 60%-before-presentation u0293/0299/0302 · **picture-of-their-house u0048 (FALSE NOT_FOUND)**.
**Files:** scripts/samantha/brain1_verify.py (not yet changed), scripts/samantha/BRAIN_FIXES_HANDOFF.md
**Recurrence:** Second measurement-artifact defect in the verifier today (see [BRAIN-VERIFIER-COVERAGE-INVALID]).

## [BRAIN-VERIFIER-COVERAGE-INVALID] Follow-up: end-to-end re-run of the §0 query — 20:40 AEST
**What ran:** the original handoff's exact query, full pipeline, all fixes live, entities-enabled package.

**Result — the plumbing fixes all held:**
- **No `[judge] FAIL-OPEN` line at all.** The original run lost a whole 18-unit batch to `Extra data: line 7 column 1`; the balanced-bracket parser eliminated it.
- Facets served from cache on the repeat → the run is now reproducible.
- Source concentration fired correctly: `RealEstate_Gym = 160/245 (65%) ⚠ >50%`.
- Repair corrected the single misattribution (`i0560085714` → `k02964`), **1 → 0 MISATTRIBUTED**, and the corrected text is what was written to `--out` (verified by re-auditing the saved file).
- Malformed-id guard caught the invented `i0048` (4 digits where `i` ids have 10).

**Result — the real defect is now visible and it is NOT what the handoff predicted:**
`31 quoted spans | 30 attributable | 22 verified | 0 MISATTRIBUTED | 8 NOT_FOUND | 73.3% fidelity`

Misattribution — the thing the whole shard-boundary hypothesis was built on — is **zero** after repair. The actual problem is **8 fabricated quotes**: the synthesis puts its own paraphrase and section labels inside quotation marks next to a unit id ("frequency outweighs content substance", "platform-as-credential... un-copyable", "quarterly NYC market report as lead magnet"). Checked individually against the corpus: best coverage 0.54–0.81, all well below the 0.90 bar — these are genuine paraphrases, not matching artifacts. The old verifier reported this class as **0** because its broken coverage always found a spurious cov-1.0 "true source".

**One refinement applied during this check:** `fragments()` now also splits on square-bracket editorial insertions. `"I do 50 [letterbox drops per week]"` quotes only "I do 50" — the bracketed words are the brief's own gloss and are not in the source, so matching against them guaranteed a false fabrication verdict. Correctly reclassified to UNVERIFIABLE (7 verbatim chars is too little to attribute), taking NOT_FOUND 9 → 8.

**Definition of done: NOT met, and that is the correct outcome.** The target was "100% quote fidelity, 0 invented ids". Actual: 73.3%, 1 invented id, 8 fabrications — and the brief is correctly stamped `⚠ NOT publication-ready`. The system is now measuring honestly instead of reporting 89.8% while masking 4 fabrications. Closing the remaining gap is a **synthesis-prompt** problem (stop using quotation marks for paraphrase), not a retrieval or sharding problem — that is the next session's highest-value target, and it is now measurable.

**Files:** scripts/samantha/brain1_verify.py, scripts/samantha/BRAIN_FIXES_HANDOFF.md
**Recurrence:** 2nd (same problem id, follow-up within the same day)

## [BRAIN-SYNTH-PARAPHRASE-IN-QUOTES] Quotation rule + systematic id remap → 100% fidelity — 21:35 AEST
**Symptom:** After the verifier was fixed, the surviving defect was visible for the first time: 8 of 30 attributable spans were NOT_FOUND — the synthesis putting its own paraphrase and section labels inside quotation marks next to a unit id ("frequency outweighs content substance", "platform-as-credential... un-copyable"). Fidelity 73.3%, brief stamped NOT publication-ready.

**Priority check first (both contenders dropped):**
- **Brain 2 label drift — not real.** 92/92 annotated ads conform on every enum (`primary_emotional_lever` 8 values, `hook_type` 9, `target_persona` 4). `message_theme` has 28 values across 92 ads, but it is specified as free text ("short lowercase phrase"), not an enum — working as designed. My earlier "genuine Brain 2 integrity gap" framing was too strong.
- **Zero-yield libraries — correctly irrelevant.** `KB:financial`/`KB:general`/`KB:project` are academic housing-economics papers (auction fever, time-on-market models, foreign investment, price distributions). Zero relevant units for a question about practitioner marketing collateral is the right answer, not unreachability.

**Root cause:** `rules_for()` said "include DIRECT VERBATIM QUOTES throughout" but never constrained what quotation marks may enclose. Nothing told the model that quote marks are a claim about provenance.

**Fix 1 — QUOTATION RULE in `rules_for()`.** Quote marks mean exactly one thing: text copied character-for-character from a unit's `quotes` field. Never around the model's own labels, coined shorthand, paraphrase, or scare-quoted terms — use plain text or bold. If the gist is remembered but not the wording, write plain prose and cite the id. States that a paraphrase inside quote marks beside a unit id is a FALSE ATTRIBUTION, and that every quoted span is machine-checked post-generation, so "quote less and quote exactly".

**Fix 2 — systematic id remap in `fix_citations()`.** The re-run surfaced a new class: the brief cited `u2909` four times for McGrath Magazine material whose true source is `k02909` — the model dropped the "k0" prefix. Because `u2909` is itself a real unit (a Sell It unit on rapport building), the id-membership check passed it; only quote verification caught it. Two of the four citations were in prose with **no quote at all** — structurally invisible to quote verification, so nothing would ever have caught them. Added a whole-document remap pass, strictly guarded: remap X→Y only when NO quote anywhere verifies against X, and every unambiguously-resolved quote citing X resolves to the same single Y.

**Fix 3 — question-echo class.** A brief restating the user's own words ("a LARGE substantial report vs. a SHORT eight-page quarterly review") is quoting the question, not claiming a source. Matched with the same contiguity-aware `coverage` at ≥0.85 rather than exact containment, because the restatement is near-verbatim but not identical (the brief wrote "vs." where the question said "against"). Guard-tested: the three genuine fabrications from the prior run score 0.00–0.22 against the question, nowhere near the threshold. Without this, models restate the question routinely enough that almost every brief would be stamped NOT publication-ready — a gate that always fires is a gate that gets ignored.

**Measured, same 245-unit evidence set throughout (`--load-relevant`, so only the prompt/repair changed):**
| | baseline | + quotation rule | + remap & echo |
|---|---|---|---|
| fidelity | 73.3% | 87.1% | **100.0%** |
| fabricated (NOT_FOUND) | 8 | 1 | **0** |
| misattributed | 0 | 3 | **0** |
| invented ids | 1 | 0 | **0** |
| publication_ready | false | false | **true** |

**Definition of done from the handoff: MET.** 100% quote fidelity, 0 invented ids, facets reproducible on re-run, name-anchored recall restored (Tom Panos top-45 6→32), `brain1_query.py` emitting a fidelity line. `brain2/ad_query.py` is exempt by inspection — deterministic MongoDB aggregation with no LLM and no citations. No new scheduled process was added, so rule 7 does not apply.

**Note on method:** the `--save-relevant`/`--load-relevant` scaffolding added earlier paid for itself immediately — each iteration re-synthesised the *same* judged evidence in ~6 min instead of re-paying ~20 min of retrieval and ~2,300 judge calls, and held the evidence constant so every fidelity change is attributable to the prompt/repair change alone.

**Files:** scripts/samantha/brain1_deep.py, scripts/samantha/brain1_verify.py, scripts/samantha/brain1_query.py
**Recurrence:** First
