## [STEP-107-MEMORY-MEASURED] Step 107 re-run clears the night's failure and settles the open OPS-013 question — 07:28 AEST
**Symptom:** Pipeline Processes showed "Step 107 (Database Audit & Validation) — Process timed out" and, from the same fault, "Nightly run — run failed" (`run_summary.json` for `2026-08-08T20-30-57`: `steps_completed: 25, steps_failed: 1`). Step 107 went 50-75 s on Aug 1-5, then 1921-1922 s / 3 attempts / timeout on Aug 6, 7 and 8. Two prior cycles had raised it (`WTA-OPS-013` + its 2026-08-08 addendum) but both left the decisive measurement unrun, the addendum concluding it was "a genuine hang" on an "e2-standard-2 (8 GB)" VM.
**Root cause:** `scripts/database_audit.py:123` is `properties = list(collection.find({}))` — every document, every field, no projection, across all 105 collections (`EXCLUDED_COLLECTIONS = []`, line 237). The audit reads exactly six small fields per document (`_id`, `address`, `suburb`, `listing_url`, `first_seen`, `last_updated`; lines 130-133, 165-167) and never touches `valuation_data`, which is ~79% of a `robina` document after the 2026-08-06 mass valuation rewrite. Measured this cycle on a quiet box: the unprojected scan peaks at **7,348,876 KB = 7.01 GB** and takes **150 s**; the same 400,193 documents with a 5-field projection peak at **104,548 KB = 102 MB** in **7.8 s** — **19× faster, 70× less memory**.
**Fix:** Tier 1 repair only — **re-ran the step**, no code changed. Confirmed idempotent first: the `insert_one`/`delete_one` move path (lines 426/429) is inside `fix_errors()`, reachable only via `--fix`, which the nightly command (`--export … --limit 100 --no-fail`) does not pass, so the production invocation is read-only. The re-run exited 0 in 2:29.94 and produced its full error export. **This is not a repair of the underlying defect** — the projection fix stays with Will as `WTA-OPS-013`, and the step will likely time out again tonight.
**Two corrections to the 2026-08-08 addendum, both material:** (1) it is *not* a hang — it completes; `audit_collection()` prints nothing per collection unless `--verbose` (lines 126-127), which the nightly does not pass, so silence for the entire runtime is expected output and the prior diagnostic was killed before it finished. (2) the VM is **e2-standard-4 / 16 GB** since 2026-08-01 (`free -g` → total 15), not 8 GB.
**Not proven, deliberately unasserted:** why it exceeds 600 s in the nightly window but takes 150 s idle. Memory pressure is the obvious candidate but `system_monitor.vm_metrics` retains only 61 rows ≈ 1 hour, so the 2026-08-08 22:50-23:22 AEST window is gone; no OOM kills in syslog/kern.log.
**Files:** none modified. Evidence: `/tmp/step107_rerun.log`, `/tmp/step107_projected.log`, `/tmp/dbaudit_samantha_071905.log`. Escalations: `16_General_Reinforcement_Learning/WILL_TO_ACTION.md` (OPS-013 addendum).
**Recurrence:** 3rd for step 107 in this class (raised 2026-08-07 as OPS-013, re-diagnosed 2026-08-08, measured 2026-08-09). Same "unprojected read of fat documents" family as the memory-driven lockups behind the 2026-08-01 VM upsize.

## [FIELD-NAME-GUESS-FALSE-ABSENCE] "No aerials exist" — a guessed field name reported as missing data — 08:30 AEST
**Symptom:** A query for `aerial_image_url` returned zero documents, and the zero was reported to Will as a fact about the database: no aerial imagery exists. **14,531 documents had one.** The field had simply never been called that — the data lives at `satellite_analysis.satellite_image_url` (plus `domain_hero_image_url`, `scraped_data.images[].url` and 22 other image paths in `Gold_Coast.robina` alone).
**Root cause — two layers, and the second is the one that matters:**
  1. **Behavioural:** a field name was invented from intuition rather than read from a document, and an empty result set was then treated as evidence about the data instead of evidence about the name. A zero result is only ever a statement about the question asked.
  2. **Structural — the safeguard could not have caught it.** `SCHEMA_SNAPSHOT.md` exists precisely to prevent this and instructs "read before writing ANY MongoDB query", but `generate_schema_snapshot.py` sampled each collection with `collection.find({}).limit(5)` — the **five oldest documents, not a random draw** — and walked only the top level, capping nested objects at 10 subkeys with no ellipsis and never expanding arrays of objects. In `Gold_Coast.robina` (12,092 docs: mostly cadastral stubs, a few hundred enriched listings) the first five documents are all one shape, so the file listed **75 fields where live listings carry 233 top-level keys and 2,523 total paths** — and the omission was biased toward exactly the enrichment fields anyone would ask about. It also recorded no fill counts, so a field present on 1 document and one present on all of them were indistinguishable. Following the existing rule to the letter would have produced the same wrong answer.
  3. **Vocabulary gap (why an index alone is insufficient):** even with a complete index, `grep -i aerial SCHEMA_PATHS.tsv` returns **1 irrelevant hit** — this schema says *satellite*. Searching the index for your own guessed word just confirms your own assumption.
**Fix — three layers:**
  1. **`shared/doc_shape.py` (new)** — one walker shared by the generator and the probe so they can never drift: random `$sample` instead of `.limit()`, full recursion through nested objects and arrays (`transactions[].price`), a fill count on every path, dynamic-key maps collapsed to `<dynamic>`, hard path ceiling.
  2. **`generate_schema_snapshot.py` (rewritten)** — now emits two artifacts. `SCHEMA_SNAPSHOT.md` (628KB, down from 969KB) is readable orientation: top-level fields with fill counts. `SCHEMA_PATHS.tsv` (5.3MB) is the complete greppable index — **67,066 paths across 335 collections**, one per line, all depths. Wrapped in `job_run("schema_snapshot", cadence_hours=24)` per Rule 7, with a **7b assertion**: raises if it documents 0 collections, or if path coverage drops below 60% of the previous run, rather than silently overwriting a good snapshot with a blind one. Runtime 37s.
  3. **`scripts/db_fields.py` (new)** — the probe. `--find TERM` searches every database and **expands the term into the vocabulary this schema actually uses** (aerial→satellite, photo→image, coords→georeference), so `--find aerial` surfaces the whole `satellite_analysis` subtree and prints "no path is actually named 'aerial'". `--check FIELD` makes a zero self-refuting: on zero fill it prints every related path that *does* exist in the same output and exits non-zero, so an absence and its rebuttal cannot be separated. `--grep`, `--query` (scope to `listing_status: for_sale`), `--min-fill`.
  4. **CLAUDE.md Rule 8** added to both copies — never infer absence from a guessed name; report a zero as a wrong name, not as missing data.
**Files:** `shared/doc_shape.py` (new), `scripts/db_fields.py` (new), `generate_schema_snapshot.py` (rewritten), `CLAUDE.md` + `/home/fields/CLAUDE.md` (Rule 8), `.gitignore` (ignore the two generated artifacts).
**Recurrence:** **5th in this class, 2nd sub-class.** Wrong/assumed field names: [VALUATION-FLOOR-001] (systemic `floor_area_sqm` mismatch), [VALUE-DRIVERS-SCHEMA] 2026-02-28 (NPUI read `pvd.interior`, which does not exist), [YOURHOME-PHOTO-SCHEMA] 2026-06-02 (resolver written against invented mock shape; its own lesson was *"verify resolvers against a real document"*), [BEDS-BATHS-BACKFILL]. Absence-of-evidence treated as evidence-of-absence: [PHOTO-ABSENCE-NEGATIVES] 2026-07-30, same epistemic error one layer up (vision reporting "no ensuite visible" as a defect). Every prior fix patched the individual caller. This is the first one to fix the *instrument* — and it found that the instrument was 97% blind (75 of 2,523 paths).

## [V4-PORT-ACTUALLY-COMPLETE] 14 of 14 sections; three wrong claims corrected — 08:25 AEST
**I reported the port complete at 7 of 14 sections.** Will checked the live page against the
prototype and found the aerial missing, the colours wrong and sections absent. All three were mine.
**1. "Zero aerials exist."** FALSE. I queried `aerial_image_url`, a field that does not exist, got 0,
and reported the absence as a fact — telling Will a batch render was a prerequisite. The real field
is `cadastral_photos_dir`: **14,531 documents have an aerial**, already served publicly from
blobs.fieldsestate.com.au. Verified by fetching one (1024x768, genuine top-down). Now wired.
⚠ Still true: the PROTOTYPE's aerial has the title boundary drawn on; that needs a per-property
cadastre polygon (present on 16 docs), so the live hero shows the real roof without a boundary.
**2. Wrong palette.** I styled everything with --fields-birch/#e6ddd2 etc., taken from
Design/02_VISUAL_SYSTEM_PARKED.md — a direction that document says was PARKED and never executed.
None of those variables are defined anywhere on the live site either, so every rule fell back to a
hardcoded literal. The prototype's palette is --paper #F7F5F1, --accent #B4643F, --serif 'Iowan Old
Style'. Copied verbatim into v4/tokens.css and all 9 modules repointed.
**3. "Port complete" at half done.** Now 14 of 14: hero, which, answer, nearby, comps, reliable,
dispersion, different, now, buyer, timing, since, correct, plan, next.
**Data source that unlocked the last three:** `system_monitor.offmarket_discovery` — 26,297 docs
(the whole book) carrying competition/value_drivers/buyer cards. The V4 branch now reads it for those
three sections only, ignoring the deck's narrative arc.
**Verified live:** 14 sections, h1 present, aerial loading, tokens resolving (bg rgb(247,245,241)),
1,963 words, zero page errors.
**Pattern across all three:** each was a confident claim from a search that did not match reality —
wrong field name, parked doc, half-finished checklist. Same shape as the phantom payment gate and the
tsc that checked nothing. **Verify the thing itself, not a proxy for it.**
**Files:** 01_Website/src/pages/OffMarketPage/v4/*, src/routes/off-market.$slug.tsx
**Recurrence:** 4th of the "searched for the wrong thing and reported the absence" class today.

## [CLAUDE-MD-DUAL-COPY-DRIFT] Two copies of the mandatory rules, silently diverging — 08:35 AEST
**Symptom:** `/home/fields/CLAUDE.md` and `/home/fields/Fields_Orchestrator/CLAUDE.md` were independent files, both auto-loaded into every session's context, and had already drifted: the parent copy still listed Facebook pixel `137811233253065` as an active passive pixel, which the orchestrator copy records as dropped from CAPI 2026-07-15 (token lacked permission, every call 400'd) and removed from the browser 2026-08-05. Two copies of rules described as "not optional" that can disagree is the same silent-divergence failure the rules themselves exist to prevent — and an edit to one is invisible from the other.
**Root cause:** The parent-directory CLAUDE.md governs all sibling projects under `/home/fields` (e.g. `Feilds_Website`, where the orchestrator copy never loads), so it must exist — but it was maintained by copying rather than by reference, with no mechanism to detect drift.
**Fix:** Verified by diff that the ONLY difference was the stale pixel line and that the orchestrator copy was strictly more current (so nothing was lost), backed up the parent, then replaced it with a relative symlink → `Fields_Orchestrator/CLAUDE.md`. One file, one source of truth, still resolving for every sibling project. Confirmed no writer exists that could silently re-fork it: all four in-repo references and all four cron references are reads or comments — an atomic temp+rename write would have replaced the symlink with a regular file and restarted the drift unnoticed.
**Files:** `/home/fields/CLAUDE.md` (now a symlink; outside any repo, so not pushed). Backup of the pre-symlink parent retained in the session scratchpad.
**Recurrence:** First for CLAUDE.md specifically. Same family as this morning's [FIELD-NAME-GUESS-FALSE-ABSENCE] — a safeguard that cannot report its own blind spot.

## [OFFMARKET-COMP-PHOTOS-MISSING] V4 comparable cards shipped with no photos and no map — 12:05 AEST
**Symptom:** Will: "Don't we include thumbnails for the 8 comparables? We did in the prototype." The
live V4 evidence section rendered each comparable as bare text with a mid-word truncated narrative —
no photo strip, no evidence map — against a prototype that showed four thumbnails and three
plain-English difference chips per card.
**Root cause:** Two independent defects.
(1) `buildValuationEvidence(doc)` takes photos/coordinates as an OPTIONAL pre-fetched `extras` map,
and the off-market loader passed none — its own comment described that as acceptable degradation. It
is not: the photo strip and the map are the section.
(2) Once wired, all twelve images still failed. `db.server.ts` carries its own copy of the Domain URL
rewrite, and that copy still emitted `bucket-api.domain.com.au`, which Chrome kills with ORB. The
canonical copy in `netlify/functions/shared-utils.mjs` was fixed on 2026-08-05; this duplicate was
missed, so every off-market hero and comparable photo has been ORB-blocked since. Third occurrence of
this exact block (`[PHOTO-ORB-BLOCK]`, `[LISTING-IMAGE-HOSTS-ORB-BLOCKED]`) — always found because
curl reports 200 image/jpeg where a browser reports nothing.
**Fix:** Added `fetchCompExtras()` to `db.server.ts` — comps grouped by the suburb collection parsed
from their own address, then ONE `$in` query per collection (not one per comparable), returning
{thumb, full} pairs plus lat/lng. The loader builds the evidence twice: pass one names the comps, pass
two attaches their extras. Rewrote `_toFullRes` to emit `b.domainstatic.com.au` for both rimh2 and
already-stored bucket-api URLs, re-tiered the hero ranking to match, and applied the rewrite to the
full-res side of the comp photos while leaving the signed rimh2 thumbnails as the strip source.
Added the subject anchor to the evidence payload so the map can render at all. Ported the prototype's
`differences_in_words()` to the card as "Main differences" chips, replacing a truncated narrative that
spent its whole 130-char budget restating the comparable's own address and price.
**Verification:** headless Chrome on 5 Chantilly Place after each deploy, measured with every lazy
image scrolled into view: 12 images, 0 broken, map present, chips reading "a far smaller house /
2 more car spaces / a larger block" — the prototype's own wording.
**Files:** `src/lib/db.server.ts`, `src/lib/valuationEvidence.server.ts`, `src/routes/off-market.$slug.tsx`,
`src/pages/YourHomePage/components/ValuationEvidence.tsx` + `.module.css`
**Recurrence:** ORB block — 3rd. Comp photos on V4 — first.

## [OFFMARKET-NEARBY-SET-CLAIM] "It sits in the set below" pointed at a card the reader could not see — 13:10 AEST
**Symptom:** The "sale up the road" box told the reader the sale it walks through "sits in the set
below". On 5 Chantilly Place that sale is 26 Ballyliffen Court, which is **6th** — and only three
cards render before "See all 8". A reader who checked the claim scanned three cards, failed to find
it, and the one line whose job was to build trust was the line that failed.
**Root cause:** The nearby box picks by DISTANCE; the cards below are ordered by WEIGHT. Two
independent orderings with a sentence between them asserting they line up. Nothing enforced it.
**Fix:** Extracted the pick into `nearbySalePick()` in `v4/valuationCopy.ts`, read by BOTH sections,
so the position named in the copy and the card carrying the tag cannot disagree. The copy now names
the position and links to it (`#comp-6`); each card carries `id="comp-N"`; `ValuationEvidence` opens
its collapsed list on a matching hash at mount and on `hashchange` (a link to a hidden card is a dead
link) and scrolls after the commit frame. The target card renders a "The sale up the road" tag so the
connection survives a reader who scrolls past the link. Also: a comparable whose weight rounds to
zero now reads "<1%" rather than "Weight: 0%", which told the reader a listed sale contributed
nothing.
**Why not the alternative:** Will asked whether the box should instead sit as a breakout beside the
eight cards. Rejected — the right column already carries the photo strip; a sidebar reads as optional
when this is the worked example that teaches the cards; and on mobile a right rail collapses to an
arbitrary point inside the card list, which is where the reader least needs it.
**Verification:** headless Chrome — link reads "it is sale 6 in the set below" → `#comp-6`, click
expands 3 cards to 10, scrolls the target to 90px, tag present, address 26 Ballyliffen Court.
**Files:** `v4/valuationCopy.ts`, `v4/NearbySaleSection.tsx` + `.module.css`, `v4/ComparablesSection.tsx`,
`YourHomePage/components/ValuationEvidence.tsx` + `.module.css`
**Recurrence:** First.

## [OFFMARKET-V4-PARTS-AND-MEDIAN-BASIS] Page changed subject with no cue; then two medians under one label — 14:20 AEST
**Symptom:** Will: the page moves from the valuation to the home's own attributes and the buyers they
attract "however there is no visual, text, or numerical cue that we have transitioned to a new subject
area." He proposed numbering sections 01/02/03.
**Fix (structure):** Three PART dividers, not fourteen numbered sections — numbering every section
makes every eyebrow a chapter heading and destroys the signal. The page changes subject exactly three
times: 01 The valuation, 02 The home itself, 03 Where that leaves you. Each divider is distinguished
by INVERSION rather than size: sections are a tracked sans eyebrow over a serif headline, so a chapter
opener puts the large serif on the NUMERAL and sets its title in tracked sans, over a --stone band
hairlined top and bottom. Legible as a different kind of element without an escalation the rest of the
page then has to answer. The burger menu groups by the same parts. `parts.ts` is the single source;
`partHasContent()` mirrors each section's own guard so a divider can never render over an empty part
(part 02's three sections all come from one discovery document, and 26% of homes have none).
**Second fault, found while verifying:** Will asked for the market-metrics median chart in this page's
styling. Rendering it put "$1,490,000" and "Q2 2026 · $1,410,000" on one screen, both labelled "Robina
median house price". Not a bug in either number — `medianPrice` is the 12-month ROLLING median (the
Domain∪onthehouse union figure) and `priceSeries` plots SINGLE QUARTERS. Unlabelled, one page showed a
reader two medians for their suburb and invited them to decide which was wrong. This is precisely the
failure CLAUDE.md Rule 6 exists for, reached from the other direction: not stale content, but two live
figures on different bases sharing a label. `medianPriceBasis` was already computed in
`getMarketMetricsSummary` and simply never carried through; it now renders under the figure, the chart
is titled "by quarter", and the caption states the two are different bases rather than different
answers.
**Also:** the loader's local `market` annotation named five fields while the assignment wrote nine, so
`inProgressQuarter` was a type error at the assignment and invisible to consumers — which is why the
new chart could not initially see the in-progress quarter it must draw dashed. Now typed as
`MarketSummary`.
**Chart:** hand-rolled SVG in page tokens rather than the Recharts component — same precomputed series
/market-intelligence plots, so they cannot drift; the blue/green Recharts palette on warm paper would
have read as a screenshot of another product.
**Verification:** headless Chrome — three dividers present and labelled, menu grouped, chart renders
Q3 2016→Q2 2026 with the dashed in-progress segment, and both medians now carry their basis.
**Files:** `v4/parts.ts`, `v4/PartDivider.tsx` + `.module.css`, `v4/MedianTrend.tsx` + `.module.css`,
`v4/OffMarketV4.tsx`, `v4/ReportHeader.tsx` + `.module.css`, `v4/TimingSection.tsx` + `.module.css`,
`OffMarketPage/types.ts`, `routes/off-market.$slug.tsx`
**Recurrence:** First.

## [OFFMARKET-COMP-FUNNEL-IMPOSSIBLE] "Built from 8 comparable sales, 44 of them independently verified" — 16:05 AEST
**Symptom:** Will, reviewing the evidence section: the page said the figure was built from all 49
comparable sales, then said "Built from 8 comparable sales (44 independently verified), drawn from 49
recent sales reviewed". Eight sales cannot contain forty-four. On a page whose whole claim is "we show
our working", an analytical owner who spots this distrusts the engine, not the sentence.
**Root cause:** Two separate render sites paired `comparables.length` — the 8 DISPLAYED — with
`confidence.nVerified`, which counts the whole reviewed pool. Three different funnel stages, two of
them multiplied together. Systemic, not one property: 295 of 321 sampled Robina valuations carried it.
**The true funnel, established before any copy was written:** 49 candidate sales reviewed → 44
independently verified → ALL 49 weighted into the figure → the 8 strongest displayed, carrying 19.8%
of total weight. Verified: Σ(adjusted × weight) over the pool = $1,756,164; × the Robina calibration
1.0189 = $1,789,357, matching `reconciled_valuation` to the dollar. Over the displayed 8 it sums to
$343,574, i.e. the displayed set is emphatically NOT what the figure is built from.
**Fix:** One `funnelSentence()` in ValuationEvidence.tsx, used by both sites and by the answer block:
"49 recent sales were reviewed and weighted into this figure, 44 of them independently verified. The 8
carrying the most weight are shown below." Falls back to the old phrasing when a document holds no
pool count or a pool equal to what is shown, so the appraisal path is unchanged.
**Also from the same review:** "comparable with" → "comparable to" (Australian English); the valuer
claim softened to what an inspection can actually resolve; "In testing, 79% of eventual sale prices
fell inside a band this wide"; dropped "and we do not describe it as one" (defensive); "Most
comparisons stop at bedrooms and bathrooms" → "Bedrooms and bathrooms are only the beginning"
(unsupported competitor claim); "the people paid to try have spent this year disagreeing in public" →
"the forecasts published this year have not agreed with each other"; "None of that tells you what to
do, and we are not going to" → owner-supplies-constraints/Fields-recommends framing; plan option
"Whatever pays for itself" → "Worth doing where the case is strong"; the ending now asks what the home
would let the owner move INTO, since the old closing link answered a different question than the one
the closing line opened.
**Files:** `YourHomePage/components/ValuationEvidence.tsx`, `v4/{AnswerBlock,ReliabilitySection,HeroSection,ScarcitySections,TimingSection,ClosingSections}.tsx`
**Recurrence:** First.
**Open:** the funnel LABELS (12,000 / 183 / 6), the pool insight and the buyer-section copy are stored
in `offmarket_discovery` documents written from `15_Off-Market/Page_Redesign_V2/copy.yaml`, not in the
page — they need a builder change plus a rebuild. Definitions established: 12,000 = homes in the
suburb; 183 = active listings across the 9-suburb comparison catchment; 6 = of those, broadly
comparable; 58 = the market-metrics active-listing snapshot for the suburb; 40 = houses for sale in
the suburb. ⚠ The stored pool claim ("1-4% to value per square metre") does not match our own engine,
whose regression-validated rate is $75,000 per pool — about 4% of a home at this value.
