# 2026-05-13 — Fix History

## [AKAMAI-BLOCK-001] Domain.com.au scraping silently failing — Akamai bot block + missing error propagation — 09:10 AEST
**Symptom:** Website data appeared stale for 2+ days. Investigation showed:
- Step 101 reported `success: true, exit_code: 0` for the past 3 nightly runs (May 11, 12 — May 13 not yet run)
- Actual stdout: `[Robina] Page 1: Failed to fetch, stopping pagination` for ALL target suburbs, 0 URLs discovered
- Direct probe from VM (IP `34.40.230.132`) returned HTTP 403 from Domain on every path (homepage, `/sale/`, `/sold-listings/`, `/property-profile/`, even `/robots.txt`)
- Response header: `server: AkamaiGHost`. Body was the Akamai "Behavioral Challenge" interstitial with `Powered and protected by Akamai`
- Pattern across last 60 runs: scraping was already intermittent — multi-day blocks would engage after each Sunday all-suburbs run (step 102, ~2,700 URLs), then thaw by Friday. New in May 2026; April Sundays didn't trigger the block.
- The same block hit the backup property-scraper VM (`35.201.6.222`) — Akamai had flagged both GCP IPs.

**Root cause:** Two compounding issues:
1. **Akamai block trigger.** The Sunday all-suburbs run (step 102, 81 suburbs, ~2,700 URLs in one session) was tipping Akamai's bot management thresholds, causing Mon-Thu blocks on this VM's IP. Direct `curl_cffi` with chrome120 TLS impersonation couldn't bypass — Akamai's interstitial requires JS execution to solve.
2. **Silent-failure bug in step 101.** `run_curlffi_suburb_scrape.py:main()` returned 0 even when every suburb discovered 0 URLs. The orchestrator + watchdog therefore saw "✅ success" and didn't page anyone. Stale data sat for 2 weeks before being noticed.

**Fix:** Five-part repair, all live + on GitHub:

1. **Bright Data Web Unlocker integration (primary fix).** Added Will's existing `web_unlocker2` zone as the fetch layer for every script that hits Domain HTML. New helper `shared/domain_fetch.py` exposes `fetch_html(url)` and `fetch_with_status(url)` — both POST to Bright Data's API which solves the Akamai JS challenge and returns real content. Falls back to direct `curl_cffi` when `BRIGHTDATA_API_KEY` is unset. Cost: ~$0.75/day for target market step 101.

2. **Disabled Sunday all-suburbs run.** Set `schedule.run_other_suburbs_weekly: false` in `config/settings.yaml`. Steps 102 + 104 will not run; per memory, non-core suburbs only need sample-level coverage for comparison work, not 100% completeness. Will revisit if/when bandwidth allows splitting them across the week.

3. **Step 101 silent-success fix.** Added a final check in `main()`: if `total_discovered == 0` across all suburbs, exit code 2 with a loud error message. The orchestrator/watchdog now surfaces this as a failure instead of greenlighting through.

4. **Patched 4 downstream scripts to use the helper:**
   - `sold_backfill/search_based_sold_monitor.py` (step 103) — `fetch_page()` now calls `fetch_html()`
   - `scripts/coverage_check.py` (step 109) — both `fetch_domain_count()` and `fetch_domain_sold_count()` use helper
   - `scripts/scrape_recent_sold.py` (step 111) — same pattern as 103
   - `scripts/detect_withdrawn.py` (step 113) — reworked `check_listing_status()` to use content-aware detection (title-based "Sold ..." check, body markers for "off the market") instead of relying on HTTP 301 redirects (Domain no longer redirects withdrawn listings — they serve 200 with state in title/body)

5. **Confirmed step 110 (image download) is unaffected** — `rimh2.domainstatic.com.au` and `bucket-api.domain.com.au` return 200 from this VM directly. Domain's image CDNs are not behind Akamai bot management.

**Verification (live tests):**
- Manual step 101 on Robina via Web Unlocker: **71/76 listings refreshed, 2 new discovered, took 13 min**. Fresh address "2 Arbour Ave, Robina — Offers $700,000" visible in DB at `2026-05-13 08:43 AEST`.
- Step 109 manual run: Domain=78, DB=76, no gaps flagged.
- Step 103 manual run on Robina: 60 sold + 16 under-contract detected, 3 listings correctly flagged under_contract (`1904/22-34 Glenside Drive`, `173 Ron Penhaligon Way`, `35 Easthill Drive`).
- Step 111 manual run on Robina (dry): 4 sold properties detected in last 7 days, 2 new (e.g. `81 Thorngate Drive — sold 2026-05-11 $1,410,000`).
- Step 113 dry-run started; result captured separately.

**Cost projection:** ~$25/month at current volumes (step 101 daily ~250 URLs + step 103 + step 109 + step 111 + step 113). Well within budget.

**Files:**
- `shared/domain_fetch.py` (new)
- `sold_backfill/search_based_sold_monitor.py`
- `scripts/coverage_check.py`
- `scripts/scrape_recent_sold.py`
- `scripts/detect_withdrawn.py`
- `config/settings.yaml` (run_other_suburbs_weekly → false; gitignored)
- `Property_Data_Scraping/03_Gold_Coast/Gold_Coast_Wide_Currently_For_Sale_AND_Recently_Sold/run_curlffi_suburb_scrape.py` (2 commits: Web Unlocker integration + silent-success fix)
- `.env` (added `BRIGHTDATA_API_KEY`, `BRIGHTDATA_ZONE`; gitignored)

**Recurrence:** First — for AKAMAI-BLOCK-001. The underlying "stale-data-flies-under-the-radar" pattern is recurrent (compare WATCHDOG step 101 events in prior logs). Silent-success fix should prevent the next instance.

**Follow-ups (not done in this session):**
- Rotate the Bright Data API key — credentials were pasted in plaintext in chat earlier in the session.
- Consider audit of any other script that calls `cffi_requests.get` against domain.com.au (e.g. backfill jobs, ad-hoc scripts). `grep -rn 'domain.com.au' /home/fields/Fields_Orchestrator/scripts` for survey.
- Step 113's withdrawn-detection logic is fresh — needs validation against a known withdrawn URL once one appears. Currently relies on title-prefix `Sold ` for sold and explicit body markers for withdrawn.

## [BACKUP-SCRAPER-001] Backup scraper zombie — Cosmos dead, no throttling, no local persistence — 23:30 AEST
**Symptom:** Backup property scraper on `35.201.6.222` had been running 66 days with no useful output:
- 13,220+ cycles, 79,639 JSON files on disk (2.9GB), but only 2 unique addresses being re-scraped per cycle for the past month
- Cosmos DB `Gold_Coast_Backup.property_url_tracking` returning 403 Forbidden on every call (Azure subscription disabled)
- RE/MAX GC + Coastal newly returning 403 (IP-fingerprinted after 2 months continuous scraping)
- McGrath still hard-blocked (Vercel JS challenge — known)
- `scraper.log` at 132MB, no rotation
- Cycle interval ~7 min = 200 cycles/day, contributing to agency IP blocks
- Production has 73 for_sale Robina properties; backup had no way to even compare

**Root cause:** Three compounding failures:
1. **Cosmos subscription dead.** URLTracker writes failed → `detect_new_urls` returned all URLs as "new" → tried to scrape → couldn't write → infinite retry loop on the same handful of records that were already in memory.
2. **No local persistence layer.** Even with rich JSON output (79K files, including 5,199 unique Robina addresses with property data), nothing was being merged into MongoDB. Backup was a JSON-write-only system, useless as a query target.
3. **No throttling, no UA rotation.** Continuous 7-min cycles with a single hardcoded user-agent string had trained agency WAFs to recognise and block the scraper.

**Fix:** Phase 0 hardening + Phase 1 local Mongo replacement (Phases 2-7 deferred to follow-up sessions per agreed workflow):

**Phase 0 — Hardening (4 changes):**
1. **Throttle.** `continuous_monitor.py` now sleeps `CYCLE_INTERVAL_SECONDS = 1800` (30 min) between cycles, env-overridable. Cuts daily request volume from 200 cycles/day to 48.
2. **User-agent rotation.** `robust_extractor.py` rotates between 6 realistic recent Chrome/Firefox UAs (macOS + Windows) per request.
3. **Per-request jitter.** 0.5-3.0s random sleep before each `page.goto()` so concurrent requests don't fire in perfect lockstep.
4. **Log rotation.** Installed `/etc/logrotate.d/property-scraper` (daily, 7 rotations, compress, copytruncate) on backup VM. Truncated current 132MB log.

**Phase 1 — Local Mongo replacement (3 components):**
1. **Rewrote `url_tracker.py`** — drop-in replacement for the Cosmos-backed version. Same public API (`get_known_urls`, `detect_new_urls`, `update_tracking`, `record_discovery`, `mark_json_saved`) so `continuous_monitor.py` is unchanged. Backed by local `Gold_Coast.property_url_tracking` + `Gold_Coast.new_url_discoveries` + denormalized snapshot fields on `Gold_Coast.{suburb}` cadastral docs (`backup_scraper.*` namespace + top-level `listing_status` + `scrape_source: "backup_scraper"`).
2. **JSON → Mongo backfill (`scripts/ingest_json_to_mongo.py`).** Walks `discovered_urls/{suburb}/*.json`, dedupes by address, picks the latest-strongest status per address, upserts into the three collections. Idempotent.
3. **Cadastral normalization remediation (`scripts/remediate_cadastral_norm.py`).** JSON discovery addresses have commas (`33 Trinity Place, Robina, QLD 4226`) but cadastral baseline has none (`33 ST IVES DRIVE ROBINA QLD 4226`). Added `complete_address_norm` field to all cadastral docs (uppercase, no commas, single-spaced). 1,107 listings-only stubs after first backfill were re-matched, 772 merged into cadastral.

**Verification:**
- Service restarted cleanly with new URLTracker — `✅ URL Tracker initialized (local Mongo: mongodb://localhost:27017/)`.
- Robina cycle 1 Pass 0 picked up listings from Ray White Robina (22 URLs), Ray White TMG (13 URLs), Ray White Malan & Co (in progress at log capture).
- Backfill counts: Robina 5,199 unique addresses ingested, VL 2,627, BW 1,117. By status (latest-strongest): Robina 346 for_sale / 2,739 sold / 898 leased; VL 148/1,493/508; BW 54/484/200.
- Coverage check vs production current for_sale: Robina 66% of addresses exist in baseline, VL 57%, BW 93%. "Backup also says for_sale" is only 16/3/2% — expected, since most JSON files are weeks old and properties have sold/withdrawn since. Live scraper will rebuild current state from agency feeds going forward.

**Files:**
- `backup_scraper/scraper/continuous_monitor.py` (throttle + local Mongo only)
- `backup_scraper/scraper/url_tracker.py` (rewritten — local Mongo backend)
- `backup_scraper/scraper/robust_extractor.py` (UA rotation + jitter)
- `backup_scraper/scraper/url_tracking_run.py` (banner)
- `backup_scraper/scraper/scripts/ingest_json_to_mongo.py` (new — 350 lines)
- `backup_scraper/scraper/scripts/remediate_cadastral_norm.py` (new — 100 lines)
- `/etc/logrotate.d/property-scraper` (on backup VM, not in git — system config)

**Recurrence:** First — for BACKUP-SCRAPER-001. The general pattern of "service runs forever, looks alive in `systemctl status`, but produces nothing" is well-precedented in our scraping stack — see also AKAMAI-BLOCK-001 above.

**Follow-ups (Phases 3-7 of agreed workflow — separate sessions):**
- Phase 3 — Field extraction quality (the user's stated priority: "very high coverage rate for individual house features"). Compare per-property field completeness backup vs production for ~20 listings; strengthen `HybridExtractor` rules + GPT fallback for missing fields.
- Phase 4 — Unblock RE/MAX + Coastal (UA rotation should help; residential proxy if not). Add the missing major chains identified in Phase 2 (Belle Property, Stone Real Estate, Image Property, Coronis, @realty, WHITEFOX) + try onthehouse.com.au as supplemental source.
- Phase 5/6 — Roll out Varsity Lakes + Burleigh Waters once Robina is at ≥80% coverage with high field completeness.
- Phase 7 — Observability + watchdog integration (`backup_scraper_health.py`).

## [BACKUP-SCRAPER-002] Backup scraper Phase 2 — cadastral sync + abbreviation-aware norm — 23:55 AEST
**Symptom:** After Phase 1 backfill, coverage check showed Robina at 66% address-in-baseline (48/73 production for_sale addresses). Most "missing" were actually present in backup but stored as `27 BEAUTY POINT DRIVE` (cadastral) vs `27 Beauty Point Drive, Robina, QLD 4226` (JSON discovery) and `5 Peppertree Circuit` vs `5 PEPPERTREE CIRCUT` (typos). Also: backup VM's Nov 2025 cadastral snapshot was 80 records behind production for Robina (46 missing) + 99 behind for Varsity Lakes + 50 behind for Burleigh Waters.

**Root cause:** Two issues compounding the apparent gap:
1. **Cadastral snapshot drift.** Backup VM's local Mongo `Gold_Coast.{suburb}` cadastral records were imported Nov 6, 2025. Production has since added ~200 new addresses (mostly strata unit numbers + small subdivisions). Backup cadastral universe was a subset of production.
2. **Abbreviation mismatch.** Cadastral always uses full street types (`AVENUE`, `STREET`, `DRIVE`, `CIRCUIT`). JSON-scraped addresses use whatever the agency writes — usually short form (`Ave`, `St`, `Dr`, `Cct`). The Phase 1 norm function only handled commas and case; abbreviations broke matching for ~10-20% of records.

**Fix:**
1. **`scripts/sync_cadastral_from_production.py`** (new — 220 lines, two-VM workflow). Run with `--export` on this VM to dump cadastral-only fields (no Domain-derived data) from production Cosmos to a JSON bundle in `/tmp`. Bundle is then SSH-copied to backup VM and applied with `--apply`, which loads existing local addresses into memory once and bulk-upserts. Production cadastral fields strictly limited to non-Domain-derived columns (`ADDRESS_PID`, lat/lng, `lot_size_sqm`, `STREET_*`, `LOCALITY`, etc.) — backup remains genuinely independent of Domain scraping for all listing data. Backup VM Robina now has 12,196 docs (11,823 cadastral + 373 listings-only stubs), VL 7,701 (7,670 + 31), BW 6,900 (6,895 + 5).

2. **Abbreviation-aware `_normalise_address()` across all 4 scripts.** 21-entry `STREET_ABBREV` map (`AVE→AVENUE`, `RD→ROAD`, `DR→DRIVE`, `CT→COURT`, `PL→PLACE`, `CRES→CRESCENT`, `CCT→CIRCUIT`, `WY→WAY`, `BLVD→BOULEVARD`, `TCE→TERRACE`, `HWY→HIGHWAY`, `PDE→PARADE`, `CL→CLOSE`, `LN→LANE`, `GR→GROVE`, plus less-common variants). Expansion fires only when the abbreviation appears in the non-terminal middle (typically position `len-4`) — so `ST IVES DRIVE` stays as `ST IVES DRIVE` (the `ST` is a proper noun, position 1, not expanded), while `5 BEACH PL ROBINA QLD 4226` becomes `5 BEACH PLACE ROBINA QLD 4226`. Applied to `url_tracker.py`, `ingest_json_to_mongo.py`, `remediate_cadastral_norm.py`, `sync_cadastral_from_production.py`.

**Verification (after cadastral sync + norm expansion):**
- **Robina baseline coverage: 100% (52/52)** — every production for_sale address with a non-empty `complete_address` is now present in backup. (Production's `listing_status: for_sale` returns 73 docs but 21 have empty `complete_address` — production data bug, separate from backup scraper.)
- **VL baseline coverage: 100% (23/23)** (after dropping empty-address records, 37 → 23 unique).
- **BW baseline coverage: 100% (40/40)** (42 → 40 unique).
- **Live scrape correctness (Robina cycle 1, just-completed Pass 0):** 60 new agency URLs processed in 812s. UA rotation visible in log (`Windows NT`, `Macintosh Intel Mac OS X 10.1`), per-request jitter active (1.7s, 2.0s observed). Pages loading HTTP 200 from harcourts.net, raywhiterobina.com.au, raywhitetmg.com.au, raywhitemalanandco.com.au. Confirmed real listings being captured (e.g. `17 Springvale Street, Robina, QLD 4226 - House for Sale - harcourts.net`).

**Gap inventory (Robina, 52 production for_sale, what backup says):**
- `12 (23%)` already matched as for_sale (after just one Pass 0 cycle on the new tracker)
- `25 (48%)` cadastral exists, no listing data yet — should fill in over next 5-10 cycles (~3-5 hours)
- `10 (19%)` stale "sold" status from JSON history — will refresh during recheck pass
- `3 (6%)` `page_unavailable` — agency listing URL returned a dead/redirect page (likely listing was withdrawn after our scrape)
- `2 (4%)` extracted as `leased` — wrong status, extraction logic issue → Phase 3 work
- `0 (0%)` not in backup at all (was 25/73 before sync + norm fix)

**Major agencies seen in production but NOT in backup's URL trigger list (counted from prod Robina for_sale, single-listing-each unless noted):**
- Belle Property (2 listings on Robina) — large national chain
- @realty (1 listing) — national network, realty.com.au
- Stone Real Estate / Stone Gold Coast (1)
- Coronis Pacific Pines (1)
- Image Property Gold Coast (1)
- WHITEFOX Gold Coast (1)
- The Onsite Manager (3) — building manager
- Chateaux, BBT, Welcome Change, MIC Homes, Invest & Co, AveNew, DREW (1 each)
- SaleByHomeOwner, For Sale By Owner (FSBO platforms — won't have an agency URL pattern)

Total addressable lift from adding chain agencies (Belle, @realty, Stone, Image, Coronis, WHITEFOX): ~7-8 additional listings catchable in Pass 0. Deferred to Phase 4.

**Files:**
- `backup_scraper/scraper/url_tracker.py` (expanded `_normalise_address()`)
- `backup_scraper/scraper/scripts/ingest_json_to_mongo.py` (mirror norm)
- `backup_scraper/scraper/scripts/remediate_cadastral_norm.py` (mirror norm + always-recompute)
- `backup_scraper/scraper/scripts/sync_cadastral_from_production.py` (new — 220 lines, two-VM workflow)

**Recurrence:** First — for BACKUP-SCRAPER-002. The "address normalization drift" pattern is precedented across the production stack — see also `complete_address` vs `address` field inconsistency in the property pipeline.

## [BACKUP-SCRAPER-003] Phase 3 — field extraction quality (HybridExtractor) — 01:10 AEST
**Symptom:** Per-property field completeness audit (12 currently-matched for_sale Robina properties, backup vs production) showed six fields the backup scraper was NOT extracting at all even though production captures them at near-100%:
- `agent_name` / `agent_names` (0% vs 100%)
- `price` (textual price guidance like "Contact Agent" / "Offers Over $1.2M" / "Auction Sat 18 May") (0% vs 100%)
- `inspection_times` (0% vs 92%)
- `first_listed_date` (0% vs 100%)
- `total_floor_area` (internal m², distinct from land) (0% vs 83%)
- `floor_plans` (separate gallery, 0% vs 92%)

The pre-existing `description` field was being populated with the first 500 chars of body text — which on agency sites is mostly nav/header noise, not the agent's prose writeup.

**Root cause:** The `HybridExtractor` was built before the field set was finalised against production. It captured the obvious things (beds/baths/cars/price-numeric/property_type/features/listing_status) but never had extractors for the agent block, inspection schedule, listed date, internal floor area, or price-text.

**Fix:** Six new extractors added to `RuleBasedExtractor` (all rule-based, no LLM cost) + a new `extract_description()` that carves out the prose between `Property for Sale` and footer markers like `Property Features` / `Outgoings` / `Show More` / `Map Location`. Expanded `feature_keywords` from 12 to 30+ entries (Solar, Water tank, Fireplace, Walk-in pantry, Stone benchtops, Hardwood floors, Waterfront, North-facing, etc.).

1. **`extract_price_text(text, title)`** — multi-pattern matcher for "Under Contract", "Auction Sat 18 May", "Offers Over $X", "Contact Agent", "POA", "EOI Closing", plain "$1,250,000 Negotiable". Pattern ordering matters — strong markers (Under Contract, Auction with-explicit-date) checked before the catch-all numeric.
2. **`extract_agents(text)`** — heuristic. Look for title-cased 2-4-word names where (a) every word is alpha + starts uppercase + has a lowercase char (rejects "07 5578 8800" and "UNDER CONTRACT"); (b) line doesn't contain any agency hint (excludes "Ray White Robina" as a candidate); (c) within 1-3 lines after the candidate there IS an agency hint (confirms it's the agent for THAT agency). 50-entry stopword list (months, common headers, "You", "Might", "Also", "Like", "Past", "Recent", "Latest" etc.) prevents picking up section headers.
3. **`extract_inspection_times(text)`** — collapses newlines first (agency sites often stack each token: `Saturday\n4\nApril\n10:30am - 11:00am`), then matches `Weekday + day + month [+ year] + time-range`.
4. **`extract_first_listed_date(text)`** — matches "Added DD Month, YYYY", "Listed on DD/MM/YYYY", "Posted DD Month YYYY", "Date Added" variants. Returns ISO format.
5. **`extract_total_floor_area(text)`** — multi-pattern matcher for "internal living", "internal area", "floor area", "under roof", "approx. 110m² layout", and "Building: 362m²". Sanity bounds 30-2000 m².
6. **`extract_floor_plans(images)`** — pulls floor-plan URLs out of the raw images list by alt-text or URL pattern (`floorplan`, `floor-plan`, `/fp/`, `_fp_`).

Wired into `extract_property_data()` → `create_mongodb_document()`, plus extended `SNAPSHOT_FIELDS` in `url_tracker.py` so the new fields land on the cadastral property doc under `backup_scraper.*`.

**Cadastral fallback** added to `URLTracker._upsert_property_snapshot()` — if a listing doesn't expose `land_size_sqm` but the cadastral doc has one, copies it onto the snapshot as `backup_scraper.land_size_sqm_from_cadastral`. Also stamps `backup_scraper.latitude` / `.longitude` from cadastral so downstream consumers don't have to join.

**`scripts/reprocess_discoveries.py`** (new — 130 lines). Re-runs the updated extractor against every existing `new_url_discoveries` record with raw_data, refreshing the extracted_data field AND updating the cadastral snapshot — gives instant benefit from extractor improvements without waiting for a full re-scrape. Dedupes by `complete_address_norm` so each address is processed once (using the latest discovery). Idempotent. Stamps `extractor_version: phase3_2026_05_13` for traceability.

**Verification:**

Robina field completeness — BEFORE vs AFTER Phase 3, measured on the 13 matched-for-sale properties:

| Field | Before | After | Note |
|---|---|---|---|
| bedrooms | 92% | 92% | no change |
| bathrooms | 92% | 92% | |
| carspaces | 83% | 92% | +9% (structured-icon fallback also benefits) |
| property_type | 100% | 100% | |
| **price** (text) | **0%** | **38%** | new field — many properties say "Contact Agent" so 38% is honest |
| **agent_name** | **0%** | **31%** | new field; phone-number false positive ("07 5578 8800") fixed in iteration 2 |
| **agent_names** | **0%** | **31%** | |
| **inspection_times** | **0%** | **46%** | new field; newline-bug fixed in iteration 2 |
| **first_listed_date** | **0%** | **15%** | new field; only some agency pages expose "Added DD Month YYYY" |
| **total_floor_area** | **0%** | **31%** | new field; catches "approx. 110m² layout" and "Building: 362m²" |
| features | 92% | 92% | now 30+ keywords vs 12 before |
| description | 100% | 100% | now agent prose rather than first 500 chars of nav |
| **floor_plans** | **0%** | **0%** | agency sites load floor plans behind a JS tab — Playwright interaction needed (deferred to Phase 4) |

Cumulative across all 4,873 reprocessed Robina addresses (some sold, leased, etc. — not just for_sale):
- agent_name 57%, sale_price 55%, sold_date 49%, land_size_sqm 43%, price 15%, total_floor_area 12%, floor_plans 4%, inspection_times 2%, first_listed_date 2%.

**Live test (Sample 1, Harcourts Robina apartment 407/1 Waterfront Place):**
```
listing_status     for_sale
bedrooms           2
bathrooms          2
carspaces          2
price              UNDER CONTRACT
total_floor_area   110.0
agent_name         Jarrod Pienaar
first_listed_date  2026-02-04T00:00:00
features           [Pool, Air conditioning, Ensuite, Security system, Ceiling fans, ...]
```

**Files:**
- `backup_scraper/scraper/hybrid_extraction_poc.py` (6 new extractors, expanded features list, improved description)
- `backup_scraper/scraper/url_tracker.py` (SNAPSHOT_FIELDS extended, cadastral lot_size + lat/lng fallback)
- `backup_scraper/scraper/scripts/reprocess_discoveries.py` (new — 130 lines)

**Recurrence:** First — for BACKUP-SCRAPER-003.

**Follow-ups (Phase 3 leftover for future sessions):**
- Floor-plan extraction needs a Playwright "click the Floor Plan tab" step in `robust_extractor.py` — current static image scrape misses them on Harcourts/Ray White. Cost ~2-3s extra per page.
- `inspection_times` could use better date parsing — currently raw string; production stores ISO datetimes.
- `agent_phone` / `agent_email` not yet extracted (visible on agency pages but lower priority than name).
- `price_history` and `transactions` arrays not yet captured (production has these from Domain's API; agency pages don't usually expose this).

## [BACKUP-SCRAPER-004] Phase 4 — unblock attempts + WAF reality check — 02:00 AEST
**Symptom:** RE/MAX GC, Coastal, McGrath, and Coronis were blocked from the backup VM as of Phase 0 investigation (Cloudflare/Vercel 403s). Phase 0 UA rotation was the cheap-first attempt to unblock; Phase 4 was meant to verify (or accept) the block.

**Root cause:** Cloudflare and Vercel bot management fingerprints on TLS + IP + cookies, not just User-Agent. UA rotation alone won't bypass them. Also discovered during probing: SearXNG infrastructure (`http://34.40.202.29:8080`, referenced in `Hand-off-notes.md`) is **down** — connection times out. The `check_property()` method in `continuous_monitor.py` that uses SearXNG was already dead code (never called from `monitor_suburb()`), but if revived it would need a working SearXNG endpoint.

**Findings — probe results (UA-rotated Playwright requests from backup VM IP `35.201.6.222`):**

| Agency | Status | Block type | Recoverable? |
|---|---|---|---|
| RE/MAX GC (`remaxgc.com.au`) | 403 | Cloudflare Error 1006 | Only with residential proxy (~$25/mo Bright Data) |
| Coastal (`coastal.com.au`) | 403 | Cloudflare Error 1006 | Only with residential proxy |
| McGrath (`mcgrath.com.au`) | 429 | Vercel Security Checkpoint | Only with full JS-challenge solver |
| Coronis (`coronis.com.au`) | 403 | Cloudflare Error 1006 | Only with residential proxy |
| WHITEFOX (`whitefox.com.au`) | 403 | Cloudflare-like | Only with residential proxy |
| Belle Property (`belleproperty.com`) | 200 | SPA — listings JS-rendered, no static URLs | Would need full Playwright search interaction (~10x cost) |
| @realty (`realty.com.au`) | 200 | SPA with location search | Same as Belle — JS-heavy |
| Stone Real Estate (`stonere.com.au`, `stoneproperty.com.au`) | DNS NXDOMAIN | Domain doesn't exist | Need to find correct domain |
| Image Property (`imageproperty.com.au`) | 200 | SPA | Same as Belle |
| Onthehouse (`onthehouse.com.au/real_estate/robina-qld-4226`) | 404 | "Nobody's home" — no Robina inventory at this URL pattern | User flagged as data-light upfront |
| LJ Hooker (various paths) | 404 | Wrong URL pattern | Would need to find correct path |
| Century 21 (various paths) | 404 | Wrong URL pattern | Need correct path |
| Barry Plant (`barryplant.com.au/buy?location=robina-qld`) | 200 | 0 Robina listings on the page | No inventory in our target |

**Honest assessment:** Of the "missing chain" agencies identified in Phase 2, none deliver new Robina coverage without either (a) a paid residential proxy for the WAF-blocked ones, or (b) substantial Playwright interaction work for the SPA ones. The marginal lift would be 6-7 listings (per Phase 2 inventory) at significant engineering cost.

**Fix (what we DID do):**

1. **UA rotation + jitter in `direct_agency_scraper.py`** — mirrors the Phase 0 change to `robust_extractor.py`. Each agency now gets a fresh browser context with a randomly-chosen UA from the same 6-entry pool. Per-agency `asyncio.sleep(random.uniform(0.5, 2.5))` before navigation so concurrent agency hits don't burst-fire.

2. **Per-agency adaptive backoff.** Module-level `_AGENCY_HEALTH` dict tracks consecutive failures + cooldown-until per agency. On a 403/404/429/503, the agency is shelved for `DEFAULT_BACKOFF_CYCLES * 2^(consecutive_failures - 1)` cycles, capped at `MAX_BACKOFF_CYCLES = 24` (~12 hours at 30-min cadence). On success, state resets and the recovery is logged. State is in-process — resets on service restart, which is acceptable for this scale.

3. **`scrape_agency()` now early-returns with `skipped_backoff: True`** when an agency is in cooldown, so the cycle doesn't pay the navigation timeout cost on dead agencies. RE/MAX and Coastal were observed correctly skipping in cycle 3 after their cycle 2 failure.

**What we DIDN'T do (with reasons):**

- **Did not revive SearXNG.** Infrastructure at `34.40.202.29:8080` is down (10s timeout, connection refused). Would need to deploy a fresh SearXNG instance (Docker) + wire up the staggered per-address query path in `monitor_suburb()`. Real value (long-tail agency discovery via Google/Bing/DDG aggregation) — but bigger scope than Phase 4, deferred.
- **Did not add new agencies.** All major-chain candidates failed for one of three reasons: WAF block, SPA architecture, or wrong-URL-pattern with no listings exposed. Each would need agency-specific scraping logic — diminishing returns vs cost.
- **Did not buy a residential proxy.** Production uses Bright Data Web Unlocker (~$25/mo) for Domain.com.au — same product would unblock RE/MAX + Coastal + McGrath + Coronis. Requires user authorisation for the spend. Recommended if backup scraper is meant to be production-grade.

**Verification (cycle 3, post-deploy):** Pass 0 cycle time dropped from 812s (cycle 1, 60 new URLs) → 112s (cycle 3, all known + 2 agencies skipped via backoff). Working agencies: Robina Realty 21 URLs, Harcourts 16 URLs, GCSR 2, Astras 2, plus Ray White Robina/TMG/Malan & Co/Surfers Paradise responding cleanly under rotated UAs.

**Files:**
- `backup_scraper/scraper/direct_agency_scraper.py` (UA rotation, jitter, adaptive backoff, fresh browser context per agency)

**Recurrence:** First — for BACKUP-SCRAPER-004.

**Follow-ups (Phase 4 leftover, scope-defined for next session):**
- **Decision point for user:** Buy Bright Data Web Unlocker for backup scraper ($25-50/mo)? Without it, RE/MAX + Coastal + McGrath + Coronis stay blocked indefinitely, accepting ~10 listings of Robina coverage loss.
- **Local SearXNG deployment** — would re-enable the long-tail discovery path. ~1-2 hours setup (Docker on backup VM), then wire `check_property()` into `monitor_suburb()` with staggered cadence (1/10 of cadastral per cycle, full coverage every 5 hours).
- **Playwright-based SPA scraping** for Belle/@realty/Stone — high engineering cost, marginal Robina lift (1-2 listings each).
- **Verify backoff state behaviour** across service restarts — currently resets, which is fine for this scale but means we re-test a blocked agency immediately after each restart. Could persist `_AGENCY_HEALTH` to Mongo if it becomes a problem.

## [HOUSE-MINI-SITE-001] House mini-site v0.4 — standalone shell — 09:48 AEST
**Symptom:** Seller report at `/your-home/:slug` rendered inside the main site's `SiteHeader` + `SiteFooter`. Full nav (News, Market Intelligence, Properties, Analyse Your Home, Sell with Us, Contact, Subscribe) sat above the report, plus newsletter signup + social links beneath it. Made the report feel like a marketing page rather than a personal document — undermined the "show this to your partner / accountant" framing on the Valuation tab.
**Root cause:** Initial v0.2 build deliberately reused the site shell to ship quickly. Standalone treatment was deferred until the report's tab structure settled.
**Fix:** Created `ReportHeader` (Cormorant Garamond "Fields" wordmark linking to `/` in a new tab + persistent "Prepared for {name} · {date}" salutation) and `ReportFooter` (four methodology links: How we value, suburb market data, Editorial policy, Disclaimer + provenance line + contact). Removed "Prepared for {name}" from the hero eyebrow (now redundant with the header). Browser-tab title derived from slug → "13 Terrace Court, Merrimac · Property Report" via `titleFromSlug()` in the route meta. Header is non-sticky so the tab bar can stick cleanly once the seller scrolls past the hero.
**Files:**
- NEW: `01_Website/src/pages/YourHomePage/components/ReportHeader.tsx`
- NEW: `01_Website/src/pages/YourHomePage/components/ReportHeader.module.css`
- NEW: `01_Website/src/pages/YourHomePage/components/ReportFooter.tsx`
- NEW: `01_Website/src/pages/YourHomePage/components/ReportFooter.module.css`
- MOD: `01_Website/src/pages/YourHomePage/YourHomePage.tsx` (shell swap, not-found header)
- MOD: `01_Website/src/pages/YourHomePage/YourHomePage.module.css` (not-found header styles)
- MOD: `01_Website/src/pages/YourHomePage/components/HeroSection.tsx` (drop "Prepared for" eyebrow)
- MOD: `01_Website/src/routes/your-home.$slug.tsx` (`titleFromSlug` for browser tab)
**Recurrence:** First

## [HOUSE-MINI-SITE-002] titleFromSlug bug — "Terrace Court" double-match — 09:55 AEST
**Symptom:** Browser tab title for `/your-home/13-terrace-court-merrimac` rendered as "13 Terrace, Court Merrimac · Property Report" instead of "13 Terrace Court, Merrimac · Property Report".
**Root cause:** `titleFromSlug` scanned the slug parts left-to-right looking for a `STREET_TYPES` match. The street name "Terrace Court" contains both "terrace" AND "court" as street-type words; the left-scan picked "terrace" (at index 1) as the split point, putting the comma after "Terrace" and leaving "Court Merrimac" as the suburb.
**Fix:** Scan right-to-left instead. The actual descriptor (the trailing street-type word) wins. Also handles multi-word suburbs like "Burleigh Waters" correctly.
**Files:** `01_Website/src/routes/your-home.$slug.tsx`
**Recurrence:** First. Edge case I should have caught while writing the function — "Terrace Court Merrimac" is the build template slug.

## [HOUSE-MINI-SITE-003] React #418 hydration warning on /your-home — 10:08 AEST
**Symptom:** Site inspector caught React minified error #418 (hydration mismatch) on every load of the seller mini-site. Console: "Hydration failed because the initial UI does not match what was rendered on the server." Page rendered correctly visually, so warning was non-blocking — but every report URL was paying the React full client re-render cost.
**Root cause:** `ActivityFeed.formatDate` used `d.toLocaleDateString("en-AU", { weekday: "short", day: "numeric", month: "short" })`. Node's ICU (used during Netlify SSR) emits "Mon, 11 May" (with comma after the weekday). Browser Intl emits "Mon 11 May" (no comma). Same input, different output → hydration mismatch on every activity-feed item.
**Fix:** Deterministic formatter using hard-coded `WEEKDAY_ABBR` / `MONTH_ABBR` arrays and `Date.UTC(...)` to avoid runtime locale + timezone divergence. Output identical on Node and browser.
**Files:** `01_Website/src/pages/YourHomePage/components/ActivityFeed.tsx`
**Recurrence:** First. Note for future: `toLocaleDateString` without a fixed format string is a hydration hazard inside any SSR component. Numeric `toLocaleString("en-AU")` is usually safe (thousand separators consistent), but date formatting is not.

## [HOUSE-MINI-SITE-004] Competitor map — interactive Mapbox (replaces static JPG) — 10:24 AEST
**Symptom:** Market tab showed a static competitor_map.jpg image — no interactivity, no popups, no per-listing detail, and the "1 of 4" combinatorial-match claim couldn't be visually verified by clicking through the actual matches.
**Root cause:** v0.2 shipped with a placeholder JPG; interactive map was deferred until tab structure settled.
**Fix:** New `CompetitorMap` component (Mapbox GL JS, light-v11 style) with three marker tiers — subject (copper, pulsing aura), combinatorial matches (sun yellow, ringed), general competitors (birch grey dot). Click → popup with address, price, bd/ba/car, feature chips, days-on-market. Legend bottom-left. Reused the existing public Mapbox token from MapboxChoropleth. Client-only mount gate (`mounted` state + `useEffect`) keeps SSR + hydration clean. Skeleton placeholder keeps layout height stable while map loads. Seeded `competitorMap` in fixture with subject + 3 combinatorial matches + 12 general competitors using real Gold_Coast active-listing addresses and suburb-plausible approximate coordinates (no geocoded lat/lng on active listings yet — TODO when cadastral geocoder lands).
**Files:**
- NEW: `01_Website/src/pages/YourHomePage/components/CompetitorMap.tsx`
- NEW: `01_Website/src/pages/YourHomePage/components/CompetitorMap.module.css`
- MOD: `01_Website/src/pages/YourHomePage/data/homeFixture.ts` (Competitor + CompetitorMapData types, 15-listing seed)
- MOD: `01_Website/src/pages/YourHomePage/tabs/MarketTab.tsx` (swap JPG → component)
**Recurrence:** First

## [HOUSE-MINI-SITE-005] Positioning tab — 10:38 AEST
**Symptom:** Concept.md called for a Positioning section ("vocabulary we may use to build emotion about the property, trade-offs linked to value, photography, demonstrate our ability to correctly position this specific home") — never built. Sellers had no way to see how Fields would actually write the listing for their home.
**Root cause:** v0.2 prioritised Valuation + Market tabs. Positioning required curated copy, not just data plumbing.
**Fix:** New `PositioningTab` covering:
  1. **Strategic frame** — single-sentence angle + reasoning the seller can audit.
  2. **Buyer in mind** — three personas (multi-generational family, school-catchment upgrader, local interstate-return buyer), each with feature-matched 'why this home' + named acquisition channels.
  3. **Vocabulary palette** — 7 approved phrases each anchored to a verifiable fact; an 11-word "avoid" list (forbidden CLAUDE.md words plus agent clichés) with line-through styling.
  4. **Trade-offs framed as value** — 4 apparent flaws (internal floor area below median, no year-built, flood overlay, cul-de-sac low foot-traffic) with reframe + cohort evidence for each.
  5. **Photography brief** — 6 shots, each with a concrete brief and a named claim it substantiates.
  6. **Worked sample paragraph** — 120-word opener in Cormorant Garamond, derived from angle + personas + vocabulary + trade-offs.
  7. **Honest-by-design footer** — explicit invitation to challenge any phrase before campaign.
All copy editorial-compliant (no advice, no predictions, no forbidden words, value framing, sourced claims). Tab inserted between The Market and What Happens Next; hash route `#positioning`. Fixture extended with `PositioningData` + 6 sub-types (frame, persona, vocabulary, tradeOff, photographyShot).
**Files:**
- NEW: `01_Website/src/pages/YourHomePage/tabs/PositioningTab.tsx`
- MOD: `01_Website/src/pages/YourHomePage/YourHomePage.tsx` (TabId + TABS + render switch)
- MOD: `01_Website/src/pages/YourHomePage/data/homeFixture.ts` (PositioningData types + content)
**Recurrence:** First

## [HOUSE-MINI-SITE-006] Hydration warning persisted after date-formatter fix — hash-driven tab state — 10:48 AEST
**Symptom:** After deploying the deterministic date formatter (HOUSE-MINI-SITE-003), React #418 hydration mismatch was still firing on `/your-home/...#market`. The /home tab (no hash) was clean — hash-bearing URLs still warned.
**Root cause:** `useState<TabId>(() => parseTab(location.hash))` reads `location.hash` during the initial render. On Netlify SSR there's no hash (server emits "home" tab); on the client the hash is `#market` (browser emits "market" tab). Different initial tree → React refuses hydration, falls back to client re-render.
**Fix:** Always seed `activeTab = "home"` on the initial render. The existing `useEffect` that listens to `location.hash` already syncs the tab on client mount and on hash changes, so the user sees the correct tab a tick after hydration with no flicker (the layout above the tab content is identical across tabs).
**Files:** `01_Website/src/pages/YourHomePage/YourHomePage.tsx`
**Recurrence:** First (related to 003 — same class of bug, second instance). Pattern note: any `useState(() => readWindowOrLocation())` initialiser is a hydration trap. Default to a deterministic seed value, sync via useEffect.

## [HOUSE-MINI-SITE-007] Your Home activity feed refocus — 11:43 AEST
**Symptom:** Will reviewed the live feed (9 items) and flagged two generic articles ("How Accurate Is Your Domain Property Estimate?" and "How to Choose a Real Estate Agent on the Gold Coast") that had no specific bearing on 13 Terrace Court. Three cross-suburb 5-bd listings (Massachusetts Court, Straite Drive, Chatswood Court) were also weakly relevant. Section heading promises "what it means for you" but feed didn't enforce that test.
**Root cause:** Two filter bugs in `scripts/refresh_property_reports.py`:
  1. `new_articles_activity` matched the suburb name against article `html` body. A 5,000-word generic article mentioning Merrimac in passing slipped through.
  2. `new_listings_activity` had a single `competition_min_bedrooms` threshold (5+) and no cross-suburb feature gate. Robina + Varsity Lakes listings only price-band-matched the subject — no feature stack overlap required.
Also missing: any way to track the valuation moving over time, and any way to flag when one of the 6 named comps changes state (price, sells, withdraws).
**Fix:** Four-part refocus in `scripts/refresh_property_reports.py`:
  1. **Article filter:** dropped html-body regex. Article passes only if title contains the suburb at a word boundary (`\bMerrimac\b`) OR tags include the suburb / a subject-feature tag (e.g. `north-facing-rear`, `dual-living`). Effect-on-your-home line is tagged-to-feature-specific when a feature tag matched. Result: 2 generic articles dropped; 1 article ("Worongary Leads the Corridor… and Merrimac Records Year-on-Year Declines") legitimately passes with `\bMerrimac\b`.
  2. **Cross-suburb feature gate:** new `_cross_suburb_feature_match()` — listings outside subject's suburb additionally require bedrooms ≥ subject's bedrooms AND (pool in features OR land_area ≥ 600 m²). Named comps bypass the gate. Result: 3 weak cross-suburb 5-bd listings dropped.
  3. **`valuation_delta` activity kind (weekly cadence):** new `valuation_delta_activity()` compares current config valuation to last `valuation_history[]` entry. Emits when midpoint movement ≥ max($5,000, 0.5%); silent seed when no baseline exists. History persisted via `$push valuation_history`.
  4. **`comp_lifecycle` activity kind:** new `comp_lifecycle_activity()` queries each of the 6 named comps across all watch suburbs each run, compares to `comp_state_snapshots` keyed by normalised address, emits `comp_sold` / `comp_price_change` / `comp_withdrawn` on state transitions.

Persistence: `upsert_report` extended to set `comp_state_snapshots` every run and `$push valuation_history` when a new baseline lands. `build_activity_for_slug` now returns `(activity, side_state)` tuple.

Frontend: `ActivityItem.kind` union extended with `valuation_delta`, `comp_sold`, `comp_price_change`, `comp_withdrawn` in `homeFixture.ts`. `ActivityFeed.tsx` adds labels + reuses existing badge classes (valuation_delta → valuation badge; comp_* → comp_on_market badge; withdrawn → competitor badge).

Verification: dry-run + live-run on `13-terrace-court-merrimac` produces 5 items (down from 9, all subject-specific), `valuation_history[]` seeded (1 entry, no emit yet), 6 comp snapshots tracked (8 Trinity Place captured at $2.3M for_sale; other 5 unknown — out of DB after sale rolloff).

**Decisions deferred (Will, 2026-05-13):** buyer-interest signal from PostHog skipped (not enough traffic yet). Weekly email digest shelved (no lead capture yet).

**Files:**
- MOD: `scripts/refresh_property_reports.py` (article filter, cross-suburb gate, valuation_delta_activity, comp_lifecycle_activity, build/upsert signatures)
- MOD: `01_Website/src/pages/YourHomePage/data/homeFixture.ts` (kind union)
- MOD: `01_Website/src/pages/YourHomePage/components/ActivityFeed.tsx` (kind labels + badges)
**Recurrence:** First

## [HOUSE-MINI-SITE-008] MatchCards — "the other three" beneath the competitor map — 12:14 AEST
**Symptom:** Market tab made the "1 of 4 of 142" scarcity claim verbally but not visually. The seller saw dots on a map and a sentence. The 3 alternative properties carrying the same feature stack were named in popups only — no photos, no specific comparison framing.
**Root cause:** v0.5 stopped at the map. Cards were considered a "Domain search" risk but the comparison framing (specific difference vs subject) flips that risk into proof of the scarcity claim.
**Fix:** New `MatchCards` component rendered directly below `CompetitorMap`. Filters `competitorMap.competitors` for `combinatorialMatch: true` (the 3 matches, not all 15). Each card: hero photo (pulled from Azure Blob via the active-listings pipeline), suburb badge, address (Cormorant Garamond), price, bd/ba/car, matched-feature chips (Pool, Dual living, Cul-de-sac), and the critical `differenceVsSubject` line — what makes the subject's variant unique. The 12 general competitors stay map-only on purpose (avoids the "Domain search results" feel).
- Heroes: real listing photos from `https://fieldspropertyimages.blob.core.windows.net/.../photos/00.jpg` for each of the 3 matches.
- Comparison copy: e.g. 28 Federal Place — "Eight bedrooms but no bushland adjacency"; 20 Vanderbilt Court — "Same bedroom count but in Varsity Lakes, no walking-distance school catchment"; 22 Nicklaus Court — "Six bathrooms but rear boundary is residential, not Kingary Wetland Reserve".
- Editorial: each card frames the alternative as comparison ("difference vs your home" sub-section), not as a recommendation. No "view on Domain" CTA — keeps the seller on the report.
- Extended `Competitor` type with `imageSrc` + `differenceVsSubject` optional fields. Used by 3 matches; general competitors leave them blank.
**Files:**
- NEW: `01_Website/src/pages/YourHomePage/components/MatchCards.tsx`
- NEW: `01_Website/src/pages/YourHomePage/components/MatchCards.module.css`
- MOD: `01_Website/src/pages/YourHomePage/data/homeFixture.ts` (type + 3 match seeds)
- MOD: `01_Website/src/pages/YourHomePage/tabs/MarketTab.tsx` (filter + render)
**Recurrence:** First

## [HOUSE-MINI-SITE-009] MatchCards photos 403 — Azure blob account disabled — 12:24 AEST
**Symptom:** Will reported the 3 MatchCard hero photos rendered as broken images. Investigation found all three Azure blob URLs (`fieldspropertyimages.blob.core.windows.net/...`) return HTTP 403 with body "The specified account is disabled" — site-wide, not just our 3 cards.
**Root cause (immediate):** Fixture seeded match-card photos with the Azure blob mirror URLs. The blob storage account is currently disabled.
**Root cause (systemic):** This affects EVERY property image across the site — `/for-sale`, `/property/:id`, `/discover`, etc. all reference `fieldspropertyimages.blob.core.windows.net` paths. Confirmed by grepping live `/for-sale` HTML.
**Fix (immediate):** Repointed the 3 match-card `imageSrc` values to Domain CDN URLs (`https://bucket-api.domain.com.au/v1/bucket/image/<id>`). Verified each returns 307→200 with `Content-Type: image/jpeg`. Screenshot confirms photos render correctly.
**Files:** `01_Website/src/pages/YourHomePage/data/homeFixture.ts`
**Recurrence:** First (for these cards). Related to the broader blob outage — flagged separately to Will.
**Open issue — escalate:** The Azure blob storage account `fieldspropertyimages` is currently disabled. ALL property images across the live website are 403'ing. This is a P0 visual regression affecting every property listing page on `fieldsestate.com.au`. The Domain CDN URLs (in `property_images_original` or `scraped_property_images` on each listing doc) provide a working fallback for current active listings but lose images for withdrawn/sold listings the moment Domain rotates them.

## [IMAGE-OUTAGE-001] Azure blob storage account disabled — site-wide property image outage — 12:00 AEST
**Symptom:** Every property image across `fieldsestate.com.au` returns broken-image icon — `/for-sale` thumbnails, `/property/:id` galleries, `/discover` feed, MatchCards on the seller mini-site. Image hostname `fieldspropertyimages.blob.core.windows.net` returns HTTP 403 with body "The specified account is disabled".
**Root cause:** Azure Blob storage account was shut down as part of the migration to local MongoDB. The structured data migrated to `localhost:27017` (1.4 GB at `/var/lib/mongodb`); the image binaries did not — they were the only thing the local migration didn't capture. There is no local image archive on the VM. ~20,000 cadastral + active-listing records still have `property_images[]` arrays pointing at the dead Azure URLs.
**Investigation findings:**
- Image binaries NOT in MongoDB (no GridFS, no binary fields on listing docs).
- Domain CDN URLs (`bucket-api.domain.com.au/...`, `rimh2.domainstatic.com.au/...`) on listing docs in `property_images_original` and `scraped_property_images` ARE working — they're the same URLs the scraper pulled images from originally. Effective for currently-advertised listings; Domain rotates them when listings withdraw.
- Database has 28,285 off-market cadastral records across 4 target suburbs (Merrimac, Robina, Varsity Lakes, Burleigh Waters). 73% (~20,650) were scraped 6+ months ago and need refresh.

**Fix (Phase A — live):** All Netlify functions that read `property_images[]` now filter dead Azure URLs and prefer Domain CDN sources. Patched files (all pushed):
- `01_Website/netlify/functions/shared-utils.mjs` — new `isLiveImageUrl()` + comprehensive `extractPhotos()` helper
- `01_Website/netlify/functions/properties-for-sale.mjs` — extractPhotos replaced
- `01_Website/netlify/functions/property.mjs` — extractPhotos replaced
- `01_Website/netlify/functions/discover-feed.mjs` — `isValidImageUrl` inverted to reject Azure
- `01_Website/netlify/functions/decision-feed-v3.mjs`, `decision-feed.mjs`, `recently-sold.mjs`, `analyse-property.mjs` — extractPhotos replaced
All readers also now prefer (in order) the new `domain_hero_image_url` + `domain_image_urls` + `scraped_data_v2.image_urls` fields populated by the v2 scraper.
Verified live: `/for-sale` Robina sample returns 9/20 Domain CDN URLs, 0 Azure. Screenshot confirms photos rendering across the page.

**Fix (Phase B — running):** New `scripts/scrape_property_profiles.py` re-pulls cadastral data from Domain property-profile pages (`/property-profile/<slug>`) via the existing Bright Data Web Unlocker. Parser extracts: structured address, beds/baths/parking, land/internal area, lot+plan, hero image URL (working — Domain CDN, no Akamai), valuation (low/mid/upper), rental estimate, sale timeline, 6 comparable sales. Writes to new fields (`scraped_data_v2`, `scraped_at_v2`, `domain_hero_image_url`, `domain_image_urls`) — non-destructive to existing data.

Test on 50 properties: 46 parsed + written successfully (92% success), 4 fetch failures (URLs that don't exist on Domain), 0 parse failures. Data quality verified — valuations, rentals, timelines, comparables all populated; hero image URLs return 200 OK from rimh2.domainstatic.com.au.

**Full pass status (as of 14:30 AEST 2026-05-13):**
- Process: PID `3365564`, running in background via `nohup`
- Queue: 28,774 properties across 4 suburbs
- Throttle: `--rate 2` (1 request every 2s to Bright Data) per Will's spec
- Workers: 30 concurrent
- Log file: `logs/scrape/full_pass_20260513_1427.log`
- Observed rate: ~0.11-0.23 req/sec (slower than the 0.5/sec dispatch target — Bright Data Web Unlocker latency dominates)
- **Projected ETA at observed rate: 35-70 hours** (vs 16h estimate). Tomorrow morning Will will see ~50-70% completion if rate holds.

**Monitor / control commands for Will's return:**
```bash
# Check progress
tail -50 /home/fields/Fields_Orchestrator/logs/scrape/full_pass_20260513_1427.log
ps -p 3365564 -o pid,etime,rss,cmd

# Count completed
source /home/fields/venv/bin/activate
python3 -c "
import sys; sys.path.insert(0, '/home/fields/Fields_Orchestrator')
from dotenv import load_dotenv; load_dotenv('/home/fields/Fields_Orchestrator/.env')
from shared.db import get_client
import datetime as dt
c = get_client(); db = c['Gold_Coast']
cutoff = dt.datetime.utcnow() - dt.timedelta(hours=24)
for s in ['merrimac','robina','varsity_lakes','burleigh_waters']:
    n = db[s].count_documents({'scraped_at_v2': {'\$gte': cutoff}})
    print(f'{s}: {n}')
"

# Kill (script is resumable — picks up where it stopped)
kill 3365564

# Resume at faster rate (after killing the existing one)
cd /home/fields/Fields_Orchestrator
source /home/fields/venv/bin/activate
nohup python3 scripts/scrape_property_profiles.py --rate 1 --workers 60 \
  --log-file logs/scrape/full_pass_resumed_$(date +%H%M).log > /dev/null 2>&1 &
```

**Open questions for Will:**
1. **Bright Data spend** — Test of 50 properties used 50 requests. I couldn't determine per-request cost programmatically. At rough estimate $0.025/URL the full pass = $700; at $0.005/URL = $140. Worth checking Bright Data billing dashboard.
2. **Throttle rate** — Spec was 1 req/2s. Observed actual is much slower than that target. Probably safe to bump to `--rate 1` or even `--rate 0.5` (halving / quartering ETA) since current request volume is moderate.
3. **Image durability** — v2 scrape stores Domain CDN URLs, not binary files. Same vulnerability as `property_images_original`: Domain rotates URLs on listing withdrawal. For true durability we'd download each hero image to disk + serve via a Netlify proxy function. Defer until current pass is done.

**Files (this fix):**
- NEW: `Fields_Orchestrator/scripts/scrape_property_profiles.py`
- NEW: `Fields_Orchestrator/shared/domain_fetch.py` (was already wired by morning fix)
- MOD: 8 Netlify functions in `01_Website/netlify/functions/`
- All pushed to GitHub on respective repos.
**Recurrence:** First

---

## [WATCHDOG] Step 113 (Detect Withdrawn Properties (Target Market)) — 18:06 AEST

**Failure class:** UNKNOWN
**Cause:** No recognizable error pattern — check logs manually
**Action taken:** ESCALATED to Claude (id=watchdog_113_1778659581)
**Result:** queued — awaiting repair-agent or Claude
