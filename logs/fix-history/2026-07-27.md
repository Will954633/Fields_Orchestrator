# Fix History — 2026-07-27

## [OFFMARKET-LOGO-LOCKUP] Topbar logo → full Fields hero lockup (wordmark + F-icon) — 17:35 AEST
**Symptom:** Will: the topbar showed only the "Fields" wordmark, missing the F-wing icon beside it. He wants the full lockup (pasted the birch hero image; pointed to `2-Birch/• PNG/1-Fields-Hero-Grass.png`).
**Fix:** Copied that hero lockup → `src/assets/fields/fields-hero-birch.png` (1194×360, cream wordmark + F-wing icon) and repointed the deck's logo import to it (from `fields-wordmark-birch.png`). `.brandLogo` height 22px → ~73px wide, fits the 52px topbar.
**Verified:** build clean; live headless screenshot read — "Fields" + F-wing icon now render top-left on the dark deck. 0 errors.
**Files:** `src/pages/OffMarketPage/OffMarketDeck.tsx` (import), `src/assets/fields/fields-hero-birch.png` (new). Website commit 6d5848d9. (Supersedes the wordmark-only choice in the entry below.)
**Recurrence:** First.

## [OFFMARKET-CARD1-COPY-LOGO] Card-1 copy refinements + real Fields wordmark logo — 17:15 AEST
**Symptom:** Will's iterative copy direction on the new card-1: warmer 3-line intro, shorter/less-generic option labels, a more human sale line, and the real logo (not the "Fields ▪" text) in the topbar.
**Fix:** (1) Intro → "Hi!" / "We're Fields Real Estate." / "We help local owners understand their property, the market and their options." (new `MENU_GREETING`/`MENU_NAME`/`MENU_INTRO` + `heroMenuGreeting`/`heroMenuName` CSS). (2) Sell label "…would sell in today's market" → "See how this home might sell today". (3) Market label made per-suburb via `menuOptions(suburb)` → "Get the latest Robina market update". (4) Sale line humanised via new `PRICE_K` (nearest $1,000), `MONTH_YEAR_LONG` (full month), `HUMAN_YEARS` ("almost 23 years") → "Last recorded sale: $326,000 · August 2003 · held for almost 23 years". NOTE: rounding the last-sale $ softens an exact transaction vs the "exact prices" editorial rule — done at Will's explicit request for a hook line, flagged to him. (5) Real Fields **wordmark** logo (birch PNG from the logo pack) in the topbar via `.brandLogo`, theme-aware (darkened on the light arm). HUMAN_YEARS threshold tightened 0.15→0.05 so 22.9 reads "almost 23 years" not "23 years" (Will's spec).
**Verified:** `react-router build` clean; live headless (mobile, dark arm) — screenshot read: logo renders top-left, 3-line intro + labels + "$326,000 · August 2003" all correct; "almost 23 years" confirmed post-threshold-fix. 0 errors.
**Files:** `src/pages/OffMarketPage/{flowContent.ts,OffMarketDeck.tsx,ladderShared.ts,OffMarketDeck.module.css}`, `src/assets/fields/fields-wordmark-birch.png` (new). Website commits b851402a (one Trees-API commit) + fad8972d (HUMAN_YEARS threshold). Follows [[offmarket-deck-redesign]] earlier same session.
**Recurrence:** First.

## [OFFMARKET-DECK-REDESIGN] Fused hero+intent menu (card 1); removed ownership gate + generic market cards — 16:40 AEST
**Symptom:** Off-market deck bled ~95% across 7 market-education cards before any ask (measured funnel: only 3 of 59 hero-viewers reached the ownership gate card, 0 answered it). Will directed a redesign: fuse an intent menu into card 1, remove the "Is this your home?" gate, make market content opt-in.
**Root cause (design, not bug):** The deck force-marched every visitor through hero → value-range → 5 generic market cards → a gated ownership card. Market macro content isn't what an owner who Googled their own address came for; it sat between them and their home's story.
**Fix (Will's redesign, built + shipped this session):** Card 1 is now the fused hero (their home + address = the personalised hook) + a "What would be most useful right now?" menu with 4 options. `sell` → sets owner tone + drops into the tightened house flow (value-range → capital-gain → the one house-relevant segue → positioning → plan; the 5 generic market cards are GONE from this path — deck went 12→9 cards). `market` → links out to `/market-metrics/<suburb>`. `surprise` → one address-specific data nugget (comparable range + last sale) + soft CTA into the sell flow. `else` → a free-text box (recorded). Ownership gate card removed; its intent signal is now captured up-front by the menu (picking `sell` = owner). Each option fires a distinct `offmarket_menu_*` PostHog event + writes intent server-side via the existing offmarket-qualify endpoint. Intro copy dropped "not pressure" (Will: don't be negative).
**Verified:** `react-router build` clean; live headless (mobile, dark arm) — card 1 renders the fused hero+menu, gate holds ("Choose one to continue" + disabled arrow), clicking `sell` selects it, rebuilds the deck to 9 cards, and unlocks the forward CTA "See what it may be worth" → value-range. 0 console/network errors. Screenshots read.
**Files:** `src/pages/OffMarketPage/OffMarketDeck.tsx`, `flowContent.ts`, `OffMarketDeck.module.css`. Website commits 78d9b16a (redesign, one Trees-API commit) + 063c0c51 (copy). Deploy-tracked + change_ledger `2026-07-27_off-market-deck-fused-hero-intent-menu-c` (metric offmarket_reach_ask, baseline 3/59-reach-0-answer, review 2026-08-03/10). Revert = restore parent of 78d9b16a.
**Recurrence:** First. Diagnosis: [[offmarket_deck_card_dropoff_2026-07-27]].

## [STALLED-PROJECTS-CHECK] Mechanical scoped-but-unbuilt backlog-debt check — 16:05 AEST
**Symptom:** Behavioural charter rules now tell Samantha to discharge scoped-but-unbuilt project debt, but nothing made the debt VISIBLE/trackable so it couldn't quietly regress.
**Root cause:** No mechanical surfacing of Backlog items stuck at Scoped/Proposed/Open across sessions.
**Fix:** Added a "Scoped-but-unbuilt projects" check to main_site_health_check.py CEO Governance. Reads the Task Board Backlog, classifies each row (DONE/superseded, PARKED-on-Will/blocked, or STALLED=owed debt) via status/result text, counts the stalled bucket + any dated >14d, and flags ERROR at >=6 / STALE at >=3 with the oldest examples. Lands on the Systems Health sheet nightly. First run: 27 scoped/proposed-but-not-shipped (ERROR) — directional (heuristic over freeform status text) but honest; trends down as she ships or parks-on-Will with a reason. Complements the [SAMANTHA-SCOPED-DEBT] charter rules.
**Files:** scripts/main_site_health_check.py.
**Recurrence:** First.

## [SAMANTHA-SCOPED-DEBT] Orange=work-done not reply-done; scoped projects are debt to discharge — 15:45 AEST
**Symptom:** Will asked why Samantha never picked up previously-flagged-but-unbuilt projects (Brain 1 development, AI-as-distribution / GPT-referral, science-data FB ads, JustCall, etc.). Verified: the Will Notes doc marked those project paragraphs ORANGE after a *reply/scope* ("Answered/actioned in full: the AI-distribution-channel opportunity, brain 1..."), and orange = never-re-read = buried. The Task Board Backlog holds 42 open rows, many stuck at "Scoped / Proposed / Reviewed / Answered / Open" for weeks with no build progress, while each session picks up new reactive work instead.
**Root cause:** Two behaviours — (1) orange was earned by *replying/scoping/reviewing*, not by delivering, so build/project asks got orange-buried after a thoughtful answer; (2) "Scoped" was treated as a terminal resting state, so owned projects accumulated as permanent debt.
**Fix (behavioural charter edits, not doing her tasks):** (a) "Replying to Will Notes" section — ORANGE means the WORK is DONE, not that you replied: a QUESTION answered can go orange, but a PROJECT/BUILD ask is done only when delivered; scoped-but-unbuilt items stay ACTIVE (un-orange) or become a Backlog row that isn't closed until built — scoping is a promissory note, not delivery. (b) Session-end sweep dimension #2 — a project stuck at Scoped/Proposed across sessions is a FINDING not a resting state; each session, before new reactive work, advance the oldest scoped-but-unbuilt project you own (or park-on-Will with a dated reason), and NAME the advanced project in the summary.
**Files:** scripts/samantha/charter.md.
**Recurrence:** First (related to [SAMANTHA-BEHAVIOUR] earlier today).

## [SAMANTHA-BEHAVIOUR] Funnel-movement-first reporting + "a failure is yours to diagnose" — 15:20 AEST
**Symptom:** Post-session review of Samantha's 2026-07-27 run (a strong session overall) found two behavioural gaps: (1) her summary led with a task list, not the north-star FUNNEL MOVEMENT trend (she rolled the KPIs to the sheet but never narrated what moved); (2) she bundled a technical failure ("two article workflows are failing") into a Will-accountability item without diagnosing the failure — conflating a bug she owns with a publish decision that is Will's.
**Root cause:** Charter had the principles nearby (review processes not just numbers; funnel-movement report section) but no hard rule that the summary must OPEN with funnel movement, and no explicit anti-pattern separating "a process is erroring (mine to diagnose)" from "a judgment call (Will's)".
**Fix:** Behavioural charter/task edits (not doing her tasks): (a) charter "Who you are" — added principle "A FAILING process is YOURS to diagnose — never hand Will a bug as if it were a decision" (diagnose first; escalate only the genuine judgment call). (b) charter rule 9 session-summary format — added a mandatory "Funnel movement (LEAD WITH THIS)" opener before Done-this-session. (c) daily_tasks Task C — rolling numbers into the sheet is NOT reporting; must write the funnel-movement narrative that opens the summary.
**Files:** scripts/samantha/charter.md, scripts/samantha/daily_tasks.md.
**Recurrence:** First.

## [MAILOUT-QR-ATTRIBUTION] Per-address QR + completed engaged off-market reports so they're mailable — 14:20 AEST
**Symptom:** Will's mailout ask (post printed positioning packages to no-phone engaged owner-lookups) — but (a) the mailer QR carried only a campaign-level UTM, so before/after engagement uplift wasn't measurable per posted address, and (b) the most-engaged owner-lookup reports (201/50 Riverwalk 25-min dwell, 213 Acanthus 4 visits, 12 Beaconsfield withdrawn) were REFUSED by the mailer generator ("not ready — missing: buyer_desc").
**Root cause:** (a) `make_qr()` used a fixed campaign UTM with no per-address key. (b) The off-market PREWARM (`offmarket_ladder_prewarm`) builds only a partial report — it resolves comps/scarcity/competitor slots but never runs the positioning→personas→buyers LLM chain, so `slot_status.positioning` stays unapproved and `positioning.personas` is empty; the mailer gate requires a `buyer_desc` (humanised persona), so those reports can't print. Slot approval is automatic on successful generation (slot_resolver.py), so this is a build-completeness gap, not a QA gate.
**Fix:** (a) Added `&utm_content=<slug>` to the mailer QR in `11_House_Mini_Site/mailer/generate_mailers.py::make_qr` — each scan now attributes to one posted address, so before/after uplift is measurable per-address. Documented method + re-measure query in `scripts/samantha/mailout_uplift_tracking.md` (new). (b) Ran the canonical `python3 -m scripts.property_reports.build_property_report --slug <X> --force` (idempotent, additive) on the engaged batch to complete the positioning slot — unlocked the top 3 engaged addresses. Generated 6 bespoke mailers → `output/all_mailers.pdf`. ~6 more engaged addresses still rebuilding in background (Overpass 429s on walking-distance POIs slowed it).
**Follow-up (queued, not done):** wire the positioning/persona slot into the off-market prewarm so future prewarmed reports are mailable without a manual `--force`.
**Files:** `11_House_Mini_Site/mailer/generate_mailers.py`, `scripts/samantha/mailout_uplift_tracking.md` (new). DB: positioning slots completed on ~6 property_reports (additive).
**Recurrence:** First.

## [OFFMARKET-DECK-FUNNEL] Quantified off-market deck card dropoff (Will item 2) — 13:50 AEST
**Symptom:** Will observed visitors drop off on the first question card ("are you the homeowner?") on the off-market deck; asked Samantha to quantify.
**Root cause (investigation):** Verified instrumentation is sound first (card_viewed fires 144x/30d, so the zeros are real dropoff not a wiring bug — per the "familiar cause hardest" rule). Card-by-card funnel: hero 59 → value-range 38 → market-direction 12 (−68%, steepest) → capital-gain 10 → market-now 9 → market-drivers 8 → market-segue 5 → ownership 3 → 0 answered. Only 3 of 59 hero-viewers reach the ownership question; the deck bleeds ~95% across the 7 market cards BEFORE the ask. The ownership card is a symptom, not the disease.
**Fix:** No code change (Will owns the deck change). Recorded to memory `offmarket_deck_card_dropoff_2026-07-27`; queued hypothesis (shorten pre-ask sequence) to `hypothesis_queue` (score 9, website). Replied on Will Notes.
**Files:** none (analysis).
**Recurrence:** First.

## [POSTHOG-MCP-PERMS] Granted all PostHog MCP tools (stop the repeated prompts) — 14:50 AEST
**Symptom:** Samantha kept getting permission prompts for PostHog MCP tools in the interactive Claude Code channel.
**Root cause:** .claude/settings.local.json permissions.allow only listed the single tool `mcp__posthog__exec`; `enableAllProjectMcpServers` connects the server but per-tool calls still need an allow rule, so every other posthog tool (query, insights, dashboards, feature-flags, experiments, etc.) prompted. (The nightly run is unaffected — it uses bypassPermissions.)
**Fix:** Added the server-level rule `mcp__posthog` (grants ALL tools of the posthog server) to permissions.allow in .claude/settings.local.json (the file the interactive channel reads — immediate fix), and created project .claude/settings.json with the same rule for a durable, shared, backed-up grant. Both JSON-validated.
**Files:** .claude/settings.local.json, .claude/settings.json (new).
**Recurrence:** First.

## [WEB-RESEARCH-AWARENESS] Told Samantha she can do web research / spawn a research agent — 14:35 AEST
**Symptom:** Will asked whether Samantha can call a web-based research agent. She CAN (verified: daily_run.py sets no tool allow-list + bypassPermissions on the claude_code preset, so WebSearch/WebFetch + Task subagents are available; interactive channel also has the deep-research skill) — but her charter never told her to reach for it, so it was likely under-used (same tools-go-unused-without-a-nudge pattern as hypothesis_queue).
**Root cause:** Capability present, awareness/instruction absent.
**Fix:** Added a "Web research — you can search the web and spawn a research agent" section to charter.md: WebSearch/WebFetch for external info (competitor playbooks, case studies, vendor/pricing, policy/market context, Brain-1 source papers); spawn a Task research subagent for deep multi-source questions; deep-research skill interactively; capture findings to memory/scoping/hypothesis with citations (grows Brain 1); honesty guardrail (cite source, web claim != our tested result, Brain 1 vs Brain 2 line).
**Files:** scripts/samantha/charter.md.
**Recurrence:** First.

## [PROPRADAR-ACCESS] Made Samantha aware she can pull PropRadar data — 14:20 AEST
**Symptom:** Will wanted Samantha to know she has access to PropRadar (independent title/settlement property data) so she can pull data we don't currently have, when needed.
**Root cause:** The integration + key existed (scripts/propradar/propradar_client.py, key in the secrets file, memory propradar_api.md) but her charter never told her it was an available, standing data source to reach for.
**Fix:** Added an "External data you can pull — PropRadar" section to charter.md: how (propradar_client.call("/suburbs/QLD/{Suburb}"), key auto-loads, never print/commit), the cheap high-value suburb-snapshot call, the hard FREE-tier budget (50 calls/month, ~46 left — watch x-ratelimit-remaining, don't loop), and when to use it (verify VOLUME/months-of-supply which our Domain scrape under-captures ~2x). Verified the key loads without spending an API call.
**Files:** scripts/samantha/charter.md.
**Recurrence:** First.

## [SAMANTHA-OPS-HARDENING] Measurement loop + OAuth tracking + live KPIs + impact view — 14:00 AEST
**Symptom:** Will's review of Samantha-as-asset flagged: (2) she ships but never measures (change_ledger due showed 3 changes from 17 Jul never read); (3) KPI dashboard had MEASURE placeholders (PostHog never wired, website_daily_metrics broken); (4) Drive OAuth (7-day testing-mode expiry) fails silently with no visibility; (5) no view of her actual value.
**Root cause:** measurement + OAuth health + KPI population + impact were all unmonitored/unbuilt; the intended nightly cadence is also intentionally OFF (on-demand) so nothing self-drives them.
**Fix:** (2) Added a "Change measurement loop" check to main_site_health_check.py CEO Governance (flags overdue change_ledger checkpoints, using the ledger's real review_dates logic) + closed the 3 open loops honestly (all confounded by the FB billing outage: spend->$0 nominal-improved-but-confounded; AYH copy test 0 leads = no fair chance; meta-desc organic /property = 27 vs 46 baseline via HogQL, DO-NOT-revert, indexation still ramping). (3) New kpi_rollup.py `rollup` populates Dashboard 'Latest' + a Weekly Log row from HogQL (engaged 44, organic 172) + Mongo funnel 7d; wired into Task C. (4) Added "Drive OAuth token" check to CEO Governance (OK/FAIL + age) so an expiry shows on the Systems Health sheet (publishing the OAuth app not an option yet). (5) kpi_rollup.py `impact` builds a "Samantha Impact" tab (actions, ship:analysis, change verdicts, measured %, ~cost). Also recorded ON-DEMAND cadence as intentional in charter (do not re-enable nightly cron).
**Files:** scripts/main_site_health_check.py, scripts/samantha/kpi_rollup.py (new), scripts/samantha/charter.md, scripts/samantha/daily_tasks.md.
**Recurrence:** First.

## [ACTION-LOG-COVERAGE] Interactive-channel auto-harvest + weekly Telegram review — 13:05 AEST
**Symptom:** The unified action log auto-captured only the NIGHTLY run; the interactive Claude Code channel (where Will directs Samantha) was not auto-harvested, and improvement targets had to be pulled manually.
**Root cause:** Only daily_run.py harvested (its own transcript); no mechanism for the 7,447 interactive session JSONLs, and no scheduled push of the report.
**Fix:** Reworked `samantha_actionlog.py` to be INCREMENTAL (stable key `source:line:block` + per-file line cursor in `samantha_actionlog_state`, so re-runs only parse new lines — the interactive dir has 50MB+ files). Added `harvest-interactive` (harvests recent Claude Code session files, channel=interactive) and `report --telegram`. Two crons (AEST): hourly `harvest-interactive` (:25) + weekly review `report --days 7 --telegram` (Mon 07:30). Verified: interactive harvest captured 5,865 rows from 12 recent sessions in 13s; incremental re-run added only 6; report aggregates both channels (nightly 276 + interactive 5,871) and surfaces analysis:ship + skipped-Task-G + repeat-command targets. Sent Will a preview of the weekly Telegram.
**Files:** scripts/samantha/samantha_actionlog.py (reworked); crontab (2 entries added).
**Recurrence:** First.

## [PROPRADAR-COVERAGE-B] Coverage-gap validation + enrichment mechanism proven; article re-gen triggered — 14:45 AEST
**Workstream B (coverage gaps → off-market SEO), first steps:** `validate_coverage_gaps.py` triaged the 30 gaps → 28 enrichable, 2 invalid marked (DETACHED property-type leak; postcode 4000≠4227). `enrich_coverage_gaps.py` proves the "PropRadar discovers → our Domain scraper enriches" flow: builds the Domain property-profile URL, fetches via Bright Data (shared.domain_fetch; raw curl_cffi is Domain-blocked), parses via domain_profile_scraper._extract_from_html, writes to STAGING `Gold_Coast.propradar_gap_enriched` (not the live suburb collection — promotion to public off-market pages needs deliberate url_slug + twin-dedup handling per [[offmarket_property_twin_dedup]]). Tested on 73/1 Vue Boulevard (a gap PropRadar had, we didn't): recovered FULL 3-sale history (2026 $1.0M / 2025 $950k / 2021 $665k) — reconfirms Domain profiles are the deep-history source (PropRadar /history gave only the 2026 sale). Queue is mostly apartment units in towers (7 in Christine Ave 397). **Article re-gen TRIGGERED** on PR-corrected data: dispatched fields-automation market_monitor.yml (completed success) + market_insight.yml (running) via workflow_dispatch on origin/main.
**Files:** scripts/propradar/{validate_coverage_gaps,enrich_coverage_gaps}.py (new, pushed).
**Recurrence:** First. Remaining B: batch-enrich 28 + promote staging→live docs→off-market pages→sitemap (gated step).

## [PROPRADAR-ARTICLEGEN] Article generator volume/supply → PropRadar; Robina main flow verified — 14:30 AEST
**Symptom:** The weekly article generator (fields-automation run_market_monitor.py) still computed sales_volume_trailing12 + months_of_supply from the under-captured Domain scrape.
**Fix:** `extract_suburb_metrics` now overrides sales_volume_trailing12 = PR house_sales_12mo and months_of_supply = PR house_inventory_months (from Gold_Coast.propradar_suburb_stats), and NULLs volume_vs_prior_12 (PropRadar gives a 12mo total, not a prior-year comparison — so we don't assert an unsourced volume trend, which was the "climbs off multi-year low" failure mode). Price/DOM already flow from the PR-recalibrated precomputed_* collections (single source of truth). Tested all 3: vol 387/240/205, mos 2.18/3.57/1.77, median 1450000/1710000/1350000, yoy 10.9/12.2/15.5. Pushed to Will954633/fields-automation (CI uses origin/main). Next weekly run regenerates articles on correct data.
**Robina main-flow verification (Will asked):** live https://fieldsestate.com.au/market-metrics/Robina — headless text scan of all 7 categories = 0 contradictions (median $1,450,000/$1,456,067 recalibrated chart pt, growth 10.9%, absorption 2.2 houses, volume recalibrated, 387 house sales, DOM 28; asking $1,725,311 correctly labelled distinct from median). Visual screenshot read: renders clean, 0 console/network errors, all charts present. **Main flow FIXED + verified (text + visual).**
**Files:** fields-automation/pipeline/run_market_monitor.py (commit on origin/main).
**Recurrence:** First.

## [PROPRADAR-RESIDUALS] AbsorptionRateChart deploy + prose re-gen + stale narrative.pillars landmine cleared — 14:10 AEST
**Symptom:** Two residuals from the publish: (1) AbsorptionRateChart still showed 2.9mo all-types-30d vs 1.77 houses; (2) market_pulse prose cited raw quarterly counts (22/12) pre-recalibration.
**Fix:** (1) `market-insights.mjs` absorption block now prefers PropRadar house_inventory_months (settlement-based) from propradar_suburb_stats + aligns the absorption_rate_snapshot so SSR matches; `AbsorptionRateChart.tsx` renders a house/PropRadar-scoped card + disclaimer when source=propradar_houses. Built clean (react-router build), deployed as ONE Trees-API commit 59243663 (one Netlify build), deploy-tracked. (2) Re-ran full market_pulse (21/21 on PR, today) so prose picks up recalibrated transaction_count.
**§6 live-verify caught a THIRD path (the CLAUDE.md §6 landmine):** the stale `narrative.pillars` layer on the 3 sell-now docs still rendered "2.9 months (53 active, 18 sold last-30d)" AND the killed "volume is actually rising" Varsity story. No active producer exists for narrative.pillars (frozen stale, per §6's 2026-07-03 note); SellNowSection consumes it conditionally (`if pulseData?.narrative?.pillars`) and falls back to its own template using the corrected snap.absorption_rate_months. So `$unset narrative` on the 3 sell-now docs — safe + durable. Re-verified: all 3 sell-now pages 0 contradictions, single PR figure each (R 2.2 / B 3.6 / V 1.77-1.8). **§6 CLEAN across all three suburbs.**
**Files:** Feilds_Website/01_Website/netlify/functions/market-insights.mjs, src/components/MetricCharts/AbsorptionRateChart.tsx (website commit 59243663); live DB market_pulse (21 regen + 3 narrative unset).
**Recurrence:** 2nd occurrence of the stale-narrative-layer class (see [[market_metrics_frontend_content_gaps]] / §6 2026-07-23) — this time narrative.pillars on sell-now. Worth a producer/cleanup so it can't recur.

## [PROPRADAR-PUBLISH] Coordinated publish of PR-anchored median/growth/absorption/volume + §6 verify — 13:30 AEST
**Symptom:** Repointed metrics needed to go live consistently (median/growth/absorption/volume) without a piecemeal state that §6 forbids.
**Root cause:** N/A (rollout). Median+volume trend charts are absolute-level from our skewed sample, so a PR headline would contradict the chart line on the same card.
**Fix:** Method A (Will-approved) = rebase charts to PR anchor. `recalibrate_charts.py` (idempotent via *_raw): scales `precomputed_indexed_prices` median_price + transaction_count and `precomputed_market_charts` sales_count so trailing-12mo matches PR exactly; sets rolling_12m median/yoy = PR. Applied live (factors: median R×0.97/B×0.91/V×0.96, volume R×1.64/B×1.55/V×1.99). Regenerated all 21 market_pulse docs (3×7) — timed out mid-Varsity first (exit143), finished Varsity in a 2nd bg run; confirmed 21/21 on PR, single absorption per suburb (R2.18/B3.57/V1.77). **§6 live-render verified** (verify_market_metrics_live.py): Varsity + Burleigh pages now show consistent PR figures — Burleigh growth 23.7%→12.2% live (×8), median $1.71M; Varsity's contradictory 4.2(Jul-3)/3.1(Jul-23) absorption GONE. Durability: recalibration added to weekly propradar cron + a 1st-of-month cron (runs after the 05:00/05:16 precomputes that reset docs to raw).
**Residuals (NOT yet clean — next pass):** (1) AbsorptionRateChart shows 2.9mo (market-insights.mjs 30-day all-types) vs 1.77 houses — disclosed by on-page disclaimer, but needs market-insights.mjs repoint (Netlify deploy) for full consistency. (2) market_pulse PROSE still cites raw quarterly counts ("22 and 12 sales") from pre-recalibration transaction_count — needs one more market_pulse regen now that transaction_count is recalibrated. (3) run_market_monitor.py (article generator) not yet repointed.
**Files:** scripts/propradar/{recalibrate_charts,run_ingest_cron}.py; live DB: precomputed_indexed_prices, precomputed_market_charts, system_monitor.market_pulse (21 docs); crontab.
**Recurrence:** First.

## [PROPRADAR-STATS] Authoritative suburb-stats ingest + market_pulse headline repoint — 12:45 AEST
**Symptom:** Triangulating our data vs realestate.com.au + PropRadar showed we're off on THREE headline house stats, same direction, same root cause (under-captured + premium-skewed sold sample): volume undercounted (~55-70% of REA/PR), median overstated ~5-11%, 12mo growth overstated (Burleigh 23.7% vs REA 9.1% / PR 12.2%).
**Root cause:** Two mistakes in the initial approach, both corrected: (1) PropRadar's `/sold` ROW feed is TRUNCATED (~113 rows ≈ 26/mo, < its own house_sales_12mo stat) — not usable for counting; (2) the authoritative figures live in PropRadar's precomputed `/suburbs` `market_dynamics`+`medians`+`growth` stats (validated: PR median = REA median exactly for Robina & Varsity). Burleigh growth outlier traced to `precomputed_indexed_prices` rolling_12m median inflating to $2.115M (vs REA/PR $1.71-1.91M) off small (n=26, half of a year ago) premium-skewed quarterly samples; a 2nd contradictory quarterly-YoY calc (4.1%) also exists in generate_market_pulse.
**Fix:** `scripts/propradar/ingest_suburb_stats.py` → `Gold_Coast.propradar_suburb_stats` (one /suburbs call per suburb; medians/growth/market_dynamics/yields). `suburb_stats.house_headline()` accessor. `generate_market_pulse.py` fetch_all_data now overrides current_median_price / yoy_growth_pct / qoq_growth_pct / absorption_rate_months + adds sales_12mo_house from PropRadar (DOM stays ours; graceful fallback if stats absent). Verified in-memory: Burleigh 23.7%→12.2%, median 2.115M→1.71M; all 3 suburbs match PR/REA. Wired stats into weekly cron. **NOT yet published** — market_pulse not regenerated, and precomputed_indexed_prices (direct SSR/market-narrative source of the inflated median/growth) still to be repointed before §6 live-verify.
**Files:** scripts/propradar/{ingest_suburb_stats,suburb_stats,sold_metrics,run_ingest_cron}.py, scripts/generate_market_pulse.py.
**Recurrence:** First.

## [PROPRADAR-CRON] Weekly ingest cron + Fields Systems Health self-report — 12:10 AEST
**Symptom:** The PropRadar sold ingest (which now feeds recent volume/months-of-supply) needed a recurring refresh that can never fail silently — a stale feed would let those metrics drift back to the Domain-scrape undercount unnoticed.
**Root cause:** New pipeline had no scheduler and no health surface.
**Fix:** `scripts/propradar/run_ingest_cron.py` runs ingest+link for the 3 core suburbs and self-reports outcome via `job_status.record_job_result('propradar_ingest', ...)` → `system_monitor.job_runs`; Telegram alert on failure. Added `collect_propradar_ingest` + a "PropRadar Ingest" PAGE to `main_site_health_check.py` (job-outcome check mirroring Market Signals Fetch: ERROR on job error / 0 records, STALE after 8d) → surfaces as a row+tab on the "Fields Systems Health" sheet (id 1Oa7…BSgTk, daily 01:00 regen) with existing `maybe_alert` Telegram. Weekly cron Sun 05:30 AEST (with `cd` prefix — avoided the refresh_property_timelines cwd/.env cron bug). Verified end-to-end: cron run recorded success (267/237/30), collector emits the OK row.
**Files:** scripts/propradar/run_ingest_cron.py (new), scripts/main_site_health_check.py, crontab.
**Recurrence:** First.

## [DATA-SOURCE-RESET] PropRadar sold-data ingest + address-link pipeline (core suburbs) — 11:55 AEST
**Symptom:** Our Domain-listing-scraped sold data under-captures ~40-50% of house sales vs PropRadar (title/settlement-based), making sales VOLUME (~2x low) and MONTHS-OF-SUPPLY (~2x over-stated → false "softening") unreliable across the website, article generator, and off-market decks.
**Root cause:** Every volume/supply figure resolves to scraped `Gold_Coast.<suburb>` sold events (property_timeline + listing_status:sold), which structurally miss off-market/private sales. Investigated across all public surfaces (mapped in DATA_SOURCE_RESET_SCOPING.md).
**Fix:** New `scripts/propradar/` pipeline: `propradar_client.py` (Cloudflare browser-UA — default urllib gets 403 error 1010, same class as the ABS UA fix — + rate-limit-aware cursor pagination), `ingest_sold.py` (→ `Gold_Coast.propradar_sold`, upsert by property_id+sold_date), `addr_match.py` (locality-tail-only normaliser, abbrev expansion, unit/range handling), `link_property_ids.py` (collision-resolved address match → writes `propradar_property_id` join field onto our docs → emits `propradar_coverage_gaps` worklist). Empirically established PropRadar is **recent-complete (~4mo) not deep-history** (all endpoints recent-only; our own property_timeline is richer for history) — so source-of-truth splits by TIME: recent volume/supply→PropRadar, deep history→our timelines, coverage gaps→PropRadar discovers + our Domain scraper enriches. Ran all 3 core suburbs: propradar_sold Robina 113 / Burleigh 64 / Varsity 90; linked 103/60/74; coverage gaps 10/4/16 (match 91/93/82%). All additive — no existing data modified.
**Files:** scripts/propradar/{propradar_client,ingest_sold,addr_match,link_property_ids}.py (new), DATA_SOURCE_RESET_SCOPING.md (new). PropRadar key upgraded Free→Hobby by Will mid-session.
**Recurrence:** First (data-source reset; supersedes the parked run_market_insight n=0 thread as subordinate).

## [ACTION-LOG] No unified log of Samantha's actions — built samantha_actions + harvester — 12:35 AEST
**Symptom:** Will asked whether we log everything Samantha does so we can target improvements. Audit: no unified log. What existed was a raw nightly-only transcript JSONL (logs/samantha/{date}-transcript.jsonl — local, not queryable, gaps: last was 2026-07-19), three narrow slices that depend on her remembering (fix-history, change_ledger/samantha_changes, ad_decisions), and a Decision Log tab that was chronically empty. `audit_log` is suburb snapshots, not actions. Can't target improvements from that.
**Root cause:** Action capture was fragmented + partly manual + not queryable; the intended chronological record (Decision Log tab) went unused.
**Fix:** New `scripts/samantha/samantha_actionlog.py` — unified queryable log in `system_monitor.samantha_actions`, fed by (1) `harvest` (parses a run transcript — daily_run compact format AND Claude Code session format — and extracts EVERY tool call into a categorised row: push/code_edit/query/read/ad/leads/willnotes/board/ledger/growth_ideation/etc., dedup by source+seq) and (2) `log` (semantic action + rationale). `report` aggregates for improvement-targeting (analysis-vs-ship ratio, unmeasured live changes, skipped Task G, repeated commands). Auto-wired into daily_run.py (harvests the transcript at end of every nightly run). Charter Hard-lines updated to make the unified log the backbone + report a CEO-Business-Review input. Tested: harvested 279 actions from the 2 existing transcripts, dedup verified, report immediately surfaced board-writes=0 (the chronic stale-board finding) and analysis:ship 2.2:1.
**Files:** scripts/samantha/samantha_actionlog.py (new), scripts/samantha/daily_run.py, scripts/samantha/charter.md.
**Recurrence:** First.

## [SESSION-IO-PROCESS] Dated session folder + reply-to-all confirmation — 12:05 AEST
**Symptom:** Will asked (a) what process exists for Samantha to reply to every comment/note in Will Notes, and (b) where she places session summaries, scoping docs, and shareables — he wants a dedicated dated session folder in her Drive folder + Telegram when appropriate.
**Root cause:** (a) already existed (`running_doc.py reply` + the charter's MECHANICAL RUNNING-DOC AUDIT touches every active item) — needed confirmation, not a build. (b) real gap — session outputs were dropped loosely in the root Drive folder; no dated session folder and no consistent home for scoping docs/shareables.
**Fix:** New `scripts/samantha/session_folder.py` (`ensure` creates/returns a dated "Session YYYY-MM-DD" folder in her Drive folder; `put --file` uploads a local file into it). Wired into: charter.md rule 9 (session-output placement + "Telegram when appropriate" + a new "Replying to Will Notes — the standing process" section confirming the reply-to-each mechanism), daily_tasks.md finalise step, and daily_run.py Phase-A/C prompt (report Doc + scoping docs go inside the session folder; folder link in Telegram + Will Notes). Verified: created "Session 2026-07-27" folder and uploaded the stock-take doc into it.
**Files:** scripts/samantha/session_folder.py (new), scripts/samantha/charter.md, scripts/samantha/daily_tasks.md, scripts/samantha/daily_run.py.
**Recurrence:** First.

## [GROWTH-IDEATION-FACILITATION] Samantha was instructed to experiment but not facilitated — 11:35 AEST
**Symptom:** Will asked whether Samantha is actually equipped to identify A/B tests, funnel engagement/conversion lifts, lead-capture improvements, and new ad concepts each session. Audit found: instructed (Task 3 discipline) but NOT facilitated — `hypothesis_queue` had 2 null-filled stubs untouched since it was built (2026-07-17), `samantha_changes` had 3 live tests all unmeasured (verdict None), both `website_experiments` inconclusive, no structured per-session ideation method, no contextual-data brief, and no mechanical completion check (only `from_will` + `ceo_business_review` exist in `samantha_state`).
**Root cause:** A principle with no facilitation (a ready-made brief) and no mechanical enforcement (a completion check) silently doesn't happen — the same failure class documented throughout charter.md.
**Fix:** (1) New `scripts/samantha/growth_ideation.py` — a decision-ready per-session brief assembling funnel stage drop-offs, DEAD/thin lead-capture doors, worst-converting high-traffic entry pages, ad-angle performance, and the live/queued experiment pipeline, with ideation prompts across the 5 areas; `--record` stamps `samantha_state.growth_ideation`. (2) New daily_tasks.md "Task G — Growth Ideation & Experiment Design (mandatory every session)" driving hypothesis_queue + change_ledger. (3) charter.md session-end sweep extended 8→9 dimensions (added Growth Ideation). (4) `main_site_health_check.py` CEO Governance page now flags growth_ideation MISSING/>2d-stale (same pattern as ceo_business_review). (5) KPI Monitor sheet gained an "Experiment Backlog" tab (seeded with current pipeline). Tested: brief runs, immediately surfaced /analyse-your-home 22 sessions→2 engaged→0 conv as the top CRO finding.
**Files:** scripts/samantha/growth_ideation.py (new), scripts/samantha/daily_tasks.md, scripts/samantha/charter.md, scripts/main_site_health_check.py (+ KPI Monitor sheet Experiment Backlog tab).
**Recurrence:** First.

## [SAMANTHA-REDIRECT] North Star → inbound enquiry (strategic re-direction) — 11:05 AEST
**Symptom:** N/A — Will re-directed Samantha away from the bottom-funnel "book 5 appraisals" push toward generating inbound enquiry (warm audience + tease out 12-month sellers). Data supported it: analyse_leads dead since 2026-04-10, report_review_bookings=1, engagement (~1 click/80) is the real constraint.
**Root cause:** N/A.
**Fix:** Ran a 4-agent stock take of ~36 initiatives + full data-capture map; wrote `07_Focus/05-STOCKTAKE-KPI-AND-SAMANTHA-REDIRECT.md`; built the KPI Monitor Google Sheet (`1BxDgfEVLOsmGujZe5R1LNsq9sY2WtVt6d_wEpxJMLYY`, 5 tabs) in Samantha's Drive folder. Applied to charter.md: new North Star (inbound enquiry, 5 steer metrics, rate/trend targets) + scope line. Applied to daily_tasks.md: new TASK PRIORITY A–F block (intent-signal sweep first, leads worklist demoted), PRIME DIRECTIVE re-framed, "PATH TO 5 APPRAISALS" report section → "FUNNEL MOVEMENT". Queued the FB ad-spend review as a P1 Task Board task (scheduled session with Will; Samantha preps cut-list, no unilateral overhaul). Editorial rules, DOER tier, caps, brains, session-end sweep unchanged.
**Files:** scripts/samantha/charter.md, scripts/samantha/daily_tasks.md, 07_Focus/05-STOCKTAKE-KPI-AND-SAMANTHA-REDIRECT.md, memory/samantha_redirect_inbound_enquiry_2026-07-27.md (+ KPI Monitor sheet, Task Board).
**Recurrence:** First.

## [FORSALE-LADDER] for-sale-v3 buyer-brief ladder onboarding (feature) — 10:26 AEST
**Symptom:** N/A — new feature. `/for-sale-v3` dropped visitors straight into the raw feed with no qualification or lead capture, and no on-page path into 5 Property Friday.
**Root cause:** N/A.
**Fix:** Added a vertical, chat-style onboarding flow (`ForSaleLadder`) mounted above the feed on `/for-sale-v3`: teaser (the pre-computed Fields Pick, with the real listing photo) → suburbs (multi, live counts) → bedrooms (multi) → special requirements (yes → free-text, recorded only) → 5 Property Friday opt-in with dynamic count/suburb copy. Cards append downward (previous ones stay visible), unlike the horizontal off-market deck. Answers recorded session-keyed to `system_monitor.forsale_ladder_responses` via new `forsale-ladder-qualify.mjs`. Default-on for all traffic; kill switch = `forsale_ladder_v1` PostHog flag set false, or `?flow=off`; localStorage `fields_forsale_ladder_done` skips it on repeat visits. Feed stays in the DOM below (SSR/SEO untouched). PostHog funnel events: `forsale_ladder_view/start/answer/optin_open/optin/complete`.
**Files:** src/components/DecisionFeedV3/ForSaleLadder.tsx (new), src/components/DecisionFeedV3/ForSaleLadder.module.css (new), src/pages/DecisionFeedV3Page/DecisionFeedV3Page.tsx, netlify/functions/forsale-ladder-qualify.mjs (new). Website commit 7d3f5246.
**Recurrence:** First.

## [FORSALE-LADDER-GATE] Ladder was skippable — gate the feed until first action — 11:10 AEST
**Symptom:** On `/for-sale-v3` the ladder sat above the feed as a normal section; visitors could scroll straight past "Show me the ones that matter" into the properties without engaging, so the qualification/opt-in step was easy to miss.
**Root cause:** The ladder was in normal document flow, not blocking; the feed below was immediately scroll-reachable.
**Fix:** Turned the ladder into a scroll-locked overlay gate: fixed below the site header, feed behind it dimmed + `backdrop-filter: blur` (feed stays in the DOM, so SEO/crawlers unaffected — only visually blocked), body scroll locked while open. Moved the primary action out of the cards into a fixed bottom action bar (teaser → "Show me the ones that matter →", questions → "Continue" with disabled hint) so it's a standalone, unmissable action. Cards still stack + scroll inside the panel (scroll-up-to-see-previous preserved). Gate closes on completion: opt-in shows "You're in ✓" + a "See your listings →" reveal button; decline reveals immediately. Page renders the ladder only while `!ladderCompleted`, so unmount restores scroll.
**Files:** src/components/DecisionFeedV3/ForSaleLadder.tsx, src/components/DecisionFeedV3/ForSaleLadder.module.css, src/pages/DecisionFeedV3Page/DecisionFeedV3Page.tsx. Website commit 266c70f8.
**Recurrence:** First.

## [FORSALE-LADDER-BTN] "Show me the ones that matter" button not visible — 11:40 AEST
**Symptom:** On the live gated `/for-sale-v3`, the ladder's primary action bar (with "Show me the ones that matter →") was not visible; the bottom of the screen showed the page's "See what buyers would pay for your home" CTA instead.
**Root cause:** The page's `StickyCTA` (`position: fixed; bottom: 0; z-index: 90`) was rendered unconditionally and sat on top of the gate's action bar (gate `z-index: 40`), covering it.
**Fix:** Render `StickyCTA` only when `feedInteractive` (hidden while the gate is open, same as the fixed filter bar), and raised the gate `z-index` 40 → 100 so it sits above StickyCTA and any other fixed page chrome.
**Files:** src/pages/DecisionFeedV3Page/DecisionFeedV3Page.tsx, src/components/DecisionFeedV3/ForSaleLadder.module.css. Website commit cc63ff94.
**Recurrence:** First.

## [FORSALE-LADDER-IMMERSIVE] Make the gate immersive + teaser preview-only — 11:55 AEST
**Symptom:** Two distraction/leak points in the gate: the site header (nav) stayed visible above it, and the teaser's "See the full analysis →" link let visitors jump to a property page and skip the Q&A entirely.
**Root cause:** Gate started below the header (`top: var(--header-h)`) with the header still rendered; teaser rendered a full `FieldsPickCard` including its property Link.
**Fix:** (1) Gate now full-screen (`top: 0`) with the site header hidden while it's open (`{feedInteractive && <SiteHeader/>}`) — immersive/distraction-free like the off-market deck; header returns on completion/skip. (2) Added a `preview` prop to `FieldsPickCard` that hides the "See the full analysis" CTA; the ladder teaser passes it, so the card is a non-navigable sample and the only way forward is the action bar. Feed's own FieldsPickCard usage unchanged (link intact).
**Files:** src/components/DecisionFeedV3/FieldsPickCard.tsx, src/components/DecisionFeedV3/ForSaleLadder.tsx, src/components/DecisionFeedV3/ForSaleLadder.module.css, src/pages/DecisionFeedV3Page/DecisionFeedV3Page.tsx. Website commit d1ea0adf.
**Recurrence:** First.

## [FPF-WEBSITE-GAP] Website 5 Property Friday opt-ins stored but never emailed — 10:26 AEST
**Symptom:** Website 5PF signups were written to `system_monitor.five_property_friday_subscribers` but never received the weekly shortlist. 2 existing active subscribers had never been emailed.
**Root cause:** `fpf_send.friday_batch()` only queried `fb_leads` (Facebook buyer-brief leads); nothing ever read `five_property_friday_subscribers` except the write endpoint. The two stores were disconnected.
**Fix:** Extended `friday_batch()` to also iterate active `five_property_friday_subscribers`, resolving suburbs via new `website_sub_suburbs()` (prefers the `suburbs` key list from the ladder opt-in, falls back to `suburb_preference`), deduped against active FB-lead emails so nobody gets two emails; same double-send guard (`last_shortlist_at`). Also extended `five-property-friday.mjs` to capture the full buyer-brief shape (email + suburbs + bedrooms + bathrooms + timeframe + owns_home + phone) so the sender has data to build picks from. Ladder opt-ins now flow into the real weekly send.
**Files:** scripts/fpf_send.py, (website) netlify/functions/five-property-friday.mjs. Orchestrator commit 426ba915, website commit 7d3f5246.
**Recurrence:** First.

## [FORSALE-V3-DISCLAIMER] Valuation disclaimer added to /for-sale-v3 — 14:55 AEST
**Symptom:** N/A — compliance gate. The paid buyer-brief carousel (comps \$ claims) points to /for-sale-v3, which showed price-gap narratives but had NO visible valuation methodology/confidence disclaimer (property pages did; the feed page didn't).
**Root cause:** N/A — new requirement from the FB-ads valuation-data policy change (see CLAUDE.md Rule 5 update 2026-07-27).
**Fix:** Added a persistent disclaimer `<p>` above `<SiteFooter/>` in DecisionFeedV3Page.tsx — "Valuation figures are estimates based on comparable sales — a defensible range, not a formal appraisal or a guaranteed sale price…". Always in the DOM (covers the feed regardless of ladder gate state).
**Files:** Feilds_Website/01_Website/src/pages/DecisionFeedV3Page/DecisionFeedV3Page.tsx. Website commit 5dedb6e.
**Recurrence:** First.

## [FORSALE-LADDER-CTA-VISIBILITY] Ladder CTA hidden below mobile browser chrome — 33d4697 — 16:05 AEST
**Symptom:** Live paid carousel traffic landing on /for-sale-v3 (PostHog sessions confirmed) but users couldn't see the "Show me the ones that matter →" button to advance the ladder. Button present but below the visible fold on mobile.
**Root cause:** The ladder gate was `position: fixed; top:0; bottom:0` = the LAYOUT viewport height (~812px on iPhone). The bottom-pinned action bar sat at the layout-viewport bottom, which falls BELOW the visible viewport once mobile browser chrome (address/tool bars, ~120-150px) is shown — so the CTA was off-screen on real phones (visible in headless capture where there's no chrome).
**Fix:** Gate now `height: 100dvh` (dynamic viewport, 100vh fallback), removed `bottom:0`. The gate fits the VISIBLE viewport so the action-bar CTA stays on-screen. Verified the reported symptom via a 375x812 mobile capture (button pinned at layout bottom).
**Files:** Feilds_Website/01_Website/src/components/DecisionFeedV3/ForSaleLadder.module.css. Website commit 33d4697.
**Recurrence:** First (related class: FORSALE-LADDER-BTN z-index, 2026-07-27).

## [FORSALE-LADDER-DROP-TEASER] Ladder opened with a redundant analysis example — 3a296ee — 16:20 AEST
**Symptom:** Paid-carousel visitors landing on /for-sale-v3 were confused — the carousel already shows 4 analysis examples, then the site opened with a 5th (the "This is how we read a listing" Fields-Pick teaser) before asking anything. Stalled them before the first question.
**Root cause:** The ladder's first beat was 'teaser' (built before the carousel existed). Redundant once the ad itself carries the analysis examples.
**Fix:** Removed the 'teaser' beat — BEATS now ['suburbs','bedrooms','special','optin'] so the gate opens directly on "Which suburbs are you looking in?". Removed renderTeaser + FieldsPickCard import + teaser cases in gateSatisfied/advance/renderers; moved forsale_ladder_start capture to mount; kept teaser_property_id analytics field. Type-clean (tsc -p tsconfig.app.json).
**Files:** Feilds_Website/01_Website/src/components/DecisionFeedV3/ForSaleLadder.tsx. Website commit 3a296ee.
**Recurrence:** First.

## [FORSALE-V3-KEEPWARM] Feed API cold-start 500 protection — 18:40 AEST
**Symptom:** Netlify logged a 500 on GET /api/v1/properties/decision-feed-v3 (6:15 PM). Live paid-carousel traffic lands on /for-sale-v3 which needs this feed.
**Root cause:** Transient cold start — the Netlify function goes cold when idle; first invocation opens a fresh Cosmos connection (~5-6s observed) and can exceed the timeout / hit RU slowness → 500. Verified healthy warm (6/6 HTTP 200, ~0.13s warm; first hit 5.3s). Endpoint was NOT kept warm (api_health_monitor pings external APIs only, not this function).
**Fix:** New `scripts/keep_warm_forsale_v3.py` + cron `*/5 * * * *` — pings the feed endpoint to keep it hot, and Telegram-alerts Will on state transitions only (up→down, down→up; no spam during sustained outage). State in logs/keep_warm_forsale_state.json. Not a code fault in the new ladder flow (those changes were client-side only).
**Files:** scripts/keep_warm_forsale_v3.py (new), crontab (VM-local, every 5 min).
**Recurrence:** First (related: netlify_mongo_eventloop_hang, cosmos_retry_logic).

## [VM-WEDGE-PREVENTION] Off-VM watchdog + session memory caps to stop OOM hangs — 18:42 AEST
**Symptom:** VM became fully unresponsive — SSH/ports 22,443,80 all timed out while the instance showed RUNNING at the hypervisor. Serial console showed an OOM cascade (kernel killed chrome ×2 and a `claude` process holding 3.8 GB) then went silent for ~3 weeks. Recovery required an external `gcloud compute instances reset` from Will's laptop; the on-VM watchdog was dead with the box.
**Root cause:** Two gaps. (1) On the 8 GB e2-standard-2, Claude Code / code-server sessions accumulate memory (a single `claude` reached 3.8 GB) on top of scraper Chrome, exhausting RAM. Prior OOM fixes (Mar 6/11, May 17) only capped `fields-orchestrator` + killed Chrome — they never bounded the code-server/Claude sessions, which are the current culprit, and `watchdog.py`'s memory check ran only every 60 min and only killed Chrome. (2) The only watchdog ran ON the VM, so when the guest wedged nothing could detect or recover it.
**Fix:**
- **Layer 1 (prevent, on-VM):** systemd drop-ins cap the memory-hungry slices. Key insight — all Claude sessions (code-server + `claude` + MCP servers) share the `code-server.service` cgroup, so `MemoryHigh=4G`/`MemoryMax=5G` on it bounds the entire session blast radius; the kernel now OOM-kills a *session* within-slice, never mongod/pipeline. `OOMScoreAdjust`: mongod −800, orchestrator −500 (protected); sessions +500, ollama +300, runner +400 (sacrificed first). Applied live via daemon-reload (no session restart) + live `/proc/*/oom_score_adj` writes. Swap 2 GB → 4 GB. New `scripts/resource_guard.py` on a 90 s systemd timer (`fields-resource-guard.timer`) reaps provably-orphaned session/Chrome procs (PPID==1), cleans disk before `/` fills, and audits to `system_monitor.resource_guard_actions`.
- **Layer 3 (early warning, on-VM):** the guard Telegrams Will when memory/disk trend toward the cliff while the box is still reachable.
- **Layer 2 (detect+recover, off-VM):** `cloud-watcher/` — a GCP Cloud Function (Cloud Scheduler every 3 min) reads the `vm_metrics` heartbeat + TCP-probes 22/443; on a confirmed wedge (stale heartbeat AND dead port) it Telegrams Will with a one-tap reset (alert-first, never automatic — Will's choice). Code + tiered `DEPLOY.md` written; Will deploys with his GCP creds (VM SA is read-only, can't create Functions/grant IAM).
**Files:** scripts/resource_guard.py (new), deploy/vm-resource-safety/{install.sh,README.md,fields-resource-guard.{service,timer},dropins/*.conf} (new), cloud-watcher/{main.py,requirements.txt,DEPLOY.md} (new); live systemd drop-ins under /etc/systemd/system/{code-server,ollama,mongod,fields-orchestrator,actions.runner...}.service.d/ and fields-resource-guard.{service,timer}; /swapfile2 + /etc/fstab.
**Recurrence:** Nth (recurring OOM class — prior: [OOM-CHROME-KILL] 2026-03-06 & 2026-03-11, memory-limits 2026-05-17). First fix to (a) cap the code-server/Claude-session cgroup — the actual current culprit — and (b) add an OFF-VM detection/recovery path so a wedged guest no longer needs a manual laptop reset.

## [VM-WATCHDOG-DEPLOY] Off-VM watchdog deployed (Tier A live) — heartbeat pivoted Cosmos→GCS — 19:15 AEST
**Symptom:** During deploy, the Cloud Function returned `heartbeat_age_min: null` — it could not read the `system_monitor.vm_metrics` heartbeat from Cosmos, though the VM reads Cosmos fine.
**Root cause:** Cosmos (Azure) firewalls off GCP Cloud Function egress IPs. Any Cosmos-based heartbeat is unreadable from GCP, so the safety net can't depend on it.
**Fix:** Pivoted the heartbeat + watcher state to **Google Cloud Storage** (always reachable from GCP, and decoupled from both the VM and Cosmos). VM cron `scripts/write_heartbeat.sh` re-uploads `gs://fields-vm-watchdog/vm-heartbeat.txt` every minute; the function reads the object's last-updated time; state/cooldowns live in `gs://fields-vm-watchdog/vm-watchdog-state.json`. Deployed Tier A end-to-end while gcloud was already authed as `will.simpson@blueoceans.com.au` on the VM: enabled APIs, `tg-bot-token` secret, dedicated `fields-vm-watchdog` bucket (function SA granted objectAdmin on it only), Cloud Function `vm-watcher` + Scheduler `vm-watcher-ping` (*/3, OIDC). Verified: healthy = `status:ok`; forced-wedge test (unreachable IP + STALE_MIN=0) sent the real Telegram to Will end-to-end, then reverted. Dropped the unused `cosmos-conn` secret.
**Files:** cloud-watcher/main.py (Cosmos→GCS), cloud-watcher/requirements.txt (pymongo→google-cloud-storage), cloud-watcher/DEPLOY.md (rewritten to as-deployed), scripts/write_heartbeat.sh (new + per-minute cron), deploy/vm-resource-safety/README.md; GCP: functions/scheduler/bucket/secret/IAM under fields-estate.
**Recurrence:** First (new off-VM layer). Tier B (one-tap phone reset button) still pending — needs a BotFather bot only Will can create.
