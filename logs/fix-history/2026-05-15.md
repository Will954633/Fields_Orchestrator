
---

## [WATCHDOG] Step 114 (Backfill URL Slugs) — 00:08 AEST

**Failure class:** UNKNOWN
**Cause:** No recognizable error pattern — check logs manually
**Action taken:** ESCALATED to Claude (id=None)
**Result:** queued — awaiting repair-agent or Claude

---

## [WATCHDOG] Step 101 (Scrape For-Sale Properties (Target Market)) — 00:08 AEST

**Failure class:** UNKNOWN
**Cause:** No recognizable error pattern — check logs manually
**Action taken:** ESCALATED to Claude (id=None)
**Result:** queued — awaiting repair-agent or Claude

---

## [TEMPLATE-PHASE-E-01] §03 receipts + recommendation pages templatized — 22:45–23:30 AEST

**Built on Phase A-D. New deliverables:**

1. **§03 receipts page** (comp-by-comp adjustments) templatized:
   - `data_pull.section_03_receipts()` pulls top-N most-weighted comparables from `valuation_data.comparables` (filtered to `included_in_valuation=True`), sorts by weight, builds per-card adjustment rows from `adjustment_result.adjustments`.
   - `render.render_section_03_receipts_html()` renders comp cards with address, sold price, distance, line-itemised adjustments (subject vs comp + dollar amount, signed), adjusted estimate, and weight percentage.
   - **Audit B14 fix:** removed "Domain's equivalent on the same market: 15.0%. Fields publishes its accuracy. Domain does not." — now a Fields-only statement: "11.4% mean absolute error across our published backtest, against 1,270 actual Gold Coast sales. The methodology is at fieldsestate.com.au/methodology." (per framework Rule 4 — no named-competitor quantitative comparison without same-sample evidence file).
   - For 13TC specifically: rendered the page header + backtest stat correctly; comp cards empty because the valuation engine flagged `insufficient_data` (only 2 comps total, none with `included_in_valuation=True`). For subjects with richer comp sets, the cards populate from real data.

2. **Recommendation pages** (page 11 + page 18) templatized:
   - `data_pull.section_recommendation(subject_id, page_number=11|18)` returns the full payload — listing price, target sale price range, derived range, midpoint, gap, campaign duration.
   - Inputs cascade: `pipeline_record.recommendation` (preferred — analyst-confirmed) → `valuation_data.confidence.range` (model fallback) → empty (renders em-dash).
   - `render.render_section_recommendation_html()` renders both pages with the same template, differentiated by `page_number` (page 11 has prose body, page 18 has campaign-duration + inspections stats).
   - For 13TC: listing $1,915,000, target $2,050,000 – $2,101,250 (auto-extended from pipeline's $2,050,000), derived $1,760,000 – $2,240,000.

3. **Splice-ordering fix in V4 generator:** changed splice loop to bottom-up document order (rec_p18 first, then s06_right, then s05_right, ..., cover last) so earlier splices don't invalidate later splices' end markers. Documented the cascade: each templated section rewrites its starting page comment, so subsequent splices must use the new comment as their end-boundary. Markers updated: `s06_right` end → "PAGE 18 — RECOMMENDATION", `s03_receipts` end → "PAGE 11 — RECOMMENDATION", `s03_right` end → "PAGE 10 — SECTION 03 RECEIPTS".

**Section count templated in V4 generator:** cover + §01L + §01R + §02R + §03R + §03receipts + §04R + §05R + §06R + rec_p11 + rec_p18 = **11 sections**, covering every editorial-variable page in the document. The remaining static thesis pages (§02L, §03L, §04L, §05L, §06L, philosophy, 28-day plan) carry generic copy that works for any subject without templating.

**Outstanding (Phase F or done):**
- Generalize `AppraisalPipelinePanel` to natively read both `property_reports` and `appraisal_pipeline` (retires bridge sync) — defer until subject volume justifies the unified-panel refactor.
- Decide on legacy `seller_report_v2.html` — retire in favor of `generate_appraisal_v4.py`, or harmonise.
- Auto-derive `evidence_stack` for §03 from valuation engine attribute weights (currently a 3-row default).

**Recurrence:** First.

---

## [TEMPLATE-PHASE-D-01] §01 left templatized + hero-image picker UI — 22:00–22:45 AEST

**Built on Phase A+B+C. New deliverables:**

1. **§01 left thesis templatized** (`scripts/appraisal_template/render.py:render_section_01_left_html`):
   - Retired the locked PNG (`assets/locked_p01.png` — was rendered from V3 with old "Scarcity" copy).
   - Replaced with live HTML using framework doc §5.1 copy: *"Buyers pay more for what they cannot easily replace."*
   - Subject-aware closing line: *"For {{ subject.address }}, the strongest position is the combination."*
   - Substantiation file saved per render.
   - V4 generator extended with `s01_left` splice point + renderer call. Total templated sections now: cover + §01 L+R + §02 R + §03 R + §04 R + §05 R + §06 R = **8 sections programmatic**.

2. **Hero-image picker UI** in `AppraisalPipelinePanel.tsx`:
   - Collapsible card per appraisal — lists 40 candidate cover images from the subject's property doc.
   - Sources surfaced (badge per image): `v2_hero`, `v2`, `apr01_recovery`, `apr01_sold`, `property_images_original`, etc.
   - Dead Azure URLs filtered (same pattern as Netlify extractPhotos chain).
   - Visual thumbnail grid (responsive auto-fill), copper border + tint on the chosen image, source badge below each.
   - One-click selection → saves to `appraisal_pipeline.cover_hero_image_src`. The V4 generator reads this on the next render.

3. **Backend endpoints** (`netlify/functions/system-monitor.mjs`):
   - `GET /api/monitor/appraisal-images?pipeline_id=X` — returns candidate cover images, dead-URL-filtered.
   - `POST /api/monitor/appraisal-cover-pick` — persists chosen image_src to the pipeline record + audit timestamp.

**Satellite backfill final state:** 222/234 sold-cohort docs analysed (~95%, running). Ranker now has cul-de-sac + bushland_boundary signals on the majority of the catchment. Every new appraisal automatically gets these as candidate combinations.

**Outstanding (Phase E):**
- §03 receipts page templatize (comp-by-comp adjustments — requires integration with comp engine output)
- Recommendation pages templatize (page 11 listing+target, page 18 final synthesis — pulls from valuation_data + pipeline targets)
- Generalize `AppraisalPipelinePanel` to natively read both `property_reports` and `appraisal_pipeline` (retires bridge)
- Decide on legacy `seller_report_v2.html` — retire in favor of `generate_appraisal_v4.py`, or harmonise to V4 design

**Files (modified):**
- `scripts/appraisal_template/render.py` — added `render_section_01_left_html`
- `scripts/generate_appraisal_v4.py` — wired `s01_left` splice
- `netlify/functions/system-monitor.mjs` — 2 new endpoints
- `src/pages/OpsPage/panels/AppraisalPipelinePanel.tsx` — HeroImagePicker component

**Recurrence:** First.

---

## [TEMPLATE-PHASE-C-01] V4-native end-to-end appraisal generator + Alex cover system — 20:00–22:00 AEST

**Built on Phase A+B. New deliverables:**

1. **Alex cover system ported** (`09_Appraisals/Version_Four/preview.html` + `scripts/appraisal_template/render.py:render_section_00_cover_html`):
   - CSS lifted from `Alex_Working_Files/preview3.html` — `.cover`, `.cover-image`, `.cover-mark-tl`, `.cover-mark-tr`, `.cover-card`, `.cover-bottom`, plus the @font-face for Hurme Geometric Sans.
   - Assets copied: `HurmeGeometricSans-Bold.{woff2,woff,ttf}` to `assets/fonts/`, `fields_logotype.svg` + `fields_logotype_white.svg` to `assets/img/`.
   - Deleted V4-original cover CSS that was shadowing Alex's classes (line 437+ of preview.html — same class names but different values).
   - V4 preview.html page 1 now uses Alex's structure with full document metadata band (PROPERTY POSITIONING REPORT eyebrow + Prepared for + horizontal rule + by Will Simpson + URL + date).
   - **Confirmed parametric** — the federal variant in Alex_Working_Files proves the same cover template handles any subject by swapping hero image, address parts, prepared-for, and date.
   - For 13TC: cover hero `cover_hero_13_terrace_court.jpg` shows the pool exterior with proper card placement, copper F top-left, white F top-right, dark green band with full metadata.

2. **V4-native production generator** (`scripts/generate_appraisal_v4.py`):
   - Single CLI: `python3 scripts/generate_appraisal_v4.py --pipeline-id <oid> --update-pipeline`.
   - Renders all seven sections programmatically: cover + §01 right + §02 right + §03 right + §04 right + §05 right + §06 right.
   - Splices each into `preview.html` at named markers, writes rendered HTML + PDF to `artifacts/appraisals_v4/<subject>_<timestamp>.{html,pdf}`.
   - Copies the V4 assets folder alongside output so `file://` rendering resolves all references.
   - Updates the appraisal_pipeline record with `report_path_v4`, `report_html_v4`, `report_rendered_at_v4`, `report_sections_rendered`.
   - Substantiation dual-write fires for every rendered section.
   - Editorial overrides per section read from `appraisal_pipeline.section_<NN>_editorial_overrides`.
   - Pipeline-record fields read by the generator: `name`, `cover_hero_image_src`, `cover_date_override`, `satellite_image_src`, `highlight_chosen_key`, `section_NN_editorial_overrides` (×6).

3. **Dead Azure URL filter** in §01 renderer — `satellite_analysis.satellite_image_url` stored on docs is often a dead Azure blob URL (post-migration). Renderer now filters `blob.core.windows.net` from the fallback chain and uses the supplied `satellite_image_src` or per-subject local asset instead.

4. **Verified end-to-end against 13TC** — full 19-page PDF generated in one command at `artifacts/appraisals_v4/690bd7e68b8f5465926045fc_20260515T173954.pdf`. Cover, §01 right (Alex satellite + 458/6 dot grid + 5-feature consultant subhead + new FA-01), §02–§06 right pages all rendered correctly. PDF 5.2 MB. Pipeline record `6a06b587c94f0361dd66d091` updated with all V4 fields.

**Outstanding (Phase D):**

- Templatize §03 receipts page (comp-by-comp adjustments — high subject-specific content, currently hand-written for 13TC).
- Templatize recommendation pages (page 11 listing+target, page 18 final synthesis).
- Templatize §01 left thesis page (currently a locked PNG from V3 — convert to live HTML).
- Hero-image picker UI in `AppraisalPipelinePanel.tsx` so Will can curate the cover photo from the ops dashboard.
- Generalize panel to natively read both `property_reports` and `appraisal_pipeline` (retire bridge sync).
- Restructure legacy `seller_report_v2.html` to match V4 OR fully retire it in favor of `generate_appraisal_v4.py`.

**Recurrence:** First.

**Files (new):** `scripts/generate_appraisal_v4.py`, `09_Appraisals/Version_Four/assets/fonts/HurmeGeometricSans-Bold.{woff2,woff,ttf}`, `09_Appraisals/Version_Four/assets/img/{fields_logotype.svg,fields_logotype_white.svg,cover_hero_13_terrace_court.jpg}`.

**Files (modified):** `09_Appraisals/Version_Four/preview.html` (Alex cover CSS + new cover HTML, V4-duplicate CSS removed), `scripts/appraisal_template/render.py` (cover renderer + dead Azure URL filter).

---

## [TEMPLATE-PHASE-B-01] Appraisal template system Phase B substantively complete — 16:30–20:00 AEST

**Built on Phase A. New deliverables:**

1. **Satellite analysis backfill** on sold cohort (`scripts/step117_satellite_analysis.py`):
   - Added Merrimac to `TARGET_SUBURBS`.
   - Pilot 5/5 success on sold cohort.
   - Full backfill running in background as `nohup` job. 234 sold docs queued, ~22-25s per doc, ~2h ETA.
   - Once complete, every property in the cohort gains: `adjacency.backs_onto`, `adjacency.frontage`, `amenity_premiums.green_space_proximity`, `detractants.traffic_noise_risk`, `lot_characteristics.*`.

2. **Pick_highlight extended with satellite signals** (`scripts/appraisal_template/pick_highlight.py`):
   - Reads `satellite_analysis.categories.adjacency.backs_onto` for bushland boundary, `.frontage` for cul-de-sac.
   - 5 new candidate combinations: bushland-boundary alone, cul-de-sac alone, beds+bushland, cul-de-sac+pool, plus compound variants.
   - Discovered finding: 13TC ranks `beds+bushland` at **1/458 (0.2%)** — a much stronger candidate than the 6/458 `exact-bedrooms` Will originally picked.

3. **Bridge sync systemd service** (`systemd/fields-bridge-sync.service`):
   - Installed, enabled, running. Polls property_reports every 60s. Refreshes highlight_candidates on existing bridged records. Service uses `--watch --refresh --quiet` flags.

4. **Sections §02–§06 right pages templatized** (`scripts/appraisal_template/data_pull.py` + `render.py`):
   - `section_02_right` — three personas via `personas.py` library (southern GC premium tier with feature weights × subject attributes → match-bar fills). Willingness-to-pay derived from valuation_mid × per-persona multipliers (within $10K of V4 reference).
   - `section_03_right` — cohort medians auto-queried per subject's bedroom count, reconciled range from `valuation_data.confidence.range`, derived range banner with midpoint + 90% CI.
   - `section_04_right` — active/passive/retargeting modes + 28-day campaign benchmark.
   - `section_05_right` — photography contrast + +118% professional-vs-phone stat + 3-row presentation strategy.
   - `section_06_right` — 3 structural assets (price/trade-offs/method) + +9.6% relationship-premium stat.

5. **Fields Advantage boxes (FA-02 through FA-06) rewritten** — all framework-compliant. Audit issues fixed:
   - FA-02: "No agency in the southern Gold Coast operates this analysis on a per-listing basis" → removed (audit B6).
   - FA-03: "Most appraisals rely on a handful of comparable sales" → removed (audit B13).
   - FA-04: "Most agents market to buyers already searching on the portals" → removed (Rule 7).
   - FA-05: "Most agents list features. Fields turns them into desire" → removed (Rule 7).
   - FA-06: "Real estate has a trust problem" → removed (industry-level negative).
   - Compliance audit confirms zero forbidden phrases across all 6 rendered sections.

6. **Persona library** (`scripts/appraisal_template/personas.py`):
   - Three archetypes for southern GC premium catchment ($1.5M+).
   - Each: id, demographics, evidence_note, share_default, willingness_multipliers, feature_weights table.
   - Match-bar fills auto-computed from subject's actual structured + satellite-derived attributes.

7. **Editorial overrides UI** (`AppraisalPipelinePanel.tsx` + `system-monitor.mjs`):
   - Collapsible per-section JSON editor in the ops dashboard.
   - Power-user UX: textarea per section, save button per section.
   - Backend endpoint `POST /api/monitor/appraisal-editorial-overrides` whitelists valid section keys and persists to `appraisal_pipeline.section_<NN>_editorial_overrides`.
   - Each render function in `render.py` already accepts `editorial_overrides` — the UI completes the loop.

**Deferred to Phase C:**

- Production generator (`generate_appraisal_report.py` + `seller_report_v2.html`) wiring for sections §02–§06. Currently only §01 right is integrated. Full integration requires restructuring `seller_report_v2.html` to the V4 19-page format (substantial work; design-system divergence).
- Generalize `AppraisalPipelinePanel` to read both `property_reports` and `appraisal_pipeline` natively → retire the bridge. Defer until more subjects flow through the system.
- Auto-derived `evidence_stack` for §03 (currently a 3-row default; can be derived from valuation engine attribute weights).

**Files (new):** `scripts/appraisal_template/personas.py`, `systemd/fields-bridge-sync.service`.

**Files (modified):** `scripts/appraisal_template/{data_pull,render,pick_highlight}.py`, `scripts/step117_satellite_analysis.py`, `09_Appraisals/Version_Four/preview.html` (templated §02 + §03), `netlify/functions/system-monitor.mjs` (new endpoint), `src/pages/OpsPage/panels/AppraisalPipelinePanel.tsx` (overrides UI).

**Verification:**
- All 6 render functions tested against 13TC. PDF previews rendered cleanly via headless chromium.
- Compliance audit: zero forbidden phrases ("no agency", "Most agents", "Most appraisals", "Real estate has a trust problem") in any of the 6 section outputs.
- Substantiation dual-write confirmed for §01–§06.
- Bridge sync running as systemd service; refresh updates candidates within 60s of property_reports changes.

**Recurrence:** First.

---

## [TEMPLATE-PHASE-A-01] Appraisal template system Phase A complete — 13:00–16:30 AEST

**Symptom (the underlying problem):** Per the framework doc (`07_amendments_and_operating_framework.md`), every appraisal needs auto-generated cohort comparisons with universe disclosure, substantiation files, and a 90/10 human-in-the-loop split. None of that existed; everything was hand-authored per-appraisal.

**Fix (Phase A — five items, all four delivered):**

1. **Foundation utilities** (`scripts/appraisal_template/`):
   - `data_pull.py` — `section_01_right(subject_id)` returns full render payload (universe count, highlight, caption with auto-disclosure, substantiation record).
   - `pick_highlight.py` — auto-ranks 10+ candidate attribute combinations by catchment rarity. Returns top-5 with rarity ratios.
   - `dot_grid.py` — deterministic SVG generator (seed = subject_id).
   - `substantiation.py` — dual-write to `system_monitor.appraisal_substantiation` + flat JSON in `09_Appraisals/Substantiation/`.
   - `render.py` — Jinja-templated §01 right HTML block generator. Plugs into V4 preview.html (live HTML) and into production seller_report_v2.html (via optional template variable).

2. **Bridge sync** (`scripts/property_reports_to_appraisal_pipeline_bridge.py`):
   - Mirrors mini-site funnel docs (property_reports) into the unified review pipeline (appraisal_pipeline).
   - Auto-computes highlight candidates and stores them on the pipeline record (so the ops UI doesn't need to call Python from Netlify functions).
   - Idempotent. `--refresh` flag re-populates candidates on existing records.
   - 13TC bridged with 5 candidates; Will's pick (`exact-bedrooms`) persisted.

3. **`/methodology` public transparency page** (`Will954633/Website_Version_Feb_2026` → `src/pages/MethodologyPage/`):
   - Documents five methods currently in use: cohort comparison, comparable-sales valuation, agent-cohort analysis, photography-views study, agent relationship premium.
   - Each with dataset, n, time window, controls, limitations, last reviewed.
   - URL referenced by all auto-injected captions (per framework Rule 8).

4. **Ops dashboard highlight picker**:
   - `AppraisalPipelinePanel.tsx` — new `HighlightPicker` sub-component shows top-5 ranked candidates with rarity ratios. One-click pick, immediate save.
   - Backend endpoint `POST /api/monitor/appraisal-highlight-pick` validates the key against the pre-computed candidate list and persists.

5. **Production pipeline integration**:
   - `generate_appraisal_report.py` extended: when the pipeline record has `highlight_chosen_key`, calls `render.render_section_01_right_html()` and passes the result as `section_01_right_html` context variable.
   - `templates/seller_report_v2.html` updated: `{% if section_01_right_html %}` renders the new block, otherwise falls back to the legacy scarcity-banner.
   - Graceful fallback if template render fails — production never breaks.

**Verification:**
- 13TC's full §01 right page rendered live in V4 preview.html via `render_section_01_right_html()`. PDF identical to manual version.
- Substantiation files dual-written (Mongo id `6a06ae7fbe3042493f5b4c6a` + flat JSON).
- Auto-injected caption fully framework-compliant (universe + suburbs + as-at + methodology link).
- Bridge produces 5 candidates for 13TC; `exact-bedrooms` (6/458, 1.3%) locked as chosen.

**Files (new):** `scripts/appraisal_template/{__init__,data_pull,pick_highlight,dot_grid,substantiation,render}.py`, `scripts/property_reports_to_appraisal_pipeline_bridge.py`, `09_Appraisals/Version_Four/assets/satellite_13_terrace_court.png`, `09_Appraisals/Version_Four/generated/section_01_right_dot_grid_690bd7e68b8f5465926045fc.svg`, `09_Appraisals/Substantiation/690bd7e68b8f5465926045fc_01_right_20260515T052623.json`, `src/pages/MethodologyPage/{MethodologyPage.tsx,MethodologyPage.module.css}`, `src/routes/methodology.tsx`.

**Files (modified):** `09_Appraisals/Version_Four/preview.html` (locked PNG → live HTML), `templates/seller_report_v2.html` (optional new block), `scripts/generate_appraisal_report.py` (template integration), `netlify/functions/system-monitor.mjs` (new endpoint), `src/pages/OpsPage/panels/AppraisalPipelinePanel.tsx` (highlight picker UI), `src/routes.ts` (new route).

**Outstanding (Phase B):** Generalize AppraisalPipelinePanel to read both property_reports and appraisal_pipeline natively (retire the bridge), templatize other §s (02 personas, 03 valuation, 04 targeting, 05 presentation, 06 trust), restructure seller_report_v2.html to match V4 design system end-to-end, build the editorial-overrides UI in the ops panel, run satellite_analysis enrichment retrospectively on sold cohort (unlocks cul-de-sac + bushland boundary as cohort-comparable attributes).

**Recurrence:** First.

---

## [VM-IP-DOC-01] Corrected stale VM IP in CLAUDE.md — 11:30 AEST
**Symptom:** Will attempted scp to `35.189.1.73` (the IP cited in CLAUDE.md) and got port 22 timeout.
**Root cause:** CLAUDE.md said `IP: 35.189.1.73` but the actual external IP since the April migration is `34.40.230.132` (confirmed against GCE metadata + ipify). The recovery guide on Drive had the correct IP; CLAUDE.md was never updated post-migration.
**Fix:** Updated both `/home/fields/CLAUDE.md:109` and `/home/fields/Fields_Orchestrator/CLAUDE.md:109` from `e2-medium, IP: 35.189.1.73` to `e2-standard-2, IP: 34.40.230.132` (also corrected the machine type which had been upgraded).
**Files:** `/home/fields/CLAUDE.md`, `/home/fields/Fields_Orchestrator/CLAUDE.md`.
**Recurrence:** First.

---

## [APR01-MERGE-01] Non-destructive merge: apr01 snapshot → live Gold_Coast — 12:00–12:06 AEST
**Symptom:** Live `Gold_Coast.<suburb>` collections had lost `scraped_data` blocks (images, timeline, valuation, rental_estimate) for the off-market property cohort. Investigation today found Will's local Mongo still had this data intact for 214,840 properties across 81 suburbs (3.48M image URLs, 979K timeline entries, all on Domain CDN). Mongo dump from 2026-04-01 was SCP'd to VM.
**Root cause:** The Cosmos→Mongo migration (2026-04-12) appears not to have carried the full `scraped_data` block across for off-market records. Image URLs were never dead — they were lost from the live DB. The Azure Blob shutdown was a red herring for this dataset.
**Fix:** mongorestore --gzip --archive of the 261 MB Apr 1 dump into side namespaces (Gold_Coast_apr01, Gold_Coast_Recently_Sold_apr01, etc.) with --noIndexRestore for cross-version safety (snap was Mongo 8.0.4, VM is 7.0.31). Then bulk merge into live by `_id` — for every snap doc with `scraped_data.images.0` populated, wrote the full snap `scraped_data` to live `scraped_data_apr01_recovered` sidecar + `apr01_recovery_at` + `apr01_recovery_source: "mongodump_20260401"`. Idempotent (skips docs already recovered). Order: target suburbs first (Robina, Merrimac, Varsity Lakes, Burleigh Waters), then remaining 77.
**Results:** 214,840 scanned, **204,702 recovered**, 10,138 missing_live (all Surfers Paradise — see SURFERS-GAP-01 below), 0 errors. End-to-end verified: sample URL returns HTTP 200 image/webp 152KB from rimh2.domainstatic.com.au.
**Files:** ad-hoc Python merge script (not retained), log at `logs/apr01_merge_20260515_1200.log`, side namespaces in local Mongo (`Gold_Coast_apr01`, `Gold_Coast_Recently_Sold_apr01`, `Gold_Coast_Currently_For_Sale_apr01`, `Gold_Coast_URL_Tracking_apr01`, `Gold_Coast_POIs_apr01`, `property_data_apr01`), archive at `/tmp/mongodump_apr01.archive.gz` (274 MB).
**Recurrence:** First.

---

## [APR01-MERGE-02] Phase 2 address-match merge from legacy snap DBs — 12:09–12:12 AEST
**Symptom:** After Phase 1, two known appraisal-cohort properties (27 Corvus Way, 11 Outrigger Drive — both sold Nov 2025) still had zero images. Both pre-dated the live Gold_Coast consolidation (2026-03-05). Their data may live in the deprecated `Gold_Coast_Recently_Sold_apr01` and `Gold_Coast_Currently_For_Sale_apr01` side namespaces with a different `_id` scheme.
**Root cause:** Phase 1 join was `_id`-based. The legacy DBs (consolidated into Gold_Coast on 2026-03-05) use separate `_id`s — same docs were created independently. Must match by complete_address (case-insensitive, whitespace+punctuation normalized).
**Fix:** Built an address-normalized lookup table per suburb (live `_id`s keyed by both `complete_address` and `street_address`), iterated all snap docs in Recently_Sold_apr01 and Currently_For_Sale_apr01, matched by normalized address. For matches without existing Phase-1 sidecar, wrote a Phase-2 sidecar (`scraped_data_recently_sold_apr01_recovered` or `scraped_data_for_sale_apr01_recovered`).
**Results:** 2,358 snap docs scanned, 1,415 matched to live, **608 new sidecars written**, 807 skipped (already had Phase 1), 943 no live match (Surfers stragglers + address drift). Combined with Phase 1: **205,310 total properties recovered**.
**Files:** `/tmp/phase2_merge.py` (ad-hoc, retained for audit), log at `logs/apr01_phase2_20260515_1209.log`.
**Recurrence:** First.

---

## [NETLIFY-APR01-WIRING-01] Wire apr01 sidecars into image extraction fallback chain — 12:14 AEST
**Symptom:** Website image outage from 2026-05-13 IMAGE-OUTAGE-001 affected 205,310 properties that now have apr01 recovery sidecars on the VM, but the Netlify functions had no fallback to read those sidecars — they only checked `domain_hero_image_url`/`v2`, then dead Azure URLs.
**Root cause:** The image-recovery sidecar fields (`scraped_data_apr01_recovered`, `scraped_data_recently_sold_apr01_recovered`, `scraped_data_for_sale_apr01_recovered`) were freshly added to live docs but no reader knew about them.
**Fix:** Inserted three new lines in the extractPhotos fallback chain — `pushFromArray((doc.scraped_data_apr01_recovered || {}).images)` and the two Phase-2 sidecar equivalents — placed AFTER v2 scrape fields (freshest, Domain CDN) and BEFORE `property_images_original` (older, may have stale URLs). Applied to the central helper `shared-utils.mjs` plus 7 local copies (`properties-for-sale.mjs`, `property.mjs`, `discover-feed.mjs` [different shape — added as Priority 0.5 early-return block], `decision-feed.mjs`, `decision-feed-v3.mjs`, `analyse-property.mjs`, `recently-sold.mjs`). All 8 files pushed to `Will954633/Website_Version_Feb_2026`; Netlify auto-deploys.
**Verify:** `/for-sale` page rendering (~60-70% of cards have images, no image-related network errors). Console clean. Deploy logged + website-change-log entry created.
**Files (website repo paths):** `netlify/functions/shared-utils.mjs`, `properties-for-sale.mjs`, `property.mjs`, `discover-feed.mjs`, `decision-feed.mjs`, `decision-feed-v3.mjs`, `analyse-property.mjs`, `recently-sold.mjs`.
**Recurrence:** First.

---

## [SURFERS-GAP-01] Surfers Paradise live collection missing 10,138 records vs Apr 1 snapshot — 12:06 AEST
**Symptom:** During APR01-MERGE-01, Surfers Paradise was the only suburb where target docs were missing entirely from live. 10,138 of 12,070 snap-with-images docs (84%) had no matching `_id` in `Gold_Coast.surfers_paradise`. Every other suburb came back at 100% recovery rate.
**Root cause:** Unknown. Possibly a misfired migration query, a delete script that targeted Surfers specifically, or a consolidation step that dropped Surfers data. Surfers was not a target market suburb so wasn't being actively monitored.
**Fix:** None yet — needs investigation. Data is preserved in `Gold_Coast_apr01.surfers_paradise` (12,070 docs intact) so recovery is possible by direct insert if root cause confirms safe.
**Files:** `Gold_Coast.surfers_paradise` (live, ~partial), `Gold_Coast_apr01.surfers_paradise` (full).
**Recurrence:** First. **P1 data integrity issue — investigate before any further consolidation work.**

---

## [SCARCITY-AMEND-01] Created amendments doc + operating framework for scarcity legality — 22:30 AEST

**Symptom:** Legal review (`09_Appraisals/Scarcity_Legality/`) identified high-risk absolute scarcity claims in appraisal V4 and mini-site planning docs, but no consolidated implementation document existed to govern the response.
**Root cause:** Audit and playbook produced (files 00–06), but the operational synthesis — what to change now + how to work going forward — was missing. Will also received external consultant feedback proposing a strategic reframe (scarcity → irreplaceability / substitutability) that needed integrating with the audit findings and page-level review.
**Fix:** Wrote `07_amendments_and_operating_framework.md` — synthesises audit + consultant feedback + page-level review into: (a) two-track model (internal intelligence stays rich, client-facing translated), (b) 38 specific amendments (appraisal P0/P1, mini-site doc P0, live property-page pipeline P0, `/methodology` page build P0), (c) nine standing operating rules, (d) full replacement copy for V4 Section 01, (e) governance + quarterly re-audit cadence. Updated `00_README.md` index to reference the new doc.
**Files:** `09_Appraisals/Scarcity_Legality/07_amendments_and_operating_framework.md` (new), `09_Appraisals/Scarcity_Legality/00_README.md` (index update).
**Recurrence:** First.
