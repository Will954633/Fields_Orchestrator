
---

## [WATCHDOG] Step 107 (Database Audit & Validation) — 00:58 AEST

**Failure class:** UNKNOWN
**Cause:** No recognizable error pattern — check logs manually
**Action taken:** ESCALATED to Claude (id=None)
**Result:** queued — awaiting repair-agent or Claude

## [BRIEF-QUESTIONS-NOT-TAPPABLE] Will's 9 taps answered 1 of 5 briefed questions — two unlinked ask-Will channels — 14:20 AEST
**Symptom:** Samantha's 2026-08-16 weekly brief opened "You didn't answer last week's 5 questions
(you tapped 9 buttons though)". Will had tapped every button he was sent and reasonably believed he
had answered. The same 5 questions were re-asked a week later.

**Root cause — NOT a lost tap.** All 9 taps were received, stored and consumed correctly
(`ceo_chat_messages` seq 2-10; the 6 `RV YES` taps recorded `will_verdict` within 2 min). The defect
is that there were **two ask-Will channels that did not know about each other**:
1. The weekly brief — sent via `telegram_notify.py "<msg>"`, which **had no way to attach a keyboard**:
   `reply_markup` was a Python-only kwarg, never exposed as a CLI argument. So the brief's 5 questions
   were *structurally unanswerable by tapping*.
2. `recommendation_approval.py` — the only tokenised channel, selecting on a bare `{"status": "open"}`
   with no knowledge of the brief, i.e. bypassing Samantha's triage entirely.
Overlap on 2026-08-13 was **exactly 1 of 5** (REC-seo-001). The other 5 buttons were items Samantha
had deliberately *filtered out* of the brief.

Secondary: Step 1 of `samantha_weekly_prompt.md` reads verdicts out of `ceo_chat_messages` text only
and never cross-checks `rl_recommendations.will_verdict`, so a verdict arriving by token read as
unanswered — hence "0 of 5" when the truth was 1 of 5.

**Fix:**
1. `telegram_notify.py` — added `--reply-markup-json`, so the existing `reply_markup` kwarg is reachable
   from the CLI. Rejects non-JSON and non-object input, and refuses `--queue` combination (the digest
   re-renders the text; buttons would be silently lost — that silent drop would recreate this exact bug).
2. `recommendation_approval.py` — new `mint` (token without sending, for the brief to embed) and
   `keyboard` (token list -> inline-keyboard JSON) commands. `mint` reuses a live token rather than
   issuing a second one for the same question, since two valid tokens means a tap on the older message
   records against a stale ask.
3. `recommendation_approval.py cmd_send` — now skips any rec whose `briefed_in` contains the current
   ISO week, so the mid-week channel cannot out-shout the brief on a question the brief already owns.
4. `samantha_weekly_prompt.md` — Step 6 rewritten to mint-then-attach (tokens printed beside each
   numbered question, so tap and typed reply are interchangeable); Step 1 now requires counting answers
   from `will_verdict` regardless of channel.
5. Corrected a **stale docstring** in `telegram_notify.py` that stated the bridge never receives
   `callback_query` and inline buttons "fire into a void". Untrue since 2026-08-13
   (`ceo-telegram-bridge.py:1389` requests it, `extract_message_text` :1355-1375 translates it). Left
   alone it would have kept steering maintainers toward the reply keyboard that already failed.

**Verification:** faked `requests.post` and asserted the real payload — `reply_markup` present,
`callback_data` `RV YES 436F`, token present in body text; then fed that exact `callback_data` and the
typed form `RV YES 436F` through the live `VERDICT_RE`, both yielding `('YES','436F','')`. So a tap and
a typed reply are provably the same record. CLI guards confirmed rejecting bad JSON, JSON arrays, and
`--queue` combination. `mint` confirmed refusing a non-open rec and a non-existent id.

**⚠ One reported defect was NOT real and was dropped.** The investigation claimed the 4/day drip cap is
"computed once per process, so three invocations sent 6". It is a *database query*
(`count_documents({"sent_at": {"$gt": since}})`) re-evaluated per invocation, so it does hold across
processes; the 6 sends at 09:44:09/35/38 got through via `--force`. Changing it as proposed would have
fixed nothing. Verified before writing code, not after.

**Files:** scripts/telegram_notify.py, 16_General_Reinforcement_Learning/recommendation_approval.py,
16_General_Reinforcement_Learning/samantha_weekly_prompt.md
**Recurrence:** First for this defect. But it is the same *class* as [APPRAISAL-AERIAL-SCRATCH-LEAK] and
the valuation-sensor bug approved the same day: **a reader silently pointed at a different source than
the writer.** The durable lesson is that two channels asking the same person the same kind of question
must share one selection, not be kept in sync by discipline.

## [GC-GEO-OVERBROAD] Home Owner funnel GC campaign was targeting 900k people, not three suburbs — 22:40 AEST
**Symptom:** The GC seller-lead campaign (`120251770885910134`, built 2026-07-28) targeted a
25-mile radius from Surfers Paradise — **772,800–909,200 reachable**, covering Logan, Tweed Heads
and southern Brisbane. Will can only personally serve Robina / Varsity Lakes / Burleigh Waters,
and the campaign's form thank-you page promises "Will will call you shortly."
**Root cause:** An `ad_decisions` entry dated 2026-07-27 records Will's directive to "tighten geo
to core southern suburbs on all over-broad adsets". This campaign was created the *next day* and
never received it. The adset names ("GC 25mi") were accurate, so nothing looked wrong.
**Fix:** Retargeted all three live adsets to Meta neighbourhood keys — Robina `2687074`,
Varsity Lakes `2674227`, Burleigh Waters `2719184` — `location_types: ["home"]` only, which also
drops tourists (material on the Gold Coast). Reachable now **34,600–40,700**. Adsets renamed so
the name states the real targeting.
⚠ **Non-obvious constraint worth remembering:** the `HOUSING` special ad category enforces a
**17 km minimum radius** (`#2909052`) — a first attempt at 3 km was rejected. That floor applies
only to *radius / dropped-pin* targeting. **Named neighbourhoods are accepted and have no minimum**,
which is the only way to target a single suburb for a housing ad.
**Files:** no code — Meta Graph API writes to adsets `120251770889000134`, `120251770891620134`,
`120252303732870134`. Logged to `system_monitor.ad_decisions`.
**Recurrence:** First.

## [AN3-CLAIM-UNSUBSTANTIATED] Live ad copy carried a sales pair that does not exist — 22:40 AEST
**Symptom:** `AN3_neighbour` (GC campaign, paused) claimed "a home near you sold for **$55,000
more** than a near-identical one two streets away — and it sold **59 days faster**", plus "sat 61
days, and came down".
**Root cause:** The figures were carried over from the Brisbane geo-neutral test copy and had never
been checked against `Gold_Coast`. Searched every sold house in the three suburbs with a
days-on-market value across **both** DOM fields (`days_on_market` 687/998, plus `days_on_domain`) —
838 rows, tolerance ±$3,000 and ±3 days: **zero matching pairs**. No price reduction is recorded for
any candidate, so "came down" was also unsupported.
**Fix:** Rebuilt on a verified pair — 14 Bergamo Drive (4 days, $1,400,000, 10 Oct 2025) vs
19 Tourangeau Crescent (61 days, $1,280,000, 3 Nov 2025), both 3 bed / 2 bath / 2 car on **exactly
350 m²**, sold three weeks apart, same `days_on_market_source`. Real gap **$120,000 / 57 days** —
more than double the claimed one. Also removed the causal line ("launched on a number the market
didn't support"): we observe *what*, not *why*, and our data names the listing agencies, making it
a verdict on an identifiable agent (POA ss207-209). Pair now published de-identified.
⚠ Rule 8 caught a wrong field name mid-investigation: `land_size` is 6% filled, **`land_size_sqm`
is 69%** — the first search wrongly concluded land data was absent.
**Files:** `03_Facebook/Home_Owner_Lead_Funnel_Search/AN3_GC_REBUILD_2026-08-18.md`,
`render_gc_relaunch_cards.py`, `00_EVIDENCE_BASE.md` §2b/§2c/§4.
**Recurrence:** First.

## [EVIDENCE-469K-WRONGLY-BANNED] I banned a publishable figure by conflating two studies — 22:40 AEST
**Symptom:** `00_EVIDENCE_BASE.md` §4 instructed "quote $469,000 *only* with 'same suburb, twelve
months' attached", and I repeated that to Will as a reason to prefer $227,500. Will pushed back:
$469,000 is already published on the live `/offmarket` page.
**Root cause:** Two different experiments share a subject but not a pool. `three_comp_selection.py`
(n=656/313) has a loose *same-suburb, 12-month* pool; `RESULT_dispersion_512.md` (n=512) matches on
type, bedrooms, bathrooms **and** land ±20%, median qualifying pool 21 sales. Both report ~$469,000
but they are not the same number, and the dispersion one is well constructed.
**Fix:** Corrected §4 with a comparison table. The real constraint is narrower than stated — do not
attach $469,000 to the QLD **statutory** three-comparable definition, because the statutory pool is
*wider* (5km, crossing suburbs) and pairing them misattributes the population.
**Files:** `03_Facebook/Ad_Categories/Valuation/00_EVIDENCE_BASE.md` §4.
**Recurrence:** First.

## [RUNNER-OOM-NO-RESTART] GitHub Actions runner dead 1.5 days — stock unit has no Restart= — 21:00 AEST
**Symptom:** Background-process audit found `actions.runner.Will954633-fields-automation.fields-vm-runner`
in `failed (Result: oom-kill)` since 2026-08-17 08:17 AEST. All 12 `fields-automation` workflows —
including auto-article generation — had not run for 1 day 12h. Nothing reported it.
**Root cause:** Two faults compounding. (1) The stock GitHub runner unit ships with **no `Restart=`
directive**, so a single OOM is permanent until someone notices. (2) The OOM itself came from root-disk
/memory pressure: `/` was at 90% (88G/97G).
**Fix:** Drop-in at `…fields-vm-runner.service.d/restart.conf` with `Restart=on-failure`,
`RestartSec=30`, `StartLimitBurst=5`, `StartLimitIntervalSec=600`. Deliberately **on-failure, not
always** — the runner exits 0 on purpose after a self-update ("stop the service, no retry needed") and
`always` would fight that; systemd did record the OOM as a failure, so `on-failure` catches it.
⚠ Gotcha: `StartLimit*` belong in `[Unit]`. Placed in `[Service]` systemd logs
`Unknown key name … ignoring` and silently leaves the 10s default window. Caught and corrected.
**Files:** `/etc/systemd/system/actions.runner.Will954633-fields-automation.fields-vm-runner.service.d/restart.conf` (new).
**Recurrence:** First for this unit; OOM on this VM is Nth (`[OOM-CHROME-KILL]`, `[OOM-CHANGE-DETECTOR]`).

## [ARTIFACTS-ON-WRONG-DISK] 12G cover cache on the 97G root while a 738G disk sat 47% empty — 21:05 AEST
**Symptom:** `/` at 90%. `artifacts/` was 23G of it, of which `appraisals_v4/assets/img` was **12G**
(8,372 `cover_hero_*.jpg`). `resource_guard` logged `level=ok … reclaimed 0MB, disk 90%->90%` every 90s.
**Root cause:** Not junk — a legitimate cache on the wrong filesystem. `resource_guard.py`'s allowlist
comment explicitly forbids sweeping it ("deleting it forces a full, expensive rebuild of the catchment"),
correctly. But `/data/blobs` is a **separate 738G disk at 53%** with 331G free, so the cache never needed
to be on root at all. The guard was right to refuse to delete and had no other move available to it.
**Fix:** Relocated, **deleting nothing** — `rsync -a` to `/data/blobs/appraisal_assets/img`, verified
identical (8374=8374 files, byte-for-byte equal file totals, empty `--dry-run --itemize-changes`, 20
random md5s), then symlinked `assets/img` → the new path and removed the original.
**Root 90% → 79%, 11G reclaimed with zero data loss.** Verified end-to-end by rendering a real appraisal
through the symlink (`--strict-photos`): resolved `cover_hero=boundary_aerial`, not the placeholder, and
the cover PDF was rasterised and read back visually — correct aerial for 5 Chantilly Place, Robina.
Updated the `resource_guard.py` allowlist comment: it claimed **95** live appraisal PDFs; the real count
re-measured today is **682 refs across 278 pipeline docs** (7x stale since 2026-08-15). Embedded the
recount snippet in the comment so the number can't silently rot again.
**Files:** `scripts/resource_guard.py` (comment only, no logic change); filesystem move.
**Recurrence:** Nth — root-disk pressure with no alert was already flagged unfixed on **2026-08-15**
("`/` reached 100% silently … the only disk watcher covers the blob disk"). Still true: see below.

### Not fixed — needs Will's decision
- **Root disk still has no alert.** `resource_guard`'s ineffective-cleanup escalation
  (`CLEANUP_INEFFECTIVE_BYTES`) only fires at `DISK_CRIT_PCT` 92%. Root sat at 90-91% for days, just
  under it, emitting `level=ok`. Same gap as 2026-08-15, now with a second incident behind it.
- ~~**~10 permanent false STALEs on the health board.**~~ **⚠ THIS CLAIM WAS WRONG — see
  `[RL-SENSORS-HIDDEN-NOT-STALE]` below for what was actually happening and the fix.** I audited with
  my own `cadence×1.5` rule instead of reading the board's logic, and reported a symptom the board was
  not in fact showing. The memory note `health_board_paused_vs_dead` warns about exactly this.
- **7 jobs erroring**, longest `offmarket_coverage_scraper` (20 days, `JSONDecodeError: Unterminated
  string` — truncated response). Also `search_intent_gsc` (`invalid_scope`, OAuth scope lost),
  `seo_dashboard` (Sheets 503), `rl_weekly_seo` (rc=124 timeout), `youtube_brain_ingest` (11 available,
  0 transcribed), `ops_cycle` (rc=1), `offmarket_deck_targeted_rebuild` (Rule 7b assertion firing
  correctly: "rebuilt every target and closed NO gaps — the builder is not reading the attribute").

## [RL-SENSORS-HIDDEN-NOT-STALE] 10 live weekly sensors suppressed as "paused" — 21:25 AEST
**Symptom:** I first reported these as permanent false STALEs. That was wrong, and the way it was wrong
matters more than the fix: they were rendering as **KNOWN-GAP (deliberately paused)**, i.e. invisible.
**Root cause — two errors cancelling out.** (1) The 2026-07-30 `[RL-AGENTS-PAUSE]` added all ten domain
sensors to `_PAUSED_JOBS`. That pause stopped the DAILY self-pacing dispatchers/cycles, but the
**Sunday `weekly_cycle.sh` crons kept running**, and the weekly agent session runs each sensor per its
`*_prompt.md`. So the sensors came back to life and nobody removed the pause entries. (2) Each declared
`cadence_hours=24` while actually running every 168h. Suppression keys on `age_h > cadence_h`, so a
healthy 63h-old Sunday heartbeat was always > 24 and always suppressed. Either bug alone would have
been visible: an un-paused job with the wrong cadence reads STALE and gets noticed; a paused job with
the right cadence trips the "LISTED AS PAUSED but ran Nh ago" safety valve. **Together they rendered a
live sensor as a deliberate gap — and had any of the ten genuinely broken, the board would have shown
nothing.** The registry's own contract ("can hide a job only while genuinely silent") was violated
without ever firing its own guard.
**Evidence it was live, not paused:** all ten last ran **Sun 2026-08-16** at their exact `weekly_cycle`
slot times — ops 06:00, ops_integrity 06:12, geo 08:00, ads 09:00, seo 16:04, articles 16:44, onsite +
reward 17:17, friction 17:25, arm_grades 17:23. By contrast `rl_conductor`, `rl_personalization_policy`
and `rl_selftest` last ran **2026-07-30**, the pause date itself — those are genuinely dead and stay
paused. `job_runs` keeps ONE doc per job (`replace_one(upsert=True)`), so run intervals cannot be
measured from history — the Sunday slot-time correlation is the available evidence.
**Fix:** (a) `cadence_hours` 24 → 168 in the ten sensor scripts (`ops_integrity` also `stale_hours`
40 → 180); (b) removed those ten from `_PAUSED_JOBS`, leaving the four genuinely-dead ones, with a
comment explaining why and warning not to re-add without checking `run_at`; (c) backfilled the stored
`cadence_hours` on the ten `job_runs` docs, since the scripts only rewrite it on their next Sunday run
and the board would otherwise have read STALE for five days.
**Verified:** re-ran `main_site_health_check.py`. Process Registry **85% → 91%**, STALE **20 → 10**,
OK 155 → 165, and all ten sensors render OK with real detail. They are now genuinely monitored: miss a
Sunday and they go STALE.
**Files:** `16_General_Reinforcement_Learning/{ops_signal,ops_integrity,geo_signal,ads_signal,
seo_signal,articles_signal,reward_ledger,onsite_signal,onsite_friction_signal,arm_grader}.py`,
`scripts/main_site_health_check.py`.
**Recurrence:** Second — `[HEALTH-BOARD-PAUSED-VS-DEAD]` (2026-08-05) built `_PAUSED_JOBS` after 29
deliberate pauses buried 3 real failures. This is the same registry failing the opposite way: an entry
that outlived its pause. **A pause entry needs an expiry review, not just a reason and a date.**

## [DISK-WARN-NO-ESCALATION] Ineffective-cleanup alert only existed at CRITICAL — 21:30 AEST
**Symptom:** `/` sat at 90-91% for days while `resource_guard` freed 0MB every 90s and published
`level=ok disk=90%` to its own heartbeat. No alert of any kind. The 12G cause was found only by a
manual audit (see `[ARTIFACTS-ON-WRONG-DISK]`).
**Root cause:** The `ineffective = freed < CLEANUP_INEFFECTIVE_BYTES` escalation was nested inside
`if aggressive:` — i.e. it could only fire at `DISK_CRIT_PCT` (92%). The WARN band (85-92%) ran cleanup
and then did nothing at all, not even setting `level`, so the non-aggressive branch left `level="ok"`.
A disk stuck just below the critical threshold was the one case that alerted least.
**Fix:** Added an `else` branch for the WARN band: sets `level="warn"`, and if cleanup freed
<200MB while still ≥85% for ≥`SUSTAIN_N` consecutive checks, sends an orange "cleanup is not
reclaiming" alert naming the largest directories — same diagnosis as the critical path, calmer wording,
separate cooldown key (`disk_ineffective_warn`, 6h vs 1h) so it cannot consume the critical alert's
budget.
**Verified** by monkeypatching metrics and capturing `send_telegram` across three bands:
93% + 0MB freed → 🔴 critical alert (unchanged); 86% + 944MB freed → **no alert** (cleanup worked);
86% + 0MB freed → 🟠 warn alert with the directory list; 70% → silent. Live `--dry-run` at the real
79% is silent, as expected.
**Files:** `scripts/resource_guard.py`.
**Recurrence:** Second — flagged unfixed on **2026-08-15** ("No alert on root disk. The only disk
watcher covers the blob disk. `/` reached 100% silently"). Now fixed.
