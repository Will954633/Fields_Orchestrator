
---

## [WATCHDOG] Step 6 (Batch Valuate Properties) — 01:29 AEST

**Failure class:** UNKNOWN
**Cause:** No recognizable error pattern — step failed with non-zero exit code
**Action taken:** ESCALATED to Claude (id=watchdog_6_1780673380)
**Result:** queued — awaiting repair-agent or Claude

---

## [MINISITE-HEALTH] Mini-site data dictionary + completeness/freshness checker — 08:46 AEST

**Symptom:** No systematic way to know whether the dynamic data on each house mini-site
(`property_reports`) is complete and fresh, or which fields are stale/missing per home.
**Root cause:** New capability, not a regression. Mini-site fields span two freshness tiers
(nightly-refreshed "living" data vs build-time slots that only refresh on rebuild) with
freshness signals scattered across the report doc and several upstream `precomputed_*`
collections — easy to have silently stale data.
**Fix:** (1) Documented every dynamic field, its data flow, updating process, freshness
signal, completeness rule and stale-after threshold in `MINISITE_DATA_DICTIONARY.md`.
(2) Built `scripts/minisite_health_check.py` — evaluates all 18 reports field-by-field,
emits OK/STALE/MISSING/PENDING-EXPECTED/ERROR/UNKNOWN/KNOWN-GAP per field, uses
expected-nightly-run-date logic so market/living data flags the moment it misses one run,
and writes per-run snapshots to `system_monitor.minisite_health_snapshots` for "last changed"
tracking. Two parsing bugs fixed during build (market_charts `_id` suburb prefix; valuation
freshness uses build-time `comps_resolved_at` not an unreliable subject lookup).
**Finding (real issue surfaced by the tool):** `activity_refreshed_at` is frozen at build
time fleet-wide (e.g. 25 Huntingdale stuck at 2026-05-23) while `data_pull_date` and
`comparables_refreshed_at` update nightly — the activity-feed refresh step is not running for
existing reports. Separately, several reports (1 Glenside, 9/12 Longueville, 3 Lumley) are
fully frozen since mid-May (stuck builds). To be investigated.
**Files:** MINISITE_DATA_DICTIONARY.md (new), scripts/minisite_health_check.py (new).
**Recurrence:** First

---

## [MINISITE-HEALTH] Google Sheet dashboard + daily sync — 09:10 AEST

**Symptom:** Health audit only existed as CLI output; no at-a-glance surface for Will.
**Root cause:** New capability.
**Fix:** Built `scripts/minisite_health_to_sheet.py` — runs `run_audit()` then builds a
multi-tab .xlsx (Dashboard + one field-level tab per home, status cells colour-coded) and
imports it into the Drive folder `1x6GGEubGPhsPMUhaN8mKUpLBTHEVZs11` as a Google Sheet,
reusing one stable file ("Mini-Site Health"). The Sheets API is disabled on the OAuth
project (6178359532, project `fields-estate` can't enable it), so it uses Drive import of
xlsx instead — works with the existing `auth/drive` scope, no API enablement needed.
Added daily cron at 00:30 AEST (after the ~23:30 nightly report refresh) which runs the
sync (audit + snapshot persist + sheet rebuild in one).
Sheet: https://docs.google.com/spreadsheets/d/1afRguQmChK1Wa-_VM66xl1RjKYK1ytwcmas4soQhUzA/edit
**Files:** scripts/minisite_health_to_sheet.py (new), scripts/minisite_health_check.py
(refactored to expose run_audit()), crontab.
**Recurrence:** First

---

## [MINISITE-ACTIVITY] Home-tab activity feed frozen fleet-wide — 10:14 AEST

**Symptom:** `activity_refreshed_at` stuck at build time on every real report (e.g. 25
Huntingdale frozen at 2026-05-23) while `comparable_events`/`comparables_refreshed_at`
updated nightly — surfaced by minisite_health_check.
**Root cause:** `refresh_property_reports.py` nightly job refreshes the comparable feed for
every report, but only rebuilt the `activity[]` timeline (and stamped `activity_refreshed_at`)
for the hardcoded legacy Merrimac `REPORT_CONFIGS`. Real reports' activity feed was only ever
set once, at build time.
**Fix:** Added `rebuild_activity_feed()` — folds tonight's `comparable_events` (mapped to
activity items, newest-first) on top of the preserved build-narrative items, deduped and
capped, then sets `activity` + `activity_refreshed_at` inside `refresh_comparables_for_doc`
(runs for EVERY report). Verified on 25 Huntingdale: feed went from 16 stale build items to
10 items led by fresh "just listed" events; `activity_refreshed_at` now current. Takes effect
on the 23:30 AEST nightly cron automatically.
**Files:** scripts/refresh_property_reports.py.
**Recurrence:** First

---

## [MINISITE-SEASONALITY+DRIFT] Real seasonality resolver + valuation/narrative drift — 10:28 AEST

**Symptom:** Process-tab seasonality strip was fixture placeholder (no `seasonality` slot in any
real report); build-time slots (valuation, market narrative) had no way to detect when the
market moved underneath them.
**Fix:** (1) `scripts/precompute_seasonality.py` computes per-suburb monthly sale-price
seasonality from `scraped_data.property_timeline` House sold events, growth-DETRENDED (price /
year-median, median-centred) with sample-size guards (≥150 total, ≥8/month). Writes
`Gold_Coast.precomputed_seasonality` (monthly cron, 1st @ 05:45). (2) `refresh_property_reports.py`
applies it to every report nightly + approves the `seasonality` slot — backfilled all 12 live
reports (Robina peak Dec +6.1% / trough Apr −4.8%). (3) Drift detection in
`minisite_health_check.py`: market narrative compared to live precomputed market data (flags
price >3%, DOM >7d, YoY >2pp moves), valuation comps vs newest suburb sale (>30d). Caught a real
one: 12 Beaconsfield narrative says YoY 6.8% but live is 15.0%. (4) Frontend copy fixed
(SeasonalityStrip.tsx) — removed inaccurate "southern Gold Coast / matched-cohort / suburb too
few" wording now that real suburb house data flows.
**Files:** scripts/precompute_seasonality.py (new), scripts/refresh_property_reports.py,
scripts/minisite_health_check.py, MINISITE_DATA_DICTIONARY.md, SeasonalityStrip.tsx (website),
crontab.
**Recurrence:** First

---

## [SEARCH-INTENT] Search Intent Explorer button "not working" — slow/hanging load — 11:30 AEST

**Symptom:** The Search Intent Explorer button on the ops dashboard (Marketing tab) appeared
broken — the new tab hung/never painted. Will needed up-to-date market research.
**Root cause:** Not a crash and not stale data — the underlying collections are fresh
(latest 2026-06-04). `search-intent-explorer.mjs` loaded EVERY historical document from 11
collections (~50K rows) and embedded them inline as a single 14.4 MB HTML page. The page only
renders top-N per tab, so >95% of the payload was unused. Server build was 6.8s and the browser
needed ~11.5s to download+parse 14.4 MB — long enough that the tab looked dead and Will gave up.
Payload grew unbounded because the daily collectors keep appending.
**Fix:** Scoped the five fresh, high-volume collections (search_suggestions, search_paa_questions,
search_reddit_posts, search_trends, search_youtube_suggestions) to a recent window — default 30
days, overridable via `?days=N` (clamped 7..90) and shown as a "Last 30d" header chip. Left the
small/stalled-collector collections (ad_queries, search_console, youtube competitor channels/videos,
intent_analysis, scored_questions) loading in full so their tabs never go blank. Also truncated
Reddit `selftext` to 200 chars (it's only used for client-side search matching, never displayed —
saved >1 MB). Payload 14.47 MB → 6.20 MB live (will be ~5 MB once full 30d window is cached).
Verified headless: no JS errors, all 11 tabs populated (incl. the full-load Ads/GSC/YouTube tabs),
Overview renders fresh data.
**Files:** netlify/functions/search-intent-explorer.mjs (Website_Version_Feb_2026, commit 4cb7680).
**Recurrence:** First

---

## [MINISITE-PROVENANCE] Build-provenance columns + entry→build gap check — 10:50 AEST

**Symptom:** No surfaced record of when an address was entered vs when its mini-site built; the
10-day stub→build gap on 25 Huntingdale was invisible.
**Root cause:** Provenance timestamps (`created_at`, `build_completed_at`) existed on the doc but
weren't tracked/displayed. (Investigation confirmed the gap was a deploy artifact: poller
restarted 2026-06-02 19:53 AEST and built the waiting 05-23 stub 2 min later; current flow stamps
`build_completed_at` at build completion = the frontend auto-load signal.)
**Fix:** Added info-only provenance fields to `minisite_health_check.py` (Address entered, Build
completed, Entry→build gap; gap >1h flagged STALE, excluded from health %) and Entered/Built/Gap
columns to the Dashboard in `minisite_health_to_sheet.py`. Verified live in the sheet.
**Files:** scripts/minisite_health_check.py, scripts/minisite_health_to_sheet.py,
MINISITE_DATA_DICTIONARY.md.
**Recurrence:** Nth — see [MINISITE-HEALTH] entries above

---

## [MAINSITE-HEALTH] Main-site data dictionary + freshness checker + Google Sheet — 11:45 AEST

**Symptom:** No systematic way to know whether the dynamic data served on the main website
(`fieldsestate.com.au` — Market Intelligence, For Sale/Sold, Property, Articles, Valuation
Accuracy) is current or silently stale. Several feeds were in fact stale and serving old data.
**Root cause:** New capability, not a regression. Main-site data spans three tiers (nightly
`precomputed_*` collections, weekly/monthly feeds, and build-time/request-time blind spots)
with freshness signals scattered across many collections and no single audit.
**Fix:** Replicated the proven mini-site pattern for the main site. (1) `MAIN_SITE_DATA_DICTIONARY.md`
documents every dynamic data point per page → Netlify function → DB.collection → source field →
freshness signal → cadence → stale-after → completeness rule, plus known blind spots. (2)
`scripts/main_site_health_check.py` audits the catalogue (per-suburb fan-out over the 6 target
suburbs), reuses the mini-site `expected_last_run()` missed-nightly-run logic + clock-age
thresholds for periodic feeds, emits OK/STALE/MISSING/ERROR/UNKNOWN/KNOWN-GAP per data point,
and persists per-run snapshots to `system_monitor.mainsite_health_snapshots` (with value-hash
"last changed" tracking). (3) `scripts/main_site_health_to_sheet.py` clones the xlsx→Drive-import
flow to build the "Main Site Health" Google Sheet (Dashboard tab + one tab per page, colour-coded)
in folder `1x6GGEubGPhsPMUhaN8mKUpLBTHEVZs11`, stable URL, with a Telegram one-liner on breach only.
Added nightly cron at 01:00 AEST. Two build bugs fixed: health % >100 (numerator counted info-row
OKs the denominator excluded) and Excel tab titles rejecting `/` ("For Sale / Sold").
**Finding (real issues surfaced by the tool — monitor-only scope, to triage separately):**
article publishing halted since 2026-04-07 (~60d; fields-automation Actions likely failing),
`precomputed_macro_indicators` stale since 2026-03-23 (~75d; powers Crash-Risk charts),
`valuation_accuracy` backtest stale since 2026-03-15 (~83d), `worongary_market_cycle` chart
stale since 2026-04-06 while its other charts refresh nightly, and Reedy Creek/Worongary
recently-sold + active-listing data lagging.
Sheet: https://docs.google.com/spreadsheets/d/1Oa7uZv0shzsxftDYJJ3WErxhr7OZMf_SOxRFawbSgTk/edit
**Files:** MAIN_SITE_DATA_DICTIONARY.md (new), scripts/main_site_health_check.py (new),
scripts/main_site_health_to_sheet.py (new), crontab.
**Recurrence:** First (main-site); mirrors [MINISITE-HEALTH] pattern

---

## [ARTICLES-HALT] Article publishing dead since 2026-04-07 — CI couldn't reach migrated DB — 15:50 AEST

**Symptom:** No new published/created article in `system_monitor.content_articles` since 2026-04-07
(~60d) despite 14 fields-automation GitHub Actions workflows running on schedule; several runs
reported "success" while creating nothing, others failed. Surfaced by main-site health checker.
**Root cause:** The live DB was migrated off Azure Cosmos (subscription DISABLED on the old
`fieldspropertydataserverlesssoutheast-westus2` account) to a **self-hosted MongoDB on the VM**
(`mongod` on the VM public IP `34.40.230.132:27017`, firewalled via `allow-mongodb` to AWS/Netlify
egress + Will's IP). The VM uses it via localhost; the website (Netlify, AWS) reaches it remotely.
The `fields-automation` CI secret `AZURE_COSMOS_URI` still pointed at the dead Cosmos account →
pymongo push got 403 "Subscription owning the database account is disabled". The push step (in
`scripts/push_to_ghost.py` → `_push_article_list`) swallowed push errors, so the job still
git-committed and exited 0 → green-but-empty. GitHub Actions runners are Azure-hosted, so even
pointing the secret at the new VM host fails (not in the AWS allowlist → NetworkTimeout).
**Fix:** Routed CI article writes through the existing authenticated Netlify endpoint (AWS-hosted,
already reachable) instead of direct Mongo. (1) `scripts/push_to_ghost.py`: added an API path —
`post_exists` and `create_ghost_draft` POST to `/api/monitor/content-article{,s,-create,-update}`
with `Bearer OPS_AUTH_TOKEN` when `OPS_AUTH_TOKEN` is set, falling back to pymongo on the VM.
(2) Added `OPS_AUTH_TOKEN` repo secret + exposed it as a step env var in all 14 workflows.
(3) Corrected the `AZURE_COSMOS_URI` secret to the live remote connstr (still used for reads).
**Verified:** dispatched `state_of_market` → "Pushed: 3 | Skipped: 0 | Errors: 0"; 3 drafts landed
(content_articles 133→136). NOTE: articles arrive as **draft** (pre-existing script behaviour) —
they need publishing to reach the public site; auto-publish vs editorial-review-gate is an open
decision (left as draft, matching prior behaviour).
**Gotchas hit:** `.env` defines a stale `GITHUB_TOKEN` that hijacks `gh` (must run gh with
`env -u GITHUB_TOKEN -u GH_TOKEN GH_CONFIG_DIR=…`); first `COSMOS_CONNECTION_STRING=` line in `.env`
is a localhost fallback (the sourced/last value is the live one); Netlify secret-context values are
masked except `dev`.
**Files:** `Will954633/fields-automation`: scripts/push_to_ghost.py + 14 .github/workflows/*.yml;
GH secrets AZURE_COSMOS_URI + OPS_AUTH_TOKEN.
**Recurrence:** First

---

## [SCRAPE-WITHDRAWN] Withdrawn detection silently broken — Domain counts drift from reality — 11:30 AEST

**Symptom:** Mini-site active-listing counts drift above Domain (our Robina 81 vs Domain 76);
for-sale records persist months after listing (back to 2025-08). Surfaced via the 25 Huntingdale
Market-tab review.
**Root cause:** Two compounding scraper failures. (1) `detect_withdrawn.py` (step 113) ran but
got 124 "could not determine status" errors / 1 withdrawn out of ~150 — `shared/domain_fetch.py`
`fetch_with_status` used Bright Data's `format:json` envelope, which for Domain is markedly
flakier (frequent 502 `min_size` empty responses) AND returns `url:null`, so the
`/property-profile/` redirect check never fired. (2) For-sale discovery (step 101) under-collects
(Robina found 15 of 76 expected; several suburbs 0) due to the same Bright Data flakiness + no
reconciliation sweep, so stale listings never get cleared. Bright Data account itself is healthy
(`raw` mode returns full Domain HTML; `country=au` override 502s — must omit it).
**Fix:** Rewrote `_post_unlocker` to always use `format:raw` (reliable), take HTTP status from the
`x-brd-status-code` header, and recover the resolved URL from the page canonical/og:url tag (so
withdrawn-redirect detection works). Bumped `MAX_RETRIES` 2→5 to ride out unlocker flakiness.
Validated: detector now classifies correctly (fresh listings → active incl. a 17d-old one) vs
0/124 before. Takes effect on the nightly step-113 cron.
**Files:** shared/domain_fetch.py, scripts/detect_withdrawn.py.
**Recurrence:** First (Domain/Akamai scraping fragility is recurring — see Chrome-free migration, Sunday-run disable)
