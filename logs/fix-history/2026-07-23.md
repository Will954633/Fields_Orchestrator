# Fix History — 2026-07-23

## [RUNNING-DOC-REPLY-CHANNEL-CHANGED-TO-BODY-TEXT] Replaced unverifiable comment anchors with self-verifying body-text replies as the primary channel — 14:30 AEST
**Context:** After the anchor fix ([DRIVE-COMMENTS-NEVER-ANCHORED] below), Will asked about the PropRadar reply specifically a second time. Re-checked that exact comment's full record and found `quotedFileContent` still empty even after the "fix" — the anchor field is accepted and stored by the API without erroring, but there's no way to confirm from the API alone whether it resolves to a real attached position in the rendered document. Given zero ability to visually inspect the Docs UI, continuing to assert the anchor fix worked would repeat the exact mistake that caused this whole chain: reporting success based on "the API call didn't error" rather than a channel whose delivery can actually be confirmed.
**Structural decision (Will explicitly asked for a structural fix, not another patch):** stop depending on an inherently unverifiable channel. The document BODY has been proven reliable and directly observable all session — Will has read and reacted to body-inserted text repeatedly with zero ambiguity. Comments are optional metadata that may or may not render; body text unambiguously exists once inserted.
**Fix:**
1. New `running_doc.py reply` command — finds the target paragraph, inserts the answer as real body text immediately after it (indented, italicised, prefixed "→ Samantha:"), marks both paragraphs orange, and **re-reads the live document afterward to confirm the reply text is genuinely present before reporting success** — the API call succeeding is not treated as sufficient evidence.
2. Retrofitted all 26 of this session's comment-only replies onto the new channel (content re-used from the existing comments, not retyped) — hit 2 edge cases (the anchor-fix migration's known apostrophe/smart-quote substring-match miss, and a new one: inserting at the very last paragraph in the document merges into that paragraph instead of creating a new one, since its end-index equals the document's absolute end and there's no following newline to split on). Both fixed manually; final verification re-read confirms 26/26 replies present as `→ Samantha:`-prefixed body paragraphs and 0 active (un-actioned) paragraphs remain.
3. `charter.md` rules 2 and 10, and `daily_tasks.md`'s running-doc section, updated: `running_doc.py reply` is now the REQUIRED primary answer channel; `drive_comment.py` reply-in-thread (on a comment Will started, which inherits a real anchor and does work) remains fine as a secondary nicety; fresh `drive_comment.py comment --quote` calls must never be the only place an answer lives. Rule 10 gained a 6th step: verify "items answered" against a live re-count of `→ Samantha:` body paragraphs before declaring the audit done, not just the orange count.
**Files:** `scripts/samantha/running_doc.py` (new `reply` command, `Fields_Orchestrator`, 1 commit); `scripts/samantha/charter.md`, `scripts/samantha/daily_tasks.md` (channel-change rules, 1 commit each).
**Recurrence:** This closes the loop on [DRIVE-COMMENTS-NEVER-ANCHORED] below — that fix addressed the immediate bug (missing anchor field) but this entry is the one that actually resolves the underlying reliability problem (an unverifiable channel), which is the structural question Will asked directly.

---

## [DRIVE-COMMENTS-NEVER-ANCHORED] Every "quoted" comment this session existed but was invisible in context — root-caused and fixed live — 14:14 AEST
**Context:** Mid-sweep, Will asked in chat "where are your replies to my points in my notes?" — a direct signal something was wrong despite `drive_comment.py list` showing all 26+ comments as present.
**Root cause:** `drive_comment.py`'s `cmd_comment()` never set the Drive API's `anchor` field — it only faked "quoting" by prepending `Re: "<quote>" — ` as plain text inside the comment body. Verified directly: fetched comment `AAACDqdH0sU` (the PropRadar reply) via `comments().get()` and confirmed `anchor: None, quotedFileContent: None`. An unanchored comment has no attachment point to any text in the document — it exists (visible via API/comment-history), but does not show as an inline bubble next to the relevant paragraph in the normal Docs editing view, which is exactly why Will couldn't find it while reading the doc normally. My REPLIES (via `cmd_reply`, replying to Will's own pre-existing anchored comments) worked fine — only fresh `comment --quote` calls were broken, which is nearly everything posted this session.
**Fix:** (1) `drive_comment.py`'s `cmd_comment()` now looks up the quoted paragraph via the Docs API (reusing `running_doc.py`'s `paragraphs()`) and builds a real anchor (`{"r":"head","a":[{"txt":{"o":start,"l":length}}]}` — the community-documented Drive API text-anchor format; confirmed empirically that the API accepts and echoes it back, unlike the None returned with no anchor at all). (2) Wrote and ran a one-off migration: found all 26 of this session's unanchored "Samantha:" comments, deleted each, recreated it with a real anchor derived from re-parsing its own `Re: "<quote>" — <text>` content. 25/26 succeeded automatically; 1 failed on a smart-quote/apostrophe character mismatch in the substring match, fixed manually the same way. Verified via a final `comments().list()` sweep: 26/26 mine, 0 unanchored. Migration script deleted after use (one-off, not general-purpose).
**Honest caveat:** I cannot visually inspect the rendered Google Docs UI myself — the fix is based on the API accepting and persisting the anchor field (verified) and the documented/community-confirmed format, not a screenshot confirmation that it renders as a highlighted inline comment. Asked Will to confirm visually.
**Files:** `scripts/samantha/drive_comment.py` (`Fields_Orchestrator`, 1 commit). Migration script was temporary, not committed to keep.
**Recurrence:** First. Meta-note: this was found only because Will explicitly said "I can't find X" rather than assuming silence meant success — a tool reporting "commented on {file}: {id}" with no error is not the same as the comment being genuinely useful; success at the API-call level and success at the actual-goal level (Will can see and read the reply in context) are different things and this session conflated them for 26 comments before being caught.

---

## [SESSION-END-SWEEP-NEVER-TRACKED] The 2-consecutive-clean-sweeps stopping definition never actually ran this session — 14:04 AEST
**Context:** Will asked directly why the sweep mechanism (charter.md's stopping definition, requiring 2 clean passes over 8 dimensions before ending a session) never triggered, despite a long session with real work and an already-fixed running-doc-audit gap earlier the same day.
**Root cause:** the sweep existed only as prose describing a gate to check before stopping — it was never itself a tracked task. Everything that DID happen this session lived as an explicit `TodoWrite` item that got worked through mechanically; the sweep was never added to that list, so nothing forced it to run. Identical failure shape to [SAMANTHA-SESSION-SCOPE-AND-DOC-AUDIT-PERMANENT-FIX] (same day, ~1hr earlier) — a correct, clearly-written rule with no mechanical enforcement point silently doesn't execute, even for the same session that just fixed the exact same failure class for something else.
**Fix:** `charter.md`'s Task board section — the sweep must now be an explicit tracked entry ("Session-end sweep — pass N" with 8 sub-items, one per dimension) added the moment the work queue is about to empty or a session is about to conclude, worked through for real, not marked done until all 8 are genuinely checked. Two consecutive zero-finding passes required to stop. An interrupting request concluding does not exempt the session from still running this.
**Files:** `scripts/samantha/charter.md` (`Fields_Orchestrator`, 1 commit); `samantha_interactive_channel_autonomy.md` memory updated with all 3 same-day incidents + the generalised meta-lesson.
**Recurrence:** 3rd same-class fix in one day (session-scope inference, running-doc audit, now the sweep itself) — all three are "principle existed, no tracked enforcement, silently skipped" using the identical root-cause and identical fix shape. Demonstrates the meta-lesson holds even within the same session that already applied it once.

---

## [TASK-BOARD-SHEETS-API-DISABLED] Task Board Backlog/Decision Log writes were never possible — Sheets API disabled since setup — 13:29 AEST
**Context:** Charter.md has required logging findings to the Task Board's Backlog/Decision Log tabs since 2026-07-15. No session had ever actually written to Sheets programmatically — always deferred, worked around via memory/reports instead. Building `task_board.py` (mirroring `running_doc.py`'s working OAuth pattern) to finally close this gap surfaced why: `Google Sheets API has not been used in project 6178359532 before or it is disabled` — a 403 on the very first `spreadsheets.get()` call, despite the OAuth token already carrying the `spreadsheets` scope (granted since the original 2026-07-15 consent, per [[samantha_management_ai]] memory).
**Root cause:** the OAuth app's own GCP project (`fields-estate-ads`, project number 6178359532 — the same project [[samantha_management_ai]] flags as having Sheets API disabled, previously worked around by writing via a service account instead) never had the Sheets API enabled at the project level. A scope grant on a token doesn't matter if the underlying API is off for the whole project — this is a project-level API activation, unrelated to per-request auth. Every session that deferred a Task Board write was hitting this, whether it knew it or not (most just skipped straight to memory/report — the actual blocker was never diagnosed).
**Fix:** `gcloud services enable sheets.googleapis.com --project=fields-estate-ads` — Will's own `gcloud` CLI session on this VM was already authenticated (`will.simpson@blueoceans.com.au`) with access to this project, so no new credentials were needed. Took ~60-90s to propagate; verified live with a real read (`spreadsheets.get`) then real writes (1 Decision Log row + 8 Backlog rows, this session's findings).
**Files:** `scripts/samantha/task_board.py` (new — `backlog`/`decision` subcommands, same OAuth pattern as `running_doc.py`/`drive_comment.py`).
**Recurrence:** First real fix. This was silently blocking a CLAUDE.md-mandated behavior since 2026-07-15 — worth noting as a lesson: a requirement that's never been verified to actually work (only ever "should log to the board") can be broken from day one and nobody notices, because the fallback (just skip it, log elsewhere) is silent and doesn't fail loudly.

---

## [SAMANTHA-SESSION-SCOPE-AND-DOC-AUDIT-PERMANENT-FIX] Two process failures corrected in charter.md + memory, not just in-conversation — 13:05 AEST
**Context:** Will corrected two failures from the earlier session today: (1) told to "run a fresh Samantha
session," it ran a single bounded pass (Load + a few findings + a report) and stopped, instead of the full
multi-cycle loop; (2) it read `from_will.py`'s "new since pointer" digest, found one item, and considered
Will's running doc handled — leaving ~30 other genuine open items in the doc completely untouched (no
comment, no action, not orange), despite `charter.md` already having rules 1-3 (written one day earlier
from an almost-identical incident) saying to check the full comment history and comment on everything.
Will explicitly asked for PERMANENT fixes "in Samantha running code and process and prompts," not just a
correction in this conversation.
**Root cause (both):** prose-only rules that depend on correctly inferring scope/completeness were
insufficient — (1) nothing in `charter.md` said explicitly that "run a session" always means the full loop,
so brevity of the instruction got misread as license for a smaller scope; (2) the running-doc rules said
"check everything" but had no mechanical enumeration step, so a partial digest read got mistaken for a
complete audit.
**Fix:**
1. `charter.md` Comms section — new explicit rule: any phrasing that starts/resumes a Samantha session
   ("run a fresh Samantha session," "run Samantha," "start Samantha," etc.), however terse, means the full
   `## The run loop` + the two-consecutive-clean-sweeps stopping bar, never a single bounded pass. Only an
   explicitly scoped-down request is smaller.
2. `charter.md` "Gaps found" — new rule 10: a MECHANICAL running-doc audit procedure. Run
   `running_doc.py read --all`, take the literal ACTIVE paragraph count from the header, enumerate every
   active paragraph one at a time (check for an existing reply → do real proportional work → post an
   in-doc comment → mark orange), and do not report the running-doc task done until every paragraph from
   that count has been touched. Countable procedure with an explicit completion check, not a stronger
   restatement of "be thorough."
3. Stopping-definition sweep item 3 (Will's running doc) cross-referenced to require rule 10's procedure,
   not just `from_will.py`'s digest.
4. Persistent memory `samantha_interactive_channel_autonomy.md` updated with both incidents (why + fix),
   so the lesson survives independent of this one conversation.
**Files:** `scripts/samantha/charter.md` (`Fields_Orchestrator`, 1 commit); memory file (not in git).
**Recurrence:** Both are 2nd instances of their respective failure class within 24-48h (running-doc gaps:
2nd time in 2 days; premature stopping: 2nd time after the 2026-07-22 stopping-definition correction).
Meta-lesson applied directly in the fix itself: when a principle-level rule has already failed once, escalate
to a mechanical/countable procedure rather than restating the principle more strongly.

---

## [GOOGLE-INDEXING-SUBMIT-NEW-NEVER-PERSISTS-STATE] Nightly indexing submission silently full-catalog-resubmits forever, starving new listings of quota — 13:00 AEST
**Context:** Investigating Will's running-doc complaint ("still not seeing any of our listings editorial
coverage in google results... why have all 17k off-market pages been indexed almost immediately but none
of our listings editorial pages"). Checked live GSC indexation for a sample of active-listing editorial
pages old enough to judge (>1 week): 8/10 were actually "Submitted and indexed" — the blanket "none
indexed" read was mostly a timing artifact (95/110 core-suburb editorial pages were published in a single
2026-07-21/22 burst, too new to have been crawled yet). But 3 properties (38 + 83 Glen Eagles Drive Robina,
157 Christine Avenue Burleigh Waters) were genuinely stuck, and organic PostHog traffic to `/property/`
pages IS flowing (real google.com/bing.com/duckduckgo.com referrers, 14-day window) — so tracking itself
isn't broken. Investigating why NEW listings don't get the same fast indexing off-market pages get led to
the real systemic bug.
**Root cause:** `google_indexing.py cmd_submit_new()` (nightly cron `0 1 * * *`) is meant to submit only
URLs changed since its last run, tracked via `logs/indexing-last-run.json`. That state file has **never
existed on disk** — `grep -c InvalidURI logs/google-indexing.log` shows 264 historical Mongo-connection
crashes plus, independent of those, the code path itself never writes state on a first-ever run: when
`last_run` is unset, `cmd_submit_new()` falls back to `return cmd_submit_all()` — and `cmd_submit_all()`
never touches the state file. Every single night, `last_run` reads as unset, so the job re-submits the
ENTIRE property catalog (1700+ URLs) capped at the 200/day Indexing API quota, in whatever order the DB
query returns them (only loosely "static + for-sale first") — burning the day's entire quota on a mix of
old/already-indexed URLs before genuinely NEW listing pages (like 24 Elfin Street, published <24h before
Will's note) ever get a proactive submission. This is a permanent, self-perpetuating failure: it can never
transition into the efficient incremental mode because the very branch meant to bootstrap it doesn't save
the bootstrap.
**Fix:** After the `cmd_submit_all()` fallback call in `cmd_submit_new()`, write `indexing-last-run.json`
(same format as the normal incremental path). Verified via `py_compile`; did not live-run `cmd_submit_all()`
again since today's 200/day quota is already exhausted from earlier manual resubmit attempts — tonight's
1am cron run will exercise the fix for real (state file should exist afterward, and tomorrow's run should
use the fast incremental path querying only `last_updated >= last_run`, well under 200/day for 3 suburbs).
**Files:** `scripts/google_indexing.py` (`Fields_Orchestrator`, 1 commit).
**Recurrence:** First for this specific bug. Worth a Systems Health follow-up: add a check that
`indexing-last-run.json` exists and is recent, so a regression here is caught automatically instead of
requiring someone to notice indexation is silently starved.

---

## [APPRAISAL-BACKLOG-DISCOVERY] Found 39 real ready-to-post appraisal PDFs sitting unshipped since April; built #40 for today's top lead — 12:52 AEST
**Context:** Reviewing `lead_worklist`'s single HIGH-priority lead (12 Beaconsfield Drive, Burleigh Waters — owner-occupier, withdrawn 2026-07-16, ~26.4y held), checked whether an appraisal package already existed for it. It did — but the existing `appraisal_pipeline` record was `_synthetic_test: True` (`name: "E2E Test", email: "e2e@x.com"`), an end-to-end test artifact that happened to reuse a real address, not a genuine client entry.
**Wider finding:** Querying all `stage: draft_ready` pipeline records (40 total) found only 1 synthetic — the other 39 are real, rendered PDFs (`artifacts/appraisals_v4/`, 17MB+ each, 0 hard-breach audit failures) for real Robina/Burleigh Waters/Varsity Lakes properties, dating back to **2026-04-10** — over 3 months of built-and-ready packages that were never printed or posted. Production is not the bottleneck to 5 appraisals; distribution is. Of these 39 (+the new one, 40), only 2 carry a real captured name/email (one is "Test Pipeline" test data, the other — "Dee", 13 Terrace Court Merrimac — is already correctly `dismissed: true, reason: now_listed` in `lead_worklist`, confirming the existing dismissal logic worked as designed). **38 have zero contact info** — they originate from anonymous mini-site address lookups, meaning physical mail to the property address is the only viable outreach channel for nearly the whole batch.
**Action taken:** Generated a genuine (non-test) appraisal for 12 Beaconsfield Drive — `generate_appraisal_v4.py --subject-id 690bd7e48b8f546592603cb9 --strict-photos` (0 hard breaches, strict-photos passed), inserted a real `appraisal_pipeline` record (`stage: draft_ready`, sourced from `lead_worklist`, notes explaining the no-contact-info situation). Wrote a reusable print-ready cover-note template (`artifacts/appraisals_v4/COVER_NOTE_TEMPLATE.md`) addressed generically to "The Homeowner," since 38/40 records need exactly this, plus the query to pull the current batch list and the 2 named exceptions needing individual handling. Escalated the headline number to Will via Telegram immediately (with a same-session correction once the synthetic-record contamination was caught, 40→39 real + 1 built = 40 real).
**Files:** `system_monitor.appraisal_pipeline` (1 new doc, data-only); `artifacts/appraisals_v4/690bd7e48b8f546592603cb9_20260723T124717.{pdf,html,audit.json}` (new); `artifacts/appraisals_v4/COVER_NOTE_TEMPLATE.md` (new).
**Recurrence:** First. Worth a standing rule: before reporting a pipeline-stage count as "N ready," always filter `_synthetic_test`/test markers first — this session almost sent Will an inflated number and caught it only by chance while investigating a specific lead, not by a systematic check.

---

## [MARKET-METRICS-CADENCE-MISMATCH] Absorption rate + Asking $/sqm health checks flagged monthly data as stale — 12:44 AEST
**Context:** Systems Health sweep showed "Absorption rate" and "Asking $/sqm" as STALE for all 3 core suburbs (>10d old, data dated 2026-06-30, 22.2-22.3 days ago). Given today's earlier `[QUARTER-COMPLETENESS-FALSE-POSITIVE]` and the `[AD-ATTRIBUTION-ARCHITECTURE-DECISION]` cadence-mismatch bugs (both logged in `2026-07-22.md`, same file class), treated the "familiar-looking cause" as the one to verify hardest rather than assume, per the 2026-07-23 self-audit rule.
**Root cause confirmed:** both `refresh_absorption_snapshots.py` and the SQM asking-price refresh run via crontab on `24 5 1 * *` / equivalent — **1st of the month only** (their own Systems Health section labels even say "(monthly)"). But `main_site_health_check.py` judged both against `judge(ts, "weekly", ...)` (10-day tolerance) instead of `"monthly"` (40-day tolerance, already used correctly elsewhere in the same file for market pulse / macro indicators / off-market liquidity). 22 days old is completely normal for data refreshed on the 1st of the month — this was a false positive, the 4th instance of the cadence-mismatch bug class in 2 days (Indexed prices, ad_downstream, quarter-completeness's sibling issue, now this).
**Fix:** Changed both `judge(ts, "weekly", ...)` calls to `judge(ts, "monthly", ...)` (lines formerly 786, 840). Verified: re-ran `main_site_health_check.py --json` after the fix — both rows no longer appear in the non-OK output.
**Files:** `scripts/main_site_health_check.py` (`Fields_Orchestrator`, 1 commit).
**Recurrence:** 4th instance of the "`weekly`/`expected_last_run()`-style cadence assumed instead of checked" bug class within 2 days in this same file — strongly suggests every remaining `judge(ts, "weekly"...)` / `judge(ts, "nightly"...)` call site in `main_site_health_check.py` should be audited against its actual cron cadence in one pass rather than waiting to trip over each one individually.

---

## [FB-ACCOUNT-UNSETTLED-BILLING] FB ad account UNSETTLED — entire paid lead engine stalled since 2026-07-22 — 12:39 AEST
**Symptom:** Investigating why the 2026-07-17 AYH ad-copy-test-3-variants change-ledger item showed zero leads after 6 days, pulled per-ad FB insights: total spend across all 3 variants was only $31.46 over 6 days (vs the $10/day/variant = $180 expected), zero `lead` actions on any variant. Pulled account-level daily spend to check if this was test-specific — it wasn't: account-wide spend fell from $148.67 (Jul 16) → $148.18 (Jul 17) → …→ $69.36 (Jul 21) → **$1.86 (Jul 22)** and stayed near-zero into today. All 3 test ads themselves show `status: ACTIVE` and their adsets show `effective_status: ACTIVE, budget_remaining: full daily budget` — nothing paused deliberately.
**Root cause:** `act_1463563608441065` → `account_status: 3` (Meta's UNSETTLED code — an outstanding balance failed to charge), `balance: 10338` (= $103.38 AUD unpaid) on funding source VISA *2096. Meta throttles/stops delivery account-wide when an ad account goes unsettled, which matches the spend collapse timing exactly (billing cycle failure ~Jul 21-22).
**Fix:** None possible from this session — updating a payment method / clearing a billing hold is not in Samantha's tool access or autonomy tier (hard line: spend/financial account changes need Will). **Escalated via Telegram immediately** with the exact numbers and the required action (Meta Ads Manager → Billing → settle balance / check card *2096 for a decline).
**Files:** None (investigation + escalation only, no code/config changed).
**Recurrence:** First. Flagging for a Systems Health follow-up: `main_site_health_check.py`'s ads-compliance check (added 2026-07-22 per `[AD-ATTRIBUTION-ARCHITECTURE-DECISION]`) checks `ad_downstream` freshness but does not check FB `account_status` directly — worth adding an account-status/billing check there so this class of failure surfaces on the Systems Health sheet automatically next time, not only when a session happens to pull account-level insights while investigating something else.

---

## [QUARTER-COMPLETENESS-FALSE-POSITIVE] Systems Health flagged a working fix as broken — direction-blind comparison — 03:35 AEST
**Context:** A loop cycle's Task 0.5 sweep found 3 new ERROR rows ("Indexed prices — quarter completeness", Burleigh Waters/Robina/Varsity Lakes) that hadn't appeared in earlier cycles' checks that same session.
**Symptom:** `raw sold data has 52 sales in Q1 2026 but the precomputed series' latest complete quarter is Q2 2026` — read at a glance like a regression.
**Root cause:** the check (added earlier today per `[SYSTEMS-HEALTH-CONSOLIDATION]`/`[PROPERTY-TIMELINE-REFRESH-DEAD]`, see `2026-07-22.md`) computes an `expected_label` two quarters back from now (a grace window, since `property_timeline` is weekly-refreshed and `indexed_prices` monthly-precomputed) and flags ERROR whenever `latest_complete_period != expected_label`. Plain inequality doesn't distinguish "behind schedule" from "ahead of schedule" — and this morning's `[PROPERTY-TIMELINE-REFRESH-DEAD]` fix had, by this evening, genuinely caught the precomputed series up to Q2 2026 — ONE quarter ahead of the check's own 2-quarter grace expectation. The check flagged the exact outcome the morning's fix was meant to produce as an error.
**Fix:** `main_site_health_check.py`'s quarter-completeness check now parses both periods into `(year, quarter)` tuples and only flags ERROR when the precomputed series is genuinely BEHIND the expected quarter (`<`), never merely not-equal. Verified live: all 6 target/non-target suburbs checked now read OK; totals ERROR count dropped 12→9.
**Files:** `scripts/main_site_health_check.py`.
**Recurrence:** First. Lesson: a comparison written as `!=` when the real intent is "not stale" needs to check direction, not just equality — the same subtlety as the earlier `expected_last_run()` cadence-mismatch bugs found in this file, different mechanism (direction-blind vs wrong-baseline).

---

## [FIELDS-AUTOMATION-GH-ACTIONS] Investigated the 3 known-broken GitHub Actions workflows properly — 2 root causes, not 1 — 04:06 AEST
**Context:** A Task 0.5 sweep re-surfaced 3 GitHub Actions failures (Light Rail Stage 3, Market Insight, Market Shift — all flagged since `[SYSTEMS-HEALTH-CONSOLIDATION]` yesterday) that had been noted but never actually investigated this session. Rather than wave them through as "already known," pulled the actual failure logs via `gh run view --log-failed`.
**Finding — these are TWO unrelated problems wearing the same "3 broken workflows" summary:**
1. **Light Rail Stage 3** (5 straight weekly failures) and **Market Shift** (regressed the last 2 runs) both fail with `anthropic.BadRequestError: ... Your credit balance is too low to access the Anthropic API` — the exact same root cause as the already-known "Vision provider — Anthropic (primary): credit balance too low" Systems Health row. Confirms the blast radius is wider than previously understood (also breaks off-VM content-generation workflows, not just on-VM photo/floor-plan analysis) — still blocked on Will topping up credits, nothing to fix in code.
2. **Market Insight** is a GENUINELY DIFFERENT bug: its scheduled runs actually SUCCEED at generating content ("Done. Pushed: 2 | Skipped: 0 | Errors: 0") and only fail on the final `git push` — `error: failed to push some refs`. All 3 workflows run on the same self-hosted runner (per `[[fields_automation_self_hosted_runner]]`) on similar nightly/weekly schedules and all had an unguarded `git add && git commit && git push` with no pull/rebase first — a classic race when two scheduled workflows land commits on `main` close together.
**Fix:** added `git pull --rebase --autostash origin main` immediately before `git push` in all three workflow files (`market_insight.yml`, `market_shift.yml`, `light_rail_stage_3.yml` — applied to all three since Market Shift/Light Rail will hit the identical race the moment the credit issue is resolved and they start succeeding at generation again, not just to the one that already proved it). Standard, well-established pattern — not live-verified via an actual trigger (deliberately didn't force a real content-publish at 4am just to test; Market Insight's fix will validate naturally on its next Monday 7am AEST scheduled run, or whenever manually dispatched).
**Files:** `.github/workflows/market_insight.yml`, `.github/workflows/market_shift.yml`, `.github/workflows/light_rail_stage_3.yml` (all `fields-automation` repo, not `Fields_Orchestrator`).
**Recurrence:** First for the push-race diagnosis. The credit-balance cause is now confirmed in 3 separate places (Vision provider, Light Rail, Market Shift) — worth escalating to Will as one consolidated ask ("top up Anthropic API credits — currently blocking photo/floor-plan analysis AND 2 of 3 broken content-generation workflows") rather than three separate mentions.

---

## [SAMANTHA-LOOP-PROCESS-GAPS] Will's review of the first full interactive-loop run found 5 real process gaps — 07:40 AEST
**Context:** After the 17-cycle loop concluded, Will reviewed it and found 5 things missed despite the loop otherwise working mechanically (persistence, sweep, stopping definition all held up). Diagnosed root cause for each — deliberately did NOT go complete the actual missed items (Will's explicit instruction: leave them for the next Samantha run so the fixed process can be proven to catch them).
**Findings (see `charter.md`'s new "Gaps found in the first real interactive-loop run" section for the full fix text, 9 numbered rules):**
1. **Doc engagement was worse than "missed" — it duplicated real work.** `drive_comment.py list` (never checked this run) showed 2 items answered from scratch this session — the self-audit question, the Brain 1/2/3 question — had ALREADY been answered via comment reply by a session SIX DAYS earlier (2026-07-17), just never marked orange. Only 1 of ~10 items actually worked this session got a new doc comment; the rest were answered in chat/Backlog/memory only, never on the document itself.
2. **Zero timestamps on 30+ Backlog updates**, and the Task Board's "Decision Log" tab (existed since 2026-07-15) was never written to even once — no chronological record independent of fix-history.
3. **The sweep collapsed into backlog-grinding** — north-star/Task-0.5 checks were a few narrow Mongo counts, never a genuine broad business survey (no financial snapshot, no spend-efficiency trend, no competitor scan).
4. **The brains were queried once in 3 hours** (`brain_search.py`, one early SEO question) despite Task 3 already instructing hypothesis-sourcing from Brain 1, and despite multiple concepts drafted afterward with zero brain queries.
5. **No compiled action-item list for Will** — 8 "Needs Will" Backlog rows never pulled into one place; the "Questions for Will" tab (existed since 2026-07-15) never used. Will's own words: "I wasn't given a list of action items."
**Fix:** added a new mandatory 9-point section to `charter.md` (check full comment history before answering / post a real doc comment for every new item / mark orange immediately / timestamp every Backlog update / write to Decision Log every cycle / run an explicit broader "CEO Business Review" periodically, not just the 20-min sweep / query both brains before drafting anything substantive / compile + deliver a Will action-item list before stopping / make the self-audit's location impossible to miss). Cross-referenced from `daily_tasks.md` at the two exact spots this executes (the running-doc protocol, Task 3's brain-sourcing line).
**Meta-lesson:** several of these were ALREADY documented rules (orange-marking, Decision Log, Brain 1 sourcing, Questions for Will) before this run — the gap wasn't missing instructions, it was a long, visibly-productive interactive session creating a false sense that mechanical hygiene was optional because Will was live in chat. It isn't — the durable record has to stand on its own.
**Files:** `scripts/samantha/charter.md`, `scripts/samantha/daily_tasks.md`.
**Recurrence:** First. Explicitly not yet resolved in practice — the next Samantha run (headless or interactive) is the actual test of whether these fixes hold.

---

## [ENV-LOADING-STANDARDIZED] Migrated all 14 ad-hoc .env parsers onto python-dotenv — self-audit point 8 — 08:08 AEST
**Context:** Will asked what to do about the self-audit's tooling-gap findings. This is the concrete fix for the `.env`-loading friction (the second half of point 8) — 14 scripts each hand-rolled their own `.env` parser instead of using the already-installed `python-dotenv` library, which is exactly the class of bug that caused `[GOOGLE-INDEXING-INVALIDURI]` (2026-07-22) in the first place.
**Fix:** replaced each script's `load_env()`/`set_env_from_file()`/`load_env_file()` body with `load_dotenv(<path>, override=False)` — `override=False` (python-dotenv's default) preserves the exact same semantics as the old `os.environ.setdefault(...)` pattern (an already-exported var always wins), so this is a pure implementation swap, not a behaviour change. Kept every function's name/signature/call sites untouched — only bodies changed, so nothing calling these functions needed updating.
**Verified:** all 14 files compile (`py_compile`); functional smoke test on 2 of them (`google_indexing.py`, `main_site_health_to_sheet.py`) confirms `COSMOS_CONNECTION_STRING` loads clean (no stray quote characters) with zero pre-exported environment, the exact scenario that broke before.
**Files:** `scripts/ceo_agent_lib.py`, `scripts/check_systems_health_ran.py`, `scripts/delete-ghost-article.py`, `scripts/main_site_health_to_sheet.py`, `scripts/builder-telegram-bridge.py`, `scripts/push-ghost-draft.py`, `scripts/refresh-ops-context.py`, `scripts/google_indexing.py`, `scripts/minisite_health_to_sheet.py`, `scripts/sold_homes_to_sheet.py`, `scripts/ceo-telegram-bridge.py`, `scripts/migrate-ghost-to-mongodb.py`, `scripts/sync-memory-to-codex.py`, `scripts/live_leads_to_sheet.py`.
**Recurrence:** N/A (removes an entire recurring bug class rather than fixing one instance).

## [EDITORIAL-REJECT-SILENT-FAIL] Ops "Reject" button appeared to publish instead of reject — 08:20 AEST
**Symptom:** Will rejected "33 Montezuma Drive, Burleigh Waters" in the Editorial Review ops tab; the property stayed/showed as published instead of rejected.
**Root cause:** Two-part. (1) The property had been auto-published by the nightly AI editorial pipeline (step 120, AUTO_PUBLISH=1) at 08:12 AEST — 6 minutes before Will's reject click — right after that pipeline's heavy write load, which is a known Cosmos DB serverless RU-throttle risk window (error 16500). `handleEditorialPublish`/`handleEditorialReject`/`handleEditorialUpdate` in `netlify/functions/system-monitor.mjs` did NOT use the existing `withCosmosRetry` wrapper on their `updateOne` calls, unlike other write-heavy paths in the same file. (2) `EditorialReviewPanel.tsx`'s `handleReject`/`handlePublish` never checked `res.ok`/`result.ok` — a failed write (500 from Cosmos throttle, or any other error) was silently treated as success: no error shown, button just re-enabled and the list refreshed to reflect the unchanged (still-published) DB state. Verified backend logic itself was correct via direct curl to `/api/monitor/editorial-reject`, which worked and set status=rejected — confirming the bug was the missing retry + missing frontend error handling, not broken routing/logic.
**Fix:** Wrapped the 3 editorial mutation `updateOne` calls in `withCosmosRetry` (already imported in the file, used elsewhere). Added `res.ok`/`result.ok` checks in `handlePublish`/`handleReject` in the ops frontend — failures now show a blocking `alert()` and do NOT refresh the list (so the item stays visible for retry) instead of silently pretending success. Also manually re-issued the reject for 33 Montezuma Drive via the (now-verified-working) API — it is now correctly `status: rejected`.
**Files:** `Feilds_Website/01_Website/netlify/functions/system-monitor.mjs`, `Feilds_Website/01_Website/src/pages/OpsPage/panels/EditorialReviewPanel.tsx`
**Recurrence:** First

---

## [OFFMARKET-RARITY-POI-SOURCE] Quality-checked live OSM+Mapbox vs pre-harvested Google Places POI data before building the off-market deck's new rarity card — 10:50 AEST
**Context:** Designing a new "rarity/positioning" card for the `/off-market/:slug` swipe deck needed a fast (sub-second, loaded before the user swipes to it) nearest-POI-per-category lookup. `walking_distances.py`'s `resolve_pois()` (live OSM Overpass + Mapbox Directions) was already known unsafe for a live per-request path (124s measured worst case). Before defaulting to the alternative fast method (`compute_georeference()` in `on_demand_valuation.py` — a hand-typed list of ~8 landmark coordinates), Will asked for a real accuracy check of fast vs slow, since a wrong "nearest school" claim on a public-facing positioning card is a factual-accuracy risk (CLAUDE.md editorial rule 5).
**Finding:** Ran both methods live against 6 real properties across the 3 core suburbs. The slow (OSM+Mapbox) method was NOT just slow — a single sequential process already triggered `HTTP 429 Too Many Requests` from the primary Overpass mirror on request #2, ruling out any parallel/batch use against public Overpass infrastructure (risk of IP-ban would also break the existing full deep-dive builds, which already depend on this same Overpass access via `slot_resolver.py`). Separately, the embedded hand-typed list's coordinates were sometimes badly wrong: for 7 Turnberry Court, Robina, it computed "Robina State School — 1.55km" while OSM's live routed distance (and independently, Google Places' own coordinate) both agreed on ~190-224m — an 8x error that would flip a "walk to school" claim from false to true. The embedded list is also missing 2 entire categories (childcare, train station) needed for this corridor's positioning (G:link stations at Robina/Varsity Lakes).
**Fix:** Discovered `scripts/harvest_pois.py` already existed (unused) — a one-time-per-suburb Google Places Nearby Search harvester writing to `Gold_Coast_POIs.pois`, last run Feb-Mar 2026 (769 POIs, 9 suburbs, 7 categories). Cross-validated its data against the live OSM/Mapbox ground truth: equal or better accuracy (Google found real, closer parks/supermarkets that OSM's narrower tag filters missed entirely). Added a `train_station` category (Google Places `type=train_station` cleanly returns exactly the 2 real G:link stations) and re-ran the harvest (788 POIs total, 19 inserted/716 refreshed). Built `scripts/property_reports/nearby_pois.py` — pure local haversine against an in-process cache of this harvested collection (no external API calls, same latency profile as `compute_georeference()` but sourced from real, cross-validated data) — and wired it into `offmarket_intel_poller.py`'s `compute_intel()` as a new `proximity` field. Also fixed subject-doc coordinate field precedence (`LATITUDE`/`latitude` vs `geocoded_coordinates.latitude`, varies by doc vintage) to match the pattern already used in `db.server.ts`.
**Verified:** Live-tested against 2 real properties end-to-end via `--slug`; full `compute_intel()` call (scarcity + competition + new proximity) completes in ~1.1s. Restarted `fields-offmarket-intel-poller` service.
**Files:** `scripts/harvest_pois.py`, `scripts/offmarket_intel_poller.py`, `scripts/property_reports/nearby_pois.py` (new).
**Recurrence:** First.

---

## [OFFMARKET-POSITIONING-CARD] Built the off-market deck's "How your home is positioned" rarity+proximity card on top of the new intel — 11:10 AEST
**Context:** Continuation of `[OFFMARKET-RARITY-POI-SOURCE]` above — the proximity data was flowing, but no card consumed it yet. Will's brief: a second real hero photo (satellite fallback only, never leading with it — explicit instruction) plus a synthesis of genuine property-attribute rarity and location-proximity facts, framing "how the home is positioned in this market" for buyers at its price point.
**Build:**
1. `db.server.ts` already had `secondHeroPhoto()` from an earlier session (shared `_rankedHeroPhotos()` ranking) — wired it into the loader (`off-market.$slug.tsx`) and `PropertyData` type (`secondPhoto` field), not yet connected before this.
2. `offmarket_intel_poller.py`'s `notable[]` previously only exposed a generic `label` (e.g. "Bedrooms") — extended to also carry the rule engine's natural-language `phrase` (e.g. "5 bedrooms", "a pool") and `tier`, since card copy needs sentences, not category names. Confirmed nothing else in the codebase consumed the old shape (grep-verified) before changing it.
3. Added `computePositioning(data, intel)` to `ladderShared.ts` — templates a dynamic title from the strongest rarity phrase (falls back to neutral copy when there's none), builds rarity chips from `scarcity.notable` (excluding the two beach-distance rules specifically, since that figure has a second, different-methodology source in the new `proximity` field — showing both risked two disagreeing beach distances on one card) and proximity chips from the new `proximity` field, ordered by buyer-relevance (train station/beach/supermarket/schools first) rather than raw distance.
4. Built the card in `OffMarketDeck.tsx` — reuses the card-0 hero visual treatment (full-bleed photo, scrim, white chip styling) with new CSS (`positionTitle`, `chipProximity`, `heroNote`, `heroShimmer` in `OffMarketDeck.module.css`). Gated like the existing "standout" card (`positioning.hasContent || (engaged && intelPending)`) so it never renders with nothing to show, and distances are explicitly labelled "away"/straight-line, never "walk", since they're haversine not routed.
5. Moved the `Intel` type (previously private to `OffMarketDeck.tsx`) into the shared `types.ts` so `ladderShared.ts` could import it too.
**Verified:** `tsc --noEmit` and `npm run build` both clean. Pushed all 6 files, deployed via Netlify. Live-screenshotted (mobile viewport, keyboard-navigated via the deck's existing ArrowRight handler) on 1/1 Alexandrina Drive, Varsity Lakes — card renders at the correct position (between "standout" and "doors"), real distinct hero photo, correct dynamic title ("One of the few homes in Varsity Lakes with 5 bedrooms."), real rarity chips (5 bedrooms, 3 bathrooms) and real proximity chips (2 km to Varsity Lakes Station, 3.2 km to Nobby Beach, 190 m to Coles Varsity Lakes, 420 m to Varsity College Secondary Campus), 0 console errors, 0 network errors.
**Files:** `src/lib/db.server.ts`, `src/routes/off-market.$slug.tsx`, `src/pages/OffMarketPage/types.ts`, `src/pages/OffMarketPage/ladderShared.ts`, `src/pages/OffMarketPage/OffMarketDeck.tsx`, `src/pages/OffMarketPage/OffMarketDeck.module.css` (all `Feilds_Website/01_Website`).
**Recurrence:** First.

---

## [OFFMARKET-INTEL-STALE-CACHE] Positioning card missing on 5 Glenferrie Drive after deploy — stale pre-proximity cache — 11:10 AEST
**Symptom:** Will refreshed `/off-market/5-glenferrie-drive-robina?arm=dark` right after the positioning card shipped and didn't see it, even though a fresh test property (1/1 Alexandrina Drive) showed it correctly.
**Root cause:** `offmarket_intel_poller.py` caches its result permanently once `status: "done"` — it never recomputes an already-answered slug. 5 Glenferrie Drive had been queried during earlier debugging on 2026-07-22 (before today's `proximity` field existed), so its cached `result` had no `proximity` key at all. `computePositioning()` correctly saw zero rarity AND zero proximity for that stale shape and (correctly, per its honesty-gating design) didn't render the card — the code was working exactly as designed, just against data cached before the feature existed.
**Fix:** Queried `system_monitor.offmarket_intel` for `status: "done"` docs missing `result.proximity` (2 of 3 total cached docs — this is a brand-new dormant arm, so total volume is tiny) and reset them to `status: "pending"` so the poller's next 3s cycle recomputed them with the current code. Verified live via screenshot — 5 Glenferrie Drive now shows the card (fallback title since it has no rarity anchors, but real proximity chips: "2.4 km to Robina" station, "640 m to Robina State School", etc).
**Not fixed (accepted risk, flagged for later):** any future change to `compute_intel()`'s output shape will hit this same staleness for whatever's already cached by then. Fine for now given `offmarket_intel` volume is near-zero (dormant `?arm=` only feature, no real traffic routed to it yet) — worth revisiting (e.g. a schema-version stamp that forces recompute on mismatch) before this arm goes live in PostHog.
**Files:** none (data-only fix — 2 Mongo docs in `system_monitor.offmarket_intel`).
**Recurrence:** First.

---

## [PROPERTY-TIMELINE-REFRESH-SLUG-BUGS] Two more silent bugs in the property-timeline refresh fixed yesterday — 11:12 AEST
**Context:** Continuation of `[PROPERTY-TIMELINE-REFRESH-DEAD]` (2026-07-22) — that fix got the weekly refresh job actually running again (dead cron + blocked scraper), but a targeted Varsity Lakes coverage push this morning surfaced two more bugs in `build_profile_url()` that were silently producing wrong/thin data for a subset of properties even with the pipeline "working."
**Bug 1 — unit-numbered addresses mangled.** `build_profile_url()` stripped `/` via its `[^a-z0-9\-]` character-class filter, so "1/18 Thornleigh Crescent" became slug `118-thornleigh-crescent` instead of Domain's actual `1-18-thornleigh-crescent`. Confirmed via direct fetch: the wrong slug returned a 1KB fallback page (no `__NEXT_DATA__` content), the correct one returned 98KB of real data. This means every unit-numbered address refreshed before this fix (including ones that appeared to "succeed" — a wrong-slug fetch can still return *some* page, just not the right property's) needs re-verification, not just the ones that outright failed.
**Bug 2 — street-type abbreviations not expanded.** "8 Duxton Dr" built the slug `8-duxton-dr`; Domain's real URL needs the type spelled out (`8-duxton-drive`) — same 1KB-fallback-vs-98KB-real symptom. Ported the existing `STREET_ABBREVS` map from `03_For_Sale_Coverage/enrich_property_timeline.py` (already solves the identical problem there) into `refresh_property_timelines.py` rather than inventing a second copy.
**Also added:** `--houses-only` and `--skip-refreshed` CLI flags so a targeted catch-up run can scope to exactly "the N properties still missing" instead of re-fetching an entire suburb's already-successful properties every time (was wasting Bright Data spend/time re-verifying known-good data on every retry pass).
**Verified:** all 3 of Varsity Lakes' remaining un-refreshed Q2-house properties fetched correctly after both fixes (one per bug, one property needed a third manual fix — its unit prefix doesn't appear in Domain's own URL at all, a one-off not worth a general rule). Varsity Lakes Q2 2026 house coverage went from 15/18 to 18/18 refreshed (13/15 true houses countable in the median once the last property-type classification lands via tonight's step 112 run — 3 of the 18 are genuinely not houses per Claude Vision, confirmed independently of Domain's own top-level tag which was wrong for those 3).
**Files:** `scripts/refresh_property_timelines.py` (`Fields_Orchestrator`, 3 commits).
**Recurrence:** 2nd/3rd bugs found in this same function within 24 hours — worth a fuller audit of `build_profile_url()` against a larger random sample before trusting it fully; the pattern so far is "anything that doesn't look like a plain `123 Street Name` address is suspect."

## [MARKET-PULSE-FULL-REWRITE] Completed the 21-summary Market Pulse rewrite across all 3 core suburbs — 11:12 AEST
**Context:** Started 2026-07-22 from Will's request to review/update all `/market-metrics` data and analysis. Spanned two sessions: yesterday fixed 4 upstream data bugs (see `2026-07-22.md`) and wrote Robina's first draft; today extended coverage, found and fixed the `classified_property_type` vs `scraped_data.features.property_type` field-mismatch bug (recovers correctly-classified houses excluded by a stale, unvalidated field — wrote up as a standalone fix prompt, not yet applied to the precompute scripts themselves, only spot-patched for properties this session needed), and — the most consequential methodological finding — discovered that single-quarter median price comparisons were producing misleading verdicts for Burleigh Waters and Robina that a trailing-12-month rolling comparison directly contradicted (Burleigh Waters: single-quarter showed 2 consecutive declines from a thin, peak-distorted Q4 2025 quarter, while the robust rolling measure showed *accelerating* growth; Robina: single-quarter showed an -8.1% QoQ drop on a thin 26-transaction sample, while rolling data showed steady, merely-decelerating growth with no reversal). Rewrote Robina's `overview`/`direction`/`sell-now`/`buy` on the corrected basis (previously written same-lens-missing on 2026-07-22) after Will explicitly asked for the recheck. Also corrected `suburb-compare`'s 3-suburb comparison methodology mid-session: the original approach (single "latest quarter" price as the 5-year endpoint) swung Robina's headline 5-year growth figure from 76.7% to 45.4% between two consecutive precompute runs purely because the anchor quarter changed and was itself thin — replaced with a rolling-12-month-basis comparison (built on 126-290 transactions per endpoint instead of 12-26), which is stable across precompute re-runs.
**Also incorporated per Will's direction:** national/state policy context (RBA's 3 2026 rate hikes to 4.35%, the 12 May 2026 negative gearing/CGT reform and its new-build carve-out, 5% Deposit Scheme/Help to Buy expansions) woven into every category where materially relevant, sourced via ad-hoc WebSearch research (no dedicated news pipeline in this codebase — flagged as a possible future build, not done).
**Editorial standard applied throughout (per Will's explicit feedback mid-session):** every category rewritten to state the tension in the data explicitly and land on an actual conclusion, not list metrics sequentially — e.g. Varsity Lakes' `sell-now` reasons from "still-positive rolling price + collapsing volume + doubling listings" to an explicit conclusion about buyer-pool thinning being the leading edge of a shift toward buyers, rather than presenting those three facts side by side.
**Final state:** 21/21 categories (`sell-now`, `buy`, `crash-risk`, `overview`, `houses-vs-units`, `direction`, `suburb-compare` × Robina/Burleigh Waters/Varsity Lakes) verified `source: manual` via `manual_market_pulse.py --check`.
**Files:** No code changes for this entry specifically (content only, written directly to `system_monitor.market_pulse` via one-off Python scripts per suburb — not committed to git, DB content). Code fixes from this work are logged separately (`[PROPERTY-TIMELINE-REFRESH-SLUG-BUGS]` above; `2026-07-22.md` for the ABS/regex/in-progress-quarter/dead-cron fixes).
**Open follow-ups:** (1) `classified_property_type` field-mismatch fix — prompt written (`market_pulse_workflow.md` memory), not yet applied to `precompute_indexed_price_data.py`/`precompute_market_charts.py` themselves. (2) `refresh_property_timelines.py`'s `build_profile_url()` — recommend a broader audit given 2 bugs found in 24 hours (see above). (3) A recurring monthly policy-news research step ahead of each pulse cycle — flagged by Will as wanted eventually, not built.
**Recurrence:** N/A (content work, not a recurring bug).

---

## [OFFMARKET-POSITIONING-CARDS] Rebuilt the single "positioning" card into 3 mini-site-quality cards (Combination / Right Buyer / How we'd position it) — 11:50 AEST
**Context:** Will reviewed the shipped "How your home is positioned" card and rejected it — "just shows some basic distance stats... provides no value to the reader. They know where their own home is and whats around it." He wanted the same quality bar as the mini-site's actual positioning copy, quoting real examples: the "not just the pool or the four bedrooms — it is the combination" hero and the "Right Buyer" tab's "The buyer who needs this home most... whose life is solved by this combination" copy.
**Investigation:** Dispatched an Explore agent to find the exact source of both quotes. Found they come from two DIFFERENT mechanisms: (1) `scarcity_narrative.py`'s `resolve_scarcity_narrative()` — genuinely Claude-authored (Opus in the mini-site), with a deterministic closing sentence appended (`CLOSE_SCARCE`/`CLOSE_COMMON` constants); (2) the mini-site's "Right Buyer" tab hero in `YourHomePage.tsx` — 100% static template strings + a plain comma-join of `scarcity_features.notable_features[].phrase` plus the nearest-school walk distance, no LLM at all. A third module, `positioning_object.py` (`resolve_positioning_object`), was already built and fully deterministic — computes a buyer archetype/frame, price-vs-buyer driver split, and an honest "anti-frame" (what we wouldn't claim), and was previously explored but never wired into anything.
**Model/latency decision:** Asked Will whether the combination headline should be a deterministic template or a real LLM call; he asked specifically about Sonnet 5 and whether it could run "quickly enough." Live-tested `resolve_scarcity_narrative()` with `MODEL` overridden to `claude-sonnet-5` (env already has `USE_CLAUDE_MAX=1`, so `get_client_and_model()` maps it to the Max CLI "sonnet" alias) — **11.02s, single attempt, no retries**, output matching the reference quote's exact voice. Too slow to bundle into the existing ~1.1s fast-intel path (would slow the already-shipped standout/proximity cards too) — decoupled it onto its own queue instead.
**Honesty fix caught before shipping:** `positioning_object.py`/`scarcity_narrative.py`'s `_walkable_differentiators()` hardcodes "a {N}-metre walk to {name}" phrasing regardless of whether the input distance is routed or straight-line — reusing the new `nearby_pois.py` proximity data directly would have reintroduced the exact straight-line-labelled-as-walking problem fixed this morning (`[OFFMARKET-RARITY-POI-SOURCE]`). Fixed by adding `to_walking_poi_list()` to `nearby_pois.py`: only candidates within a straight-line 1200m prefilter get a single targeted Mapbox Directions walking call each (a handful per property, once, on the decoupled/cached-forever queue — a completely different usage pattern from the earlier-rejected live-per-pageview Overpass batch), and anything Mapbox can't route or that comes back over the real 1000m walkable ceiling is dropped rather than shown unverified.
**Second bug found and fixed in the shared module itself (affects the live mini-site too, not just this card):** `positioning_object.py`'s `_soft_lead_phrases()` returned an empty list for the `scarcity_play`/`renovator_valueadd`/`prestige_privacy` frames whenever there were zero price-driver anchors (only differentiators), producing "We'd lead on  — and let the right buyer see themselves in it." (double space, no content). Fixed by falling back to `FRAMES[primary]["lead"]` — the archetype's own generic lead phrase, exactly designed for this case — when both the price-driver and frame-signature lookups come up empty. Caught live-testing 5 Glenferrie Drive (a thin single-differentiator property), not by inspection — reinforces testing against genuinely thin/edge-case properties, not just rich ones.
**Build:**
1. `nearby_pois.py` — added `to_walking_poi_list()` (targeted real-routing adapter, described above).
2. `offmarket_intel_poller.py` — new `compute_positioning()` (scarcity + routed pois → `resolve_positioning_object()` + `scarcity_narrative.resolve_scarcity_narrative()` with `MODEL` overridden to `claude-sonnet-5`, plus a server-side `_build_feature_combination()` mirroring `YourHomePage.tsx`'s exact join logic so both surfaces build the identical string from identical inputs); patches `beach_distance_km` into the features snapshot from the harvested proximity data since the valuation engine's own figure covers ~0.2% of docs; new `run_once_positioning()` (limit=1/cycle, decoupled from the fast intel queue) wired into `main()`'s loop and `--once`/`--slug --positioning` test paths.
3. `offmarket-positioning.mjs` (new Netlify function) — same queue-front-end pattern as `offmarket-intel.mjs`, but `FRESH_DAYS=3650` (effectively permanent) since the Sonnet call has real per-property cost and a home's feature combination doesn't change.
4. Frontend: removed the old `computePositioning()`/single card entirely. Added `Positioning`/`PositioningObject`/`ScarcityNarrative` types (`types.ts`), `rightBuyerSubhead()`/`RIGHT_BUYER_TITLE`/`RIGHT_BUYER_BOLD_LINE` (`ladderShared.ts`, verbatim-matching `YourHomePage.tsx`'s fixed template), and a new decoupled `pos`/`posDone` poll (own 2.5s/20-try budget, separate from the fast `intel` poll) plus 3 new cards in `OffMarketDeck.tsx` between "standout" and "doors": **Combination** (second real hero photo, Sonnet narrative + deterministic close), **Right Buyer** (fixed mini-site copy + `positioning.render.leadBuyer`), **How we'd position it** (`positioning.render.thesis` + honest anti-frame). Each card independently degrades (shimmer while `!posDone`, silently absent if genuinely empty once done) — same honesty-gating pattern as the rest of the deck.
**Verified:** `tsc --noEmit` + `npm run build` clean both times (Python fix + frontend rebuild). Triggered a real queue computation via the live `/api/v1/offmarket-positioning` endpoint (not just CLI) for 1/1 Alexandrina Drive, confirmed `status: done` in Mongo, then screenshotted all 3 cards live (keyboard-navigated, mobile viewport) — headline, closing line, and walking-distance-monopoly line match the reference quote's exact voice; Right Buyer card matches the mini-site's fixed copy exactly; How-we'd-position-it correctly omits the anti-frame section when it's empty (no console/network errors across any capture).
**Files:** `scripts/property_reports/nearby_pois.py`, `scripts/property_reports/positioning_object.py`, `scripts/offmarket_intel_poller.py` (all `Fields_Orchestrator`); `netlify/functions/offmarket-positioning.mjs` (new), `src/pages/OffMarketPage/types.ts`, `src/pages/OffMarketPage/ladderShared.ts`, `src/pages/OffMarketPage/OffMarketDeck.tsx`, `src/pages/OffMarketPage/OffMarketDeck.module.css` (all `Feilds_Website/01_Website`).
**Recurrence:** First for both the empty-leadLine bug and the walk-phrasing honesty risk.

---

## [MARKET-METRICS-CLASSIFIED-TYPE-FIX] Applied the classified_property_type field fix + found stale data_snapshot regression on live verification — 12:00 AEST
**Context:** Working through the 3 follow-ups flagged at the end of yesterday's Market Pulse rewrite. Item 1: apply the previously-written-but-unapplied fix so `precompute_indexed_price_data.py`/`precompute_market_charts.py` trust `classified_property_type` (Claude Vision, validated) over the raw `scraped_data.features.property_type` (unvalidated, contains garbage like "Sport").
**First attempt introduced a real regression, caught before push:** the initial fix dropped the `scraped_data.features.property_type` fallback entirely in favour of top-level `property_type` — but for non-core suburbs, top-level `property_type` covers only ~1-2% of documents (216/9945 in Broadbeach, 218/10816 in Palm Beach) while the raw scraped field covers ~80-90%. This silently dropped "Successfully processed" from 69 suburbs to 20 on a full precompute run (72 skipped vs the normal 23). Root-caused via direct isolated aggregation tests (core suburb vs non-core suburb, NEW vs OLD query) before pushing. Corrected precedence: `classified_property_type` → `scraped_data.features.property_type` (fallback, not eliminated) → top-level `property_type` (last resort only). Also fixed the same precedence in `_resolve_ptype()` (the 3rd merged sales-volume source) and its `.find()` projections, which needed the raw field restored. Re-verified: 69/23/0 baseline preserved, core-suburb transaction counts increased as expected (e.g. Robina Q1 2026 26→32 transactions).
**Live verification surfaced an unrelated but real gap:** Will asked directly whether all `/market-metrics` data was actually live on the website — rather than assume yes, checked via the live API (`/api/v1/market-pulse`) and a `site-inspector.js` screenshot. Narrative text and verdicts were confirmed live and correct. But the same page showed **two different numbers for the same stat** — a fresh hero card ($1,500,000) next to a stale FAQ sentence ("$1,520,000... Up 8.6%"). Root cause: `market_pulse` documents have a separate `data_snapshot` object (raw numeric fields — `current_median_price`, `yoy_growth_pct`, `dom_median`, etc.) consumed directly by `SellNowSection.tsx` and `DirectionSection.tsx` (confirmed via source: `buy`/`crash-risk`/`overview`/`suburb-compare` only read `verdict`/`generated_at`, unaffected). Yesterday and today's rewrite only ever `$set` `summary`/`verdict`/`key_signals` — `data_snapshot` was never touched, so it silently held whatever `generate_market_pulse.py` last wrote on 2026-07-03.
**Fix:** Rebuilt `data_snapshot` for the 6 affected documents (`sell-now` + `direction` × 3 core suburbs) from current precomputed data (median price, YoY/QoQ growth, DOM, absorption, sales volume, market signals array). Verified live via API (`current_median_price` now matches the hero stat exactly) and a fresh screenshot (page-text.txt shows "$1,500,000... Up 8.7%" consistently everywhere on the page, no mismatch).
**Also found, not fixed (flagged for Will, separate scope — frontend code + deploy, not a data bug):** `DirectionSection.tsx`'s market-signals indicator grid matches `s.indicator.includes('Wage')` etc. against snake_case field values (`wage_price_index`, `lending_housing`) — case-sensitive substring match that can never succeed regardless of data freshness. Pre-existing, not introduced by this session's work.
**Files:** `08_Market_Narrative_Engine/precompute_indexed_price_data.py`, `08_Market_Narrative_Engine/precompute_market_charts.py` (`Website_Version_Feb_2026`, 4 commits — includes a self-caught-and-fixed regression); `system_monitor.market_pulse` (data-only, 6 docs, not committed to git).
**Recurrence:** N/A for the data_snapshot gap (new finding). The "assumed a field precedence without checking actual coverage across suburb types" mistake echoes `[PROPERTY-TIMELINE-REFRESH-DEAD]`'s meta-lesson from 2026-07-22 — verify against real data before trusting an inference, even when fixing a bug you're confident about.

---

## [OFFMARKET-VALUE-LIQUIDITY-FRAME] Fixed 2 positioning bugs + added evidence-gated "affordable sells faster" frame — 12:27 AEST
**Context:** Will flagged the "How we'd position it" card as genuinely broken for 5 Glenferrie Drive — circular copy ("a hard-to-replicate combination... a combination the market rarely offers") that named nothing concrete. He also noticed the property's own valuation range ($1,150,000-$1,340,000) is affordable for Robina and asked whether real data supports an "affordable sells faster" positioning angle, or whether that's just a nice story.
**Root cause of the original bug (two, compounding):** (1) `positioning_object.py`'s `_soft_lead_phrases()` fell back to the archetype's own generic descriptor when there were no price-driver anchors — self-referential when the frame IS the description ("hard-to-replicate combination" naming nothing). (2) `scarcity_narrative._is_scarce()` treated `matching <= 0` as automatically scarce — but for 5 Glenferrie Drive, `matching=0` only because the property has ZERO qualifying anchors (no bed/land/floor/bath/pool/water/beach anchor cleared its floor), not because a real combination is rare. "No data to compare" was being mislabelled as "rare."
**Data investigation:** Pulled 24 months of real sold-house days-on-market by price tercile across all 3 core suburbs. Robina: lower tercile median 17 days vs upper 31 days (45% faster, n=42/40). Burleigh Waters: 29 vs 60 days (52% faster, n=39/35). Varsity Lakes: 24.5 vs 22.5 days — flat, no pattern (n=24/22). Confirms the hypothesis is real but suburb-specific, not a universal Gold Coast rule.
**Fix:**
1. `_is_scarce()` gained `has_countable_anchors` — when a property has no anchors at all, scarcity is now correctly `False` (no false "uncommon_combination" claim), threaded through both call sites (`scarcity_narrative.py`, `positioning_object.py`).
2. `_soft_lead_phrases()` now falls back to the home's REAL buyer drivers (differentiators, walkable POIs) before the generic archetype descriptor — concrete beats vague.
3. New `scripts/precompute_price_tier_liquidity.py` — per-suburb, per-property-type (House only for now) tercile DOM stats, written to `Gold_Coast.precomputed_price_tier_liquidity`, with a `qualifies` gate (min sample size + genuine gap, `lower_median_dom <= 0.75 × upper_median_dom`) so it only activates where the data actually supports it.
4. New `value_liquidity_play` frame in `positioning_object.py` — fires when the subject's own price falls in its suburb's qualifying lower tercile (comps-based `estimate_price_anchor()` added to `offmarket_intel_poller.py`, since off-market docs almost never carry a cached `valuation_data.confidence.range` — that figure is a client-side calc specific to this deck; mirrors the same nearby-sold-comps logic as the deck's own "wealth reveal" card so the two stay consistent for the same visitor). Real evidence (`gap_pct`, tier medians) exposed in `positioning.evidence.liquidity` and cited verbatim on the frontend card when this frame wins.
**Verified:** 5 Glenferrie Drive now gets `primary_frame: value_liquidity_play`, `frameLine: "A Robina well-priced, fast-moving home."`, and a cited sentence: "Houses at this price point in Robina sold in a median of 17 days over the past 2 years — about 45.2% faster than higher-priced Robina houses (31 days)." Screenshotted live after reset+recompute; a Netlify deploy-timing gap (screenshot taken before the latest build finished) was caught and re-verified once `ready`.
**Files:** `scripts/property_reports/scarcity_narrative.py`, `scripts/property_reports/positioning_object.py`, `scripts/offmarket_intel_poller.py`, `scripts/precompute_price_tier_liquidity.py` (new) — all `Fields_Orchestrator`; `src/pages/OffMarketPage/types.ts`, `src/pages/OffMarketPage/OffMarketDeck.tsx` — `Feilds_Website/01_Website`.
**Recurrence:** First. `precompute_price_tier_liquidity.py` should be re-run periodically (not yet scheduled/cronned) as sold data accrues — currently a one-off manual run.

---

## [BUILD-PROFILE-URL-AUDIT] Broader address audit of build_profile_url() — 1 more real bug, 1 false alarm — 12:20 AEST
**Context:** Item 2 of the 3 follow-ups from yesterday's rewrite. Given 2 slug bugs had already turned up in the prior 24h (unit-number concatenation, street abbreviations), tested a random 45-address sample (15 per core suburb, deliberately including unit prefixes, hyphenated street-number ranges, huge apartment-style unit numbers, and letter-suffixed street numbers) against the live fetch, using blob size (<5KB = suspect, matching the established "thin fallback page" signature) as the pass/fail signal.
**Result: 43/45 clean**, including all the previously-fixed categories (unit slashes, abbreviations) holding up correctly across a broad sample. 2 flagged:
1. **Real bug:** "17A Sandpiper Drive" — Domain's URL drops the letter suffix entirely (`17-sandpiper-drive`, not `17a-sandpiper-drive`; confirmed 99KB real page vs 1KB fallback). Fixed with a regex scoped to the leading street-number token only, specifically excluding unit-style prefixes like "17A/5 X Street" (letter must be followed by whitespace, not a slash, to trigger the strip) so it doesn't risk stripping a meaningful unit letter.
2. **False alarm:** "38 Palma Crescent" returned an empty response during the audit run, but re-fetching immediately after returned a full 385KB real page — confirmed pure Bright Data transient flakiness (already handled by existing retry logic in `fetch_timeline()`), not a URL construction issue.
**Verified:** re-tested the fix against the real failing case (3 timeline events returned) and 3 controls (unaffected formats unchanged) before pushing.
**Files:** `scripts/refresh_property_timelines.py` (`Fields_Orchestrator`).
**Recurrence:** 3rd slug-construction bug found in `build_profile_url()` across 2 sessions, but the broad-sample audit came back 95.6% clean — treating this function as adequately hardened for now rather than continuing to hunt for a 4th edge case; revisit only if future refresh runs show an unexplained failure pattern.

---

## [OFFMARKET-ANTIFRAME-SUBURB-GATE + LIQUIDITY-CRON-HEALTH] Suppressed the beachside anti-frame for inland suburbs, cronned + health-checked the liquidity precompute — 12:45 AEST
**Symptom:** Will flagged the "How we'd position it" card's anti-frame — "Not a beachside lifestyle home / It's 3.6 km from the coast" — as answering a question nobody asked for Robina/Varsity Lakes, suburbs that were never plausible beachside candidates for any property in them.
**Root cause:** `positioning_object.py`'s `beachside_lifestyle` anti-frame fired purely on the subject's own beach distance (`notCoastal = beach_km > 3.5`), with no check on whether "beachside" was ever a plausible default assumption for that SUBURB in the first place. For a genuinely coastal-adjacent suburb (Burleigh Waters/Burleigh Heads) disqualifying it is useful — some properties there are near the beach, some aren't. For an inland suburb, every property clears `notCoastal`, so the anti-frame fires unconditionally and reads as arguing against a strawman.
**Fix:** Added `COASTAL_ADJACENT_SUBURBS = {"burleigh_waters", "burleigh_heads"}` allowlist; the anti-frame test now requires both `notCoastal` AND suburb membership. Verified live: Robina now returns `anti_frames: []`; allowlist membership independently verified for all 4 core+adjacent suburbs.
**Also addressed (Will, same message):** cronned `precompute_price_tier_liquidity.py` (1st of month, 05:52 AEST, same slot pattern as the other monthly precompute jobs) and added a per-suburb freshness+qualifies row to `main_site_health_check.py` ("Off-market price-tier liquidity", Market Metrics page) so a stalled/never-run precompute shows MISSING/STALE in the existing "Fields Systems Health" Google Sheet (pushed nightly at 01:00 AEST by the existing `main_site_health_to_sheet.py` — no separate sheet-writer change needed, it reads whatever `main_site_health_check.py`'s `collect()` produces) rather than failing silently. Verified the new rows render correctly (Robina/Burleigh Waters `qualifies=True` with real gap%, Varsity Lakes `qualifies=False`, all `OK` freshness today).
**Gotcha hit installing the cron:** `crontab <path>` failed with a truncated "file not found" error when the path was under this session's long scratchpad directory (UUID-bearing path) — some quirk in the `crontab` binary's argument handling, not a real missing-file issue (confirmed the file existed via `ls`). Worked immediately from a short `/tmp/...` path. Not root-caused further; noting the workaround for next time a crontab install needs a generated file.
**Files:** `scripts/property_reports/positioning_object.py`, `scripts/main_site_health_check.py` (`Fields_Orchestrator`); crontab (VM-level, not in git).
**Recurrence:** First.

## [PROPERTY-TITLE-AI-OVERRIDE] Published editorial pages invisible to Google for their own address search — 12:50 AEST
**Symptom:** Will reported 6 Notre Dame Court, Varsity Lakes (an AI-editorial-published new listing) doesn't appear anywhere in Google search results for its own address — not low-ranked, completely absent, even though GSC URL Inspection showed it "Submitted and indexed" (crawled 2026-07-22).
**Root cause:** `property.$id.tsx:151` set the page's `<title>` tag to `p.ai_meta_title` whenever AI editorial was published, unconditionally overriding the address-first fallback. The AI-generated title ("6 Notre Dame Ct — $85K Under Its Street Twin") abbreviates the street type (Court→Ct), and never includes the suburb, state, or postcode. For a literal address search — the most exact-match-dependent query type there is — Google's relevance matching leans heavily on the `<title>` tag; with most of the query terms missing from it, the page couldn't be selected as relevant at any rank, regardless of indexation state. Confirmed systemic: identical pattern (abbreviated street type, no suburb/state) across all 141 published editorial pages (66 Robina, 47 Burleigh Waters, 28 Varsity Lakes) — this is what step 120 (AI Property Editorial, New Listings) produces every night. Directly undercuts the SEO demand-engine's stated goal (owners googling their own address should land on Fields).
**Fix:** Decoupled the `<title>`/`twitter:title` tags from `og:title`/social share preview. `<title>` now always renders `${address} | Fields Estate` regardless of editorial status (SEO-critical, exact-match address terms). `og:title`/`twitter:title` still use the AI-generated hook when published (good for Facebook/Twitter link-preview engagement, where a catchy headline beats a bare address). H1, JSON-LD, and meta description were unaffected and already correct — this was purely a `<title>` tag scoping bug.
**Follow-up (same investigation, 13:05 AEST):** After deploying the SSR fix, verified via `curl` (correct new title) but a real headless-Chrome render (`site-inspector.js`) still showed the OLD AI-hook title in `document.title` after JS ran — SSR was right, hydration wasn't. Root cause: `PropertyPage.tsx:315` independently re-fetches the property client-side on mount and had its OWN `document.title = ai.meta_title || ...` assignment via `updateSeoMeta()` (a legacy pre-SSR client-side SEO mechanism), silently reverting the fix immediately after hydration. Fixed the same way — `document.title` now uses the identical address-first fallback as the SSR `meta()`, the AI hook still drives `og:title`/`twitter:title` via `updateSeoMeta`. Re-verified with a full browser render: title stays correct post-hydration, 0 console errors, no new network errors, page visually unchanged (screenshot confirmed — on-page editorial headline is untouched, only invisible `<title>`/meta changed).
**Files:** `Feilds_Website/01_Website/src/routes/property.$id.tsx` (commit dd6b92a), `Feilds_Website/01_Website/src/pages/PropertyPage/PropertyPage.tsx` (commit 09fd920), both `Will954633/Website_Version_Feb_2026`
**Recurrence:** First (check: `grep -h "^## \[" logs/fix-history/*.md | sed 's/ — .*//' | sort | uniq -d`)

---

## [POLICY-RESEARCH-AUTOMATION] Built the recurring pre-cycle policy-research step — 12:50 AEST
**Context:** Item 3 of the 3 follow-ups from yesterday's rewrite — Will asked to flag (not build) a recurring policy-news research step ahead of each monthly pulse cycle. Asked Will to resolve the real architectural fork first: headless scripts don't have WebSearch, so automating this needs either a paid Anthropic API call (small ongoing cost, risk given Anthropic credits are already low elsewhere on this VM) or keeping it a manual checklist step. Will asked whether it could route through his Claude Max subscription instead.
**Finding: yes, and it wasn't previously being done.** `scripts/backend_enrichment/claude_max_client.py` (the existing Max-billing pattern in this codebase) deliberately strips tools including web_search and falls back to the pay-as-you-go API the moment a tool is requested — it's designed for simple single-shot text generation, not research. But the `claude` CLI itself, invoked normally (not through that restrictive shim — no `--max-turns 1`, no tool-stripping), runs as a full agentic session with WebSearch available, authenticated via the VM's existing persisted CLI login (no `CLAUDE_CODE_OAUTH_TOKEN` needed, contrary to what the auth-setup memory implied), and bills against Max subscription. Verified live: a real research call returned current, correctly-sourced facts (RBA rate, budget dates) for ~$0.30-0.65 notional cost, no API credit consumption.
**Built:** `scripts/fetch_policy_research.py` — invokes `claude -p <prompt> --output-format json` directly (bypassing `claude_max_client.py`'s shim entirely) with a prompt covering RBA rate path, federal tax reform (negative gearing/CGT), first-home-buyer schemes, QLD-specific policy, and any other market-moving news from the past ~30 days — explicitly asks for "unchanged from last month" callouts so the monthly diff is fast to read, not a full rewrite every time. Writes to `system_monitor.policy_research_briefs`. First real run surfaced genuinely new items my own ad-hoc WebSearch research earlier this session had missed (AML/CTF real estate rules live 1 July 2026, QLD's new "Boost to Buy" shared equity scheme, FHOG extension, foreign buyer ban extended to 2029) — validates the approach beyond just "it runs."
**Wired up:** cron `0 6 28 * *` (a few days ahead of the existing 1st-of-month Telegram reminder) plus `telegram_notify.py`'s `market_pulse_reminder()` now checks for a cached brief and surfaces it (or flags it's missing) in the reminder message, so the collaborative writing session starts with the research already done instead of redone ad-hoc.
**Gotcha hit installing the cron change:** `crontab <filepath>` failed silently with a truncated-path error ("...scratchpad/c: No such file or directory") despite the file existing and being readable — same symptom seen twice in this session on unrelated cron edits (2026-07-22 and earlier today), still unexplained (possibly an environment-specific `crontab` binary quirk in this sandbox, not investigated further). Piping the same content via stdin (`cat file | crontab -`) worked cleanly both times it was tried. **Recommend using stdin, not a file argument, for all future `crontab` edits on this VM.**
**Files:** `scripts/fetch_policy_research.py` (new), `scripts/telegram_notify.py` (both `Fields_Orchestrator`); crontab (not in git — VM-local, installed via stdin).
**Recurrence:** N/A (new capability). The `crontab <file>` truncation issue is a 3rd occurrence within this session and the prior one — worth a standing note in memory even without a root cause.

---

## [OFFMARKET-PHOTO-CARD-BACKGROUNDS] Added real photo backgrounds to Right Buyer + How we'd position it cards — 12:53 AEST
**Context:** Will noticed the "Right Buyer" and "How we'd position it" cards had lost their photo backgrounds (they never had one — only the hero and Combination cards were photo-backed) and asked for all 3 property-attribute/positioning cards to use a real photo where available, falling back to today's plain look otherwise.
**Build:** `db.server.ts` gained `allHeroPhotos(doc)` (exposes the full best-first ranked photo list `_rankedHeroPhotos()` already computed, not just index 0/1) — threaded through the loader as a new `heroPhotos: string[]` field. `OffMarketDeck.tsx` gained a `PhotoCardWrap` component: layers a real photo (index 2/3 of the ranked list) + scrim behind a card's EXISTING, unmodified content when one exists, and renders the content un-wrapped (today's exact look) when it doesn't — a pure addition, never a behaviour change for thin-gallery properties. Deliberately no satellite fallback for these two cards (per Will's "fallback to the current view" — satellite was never their look, only the hero/Combination cards' fallback).
**CSS restructure required:** the existing hero-card layout (`.cardHero`/`.heroContent`) was built for SHORT content (address+chips, or a 2-3 line combination sentence) via `position:absolute; bottom:0` — Right Buyer/How-we'd-position-it carry multiple paragraphs + an anti-frame box, which would have overflowed/clipped under the old layout. Changed `.cardHero` to a flex column and `.heroContent` to `margin-top:auto` (sticks to bottom when short — visually identical to the old behaviour for the 2 existing short cards — but grows the card and scrolls via the slide's own `overflow-y` when long, instead of clipping). Strengthened `.heroScrim` (more opaque throughout, not just a bottom sliver) so white text stays legible over more of the image. Added `.onPhoto` scoped overrides cascading the existing CardShell text classes (`.lead`, `.note`, `.verdictHeadline`, `.verdictSub`, `.standoutLabel`, etc.) to white — CardShell's own JSX is completely unchanged, only the wrapping container differs.
**Verified:** `tsc`/`build` clean. Live-screenshotted both cards on 1/1 Alexandrina Drive (32-40 photo gallery) — real distinct photos, fully legible multi-paragraph text, anti-frame box and evidence citations all render correctly over the photo.
**Files:** `src/lib/db.server.ts`, `src/routes/off-market.$slug.tsx`, `src/pages/OffMarketPage/types.ts`, `src/pages/OffMarketPage/OffMarketDeck.tsx`, `src/pages/OffMarketPage/OffMarketDeck.module.css` (all `Feilds_Website/01_Website`).
**Recurrence:** First.

---

## [OFFMARKET-RIGHTBUYER-TYPEWRITER] Typewriter reveal + spacing/font fixes on the Right Buyer card — 13:13 AEST
**Context:** Will found the Right Buyer card text-heavy but fine content-wise; asked for (1) a character-by-character typewriter reveal matching chat-assistant streaming cadence, (2) a visible gap between the leadBuyer paragraph and the closing bold line, (3) the bold line in a distinctly smaller/different size than the h2 heading above it.
**Build:** New `RightBuyerTyped` component in `OffMarketDeck.tsx` — sequentially types the subhead/leadBuyer-body/bold-line (20ms/char, 280ms pause between blocks, blinking CSS-drawn caret), restarting whenever the card becomes the active one in the swipe deck (same active-prop-driven replay pattern as `CapitalGainChart`). Empty blocks (leadBuyer body can be blank on a thin positioning result) are skipped straight through rather than stalling. New `.rightBuyerBoldLine` CSS class (15px, italic, bold) replaces reuse of the shared `.verdictHeadline` — kept separate so this change couldn't affect the capital-gain card's verdict styling. The bold line is wrapped in the existing `.verdictBlock` (top margin + divider line) for the requested spacing, with an `.onPhoto` border-color override so the divider stays visible over a photo background.
**Verified:** live-screenshotted mid-type (caret visible, text correctly truncated mid-word) and fully settled (clear divider + visibly smaller italic closing line) on 1/1 Alexandrina Drive.
**Files:** `src/pages/OffMarketPage/OffMarketDeck.tsx`, `src/pages/OffMarketPage/OffMarketDeck.module.css` (`Feilds_Website/01_Website`).
**Recurrence:** First.

---

## [OFFMARKET-PERSONA-CARD] Real buyer persona replaces generic Right Buyer copy; liquidity evidence relocated off the frame card — 14:23 AEST
**Context:** Will asked for a deep-dive on what the mini-site's full "Right Buyer" tab does (12 sections, 3 chained Opus resolvers) versus the off-market deck's much thinner version, and specifically flagged that "sells fast because affordable" (`value_liquidity_play`) was mislabeled as "how we'd position it" when it's really a market-demand comfort fact, not a seller-positioning strategy. Agreed plan: pull the mini-site's real single-persona depth into the Right Buyer card (reusing the proven `personas_narrative.py` prompt, not a new one), demote `value_liquidity_play` out of the frame-scoring competition entirely, and surface the deterministic `drivers.price`/`drivers.buyer` split (already computed, previously unused) on the frame card for real comprehensiveness.
**Build:**
1. `positioning_object.py` — removed `value_liquidity_play` from `_score_archetypes()` (can never win `primary_frame` again), removed its now-dead `_soft_lead_phrases()` sig branch and FRAMES catalogue entry. `affordableFastMover`/`evidence.liquidity` still computed and exposed — just never as a "frame."
2. `offmarket_intel_poller.py` — `estimate_price_anchor()` → `estimate_price_range()` (now returns `{low, mid, high}`, not just a midpoint) so it can feed both `positioning_object`'s price-tier check AND the personas prompt's "working valuation range" input. New `resolve_personas_narrative()` call reusing the mini-site's exact, battle-tested 3-persona prompt (only `personas[0]` is stored/used) — **model choice needed a live pivot**: Sonnet 5 measured reliably MALFORMED JSON on this schema's complexity (unterminated strings, not just truncation) even after raising `MAX_TOKENS` 2200→3600; switched to Opus 4.7 (its own proven default, already running this exact prompt for 23 live mini-site reports) — confirmed working live (3-attempt retry succeeded, real concrete persona with grounded numbers). Frontend's positioning poll budget raised from 20×2.5s (50s) to 70×3s (210s) — measured worst-case chain (combination narrative + persona, with retries) up to ~2m30s live.
3. Frontend — `RightBuyerTyped` rebuilt: with a persona, sequentially types brief → (instant) whyThisHome bullets → paysMoreFor → hesitation (in a distinct callout box) → whereFound → (instant) liquidity comfort line if the suburb qualifies → bold closer; falls back to the prior 3-block sequence when no persona exists (yet, or ever, for a given property). Card title becomes `persona.label` (e.g. "Varsity College catchment families") instead of the generic fixed title. "How we'd position it" card gained a `drivers.price`/`drivers.buyer` breakdown (new `.driverBreakdown`/`.microLabel` CSS) and lost the old liquidity citation block (moved to the persona card via new `liquidityDemandLine()` helper).
**Verified:** live end-to-end via the real queue (not just CLI) on 1/1 Alexandrina Drive — persona title, all 4 why-bullets, pays-more-for, hesitation, where-found, and bold closer all rendered correctly; Varsity Lakes correctly showed NO liquidity comfort line (that suburb doesn't qualify — confirms the gate isn't just always-on); "How we'd position it" correctly showed the new price/buyer driver breakdown.
**Known follow-up, not fixed:** this new persona call duplicates one of the 3 Opus calls the full mini-site's own `prewarm()` background build already fires once a visitor reaches idx>=3 — two independent computations of the same expensive content for a visitor who goes on to open the full deep-dive. Fine at today's near-zero volume (dormant `?arm=` feature); worth de-duplicating (e.g. checking if a `property_reports` doc's persona already exists before generating a second one) before this arm goes live in PostHog.
**Files:** `scripts/property_reports/positioning_object.py`, `scripts/offmarket_intel_poller.py` (`Fields_Orchestrator`); `src/pages/OffMarketPage/types.ts`, `src/pages/OffMarketPage/OffMarketDeck.tsx`, `src/pages/OffMarketPage/OffMarketDeck.module.css` (`Feilds_Website/01_Website`).
**Recurrence:** First.

## [MARKET-PULSE-SUMMARY-NEVER-RENDERED] Written analysis prose invisible on live site + 4/6 category pages found to be hardcoded static content — 14:34 AEST

**Symptom:** User asked where the collaboratively-written analysis (including policy research) appears on `/market-metrics/Robina/buy` — it doesn't, anywhere, for any suburb or category.

**Root cause (part 1 — the reported bug):** `pulse.summary` is only ever rendered by `CategorySummary.tsx`. That component is imported into `MarketMetricsPage.tsx` but never mounted in JSX (`grep -n "<CategorySummary"` → zero matches). Every individual Section component (BuySection, CrashRiskSection, OverviewSection, DirectionSection, SuburbCompareSection) imports only `CategorySummary`'s TYPE, never the component itself, and never references `.summary`. The only place written content is visible at all is `sell-now`, via a separate mechanism (`narrative.pillars` → `SellNowSection`'s own `chartNarrative` lookup).

**Root cause (part 2 — discovered during investigation, changes the fix's scope):** Read all 6 Section components in full. Only **SellNowSection** and **DirectionSection** are actually wired to live pulse data (`data_snapshot`, and for sell-now, `narrative.pillars` commentary per chart). **BuySection, CrashRiskSection, OverviewSection, and SuburbCompareSection are almost entirely hardcoded static text dated March 2026** — fixed dollar figures ("$1,400,000–$1,500,000"), fixed days-on-market ("26 days"), fixed RBA references ("8.27% SVR", "third hike expected in May" — a stale forecast), baked into hero text, dashboard cards, bullets, and 10-12 FAQ answers each. They only use `pulseData` for the verdict badge colour/label and the generated date. Mounting `<CategorySummary>` (which shows `.summary`) on these 4 pages today would put our current, correct analysis directly above/beside contradicting stale hardcoded numbers — a worse outcome than the current silence, and itself a CLAUDE.md editorial-compliance problem (the crash-risk hardcoded content is a stale prediction, which the rules forbid).

**Decision (with user, 2026-07-23):** Do not attempt a full or partial frontend fix today. Confirmed `sell-now` and `direction` — the two pages actually wired to data — reflect current Q2 2026 data correctly for all 3 suburbs via `verify_market_metrics_live.py`. Fixing the other 4 hardcoded pages is logged as a tracked follow-up, not rushed.

**New bugs found during the verification pass (not yet fixed, logged for follow-up):**
1. **Verdict badge vs. computed market-type mismatch** (`SellNowSection.tsx`) — the badge label reads from the stored `pulse.verdict` field; the body text's "buyer's/balanced/seller's market" phrase is separately computed client-side from `data_snapshot.absorption_rate_months` via local thresholds. These disagree on all 3 suburbs, e.g. Varsity Lakes shows badge "BALANCED MARKET" directly above text "varsity lakes is currently a **buyer's** market." Robina: badge "BALANCED MARKET" vs text "buyer's market" (absorption 5.1mo). Burleigh Waters: badge "SELLERS ADVANTAGE" vs text "balanced market" (absorption 3.3mo).
2. **QoQ/YoY forecast chart duplication** (`DirectionSection.tsx`, `PriceGrowthChart` with `metric="qoq"` vs `metric="yoy"`) — both chart instances render an identical CURRENT/PEAK/LOW stat box on all 3 suburbs, and the figure doesn't match the dashboard card above it computed from `data_snapshot` (e.g. Robina dashboard says QoQ -3.8%, chart says "CURRENT: 10.5%" — same number appears under both the QoQ and YoY chart headings). Suggests the `metric` prop isn't actually differentiating the chart's own internal "current" calculation, or both charts are pulling from the same cached/undifferentiated source.

**Also reconfirmed still open (previously logged, not new):** lowercase suburb name rendering throughout body text/headings (breadcrumbs, H1s — e.g. "burleigh waters" not "Burleigh Waters", violates editorial capitalisation rule); SSR-vs-pulse YoY drift in the hero stat card (+9.1% shown at top of page vs +8.7% in body text, two independent computations); absorption-rate dual-pipeline split (`data_snapshot` value vs `absorption_rate_snapshots` collection value, e.g. Robina 5.1 vs 4.5 months — this is likely also the root cause of bug #1 above, since the SellNowSection badge/verdict may have been hand-set using one pipeline's number while the computed marketType text uses the other).

**Files:** None changed — investigation and verification only. Findings recorded here and in memory for the next session.

**Recurrence:** First (new discovery — the "4 of 6 pages are hardcoded" finding and the two new bugs are novel; the lowercase/YoY-drift/absorption-split items are recurring, already logged in earlier 2026-07-23 entries).

---

## [OFFMARKET-STALE-CACHE-REDUX + TYPEWRITER-REVERT] Stale cache after value_liquidity_play removal; typewriter effect reverted — 14:38 AEST
**Symptom 1:** Will saw 5 Glenferrie Drive's "How we'd position it" still showing the old "A Robina well-priced, fast-moving home." / "We'd lead on the price point" copy — exactly the `value_liquidity_play` frame content that should no longer be generatable after the same-day fix removing it from the frame-scoring competition.
**Root cause:** Same class of bug as the earlier `[OFFMARKET-INTEL-STALE-CACHE]` entry today — `offmarket_positioning` caches permanently once `status:"done"`, and this specific property's cached doc (`computed_at` 02:36 UTC) predated the `positioning_object.py` scoring fix (pushed later). Every OTHER property tested today happened to get reset for unrelated reasons; this one was missed.
**Fix:** Reset the doc to `status:"pending"`; poller recomputed in 75s. Confirmed `primary_frame` is now `school_walk_family` with the correct frameLine. Reinforces the standing follow-up already logged: this cache has no schema-version staleness detection — every backend change to `compute_positioning()`'s output shape needs a manual sweep of already-cached docs (or better, a version stamp) before it can be trusted live. Still not built; only 4 properties have ever been cached, all now manually reconciled.
**Symptom 2:** Will found the Right Buyer card's typewriter effect (shipped earlier today) "doesn't work" and asked for instant rendering instead, plus removal of the italic styling on the closing bold line.
**Fix:** `RightBuyerTyped` rewritten to render all content immediately — no stage/shown state, no `useEffect` reveal loop, no caret. Removed the now-fully-dead `.typeCaret`/`@keyframes caretBlink` CSS and the `active` prop threading (`rightBuyerCardIdx` was only used for this). Removed `font-style: italic` from `.rightBuyerBoldLine`.
**Verified:** live on 5 Glenferrie Drive (freshly regenerated persona "Robina Downsizers From Larger Homes" as a side effect of the cache reset) — all text present immediately in the page DOM, no animation; liquidity comfort line now correctly appears on the Right Buyer card (Robina qualifies); "How we'd position it" shows the corrected frame + drivers breakdown.
**Files:** `src/pages/OffMarketPage/OffMarketDeck.tsx`, `src/pages/OffMarketPage/OffMarketDeck.module.css` (`Feilds_Website/01_Website`); data-only fix in `system_monitor.offmarket_positioning` (no file change).
**Recurrence:** 2nd occurrence of the stale-positioning-cache pattern today (see `[OFFMARKET-INTEL-STALE-CACHE]` for the 1st, on the sibling `offmarket_intel` collection). The "needs a schema-version stamp" follow-up is now overdue rather than nice-to-have.
