# Fix History — 2026-08-19

## [WITHDRAWN-SERIAL-FETCH-BUDGET] Step 113 parallelised fetch — 11:20 AEST
**Symptom:** `detect_withdrawn.py` (step 113) aborted early with "runtime budget exceeded" every night for a week. At ~26s per BrightData Web Unlocker round-trip and 221 live listings, a full pass needs ~95 min against `MAX_RUNTIME_MIN` = 40, so a complete sweep was arithmetically impossible and withdrawn listings sat undetected.
**Root cause:** the main loop fetched one listing at a time. The 26s is pure network wait — no local CPU, no shared state — so the step was ~100% idle by wall clock.
**Fix:** parallelised ONLY the fetch. `worklist` is now processed in chunks of `2 x FETCH_WORKERS`; each chunk's `check_listing_status` calls resolve concurrently via `ThreadPoolExecutor.map` (order-preserving), then the chunk's results are iterated IN ORDER on the main thread where every DB write still happens serially, unchanged. Workers run `_resolve_status()`, which never receives the collection handle and does no DB access, so no write can originate off the main thread. Worker count from `WITHDRAWN_FETCH_WORKERS` (default 4, deliberately conservative; 6-8 is the target). Wall-clock `deadline` is now checked BETWEEN chunks so an abort never strands in-flight paid requests. Per-item `time.sleep(REQUEST_DELAY)` moved to between chunks.
Circuit breaker changed from consecutive-error counting to a ROLLING WINDOW (`>= WITHDRAWN_CIRCUIT_BREAKER` (15) errors in the last `WITHDRAWN_CIRCUIT_WINDOW` (20) results) — with concurrent completion, "consecutive" described nothing real and one healthy result between failures reset the count to zero. `circuit_broken` semantics and the `⚠ COVERAGE BEHIND` line that `main_site_health_check.py` greps are unchanged.
Round-robin interleaving, per-suburb counters, the rotation cursor field (`withdrawn_last_checked_at`) and `check_listing_status` classification logic are all untouched. Added `--limit N` for testing.
**Verification:** `--dry-run --limit 12` completed in **112.8s** (12 checked, 12 active, 0 errors, robina=4/varsity_lakes=4/burleigh_waters=4 — interleaving preserved) vs ~312s serial, ~2.8x.
**Files:** `scripts/detect_withdrawn.py`
**Recurrence:** First

## [AUDIT-UNPROJECTED-OOM] Step 107 dead 13 nights — unprojected find({}) needed ~13 GB — 19:30 AEST
**Symptom:** step 107 (Database Audit & Validation) hit its 600s timeout on all 3 attempts every night from 2026-08-06 to 08-19 — 13 consecutive nights, 1921s of wall clock burned per run. `stdout.log` was 1 byte. Nothing anywhere reported it.
**Root cause:** `audit_collection()` did `properties = list(collection.find({}))` — no projection, every field of every document across 110 collections materialised into a Python list. The audit only ever reads six fields. Measured: ~1,112 KB/doc × 12,104 robina docs ≈ **13.1 GB of Python heap** on a 15 GB VM already using ~6 GB with swap in use, so it swap-thrashed to SIGTERM rather than OOM-killing (no dmesg entry — part of why it was invisible).
It ran fine in 72.6s on 08-05 and failed from 08-06 onward: **document size changed, not count** (+9% docs cannot explain 72s → >600s). `[COMPS-DETAIL-EXPOSED]` on the evening of 08-05 deliberately persisted per-comparable adjustment detail; `valuation_data` is now ~115 KB of a 149 KB average robina doc. A legitimate schema change walked into an unbounded read pattern that was always fragile. Raising the timeout would not have helped — 600s was already ~6x the real work.
**Fix:**
1. Six-field projection + stream the cursor instead of `list()`. **8.6s / 105 MB peak RSS**, verified with `/usr/bin/time -v`.
2. Populated `EXCLUDED_COLLECTIONS`, which was `[]`. Six collections are keyed on something other than suburb (`propradar_sold`, `propradar_coverage_gaps`, `propradar_gap_enriched`, `unit_valuations`, `unit_statutory_comps`, `property_attributes`) and so reported every document as misplaced: **2,731 false positives, zero true ones**. Each verified as genuinely cross-suburb before excluding. Post-fix the audit finds **0 misplaced across 411,165 properties / 104 collections** — so the 416 reported on 08-05 were false positives too, and there was never a real misplacement problem.
3. Rule 7: wrapped the full sweep in `job_run("database_audit", cadence_hours=24)` — it had no heartbeat at all, so it was absent from the Process Registry. A targeted `--collection` run stays unheartbeated (developer action, not the ongoing process).
4. Rule 7b: assert an outcome. `--no-fail` forces exit 0 regardless of findings, so a run that scanned nothing was indistinguishable from a clean audit. Now raises if `collections_scanned == 0 or properties_audited == 0`.
5. Per-collection progress lines, so a future hang is localisable — the entire 110-collection scan printed nothing, which is why the failure could not be placed from logs.
6. Added step 107 to `_STEP_OUTCOME_CHECKS` in `main_site_health_check.py` (two checks: reached its summary at all; misplaced count judged on level, not on non-zero — the [WTA-OPS-020] mistake). Verified both regexes match real step output and return OK.
7. **Latent bug found in the process:** the shared capture-mode consumer does `int(mm.group(1))`, which raises `ValueError` on any comma-formatted number and would have taken the whole health check down. Now strips thousands separators — no-op for the existing bare-`\d+` patterns.
**Why it was invisible for 13 nights:** three independent safety nets and none covered this step — no Rule 7 heartbeat, `--no-fail` inverting Rule 7b, and no row in the step-outcome table.
**Files:** `scripts/database_audit.py`, `scripts/main_site_health_check.py`
**Recurrence:** First for the OOM. The step's failure was logged as `[WATCHDOG] Step 107` on 2026-08-18 without a root cause.

## [EDITORIAL-PRICE-ABSENCE-FALSE] 10 live pages claimed "no price" — 9 of them had one — 20:10 AEST
**Symptom:** while fixing one auction headline, a sweep found published property-page copy framing a missing price as concealment across **24+ properties / 53 fields**. Worse: on **9 of them the claim was factually false** — the page said no price was published while `price` held a real figure. Examples: 150 Christine Ave headline `"Contact Agent" Hides the Price` with `price='$1,599,000'`; 4 Pipit Parade `No price is published` with `price='Offers Over $1,795,000'`; 2a Pipers Point `Price Cut Twice, Then Hidden` with `price='$2,550,000 - Negotiable'`. 120 Christine Ave advertised `offers close 3 August` while `listing_status='under_contract'`, 16 days after the deadline.
**Root cause (two, stacked):**
1. **The prompt never covered it.** `config/property_editorial_prompt.md` PART 8 banned adjudicating the ask (rule 13) and naming competitors (rule 14) but said nothing about a *missing* price, so the model treated statutory silence as a curiosity gap. In QLD the agent is legally barred from giving a price guide on an auction property (POA s216(2)(c)), and a seller instruction gags them on non-auction sales (s216(3)) — the silence is compliance or the seller's own instruction, never concealment. Framing it otherwise is rule 14 in a different costume and puts the seller on the wrong side of the sentence, on a seller-funded business.
2. **`ai_analysis` is written once and never regenerated on a price change** (already recorded as the three-layer editorial staleness defect). Every false page launched as EOI/auction/deadline-sale and later published a figure the editorial never caught up with. The framing bug made a stale field into a false accusation.
**Fix:**
- New PART 8 **rule 17** with the POA s216 reasoning and worked ❌/✅ examples, so the model knows why, not just what. Pushed.
- **10 pages corrected in place** (4 Burleigh Waters, 5 Robina, 1 Varsity Lakes) — the 9 factually false plus the expired-deadline one. Originals preserved in `<field>_previous`; `reframed_reason` records which rule. Replacement copy reuses only facts already present in each document, each traced to its source field.
- Dropped an expired "offers close Monday 17 August" from 5 Camberwell rather than republishing it.
- **Verification:** the contradicted-price sweep now returns **0** across all three suburbs.
**Not yet done — needs Will's approval (framing-only, nothing false):** ~85 fields across ~26 properties still carry absence framing, plus adjacent breaches the sweep surfaced — rule 15 flipper framing (14 Elfin St `Bought for $930K in 2023. Renovated to $1.73M.`, 34 Aruma Ave, 24 Tropicana Cct), rule 14 named portals in headlines (6 properties in Burleigh Waters alone say "Domain's own valuation/estimate"), rule 7b single Fields figures in H1s (9 Gainsborough `$1.5M`, 7 Aruma `$2,330,299`), and ~a dozen `$1.25m`-style number-format breaches. Proposals in `docs/EDITORIAL_ABSENCE_FRAMING_PROPOSALS_2026-08-19.md`.
**⚠ The `verdict` layer was NOT swept.** 9 Auriga, 89 Camberwell, 7 Nardoo, 21 Olympus and 830 Legend Trail all repeat "no price guide has been published" in the body. Same multi-layer trap as Rule 6's `market_pulse` bug — fixing the headline and confirming it live proves nothing about the layer underneath.
**Files:** `config/property_editorial_prompt.md`, `Gold_Coast.{robina,burleigh_waters,varsity_lakes}` (10 docs)
**Recurrence:** Second for editorial framing — `[EDITORIAL-SELLER-ADVERSARIAL]` 2026-08-18 fixed price adjudication and this shape survived that pass, because that sweep looked for verdicts on the ask, not for claims about absence.

## [OFFMARKET-DIRECT-TEST-ROUND-2] Reopen the off-market → mini-site direct test on the V1.5 build + fix sticky `offmarket_arm` — 19:49 AEST
**Symptom:** Will asked what it would take to A/B the current `/off-market` V4 page against the V1.5 house mini-site. Investigation found the mechanism already existed (`off-market-direct-test.mjs`, built 2026-07-21) but had been dormant since the afternoon it shipped: it spent all 10 admission slots between 08:25 and 13:57 that day and self-terminated.
**Root cause (why round 1 can't answer the question):** all 7 round-1 reports built with the **full-AI** pipeline — measured `build_completed_at - build_started_at` of **175–568 s**, so those visitors sat on the building page for 3–9.5 minutes. The `no_llm` default inverted when V1.5 shipped 2026-08-16 (`slot_resolver.py:104` — `no_llm = build_mode != "full"`), and the same path now builds in a **median 4.5 s / p90 22.6 s (n=132)** vs `full` median 354 s (n=42). Pooling the cohorts would compare a 6-minute wait with a 5-second one and call it a page test.
**Secondary defect:** `useReportAnalytics.ts:106` registered the `offmarket_arm: "v4"` super-property and **never unregistered it**, unlike `OffMarketPage`/`DiscoveryDeck`/`DeckV3` which all do. The label is sticky for the rest of the session, so the `/your-home` view the direct-test redirect produces is filed under `offmarket_arm: "v4"` — silently pooling the exact two arms being compared.
**Fix:**
- New counter/arm/utm namespace `offmarket_direct_test_v2` (never share with v1, for the reason above); cap now `OFFMARKET_DIRECT_TEST_MAX`, default 100, so it can be raised without a deploy.
- `phUnregister("offmarket_arm")` added to the V4 analytics effect cleanup, **after** `sendExit()` so the exit event keeps its own arm tag.
- Corrected stale comment in the loader ("capped at 5" — it was 10).
**Not changed (verified already correct):** `analyse-your-home-submit.mjs:310` already stamps `build_mode = body.build_mode === 'full' ? 'full' : 'no_llm'` and persists it at :475, so new stubs are explicitly deterministic. No edit needed.
**Measurement note:** conversion-rate A/B is not viable here — V4 runs ~103 sessions/week at a 2.21% request rate resting on **3 events** (95% CI 0.0–4.7%); +20% needs 7.2 years, +100% needs 20 weeks. Judge on engagement (`v4_section_read`, scroll ≥50%) and session recordings, which run at 100% sampling (~86–94% capture for engaged sessions).
**⚠ CONFIRMED AND FIXED — the redirect was cacheable.** Suspected the edge might cache the admission redirect, then verified it live against the twin-listing 301 thrown from this same loader: request 1 → `cache-status: "Netlify Edge"; fwd=miss; fwd-status=301; stored`, request 2 → `"Netlify Edge"; hit`. Redirects thrown from this loader **are** stored and replayed. Left alone, a single admission would have redirected **every** subsequent visitor to that slug into the mini-site for the full hour **without incrementing the counter** — the cap would have silently stopped meaning anything and the arm split would have been unmeasurable. Round 1 could not have exposed this: 10 admissions across 7 distinct addresses in one afternoon, so no slug was ever hit twice inside a TTL. Fixed by giving the redirect explicit `Cache-Control: no-store, max-age=0` + `Netlify-CDN-Cache-Control: no-store`. A per-visitor decision must never be a cacheable response.
**Files:** `01_Website/netlify/functions/off-market-direct-test.mjs`, `01_Website/src/routes/off-market.$slug.tsx`, `01_Website/src/pages/OffMarketPage/v4/useReportAnalytics.ts`
**Commits:** `471f0bc6` (round-2 reopen + arm unregister), `fbab37c4` (no-store redirect fix)
**Recurrence:** 7th for the direct test — six hardening fixes on 2026-07-21 alone (`[OFFMARKET-DIRECT-TEST-VALUE-BUG]`, `-BOT-ADMISSION`, `-REFERER-OVERCORRECTION`, `-OFFSHORE-PATTERN`, `-AU-DATACENTER-GAP`, `-REPEAT-VISIT-DEDUP`). All were admission-gate defects that burned slots; this is the first change to the experiment's design rather than its gates.

## [ENVELOPE-FALLTHROUGH-MINISITE] Mini-site published valuations the engine had refused — 19:55 AEST
**Symptom:** 30 live `/your-home/` reports published a working range for homes the
comparable-sales engine had explicitly declined as outside its $1M–$2M design envelope.
In 18 cases the published figure was LARGER than the one suppressed, up to
$2,558,100–$4,287,400 (point $3,455,000) on `136-honeyeater-drive-burleigh-waters`.
One affected report, `819-legend-trail-robina`, is a genuine public AYH submission —
a real person was shown a frozen $1,403,399–$1,786,144 for a home the engine declines.

**Root cause:** Two independent holes.
(1) `precompute_valuations.py:4243` expresses refusal by NULLING `confidence.range`.
`SlotResolver._engine_valuation_range()` (slot_resolver.py:1812) reads that null and
cannot distinguish "deliberately declined" from "never valued" — both return None, and
None means fall through to Tier 2 `exterior_evidence`, a tier never backtested at any
price band. `directional_only` / `directional_reason` were never read anywhere in
`scripts/property_reports/`. The identical hazard was solved correctly for units on
2026-08-14 (Tier 0 returns before the house tiers); the envelope never got the guard.
(2) A home the engine NEVER valued has no refusal to read, so even a Tier 0.5 check
cannot fire — `136-harrier-drive-burleigh-waters` reached $2,809,000 off 4 comps
straight through the fallback.
Compounding: `refresh_valuation_slot()` only ever `$set`s, so it could add a range but
never retract one; `refresh_stale_valuations.py --only-missing` (default True) skipped
exactly the docs that already showed a figure — i.e. every doc needing retraction.

**Fix:**
- `slot_resolver.py` — new Tier 0.5 in `working_valuation_range()`: `_engine_declined()`
  reads the engine's own verdict (`directional_only`, else applies the envelope to
  `reconciled_valuation` for subjects computed before the gate landed / outside recompute
  scope) and returns None with `_no_figure_reason` set, feeding the existing `no_figure`
  path that already clears stale ranges.
- `slot_resolver.py` — the envelope now also binds the Tier 2/3 OUTPUT, so a fallback
  point outside $1M–$2M is suppressed even with no engine verdict present.
- `slot_resolver.py` — `refresh_valuation_slot()` can now RETRACT, returning
  `model_range: None` + `no_figure` when the subject is declined.
- `refresh_stale_valuations.py` — loads the subject BEFORE applying `--only-missing`, so
  a declined home is retracted rather than skipped; added the published-point envelope
  check and a `retracted` counter.

**Verification:** 30 retracted across two passes (29 engine-declined + 1 fallback-priced).
Independent re-query, not the script's own count: house-tier reports with a published
point outside $1M–$2M = 0; reports showing a range where the subject is `directional_only`
= 0. Frontend no-figure path already renders ("Working range being prepared") — no
frontend change needed. Poller restarted (caches resolver imports).

**Not fixed / follow-up:** `Gold_Coast.reedy_creek` still holds 4 unsuppressed subjects
with `reconciled_valuation >= $2M` and 1 `directional_only` retaining its range — outside
the target-suburb recompute scope. The mini-site is now protected regardless, but other
surfaces reading those subjects are not. Also: the more generous option (keep the
exterior_evidence range with Will's off-market V4 caveat copy instead of suppressing)
is deliberately deferred — suppression is the smaller, immediately-honest change.

**Files:** `scripts/property_reports/slot_resolver.py`,
`scripts/property_reports/refresh_stale_valuations.py`
**Recurrence:** First for the mini-site path. Related: `[YOURHOME-UNIT-HOUSE-COMPS]`
(2026-08-14) is the same class — a refusal re-answered by a weaker tier.

## [REPORT-API-PRIVATE-THREAD-EXPOSED] Private seller message threads served to anyone — 20:20 AEST
**Symptom:** `GET /api/v1/property-report?slug=<slug>` returned the full report document
to any caller — no auth, no device token, no ownership check. Slugs are derived
deterministically from street addresses, so they are guessable, not secret. Among the
fields served was `messages[]`, present on 211 of 243 documents, whose own welcome text
reads "This channel is private, just between the two of us." Four documents contain
messages a real homeowner typed to Will. Two further holes in the same family:
`property-report-message-send.mjs` gated with `if (ownerToken && ownerToken !== deviceToken)`,
so a document with NO owner token accepted ANY token — 172 of 243 docs — letting a forged
"seller message" email and Telegram Will as though genuine; and
`property-report-book-review.mjs` promoted an anonymous POST's email/phone onto
`owner.email` / `owner.phone` of any report, including the 161 homes whose owner never
asked us for one.

**Root cause:** The endpoint's entire security posture was a projection comment ("Owner
fields are NOT projected"), which withholds the device token and contact details and
nothing else. Ownership was used as a *feature switch* (visit-counter bumping in
property-report-activity.mjs) rather than as a *gate*, and the one endpoint that did gate
used a truthiness check that fails open on the majority of documents.

**Fix (stage 1 — surgical, shipped):**
- `property-report.mjs` — project `owner.device_token` solely to gate `messages` /
  `messages_refreshed_at`, withhold both unless the caller's `device_token` matches, then
  `delete doc.owner` so the token never reaches the payload. Verified the frontend
  fallback is `[]` (YourHomePage.tsx:1337) and not the demo fixture's thread, so a
  withheld thread renders empty rather than leaking 13 Terrace Court's messages.
- `property-report-message-send.mjs` — fail closed: `if (!ownerToken || ownerToken !== deviceToken)`.
- `property-report-book-review.mjs` — only promote contact to `owner.*` when the device
  token matches the report's owner. The booking is still recorded in full in
  `report_review_bookings` and still reaches Will, so no lead is lost.

**Verification (live, after deploy):** on `5-cedarwood-crescent-robina` — no token → 0
messages; wrong token → 0 messages; correct owner token → 1 message; `owner` block absent
in both responses; `valuation` and `slots` still served. Deploy logged
(website-deploy-tracker, commit 671f737b).

**NOT fixed — stage 2, needs Will:** the wider enumeration hole is still open. Anyone who
guesses a slug still gets our valuation of that home, our commercial analysis of how to
sell it, and — via `print_appraisal.dispatch_hold_reason` — our inference that 13 specific
addresses are investor-tenanted. `minisite-visit.mjs` remains an open WRITE that upserts
`crm_contacts.probable_address`. The designed fix is a stateless HMAC link key
(`k = HMAC(REPORT_LINK_SECRET, slug)[:16]`) that preserves link-sharing and QR arrivals,
with a `public_demo: true` escape hatch for the one report we publish deliberately.
Blocked on `REPORT_LINK_SECRET` being set on Netlify — the gate must fail closed, so the
env var has to exist BEFORE the deploy or every mini-site 404s. Rollout order: set env var
-> deploy frontend `k` forwarding -> deploy gate -> regenerate the 50 prepared mailer
pieces (none dispatched, so no reprint waste) -> update tracking-server redirect.

**Files:** `netlify/functions/property-report.mjs`,
`netlify/functions/property-report-message-send.mjs`,
`netlify/functions/property-report-book-review.mjs`
**Recurrence:** First.

## [REPORT-API-ENUMERATION-GATE] /your-home report endpoints gated behind an HMAC link key — 20:30 AEST
**Symptom:** Stage 2 of `[REPORT-API-PRIVATE-THREAD-EXPOSED]`. Beyond the message thread
closed earlier, anyone who guessed a slug still received the whole report: our valuation of
that home (`valuation.model_range`, `comps`, per-feature adjustments), our unpublished
commercial analysis of how to sell it (`buyers`, `positioning`, `scarcity`, `selling_plan`),
and — via `print_appraisal.dispatch_hold_reason` — our inference that 13 specific addresses
are investor-tenanted. `minisite-visit.mjs` was an open WRITE: any POST upserted
`crm_contacts.probable_address`, and since the slug encodes the street address, an attacker
chose the address they were attributing. 161 of 243 reports are homes whose owner never
asked us for one.

**Root cause:** Slugs are derived deterministically from street addresses, so the URL was
being relied on as a secret while being trivially computable. Ownership was used as a
feature switch (visit-counter bumping), never as a gate.

**Fix:** Stateless HMAC link key — `k = base64url(HMAC_SHA256(REPORT_LINK_SECRET, slug))[:16]`.
Chosen over a stored per-report token because any generator recomputes it from the slug
alone: no migration across 243 docs, nothing to reconcile.
- `db.mjs` — `reportLinkKey()`, `constantTimeEq()`, `mayReadReport(doc, url)`. Access order:
  `public_demo` -> valid `k` -> owning device token. Returns 404 on failure, never 403 — a
  403 confirms the slug exists and leaves an address-enumeration oracle.
  Fails CLOSED when `REPORT_LINK_SECRET` is unset.
- Gated: `property-report.mjs`, `property-report-activity.mjs`, and the `minisite-visit.mjs`
  WRITE. `property-report-progress.mjs` deliberately NOT gated — it exposes only build
  state, and gating it would put the post-submit polling handoff at risk for no gain.
- `minisite-visit.mjs` additionally refuses outright when `public_demo` is true, so demo
  traffic is never recorded as the probable owner of the demo property.
- `analyse-your-home-submit.mjs` returns `link_key` and a key-bearing `redirect_url`;
  `property-report-progress.mjs` stamps `ready_url`. ~85% of AYH traffic is the Meta in-app
  browser where localStorage is unreliable, so the URL key — not the device token — is the
  primary path back into the report for someone who just submitted an address.
- `src/utils/reportLink.ts` + call sites in `YourHomePage.tsx` and `LiveMarketStatus.tsx`.
- `shared/report_link.py` — the Python half, used by `generate_mailers_v2.py` (QR artwork)
  and `tracking-server/server.py` (`/scan` stamps `k` at redirect time, which rescues every
  already-printed code with NO reprint — the reason the QR points at /scan, not the report).

**Verification (live, post-deploy):** stranger no proof -> 404; stranger wrong key -> 404;
valid key -> 200; owner device token without key -> 200; owner slug without proof -> 404;
activity no proof -> 404; activity valid key -> 200; progress ungated -> 200; progress
`ready_url` carries `k`. Node/Python key parity confirmed byte-identical
(`21-royal-links-drive-robina` -> `AZLSMkAUjAwQ_Qfs` in both; `34-outrigger-drive-robina` ->
`gfsuP15r9fAt_ZQk` from the live endpoint and the local Python module).
`REPORT_LINK_SECRET` created on Netlify across all four contexts, functions scope,
`is_secret: true` (secret vars cannot use context "all" — see memory
`netlify_api_operations`). Also written to local `.env` for the generators; tracking server
restarted (it reads `.env` via `EnvironmentFile`).

**Known consequence, accepted:** report links previously shared WITHOUT `k` now 404. Only
71 documents carry a device token, and no mailer has been dispatched
(`print_appraisal.dispatched_at` null on all 243), so the practical blast radius is any
link Will sent by hand. Re-share with `k` — `shared/report_link.report_url(slug)` prints one.

**Files:** `netlify/functions/{db,property-report,property-report-activity,property-report-progress,minisite-visit,analyse-your-home-submit}.mjs`,
`src/utils/reportLink.ts`, `src/pages/YourHomePage/YourHomePage.tsx`,
`src/pages/YourHomePage/components/LiveMarketStatus.tsx`, `shared/report_link.py`,
`11_House_Mini_Site/_shared/mailer_v2/generate_mailers_v2.py`, `tracking-server/server.py`
**Recurrence:** Second entry today in this family; stage 1 was the surgical half.

## [WXB-RUNNER-OOM-UNPROJECTED-FETCH] Two Actions workflows OOM-killed by an unprojected Mongo fetch — 12:05 AEST
**Symptom:** `What Does X Buy — Generate & Publish` (run 32191810007, 2026-08-18) failed after
44m49s with exit code 137 and completely empty step logs, followed by
`The runner has received a shutdown signal`. Same signature on
`Watch This Sale — Generate & Publish` (run 31972705385, 2026-08-16). The runner shutdowns
also cancelled 4 further queued runs (Market Drivers, State of the Market, Market Shift,
Market Insight, How It Sold) on 16–17 Aug.

**Root cause:** `pipeline/suburb_stats.py::get_suburb_stats()` ran
`list(coll.find({"scraped_data.property_timeline": {"$exists": True, "$ne": []}}))` with
**no projection**. The loop below it reads only four field paths, but the query returned
*whole* property documents — including every enrichment blob (`property_images`,
`ollama_image_analysis`, `floor_plan_analysis`, `satellite_analysis`, `ai_analysis`).
Measured: **1,130 MB of BSON for robina** (7,398 docs), 807 MB varsity_lakes, 908 MB
burleigh_waters — materialised into a Python list, so several times that again in RAM.
The self-hosted runner service is capped at `MemoryMax=1500M`, so the kernel cgroup OOM
killer took it (`anon-rss:1446084kB`, dmesg `oom_memcg=...fields-vm-runner.service`).
Host memory was *not* exhausted (5 GB free) — this was purely the service cgroup ceiling,
which did its job. The 44-minute runtime was swap thrash as it climbed toward the cap; the
same fetch takes 33s unpressured.
Contributing: the workflows ran `python` without `-u`, so stdout was block-buffered off a
TTY and every diagnostic print was lost when the process was SIGKILLed — hence 44 minutes
of empty logs and no clue at the point of failure.

**Fix:** Added a projection for exactly the four paths the loop reads
(`scraped_data.property_timeline`, `scraped_data.features.property_type`,
`scraped_data.features.bedrooms`, `lot_size_sqm`). **1,130 MB → 7.6 MB (149×)**, fetch
33s → 1.8s, over the *identical* 7,398 documents. Verified equivalence by streaming both
the projected and unprojected cursors and SHA-256 comparing the extracted field tuples —
**byte-identical for all three suburbs**. End-to-end: peak RSS **1.38 GB → 111 MB**,
`get_suburb_stats` 44 min → 1.6s, and both `run_what_x_buys.py` and
`run_watch_this_sale.py` now dry-run to exit 0 under a real `MemoryMax=1500M` scope.
Also added `python -u` to all 12 workflow run steps so a future kill keeps its logs.
Deliberately did **not** raise `MemoryMax`: the cap correctly caught a genuine defect, and
this VM has a history of memory-driven lockouts.

**Files:** `pipeline/suburb_stats.py`, `.github/workflows/*.yml` (12 files) in
`Will954633/fields-automation` — commit `09a1b3bf`, all 13 md5-verified after push.

**Note:** the fix also covers `run_watch_this_sale.py` and `get_all_suburb_stats()`, which
call the same function.

**Retracted (same session):** I initially flagged `data/what_x_buys_rotation.json` as
possibly not persisting, because it held a single history entry dated 2026-03-04. That was
**wrong, and wrong for an instructive reason** — I read it out of `/home/fields/fields-automation`,
a clone pinned at `fad7ecc` and months stale. The real repo file has **16 entries**,
`last_updated: 2026-08-11T23:51:07Z`, and **17 commits** touch that path (last `de050e30`,
2026-08-11, 13s after the run — exactly the expected run→commit latency). The workflow does
stage it (`git add generated/what_x_buys/ data/what_x_buys_rotation.json`) and both the
success and SKIPPED paths write it correctly. No bug. Worth recording that the *predicted*
failure mode was right in kind: `_pick_next_combo` has no date-derived component, so had the
state genuinely frozen it would have returned `robina/1000000` **every week forever**, not
merely drifted.

**Latent risk this exposed (NOT yet actioned — needs Will's call):**
`/home/fields/fields-automation` is a stale clone whose `data/*.json` are **live state files
written by CI**. It already produced one false diagnosis in this session. Worse: if the
pipeline were ever run from that clone it would rebuild rotation state from March and push
it, *causing* the exact bug that was falsely suspected. Options are `git fetch && git reset
--hard origin/main`, or deleting the clone. Not done unprompted — it is not mine and it
carries local modifications (two `generated/market_monitor/*.json` still hold unresolved
March merge-conflict markers).

**Recurrence:** Nth in the unprojected-full-document-fetch family (prior entries
2026-03-07, 05-15, 05-17, 07-14, 07-17, 07-20, 07-31, 08-08). First time it has killed a
GitHub Actions runner rather than a pipeline step.

## [MAILER-QR-STRANDED-BY-GATE] Link-key gate broke every QR in the queued print batch — 21:05 AEST
**Symptom:** Immediately after `[REPORT-API-ENUMERATION-GATE]` shipped, all 50 mailer pieces
queued with Pronto (John, undispatched) carried QR codes that would land the recipient on
"Report not found". Confirmed live: the exact printed URL for
`1-bluejay-street-burleigh-waters` returned 404 from the report API.

**Root cause:** Mine, same day. `generate_mailers_v2.py` encodes the QR to point DIRECTLY at
`/your-home/<slug>?utm_...#market` — it does not route through the tracking server. When
gating the report endpoints I added key-stamping to the `/scan` redirect specifically to
rescue already-printed codes, verified that path, and assumed the mailer used it. It does
not. Decoded all 70 generated QR images: **63 decodable, 63 direct to /your-home, 0 via
/scan** — the rescue covered none of them. The failure mode is the one named in the warning
comment written into `generate_mailers_v2.py` hours earlier.

**Why it was caught:** Will asked why the pieces hadn't posted yet. Nothing in the system
would have surfaced it — the artwork is static, the gate tests all passed, and the first
signal would have been a homeowner scanning a dead code.

**Fix:**
- `generate_mailers_v2.py` now appends `&k={report_link_key(slug)}` (shared/report_link.py).
- Regenerated all 50 pieces for work orders `Fields_01.1` (38) + `Fields_02.1` (12).
- NEW `assemble_pronto_batch.py` — the numbered print-house batch was previously assembled
  BY HAND, which is why regenerating per-slug artwork left John's folder stale and silently
  disagreeing with `output/`. The script rebuilds it from the work orders and REFUSES to
  assemble unless every piece's QR decodes to a URL carrying the correct key for its slug
  (Rule 7b: the assertion is the deliverable, not the copy).

**Verification:** all 50 PDFs decoded (4 needed the standalone `qr.png` — an OpenCV detector
limit at PDF raster, not bad artwork) — 50/50 carry the correct slug and correct key. All 50
keys tested against production: **50/50 return HTTP 200**. New manifest diffed field-by-field
against the one John holds: **0 differences across 50 rows**, identical filenames — so it is a
drop-in replacement, artwork only. Batch at
`11_House_Mini_Site/_shared/mailer_v2/pronto_batch_2026-08-19/`.

**Follow-up (agreed, not done):** route future mailer QRs through a tracked redirect so codes
are both measurable and rescuable after printing. NOT the existing `/scan` — that is served
from `vm.fieldsestate.com.au` behind a Flask dev server with no redundancy, which is the wrong
host and wrong reliability profile for consumer print. Build the equivalent on the main
domain. Note the current batch therefore produces NO `qr_scan` events — attribution is
`utm_content` only, and only if the page loads.

**Files:** `11_House_Mini_Site/_shared/mailer_v2/generate_mailers_v2.py`,
`11_House_Mini_Site/_shared/mailer_v2/assemble_pronto_batch.py`
**Recurrence:** First. Directly caused by `[REPORT-API-ENUMERATION-GATE]` the same day.
