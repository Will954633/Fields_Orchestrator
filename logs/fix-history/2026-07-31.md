
## [DISCOVERY-DECK-PORT] Off-market Discovery deck → production React (P0+P1) + menu open-bug — 22:10 AEST
**Symptom:** The approved 10-card "Discovery" scroll deck (deterministic engine in `15_Off-Market/Page_Redesign_V2/`) only existed as a static Artifact prototype — nothing rendered it in the live React site. Separately, once ported, the new site-menu dropdown could never open (opened then instantly closed on the same click).
**Root cause:** (1) No production path: engine emitted markdown for review, not JSON, and no route consumed it. (2) Menu bug: the outside-click handler was a native `document` "click" listener that closed the menu; the button's React `onClick` used `e.stopPropagation()`, but synthetic stopPropagation does NOT stop the *native* event from bubbling to `document`, so the very click that opened the menu also reached the document listener and closed it (net: never opens).
**Fix:** P0 — `emit_json.py` (reuses assemble.py helpers, strips markdown, emits typed card dicts) + `offmarket_discovery_build.py` (build_one→emit_json + source_hash + generated_at, `cosmos_retry(lambda: ...)` upsert; flags --slug/--all-bundles/--no-rebuild/--delta) writing `system_monitor.offmarket_discovery` (11 test docs, unique index on slug). Contract validated: all 10 card types present, every nested sub-shape (funnel/comp/credibility/doorstep/filters/wait/driver-groups) matches types.ts. P1 — `discovery/{types.ts,discovery.module.css,DiscoveryDeck.tsx}` (brand tokens scoped to `.deck`, birch logo, IntersectionObserver reveal, progress rail, site-menu) + `getOffMarketDiscovery(slug)` in db.server.ts + loader branch in `off-market.$slug.tsx`: `?arm=discovery` preview override short-circuits BEFORE the redirect gate (real traffic never sets the param; production flag rollout is P2, downstream of the gate). Menu fix — replaced the blanket document listener with a `menuRef` containment check (close only on clicks *outside* the menu); removed the now-moot stopPropagation. Verified live via headless Chrome (desktop 1440 + mobile 390): cards 01/reveal/valuation/strategy all render correctly, menu opens with all 6 links, baseline no-arm still 301s. Two clean `react-router build`s. NOT deployed — awaiting Will's go for the single Netlify push (P2: on-demand builder + nightly --delta + PostHog flag).
**Files:** 15_Off-Market/Page_Redesign_V2/{emit_json.py,offmarket_discovery_build.py,SCOPE_React_Port.md} (new); Feilds_Website/01_Website/src/pages/OffMarketPage/discovery/{types.ts,discovery.module.css,DiscoveryDeck.tsx} (new), src/lib/db.server.ts, src/routes/off-market.$slug.tsx
**Recurrence:** First

## [OFFMARKET-HOUSE-MISCLASSIFICATION] v2 scrape relabelled houses as non-house → wrongly de-indexed — 16:05 AEST
**Symptom:** Will opened `/off-market/12-ballyliffen-court-robina` expecting the new deck; got the classic page. It's stored `property_type: Duplex` → excluded from both the Google index and the deck by the houses-only policy.
**Root cause:** The v2 Domain scrape (2026-05) mislabelled it "Duplex" and that value was promoted to the top-level `property_type`, overriding the ORIGINAL scrape (2025-11) AND the April recovery, which BOTH say "House". Physical facts confirm House (597 m² own lot, four whole-property sales, no unit fraction). Systemic: 206 core-suburb records were stored non-house while an original scrape said House + no `x/y` unit address (Apartment ×145 — 142 of which have their own lot, Duplex ×38, Townhouse ×13, etc.). 81→70 of these have sale history, so ~70 real houses were silently missing from the sitemap AND the deck (a live SEO leak).
**Fix:** New `fix_house_misclassification.py` (Will's rule: stored non-house + original scrape=House + no unit-fraction address → set `property_type: "House"`, keeping `property_type_correction` provenance {from,to,at,reason,trusted_source,rule}). Applied to 206 records (0 unit addresses touched). Ran the incremental deck builder → +70 newly-indexed houses built (0 failed), coverage 14,670. Regenerated + pushed the sitemap (off-market 15,584→15,654; commit 3bd2828). Verified LIVE: 12 Ballyliffen + 16 Banff Ct + 48 Manhattan Ave all render the DECK with `index,follow`. Pushed fix (f9befece).
**Follow-ups flagged (not done):** (1) DURABILITY — a future v2 re-scrape could re-promote the non-house label and revert the correction; the promotion step should respect an existing `property_type_correction` (root-cause fix, deferred). (2) NERANG GAP — the sitemap indexes ~1000 nerang expansion off-market URLs but the deck builder covers CORE suburbs only, so those render the classic page (deck≠index for nerang).
**Files:** 15_Off-Market/Page_Redesign_V2/fix_house_misclassification.py (new); Gold_Coast.{robina,varsity_lakes,burleigh_waters} (206 docs); public/sitemap.xml
**Recurrence:** First

## [DISCOVERY-DECK-FULLCOVERAGE] Deck coverage → FULL indexed off-market set (~14.6k), not demand-driven — 23:45 AEST
**Symptom:** After go-live, arbitrary off-market URLs still showed the classic page (e.g. Will opened `/off-market/113-mattocks-road-burleigh-waters` — an indexed house — and got the old design). Root ask: EVERY off-market URL indexed to Google (~15k) must render the new design, not just the 107 looked-up homes.
**Root cause:** I'd scoped coverage demand-driven (looked-up homes only) as a cost optimisation — wrong call for an indexed surface. Real off-market traffic is organic Google to indexed pages, so coverage must equal the indexed set.
**Fix:** Rewrote `offmarket_discovery_nightly.py` to target EXACTLY the sitemap's `getOffMarketUrls()` criteria (generate-sitemap.mjs) — the sale-history tier: `listing_status ∉ {for_sale,under_contract}`, non-waterfront, house `property_type`/`building_type` ($nin NON_HOUSE_TYPES), address not `\d+/\d+`, AND ($or) [never-listed with `enriched_data.transactions.0`] OR [sold ≥12mo with sale_price+sold_date]. Count = 14,587 across robina/varsity_lakes/burleigh_waters (matches Will's "~15k"). Builder is now INCREMENTAL + RESUMABLE: loads every existing doc's `generated_at` in one pass, builds a home only when it has no doc OR `enriched_data.last_enriched > generated_at` — first run backfills all (~4 hrs sequential ~1s/home), nightly after only touches new/changed. Each upsert makes THAT page live instantly (loader reads Cosmos live — no redeploy). Verified: built Mattocks → `fieldsestate.com.au/off-market/113-mattocks-road-burleigh-waters` immediately renders the deck. Added a `--shard i/N` option (N independent processes, each own Mongo client — no fork) and backfilled the whole set via **4 parallel shards at ~2.65 docs/s (~1 hr), 0 failures → 14,600 docs, to_build=0**. Spot-checked live: random indexed homes in all 3 suburbs render the deck. Cron 00:20 points at this script (incremental nightly). Units/dataless-stubs/waterfront are not indexed and correctly keep the classic fallback. NOTE: deck coverage now mirrors the sitemap 1:1 — if the sitemap's house/unit filter changes, update `NON_HOUSE_TYPES`/`UNIT_ADDR_RE` in BOTH generate-sitemap.mjs and offmarket_discovery_nightly.py.
**Files:** 15_Off-Market/Page_Redesign_V2/offmarket_discovery_nightly.py (rewrote demand-driven → full-indexed incremental)
**Recurrence:** First

## [DISCOVERY-DECK-GOLIVE] Off-Market Discovery deck LIVE in the normal flow — 23:05 AEST
**Symptom:** The deck rendered only under `?arm=discovery` preview; real off-market visitors still got the classic teaser page. Task: make it actually live.
**Root cause:** No normal-flow wiring, no coverage for real off-market homes, no refresh job.
**Fix:** (1) Loader (`off-market.$slug.tsx`) — AFTER the redirect gate and BEFORE the direct-test experiment, fetch `getOffMarketDiscovery(slug)`; if present return `{discovery, property}` → renders DiscoveryDeck, else falls through to the classic page (never a broken page). The `property` payload mirrors the classic path's sale-history derivation (enriched_data.transactions → doc.transactions → old-sold sale_price fallback) so `meta()` emits the SAME robots (index/noindex), title, description, canonical and JSON-LD — verified a sale-history home stays `index,follow` with Residence JSON-LD, a bare-cadastral home stays `noindex` exactly like the classic page. (2) Coverage is DEMAND-DRIVEN, not precompute-all (25.9k eligible core-suburb homes vs ~17 views/day): new `offmarket_discovery_nightly.py` builds a doc only for homes people actually look up — union of `crm_contacts.offmarket_home` (permanent owner-lookup signal) + `organic_journeys` off-market sessions — filtered to loader-eligible HOUSES (skips for_sale/under_contract/recent-sold, NON_HOUSE_TYPES, `\d+/\d+` unit addresses, and leading-double-number unit slugs). Backfilled 107/107 (0 failed, 114s). Cronned 00:20 daily (after the 23:40 brain2 demand-refresh chain), wrapped in `job_run(cadence_hours=24)` — heartbeat live on Process Registry. (3) Deployed website as a SINGLE git-tree commit (13b0855663 → one Netlify build, avoids rapid-PUT `published_at:null` bug); engine+pipeline backed up to orchestrator repo (b8e7a869a4). Verified LIVE: `fieldsestate.com.au/off-market/1-bilgola-place-robina` renders the deck, `index,follow`, 0 console errors.
**Files:** Feilds_Website/01_Website/src/routes/off-market.$slug.tsx, src/lib/db.server.ts, src/pages/OffMarketPage/discovery/* (deployed); 15_Off-Market/Page_Redesign_V2/offmarket_discovery_nightly.py (new), crontab
**Recurrence:** First

## [DISCOVERY-DECK-MENU-UX] Burger → top-right; Market Intelligence deep-links to subject suburb — 22:35 AEST
**Symptom:** Post-review polish on the Discovery deck menu: (1) the hamburger sat stacked under the logo (cramped left column, panel drifted toward the reading column); (2) the "Market Intelligence" dropdown item pointed at a hardcoded `/market-metrics/Robina` regardless of the subject home's suburb.
**Fix:** (1) Moved the burger to the top-right on the logo's centre-line (universal app-bar split): topbar `align-items: center`, new `.right` flex group holds the tagline + menu, `.menuPanel` now `right:0; left:auto` (opens right-aligned, clear of card text), tagline hidden `@media (max-width:560px)`. (2) Replaced the static NAV const with `buildNav(suburbDisplay)` — Market Intelligence → `/market-metrics/${suburb_display.replace(/\s+/g,"-")}/sell-now` (e.g. Varsity Lakes → `/market-metrics/Varsity-Lakes/sell-now`); the market-metrics route is `:suburb/:category?` and de-hyphenates the segment, and `sell-now` is a real category. Verified live (headless, desktop+mobile) on a Varsity Lakes home: burger top-right, panel right-aligned with all 6 links, Market Intelligence href = `.../market-metrics/Varsity-Lakes/sell-now`, tagline hidden on mobile. Clean `react-router build`.
**Files:** Feilds_Website/01_Website/src/pages/OffMarketPage/discovery/{DiscoveryDeck.tsx,discovery.module.css}
**Recurrence:** First

## [HOME-RECO-MODAL] "Is this your home?" recognition modal (personalization phase 3) — 09:40 AEST
**Symptom:** Returning visitors we already recognise (made a mini-site, googled their own /off-market address, etc.) still saw a cold address box on /analyse-your-home. No return-visitor recognition on the surface.
**Root cause:** Nothing consumed the my-home resolver client-side; the AYH page never checked who the visitor was.
**Fix:** New `HomeRecognitionModal` component + wiring. AYH page runs a once-per-session recognition effect (after phOnFlagsReady so distinct_id is restored): resolves /api/v1/my-home and, if a home is found, redirects to /your-home/:slug?welcome=1&home_addr=&home_src=. YourHomePage renders the modal over the (lightly, 4px) blurred mini-site ONLY when ?welcome=1 — never on a direct/bookmarked visit; Yes reveals the site + records a user_confirmed home via new endpoint my-home-confirm.mjs (upgrades them to `certain`); No → AddressSearch → normal analyse-your-home-submit claim flow. Guards: skips internal test devices, sessionStorage 'fields_home_reco' prevents re-nagging, viewing behind blur never claims ownership. Resolver reads home_confirmed as certain; home_confirmed added to crm_sync carry-forward. Will approved the design + reduced blur (8px→4px, scrim .45→.28) via an Artifact preview before deploy. Real react-router build passes; my 3 touched files have 0 tsc errors (YourHomePage's 4 errors are pre-existing). Shipped as a single Git-tree commit (3766426) to avoid multiple Netlify builds.
**Files:** Feilds_Website/01_Website/src/components/HomeRecognitionModal/{HomeRecognitionModal.tsx,.module.css} (new), netlify/functions/my-home-confirm.mjs (new), netlify/functions/my-home.mjs, src/pages/AnalyseYourHomePage/AnalyseYourHomePage.tsx, src/pages/YourHomePage/YourHomePage.tsx, scripts/crm_sync.py
**Recurrence:** First

## [OFFMARKET-HOME-SIGNAL] Permanent CRM record of google→offmarket owner-lookup (personalization phase 2) — 09:05 AEST
**Symptom:** The my-home resolver read the "google → /off-market/<addr>" owner-lookup signal LIVE from `organic_journeys`, which is rebuilt nightly on a rolling 60-day window — so any such visit older than 60 days would silently age out of the answer. Will's requirement is that this be recorded permanently in the CRM.
**Root cause:** No permanent snapshot of the signal existed; it lived only in the transient `organic_journeys` collection.
**Fix:** New nightly job `scripts/offmarket_home_signal.py` (wrapped in `job_run(cadence_hours=24)`) scans `organic_journeys` for is_offmarket + google-referred sessions, extracts the `/off-market/<slug>` address, and upserts a DEDICATED `crm_contacts.offmarket_home` sub-doc ({slug, address, slugs[], source, at}) keyed on `posthog_ids`. Dedicated field (not `probable_address`) so it never ping-pongs with minisite-visit.mjs which owns the primary probable_address. Added `offmarket_home` to crm_sync.py's replace_one carry-forward list (else the hourly full-doc replace would wipe it). Extended my-home.mjs resolver to read `offmarket_home.slug`/`.slugs` as medium_high (permanent path) alongside the live organic_journeys read. Cronned into the brain2 23:40 nightly chain right after organic_journey_build. Seeded once: 161 contacts flagged from 171 sessions; verified the cited example (019fb1bc… → 16-manakin-avenue-burleigh-waters) persisted; job heartbeat self-registered on Process Registry.
**Files:** scripts/offmarket_home_signal.py (new), scripts/crm_sync.py, Feilds_Website/01_Website/netlify/functions/my-home.mjs, crontab
**Recurrence:** First

## [MYHOME-RESOLVER] Reverse identity→home resolver endpoint (personalization phase 1) — 08:47 AEST
**Symptom:** No way to recognise a returning visitor and load "their" home. The signals (owned mini-site, google→/off-market view, probable_address) were all captured but only ever written to the CRM — never read back. `/analyse-your-home` always showed a cold address box.
**Root cause:** Every distinct_id path was write-only (client → crm_contacts upsert). No GET endpoint resolves distinct_id → known home.
**Fix:** New read-only `GET /api/v1/my-home?distinct_id=&device_token=` (`netlify/functions/my-home.mjs`). Ranks candidates by confidence — certain (owner.device_token) > high (owner.posthog_distinct_id) > medium_high (google-referred /off-market/<slug> view from organic_journeys) > medium (crm_contacts.probable_address_slug) > low (searched) — dedupes per slug, returns single best `home` + `candidates[]`. Address/slug only, no PII. Verified live against the exact example distinct_id Will cited (019fb1bc… → 16-manakin-avenue-burleigh-waters, medium_high/offmarket_google) plus a real owner (high) and unknown (null). Phase 1 of the "load your home into the Your Home tab" feature; UI (blur + confirm modal) is a later reviewed step.
**Files:** Feilds_Website/01_Website/netlify/functions/my-home.mjs (new)
**Recurrence:** First

## [OFFMARKET-CARD08-HUMAN-BUYER] Card 08 buyer was a data tag, not a person — 07:14 AEST
**Symptom:** Card 08 (The Buyer Reveal) read "data → buyer → data" — "The likely buyer: local family upgraders" + a stats sentence. Not believable.
**Root cause:** positioning_object.py:465 renders buyer as a crude binary ("local family upgraders" | "buyers who value this combination"); the harness surfaced that label verbatim and discarded the real primary_frame archetype.
**Fix:** Capture primary_frame (school_walk_family/land_lifestyle_family/turnkey_downsizer/beachside_lifestyle/renovator_valueadd/prestige_privacy/scarcity_play) into the bundle; Card 08 now renders a believable HUMAN PORTRAIT with a motivation per archetype, filled with this home's suburb + nearest school (data → human → motivation), then a "why your home fits them" line from the buyer drivers, then the emotional reframe. Also filter school/childcare drivers out of the fit line for downsizer/prestige buyers (a daycare walk isn't a believable draw for them). Deterministic, no LLM.
**Files:** 15_Off-Market/Page_Redesign_V2/{fact_bundle.py,assemble.py,copy.yaml}
**Recurrence:** First

## [OFFMARKET-FINISH-VISION-GATED] Finish/presentation claims gated on real GPT-vision data — 07:32 AEST
**Symptom:** Card 08 asserted finish/turnkey ("move straight into", "already finished", "holiday-ready finish") and Card 03 showed "premium finish throughout" — but these off-market homes have no current photos, so any finish read is inferred. Will: only reference finish where we actually have GPT-4 vision data for the property; otherwise omit.
**Root cause:** persona fit + portraits asserted finish unconditionally; my first fix over-corrected by removing it entirely.
**Fix:** Conditional per-property gating. fact_bundle carries renovation_quality_score (GPT-vision, 0-10). New _finish_signal(feat) returns 'premium' (qs>=9) / 'renovated' (renovation_level>=4 "fully_renovated") / None (no vision data OR score too low). Card 08 fit adds a finish phrase ONLY for downsizer/beachside/prestige personas when _finish_signal is truthy; families/renovators never get it. Portraits kept finish-neutral. Verified: 23 Bellbird (qs9.0)/8 Corina (qs9.2) show "done to a high standard"; 5 Comet (no vision data) shows NO finish reference anywhere; qs<9 homes make no claim. Card 03 "premium finish throughout" retained — already upstream-gated on qs>=9.
**Files:** 15_Off-Market/Page_Redesign_V2/{fact_bundle.py,assemble.py}
**Recurrence:** First

## [OFFMARKET-CARD09-ANCHOR] Card 09 wide ranges reframed with a likely-position anchor — 07:43 AEST
**Symptom:** Card 09 showed only a wide comparable range (e.g. $407k spread) with no central figure — reads as "you don't actually know", low perceived usefulness (Will).
**Fix:** Card 09 now leads with a rounded "Likely position: around $X million" anchor (midpoint to nearest $50k, spelled in millions, deliberately approximate), then the fuller "comparable range" beneath. Added a consultant line "An exact valuation for your home needs one of our property consultants to confirm it." (honesty + soft CTA). Anchor sits inside the valuation card (Valuation-Guide-tab equivalent) with "around" + disclaimer, mitigating the no-single-valuation-in-headlines rule.
**Files:** 15_Off-Market/Page_Redesign_V2/{copy.yaml,assemble.py}
**Recurrence:** First

## [NAV-RENAME-LABELS] Rename two main-nav tabs — 08:02 AEST
**Symptom:** Not a bug — requested copy change. "Analyse Your Home" and "Properties" nav labels needed renaming.
**Root cause:** N/A (product/copy request).
**Fix:** In SiteHeader.tsx changed nav label "Analyse Your Home" → "Your Home" (route /analyse-your-home unchanged) and "Properties" → "Homes for Sale" (route /for-sale-v3 unchanged). Only occurrences in the main desktop nav; no mobile-nav duplicate in this component. Pre-existing tsc errors in other components are unrelated; Vite build unaffected.
**Files:** Feilds_Website/01_Website/src/components/SiteHeader/SiteHeader.tsx
**Recurrence:** First

## [GITHUB-TOKEN-401] Stale .env GITHUB_TOKEN triggered false "AUTHERROR — 401 invalid token" Telegram alert — 08:03 AEST
**Symptom:** Telegram health alert "*GitHub*: AUTHERROR — 401 invalid token". Raised worry that pushes were broken.
**Root cause:** `scripts/api_health_monitor.py:probe_github()` probes the `GITHUB_TOKEN` in `.env`, which was a stale fine-grained PAT (returned 401). Actual pushes were never affected — they go through `gh api` using `GH_CONFIG_DIR=/home/projects/.config/gh`, whose token is valid (rate_limit HTTP 200, authed as Will954633). Two different GitHub credentials; only the `.env` one had gone dead.
**Fix:** Synced `.env` GITHUB_TOKEN to the working token from `gh auth token` (kept them consistent so both the monitor and any `.env`-sourcing script get a valid credential). Re-ran api_health_monitor → GitHub now OK (4999/5000 core left). Removed timestamped `.env.bak` (not gitignored, contained secrets).
**Files:** .env (not pushed — credentials).
**Recurrence:** First for this exact ID. Related recurring theme: `.env` GITHUB_TOKEN vs gh-config token divergence (memory: api_health_monitor).

## [OFFMARKET-CARD09-TIERED-VALUATION] Card 09 now degrades via mini-site's tiered method — 08:06 AEST
**Symptom:** Card 09 vanished for homes the full valuation engine excluded (e.g. 5 Comet: exclusion_reason missing_floor_area) — read only Tier-1 engine range.
**Fix:** V2 harness now reuses the mini-site's tiered working_valuation_range logic (property_reports/slot_resolver): instantiate SlotResolver, set _subject, call _engine_valuation_range() → valuation_exterior_range() (Tier 2 size-normalised comp dispersion) → _thin_valuation_range() (Tier 3 suburb median band). Deliberately SKIP Tier 1b (live on-demand engine run) — harness is read-only. Bundle valuation carries method/confidence/confidence_reason/point. Card 09 anchors on engine point estimate when present, and shows the tier's confidence caveat for non-engine tiers (e.g. 5 Comet → Tier 2 "exterior evidence only" disclaimer). Result: 5 Comet went from omitted → .52M-.15M Tier 2 with honest caveat; all 10 batch homes now have Card 09.
**Files:** 15_Off-Market/Page_Redesign_V2/{fact_bundle.py,assemble.py}
**Recurrence:** First

## [OFFMARKET-CARD06-ENRICH] Card 06 comparison enriched + floor-basis bug fixed + self-comp excluded — 09:41 AEST
**Symptom:** Card 06 "not comparable" case was thin (land + a floor delta). (1) Floor delta compared subject top-level floor (internal-living ~173) vs comp valuation floor (~213) — mismatched basis, wrong sign ("40m² more" when apples-to-apples was ~15m² less). (2) Some subjects compared against THEMSELVES (own prior sale sits in recent_sales at distance 0). (3) Under-using available comp data.
**Fix:** (1) _subject_features now resolves floor from valuation sp.floor_area_sqm first (same basis as comps). (2) _obvious_comp excludes comps whose id/address matches the subject. (3) Added build-year delta (approximate_build_year, ≥8yr), and a GREEN-BOUNDARY differentiator — fetches the comp's coords by id, runs green_space.classify, and adds "no bushland/golf/water/parkland behind it" ONLY when the subject has a green boundary the comp genuinely lacks (both often back onto reserves here → no false delta). Dropped the renovation/condition delta (comp lacks vision score; Will's rule). Verified across batch: 17 Placid now vs a real neighbour; 2 Longueville floor phantom gone.
**Files:** 15_Off-Market/Page_Redesign_V2/fact_bundle.py
**Recurrence:** First

## [OFFMARKET-INSIGHT-DELIGHTS] Research-backed "did you know" insight callouts — 10:03 AEST
**Symptom:** N/A (feature, Will). Wanted to delight the user with small, powerful teaching insights on relevant cards.
**Fix:** New insight library (copy.yaml `insights:`) of research-backed facts from Positioning Playbook v5.0 (2,153 sold + 14 papers, pulled from Drive to 12_Marketing/02_Drive_Research/). Each has a trigger + target card + priority + suburb-specific figures. _select_insights() fires only genuinely-relevant ones, capped ≤2/deck, ≤1/card, spread across cards; _insight_parts() renders a "> 💡" callout. Current set: water-views premium (+45%/+16%/+6% by suburb; gated to real frontage/views, not "steps from"), $/bedroom ($255K–607K), $/m² floor ($6,900–9,500), pool-is-a-marker-not-driver (1–4%). Data-not-advice; cites the research. Verified across batch.
**Files:** 15_Off-Market/Page_Redesign_V2/{copy.yaml,assemble.py}; research mirrored to 12_Marketing/02_Drive_Research/
**Recurrence:** First

## [BRAIN-MAX-RESTORE] All three brains dead on drained OpenRouter — routed back to Claude Max — 10:15 AEST
**Symptom:** Brain 1 (query/deep/normalize) and Brain 3 (nightly annotation) all failing. Brain 3's nightly cron was skip-storming `HTTP 402` on every batch ("This request requires more credits") and PAUSING with 34 batches un-annotated; Brain 1's query/synthesis path errored the same way. Brain 2 (data-only + its own Max-CLI annotators) was unaffected.
**Root cause:** On 2026-07-21 ([BRAIN-OPENROUTER]) all Brain 1/3 LLM calls were deliberately moved OFF the Max weekly budget onto the metered OpenRouter API via `scripts/samantha/openrouter_client.py`. OpenRouter has since run dry — `total_usage $13,748.62 > total_credits $13,748.48` (negative balance) → 402 on every call. Because the annotate loop caught the 402 as a generic per-batch error and *skipped*, the failure was a silent skip-storm, not a hard stop.
**Fix:** Will's direction — put the brains back on the Anthropic **Max subscription** (query tier = **Opus**, annotation = **Haiku**), Max-only with a clean pause (no metered fallback). New `scripts/samantha/max_client.py` — a drop-in replacement mirroring `openrouter_client.py`'s interface (`HAIKU`/`SONNET5`/`OPUS` constants + `call(prompt, model, timeout, max_tokens, retries)`), running `claude -p` as user `projects` (Max file-creds, `CLAUDECODE`/`ANTHROPIC_API_KEY` stripped from the child env so it bills Max and doesn't refuse as a nested session). `SONNET5` now aliases to `claude-opus-4-8` (query tier is Opus again per Will). On Max quota exhaustion `call()` raises `MaxQuotaExhausted` after a Telegram alert; `brain3_annotate.py`'s loop catches it and stops cleanly (resume next window) instead of skip-looping. Swapped `import openrouter_client as orc` → `import max_client as orc` in the 5 OpenRouter-backed scripts. Brain 1/2 annotators (`brain1_annotate.py`, `ad_annotate.py`, `ad_session_summarize.py`) already ran on Max via their own `claude -p` shim — left untouched.
**Verified:** Haiku + Opus both return "OK" through `max_client.py`. `brain1_query.py "win a listing without discounting"` returned real cross-corpus synthesis on Opus/Max citing both coaching (`u####`) and KB (`k####`) units, honouring editorial rules. `brain3_annotate.py --pool ops` resumed the 34 stuck batches on Haiku/Max, succeeding ~2 min/batch.
**Files:** scripts/samantha/max_client.py (new); scripts/samantha/{brain1_query,brain1_deep,brain1_normalize,brain3_annotate,kb_ingest}.py (import swap + quota-pause handling)
**Recurrence:** Nth — recurring OpenRouter/Anthropic credit-exhaustion class (see [BRAIN-OPENROUTER], [MINISITE-POSITIONING-402-OPENROUTER], [STEP117-OPENROUTER-CREDITS], [SATELLITE-STREETVIEW-OPENROUTER]). This closes it for the brains by making them Max-only with a visible pause.

## [OFFMARKET-DISCOVERY-PORT-P0] Discovery deck → render-ready JSON + Cosmos collection — 11:40 AEST
**Symptom:** N/A (feature build, Will). The Discovery deck engine lived only in the Python harness (markdown output); production React needs render-ready JSON per house.
**Fix:** P0 of the React port (SCOPE_React_Port.md). New emit_json.py — reuses ALL of assemble.py's logic (discovery engine, angle copy, insights, personas, tiered valuation) to emit typed card dicts matching the schema (copy stays in copy.yaml, React presentation-only); strips markdown, parses doorstep into {dist,name}, marks the reveal accent term. New offmarket_discovery_build.py — fact_bundle.build -> emit_json -> upsert system_monitor.offmarket_discovery (unique index on slug), with source_hash for --delta skip-unchanged and job_run-ready for the nightly. Deterministic (no LLM) so precompute is cheap. Seeded 11 test homes; lead-angle variety intact (parkland 5, beachside 2, water_adjacent/scale/school_walk/scarcity 1 each); delta-skip verified (built=0 skipped=11 on re-run); markdown fully stripped; all schema keys present.
**Files:** 15_Off-Market/Page_Redesign_V2/{emit_json.py,offmarket_discovery_build.py,SCOPE_React_Port.md}; Cosmos system_monitor.offmarket_discovery
**Recurrence:** First

## [BRAIN-DRIVE-INGEST] Google Drive ingestion with intelligent per-doc brain routing — 12:35 AEST
**Symptom:** The brains ingested VM files + system_monitor collections + the coaching corpus, but NOT the company's Google Drive knowledge (positioning research, academic papers, competitor analysis, seller-book drafts). Will: ingest all Drive data, and have the system intelligently decide which brain each file goes to (or exclude it).
**Root cause:** N/A — new capability.
**Fix:** New `scripts/samantha/drive_brain_ingest.py` — walks all shared Drive folders (Research, Seller_Book, Seller_Book_V2) via the non-expiring service account (`/home/fields/.gcp-floor-plan-vision.json`, NOT the 7-day OAuth MCP token), extracts text (Docs/Slides→text, Sheets→csv, PDF→pdftotext), and routes EACH document with a two-stage firewall (fail CLOSED): (1) HARD-PRIVATE regex on name/path (bank/tax/invoice/payslip/"draft appraisal"…) → exclude, no LLM; (2) Haiku-on-Max classifier → brain1 (external/public-safe published knowledge) / brain3 (internal Fields ops+strategy) / exclude (PII, financial junk), defaulting uncertain docs to brain3 (private) so nothing confidential can leak into the public-informing pool. Cached by content_hash (`doc_decisions.json`) + delta manifest (own namespace `/home/fields/brain_drive/`, per-brain tombstones) so re-runs are cheap. Coaching-transcripts folder deliberately skipped (already in Brain 1). Emitted units annotated by `brain3_annotate.py --pool b1|b3` (Haiku on Max), merged at graph-build: Brain 1 via new self-monitored `brain_drive_nightly.py` (cron 03:10; rebuilds coaching+KB+drive_b1), Brain 3 via `run_ops_nightly.sh` (cron 03:35, extended to `--merge annotations_b3.jsonl` + combined tombstones) — each brain has ONE rebuild owner.
**Verified:** First build classified 72 text docs → 18 Brain 1 (325 units: academic housing papers, competitor frameworks, marketing theory), 49 Brain 3 (249 units: Fields strategy/analysis/seller-book), 5 excluded (betting-P&L records + a client appraisal), 9 no-text (images). Both graphs rebuilt: Brain 1 = 6,400 units (+312 drive-external), Brain 3 = 3,133 units (+231 drive-internal). `brain_search` confirmed retrieval: Brain 1 surfaces "The effect of listing price strategy on real estate.pdf"; Brain 3 surfaces "Seller Core Competitive Advantages". `brain_drive_refresh` self-registered on Systems Health (success, cadence 24h). Depends on [BRAIN-MAX-RESTORE] (all LLM on Max).
**Files:** scripts/samantha/{drive_brain_ingest.py, brain_drive_nightly.py} (new); brain3_ops/run_ops_nightly.sh (VM-local, gitignored build dir); crontab (2 entries).
**Recurrence:** First

## [GIT-TOOMANYCHANGES] VS Code "too many active changes" warning — 15:42 AEST
**Symptom:** VS Code showed "The git repository ... has too many active changes, only a subset of Git features will be enabled."
**Root cause:** 19,011 untracked files in the working tree — mostly regenerable per-address data (14,600 fact bundles in `15_Off-Market/Page_Redesign_V2/bundles/`, 1,977 appraisal substantiation JSON exports in `09_Appraisals/Substantiation/`, generated deck output, and the local property-photo cache). This exceeded VS Code's git status limit.
**Fix:** Added .gitignore rules for the generated/regenerable data dirs (bundles/, Page_Redesign_V2/output/, Substantiation/, cache/). Generator scripts at parent level stay tracked. Untracked count 19,011 → 2,416. None of these dirs were ever tracked, so nothing was removed from history.
**Files:** .gitignore
**Recurrence:** First

## [AYH-LISTED-REDIRECT] Analyse-Your-Home: currently-listed addresses redirect to editorial page — 16:10 AEST
**Symptom:** People (buyers researching a listing; agents testing the portal) entered currently-listed addresses into "Analyse Your Home", spinning up bespoke house mini-site builds we can't monetise (can't be instructed to sell a home already on the market) and duplicating editorial we already publish. Example person 019e95d6-e473-7ebf-8efc-60f04574da2f created 3 stubs; 2 were live listings (819 Legend Trail $2.7M, 70 Burleigh Street $5.995M).
**Root cause:** analyse-your-home-submit.mjs created a property_reports stub for any service-area address with no check on listing_status; the poller then built it. No "is this on the market right now?" gate existed on Flow A (the analyse-property Flow B already had one).
**Fix:** (1) Backend submit endpoint: new findLiveListing() runs before any stub is created/returned — if the address has a Gold_Coast doc with listing_status:"for_sale" (querying for the for-sale doc specifically, since an address can have a cadastral twin + a separate for-sale twin), it returns {is_currently_listed, listed_property_id, redirect_url:/property/:id} and creates no stub. (2) Frontend: AnalyseYourHomePage shows a "This home is currently listed for sale" notice then redirects to /property/:id; HomeRecognitionModal + off-market ladderShared handle the same flag. (3) Defence-in-depth: build_property_report.resolve_one skips pre-existing stubs whose address is live-listed, flipping them to terminal state 'currently_listed'. Poller restarted.
**Files:** netlify/functions/analyse-your-home-submit.mjs; src/pages/AnalyseYourHomePage/AnalyseYourHomePage.tsx(+.module.css); src/components/HomeRecognitionModal/HomeRecognitionModal.tsx; src/pages/OffMarketPage/ladderShared.ts; scripts/property_reports/build_property_report.py.
**Recurrence:** First.

## [GEO-BLOCK-INSPECTIONTOOL] geo-block 403'd Google-InspectionTool / GoogleOther / AdsBot from US IPs — new pages stuck "Discovered, not indexed" — 16:55 AEST
**Symptom:** GSC URL Inspection "Test Live URL" on /property/24-tropicana-circuit-burleigh-waters (live 24 Jul) returned "URL is not available to Google — Blocked due to access forbidden (403)", crawled as "Google Inspection Tool smartphone". Page (and every page published after 21 Jul) stalled at "Discovered – currently not indexed", lastCrawlTime=None. Curl from the AU VM returned 200, so it looked technically fine.
**Root cause:** The `geo-block` Netlify edge function (deployed 2026-07-21 19:49 AEST, commit 89e53fd, [GEO-BLOCK-01]) returns 403 + a noindex page to non-AU/NZ traffic, exempting crawlers by UA — but the allowlist regex only matched `googlebot` (+ bing/social). Google's Search Console URL-Inspection / "Test Live URL" / "Request Indexing" tools fetch as `Google-InspectionTool`, and GoogleOther/AdsBot use their own UAs — none matched, so from Google's US IPs they hit the geo 403. Real Googlebot (the primary indexing crawler) IS exempt, so this blocked the inspection/request-indexing path and any non-`googlebot` Google fetcher; the last successful crawls (16 & 20 Jul, everything currently indexed) predate the geo-block. Curl from the AU VM never reproduced it because AU passes the geo check regardless of UA.
**Fix:** Broadened `BOT_USER_AGENT_PATTERN` to the full documented Google crawler family — google-inspectiontool, googleother, google-extended, storebot-google, adsbot-google, mediapartners-google, apis-google, feedfetcher-google, google-site-verification (+ msnbot) — verified real Chrome/Safari browser UAs still BLOCK (geo-block intact) and all crawler UAs ALLOW. Pushed commit 1579a45.
**Files:** Feilds_Website/01_Website/netlify/edge-functions/geo-block.js
**Recurrence:** First

## [SEO-OFFMARKET-SERP] Off-market pages indexed but weak SERP snippet + no headings — 17:30 AEST
**Symptom:** Will asked why some off-market houses rank page 1 (27 Huntingdale = #4) while others (25 Huntingdale, 16 Pymble Place) seem absent. GSC URL Inspection: all three "Submitted and indexed" — so it's ranking, not indexation. GSC Search Analytics: off-market is the #1 organic channel (8,911 impr / 242 clicks in 60d) but impression-weighted avg position = 6.8, with 94% of impressions at positions 4–10 (bottom of page 1, below the aggregator trio) and only 2% at 1–3. Two concrete on-page weaknesses found: (1) the Discovery deck rendered ZERO semantic heading tags — `<div className="h1/h2">`, so no `<h1>` and the address wasn't in a heading; (2) every off-market page shared an identical boilerplate meta description, so Google ignored it and auto-generated snippets that opened with a decades-old sale price (e.g. "$500,000. Jan 2006") next to portals showing a current estimate — underselling. Classic (deck-less) page also had run-together snippet text ("Off-marketRobina", "803 m² landHouse") from adjacent flex chips with no whitespace text node.
**Root cause:** Deck headings were styled divs (CSS classes named h1/h2). Meta description was a fixed template using only common fields. JSON-LD was minimal (single land PropertyValue). Flex `gap` spacing left no whitespace in textContent between chips.
**Fix:** (1) Deck: address → real `<h1>` (kept `.place` look via `font-weight:400` + full margin), section headlines → real `<h2>`. (2) Meta description now leads with the deck's per-address `reveal`-card hook (land/green-boundary/rarity — never a single valuation figure, per editorial rules); strips second-person "Your", falls back to physical facts then old boilerplate for deck-less pages; enriched the deck loader's `property` payload with land_size/floor_area so meta+JSON-LD have parity with the classic path. (3) JSON-LD: land + internal floor area + bathrooms as PropertyValues. (4) Classic header: `{" "}` text nodes between flex-item chips (discarded visually by flexbox, kept in textContent) so snippet reads "Off-market Robina" / "803 m² land House". vite build passed (exit 0); verified live — deck shows 1 h1 (address) + 6 h2, JSON-LD present, new per-address meta on 16 Pymble + 27; screenshot confirms no visual regression.
**Files:** Feilds_Website/01_Website/src/routes/off-market.$slug.tsx, src/pages/OffMarketPage/discovery/DiscoveryDeck.tsx, src/pages/OffMarketPage/discovery/discovery.module.css, src/pages/OffMarketPage/OffMarketPage.tsx (deployed single commit d20398d via Git Data API)
**Recurrence:** 2nd (SERP snippet quality — see [SEO-FALLBACK-META] for property pages; same class, off-market surface)
