
## [BRAIN1-PHASE2] Brain 1 concept-graph package + retrieval harness built — 10:40 AEST
**Symptom:** Phase 1 (Haiku annotation of the ~12.6M-token coaching corpus) completed overnight (309/309 batches, 3,071 units → `annotations.jsonl`, `COMPLETE` marker 10:15) but there was no Phase 2 — no graph, no way to query the intelligence layer.
**Root cause:** N/A (new build). Note: the durable annotate run wrote a clean `annotations.jsonl`, so the old "harvest from workflow journal.jsonl" path in the memory doc is obsolete — build straight from the JSONL.
**Fix:** Wrote two scripts.
- `brain1_graph.py` — deterministic package builder: normalizes concepts/entities, builds concept/question/channel indexes, typed edges (from `relationships[]`, weighted+deduped), co-occurrence edges (pruned to weight>=2). Output `package.json` = 3,071 units, 30,437 concepts, 11,650 typed edges, 345 cooccur edges, 14,120 questions, 277 channels, **176 AVOIDED-channel signals**. 9.0 MB ~= **2.24M tokens** -> too big for Opus 1M whole.
- `brain1_query.py` — retrieval + reasoning harness: (1) deterministic lexical pre-filter scores every unit by query-term overlap on concepts/topics/asks/quotes; (2) 1-hop typed-edge graph expansion; (3) feeds shortlist to Opus on Max (`claude -p`, env-stripped like annotate). general/insight modes. Validated live: "win listings without discounting" -> 7 method families, cross-course, verbatim-cited, no hallucination, ~17k-token prompt.
**Known limitation:** graph expansion is weak (concepts are near-unique strings -> typed-edge endpoints rarely string-match unit concepts). Lexical retrieval carries recall. Real fix = concept normalization, which needs embeddings (deferred per Will's 100%-Anthropic-Max directive).
**Files:** `scripts/samantha/brain1_graph.py` (new, pushed), `scripts/samantha/brain1_query.py` (new, pushed). Build artifacts (not pushed, regenerable): `/home/fields/brain1_build/package.json`, `graph_stats.json`.
**Recurrence:** First.

## [BRAIN1-PHASE2B] Concept normalization (Sonnet) + insight-mode validation — 11:20 AEST
**Symptom:** Brain 1 graph expansion was weak — 30,437 near-unique concept strings meant typed/cooccur edge endpoints rarely matched (only ~1 graph neighbour per query). Needed concept normalization to make the graph contribute.
**Root cause:** No concept canonicalization; without embeddings (deferred per all-Anthropic-Max directive), raw concept phrasings don't collapse.
**Fix:** (1) Ran `insight` mode live → 7 novel bridged client-acquisition plays, each citing real cross-course unit ids (e.g. Sold-Radius-Autopilot = u0520×u2570×u0399), zero hallucination — validates the insight path. (2) Empirically compared Sonnet vs Haiku for the normalization pass on an identical 90-concept batch (`norm_compare.py`): Sonnet found more true merges (20 vs 13 clusters), higher compression (0.378 vs 0.311), finer correct distinctions, AVOIDED Haiku's over-merges (over-merge = false graph edges), and was faster here (134s vs 189s). Chose **Sonnet** — one-time judgment-heavy build, quality>speed (opposite tradeoff to annotation where Haiku's volume win was right). (3) Wrote `brain1_normalize.py` — durable/resumable (lockfile+done_batches+cron-safe, mirrors annotate) Sonnet pass1 (raw→canonical) + `--consolidate` pass2 (dedup the label vocab to fix cross-batch drift) → `concept_canonical.json`. (4) Wired `brain1_graph.py` to apply `concept_canonical.json` when present (collapses concepts + typed-edge endpoints + cooccur before building).
**Files:** `scripts/samantha/brain1_normalize.py` (new, pushed), `scripts/samantha/brain1_graph.py` (updated, pushed), `/home/fields/brain1_build/norm_compare.py` + `norm_compare_out.json` (comparison evidence). Full pass = 254 batches ≈ 10.6h (not launched — awaiting Will's go).
**Recurrence:** First.

## [STEP113-STARVED] Withdrawn detection never reaching Burleigh Waters — 08:25 AEST
**Symptom:** Will flagged 12 Beaconsfield Drive, Burleigh Waters showing as for-sale on /for-sale-v3 despite being withdrawn from Domain. Record still `listing_status: for_sale` (308 days on Domain, price collapsed to "Contact Agent" in April, no content change since ~2026-04-27).
**Root cause:** Step 113 (`detect_withdrawn.py`) processes suburbs strictly sequentially (robina → varsity_lakes → burleigh_waters) under a hard 40-min runtime budget (WITHDRAWN_MAX_RUNTIME_MIN, added 2026-06-15). Robina now has ~95-98 for-sale listings; at up to 5 retries × 60s each on flaky BrightData, Robina alone consumes the entire budget. Last 6 runs: burleigh_waters reached 0/6 times, varsity_lakes 2/6. Runs `--no-fail`, so early budget-abort still reported success — the gap was silent. So any Burleigh Waters / Varsity Lakes withdrawal was structurally invisible.
**Fix:** (1) Manually flipped 12 Beaconsfield to `withdrawn` (detection_method=manual_confirmed_by_operator). (2) Rewrote the detection loop to interleave listings **round-robin** across all suburbs instead of sequential — burleigh_waters now checked from position 2, coverage proportional under any budget cap (verified against live DB: 60-listing cap → 20/20/20 each). Added per-suburb coverage line to RESULTS + Telegram alert (via telegram_notify.send_message) on early abort so a silent budget-starve can't recur.
**Files:** scripts/detect_withdrawn.py; DB Gold_Coast.burleigh_waters (12 Beaconsfield doc). Pushed commit 35b7f8b.
**Recurrence:** Nth — recurring theme (prior: [SCRAPE-WITHDRAWN] silently broken, [STEP113-RUNTIME] 6h grind, [WATCHDOG] step 113 x3). Root cause differs each time; this one is budget-vs-suburb-order starvation.

## [FB-AD-WRONG-LP] 'Tailored v4b 02' ad pointed to wrong landing page — 07:38 AEST
**Symptom:** Live FB ad "Tailored: Buyer Landing page test. v4b 02" was sending paid traffic to /for-sale-v4b; should have been /for-sale-v3. Will flagged it and asked to check all ads.
**Root cause:** The ad's underlying page-post CTA link was set to /for-sale-v4b (a valid but wrong landing-page variant). Not a broken link — wrong destination.
**Fix:** FB creatives are immutable, so uploaded the same image, built a new link_data creative with identical copy/CTA but link=/for-sale-v3, and swapped it onto ad 120245339779970134 (old creative 1048083131087964 → new 3397551727092734). Ad re-entered IN_PROCESS review. Audited all 89 ads: this was the ONLY active ad with a wrong destination; the sibling "Video Format" active ad already pointed to v3. 4 paused v4b buyer-landing ads also point to v4b (left as-is per Will's choice).
**Files:** No repo files (FB Graph API only). Logged to system_monitor.ad_decisions.
**Recurrence:** First

## [BRAIN2-ADFMT] Ad creative format misclassification — 08:53 AEST
**Symptom:** 50 of 92 ads in `ad_profiles` had wrong `creative.format` — videos stored as single_image, dynamic-creative ads stored as "catalog". Made "video vs non-video" / "carousel vs non-carousel" analysis impossible. Also, post-based (video) ads had no ad copy captured at all.
**Root cause:** `fb-metrics-collector.py` requested `creative{...object_story_spec}` only. For ads built from an existing page post or Advantage+/dynamic (`asset_feed_spec`) creative, `object_story_spec` is empty → format detection fell through to single_image, and the `is_catalog_ad` heuristic (empty body + page_id) fired wrongly → "catalog". Copy for post-based ads lives in the page post (`effective_object_story_id`), never fetched.
**Fix:** New `scripts/brain2/ad_creative_enrich.py` (Brain 2 Layer 1). Re-pulls every ad's creative with authoritative fields (object_type, video_id, asset_feed_spec{ad_formats,videos,images,bodies,titles,...}, object_story_spec). Correct format truth table: video (video_id/asset_feed videos/object_type VIDEO) → carousel (ad_formats CAROUSEL / child_attachments) → single_image; `dynamic_creative` flag for AUTOMATIC_FORMAT / multi-image (NOT a user-facing carousel — my first heuristic over-counted carousel 55 vs true 2). Captures ALL text variants + CTA. Page-token fallback recovers copy from the page post for post-based ads (22 ads). Writes `creative_structured` block to ad_profiles. True dist: 76 single_image, 14 video, 2 carousel; body recovered for 91/92. Nightly cron 23:30 re-corrects after the collector.
**Files:** scripts/brain2/ad_creative_enrich.py (new), crontab (+23:30 nightly)
**Recurrence:** First

## [BRAIN2-L3] Downstream ad attribution refresh — 09:14 AEST
**Symptom:** `ad_attribution` only covered 10 ads, stale since 18 March. No way to trace ads to downstream website behaviour / address entries.
**Root cause:** No refresh job; also HogQL default row cap (100) silently truncated the session set (first build found 18 sessions vs true 467).
**Fix:** New `scripts/brain2/ad_attribution_build.py` (Layer 3). Resolves session→ad via exact utm_content(ad-id) match, pulls the full PostHog event stream (explicit LIMIT to defeat the 100-row cap), aggregates per ad: sessions, unique visitors, converters (address entries), entry/top pages, content_consumed (article_view/section_marker/property_view/address_search counts), page-before-conversion, funnel. Confidence-labelled: 32 exact + 18 campaign-fallback (matched on campaign_id or decoded campaign_name, shared_across flagged) = 50/92 covered; 42 never tagged → honestly unattributed. Writes `system_monitor.ad_downstream`. Only 2 ads have ever produced an address entry (top: "Who buys $1,550,000…" 7 conv/92 sess) — this CONTRADICTS the old memory note calling that ad a 0%-conversion dead-end; data has moved.
**Files:** scripts/brain2/ad_attribution_build.py (new), scripts/brain2/BACKLOG.md
**Recurrence:** First

## [BRAIN2-L4] Granular session behaviour + own session summaries — 09:50 AEST
**Symptom:** No per-session view of what ad-driven visitors actually consumed (which articles, scroll depth, sections, properties). Will asked for PostHog AI session summaries via API.
**Root cause / finding:** PostHog's AI-summary API is inaccessible with our auth — the `/session_summaries` endpoint Google suggested is a hallucination (404); the real one, `POST /session_recordings/{id}/summarize/`, returns 403 "This action does not support personal API key access" (needs browser-cookie auth). Heatmap API returns empty. So neither is usable programmatically.
**Fix:** Built Layer 4 from the raw event stream instead. 4a `ad_behaviour_build.py` → `ad_session_behaviour` (per session: articles_read w/ max_scroll_pct, sections_viewed marker_ids, properties_viewed, cards_seen+classification, rageclicks, dwell, conversion) + `ad_content_affinity` (per-ad rollup: what its traffic reads, avg scroll, avg dwell). 449 sessions, 32 ads. 4b `ad_session_summarize.py` → `ad_session_summaries`: Opus 4.8 --effort high generates OUR OWN session summaries (narrative, intent_read, content_that_resonated, friction_signals, why_converted, ad_to_landing_fit) — funnel-tuned, superior to PostHog's generic ones. 95 notable sessions (converters + engaged).
**Files:** scripts/brain2/ad_behaviour_build.py, ad_session_summarize.py (new)
**Recurrence:** First

## [BRAIN2-POSTHOG-RESEARCH] Exhaustive PostHog capability research — 10:08 AEST
**Symptom:** Needed to know full PostHog capability surface (session data, "signal endpoint", AI summaries) + whether the new all-access key unlocks more.
**Findings:** (1) "Signal endpoint" doesn't exist → it's Alerts (threshold, API-creatable) or realtime webhook destinations (per-event push, e.g. Telegram on address entry). (2) AI session summaries UNREACHABLE with any API key (403 structural, browser-cookie only) → our Opus Layer 4b supersedes. (3) All-access key (phx_) unlocks rrweb snapshot export, native sessions table, Funnels/Paths/Actors query kinds, alerts, surveys, experiments write. (4) Heatmaps return 0 = capture OFF (needs capture_heatmaps:true, forward-only). (5) PostHog MCP = hosted HTTP, add to .mcp.json read-only. Full reference: scripts/brain2/POSTHOG_CAPABILITIES.md.
**Files:** scripts/brain2/POSTHOG_CAPABILITIES.md (new)
**Recurrence:** First

## [POSTHOG-ACTIVATE] Enabled heatmaps, error tracking, Telegram signal, MCP — 10:18 AEST
**What:** Actioned the top PostHog capability findings.
**Changes:**
1. Website src/root.tsx: added `capture_heatmaps:true` + `capture_exceptions:true` to posthog.init (commit 7f6711e). Heatmap + JS-exception capture now on (forward-only). Deployed via Netlify.
2. PostHog CDP destination "Telegram: address entry (analyse-your-home)" (hog_function id 019f6849-41ee-0000-3922-f6318e8e7c65) — LIVE, fires on `analyse_home_submit_success` → Telegram to Will (TELEGRAM_CHAT_ID). Created disabled via /hog_functions API (template-webhook), Telegram delivery verified by direct test, then enabled. This is the "signal endpoint" Will asked about.
3. .mcp.json: added PostHog MCP (hosted http, readonly, features=flags,insights,dashboards,error_tracking,experiments) using ${POSTHOG_ALL_ACCESS_KEY}; key exported in ~/.bashrc. Activates on next Claude Code restart.
**Files:** Feilds_Website/01_Website/src/root.tsx, .mcp.json, ~/.bashrc, .env (POSTHOG_ALL_ACCESS_KEY)
**Recurrence:** First

## [POSTHOG-SURVEYS] Buy-vs-sell + exit-intent surveys live — 10:28 AEST
**What:** Stood up PostHog surveys (Brain2 #5) — first surveys on the site (had 0).
**Changes:**
1. Survey "Buy vs Sell intent" (id 019f6850-011d-0000-12ba-9d8ec6721838) — popover, single-choice "what brings you to Fields today?" (buy/sell/researching/own-and-track), targets url icontains /analyse-your-home. **ACTIVE.**
2. Survey "Exit-intent — analyse-your-home abandon" (id 019f6851-9fde-0000-f15c-39fedfa9e901) — popover single-choice "what stopped you?", triggers on custom event `analyse_abandoned`. **ACTIVE.**
3. Frontend hook: AnalyseYourHomePage.tsx now emits `analyse_abandoned` on mouse-leave-via-top (desktop exit-intent) if no address selected (commit 529969b, typecheck clean). Fires while user still on page so survey renders. Desktop-only by nature.
**Verify (which live event fires):** current analyse flow uses analyse_home_* events (v3/v2 DEAD since 2026-04-27). Telegram webhook correctly on analyse_home_submit_success.
**Files:** Feilds_Website/01_Website/src/pages/AnalyseYourHomePage/AnalyseYourHomePage.tsx
**Recurrence:** First

## [BRAIN2-L6] Brain 2 deepening — native sessions + replay metadata + server-side funnels — 10:42 AEST
**What:** Deepened Brain 2 with PostHog's richer native data (all-access key).
**Changes:** (a) ad_behaviour_build.py now merges the native `sessions` table (real $session_duration, $is_bounce, $entry/$exit_pathname, $channel_type) — top converter real avg 492s / 32% bounce, vs event-approx 349s. (b) merges `session_replay_events` metadata (surfacing_score, click_count, keypress_count, active_milliseconds, console_error_count) — 330/450 sessions; converter = 271s real active time. Column gotchas: sessions id col = `session_id` (not $session_id); replay view uses `surfacing_score` not activity_score, timestamp not min_first_timestamp. (c) new ad_journey.py — server-side FunnelsQuery + PathsQuery via Query API (funnel/paths-to/paths-from). **Stage C finding:** rrweb snapshot BLOBS expire faster than the aggregate replay metadata (retroactive 404s) → full DOM replay best captured FORWARD via a rolling job; replay metadata (reliably available) merged instead.
**Files:** scripts/brain2/ad_behaviour_build.py, scripts/brain2/ad_journey.py (new)
**Recurrence:** First

## [BRAIN2-L5] Full all-channel attribution (fix ads-only blind spot) — 10:50 AEST
**Symptom:** ad_behaviour_build.py keeps ONLY sessions mapping to a known FB ad → every organic/direct/referral lead invisible. A "who engaged this week" review saw 1 conversion when there were really 4-5 across channels; the best lead (Bing→neighbour-listing→self-valuation) was structurally missed.
**Root cause:** ads-only store keyed on utm_content(ad id); non-paid traffic dropped.
**Fix:** New scripts/brain2/organic_journey_build.py (Layer 5). Keys on PostHog native $channel_type + $entry_pathname instead of ad ids. Writes: `organic_journeys` (non-paid notable sessions, full journey), `all_conversions` (EVERY address submit across EVERY channel — the register any who-engaged review MUST query), `organic_landing_affinity` (per entry URL). Auto-detects **neighbour_sale_trigger** (landed on /property/<addr> then valued a different house on the same street — verified live on the 16/18 Collingwood Ave Robina lead) and valued_the_listing_itself. Flags contact_captured (all recent = no-contact → confirms contact-capture is the real #1 gap). Nightly 23:40 refresh chain (also added attribution+behaviour builds which weren't scheduled before).
**Files:** scripts/brain2/organic_journey_build.py (new), crontab, BACKLOG.md
**Recurrence:** First

## [BRAIN2-INTERNAL+SEO] Internal-ID exclusion + GSC live + Bing verify — 11:14 AEST
**What:** (1) ID'd internal users so Will stops showing as a lead. New `system_monitor.internal_identities` (seeded Will's distinct_id 019d24b3…); organic_journey_build.py now auto-flags any distinct_id that hit /ops (365d window) + excludes all internal from `all_conversions`/`organic_journeys`. all_conversions 4→3 genuine leads (dropped 135 Mattocks = Will). (2) GSC access granted (SA floor-plan-processor@ added) → seo_landing_performance.py LIVE, wrote 99 google (page,query) rows; ranks #2.1 "fields real estate". (3) Bing verification meta tag `msvalidate.01` added to src/root.tsx <head> (commit c616329) — Will to click Verify + generate API key → BING_WEBMASTER_API_KEY. Collingwood query is Bing-sourced so needs Bing to recover.
**Files:** scripts/brain2/organic_journey_build.py, Feilds_Website/01_Website/src/root.tsx
**Recurrence:** First

## [BRAIN2-TIMELINE] Permanent timestamped event timelines — 11:30 AEST
**What:** Store full date-time event sequences in Brain 2 so signal survives PostHog's ~30-90d retention (Will: "we must store all date time events, critical to generating signal").
**Changes:** (a) ad_session_behaviour + organic_journeys docs now carry `timeline: [{t,e,path,property_id,address,...}]` — the compact, millisecond-timestamped per-session event sequence. (b) all_conversions (leads register) stores the FULL CROSS-SESSION person timeline (via person_timeline(distinct_id) — all sessions, not just the converting one) so the entry point is captured (Collingwood: 64 events from the 00:24 Bing entry). Added properties.address to ad_behaviour event pull. Timelines capped 3000/session, 5000/person.
**Files:** scripts/brain2/ad_behaviour_build.py, scripts/brain2/organic_journey_build.py
**Recurrence:** First

## [BRAIN2-AI-SIGNAL] LLM/GPT referral signal surfaced — 11:49 AEST
**Symptom:** GPT traffic existed but Brain 2 wasn't surfacing it as a signal — ChatGPT tags outbound links with utm_source=chatgpt.com (not a referrer), so PostHog labelled it channel=Direct and it was invisible.
**Fix:** organic_journey_build.py now detects AI assistants (chatgpt.com/openai/copilot/perplexity/gemini/claude via utm_source or referring_domain), tags `ai_source` on organic_journeys docs, and writes a dedicated `ai_referral_signal` collection (per source+page, all-time 400d). **Findings:** ChatGPT 15 sess (26 Apr→9 Jun) + Copilot 5 sess (12 Jun→15 Jul, ONGOING). Top-cited pages: /articles/how-accurate-is-domain-property-estimate (6, ChatGPT), /market-metrics/{Gold-Coast,Robina}/overview. 0-1 conversions (top-of-funnel authority). = GEO/AI-SEO working organically: LLMs cite our data-driven market-metrics + Domain-accuracy content as authoritative.
**Files:** scripts/brain2/organic_journey_build.py
**Recurrence:** First

## [BRAIN2-FUNNEL-REGIME] Label address-entry data by product regime (contact-PDF vs address-only mini-site) — 12:15 AEST
**Symptom:** Signal C ("/analyse-your-home leaks ~84% of high-intent traffic") conflated two different products. Before 2026-07-01 the funnel REQUIRED phone/email to receive a PDF report (high friction → drop-off expected); since 2026-07-01 it is address-only into the automated mini-site (low friction). Drop-off across the boundary is not comparable.
**Root cause:** Brain 2 collections carried no notion of the funnel regime, so pre-cutover hesitation was read as current-product friction.
**Fix:** Added `FUNNEL_REGIME_CUTOVER = 2026-07-01 00:00 AEST` + `funnel_regime(ts)` helper in organic_journey_build.py (single source of truth); label written at build-time onto `organic_journeys`, `all_conversions`, and `ad_session_behaviour` (imported into ad_behaviour_build.py). Backfilled all existing docs. Recorded the change durably in new `system_monitor.brain2_funnel_regimes` collection (2 regime records with date bounds + friction + description).
**Corrected Signal C (organic, by regime):** address_only_minisite (current) reached=19 progressed=1; contact_required_pdf (old) reached=6 progressed=1. The 84% figure mixed regimes and must not be used — current-product drop-off has only 19 organic sessions so far (inconclusive). Of the 3 retained conversions, Varsity Lakes (18 Silvabank, 2026-06-16) was OLD regime; the two July conversions (Collingwood, Reedy Creek) are current regime.
**Files:** scripts/brain2/organic_journey_build.py, scripts/brain2/ad_behaviour_build.py, + system_monitor.brain2_funnel_regimes (new collection), backfill of organic_journeys/all_conversions/ad_session_behaviour.
**Recurrence:** First

## [BRAIN2-AI-CONV] ai_source on all_conversions register — 11:58 AEST
**What:** Added `ai_source` (ChatGPT/Copilot/Perplexity/Gemini/Claude) to every all_conversions doc so an AI-referred lead stands out instantly. Currently all None (no AI lead yet); wired for the first one. Complements ai_referral_signal (per-page citation rollup).
**Files:** scripts/brain2/organic_journey_build.py
**Recurrence:** First

## [APPR-V4-SILVABANK] Wrong hero photo + "13 Terrace Court" leak on 18 Silvabank Drive appraisal — 12:15 AEST
**Symptom:** The V4 appraisal for lead 18 Silvabank Drive, Varsity Lakes (pipeline 6a31647e9b0cfc5a2401b000) — due to be mailed as a hard copy — showed the generic 13 Terrace Court cover hero (a different house), and pages 6/8/14 (inherited thesis/recommendation pages) still read "13 Terrace Court · Merrimac QLD 4226" instead of the subject.
**Root cause:** The subject is a cadastral / analyse-your-home record. Two field-name mismatches:
1. **Hero:** `generate_appraisal_v4.py` auto-fetch only tried Domain-CDN URLs from `scraped_data_apr01_recovered.images` / `scraped_data.images`. This listing was scraped months ago — every Domain rimh2/bucket-api URL now 404s, and `scraped_data` held a dead Azure blob URL (skipped). It never tried the local on-disk Street View / cadastral images that exist for the subject, so it fell to the generic 13TC placeholder. (Secondary: `to_bucket_api_url` mangles Domain URLs whose tail is a nested `http://static.domain.com.au/...` link → `.../image/http:`, so even a live Domain URL would fail — would break every live-listing hero.)
2. **Address text:** the post-splice 13TC→subject substitution (and `render._short_address` / cover title) read `street_address`/`suburb`/`postcode`, all `None` on this record (it carries `address`, `LOCALITY`, `POSTCODE`/`display_postcode`). So the substitution silently no-opped and "13 Terrace Court / Merrimac / 4226" leaked; the cover also stacked the whole address ("18 Silvabank Drive Varsity Lakes Qld 4227").
**Fix:**
- `generate_appraisal_v4.py`: hero auto-fetch now (a) tries the bucket-api rewrite **and** the original URL per candidate, and (b) after URL candidates fail, uses local on-disk subject images — Street View `front.jpg` first (the actual subject house; correct hero for a homeowner report with no listing photo), then cadastral photos — before the generic 13TC fallback. New photo-source labels `local_street_view` / `local_cadastral`. Made the address/suburb/postcode substitution derive the street portion from `address`/`complete_address` and fall back to `LOCALITY`/`POSTCODE`.
- `appraisal_template/render.py`: `_short_address()` now returns the street portion only for records lacking `street_address`; cover suburb/postcode fall back to `LOCALITY`/`POSTCODE`.
- Regenerated with `--strict-photos` (passed): `cover_hero=local_street_view`, 0 Terrace refs, cover reads "18 Silvabank Drive / Varsity Lakes, QLD 4227" with the real house. Pipeline record report_path_v4 updated to `690bd7eb8b8f546592606ed5_20260716T120628.pdf`, stage `draft_ready`. Visually verified cover + pages 6/8/14. The 3 residual "Merrimac" refs are the legitimate multi-suburb catchment list, left as-is.
**Files:** scripts/generate_appraisal_v4.py, scripts/appraisal_template/render.py (both pushed: 0d8332b, 2bda3c9).
**Recurrence:** Nth — same class as [APPR-V4-PHOTOS-AUTO-01] (per-subject hero auto-fetch) and [APPR-COVER-001] (hero thumbnail). Recurring theme: subject-photo/address sourcing assumes for-sale-listing field shapes and breaks on cadastral/analyse-your-home records.

## [APPR-V4-SCARCITY-SYNC] §01 scarcity page reads mini-site combination intelligence — 12:42 AEST
**Symptom:** The appraisal PDF's §01 "Why this home is hard to replace" page showed a weak single-attribute cohort claim (for 18 Silvabank Drive: "only 240 of 473 sold had four bedrooms — pool") — 240/473 is ~51%, i.e. the majority, which argued *against* the "hard to replace" headline and contradicted the page's own "the strongest position is the combination" close. Meanwhile the mini-site (`fieldsestate.com.au/your-home/...`, built later with deeper tech) had already uncovered far richer, more compelling data the PDF ignored.
**Root cause:** The PDF §01 used only the standalone `pick_highlight` ranker (single/pair sold-cohort rarity). It never read the mini-site's `system_monitor.property_reports` doc, which stores: the combination "full-stack" scarcity receipt (`scarcity_features` / `positioning_object.scarcity_receipt` — e.g. 40 of 202 active listings match), per-feature like-for-like sold premiums (`cohort_premiums`), and measured walk-distance POIs (`pois`).
**Fix:** `render_section_01_right_html` now loads the linked report doc (via `pipeline_record.property_reports_slug`, wired through `generate_appraisal_v4.py`; falls back to address-derived slug) and, when present, renders an elegant synopsis of it: (1) combination sentence — "Of the 202 comparable homes for sale … only 40 share this same combination — single-level living, 796 m² of land and a pool"; (2) a 3-row premium ladder — "what each has carried in comparable sales" (Pool +12.6%, Single-level +7.8%, Large block +3.8%), using like-for-like reliable %s only (no raw gaps, no median $ figures per editorial rules); (3) a proximity line (217 m to childcare, 869 m to shops) filtered by the report's `forbidden_claims` (never presents the beach as walkable) and a 1.2 km walk cap; (4) the dot grid re-pointed to the combination match (202 FOR SALE · 40 MATCH — a striking minority vs the old ~half). Combination-led subhead. When no report exists, §01 falls back to the existing ranker unchanged (verified: real slug→intel, bogus slug→None, no-report→None). Substantiation record gains a `report_intelligence` provenance block. `--strict-photos` render passed, 0 overflow (fit-check auto-applied the compact variant), visually verified page 5.
**Files:** scripts/appraisal_template/render.py, scripts/generate_appraisal_v4.py (pushed: ed2fbac, 8452a18).
**Recurrence:** First — new capability. Architectural intent: mini-site `property_reports` is the single source of truth for feature/scarcity intelligence; the PDF renders an elegant synopsis, not a parallel (weaker) computation.

## [SEO-SOLD-SITEMAP] Re-enable sold-property pages in sitemap — 13:05 AEST
**Symptom:** ~1,400 data-rich sold property pages (real transaction data, already SSR-rendering, already slugged) were invisible to search — only 163 of ~1,716 core-suburb properties were in the sitemap. Organic search is our highest-intent converting channel (Brain 2), so this suppressed the demand engine.
**Root cause:** generate-sitemap.mjs deliberately excluded sold pages (comment L167-172) because an earlier cohort had ObjectId URLs causing Google "discover-but-not-index" + crawl-budget dilution. That blocker was obsolete: every addressable sold property now has a human-readable url_slug and /property/:slug SSRs real content (verified: real title/meta/address for Googlebot).
**Fix:** Re-enabled sold in generate-sitemap.mjs — MEASURED: url_slug REQUIRED (no ObjectId fallback → 0 junk URLs; the 122 address-less sold records have no slug and are excluded), recency gate SOLD_MAX_AGE_MONTHS=48 (env-tunable), priority 0.6 (below for-sale 0.8/articles 0.7). Sitemap 259→1,690 URLs (163→1,594 property). Verified locally: 0 ObjectId URLs, valid XML, recency gate holding. Purely additive/reversible — no page/route/content change.
**Files:** Feilds_Website/01_Website/scripts/generate-sitemap.mjs (GitHub Will954633/Website_Version_Feb_2026 scripts/generate-sitemap.mjs, commit e348ae5).
**Next:** watch GSC indexation suburb-by-suburb over ~2wk; widen/narrow SOLD_MAX_AGE_MONTHS based on what indexes. Part of Brain 2 SEO demand-engine roadmap ([[brain2-seo-demand-engine-roadmap]]).
**Recurrence:** First

## [APPR-V4-BACKCOVER-QR] Back cover + even-page booklet + attributable QR — 14:20 AEST
**Symptom / request:** The appraisal PDF had no back page and an odd page count (19), so it couldn't be printed as a booklet. Will also wanted an attributable QR on both front and back linking to the property's mini-site — a homeowner scan must be recorded to PostHog, the CRM, and Brain 2 physical-material attribution.
**Build:**
- **Back cover** (`render.render_back_cover_html` + `BACK_COVER_TEMPLATE`): full-bleed grass-green page mirroring the front — Fields wordmark, "The Fields approach" closing statement (editorial-compliant: no advice/forecast), contact block (Will Simpson / will@fieldsestate.com.au / fieldsestate.com.au), Smarter-with-data mark, QR, and small-print (data source + limitations + "directional market information, not a formal valuation or advice" + methodology). Always the final page (`page-break-before`).
- **Even-page booklet** (`generate_appraisal_v4.render_appraisal`): counts printed page units (`class="page"`/`class="cover"`, exact-match so `page-pad`/`cover-image` don't false-hit), appends the back cover, and inserts one blank spacer before it when adding it would make the total odd. Silvabank: 19 → 20. (Note: even, and here also a multiple of 4; true saddle-stitch wants ×4 — flagged to Will.)
- **QR** (`render._qr_data_uri` via **segno**, SVG data-URI = vector, prints razor-sharp; error level M for ink smudge): rendered on front cover (white card, upper-right) and back cover. Encodes the **attributable** scan-redirect, not the bare mini-site URL.
- **Attribution** — new tracking-server route `GET /track/scan/<tracking_id>` (`tracking-server/server.py`): logs a `qr_scan` event (→ `email_tracking` + Telegram), writes Brain 2 `system_monitor.physical_attribution` (rolling doc/tracking_id, `channel: physical_appraisal_qr`), fires a **server-side PostHog** `physical_qr_scan` capture (public project key `phc_RQ68…`, guaranteed even if the browser never loads), syncs `crm_contacts` (`engagement.qr_scanned` + communications timeline), then 302s to the mini-site with print UTMs (`utm_source=printed_appraisal&utm_medium=qr&utm_campaign=physical_material&utm_content=<tracking_id>`) so client-side PostHog attributes the on-site session too.
- **Wiring:** `tracking_id` is now minted **pre-render** (was post-render) so the QR can encode `/track/scan/<id>` on first render; mini-site slug/url stamped on the `email_tracking` record (+ backfilled on re-render for pre-existing records). `_minisite_urls` falls back to the direct mini-site URL when no tracking_id exists.
**Verified:** regen → 20 pages; front + back QR both decode (pyzbar) to `https://vm.fieldsestate.com.au/track/scan/e06bacd2-81e`; that endpoint 302s to the mini-site with UTMs and logged qr_scan + physical_attribution + PostHog. Visually verified both covers. Test-scan artifacts cleaned off the real lead's record afterward.
**Files:** scripts/appraisal_template/render.py, scripts/generate_appraisal_v4.py, tracking-server/server.py, requirements.txt (+segno). Pushed f9676da, 34d7b64, 697897a, 75da215. Restarted fields-tracking.
**Recurrence:** First.

## [SEO-SITEMAP-BUILD-SCOPE] Live sitemap had ZERO property pages (build can't reach DB) — 14:35 AEST
**Symptom:** After re-enabling sold pages (SEO-SOLD-SITEMAP) the LIVE sitemap was 96 URLs (only static+articles, 0 property pages) — not just missing sold, missing ALL for-sale too. Predates today's change.
**Root cause:** netlify.toml build runs `generate-sitemap.mjs`, which reads `process.env.COSMOS_CONNECTION_STRING` to fetch property URLs. That Netlify env var is `is_secret` with `scopes:['functions','runtime']` — **'builds' is NOT in scope**, so the build container never receives it. Generator hits the `!uri` branch → emits 0 property URLs → overwrites public/sitemap.xml with a 96-URL file every deploy. The committed 206-URL sitemap was a stale manual artifact, silently clobbered on each build. So no property page (for-sale or sold) had been in the live sitemap.
**Fix (safe path chosen by Will — no new credential exposure):** (1) Made generate-sitemap.mjs FAIL-SAFE — when it gets 0 properties and an existing sitemap already contains /property/ URLs, it PRESERVES the committed file instead of clobbering it. (2) Generate the real sitemap on the VM (has DB access) and commit public/sitemap.xml (1,690 URLs / 1,594 property). (3) New `scripts/regenerate-sitemap.sh` + daily cron 06:15 AEST regenerates from live DB and pushes only when changed (guards: refuses to push if <100 property URLs). Alternative (rejected): add 'builds' to the var's scopes — one call but exposes the admin DB string to the build env/logs.
**Files:** Feilds_Website/01_Website/scripts/generate-sitemap.mjs, scripts/regenerate-sitemap.sh (both → GitHub Will954633/Website_Version_Feb_2026), public/sitemap.xml (committed), crontab (06:15 AEST).
**Recurrence:** First (related to [[SEO-SOLD-SITEMAP]] same day)

## [SEO-INDEXATION-BASELINE] Google on 4-month-stale sitemap; ~0% of property pages indexed — 14:50 AEST
**Symptom:** After the sold-page re-enable (163→1,594 /property URLs live today), scoping whether pages can rank. GSC sitemaps API shows Google's last sitemap download was **2026-03-20** with only **170 URLs submitted, 0 indexed** — Google never saw today's expansion. URL Inspection on a 20-URL sample: **0% indexed** — 75% "Discovered – currently not indexed", 10% "Crawled – not indexed" (crawled early April, declined), 15% "unknown to Google".
**Root cause:** Two layers. (1) Sitemap was never resubmitted after the March→July regen, so Google kept crawling the stale 170-URL snapshot. (2) The DEEPER, recurring cause (see [[GSC-INDEXING]] 2026-03-20, [[SEO-SOFT404]]/[[SEO-INDEXING]] 2026-03-12): **low domain authority + high URL count → Google won't spend crawl budget** — the exact reason sold pages were REMOVED in March. Re-enabling them changed one variable (readable slugs vs ObjectIDs) but did NOT change domain authority. The 10% "crawled-not-indexed" is compounded by the unresolved SSR-body gap: `/property/:id` SSRs meta+JSON-LD but the visible body is client-fetched ("Loading property…") — Bing (source of our best organic lead) barely renders JS, and Google's WRS may see a thin shell.
**Fix:** (1) Built `scripts/brain2/seo_indexation_check.py` — reports sitemap status + URL-Inspection coverage breakdown, writes weekly snapshot to `system_monitor.seo_indexation_snapshots`. (2) Resubmitted sitemap.xml via GSC API (now `isPending=True` → Google will re-fetch all 1,594 URLs). (3) Weekly cron Mon 07:00 AEST (resubmit + sample 30) to track the indexation curve. No page-generation done — the data says fix indexation of existing pages (SSR body + authority) BEFORE generating any new batch; a 1k-5k dump now would worsen crawl-budget dilution. Note: GSC service account only has the URL-prefix property, not sc-domain (fuller data) — Will could add it.
**Files:** scripts/brain2/seo_indexation_check.py (→ GitHub Will954633/Fields_Orchestrator, commit f328a3c7f), crontab (Mon 07:00 AEST)
**Recurrence:** Nth — discovered/crawled-not-indexed recurs (2026-03-12 [SEO-INDEXING], 2026-03-20 [GSC-INDEXING], now). Prior "fix" was removing pages; this experiment needs the opposite → authority + per-page quality are the real levers, not sitemap mechanics.

## [SEO-PILOT-REVIEW-CRON] Recurring self-prompt to check the SEO pilot — 14:55 AEST
**Symptom:** The SEO sold-page pilot needs watching over ~2wk (indexation + organic conversions) but a one-off analysis is worthless if nothing prompts a re-check.
**Fix:** Built a weekly review loop modelled on Will's run_ad_read_reminder.sh pattern. `scripts/brain2/seo_pilot_status.py` computes real numbers (live sitemap property count; GSC/Bing property-page impressions/clicks + distinct pages served = indexation proxy of ~1,594; top address-level property queries; all-channel address entries last 7d + neighbour-sale-triggers; organic /property entry sessions), persists a weekly snapshot to system_monitor.seo_pilot_weekly (for durable trend/deltas), prints Markdown, and `--telegram` sends to Will. `scripts/run_seo_pilot_review.sh` wraps it; cron Mon 08:00 AEST. Baseline (week 0, 2026-07-16): 1,594 sitemap property pages, 6 already served in GSC (queries like "8 trinity place robina", "173 ron penhaligon way"), 2 conversions/7d, 1 neighbour-trigger. Telegram baseline sent + snapshot saved OK.
**Files:** scripts/brain2/seo_pilot_status.py (new), scripts/run_seo_pilot_review.sh (new) — both pushed to Will954633/Fields_Orchestrator; crontab (Mon 08:00 AEST); system_monitor.seo_pilot_weekly (new collection).
**Recurrence:** First
