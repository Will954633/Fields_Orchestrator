
---

## [WATCHDOG] Step 117 (Satellite Analysis (Currently Listed)) — 00:21 AEST

**Failure class:** UNKNOWN
**Cause:** No recognizable error pattern — check logs manually
**Action taken:** ESCALATED to Claude (id=None)
**Result:** queued — awaiting repair-agent or Claude

## [SUBSCRIBER-SILO] Footer subscribers never reached leads/CRM — 07:33 AEST
**Symptom:** A website visitor (Richard Morris) who emailed positive feedback AND subscribed via the footer form had no CRM record. Investigation showed his email existed in exactly one place — `system_monitor.subscribers` — and nowhere in `leads`, `lead_worklist`, or `crm_contacts`.
**Root cause:** The footer subscribe form writes to `system_monitor.subscribers`, but NO downstream pipeline reads that collection. `lead_intelligence.py` (nightly 02:00 → `lead_worklist`, the unified actionable lead surface) ingested 7 sources — analyse_leads, launch_leads, leads, fb_leads, property_reports, offmarket_orders, PostHog offmarket_report_view — but not `subscribers`. `crm_contacts` is built separately from PostHog engagement + manual. Result: every footer signup since March 2026 (4 total incl. founder test) was a dead-end silo — invisible to the lead/ops workflow.
**Fix:** Added `subscribers` as an 8th source in `collect_leads()`, normalising each subscriber → a lead (`source: "<src>_subscribe"`, email, name, `subscribed_at` as first_seen, `signal: newsletter_subscribe`). Founder/test emails drop via the existing `_is_test` filter. Ran `--no-fresh` to backfill immediately: Darrell + Susannah now surface in `lead_worklist`; Richard merges with the CRM row created for him. Note: the CRM upsert (line ~522) is intentionally NOT `upsert=True`, so subscribers reach the worklist but do not auto-create `crm_contacts` rows — left as-is pending Will's decision on whether subscribers should become full CRM contacts.
**Files:** scripts/samantha/lead_intelligence.py
**Recurrence:** First

## [VISITOR-FEEDBACK-FIXES] Public /ops, tracker disclosure, footer FB id — 08:10 AEST
**Symptom:** Website visitor (Richard Morris) flagged 3 issues: (1) /ops endpoint public, (2) GA4/Google Ads/PostHog(session recording+heatmaps)/Meta trackers firing with no consent + no linked privacy policy, (3) two different Facebook page IDs (schema vs footer).
**Root cause:** (1) /ops page shell was public at a well-known path (data was still token-gated, so no data leak, but path discoverable + JS bundle exposed internal URLs). (2) A privacy policy existed at public/privacy.html but was NOT linked anywhere in the footer AND materially understated collection — Section 8 falsely claimed only the Meta Pixel set cookies (PostHog session recording/heatmaps/cookies + GA4 + Google Ads were undisclosed). (3) Footer linked FB id 61587562075596 (not managed by our ad account per Graph API — does not exist/not ours) while schema/ads/About/articles use the real page 889412530933297.
**Fix:** (1) Renamed route /ops -> /ops-hub-4417 (routes.ts + legacy App.tsx) and added `noindex,nofollow` meta to the route; updated isOpsPath + PostHog internal-traffic flags (root.tsx, posthog.ts) so trackers still skip on the new path; did NOT add the secret path to robots.txt (would leak it — existing `Disallow: /ops` prefix still covers it). (2) Corrected public/privacy.html to disclose GA4, Google Ads, PostHog (session recording + heatmaps + autocapture + cookies), Meta Pixel + CAPI; added opt-out guidance; bumped effective date to 28 July 2026; linked Privacy Policy in both footer link lists. No active-consent banner (Will's call — APPs don't mandate GDPR-style banner). (3) Repointed footer FB link to 889412530933297. All 8 files pushed as ONE commit via Git Trees API (755f87b4) to avoid rapid-commit Netlify unpublish. vite build passed; edited files introduce no new type errors.
**Files:** src/routes.ts, src/root.tsx, src/utils/posthog.ts, src/routes/ops.tsx, src/App.tsx, src/components/SiteFooter/SiteFooter.tsx, src/pages/OpsPage/OpsPage.tsx, public/privacy.html
**Recurrence:** First

## [FIELDS-QUARTERLY-COVER-V1] Developer built the F-mask cover; V1 installed canonical — 08:40 AEST
**Symptom:** Developer's first delivery had a number-only cover (no designer photo device). Sent COVER_IMAGE_INSTRUCTIONS.md back (assets in the 05 Finished Art.zip: Gold-Coast.jpg skyline + 5-Fields-Icon-Copper.eps; use inline SVG clip not CSS mask — the print gotcha).
**Fix:** Developer (Alex) returned 3 cover variants (`assets/HTML_280726/fields-versions.zip`): V1 skyline/clean, V2 line-chart + apartment, V3 bar-chart + surfer — all with the photo masked into the actual Fields "F" via inline SVG (`fields_symbol.svg`), so it renders in print. Will chose **V1 as default**. Installed V1 as canonical (`rsync … fields-version-1/ → pipeline/quarterly/`, kept INTEGRATION.md), re-synced the `/quarterly/` preview, renders 36pp with the correct cover. Chart mapping unchanged (6 pipeline PNG + 3 developer-custom suburb PNG). New cover assets: bg_1.jpg (skyline), fields_symbol.svg (F mask), fields_logotype.svg.
**Open items:** 3 custom suburb charts still PNG (no pipeline source); Q2 editorial authoring pass; cover photo swappable per issue (skyline/interior/surf via bg_*.jpg + SVG clip).
**Files:** 10_Market_Report/pipeline/quarterly/* (V1); assets/HTML_280726/.
**Recurrence:** Nth (FIELDS-QUARTERLY-* series).

## [SEO-V3-SUBURB-SSR] SSR v3 listing cards + per-suburb landing pages + canonicals — 10:40 AEST
**Symptom:** Three SEO gaps on the for-sale surface: (1) /for-sale-v3 was 100% client-fetched — Googlebot saw the h1+intro but 0 listing cards / 0 internal /property links; (2) no per-suburb landing page existed to match "houses <suburb>" queries (only a single 3-suburb feed); (3) SEO Page Integrity findings: homepage / had no `<h1>` and /market-metrics/:suburb had no canonical.
**Root cause:** v3 route had no loader (nothing SSR'd but a skeleton). No /houses-for-sale/:suburb route existed. market-metrics meta() never emitted a canonical. Homepage masthead `<h1>` was written locally 2026-07-27 but NEVER pushed to GitHub (local drift) so it wasn't deployed.
**Fix:** (1) Added `getForSaleListings()` to db.server.ts (published, non-waterfront for_sale only — matches the property noindex gate) + shared `SsrListingCards` component; v3 route now has a loader that SSRs up to 40 published listings, rendered in the loading/error branches (hydration-safe: server + client both start loading=true) — live crawler HTML now carries 84 /property links. (2) New `/houses-for-sale/:suburb` route + `HousesForSalePage` (SSR h1, market-context intro, crawlable cards, cross-links to market-metrics/news/v3) for Robina/Burleigh-Waters/Varsity-Lakes; added to sitemap STATIC_PAGES + internal nav on v3. (3) Self-referencing canonical added to market-metrics meta (suburb + optional category); pushed the drifted homepage masthead h1. Honest-count fix: heading shows "N with a full Fields analysis · of M currently on the market" (39 editorialized vs 58 active) to avoid an on-page contradiction.
**Files:** src/lib/db.server.ts, src/components/SsrListingCards/{SsrListingCards.tsx,.module.css} (new), src/pages/HousesForSalePage/{HousesForSalePage.tsx,.module.css} (new), src/routes/houses-for-sale.$suburb.tsx (new), src/routes/for-sale-v3.tsx, src/pages/DecisionFeedV3Page/{DecisionFeedV3Page.tsx,.module.css}, src/routes.ts, src/routes/market-metrics.$suburb.tsx, scripts/generate-sitemap.mjs, src/pages/MarketIntelligencePage/{MarketIntelligencePage.tsx,.module.css}. Website commits fec3cb67 (14 files, ONE atomic commit via Git Trees API to avoid 14 Netlify builds), f6193fa7 (sitemap), a28caa73 (count fix).
**Recurrence:** Nth (SEO SSR/indexing class — see [SEO-PROPERTY-SSR] 2026-07-16, [SEO-EDITORIAL-GATE] 2026-07-24, forsale_v3_seo_positioning memory 2026-07-24). Local-drift-not-deployed class recurs — masthead h1 written 07-27 but unpushed until now.

## [SEO-DASHBOARD + NO-SILENT-FAILURES] Nightly GSC dashboard + generic self-monitoring behaviour — 11:30 AEST
**Symptom:** (1) No dedicated place tracked SEO/indexation performance over time — GSC data lived only in Search Console; Drive had only partial signals (organic sessions in KPI Monitor, page-integrity in Systems Health). (2) Broader: no enforced pattern that a NEW ongoing process self-monitors, so processes kept failing silently for days-to-weeks (sitemap push 07-20→22, GitHub Actions up to 5 weeks, lead worklist 9 days).
**Root cause:** No GSC→Drive pipeline existed. Self-report infra (`job_status.record_job_result` → `system_monitor.job_runs` → main_site_health_check collectors) existed but each job needed a bespoke collector, so new jobs were easy to leave unmonitored.
**Fix:** (a) `scripts/seo_dashboard.py` — nightly GSC Search Analytics (clicks/impr/CTR/position, daily trend, top queries/pages, by-page-type) + URL-Inspection sampling reconstructing the "Page indexing" coverage buckets (the aggregate report has no API) → dedicated Drive sheet `1ePTElYggYG8ZQKag4FuLCuh8uYTYfVWhGg9X_dTzyYs` (Will-owned via one-time OAuth `--bootstrap`, shared to SA, written nightly by the STABLE service account so the 7-day OAuth death never breaks the cron). Perf tabs written before the slow sampling so they're never lost. Cron 45 6 * * *. (b) Generalised the "never fail silently" rule: added `job_run()` context manager to `job_status.py` (heartbeats success/error+traceback, self-registers via `cadence_hours`) + a GENERIC `collect_self_reported_jobs` collector in `main_site_health_check.py` that auto-renders EVERY self-registered job on the Process Registry (OK / STALE past cadence×1.5 / ERROR) — zero per-script wiring. Codified as CLAUDE.md Mandatory Rule 7 (both copies) + feedback memory. Verified: heartbeat doc written, renderer emits "[OK] SEO & Indexation Dashboard", sheet fully populated (297 clicks/11,833 impr/58.1% indexed sample).
**Files:** scripts/seo_dashboard.py (new), scripts/job_status.py, scripts/main_site_health_check.py, config/seo_dashboard.json (new), CLAUDE.md (+ /home/fields/CLAUDE.md synced). Orchestrator commit 426333ca. Cron added (06:45 AEST).
**Recurrence:** First (dashboard); the silent-failure CLASS is Nth — this rule exists to close it permanently (see [SEO-PROPERTY-SSR], sitemap-push failures 07-20→22).

## [SAMANTHA-WEEKLY-SEO] Weekly SEO-improvement workflow + weekly staleness monitor — 12:05 AEST
**Symptom:** N/A — new capability (Will's request): a once-per-week Samantha workflow that reviews the SEO dashboard and actively raises SEO performance (esp /for-sale-v3), plus a monitor confirming it ran within the last 7 days, surfaced on the Fields Systems Health sheet.
**Root cause:** N/A.
**Fix:** (a) `scripts/samantha/seo_improvement_weekly.py` — headless Max (Fable-5/Opus) runner reusing daily_run.py helpers (usage gate, telegram, sdk env, transcript), own lean run loop (no appraisal-reflection). Prompt = charter.md + new `seo_improvement_task.md` brief (review ALL dashboard tabs + live GSC + Googlebot-fetch pages → ship >=1 verified SEO improvement or escalate a Will-decision → Google Doc + Telegram). `run_seo_weekly.sh` wrapper; cron `0 8 * * 0` (Sun 08:00 AEST); documented in charter.md § "Standing weekly workflow". (b) The monitor: run wrapped in `job_run("samantha_seo_improvement", cadence_hours=168, stale_hours=180)`. Added `stale_hours` override to `job_status.py` + honored it in `main_site_health_check.collect_self_reported_jobs` (default cadence×1.5=10.5d; a weekly job wants ~7d → 180h=7.5d avoids flapping at the weekly boundary vs the 01:00 daily health check). Auto-renders on Process Registry as "Weekly SEO Improvement (Samantha)". Verified: smoke run launched the Max agent, read the dashboard via gdrive MCP, recorded the heartbeat (cadence 168/stale 180/self_registered/title correct); renderer emits "[OK] Weekly SEO Improvement (Samantha)". Usage gate correctly skips a real run when Max 5h/7d over budget (5h was 100% during the test — smoke bypasses, real run skips).
**Files:** scripts/samantha/seo_improvement_weekly.py (new), scripts/samantha/seo_improvement_task.md (new), scripts/samantha/run_seo_weekly.sh (new), scripts/samantha/charter.md, scripts/job_status.py, scripts/main_site_health_check.py. Orchestrator commit 4b124624. Cron added (Sun 08:00). Builds on [SEO-DASHBOARD + NO-SILENT-FAILURES] (same day).
**Recurrence:** First.

## [QUARTERLY-Q2] Authored Q2 2026 Fields Quarterly + eliminated multi-layer figure staleness — 13:50 AEST
**Symptom:** Q2 build re-used the developer's Q1 template. Naively updating the visible "data pages" left Q1 figures alive in ~10 other independent content layers (internal masthead, how-to-read FCI callout, Conviction Map page, standoff data-list page, volume/DOM prose, indicator-board label, suburb narrative pages, both case studies, and the "does not answer" page). The conviction-map + volume + DOM pages' PROSE also contradicted their own (already-Q2) charts, and the Robina case study's "$374,000 above median" math was computed against a stale $1,336,000 median.
**Root cause:** Same class as Rule 6 market_pulse staleness — a document with many content layers where a partial edit only touches named layers. Two legitimate price-growth metrics coexist (FCI rolling-12m `precomputed_indexed_prices.rolling_12m_yoy_pct` vs market_pulse current-quarter YoY); they diverge most for Burleigh Waters (rolling +22.8% vs current +12.2%, because rolling still carries the thin, expensive Q4 2025 peak). Mixing them silently produced text that fought the charts (chart plots BW furthest-right on price; prose claimed Varsity fastest).
**Fix:** Pulled real Q2 figures from `fci_summary.md` + `fci_series.csv` + market_pulse; rebuilt every layer. Rule: FCI-derived pages (FCI, components, conviction-map, indexed-price) speak the rolling metric to match their charts; suburb data pages speak current-quarter YoY to match the public market-metrics site; the rolling/current split is labelled. Corrected page-9 history (94.5 = lowest since 2019, NOT a "2022 trough at 92" — composite floor is 89.9 in Q4 2018). Renumbered all 41 footers to physical page position. Verified every edited page by rasterising the PDF and reading it (text-vs-chart consistency), plus a 41-page contact sheet.
**Files:** 10_Market_Report/pipeline/quarterly/index.html (Q2 authoring), pipeline/chart_theme.py, pipeline/generate_charts.py, drafts/q2_2026_editorial_outline.md. Pushed to Will954633/Fields_Orchestrator (index.html @524db15).
**Recurrence:** Nth — staleness is a known recurring class (API-HEALTH, STALE-ARTICLE, MEDIAN-DISPLAY, METHODOLOGY-CONSISTENCY, CEO-STALE-DATA all 2026-07-28). Mitigation applied here: full-document figure sweep + per-page visual verification, never trust a partial $set-style edit.
