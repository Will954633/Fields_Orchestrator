
---

## [SAMANTHA-AD-LIFECYCLE] New workflow — fast-iteration ad cull + winner→organic promote — 14:05 AEST
**Symptom:** No coded, self-checking workflow for (a) culling clearly-underperforming FB ads early (fast iteration) or (b) reposting a paid winner's creative as an organic post at a controlled cadence. Both were prose-only "someday" ideas, so neither happened reliably.
**Root cause:** Feature gap, not a bug. Ad culling relied on the human/agent noticing; there was no metric-driven, objective-aware "results" resolver, no Brain-2-preservation guarantee before killing an ad, and no monthly organic-repost tracker.
**Fix:** Built `scripts/samantha/ad_lifecycle.py` (subcommands `cull-scan` / `organic-promote` / `run` / `status`). Cull bar (Will's choice): ≥2-day age + fair-shot exposure floor (≥500 impr AND ≥$10) + clear laggard vs best sibling in the SAME campaign (0 results while a sibling proved conversion on ≤ comparable budget, OR cost-per-result ≥3× the campaign best), always keeps ≥1 ad/campaign. "Results" is objective-specific (leads via `fb_leads` join / link clicks / post engagements / content views / reach). Before pausing it verifies each Brain-2 layer, best-effort backfills (`ad_creative_enrich.py` + bounded `ad_annotate.py`), and archives the full raw dossier to `ad_lifecycle_archive` (we PAUSE not delete). Logs `ad_decisions`[pruning] + drops a replacement brief into Will's running doc for Samantha to write the rigorous proposal. `organic-promote` reposts a genuine winner (highest business-value tier: leads>conversions>traffic>engagement>reach; video skipped; editorial-compliance gated) as an organic post at max ~1/month, tracked in `ad_organic_reposts` + `samantha_state.ad_organic_promote`. Wired: daily 12:40 AEST cron (`run_ad_lifecycle.sh`, `job_run` heartbeat), charter.md standing-workflow section + session-end sweep dimension, daily_tasks.md Task 3b, and two CEO Governance rows in `main_site_health_check.py` (STALE if the daily run stops). Verified dry-run (0 culls today — nothing ≥2d old; winner selection correctly prefers the leads ad over a cheap-engagement video; Brain-2 completeness check matches known gaps on the gleneagles carousel ad). Maiden organic publish deferred to the first cron fire so Will sees it first (auto-publish policy honoured going forward).
**Files:** scripts/samantha/ad_lifecycle.py (new), scripts/samantha/run_ad_lifecycle.sh (new), scripts/samantha/charter.md, scripts/samantha/daily_tasks.md, scripts/main_site_health_check.py, crontab (12:40 daily).
**Recurrence:** First.

## [WATCHDOG] Step 117 (Satellite Analysis (Currently Listed)) — 00:21 AEST

**Failure class:** UNKNOWN
**Cause:** No recognizable error pattern — check logs manually
**Action taken:** ESCALATED to Claude (id=None)
**Result:** queued — awaiting repair-agent or Claude

## [SUBSCRIBER-SILO] Footer subscribers never reached leads/CRM — 07:33 AEST
**Symptom:** A website visitor (Richard Morris) who emailed positive feedback AND subscribed via the footer form had no CRM record. Investigation showed his email existed in exactly one place — `system_monitor.subscribers` — and nowhere in `leads`, `lead_worklist`, or `crm_contacts`.
**Root cause:** The footer subscribe form writes to `system_monitor.subscribers`, but NO downstream pipeline reads that collection. `lead_intelligence.py` (nightly 02:00 → `lead_worklist`, the unified actionable lead surface) ingested 7 sources — analyse_leads, launch_leads, leads, fb_leads, property_reports, offmarket_orders, PostHog offmarket_report_view — but not `subscribers`. `crm_contacts` is built separately from PostHog engagement + manual. Result: every footer signup since March 2026 (4 total incl. founder test) was a dead-end silo — invisible to the lead/ops workflow.
**Fix:** Added `subscribers` as an 8th source in `collect_leads()`, normalising each subscriber → a lead (`source: "<src>_subscribe"`, email, name, `subscribed_at` as first_seen, `signal: newsletter_subscribe`). Founder/test emails drop via the existing `_is_test` filter. Ran `--no-fresh` to backfill immediately: Darrell + Susannah now surface in `lead_worklist`; Richard merges with the CRM row created for him. Note: the CRM upsert (line ~522) is intentionally NOT `upsert=True`, so subscribers reach the worklist but do not auto-create `crm_contacts` rows — left as-is pending Will's decision on whether subscribers should become full CRM contacts.
**Files:** scripts/samantha/lead_intelligence.py
**Recurrence:** First

## [VISITOR-FEEDBACK-FIXES] Public /ops, tracker disclosure, footer FB id — 08:10 AEST
**Symptom:** Website visitor (Richard Morris) flagged 3 issues: (1) /ops endpoint public, (2) GA4/Google Ads/PostHog(session recording+heatmaps)/Meta trackers firing with no consent + no linked privacy policy, (3) two different Facebook page IDs (schema vs footer).
**Root cause:** (1) /ops page shell was public at a well-known path (data was still token-gated, so no data leak, but path discoverable + JS bundle exposed internal URLs). (2) A privacy policy existed at public/privacy.html but was NOT linked anywhere in the footer AND materially understated collection — Section 8 falsely claimed only the Meta Pixel set cookies (PostHog session recording/heatmaps/cookies + GA4 + Google Ads were undisclosed). (3) Footer linked FB id 61587562075596 (not managed by our ad account per Graph API — does not exist/not ours) while schema/ads/About/articles use the real page 889412530933297.
**Fix:** (1) Renamed route /ops -> /ops-hub-4417 (routes.ts + legacy App.tsx) and added `noindex,nofollow` meta to the route; updated isOpsPath + PostHog internal-traffic flags (root.tsx, posthog.ts) so trackers still skip on the new path; did NOT add the secret path to robots.txt (would leak it — existing `Disallow: /ops` prefix still covers it). (2) Corrected public/privacy.html to disclose GA4, Google Ads, PostHog (session recording + heatmaps + autocapture + cookies), Meta Pixel + CAPI; added opt-out guidance; bumped effective date to 28 July 2026; linked Privacy Policy in both footer link lists. No active-consent banner (Will's call — APPs don't mandate GDPR-style banner). (3) Repointed footer FB link to 889412530933297. All 8 files pushed as ONE commit via Git Trees API (755f87b4) to avoid rapid-commit Netlify unpublish. vite build passed; edited files introduce no new type errors.
**Files:** src/routes.ts, src/root.tsx, src/utils/posthog.ts, src/routes/ops.tsx, src/App.tsx, src/components/SiteFooter/SiteFooter.tsx, src/pages/OpsPage/OpsPage.tsx, public/privacy.html
**Recurrence:** First

## [FIELDS-QUARTERLY-COVER-V1] Developer built the F-mask cover; V1 installed canonical — 08:40 AEST
**Symptom:** Developer's first delivery had a number-only cover (no designer photo device). Sent COVER_IMAGE_INSTRUCTIONS.md back (assets in the 05 Finished Art.zip: Gold-Coast.jpg skyline + 5-Fields-Icon-Copper.eps; use inline SVG clip not CSS mask — the print gotcha).
**Fix:** Developer (Alex) returned 3 cover variants (`assets/HTML_280726/fields-versions.zip`): V1 skyline/clean, V2 line-chart + apartment, V3 bar-chart + surfer — all with the photo masked into the actual Fields "F" via inline SVG (`fields_symbol.svg`), so it renders in print. Will chose **V1 as default**. Installed V1 as canonical (`rsync … fields-version-1/ → pipeline/quarterly/`, kept INTEGRATION.md), re-synced the `/quarterly/` preview, renders 36pp with the correct cover. Chart mapping unchanged (6 pipeline PNG + 3 developer-custom suburb PNG). New cover assets: bg_1.jpg (skyline), fields_symbol.svg (F mask), fields_logotype.svg.
**Open items:** 3 custom suburb charts still PNG (no pipeline source); Q2 editorial authoring pass; cover photo swappable per issue (skyline/interior/surf via bg_*.jpg + SVG clip).
**Files:** 10_Market_Report/pipeline/quarterly/* (V1); assets/HTML_280726/.
**Recurrence:** Nth (FIELDS-QUARTERLY-* series).

## [SEO-V3-SUBURB-SSR] SSR v3 listing cards + per-suburb landing pages + canonicals — 10:40 AEST
**Symptom:** Three SEO gaps on the for-sale surface: (1) /for-sale-v3 was 100% client-fetched — Googlebot saw the h1+intro but 0 listing cards / 0 internal /property links; (2) no per-suburb landing page existed to match "houses <suburb>" queries (only a single 3-suburb feed); (3) SEO Page Integrity findings: homepage / had no `<h1>` and /market-metrics/:suburb had no canonical.
**Root cause:** v3 route had no loader (nothing SSR'd but a skeleton). No /houses-for-sale/:suburb route existed. market-metrics meta() never emitted a canonical. Homepage masthead `<h1>` was written locally 2026-07-27 but NEVER pushed to GitHub (local drift) so it wasn't deployed.
**Fix:** (1) Added `getForSaleListings()` to db.server.ts (published, non-waterfront for_sale only — matches the property noindex gate) + shared `SsrListingCards` component; v3 route now has a loader that SSRs up to 40 published listings, rendered in the loading/error branches (hydration-safe: server + client both start loading=true) — live crawler HTML now carries 84 /property links. (2) New `/houses-for-sale/:suburb` route + `HousesForSalePage` (SSR h1, market-context intro, crawlable cards, cross-links to market-metrics/news/v3) for Robina/Burleigh-Waters/Varsity-Lakes; added to sitemap STATIC_PAGES + internal nav on v3. (3) Self-referencing canonical added to market-metrics meta (suburb + optional category); pushed the drifted homepage masthead h1. Honest-count fix: heading shows "N with a full Fields analysis · of M currently on the market" (39 editorialized vs 58 active) to avoid an on-page contradiction.
**Files:** src/lib/db.server.ts, src/components/SsrListingCards/{SsrListingCards.tsx,.module.css} (new), src/pages/HousesForSalePage/{HousesForSalePage.tsx,.module.css} (new), src/routes/houses-for-sale.$suburb.tsx (new), src/routes/for-sale-v3.tsx, src/pages/DecisionFeedV3Page/{DecisionFeedV3Page.tsx,.module.css}, src/routes.ts, src/routes/market-metrics.$suburb.tsx, scripts/generate-sitemap.mjs, src/pages/MarketIntelligencePage/{MarketIntelligencePage.tsx,.module.css}. Website commits fec3cb67 (14 files, ONE atomic commit via Git Trees API to avoid 14 Netlify builds), f6193fa7 (sitemap), a28caa73 (count fix).
**Recurrence:** Nth (SEO SSR/indexing class — see [SEO-PROPERTY-SSR] 2026-07-16, [SEO-EDITORIAL-GATE] 2026-07-24, forsale_v3_seo_positioning memory 2026-07-24). Local-drift-not-deployed class recurs — masthead h1 written 07-27 but unpushed until now.

## [SEO-DASHBOARD + NO-SILENT-FAILURES] Nightly GSC dashboard + generic self-monitoring behaviour — 11:30 AEST
**Symptom:** (1) No dedicated place tracked SEO/indexation performance over time — GSC data lived only in Search Console; Drive had only partial signals (organic sessions in KPI Monitor, page-integrity in Systems Health). (2) Broader: no enforced pattern that a NEW ongoing process self-monitors, so processes kept failing silently for days-to-weeks (sitemap push 07-20→22, GitHub Actions up to 5 weeks, lead worklist 9 days).
**Root cause:** No GSC→Drive pipeline existed. Self-report infra (`job_status.record_job_result` → `system_monitor.job_runs` → main_site_health_check collectors) existed but each job needed a bespoke collector, so new jobs were easy to leave unmonitored.
**Fix:** (a) `scripts/seo_dashboard.py` — nightly GSC Search Analytics (clicks/impr/CTR/position, daily trend, top queries/pages, by-page-type) + URL-Inspection sampling reconstructing the "Page indexing" coverage buckets (the aggregate report has no API) → dedicated Drive sheet `1ePTElYggYG8ZQKag4FuLCuh8uYTYfVWhGg9X_dTzyYs` (Will-owned via one-time OAuth `--bootstrap`, shared to SA, written nightly by the STABLE service account so the 7-day OAuth death never breaks the cron). Perf tabs written before the slow sampling so they're never lost. Cron 45 6 * * *. (b) Generalised the "never fail silently" rule: added `job_run()` context manager to `job_status.py` (heartbeats success/error+traceback, self-registers via `cadence_hours`) + a GENERIC `collect_self_reported_jobs` collector in `main_site_health_check.py` that auto-renders EVERY self-registered job on the Process Registry (OK / STALE past cadence×1.5 / ERROR) — zero per-script wiring. Codified as CLAUDE.md Mandatory Rule 7 (both copies) + feedback memory. Verified: heartbeat doc written, renderer emits "[OK] SEO & Indexation Dashboard", sheet fully populated (297 clicks/11,833 impr/58.1% indexed sample).
**Files:** scripts/seo_dashboard.py (new), scripts/job_status.py, scripts/main_site_health_check.py, config/seo_dashboard.json (new), CLAUDE.md (+ /home/fields/CLAUDE.md synced). Orchestrator commit 426333ca. Cron added (06:45 AEST).
**Recurrence:** First (dashboard); the silent-failure CLASS is Nth — this rule exists to close it permanently (see [SEO-PROPERTY-SSR], sitemap-push failures 07-20→22).

## [SAMANTHA-WEEKLY-SEO] Weekly SEO-improvement workflow + weekly staleness monitor — 12:05 AEST
**Symptom:** N/A — new capability (Will's request): a once-per-week Samantha workflow that reviews the SEO dashboard and actively raises SEO performance (esp /for-sale-v3), plus a monitor confirming it ran within the last 7 days, surfaced on the Fields Systems Health sheet.
**Root cause:** N/A.
**Fix:** (a) `scripts/samantha/seo_improvement_weekly.py` — headless Max (Fable-5/Opus) runner reusing daily_run.py helpers (usage gate, telegram, sdk env, transcript), own lean run loop (no appraisal-reflection). Prompt = charter.md + new `seo_improvement_task.md` brief (review ALL dashboard tabs + live GSC + Googlebot-fetch pages → ship >=1 verified SEO improvement or escalate a Will-decision → Google Doc + Telegram). `run_seo_weekly.sh` wrapper; cron `0 8 * * 0` (Sun 08:00 AEST); documented in charter.md § "Standing weekly workflow". (b) The monitor: run wrapped in `job_run("samantha_seo_improvement", cadence_hours=168, stale_hours=180)`. Added `stale_hours` override to `job_status.py` + honored it in `main_site_health_check.collect_self_reported_jobs` (default cadence×1.5=10.5d; a weekly job wants ~7d → 180h=7.5d avoids flapping at the weekly boundary vs the 01:00 daily health check). Auto-renders on Process Registry as "Weekly SEO Improvement (Samantha)". Verified: smoke run launched the Max agent, read the dashboard via gdrive MCP, recorded the heartbeat (cadence 168/stale 180/self_registered/title correct); renderer emits "[OK] Weekly SEO Improvement (Samantha)". Usage gate correctly skips a real run when Max 5h/7d over budget (5h was 100% during the test — smoke bypasses, real run skips).
**Files:** scripts/samantha/seo_improvement_weekly.py (new), scripts/samantha/seo_improvement_task.md (new), scripts/samantha/run_seo_weekly.sh (new), scripts/samantha/charter.md, scripts/job_status.py, scripts/main_site_health_check.py. Orchestrator commit 4b124624. Cron added (Sun 08:00). Builds on [SEO-DASHBOARD + NO-SILENT-FAILURES] (same day).
**Recurrence:** First.

## [QUARTERLY-Q2] Authored Q2 2026 Fields Quarterly + eliminated multi-layer figure staleness — 13:50 AEST
**Symptom:** Q2 build re-used the developer's Q1 template. Naively updating the visible "data pages" left Q1 figures alive in ~10 other independent content layers (internal masthead, how-to-read FCI callout, Conviction Map page, standoff data-list page, volume/DOM prose, indicator-board label, suburb narrative pages, both case studies, and the "does not answer" page). The conviction-map + volume + DOM pages' PROSE also contradicted their own (already-Q2) charts, and the Robina case study's "$374,000 above median" math was computed against a stale $1,336,000 median.
**Root cause:** Same class as Rule 6 market_pulse staleness — a document with many content layers where a partial edit only touches named layers. Two legitimate price-growth metrics coexist (FCI rolling-12m `precomputed_indexed_prices.rolling_12m_yoy_pct` vs market_pulse current-quarter YoY); they diverge most for Burleigh Waters (rolling +22.8% vs current +12.2%, because rolling still carries the thin, expensive Q4 2025 peak). Mixing them silently produced text that fought the charts (chart plots BW furthest-right on price; prose claimed Varsity fastest).
**Fix:** Pulled real Q2 figures from `fci_summary.md` + `fci_series.csv` + market_pulse; rebuilt every layer. Rule: FCI-derived pages (FCI, components, conviction-map, indexed-price) speak the rolling metric to match their charts; suburb data pages speak current-quarter YoY to match the public market-metrics site; the rolling/current split is labelled. Corrected page-9 history (94.5 = lowest since 2019, NOT a "2022 trough at 92" — composite floor is 89.9 in Q4 2018). Renumbered all 41 footers to physical page position. Verified every edited page by rasterising the PDF and reading it (text-vs-chart consistency), plus a 41-page contact sheet.
**Files:** 10_Market_Report/pipeline/quarterly/index.html (Q2 authoring), pipeline/chart_theme.py, pipeline/generate_charts.py, drafts/q2_2026_editorial_outline.md. Pushed to Will954633/Fields_Orchestrator (index.html @524db15).
**Recurrence:** Nth — staleness is a known recurring class (API-HEALTH, STALE-ARTICLE, MEDIAN-DISPLAY, METHODOLOGY-CONSISTENCY, CEO-STALE-DATA all 2026-07-28). Mitigation applied here: full-document figure sweep + per-page visual verification, never trust a partial $set-style edit.

## [BYL-CAMPAIGN-LAUNCH] Before You List seller carousel campaign — built + activated via API — 13:50 AEST
**Symptom:** N/A — new capability. Will authorised building + turning on the 3-arm seller lead campaign via the Meta Marketing API.
**Root cause:** N/A.
**Fix:** Built campaign 120251770919230134 (OUTCOME_LEADS) + Instant Form 1797190291266790 (physical-only: name/email/phone/postal address) + 18 uploaded card images + 3 ad sets (A/B/C, $25/day each, LEAD_GENERATION, geo Robina/Varsity Lakes/Burleigh Waters +8km, age 35-65, advantage_audience off) + 3 carousel creatives (6 cards, SIGN_UP→form) + 3 ads. Activated all. Wired fb-lead-puller.py to route this form's leads to system_monitor.print_post_queue (arm-tagged from adset_name, blank/implausible postcode -> needs_review). Added Rule-5 confidence disclaimer to /analyse-your-home (Website commit e54c767). Logged ad_decisions + ad_experiments (before_you_list_3arm).
**Files:** 03_Facebook/CTA_Book_Before-you-list/{README,SCOPING,BUILD-PACK}.md, scripts/{launch_campaign,render_cards,select_homes,fulfilment_stub}.py, creatives/{A,B,C}/*.png; scripts/fb-lead-puller.py; (Website) src/pages/AnalyseYourHomePage/AnalyseYourHomePage.tsx.
**Recurrence:** First. API gotchas captured in memory fb_carousel_lead_ad_api.

## [HOMEOWNER-FUNNEL] Home Owner Lead Funnel built + seller-lead plumbing fixed — 14:17 AEST
**Symptom:** No working homeowner-seller lead flow (AYH seller ads: 0 leads / $203 over 90d); `fb-lead-puller.py notify()` hardcoded for buyer forms → a seller lead would reach the DB but Will's Telegram alert would be empty (no phone), so unactionable.
**Root cause:** Seller side untested at the offer/form level; puller had no seller/test branch.
**Fix:** Added `notify_seller()` (surfaces phone + selling-intent) for GC SELLER_FORM_IDS; added TEST_FORM_IDS silent-capture branch (tag `test_market`, NO Telegram/CRM/follow-up — out-of-market leads receive nothing); guarded CRM sync to skip test leads. Built full funnel: 6 lead forms, geo-neutral dark+light creatives, GC served campaign (paused), 16-ad out-of-market test (SEQ ex-GC) LIVE, hourly `checkpoint.py` cron (auto-pause losers + impression-share/dark-light rollup + Telegram, job_run self-monitored).
**Files:** scripts/fb-lead-puller.py; 03_Facebook/Home_Owner_Lead_Funnel_Search/{create_lead_forms,create_test_forms,render_launch_cards,render_test_cards,build_campaign,build_market_test,checkpoint}.py + 00_MASTER_LEDGER.md/01_STRATEGY.md/02_AD_COPY.md
**Recurrence:** First

## [QUARTERLY-STANDOFF] Rewrote Q2 "Standoff" section + fixed tension-chart rebase collision — 14:20 AEST
**Symptom:** The "Standoff" section was Q1-vintage framing (a balanced stalemate) that no longer fit Q2 (momentum broke decisively while price held). Separately, the tension chart's FCI endpoint label read 94.2 while the headline Conviction Index is 94.5 everywhere else — a near-collision that reads as an inconsistency.
**Root cause:** (1) Section written for Q1's marginal 98.6 reading; Q2's 94.5 is a decisive divergence, not a standoff. (2) `chart_tension` double-rebased the FCI to Q1 2023 = 100 (94.46 → 94.18 ≈ 94.2), colliding visually with the native-scale headline 94.5.
**Fix:** Retitled section "The Standoff Tilts"; rewrote divider standfirst + tension prose to acknowledge Issue 01 explicitly and describe the Q2 widening ("the activity side moved first"); added a verified cross-suburb case-study panel (3 Beaumaris Court 9d/$1,405,000, 79 Seville Circuit 3d/$1,415,000, 21 Altissimo Close 25d/$1,355,000 — all real, DB-verified) supporting "mispricing, not distress". Changed `chart_tension` to plot the FCI on its native scale (already 100 = baseline; ~100.3 in Q1 2023) so the endpoint reads 94.5. NOTE: our individual-sale pipeline caps at ~Feb 2026 (Domain under-capture), so verified NAMED case studies cannot be freshly sourced for Q2 without a paid tier / new scrape — existing case studies (27 Bittern, 157 Christine, 32 Outrigger, 17 North Shore) confirmed real against the DB.
**Files:** 10_Market_Report/pipeline/quarterly/index.html (@bd1b730), pipeline/generate_charts.py (@574127a), quarterly/assets/img/graph_page_09.png.
**Recurrence:** Nth (staleness/consistency class).

## [QUARTERLY-CASESTUDY-Q2] Refreshed Standoff case studies with real Q2 data (PropRadar Hobby + DB withdrawn) — 17:30 AEST
**Symptom:** Case studies were early-2026 vintage; Will re-enabled PropRadar (Hobby tier) and asked for genuinely-recent examples + a withdrawn "wait and see" home.
**Root cause:** Domain scrape under-captures Q2 individual sales; earlier I fell back to Feb-2026 verified sales.
**Fix:** PropRadar Hobby tier (~5000 calls/mo, /sold returns ADDRESS+beds+baths, not capped at 25) → pulled Q2 2026 settlement sales through 23 Jul. Rebuilt page-15 Block 2 around three seller behaviours: (1) prices held — 26 Moorabbin Pl Robina $1,640,000, 93 Coronet Cr Burleigh $1,660,000, 41 Watts Dr Varsity $1,706,000 (all July); (2) sit above market — 157 Christine; (3) withdraw-and-wait — 8 Egret Ave Burleigh, asked $2,650,000 (within $10k of Domain High-confidence est $2,660,000), 139 days, withdrawn 25 Jul. Withdrawn-home data from Gold_Coast listing_status=withdrawn (withdrawn_date, days_on_domain, price_history, domain_valuation_at_listing). 56 of 58 withdrawals across the 3 suburbs are since Q2 start — cited as choice-not-distress, tying to national record-withdrawal headlines.
**Files:** 10_Market_Report/pipeline/quarterly/index.html.
**Recurrence:** Nth (case-study freshness).

## [LEAD-FUNNEL-CYCLE4] Homeowner lead funnel cycle 4 — pauses + new fear-axis angles — 18:01 AEST
**Symptom:** Copy discovery test at $89.73 / 1,638 impr / 1 lead needed iteration: cull losers, research new angles, launch fresh concepts.
**Root cause:** Normal iteration cycle. Two clear losers identified (AN7_soldalerts_light 0% CTR on 50 impr; AN3_neighbour_dark 1.15% vs light 6.25%). Research revealed 2 untested fear axes (renovation overcapitalization + listing attention decay).
**Fix:** Paused 2 losers via Graph API. Built `build_cycle4_ads.py` — renders 4 new data cards (AN13 renoreturn + AN14 7daywindow, dark+light) and creates adsets+creatives+ads in out-of-market test campaign. All 4 ACTIVE. Campaign: 22 active / 6 paused / 28 total.
**Files:** `03_Facebook/Home_Owner_Lead_Funnel_Search/build_cycle4_ads.py` (new), `market_test_ids.json` (updated), `cycles/cycle_20260728_1801.md` (new), `00_MASTER_LEDGER.md` (updated), `creatives_test/renoreturn_{dark,light}.png` + `7daywindow_{dark,light}.png` (new).
**Recurrence:** First (cycle 4 of homeowner lead funnel test)

## [CRM-SELLER-INTENT] New seller-intent enrichment layer over lead_worklist — 21:35 AEST
**Symptom (Will flag):** CRM didn't connect a person's OWN listing status + how long it's been for sale, nor cross-reference the other CURRENT listings they viewed. The base pipeline's `property.status_age_days` measured data-freshness (showed -1.0 / 0), NOT time-on-market. And "own home not listed but browsing live listings → probable seller" wasn't inferred anywhere.
**Root cause:** lead_intelligence.py resolves only the lead's own property (from report_slug/address) and scores on that; it never joins organic_journeys page-trails to live listings, and never computes real days-on-market.
**Fix:** New `scripts/samantha/seller_intent.py` — a non-invasive layer that writes `lead_worklist.seller_intent` WITHOUT touching the base pipeline's priority/reason/signals. Per person it: (1) resolves own home (report_slug→full gc_doc for real days-on-market, else base-pipeline property for status); (2) joins organic_journeys.distinct_id→pages, extracts /property//your-home//off-market//sold/ slugs, resolves each via url_slug across ALL 96 GC suburb collections (suffix-match), records status+price+days-on-market; (3) draws a label: on_market_stale (≥90d = frustrated vendor), on_market_active, pre_market_withdrawn, browsing_while_unlisted (home identified + not-listed + viewing a listing = probable seller), viewing_listings_home_unknown (anon viewer — softer). Ran --all: 196 leads, 13 cross-signals. Validated on 41 Quambone Worongary (own for_sale 378d @ $5.15-5.65M Harcourts + viewed 23 Winton Tce Varsity, also Harcourts). 157 Christine (Quarterly's passed-in home) surfaced as on_market_stale 131d.
**Files:** scripts/samantha/seller_intent.py (pushed @f28579d).
**Recurrence:** First. NOTE: not yet scheduled — if cronned nightly it MUST wrap in job_run (Rule 7); recommended integration = append to the nightly lead_intelligence run so it inherits that heartbeat.

## [LEADS-SHEET-SITUATION] Live Leads Tracker gains a seller-intent "Situation" column — 21:55 AEST
**Ask (Will):** surface all potential leads to the Live Leads sheet (1mRjT...) WITH full contextual detail (the "meaning" — own home listed X days at $Y, withdrawn, etc.) so follow-up can be personalised.
**Fix:** live_leads_to_sheet.py now imports scripts/samantha/seller_intent.py and, per lead, computes a "Situation" line LIVE (always fresh — no cron-ordering dependency): PRIORITY, own home listing status + real days-on-market + price/advertising-method + agency, the live listings they ALSO viewed [addr, price, DOM], occupancy + years held, and the tailored seller-intent conclusion. Added as new column N (appended — NOTHING shifts, so Will's manual Status/notes edits are safe), auto-refreshed in place each run like Selling Plan (matched by hidden Lead ID col M). Real run: header set, 142 existing rows backfilled, verified live (e.g. 41 Quambone: for_sale 378d @ $5.15-5.65M Harcourts + viewing 23 Winton Tce). Bonus: for no-figure listings the price field carries the METHOD (EOI / Deadline Sale / Auction) — captured automatically.
**Files:** scripts/live_leads_to_sheet.py (pushed). No new cron — rides the existing 23:55 live-leads job (no Rule 7 obligation).
**Recurrence:** builds on [CRM-SELLER-INTENT] same day.

## [CRM-COVERAGE-HEARTBEAT] All worklist leads on sheet + nightly seller_intent heartbeat — 22:10 AEST
**Ask (Will):** (1) surface EVERY potential lead to the Live Leads sheet, (2) keep the stored seller_intent fresh nightly + self-monitored.
**Fix (1):** live_leads_to_sheet.py `worklist_only_rows(db)` emits a row for every real lead_worklist lead whose origins are NOT already covered by fb/ayh/offmarket (newsletter subscribers, direct-appraisal-request, 5-Property-Friday, price-alert, website-feedback, form-submission). Deduped by `worklist:<lead_key>` in the ledger; Situation computed via the new "worklist:" prefix in _worklist_doc_for. Real run added the 8 previously-invisible leads (incl. a Price-Alert lead whose own home is for sale, and a Direct-Appraisal-Request). Known edge: a subscriber who LATER submits AYH would appear twice (worklist row + property_reports row) — rare, accepted.
**Fix (2):** run_lead_intel.sh (cron 02:00) now runs lead_intelligence.py THEN `seller_intent.py --all`. seller_intent main() wraps the scheduled --all path in `job_run("seller_intent", cadence_hours=24, ...)` → self-registers on the Process Registry (Rule 7); verified heartbeat status=success, detail="13 cross-signals". Also fixed a cosmetic "Noned on market" when a for_sale home resolves without days-on-market.
**Files:** scripts/live_leads_to_sheet.py, scripts/samantha/seller_intent.py, scripts/samantha/run_lead_intel.sh (all pushed).
**Recurrence:** builds on [CRM-SELLER-INTENT] + [LEADS-SHEET-SITUATION] same day.

## [SELLER-INTENT-V2] Behavioral + PropRadar + verbose stories + moment detection — 22:55 AEST
**Ask (Will):** analyse a week of PostHog, find deeper stories, develop seller_intent.py to draw more/deeper insights, surface ALL potential leads, detect the MOMENT selling intent appears, output VERBOSE stories to the Live Leads sheet, and enrich on demand via PropRadar.
**Analysis:** swept 45d PostHog. Signal taxonomy the old version ignored: `analyse_home_build_complete` (address + duration), `minisite_tab`, `v3_seller_anchor_view`, `forsale_ladder_optin/answer`, `price_alert_impression`, `/sell-now` landings, `address_search.search_query`, return-session count. Surfaced a missed Robina lead (120 Glen Eagles — owner valued own home 3× + searched a neighbour).
**Build (seller_intent.py v2):** (1) `load_behavioral` — one HogQL sweep → BEHAV{distinct_id} (⚠ needs LIMIT 50000; HogQL default-caps 100). (2) `behavioral_score` weighting strongest seller signals. (3) `pr_enrich` — PropRadar property search→detail→history (DOM/valuation/PSP/last-sale/price-cuts), rationed via `--max-pr` (30/run). (4) `engaged_owner_researching` label (unlisted owner valuing own home). (5) `build_story` verbose paragraph (who/what/meaning/approach) + `hotness` + `moment` (fresh strong signal ≤3d). Run processes hottest-first. (6) sheet `format_situation` renders the full story with [NOW]-moment prefix; reads STORED seller_intent (nightly-budgeted PropRadar) not live, so the sheet never blows PropRadar quota. Bugs fixed: HogQL 100-cap, doubled-scripts PropRadar import path.
**Files:** scripts/samantha/seller_intent.py @681cf36, scripts/live_leads_to_sheet.py @14cefa0. PropRadar property endpoints ARE on Hobby (memory was wrong — corrected).
**Recurrence:** extends [CRM-SELLER-INTENT] chain. TODO: surface anonymous high-signal behavioral people not yet in the worklist.

## [SELLER-INTENT-V3] Anonymous surfacing + just-listed inbound trigger + recently-modified unlock — 23:40 AEST
**Ask (Will):** (1) surface anonymous high-signal visitors not in the worklist; (2) unlock PropRadar Starter's "who just listed/cut/sold" feed as the strongest inbound trigger + note the /for-sale-v3 + editorial integration for Samantha.
**Build:** (1) seller_intent.py `surface_behavioral_leads` — creates worklist entries (key `behavior:<did>`) for anonymous visitors passing a genuine intent gate (`_anon_qualifies`: AYH build / sell-now landing / ladder optin / seller_anchor>=3 / 4+ sessions with property views). Ran --all → 9 surfaced incl. Will's example 019f6ad1 (8 sessions, 11 seller-anchor views, ladder optin). They flow to the sheet via worklist_only_rows ("Site Behaviour (anon)"). (2) `just_listed` flip trigger in analyze() — if a tracked home was not-listed last run and is now on-market, fires a Telegram alert (`_notify_just_listed`) + a "Their home JUST LISTED" moment + hotness+50. (3) propradar_client.py `recently_modified(since, event_type, postcodes, ...)` helper unlocks the Starter feed (nationwide, 50/page cursor, postcode/state params ignored server-side → filtered client-side; no street address in summary). (4) Samantha note appended to "Will Notes" gdoc (Docs API insertText, no rewrite) with the full /for-sale-v3 + editorial integration design + gotchas.
**Files:** scripts/samantha/seller_intent.py, scripts/propradar/propradar_client.py (pushed). Note in gdoc 14U5UkX...
**Recurrence:** completes the seller_intent v2/v3 arc this session.

## [SELLER-INTENT-V4] Listing reframe (already-listed != lead), Form 6 expiry monitor, label corrections — 00:30 AEST
**Ask (Will):** (a) a home already listed is NOT a lead (committed to a competitor) — they become a lead as the listing nears expiry; build a monitor that captures near-expiry listings as leads + alerts. (b) what are "price alerts" / the "buyer/seller quiz"?
**Corrections:** price_alert_impression = the signup box was SHOWN (impression), NOT set — price_alert_submit_success=0 people ever. forsale_ladder is a /for-sale-v3 BUYER preferences quiz (not buyer/seller); forsale_ladder_optin fires on DECLINE too (only 1 real opt-in in 45d). Fixed load_behavioral to use price_alert_submit_success + forsale_ladder_complete{opted_in:true}; relabelled stories ("answered the buyer preferences quiz", "opted into the weekly buyer email"). Removed the false "set N price alerts".
**Reframe:** `listing_stage(dom)` — QLD Form 6 sole/exclusive residential sales appointment maxes at 90 days (POA 2014). Labels: on_market_fresh (<69d, NOT a lead, hot −6), on_market_expiring (within 21d of a 90-day boundary = PRIME lead, hot +22), on_market_stale (past term, lead, +12). Reclassified with PropRadar CURRENT-campaign DOM (caught 157 Christine: our data 131d but relisted 44d ago → fresh, NOT a lead). Inverted just_listed (a tracked lead listing = committed to competitor, flagged for expiry watch, no hotness boost). `listing_expiry_monitor(sm, gc_db)` scans core-suburb for_sale listings, captures on_market_expiring as `listing:<slug>` leads (24 found) + Telegram alert (soonest first); runs in nightly --all.
**Files:** scripts/samantha/seller_intent.py, scripts/live_leads_to_sheet.py (pushed).
**Recurrence:** corrects + extends the seller_intent arc.
