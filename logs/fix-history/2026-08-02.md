
---

## [WATCHDOG] Step 6 (Batch Valuate Properties) — 01:06 AEST

**Failure class:** UNKNOWN
**Cause:** No recognizable error pattern — step failed with non-zero exit code
**Action taken:** ESCALATED to Claude (id=watchdog_6_1785596778)
**Result:** queued — awaiting repair-agent or Claude

---

## [PULSE-FIVE-YEAR-INDEX-MISLABEL] Suburb-compare ranked on index levels, not five-year growth — 07:00 AEST

**Symptom:** The August suburb-compare summaries on `/market-intelligence/:suburb/suburb-compare`
rank the three suburbs on a "five-year index" that is not a five-year growth rate. Live text says
Burleigh Waters' "index gain of 61.5 exceeds Robina's 52.41"; on actual five-year median growth
Burleigh Waters is **last** of the three, not ahead of Robina. The published ranking is inverted.

**Root cause:** `build_data_snapshot()` in `generate_market_pulse.py` emitted a field named
`five_year_index` holding `indexed_series[-20].index_value` — the index *level* as at ~5 years ago,
where the index is cumulative % growth since a Q3 2016 baseline. Two problems:
1. **Mislabel.** The name reads as a five-year growth rate, so summaries compared
   `latest_index - five_year_index` and reported the result as five-year growth. That difference is
   a percentage-*point* gap on a 2016 base, which ranks suburbs differently from actual growth.
   Index deltas put Burleigh Waters top (96.6pp); true 5yr median growth puts it last.
2. **Off-by-one.** `[-20]` is 19 quarters back from the latest, not 20 — Q3 2021 against a Q2 2026
   latest, i.e. 4.75 years, and it landed on a thin quarter where Burleigh Waters swings from
   76.71 (Q2 2021) to 61.50 (Q3 2021).

**Fix:** Replaced the single ambiguous field with two explicitly-named measures —
`index_since_baseline` (+ `index_baseline_period`) for the level, and `five_year_growth_pct`
(+ `five_year_from_period`, `five_year_from_median`, `five_year_basis`) for the rate. Growth is now
computed off `rolling_12m_median_series` with `is_in_progress` entries excluded, so a single thin
quarter can't swing it, falling back to raw quarters only if the rolling series is missing. Indexing
corrected to `[-(20+1)]`. Added the same corrected block to `manual_market_pulse.py --show-data` so a
human writing summaries sees the real number, and amended the suburb-compare prompt to state that the
two measures are different and must not be mixed. Corrected ranking on 12m-rolling medians:
Varsity Lakes +93.9%, Robina +90.4%, Burleigh Waters +73.2%.

**Files:** `scripts/generate_market_pulse.py`, `scripts/manual_market_pulse.py`

**Recurrence:** First. Same failure *class* as [PROPRADAR-MEDIAN-ANCHOR-WRONG] and the March-2026
crash-risk phantom surge — a field whose name implies a different statistic than it holds.

---

## [PULSE-VERIFY-STALE-ROUTE] Rule 6 verifier pointed at the retired /market-metrics route — 07:05 AEST

**Symptom:** `verify_market_metrics_live.py` fetched `https://fieldsestate.com.au/market-metrics/...`,
retired in favour of `/market-intelligence/...` on 2026-07-31. Also `--list` was documented in the
module docstring but never added to argparse, so it exited with an argparse error.

**Fix:** Repointed `BASE_URL` to `/market-intelligence` (the 301 meant fetches still worked, but the
mandatory Rule 6 check was validating a route we no longer ship). Implemented `--list`: prints the
most recent run directory and its per-page status from `manifest.json`, flagging failures and console
errors, without re-fetching.

**Files:** `scripts/verify_market_metrics_live.py`

**Recurrence:** First.

**Note:** the most recent verify run on disk is `2026-07-27T06-35-17` and covers only 7 pages
(Robina). No full 21-page Rule 6 verification has been run since the route move.

---

## [UNION-MEDIANS-REVERTED-NIGHTLY] Nightly pipeline wiped the union medians every night — 07:40 AEST

**Symptom:** The Domain ∪ onthehouse union medians and 90% CIs promoted by the 1st-of-month chain
disappear from `Gold_Coast.precomputed_indexed_prices` within hours. Live docs had no
`median_source`, `union_from`, `method`, `coverage` or `rolling_12m_ci_*` fields at all, and
Burleigh Waters' rolling 12-month median was back to the known-inflated **$2,115,000** instead of
the union's **$1,925,000**. `/market-intelligence/burleigh-waters/overview` renders the same stat
four different ways: **$1,790,000** (hero + FAQ), **$1,710,000** (verdict block + chart caption),
**$1,877,775** (Market Pulse prose). The correct union figure appears nowhere on the page.

**Root cause:** Orchestrator step 17 "Market Narrative Pre-computation"
(`config/process_commands.yaml:314-324`) runs `RUN_PRECOMPUTE.sh` on **every** nightly pipeline run,
which invoked `precompute_indexed_price_data.py` — the same script the monthly chain runs at 05:00.
It ends in a blind full-document `replace_one({'_id': ...}, data, upsert=True)`
(`precompute_indexed_price_data.py:1044-1048`), so it clobbers rather than merges, deleting every
union-owned field. `precompute_market_charts.py` on the next line did the same to
`precomputed_market_charts`. Confirmed by the step's own log: `logs/runs/2026-08-01T20-30-44_completed/
21_step_17_market_narrative_pre_computation/stdout.log:245` reports `growth=135.2%` for Robina at
23:44-23:49 AEST, exactly the value now live, ~6 hours after the 17:57 union promote.

Disguised by a timezone mismatch: `last_updated` is written as naive **UTC**
(`precompute_indexed_price_data.py:794,952` use `datetime.utcnow()`) while the same file uses
`datetime.now()` elsewhere and `market_pulse.generated_at` is naive local. The revert timestamp
therefore reads as `13:46` — an innocuous afternoon write — when it is 23:46 AEST.

Guard existed but was incomplete: the crontab comments (lines 173, 239-241) and the docstring in
`precompute_union_prices.py:499-503` both name only the `0 5 1 * *` cron as the thing that must not
run after the promote. Neither accounts for the pipeline invoking the identical script nightly. The
earlier `[UNION-MEDIANS-NOT-DURABLE]` entry (2026-08-01 17:58) diagnosed this class but concluded the
revert would next land "on 1 September" — it missed the nightly path, so the data was already reverted
that same night.

**Fix:** Removed both precomputes from `RUN_PRECOMPUTE.sh`, leaving a comment block explaining why
they must not be re-added. They are fully redundant with the monthly crons — `0 5 1 * *` for the
indexed prices and `8/16 5 1 * *` covering all four chart types (`dom cycle` + `volume turnover`) —
the data is quarterly so it cannot change between nights, nothing in `process_commands.yaml` depends
on step 17, and the crontab states the intent outright ("1st of month only — aligns charts with
monthly commentary"), as does the live page footer ("Updated monthly on the 1st").

**Files:** `/home/fields/Feilds_Website/08_Market_Narrative_Engine/RUN_PRECOMPUTE.sh` (backup at
`.bak-2026-08-02`; was untracked — now pushed to `Website_Version_Feb_2026`)

**Recurrence:** 2nd of the same class as `[PROPRADAR-MEDIAN-ANCHOR-WRONG]` / `[UNION-MEDIANS-NOT-DURABLE]`
— a blind `replace_one` reverting a deliberately-corrected document.

**Restore (done, 07:09 AEST):** Re-ran `precompute_union_prices.py` with no `--promote` first and
diffed it — the fresh run reproduced the 2026-08-01 figures exactly (Burleigh Waters $1,925,000, CI
$1,855,550-$2,000,000, n=167), confirming the union computation was stable and only the live doc had
been reverted. Then promoted. Live now carries `median_source=domain_union_onthehouse` for all three
suburbs with `median_computed_at` post-dating `last_updated`. Burleigh Waters' rolling median went
$2,115,000 → $1,925,000 (-9.0%) and its year-on-year on the live page went **+23.6% → +6.9%**, which
is the phantom-growth correction landing.

Note: promoting the *staging* document would have been wrong. It is a partial doc holding only
union-owned fields, on a different `baseline_period` (Q1 2020 vs live Q3 2016); `--promote` does a
field-level `$set` onto the live doc, which is the correct mechanism.

**Tripwire (done, 07:12 AEST):** Added `scripts/check_union_median_integrity.py`, wrapped in
`job_run(cadence_hours=24)` per Rule 7, cronned `30 1 * * *` (after the nightly pipeline). Asserts per
core suburb that `median_source` is the union, that `median_computed_at >= last_updated`, and that the
CI + sample size survive. Seeded with a real run (heartbeat present, `self_registered: true`). Failure
path tested on a throwaway `_id` — a doctored "reverted" doc produced all 3 expected failures and a
healthy doc produced none; test doc deleted. The cron line was also verified end-to-end from a foreign
cwd under a bare environment, because the first version of it had the missing-`cd` bug that kept
`refresh_property_timelines.py` from ever running (see [[market_metrics_data_bugs_2026-07]]).

---

## [PULSE-PROPRADAR-MEDIAN-OVERRIDE-STALE] Prose still used the median anchor the charts dropped — 07:20 AEST

**Symptom:** `/market-intelligence/burleigh-waters/overview` showed $1,710,000 as "the median house
price" in the verdict block and chart caption while the hero showed a different figure. $1,710,000
came from PropRadar — whose `propradar_suburb_stats.burleigh_waters.median_price` is now **`None`**.

**Root cause:** `fetch_all_data()` in `generate_market_pulse.py` still overrode `current_median_price`
and `yoy_growth_pct` with PropRadar's, a block written when PropRadar was the median authority. On
2026-08-01 that anchor was removed from the chart pipeline (`recalibrate_charts.py`
`RECALIBRATE_MEDIAN = False`, log: "medians owned by precompute_union_prices.py") because it
substituted a median over a different dwelling population. The pulse generator was never updated, so
prose and charts disagreed by construction. It also stamped `headline_stats_source: "propradar"` even
when every PropRadar field it read was null.

**Fix:** Median and YoY now come from the union (`rolling_12m_median_price`, `rolling_12m_yoy_pct`),
same source as the charts. Exposed `median_12m` with its 90% CI, sample size and `median_source` so
summaries can state the limitation. `current_median_price` kept but labelled
`current_median_price_basis: "latest complete quarter"` — a different statistic from the 12-month
median, and conflating the two is what put multiple figures on one page. PropRadar retained for
volume-side stats only (`inventory_months`, `sales_12mo`), and `headline_stats_source` now reports
what was actually used.

**Files:** `scripts/generate_market_pulse.py`

**Recurrence:** First.

---

## [PULSE-SPARSE-ROLLING-SERIES] Five-year growth read a sparse series by position — 07:30 AEST

**Symptom:** Caught while verifying the `[PULSE-FIVE-YEAR-INDEX-MISLABEL]` fix: it reported Robina
five-year growth of 129.2% "from Q4 2017" and Varsity Lakes 159.3% "from Q2 2015" — neither five years
before the Q2 2026 latest.

**Root cause:** My own fix assumed `rolling_12m_median_series` is a contiguous quarterly series and
indexed it positionally (`[-21]`). It is not. `precompute_union_prices.py` drops any quarter it could
not recompute on the union basis ("leave it out rather than mix bases"), so after a promote the series
is sparse and irregular — Robina's surviving points run Q3 1991, Q3 1992, Q4 1992, Q2 1993 … with
multi-year gaps, 93 entries spanning 35 years. A fixed offset therefore lands on an arbitrary period.
The original `[-20]` bug was masked because it ran before the promote, on a dense series.

**Fix:** New `scripts/market_series.py` matches the five-year point on the period **label**
(Q2 2026 → Q2 2021) and returns `None` when that exact quarter is absent, rather than substituting a
nearby one. Both `generate_market_pulse.py` and `manual_market_pulse.py --show-data` use it. Corrected
five-year growth, all matched Q2 2021 → Q2 2026: **Varsity Lakes +115.1%, Burleigh Waters +75.4%,
Robina +75.3%** — so Robina and Burleigh Waters are a statistical tie (0.1pp), not the clear ordering
the published prose asserts.

**Files:** `scripts/market_series.py` (new), `scripts/generate_market_pulse.py`,
`scripts/manual_market_pulse.py`

**Recurrence:** First. Lesson: a fix verified only against pre-promote data can be wrong against
post-promote data — the union merge changes the *shape* of the series, not just its values.

**Still open (for the joint session):**
1. The 21 August summaries are still the 2026-08-01 auto-generated set, written from the reverted
   data. The stale $1,710,000 and +12.2% YoY on the page come from their `data_snapshot` and clear
   only when the prose is rewritten.
2. Display decision for Will: the hero and FAQ/JSON-LD label the latest **quarterly** median
   ($1,877,775) as "the median house price", while the 12-month union median with a CI and n=167
   ($1,925,000) — the defensible figure — is not shown anywhere on the page.
3. Durable fix still open: convert the `replace_one` at
   `precompute_indexed_price_data.py:1044-1048` to a merge preserving union-owned fields. The nightly
   path no longer runs it and the tripwire now catches a revert within a day, so this is no longer
   urgent, but any manual run still reverts the medians.

---

## [COVERAGE-SUBURB-DRIFT] Coverage check tested 8 suburbs while scraper ran 6 — 07:10 AEST

**Symptom:** Step 109 reported `ERROR Merrimac (missing 16 properties)` and
`ERROR Carrara (missing 4 properties)` every night. Neither suburb is scraped by step 101,
so the gap could never close — two permanent false ERRORs on the nightly board.

**Root cause:** The suburb list was hardcoded twice. `config/settings.yaml` `target_market.suburbs`
(what step 101 scrapes) had Merrimac and Carrara commented out, but step 109's `--suburbs "..."`
literal in `config/process_commands.yaml` still listed all 8. The two lists were free to drift and did.

**Fix:** Added `load_suburbs_from_settings()` + a `--from-config` flag to `scripts/coverage_check.py`
that reads `target_market.suburbs` directly, and changed step 109's command to
`--from-config --no-fail`. One source of truth — the check can no longer test a suburb nothing scrapes.
Verified: 6 suburbs checked, 0 gaps.

**Files:** scripts/coverage_check.py, config/process_commands.yaml
**Recurrence:** First

---

## [COVERAGE-DOMAIN-VARIANT] Burleigh Waters silently reported DOMAIN_UNAVAILABLE — 07:10 AEST

**Symptom:** 2026-08-01 run: `WARN Burleigh Waters Domain=None DB=54 (DOMAIN_UNAVAILABLE)`.
The one suburb that was also short on discovery (51/54 in step 101) was the one with no
independent count to verify against, and nothing was retained to diagnose why.

**Root cause:** The fetch succeeded but the returned page matched neither count regex. Domain serves
at least two page variants for the same URL — a ~500KB one carrying only the
`<h1 data-testid="summary">` heading, and a ~2MB one that also embeds `"totalResults"`. Extraction
failure was terminal (no retry, since only the *fetch* was retried) and discarded the body.

**Fix:** Replaced the two ad-hoc regexes with a `COUNT_PATTERNS` list covering all four known shapes
(summary heading, bare `<strong>`, `"totalResults"` JSON, generic `>N Properties<`), moved the retry
loop to wrap *extraction* rather than just the fetch, and on total failure the last body is written to
`logs/coverage_unparseable_<slug>.html` so the next occurrence is diagnosable. Re-ran: Burleigh Waters
now `OK Domain=54 DB=54`.

**Files:** scripts/coverage_check.py
**Recurrence:** First

---

## [BLOB-DEAD-HOST-VISION] Step 106 burned 54 floor-plan analyses on a retired Azure host — 07:10 AEST

**Symptom:** 2026-08-01 step 106 logged 54 × `403 Client Error: The specified account is disabled`
against `fieldspropertyimages.blob.core.windows.net`, failing floor-plan vision for those properties.
Step exited 0, so the run reported 26/26 success and the loss was invisible.

**Root cause:** The Azure `fieldspropertyimages` account was retired 2026-05-28 and the pipeline moved
to `BLOB_BACKEND=local` (nginx over /data/blobs at `blobs.fieldsestate.com.au`, identical paths), but
~20k stored documents still carry the old Azure URLs. The website already handles this —
`toLiveBlobUrl()` in `netlify/functions/shared-utils.mjs` rewrites the host. The Python pipeline had
no equivalent, so vision steps fetched the dead host directly.

**Fix:** Added `to_live_url()` to `shared/blob_storage.py` (Python mirror of the website's
`toLiveBlobUrl`, rewriting known-dead hosts to `BLOB_PUBLIC_BASE_URL` on the same path) and applied it
in `_download_and_encode_image()` in the floor-plan vision client. Verified end-to-end: a URL that
403'd last night now downloads and encodes (452KB PNG).

**Files:** shared/blob_storage.py,
Property_Data_Scraping/03_Gold_Coast/Gold_Coast_Wide_Currently_For_Sale_AND_Recently_Sold/Ollama_Property_Analysis/openai_floorplan_client.py
**Recurrence:** First (related: [STEP105-DICT-IMAGES] — same class, vision step failing silently while the run reports success)

## [HERO-PIXEL-REVEAL] Hero photo pixel-reveal concept build — 17:35 AEST
**Symptom:** N/A — new concept, not a fix. Will asked to explore a pixel-block reveal of a house hero photo, sweeping from bottom-left out toward top-right.
**Root cause:** N/A.
**Fix:** Built a canvas reveal engine: per-cell delay field (diagonal projection blended with radial distance from the origin corner, plus deterministic jitter), a pre-rendered pixelation stack so each cell *sharpens* from 64px blocks to full detail rather than just fading, and a settled-layer optimisation so each frame only draws cells currently in flight. Shipped as an interactive demo (3 real listing photos, live controls) and a drop-in React component. Component typechecks clean under the website's tsconfig.app.json; demo verified headlessly in both light and dark themes with zero console errors.
**Files:** 15_Off-Market/Concepts/Hero_image_reveal/{README.md, PixelReveal.tsx, pixel-reveal-demo.html, pixel-reveal-demo.template.html, photos/*, mid-reveal-still.png}
**Artifact:** https://claude.ai/code/artifact/b831909e-9faa-4e52-bb62-99e70b99ed3d
**Recurrence:** First

---

## [PALM-PIXEL-REVEAL] Pixel-reveal animation for the Pandanus hatch sketch — 08:05 AEST

**Symptom:** N/A — new concept asset, not a repair.

**Built:** A self-contained pixel-reveal animation in
`15_Off-Market/Concepts/Near_beach_palm_reveal/` that constructs a black-and-white
version of `Hatch_Sketch_Pandanas_Palm.png`. Coarse pixel blocks arrive and resolve
into the fine hatch, so the ink appears to grow rather than fade in. Five reveal
orders (growth / develop / dissolve / rise / sweep), light and dark paper, live
controls, plus a deterministic headless-Chrome renderer for MP4/GIF/stills.

**Two things worth recording, both caught by measuring rather than eyeballing:**
1. The source paper is a flat **254**, not 255, and 71.75% of pixels sit there. A
   percentile-based white point (the obvious first cut) mapped 255→255 and left a
   uniform alpha=1 haze over the entire background. That haze put every "empty"
   block at density 0.0039 against a 0.004 skip threshold — the whole animation was
   one rounding step away from treating 67% of the canvas as ink. Fixed by taking
   the white point as the **modal** bright tone and flattening sub-0.014 ink to zero.
2. The ink bounding box was being set by a single stray speck at y=1279, padding the
   crop with ~100px of dead paper. Fixed by requiring ≥3 inked pixels in a row/column
   before it can define the bbox.

**Design note:** the master is written as an RGBA **alpha mask** (RGB black, alpha =
coverage) rather than black-on-white, so blocks composite onto any paper colour
without painting white squares over the background — that is what makes the dark
theme possible from the same asset.

**Files:** `15_Off-Market/Concepts/Near_beach_palm_reveal/{build_master.py,
reveal.template.html,render_video.js,README.md}` (+ generated `index.html`,
`palm_bw.png`, `palm_ink_alpha.png`, `palm_reveal_growth.{mp4,gif}`,
`palm_reveal_growth_dark.mp4`)

**Recurrence:** First

**Follow-up — 08:20 AEST:** two things found while verifying, both fixed and pushed.
- `getImageData` was called on the mask canvas on every grid rebuild (each pixel-size
  change), which Chrome warns about and which would have dragged the source canvas onto
  the software path — it is only ever *drawn from* after load. Now the alpha channel is
  read back once into a `Uint8Array` at load.
- `render_video.js` leaked its temp frame directory whenever `--stills` was used (the
  early `return` skipped the cleanup) and whenever ffmpeg threw. Temp dir is now only
  created when frames are actually written, and removal moved into a `finally`.
Re-verified in headless Chrome: no console messages, mode/theme switching and backward
scrubbing (the full-rebuild path) all correct.

---

## [RAPID-MINISITE-BUILD] Card-10 pixel-reveal build box — concept + prototype — 17:52 AEST

**Symptom:** N/A — new concept, not a repair. Will asked to explore a box that
forms under the /off-market discovery deck's card 10 (strategy card) via a
"matrix-style" non-uniform pixel reveal, then behaves as a popup text box.

**Incidental finding (real bug, not fixed here):** card 10's CTA is a dead link.
`DiscoveryDeck.tsx:278` renders `href="#build-strategy"`, and
`grep -rn "build-strategy" src netlify` returns only that line — no element with
that id exists anywhere. Every visitor who scrolls the full deck and clicks
"Build my complete selling strategy" gets a no-op, while `forward_cta_clicked`
and the Telegram intent alert still fire, so it looks healthy in analytics.
Left in place deliberately (concept work only, no live-deck edits); logged as
open question / integration step 4 in the concept README.

**Built:** Standalone tunable prototype (`prototype.html`) reproducing the deck's
exact tokens: 1.5s dwell on card 10 → downward-biased jittered pixel fill →
copper border drawn as four sequenced strokes → consolidate to solid birch paper
→ live 16-step build feed (the real `STEP_ORDER` from
`property-report-progress.mjs`). Bottom-left panel tunes dwell/cell size/fill/
jitter/leader-%/downward-bias and replays.

**Three defects found and fixed during prototyping** (all would look like flaws
in the idea rather than the code):
1. *Node count* — 13px cells over a 900×540 box = ~2,800 animating divs. Added a
   `CELL_BUDGET` (900) that coarsens the cell until it fits; coarser reads more
   Matrix, so the degradation is free.
2. *Vertical seams* — `repeat(N, 1fr)` lands cell edges on fractional pixels
   (measured 23.13px/cell, lefts at 183.31/206.43/229.56/252.70/275.82…), and the
   rounding beat drew bright hairlines every ~185px down the panel, exactly at the
   predicted boundaries. `transform: scale(1.06)` is NOT sufficient (~0.7px of
   overlap at this cell size — seams survived it and I verified that before
   moving on). Fixed with `box-shadow: 0 0 0 1px var(--paper)` per cell for a
   guaranteed 1px bleed, plus `overflow:hidden` on the grid to clip it.
3. *Dead air* — unclamped jitter left stragglers landing ~400ms after the wave, so
   consolidation waited for them and showed ~1s of empty white paper before any
   content. Fixed by clamping every cell delay to a horizon
   (`fill + jitter × 0.45`) and consolidating exactly at it, content +140ms.

**Verified:** headless at 900×900 and 390×844 DPR2, screenshotted across the whole
sequence; 851 cells; seams confirmed gone mid-fill (not just after consolidate);
no JS errors. Mobile pass surfaced two design problems recorded in the README —
the gap above the box is much worse at 390px, and the 16-step panel overflows a
phone viewport so most of the reveal happens off-screen.

**Note:** `Concepts/Hero_image_reveal/PixelReveal.tsx` (built the same day, ~20 min
earlier by a concurrent session) is a canvas pixel-reveal for hero *photos*. Not
directly reusable here (needs an image src), but its canvas engine + deterministic
seeded RNG is the better foundation for the React port than this DOM grid —
canvas avoids both defects 1 and 2 by construction. Recorded in the README.

**Files:** `15_Off-Market/Concepts/Rapid_Mini_Site_Buid/{README.md,prototype.html}`
(+ copy at `/home/fields/offmarket-preview/rapid-build.html` for viewing)

**Recurrence:** First
