
---

## [WATCHDOG] Step 6 (Batch Valuate Properties) — 01:06 AEST

**Failure class:** UNKNOWN
**Cause:** No recognizable error pattern — step failed with non-zero exit code
**Action taken:** ESCALATED to Claude (id=watchdog_6_1785596778)
**Result:** queued — awaiting repair-agent or Claude

---

## [PULSE-FIVE-YEAR-INDEX-MISLABEL] Suburb-compare ranked on index levels, not five-year growth — 07:00 AEST

**Symptom:** The August suburb-compare summaries on `/market-intelligence/:suburb/suburb-compare`
rank the three suburbs on a "five-year index" that is not a five-year growth rate. Live text says
Burleigh Waters' "index gain of 61.5 exceeds Robina's 52.41"; on actual five-year median growth
Burleigh Waters is **last** of the three, not ahead of Robina. The published ranking is inverted.

**Root cause:** `build_data_snapshot()` in `generate_market_pulse.py` emitted a field named
`five_year_index` holding `indexed_series[-20].index_value` — the index *level* as at ~5 years ago,
where the index is cumulative % growth since a Q3 2016 baseline. Two problems:
1. **Mislabel.** The name reads as a five-year growth rate, so summaries compared
   `latest_index - five_year_index` and reported the result as five-year growth. That difference is
   a percentage-*point* gap on a 2016 base, which ranks suburbs differently from actual growth.
   Index deltas put Burleigh Waters top (96.6pp); true 5yr median growth puts it last.
2. **Off-by-one.** `[-20]` is 19 quarters back from the latest, not 20 — Q3 2021 against a Q2 2026
   latest, i.e. 4.75 years, and it landed on a thin quarter where Burleigh Waters swings from
   76.71 (Q2 2021) to 61.50 (Q3 2021).

**Fix:** Replaced the single ambiguous field with two explicitly-named measures —
`index_since_baseline` (+ `index_baseline_period`) for the level, and `five_year_growth_pct`
(+ `five_year_from_period`, `five_year_from_median`, `five_year_basis`) for the rate. Growth is now
computed off `rolling_12m_median_series` with `is_in_progress` entries excluded, so a single thin
quarter can't swing it, falling back to raw quarters only if the rolling series is missing. Indexing
corrected to `[-(20+1)]`. Added the same corrected block to `manual_market_pulse.py --show-data` so a
human writing summaries sees the real number, and amended the suburb-compare prompt to state that the
two measures are different and must not be mixed. Corrected ranking on 12m-rolling medians:
Varsity Lakes +93.9%, Robina +90.4%, Burleigh Waters +73.2%.

**Files:** `scripts/generate_market_pulse.py`, `scripts/manual_market_pulse.py`

**Recurrence:** First. Same failure *class* as [PROPRADAR-MEDIAN-ANCHOR-WRONG] and the March-2026
crash-risk phantom surge — a field whose name implies a different statistic than it holds.

---

## [PULSE-VERIFY-STALE-ROUTE] Rule 6 verifier pointed at the retired /market-metrics route — 07:05 AEST

**Symptom:** `verify_market_metrics_live.py` fetched `https://fieldsestate.com.au/market-metrics/...`,
retired in favour of `/market-intelligence/...` on 2026-07-31. Also `--list` was documented in the
module docstring but never added to argparse, so it exited with an argparse error.

**Fix:** Repointed `BASE_URL` to `/market-intelligence` (the 301 meant fetches still worked, but the
mandatory Rule 6 check was validating a route we no longer ship). Implemented `--list`: prints the
most recent run directory and its per-page status from `manifest.json`, flagging failures and console
errors, without re-fetching.

**Files:** `scripts/verify_market_metrics_live.py`

**Recurrence:** First.

**Note:** the most recent verify run on disk is `2026-07-27T06-35-17` and covers only 7 pages
(Robina). No full 21-page Rule 6 verification has been run since the route move.

---

## [UNION-MEDIANS-REVERTED-NIGHTLY] Nightly pipeline wiped the union medians every night — 07:40 AEST

**Symptom:** The Domain ∪ onthehouse union medians and 90% CIs promoted by the 1st-of-month chain
disappear from `Gold_Coast.precomputed_indexed_prices` within hours. Live docs had no
`median_source`, `union_from`, `method`, `coverage` or `rolling_12m_ci_*` fields at all, and
Burleigh Waters' rolling 12-month median was back to the known-inflated **$2,115,000** instead of
the union's **$1,925,000**. `/market-intelligence/burleigh-waters/overview` renders the same stat
four different ways: **$1,790,000** (hero + FAQ), **$1,710,000** (verdict block + chart caption),
**$1,877,775** (Market Pulse prose). The correct union figure appears nowhere on the page.

**Root cause:** Orchestrator step 17 "Market Narrative Pre-computation"
(`config/process_commands.yaml:314-324`) runs `RUN_PRECOMPUTE.sh` on **every** nightly pipeline run,
which invoked `precompute_indexed_price_data.py` — the same script the monthly chain runs at 05:00.
It ends in a blind full-document `replace_one({'_id': ...}, data, upsert=True)`
(`precompute_indexed_price_data.py:1044-1048`), so it clobbers rather than merges, deleting every
union-owned field. `precompute_market_charts.py` on the next line did the same to
`precomputed_market_charts`. Confirmed by the step's own log: `logs/runs/2026-08-01T20-30-44_completed/
21_step_17_market_narrative_pre_computation/stdout.log:245` reports `growth=135.2%` for Robina at
23:44-23:49 AEST, exactly the value now live, ~6 hours after the 17:57 union promote.

Disguised by a timezone mismatch: `last_updated` is written as naive **UTC**
(`precompute_indexed_price_data.py:794,952` use `datetime.utcnow()`) while the same file uses
`datetime.now()` elsewhere and `market_pulse.generated_at` is naive local. The revert timestamp
therefore reads as `13:46` — an innocuous afternoon write — when it is 23:46 AEST.

Guard existed but was incomplete: the crontab comments (lines 173, 239-241) and the docstring in
`precompute_union_prices.py:499-503` both name only the `0 5 1 * *` cron as the thing that must not
run after the promote. Neither accounts for the pipeline invoking the identical script nightly. The
earlier `[UNION-MEDIANS-NOT-DURABLE]` entry (2026-08-01 17:58) diagnosed this class but concluded the
revert would next land "on 1 September" — it missed the nightly path, so the data was already reverted
that same night.

**Fix:** Removed both precomputes from `RUN_PRECOMPUTE.sh`, leaving a comment block explaining why
they must not be re-added. They are fully redundant with the monthly crons — `0 5 1 * *` for the
indexed prices and `8/16 5 1 * *` covering all four chart types (`dom cycle` + `volume turnover`) —
the data is quarterly so it cannot change between nights, nothing in `process_commands.yaml` depends
on step 17, and the crontab states the intent outright ("1st of month only — aligns charts with
monthly commentary"), as does the live page footer ("Updated monthly on the 1st").

**Files:** `/home/fields/Feilds_Website/08_Market_Narrative_Engine/RUN_PRECOMPUTE.sh` (backup at
`.bak-2026-08-02`; was untracked — now pushed to `Website_Version_Feb_2026`)

**Recurrence:** 2nd of the same class as `[PROPRADAR-MEDIAN-ANCHOR-WRONG]` / `[UNION-MEDIANS-NOT-DURABLE]`
— a blind `replace_one` reverting a deliberately-corrected document.

**Restore (done, 07:09 AEST):** Re-ran `precompute_union_prices.py` with no `--promote` first and
diffed it — the fresh run reproduced the 2026-08-01 figures exactly (Burleigh Waters $1,925,000, CI
$1,855,550-$2,000,000, n=167), confirming the union computation was stable and only the live doc had
been reverted. Then promoted. Live now carries `median_source=domain_union_onthehouse` for all three
suburbs with `median_computed_at` post-dating `last_updated`. Burleigh Waters' rolling median went
$2,115,000 → $1,925,000 (-9.0%) and its year-on-year on the live page went **+23.6% → +6.9%**, which
is the phantom-growth correction landing.

Note: promoting the *staging* document would have been wrong. It is a partial doc holding only
union-owned fields, on a different `baseline_period` (Q1 2020 vs live Q3 2016); `--promote` does a
field-level `$set` onto the live doc, which is the correct mechanism.

**Tripwire (done, 07:12 AEST):** Added `scripts/check_union_median_integrity.py`, wrapped in
`job_run(cadence_hours=24)` per Rule 7, cronned `30 1 * * *` (after the nightly pipeline). Asserts per
core suburb that `median_source` is the union, that `median_computed_at >= last_updated`, and that the
CI + sample size survive. Seeded with a real run (heartbeat present, `self_registered: true`). Failure
path tested on a throwaway `_id` — a doctored "reverted" doc produced all 3 expected failures and a
healthy doc produced none; test doc deleted. The cron line was also verified end-to-end from a foreign
cwd under a bare environment, because the first version of it had the missing-`cd` bug that kept
`refresh_property_timelines.py` from ever running (see [[market_metrics_data_bugs_2026-07]]).

---

## [PULSE-PROPRADAR-MEDIAN-OVERRIDE-STALE] Prose still used the median anchor the charts dropped — 07:20 AEST

**Symptom:** `/market-intelligence/burleigh-waters/overview` showed $1,710,000 as "the median house
price" in the verdict block and chart caption while the hero showed a different figure. $1,710,000
came from PropRadar — whose `propradar_suburb_stats.burleigh_waters.median_price` is now **`None`**.

**Root cause:** `fetch_all_data()` in `generate_market_pulse.py` still overrode `current_median_price`
and `yoy_growth_pct` with PropRadar's, a block written when PropRadar was the median authority. On
2026-08-01 that anchor was removed from the chart pipeline (`recalibrate_charts.py`
`RECALIBRATE_MEDIAN = False`, log: "medians owned by precompute_union_prices.py") because it
substituted a median over a different dwelling population. The pulse generator was never updated, so
prose and charts disagreed by construction. It also stamped `headline_stats_source: "propradar"` even
when every PropRadar field it read was null.

**Fix:** Median and YoY now come from the union (`rolling_12m_median_price`, `rolling_12m_yoy_pct`),
same source as the charts. Exposed `median_12m` with its 90% CI, sample size and `median_source` so
summaries can state the limitation. `current_median_price` kept but labelled
`current_median_price_basis: "latest complete quarter"` — a different statistic from the 12-month
median, and conflating the two is what put multiple figures on one page. PropRadar retained for
volume-side stats only (`inventory_months`, `sales_12mo`), and `headline_stats_source` now reports
what was actually used.

**Files:** `scripts/generate_market_pulse.py`

**Recurrence:** First.

---

## [PULSE-SPARSE-ROLLING-SERIES] Five-year growth read a sparse series by position — 07:30 AEST

**Symptom:** Caught while verifying the `[PULSE-FIVE-YEAR-INDEX-MISLABEL]` fix: it reported Robina
five-year growth of 129.2% "from Q4 2017" and Varsity Lakes 159.3% "from Q2 2015" — neither five years
before the Q2 2026 latest.

**Root cause:** My own fix assumed `rolling_12m_median_series` is a contiguous quarterly series and
indexed it positionally (`[-21]`). It is not. `precompute_union_prices.py` drops any quarter it could
not recompute on the union basis ("leave it out rather than mix bases"), so after a promote the series
is sparse and irregular — Robina's surviving points run Q3 1991, Q3 1992, Q4 1992, Q2 1993 … with
multi-year gaps, 93 entries spanning 35 years. A fixed offset therefore lands on an arbitrary period.
The original `[-20]` bug was masked because it ran before the promote, on a dense series.

**Fix:** New `scripts/market_series.py` matches the five-year point on the period **label**
(Q2 2026 → Q2 2021) and returns `None` when that exact quarter is absent, rather than substituting a
nearby one. Both `generate_market_pulse.py` and `manual_market_pulse.py --show-data` use it. Corrected
five-year growth, all matched Q2 2021 → Q2 2026: **Varsity Lakes +115.1%, Burleigh Waters +75.4%,
Robina +75.3%** — so Robina and Burleigh Waters are a statistical tie (0.1pp), not the clear ordering
the published prose asserts.

**Files:** `scripts/market_series.py` (new), `scripts/generate_market_pulse.py`,
`scripts/manual_market_pulse.py`

**Recurrence:** First. Lesson: a fix verified only against pre-promote data can be wrong against
post-promote data — the union merge changes the *shape* of the series, not just its values.

---

## [MEDIAN-ONE-LABEL-ONE-NUMBER] One "median house price" per page, on one basis — 08:20 AEST

**Symptom:** `/market-intelligence/burleigh-waters/overview` showed the same stat as $1,790,000
(hero + FAQ), $1,710,000 (verdict block + chart caption) and $1,877,775 (prose), while the
defensible 12-month union median ($1,925,000, 90% CI, n=167) appeared nowhere. Two *different*
medians reached FAQPage JSON-LD on the same URL. The headline sentence also mixed bases — a
quarterly median quoted with a 12-month growth rate.

**Root cause:** three sources feeding ~40 render sites across 7 components.
**A** `db.server.ts:552` = `indexed_series[last].median_price` (SSR hero + page FAQPage JSON-LD).
**B** `rolling_12m_median_price` (+ CI + n) — plumbed everywhere but used only by `MedianPriceChart`.
**C** `market_pulse.data_snapshot.current_median_price` — read by all six tab sections for their
headline, FAQ text and per-tab JSON-LD; same statistic as A but regenerated monthly, hence stale.

**Fix:** the union median becomes THE median wherever it exists, guarded on
`median_source == "domain_union_onthehouse"` so the other 76 of 79 suburbs are untouched.
- `generate_market_pulse.py` — `current_median_price` is now the union 12m median, with
  `current_median_price_basis`, the CI, sample n and the quarterly figure kept as
  `latest_quarter_median_price`. One change corrects ~30 render sites with no React work.
- `db.server.ts` — SSR `medianPrice` prefers the union median; adds `medianPriceBasis` and
  `medianPriceQuarterly`.
- `MarketMetricsPage.tsx` — FAQ answer and JSON-LD state the basis instead of tagging a 12-month
  figure with a single quarter, and now cite the 90% CI + sample size (Rule 5: source AND
  limitations). Hero tile labelled "(12 months)" with the CI in its tooltip; source line no longer
  implies the quarter covers the median.
- `market-insights.mjs:668` — legacy `median_price` prefers the union median, so `/` and
  `/news/:suburb` cannot disagree with `/market-intelligence`.
- 8 strings across SellNow/Overview/CrashRisk/Buy described `yoy_growth_pct` as "latest quarter vs.
  a year ago"; it has always been the 12-month rolling figure. Relabelled.
- Deleted 5 unrendered `*SEOContent` components (488 lines) carrying their own median prose, plus
  the `FaqExpandable` helper they orphaned and its now-unused `ReactNode` import.

**Files:** `scripts/generate_market_pulse.py`, `src/lib/db.server.ts`,
`src/pages/MarketMetricsPage/MarketMetricsPage.tsx`, `netlify/functions/market-insights.mjs`,
`src/components/{SellNow,Overview,CrashRisk,Buy,Direction}Section/*.tsx`

**Recurrence:** First. Verified live: new basis markers present, `$1,710,000` and the mislabelled
strings gone from SSR.

---

## [PULSE-QOQ-UNRELIABLE-QUARTERS] QoQ narrated from quarters too thin to support it — 08:05 AEST

**Symptom:** `CrashRiskSection.tsx:363` and `DirectionSection.tsx:180-188` narrate a
quarter-on-quarter move ("the median has reached $X with … Y% quarterly momentum") from quarters
whose confidence interval cannot support one. `market-insights.mjs:685-689` already warned in a
comment that these quarters "must not narrate a QoQ change" — Burleigh Waters fails in 5 of its
last 6, Robina in 3 of 4 — but nothing enforced it.

**Root cause:** `precompute_union_prices.py` bootstraps a 90% CI per quarter and sets
`reliable: false` where it is too wide. `generate_market_pulse.py` computed `qoq_growth_pct` from
`series[-1]` and `series[-2]` without ever reading that flag. These are hardcoded React template
strings, so **rewriting the monthly prose would not have fixed them** — they render whatever the
snapshot contains regardless of what the summaries say.

**Fix:** `qoq_growth_pct` is now emitted only when BOTH quarters are `reliable`, with
`qoq_suppressed_reason` recorded otherwise. Suppressing at the data layer removes the claim from
every consumer at once. Varsity Lakes keeps its QoQ (+4.4%, quarters pass); Burleigh Waters and
Robina are suppressed on Q2 2026.

**Trap hit while fixing it:** setting the field to `None` was wrong — `DirectionSection` guarded on
`!== undefined`, which `null` passes, and `null <= 0` is `true`, so it would have asserted
"momentum stalled" from a figure we had just suppressed. The key is now OMITTED, all four guards
hardened to `!= null`, and `buildVerdictText()` drops the clause rather than printing
"N/A quarterly momentum".

**Files:** `scripts/generate_market_pulse.py`, `src/components/DirectionSection/DirectionSection.tsx`

**Recurrence:** First.

---

## [MONTHLY-PRECOMPUTE-CONSOLIDATED] Six cron lines → one ordered script — 08:25 AEST

**Symptom:** Not a failure — the structural cause behind `[UNION-MEDIANS-REVERTED-NIGHTLY]`. The
monthly rebuild was six separate cron entries (05:00/05:04/05:08/05:16/05:30/05:40) whose
correctness depended entirely on clock spacing and on nobody running one of them alone. Step 1 ends
in a blind `replace_one`, so any solo run silently deletes the medians until the next promote.

**Fix:** `scripts/run_monthly_market_precompute.sh` runs all six in sequence and then calls
`check_union_median_integrity.py` to confirm the promote landed. Ordering is enforced by sequence,
not by clock. The six crontab lines are commented out (not deleted, so the old schedule stays
readable) and replaced by one `0 5 1 * *` entry. The promote deliberately still runs after a partial
failure — a half-rebuilt document with correct medians beats one with raw medians.

Checked for collisions: `refresh_absorption_snapshots.py` (05:24), `scrape_sqm_asking_prices.py`
(05:32), `precompute_seasonality.py` (05:45) and `precompute_price_tier_liquidity.py` (05:52) also
run on the 1st, but none writes to `precomputed_indexed_prices` — seasonality only mentions it in a
docstring. The weekly PropRadar ingest (`30 5 * * 0`) sits next to the monthly recalibrate
(`30 5 1 * *`) and was explicitly asserted untouched.

**Files:** `scripts/run_monthly_market_precompute.sh` (new), crontab

**Recurrence:** First.

**Still open (for the joint session):**
1. The 21 August summaries are still the 2026-08-01 auto-generated set, written from the reverted
   data. They clear only when the prose is rewritten.
2. Durable fix still open: convert the `replace_one` at
   `precompute_indexed_price_data.py:1044-1048` to a merge preserving union-owned fields. Nothing
   reaches it nightly, the chain now always ends in a promote, and the tripwire catches a revert
   within a day — so this is defence-in-depth rather than urgent.
3. Full 21-page Rule 6 verification, after the prose is regenerated.

---

## [COVERAGE-SUBURB-DRIFT] Coverage check tested 8 suburbs while scraper ran 6 — 07:10 AEST

**Symptom:** Step 109 reported `ERROR Merrimac (missing 16 properties)` and
`ERROR Carrara (missing 4 properties)` every night. Neither suburb is scraped by step 101,
so the gap could never close — two permanent false ERRORs on the nightly board.

**Root cause:** The suburb list was hardcoded twice. `config/settings.yaml` `target_market.suburbs`
(what step 101 scrapes) had Merrimac and Carrara commented out, but step 109's `--suburbs "..."`
literal in `config/process_commands.yaml` still listed all 8. The two lists were free to drift and did.

**Fix:** Added `load_suburbs_from_settings()` + a `--from-config` flag to `scripts/coverage_check.py`
that reads `target_market.suburbs` directly, and changed step 109's command to
`--from-config --no-fail`. One source of truth — the check can no longer test a suburb nothing scrapes.
Verified: 6 suburbs checked, 0 gaps.

**Files:** scripts/coverage_check.py, config/process_commands.yaml
**Recurrence:** First

---

## [COVERAGE-DOMAIN-VARIANT] Burleigh Waters silently reported DOMAIN_UNAVAILABLE — 07:10 AEST

**Symptom:** 2026-08-01 run: `WARN Burleigh Waters Domain=None DB=54 (DOMAIN_UNAVAILABLE)`.
The one suburb that was also short on discovery (51/54 in step 101) was the one with no
independent count to verify against, and nothing was retained to diagnose why.

**Root cause:** The fetch succeeded but the returned page matched neither count regex. Domain serves
at least two page variants for the same URL — a ~500KB one carrying only the
`<h1 data-testid="summary">` heading, and a ~2MB one that also embeds `"totalResults"`. Extraction
failure was terminal (no retry, since only the *fetch* was retried) and discarded the body.

**Fix:** Replaced the two ad-hoc regexes with a `COUNT_PATTERNS` list covering all four known shapes
(summary heading, bare `<strong>`, `"totalResults"` JSON, generic `>N Properties<`), moved the retry
loop to wrap *extraction* rather than just the fetch, and on total failure the last body is written to
`logs/coverage_unparseable_<slug>.html` so the next occurrence is diagnosable. Re-ran: Burleigh Waters
now `OK Domain=54 DB=54`.

**Files:** scripts/coverage_check.py
**Recurrence:** First

---

## [BLOB-DEAD-HOST-VISION] Step 106 burned 54 floor-plan analyses on a retired Azure host — 07:10 AEST

**Symptom:** 2026-08-01 step 106 logged 54 × `403 Client Error: The specified account is disabled`
against `fieldspropertyimages.blob.core.windows.net`, failing floor-plan vision for those properties.
Step exited 0, so the run reported 26/26 success and the loss was invisible.

**Root cause:** The Azure `fieldspropertyimages` account was retired 2026-05-28 and the pipeline moved
to `BLOB_BACKEND=local` (nginx over /data/blobs at `blobs.fieldsestate.com.au`, identical paths), but
~20k stored documents still carry the old Azure URLs. The website already handles this —
`toLiveBlobUrl()` in `netlify/functions/shared-utils.mjs` rewrites the host. The Python pipeline had
no equivalent, so vision steps fetched the dead host directly.

**Fix:** Added `to_live_url()` to `shared/blob_storage.py` (Python mirror of the website's
`toLiveBlobUrl`, rewriting known-dead hosts to `BLOB_PUBLIC_BASE_URL` on the same path) and applied it
in `_download_and_encode_image()` in the floor-plan vision client. Verified end-to-end: a URL that
403'd last night now downloads and encodes (452KB PNG).

**Files:** shared/blob_storage.py,
Property_Data_Scraping/03_Gold_Coast/Gold_Coast_Wide_Currently_For_Sale_AND_Recently_Sold/Ollama_Property_Analysis/openai_floorplan_client.py
**Recurrence:** First (related: [STEP105-DICT-IMAGES] — same class, vision step failing silently while the run reports success)

## [HERO-PIXEL-REVEAL] Hero photo pixel-reveal concept build — 17:35 AEST
**Symptom:** N/A — new concept, not a fix. Will asked to explore a pixel-block reveal of a house hero photo, sweeping from bottom-left out toward top-right.
**Root cause:** N/A.
**Fix:** Built a canvas reveal engine: per-cell delay field (diagonal projection blended with radial distance from the origin corner, plus deterministic jitter), a pre-rendered pixelation stack so each cell *sharpens* from 64px blocks to full detail rather than just fading, and a settled-layer optimisation so each frame only draws cells currently in flight. Shipped as an interactive demo (3 real listing photos, live controls) and a drop-in React component. Component typechecks clean under the website's tsconfig.app.json; demo verified headlessly in both light and dark themes with zero console errors.
**Files:** 15_Off-Market/Concepts/Hero_image_reveal/{README.md, PixelReveal.tsx, pixel-reveal-demo.html, pixel-reveal-demo.template.html, photos/*, mid-reveal-still.png}
**Artifact:** https://claude.ai/code/artifact/b831909e-9faa-4e52-bb62-99e70b99ed3d
**Recurrence:** First

---

## [PALM-PIXEL-REVEAL] Pixel-reveal animation for the Pandanus hatch sketch — 08:05 AEST

**Symptom:** N/A — new concept asset, not a repair.

**Built:** A self-contained pixel-reveal animation in
`15_Off-Market/Concepts/Near_beach_palm_reveal/` that constructs a black-and-white
version of `Hatch_Sketch_Pandanas_Palm.png`. Coarse pixel blocks arrive and resolve
into the fine hatch, so the ink appears to grow rather than fade in. Five reveal
orders (growth / develop / dissolve / rise / sweep), light and dark paper, live
controls, plus a deterministic headless-Chrome renderer for MP4/GIF/stills.

**Two things worth recording, both caught by measuring rather than eyeballing:**
1. The source paper is a flat **254**, not 255, and 71.75% of pixels sit there. A
   percentile-based white point (the obvious first cut) mapped 255→255 and left a
   uniform alpha=1 haze over the entire background. That haze put every "empty"
   block at density 0.0039 against a 0.004 skip threshold — the whole animation was
   one rounding step away from treating 67% of the canvas as ink. Fixed by taking
   the white point as the **modal** bright tone and flattening sub-0.014 ink to zero.
2. The ink bounding box was being set by a single stray speck at y=1279, padding the
   crop with ~100px of dead paper. Fixed by requiring ≥3 inked pixels in a row/column
   before it can define the bbox.

**Design note:** the master is written as an RGBA **alpha mask** (RGB black, alpha =
coverage) rather than black-on-white, so blocks composite onto any paper colour
without painting white squares over the background — that is what makes the dark
theme possible from the same asset.

**Files:** `15_Off-Market/Concepts/Near_beach_palm_reveal/{build_master.py,
reveal.template.html,render_video.js,README.md}` (+ generated `index.html`,
`palm_bw.png`, `palm_ink_alpha.png`, `palm_reveal_growth.{mp4,gif}`,
`palm_reveal_growth_dark.mp4`)

**Recurrence:** First

**Follow-up — 08:20 AEST:** two things found while verifying, both fixed and pushed.
- `getImageData` was called on the mask canvas on every grid rebuild (each pixel-size
  change), which Chrome warns about and which would have dragged the source canvas onto
  the software path — it is only ever *drawn from* after load. Now the alpha channel is
  read back once into a `Uint8Array` at load.
- `render_video.js` leaked its temp frame directory whenever `--stills` was used (the
  early `return` skipped the cleanup) and whenever ffmpeg threw. Temp dir is now only
  created when frames are actually written, and removal moved into a `finally`.
Re-verified in headless Chrome: no console messages, mode/theme switching and backward
scrubbing (the full-rebuild path) all correct.

---

## [RAPID-MINISITE-BUILD] Card-10 pixel-reveal build box — concept + prototype — 17:52 AEST

**Symptom:** N/A — new concept, not a repair. Will asked to explore a box that
forms under the /off-market discovery deck's card 10 (strategy card) via a
"matrix-style" non-uniform pixel reveal, then behaves as a popup text box.

**Incidental finding (real bug, not fixed here):** card 10's CTA is a dead link.
`DiscoveryDeck.tsx:278` renders `href="#build-strategy"`, and
`grep -rn "build-strategy" src netlify` returns only that line — no element with
that id exists anywhere. Every visitor who scrolls the full deck and clicks
"Build my complete selling strategy" gets a no-op, while `forward_cta_clicked`
and the Telegram intent alert still fire, so it looks healthy in analytics.
Left in place deliberately (concept work only, no live-deck edits); logged as
open question / integration step 4 in the concept README.

**Built:** Standalone tunable prototype (`prototype.html`) reproducing the deck's
exact tokens: 1.5s dwell on card 10 → downward-biased jittered pixel fill →
copper border drawn as four sequenced strokes → consolidate to solid birch paper
→ live 16-step build feed (the real `STEP_ORDER` from
`property-report-progress.mjs`). Bottom-left panel tunes dwell/cell size/fill/
jitter/leader-%/downward-bias and replays.

**Three defects found and fixed during prototyping** (all would look like flaws
in the idea rather than the code):
1. *Node count* — 13px cells over a 900×540 box = ~2,800 animating divs. Added a
   `CELL_BUDGET` (900) that coarsens the cell until it fits; coarser reads more
   Matrix, so the degradation is free.
2. *Vertical seams* — `repeat(N, 1fr)` lands cell edges on fractional pixels
   (measured 23.13px/cell, lefts at 183.31/206.43/229.56/252.70/275.82…), and the
   rounding beat drew bright hairlines every ~185px down the panel, exactly at the
   predicted boundaries. `transform: scale(1.06)` is NOT sufficient (~0.7px of
   overlap at this cell size — seams survived it and I verified that before
   moving on). Fixed with `box-shadow: 0 0 0 1px var(--paper)` per cell for a
   guaranteed 1px bleed, plus `overflow:hidden` on the grid to clip it.
3. *Dead air* — unclamped jitter left stragglers landing ~400ms after the wave, so
   consolidation waited for them and showed ~1s of empty white paper before any
   content. Fixed by clamping every cell delay to a horizon
   (`fill + jitter × 0.45`) and consolidating exactly at it, content +140ms.

**Verified:** headless at 900×900 and 390×844 DPR2, screenshotted across the whole
sequence; 851 cells; seams confirmed gone mid-fill (not just after consolidate);
no JS errors. Mobile pass surfaced two design problems recorded in the README —
the gap above the box is much worse at 390px, and the 16-step panel overflows a
phone viewport so most of the reveal happens off-screen.

**Note:** `Concepts/Hero_image_reveal/PixelReveal.tsx` (built the same day, ~20 min
earlier by a concurrent session) is a canvas pixel-reveal for hero *photos*. Not
directly reusable here (needs an image src), but its canvas engine + deterministic
seeded RNG is the better foundation for the React port than this DOM grid —
canvas avoids both defects 1 and 2 by construction. Recorded in the README.

**Files:** `15_Off-Market/Concepts/Rapid_Mini_Site_Buid/{README.md,prototype.html}`
(+ copy at `/home/fields/offmarket-preview/rapid-build.html` for viewing)

**Recurrence:** First

## [ORCHESTRATOR-SIBLING-SYMLINKS] Sibling /home/fields projects visible from orchestrator tree — 08:13 AEST
**Symptom:** Main project folders (Feilds_Website, Property_Data_Scraping, Property_Valuation, samantha-*, knowledge-base, brain_drive, …) live as siblings under `/home/fields`, so they were not visible from the `Fields_Orchestrator` tree in one view.
**Root cause:** Not a bug — layout. Only brain1_build / brain3_build / brain3_ops had ever been symlinked in.
**Fix:** Extended that existing pattern to 18 more sibling folders via `ln -s /home/fields/<dir>`, and listed all 21 links in a dedicated `.gitignore` block (a committed symlink would break any clone lacking `/home/fields`). Skipped `logs`, `node_modules`, `netlify` — name collisions with real orchestrator dirs. Also `git rm --cached` on 13 stale tracked files under `Feilds_Website/`, `Property_Data_Scraping/`, `Property_Valuation/`: they were deleted on disk back in April (commit e91acd9) but still in the index, and .gitignore does not apply to tracked paths — so once the symlinks existed, a `git restore`/`git stash pop` on those paths would have written stale April copies straight through the link into the live sibling projects.
**Files:** .gitignore; 21 symlinks at repo root (untracked); untracked 13 stale paths. Pushed as 10b9072.
**Recurrence:** First

**Follow-up 2 — 08:15 AEST:** the first temp-dir fix was incomplete. The `finally` I added
only wrapped the *encoding* phase, so a browser crash during frame capture still leaked the
directory — which is exactly what happened when two Chrome instances collided on this VM
(44 orphaned frames left in `/tmp/palm-reveal-cUZzwL`). Capture + encode are now both inside
one `finally`. Also `--out` with an absolute path was being `path.join`ed onto the script
directory, silently writing to a path that does not exist; now `path.resolve`d.
Verified all four paths leave zero temp dirs: stills, successful video, absolute `--out`,
and a forced browser-launch crash.

---

## [OFFMARKET-BEACH-CARD-SIM] Near-beach Discovery Deck card simulation — 08:45 AEST

**Symptom:** N/A — new concept build.

**Built:** `15_Off-Market/Concepts/Near_beach_palm_reveal/offmarket_beach_card.html` — a
standalone simulation of the `/off-market/:slug` Discovery Deck when the **beachside** lead
angle fires. Two cards: 02 `hook` then 03 `reveal`. On scroll into card 03 the copy staggers
in line by line, then the Pandanus pixel-reveal animation arrives in the right-hand column.

**Copy is the real approved copy, not invented** — `copy.yaml:75` (`hooks.beachside`) and
`copy.yaml:153-158` (`card_03.beachside`), with the card-04 importance line folded in so one
card both states the proximity and says why it carries weight. Design tokens, card anatomy
and the 900ms/`cubic-bezier(.2,.7,.2,1)`/`translateY(26px)` reveal all lifted from
`discovery.module.css` + `DiscoveryDeck.tsx`.

**Editorial:** dropped the `c04_close` line "Proximity like yours is priced in — and it
doesn't depreciate." That is a forward-looking claim about value and breaches the no-predictions
rule. Replaced with a factual equivalent ("Distance to the sand is fixed at the title…").
The page is badged **"Simulation · sample data"** so it can't be read as a real owner report.

**Three things found by verifying in-browser rather than assuming:**
1. The media column was fading in on the *text* stagger ladder, so the animation was already
   visible at t=1000ms while the JS `mediaIn` flag was still false — the whole text-then-animation
   sequence was cosmetic only. Cause: `.revealIn > * > *` and `.media` have equal specificity,
   so the later rule won and forced `opacity:1`. Reveal is now scoped to an explicit `.stagger`
   container and `.media` is driven solely by JS. Measured after fix: text opacity 1 at
   t=1000ms, media still 0; media begins t=1400ms.
2. Same rule meant the stagger ladder never bit — it targeted the single wrapper div, so the
   whole block moved as one unit. (The live deck has this same characteristic; here the
   line-by-line arrival is the point, so the scoping is deliberate divergence, not a copy.)
3. Card 02 used a 40rem centred column and card 03 a 68rem grid, so the text jumped 224px
   sideways between cards. Both now share the grid with an empty right cell on card 02.

**Also:** added `paper` / `ink` / `grain` / `vignette` URL + CLI overrides to the animation so
it can be matched to a host page, and rendered `palm_reveal_deck.mp4` on pure `#000` with no
grain or vignette. Verified corners are exactly `[0,0,0]`, so the palm reads as appearing on
the deck rather than inside a panel — the deck has no boxed media anywhere.

**Note for whoever ships this:** the live deck is single-column `max-width: 40rem` and contains
no video, canvas or image in any card. The side-by-side text/media split is a NEW layout
primitive, not an existing pattern.

**Files:** `15_Off-Market/Concepts/Near_beach_palm_reveal/{offmarket_beach_card.html,
reveal.template.html,render_video.js}` (+ generated `palm_reveal_deck.mp4`)

**Recurrence:** First

---

## [VM-NGINX-TTYD-502] Web terminal 502 + no URL for concept mocks — 08:50 AEST

**Symptom:** Will could not open the near-beach concept page: `404` for
`offmarket_beach_card.html` and a `502 Bad Gateway` in the console. Investigating the 502
turned up a second, larger problem: `https://vm.fieldsestate.com.au/` — the documented Claude
Code terminal — was itself returning 502 for every request.

**Root causes (three separate, all pre-existing except the 404):**
1. **404** — nothing served `15_Off-Market/Concepts`. The VM serves static previews via nginx
   alias blocks (`/preview/`, `/lab/`, `/quarterly/`) and there was no equivalent for concepts,
   so the URL fell through to `location /` (the ttyd proxy).
2. **502 on `/`** — `proxy_pass https://127.0.0.1:7681`, but ttyd runs **plaintext**
   (`/etc/default/ttyd`: `TTYD_OPTIONS="-i lo -p 7681 -O login"` — no `-S`). nginx attempted a
   TLS handshake against a plaintext upstream: `SSL_do_handshake() failed ... wrong version
   number`. ttyd has been up since **Aug 1 08:04**, so the terminal had been down at least a day.
   TLS terminates at nginx; loopback plaintext is correct, so the scheme was the bug.
3. **502 on `/favicon.ico`** — browsers request it on every page; it fell through to the same
   broken proxy and put a red console error on every preview page on the VM.

**Fix:**
- `location /concepts/` → `alias /home/fields/concept-previews/` (symlink to
  `15_Off-Market/Concepts`), `autoindex on`, `no-store`, `X-Robots-Tag: noindex`.
  Audited first — concept mocks and images only, no credentials or owner data.
- `proxy_pass https://` → `http://` for the ttyd upstream (dropped the now-redundant
  `proxy_ssl_verify off`).
- `location = /favicon.ico { return 204; }`.

**Also found and cleaned:** `nginx.conf` does `include /etc/nginx/sites-enabled/*`, and two stale
backups (`fields-vm.bak`, `fields-vm.bak.1784930924`) were sitting in that directory being loaded
as live config — six `conflicting server name ... ignored` warnings on every `nginx -t`. The real
file sorts first so it always won, but any backup naming that sorted earlier would have silently
taken over the whole vhost. Moved to `/etc/nginx/backups/` (outside the glob). `nginx -t` is now
warning-free.

**Verified:** `/` 200 and serves `<title>ttyd - Terminal</title>`; `/ws` returns **101 Switching
Protocols** through nginx with a matching Origin (a 502 without one is `--check-origin` working
as intended, not a fault); `/preview/`, `/lab/`, `/quarterly/` all still 200; the concept page
loads over HTTPS with a clean console and the video plays to completion (byte-range 206 confirmed).

**Files:** `/etc/nginx/sites-enabled/fields-vm` (backup:
`/etc/nginx/backups/fields-vm.before-proxyfix-*`), `/home/fields/concept-previews/` (new)

**Recurrence:** First

---

## [BEACH-CARD-MOBILE-UNSEEN] Reveal animation played off-screen on every phone — 09:10 AEST

**Symptom:** Asked "will it work on mobile?". It rendered and stacked fine — but the animation
was never actually watched on a phone.

**Root cause:** playback was triggered by the *card* entering the viewport. The card is
`100svh` plus a long text column, so on a phone the media sits ~970px down while the viewport is
only 667-915px tall. Measured at the moment playback started: **0% of the video visible** on
iPhone SE, iPhone 14 Pro and Pixel 7 (video top 954-972px vs viewport 667-915px). The palm drew
itself and finished during the ~6.4s the reader spent on the copy above it, so scrolling down
revealed a static finished tree. On desktop the media sits beside the text and is 100% visible
at that moment, which is why it looked correct there.

**Fix:** a second IntersectionObserver on the media element itself (threshold 0.3) now triggers
playback, and the delay is the *remainder* of the text-first wait rather than a fresh one:
`max(200, min(1250, 1250 - (now - revealAt)))`. Desktop is unchanged — media becomes visible with
the card, elapsed ≈ 0, so the full 1250ms still applies and the copy reads first. On mobile the
media comes into view seconds later, elapsed already exceeds the wait, so it plays at 200ms as it
arrives instead of replaying a wait the reader has already served.

**Two more found in the same pass:**
- **Horizontal scroll on every device** (377>375, 396>393, 416>412, 751>744). `.mediaGlow` is
  `inset: -14% -10%`, absolutely positioned and unclipped, so it pushed past the viewport. Added
  `overflow-x: clip` on `.card` (with `hidden` fallback) — `clip` does not create a scroll
  container, so it can't break sticky positioning.
- **`min-height: 100svh` had no fallback.** iOS only has `svh` from 15.4; older Safari drops the
  declaration entirely and the card collapses. Added `min-height: 100vh` before it.

**Autoplay:** `muted` + `playsinline` covers iOS and Android, but Low Power Mode and Data Saver
refuse regardless. Verified the failure path by stubbing `HTMLMediaElement.prototype.play` to
reject — it now sets `controls` and shows a "▶ Tap to play" pill rather than a blank rectangle.
Worth noting the `--autoplay-policy=document-user-activation-required` Chrome flag does NOT
exercise this, because muted video is exempt; the first run of this test passed for the wrong
reason and had to be forced.

**Verified:** iPhone SE / iPhone 14 Pro / Pixel 7 / iPad mini / desktop — video stays at t=0
while off-screen, plays once scrolled to (100% visible), zero horizontal scroll on all five, and
desktop text-then-animation ordering unchanged (text opacity 1 at t=1000ms, media begins t=1400ms).

**Files:** `15_Off-Market/Concepts/Near_beach_palm_reveal/offmarket_beach_card.html`

**Recurrence:** First

## [DOM-ZERO-STALE] days_on_domain reads 0 on listings that are weeks old — 18:55 AEST
**Symptom:** 21 of the 81 editorial-complete active listings carry `days_on_domain: 0`,
so the `/for-sale-v3` decision card renders "0d listed" on homes that have been on
market for up to 19 days. Visible in a screenshot Will supplied: a Burleigh Waters
card showing "0d listed" above body copy reading "since going to market on 24 July 2026"
(9 days earlier). It also inflates the "New this week" cohort — `context_pill` and any
`days <= 7` filter counted 36 of 81 listings (44%) as new; the true figure is 4.
**Root cause:** `days_on_domain` is populated from the Domain scrape and is not
recomputed as the listing ages. When the scrape returns no value it defaults to 0
rather than null, so "unknown" is indistinguishable from "listed today". Downstream
consumers (`decision-feed-v3.mjs` card + handler `context_pill` logic) trust it directly.
`first_listed_timestamp` is present and correct on 81/81 of the same documents.
**Fix:** In the August 2026 mockup, added `days_listed()` to `build_fixtures.py`, which
derives days from `first_listed_timestamp` and falls back to `days_on_domain` only when
it is > 0 (never 0). "New this week" went 36 -> 4. **Production is NOT yet fixed** —
`decision-feed-v3.mjs` still reads `days_on_domain`; logged here so it is not lost.
**Files:** Feilds_Website/01_Website/01_New_Versions/August_2026/build_fixtures.py
**Recurrence:** First

## [CONCEPT-AUG2026] August 2026 browse-surface mockup built — 19:10 AEST
**Symptom:** N/A — new work, not a defect. Scoping + local mockup for the iView-style
rebuild of the website homepage/browse surface.
**Root cause:** N/A
**Fix:** Wrote `00_SCOPING.md` (IA, tiered-card analysis, SEO risks, phased plan) and a
standalone no-build mockup at `prototype/`, served via a symlink into
`/home/fields/concept-previews/` at `vm.fieldsestate.com.au/concepts/august-2026/`
(noindex). Light and dark themes render from one component set via CSS variables.
Three mockup bugs found and fixed during visual verification:
  1. `.drawer`/`.modal` set `display:flex|grid`, which outranks the `hidden` attribute —
     both panels rendered open on load. Added `[hidden]{display:none!important}`.
  2. `scroll-snap-align:start` snaps to the scrollport edge and consumed the rail's
     left padding, so the first card sat flush at x=0. Added `scroll-padding-inline`.
  3. Article cards printed "5 min read min read" — `readTime` already carries the unit.
**Files:** 01_New_Versions/August_2026/{00_SCOPING.md,README.md,build_fixtures.py,
prototype/{index.html,styles.css,app.js,fixtures.json}}
**Recurrence:** First

## [CONCEPT-MATRIX-RAIN] Matrix digital rain recreation — 09:29 AEST
**Symptom:** N/A — new concept build requested by Will.
**Root cause:** N/A.
**Fix:** Built a self-contained digital-rain page at 15_Off-Market/Concepts/Matrix_Recreation/index.html.
Two correctness/perf issues found and fixed during headless verification:
(1) first draft seeded all streams above the viewport, so the field took ~15s to fill —
now seeded mid-flight for instant steady state, and streams are pooled (multiple per
column, composited by max brightness) instead of one-per-column;
(2) bloom used a full-screen CSS blur, which rasterizes at viewport size — moved the
blur into the 1/5-res canvas, measured 8 fps -> 32 fps at 1080p on this GPU-less VM.
Scanlines switched from mix-blend-mode:multiply to plain alpha (identical over black,
one less blend pass). Verified via puppeteer: no console errors, panel/sliders/reset/
pause all work, atlas rebuild on glyph-size change is clean.
**Files:** 15_Off-Market/Concepts/Matrix_Recreation/index.html, .../README.md
**Recurrence:** First

## [CONCEPT-MATRIX-HOMEOWNER] Homeowner digital-rain sequence — 09:51 AEST
**Symptom:** N/A — v2 concept requested by Will (property-relevant glyphs + scripted payoff).
**Root cause:** N/A.
**Fix:** Added homeowner-sequence.html — same rain engine, property glyph set (digits 3:1
over capitals, $ % m2 arrows; unmirrored) plus token streams that stamp a real string one
char per row so it reads vertically out of the noise (BURLEIGH WATERS / 4220 / SOLD / 607M2).
Timeline: 3s full rain -> 1.6s contract to a 34%-width left band (dim 42%) -> typed terminal
(searching... x2, Found it, address, There's a problem..., ERROR x3, ABORT) with per-char
typing, slow-landing dots, alarm wash, shake + rain surge on ABORT. Address overridable via
?street=&locality= query params.
**Bug found and fixed during build:** the Edit tool wrote a raw NUL (0x00) and 0x1F byte into
the clip() regex literal instead of escape sequences, making the file 'data' not text (broke
grep; invalid in HTML). Replaced with \u0000-\u001F escapes; verified 0 control bytes remain
and 'file' now reports HTML/UTF-8. Worth watching for on any future regex containing control
ranges.
**Verified:** puppeteer timed capture at 7 marks — all 10 lines print in order, no console
errors, left/right lit-pixel split 2744/90 confirms contraction, query params render with
spaces intact.
**Files:** 15_Off-Market/Concepts/Matrix_Recreation/homeowner-sequence.html, .../README.md
**Recurrence:** First

## [CONCEPT-MATRIX-INCODE] In-code sequence (v3) — 10:04 AEST
**Symptom:** N/A — v3 concept requested by Will: message printed inside the streaming code
rather than as an HTML overlay; field switches on left->right, off right->left.
**Root cause:** N/A.
**Fix:** Added code-sequence.html. No DOM text layer — message written into the rain's own
character grid via textB/textP arrays (brightness + palette per cell), rendered through the
same atlas drawImage path as the rain. Seven per-palette atlases (rain/dim/ok/addr/warn/err/
abort) so lines can turn amber/red while staying grid glyphs. Per-column colLife drives the
build sweep (left->right, 1.4s) and shutdown sweep (right->left, 2.2s, 0.45s per-column fade)
down to a 17% surviving band. Text-only glyphs (lowercase/apostrophe/space) sit above RAND_G
so random churn never picks them.
**Bug found and fixed during verification:** printed text was corrupted —
"seaLc in2.2." / "3 ANo2et A e ue". The random-churn loop was correctly guarded against
message cells, but the TOKEN STAMPER was not; it wrote cell[] unconditionally, so token
streams (BURLEIGH WATERS etc) ate characters out of the printed words as they fell through
those rows. There are TWO writers to cell[], both need the textB[j]===0 guard.
**Also:** the raw-control-byte issue from [CONCEPT-MATRIX-HOMEOWNER] reproduced identically
in this file's clip() regex — confirms it is systematic to writing a control-char range, not
a one-off. Same fix applied; file verified as HTML/UTF-8 with 0 control bytes.
**Verified:** puppeteer capture at 10 marks. Fifth-of-screen occupancy confirms both sweeps
(t=0.9s [525,631,270,46,0] building; t=1.8-3.5s all five lit; t=5.2s [357,353,11,0,0]
shutting down; t>=6.4s left fifth only). All 10 lines render uncorrupted. No console errors.
**Files:** 15_Off-Market/Concepts/Matrix_Recreation/code-sequence.html, .../README.md
**Recurrence:** First (control-byte sub-issue: 2nd — see [CONCEPT-MATRIX-HOMEOWNER])

## [CONCEPT-MATRIX-COLLAPSE] In-code sequence: rain not fully collapsing — 10:10 AEST
**Symptom:** Will reported the word sequence was typing over the top of still-running vertical
rain; the field had not fully collapsed to the left.
**Root cause:** Two separate defects.
(1) SURVIVOR was 0.17, deliberately leaving a 17% band of columns raining — the message then
printed across those same rows, interleaving rain glyphs with the words.
(2) With SURVIVOR lowered to 0, a second bug surfaced: the sweep front only reaches its target
at the last instant of the shutdown phase, and colLife was updated ONLY while phase==='shutdown'.
The leftmost columns froze part-way through their 0.45s fade and remained on screen as a lit
stripe down the left edge.
**Fix:** SURVIVOR=0 so the field switches off completely before printing. colLife now keeps
draining during the 'print' phase until it reaches zero. layout() also pushes the message clear
of any surviving columns (startCol >= survivors+2), so text can never be printed over live rain
at any SURVIVOR setting.
**Verified:** puppeteer lit-pixel sampling — full field edge-to-edge at t=3s (rightmost lit
1436/1440), collapsing at t=5s (rightmost 528), screen fully clear at t=6.3s (lit=0). Lit pixels
left of the message column: 177 before fix, 0 after. Message renders uncorrupted; no console errors.
**Files:** 15_Off-Market/Concepts/Matrix_Recreation/code-sequence.html, .../README.md
**Recurrence:** First

## [CONCEPT-MATRIX-TOPDROP] In-code sequence: opening drops from the top — 10:14 AEST
**Symptom:** Will asked for the opening to come down from the top all at once instead of the
field switching on left-to-right.
**Root cause:** N/A — design change.
**Fix:** Added OPENING ('top' default | 'sweep' to restore the old behaviour). spawn() now takes
a mode ('top' | 'recycle' | 'seed') rather than a seeded boolean: 'top' places every stream just
above the top edge with near-zero stagger (<=0.12s) so the field arrives as one curtain and
spreads naturally because fall speeds differ. colLife starts at 1 for all columns in top mode
(no sweep). T_OPEN=5.6s, timed so the curtain reaches the bottom just as the collapse starts.
**Verified:** puppeteer lit-pixel sampling by screen quarter — at t=300ms all light is in the TOP
quarter ([284,0,0,0]) but already spans the full width (x 20->1356), confirming every column
enters together rather than sweeping. Curtain reaches the bottom (lowest lit 896px of 900) at
t=5.4s. Collapse and message sequence unaffected. JS syntax-checked; no console errors.
**Files:** 15_Off-Market/Concepts/Matrix_Recreation/code-sequence.html, .../README.md
**Recurrence:** First

## [CONCEPT-MATRIX-LOCAL] Local recognition sequence (v4) — 10:28 AEST
**Symptom:** N/A — v4 requested: rain runs twice as long, carrying words a Burleigh Waters
owner recognises, ramping from unreadable noise to hyper-local over 5-7s.
**Root cause:** N/A.
**Fix:** Added code-sequence-local.html. T_OPEN 5.6 -> 11.2s. Flat TOKENS list replaced with
4 weighted TOKEN_TIERS unlocking at 1.5/3.6/6.2/8.4s; later tiers weighted (i+1)^2 so the mix
SHIFTS toward the viewer rather than just accumulating. Piecewise RAMP holds token density at
0 for the first 1.5s (pure noise per the brief) then climbs to 0.58.
**Data provenance:** street names are REAL — queried Gold_Coast.burleigh_waters (6,885 geocoded
parcels, 193 streets), computed haversine distance from the Avocet Ave centroid, and tier 3 is
the genuine nearest-street list (Jabiru 55m, Fantail 58m, Burleigh St 86m, Corella 147m).
House numbers are invented so no real neighbour's address sits beside "WITHDRAWN". Owner-voice
phrases from the mindset brief SS3/SS5. Deliberately EXCLUDED the brief's median, +6.9% YoY and
292->161 volume drop — its own SS9/SS10 flag those as not publishable; kept DOM 29 (reliable layer).
**Non-obvious implementation point:** assigning tokens only at spawn ramps too slowly — the
opening curtain fills the screen at once and those streams take ~9s to clear, so the field would
stay noise past its cue. maintainTokens() tops up to a target each frame by converting streams
still in the top quarter of their fall. Token streams also get sLen >= phrase.length+4, else the
start of a phrase fades before the end is stamped and nothing is readable.
**Verified:** ramp math unit-tested in node (chance 0.000 at 1.4s, pickToken returns null;
tier mix 100% t1 at 2.5s -> t3 dominant at 7s -> t4 dominant at 10.5s; longest token 18 chars).
Puppeteer frames: 1.2s pure noise (no readable word); 10.5s shows BURLEIGH/WATERS/TREETOPS/
STOCKLAND/JABIRU/UNDER OFFER/PRICE REDUCED/FOR SALE/WITHDRAWN legible. Collapse + message intact
on the longer timeline. No console errors; 0 control bytes.
**Files:** 15_Off-Market/Concepts/Matrix_Recreation/code-sequence-local.html, .../README.md
**Recurrence:** First

## [CONCEPT-MATRIX-FEED] Local sequence: repeating column text + noise-first open — 10:36 AEST
**Symptom:** Will reported (1) columns showed the same word or word-pair repeating down them,
and (2) the curtain should carry recognisable text from the start, not open on pure noise.
**Root cause:** (1) each token stream held ONE phrase and indexed it with sPos % tok.length —
a deliberate ticker loop that reads as repetition at column scale. (2) the RAMP was pinned to
0 density until 1.5s, per the earlier brief which has now been superseded.
**Fix:** Replaced the modulo loop with feedChar(): when a stream exhausts its current chunk it
pulls a NEW one, so each column reads as a varied run of records. Added dataChunk() generating
fresh fragments (LOT n, RPnnnnnn QLD plan format, nnnM2, DOM n, n BED n BATH, SOLD n DAYS, mock
street numbers on real nearby streets) — ~45% of chunks are generated, not listed, which is what
prevents two columns ever running the same string. RAMP now starts at 0.34 and tier 1 unlocks at
0.0s; tiers pulled earlier (0/2.4/4.8/7.2) so localisation still deepens across the 11.2s.
feedChar() always advances even when the character is dropped, preserving row alignment.
**Verified:** simulated the feed in node and printed columns top-to-bottom — no repeats within or
across columns; 154 distinct chunks per 400 draws; density 0.34 at t=0 (was 0). Puppeteer frames
confirm words legible through the fall. Note a physical floor: a phrase needs ~its own length in
rows to render, so nothing reads until the curtain is ~15 rows deep (~1.5s) regardless of density.
**Files:** 15_Off-Market/Concepts/Matrix_Recreation/code-sequence-local.html, .../README.md
**Recurrence:** First

## [CONCEPT-MATRIX-DENSITY] Local sequence: abstract filler still dominating — 10:45 AEST
**Symptom:** Will asked whether there was still abstract filler code in the raining columns.
There was — and materially more than intended.
**Root cause:** RAMP density was 0.34 -> 0.62, so by design 38-66% of streams were pure random
glyph churn with no real content. Not a rendering artefact, just a bad parameter choice on my part.
**Fix:** Added a window.__rainStats() tuning hook that reports the live share of streams carrying
text vs target, measured it (37% at 1s, 63% at 11s — confirming the complaint), then raised RAMP
to 0.62 -> 0.98. Re-measured: 68% at 1s, 96% at 11s. Raising density does NOT flatten the look
because dataChunk() supplies the code texture (LOT n, RPnnnnnn, nnnM2, DOM n) instead of random
glyphs — the field still reads as code, just plausible code.
**Note:** actual trails target slightly mid-run (0.90 vs 0.93 at 6.8s) because maintainTokens()
only converts streams still in the top quarter of their fall; streams already mid-descent keep
their filler until they recycle. Acceptable; would need backfilling to close entirely.
**Files:** 15_Off-Market/Concepts/Matrix_Recreation/code-sequence-local.html, .../README.md
**Recurrence:** First

## [CONCEPT-MATRIX-HOTWORDS] Keyword highlighting on the local sequence — 10:52 AEST
**Symptom:** N/A — Will asked to accentuate the sparks so they highlight the keywords most
relevant to the owner of the subject property.
**Root cause of a related latent bug:** the random white sparks are set INSIDE the churn block,
and churn is skipped for locked cells (every text cell is locked). Sparks therefore only ever
fired on random filler — so raising text density to 98% in [CONCEPT-MATRIX-DENSITY] had almost
extinguished them. Invisible trade of sparkle for legibility.
**Fix:** pickToken() now returns {text, tier} so relevance is known per chunk. Chunks from tier 3
(street grid, 55% of them) and tier 4 (own block + owner-voice, always) are flagged hot; hot[] marks
each occupied cell with 1 + the character's index within its phrase. draw() applies a travelling
crest — sin(now*0.0055 - (idx)*0.5) — so the highlight SWEEPS down the phrase rather than blinking,
lifting toward white inside the existing palette rather than changing colour (keeps it reading as
code, not a caption). hot[] cleared alongside lock[] when a cell goes dark.
**Also fixed:** added Math.min(tMax, ...) clamp on the tier lookup — a brightened cell could index
past the end of the glyph atlas and render nothing, turning a highlight into a dropout.
**Verified:** __rainStats extended with hotStreams/hotCells — 0 before 5s, 10 streams at 6.8s,
44 streams / 263 cells at 10.8s. Frame capture shows AVOCET AVE / JABIRU / FANTAIL / BANKSIA /
BURLEIGH lifting near-white against green, crest mid-phrase. JS parses; no console errors.
**Files:** 15_Off-Market/Concepts/Matrix_Recreation/code-sequence-local.html, .../README.md
**Recurrence:** First

## [CONCEPT-MATRIX-HOTWORDS-2] Keyword highlight was invisible — 10:55 AEST
**Symptom:** Will: "too subtle, i was looking for it, hard, and barely saw it."
**Root cause:** the highlight only raised BRIGHTNESS — but the rain is already full of near-white
stream heads, so it competed in a dimension that was already saturated and read as nothing. The
non-crest branch was also only a 16% lift (imperceptible), and the wave offset (0.5 rad/char) fit
more than one full cycle into a phrase, so multiple part-crests appeared at once and read as noise.
**Fix:** three changes so it differs in ways the rain does NOT already use —
(1) a FLOOR: hot cells hold brightness while surrounding code decays, so contrast comes from the
surroundings dimming; floor = min(0.7, bright*life*4.2) so it tails off rather than popping;
(2) a HARD CREST: wave^3 for a narrow defined peak, offset 0.35 rad/char so exactly one crest
spans a phrase, period 1571ms;
(3) a new white-green 'hot' PALETTE so keywords differ in hue, not just level.
Hot streams also get sLen >= phrase+12 so the whole phrase is lit together, and HOT_RE
(AVOCET|BURLEIGH WATERS|MY HOME|MY STREET) marks personal phrases at ANY tier so highlights start
at the opening instead of ~5s. Tier rule widened to tier>=2 always.
**Verified:** hotStreams 4 @0.8s -> 24 @6.8s -> 58 @10.8s (307 cells, ~44% of field). Frame capture
shows BURLEIGH WATERS / FANTAIL CRT / AVOCET AVE / SANDPIPER DR / CURLEW clearly white against green.
**Note:** may now sit at the opposite extreme; tuning knobs documented in README.
**Files:** 15_Off-Market/Concepts/Matrix_Recreation/code-sequence-local.html, .../README.md
**Recurrence:** 2nd (see [CONCEPT-MATRIX-HOTWORDS])

---

## [FRUIT-ROLL-PHYSICS] Fruit tumbled end-over-end; roll never read — 11:55 AEST

**Symptom:** Will: "I didn't see the roll, it sort of hits the ground, bounces, and then appears."
Also flagged the physics: a pandanus head is a rounded prolate and would roll about its LONG
axis like a barrel, not tumble over its ends.

**Root causes — five separate defects, all found by tracing rather than by eye:**
1. **Scale was lerped, not perspective.** Ease-*out* on a linear interpolation put the fastest
   growth at the START: traced 11px -> 45px on the first scroll notch. Size goes as 1/distance,
   which is back-loaded. Now `R = R0*k/(k-(k-1)t)`; traced 11,14,23,33,53,95,150.
2. **Roll anchored to the video's live rect**, so as the page scrolled the start point scrolled
   with it and the fruit drifted UP ~120px before descending — it floated rather than rolled.
   All motion now in document coordinates, landing point frozen on impact.
3. **Straight lerp, not an arc.** Replaced with a quadratic whose control point sits below the
   start, so it leaves the trunk heading down-page then sweeps left.
4. **Wrong roll axis.** The body was elongated vertically (`squash 1.10`) and rotated about the
   horizontal axis — i.e. tumbling over its points, which a fruit that shape cannot do. Long axis
   now lies horizontal ALONG the roll axis (`LONG 1.15 / GIRTH 0.95`, rotation about the same
   axis). Silhouette now holds steady while the surface turns, which is what a rolling fruit does.
5. **Rest anchored to the viewport**, so it slid as you kept scrolling. Now anchored to card 04.

**Canvas bug worth remembering:** `ctx.restore()` restores transform, clip and styles but **not
the current path**. The per-drupe hatch built paths inside a save/clip block, so the outline
`stroke()` after `restore()` was stroking the last hatch segment — unclipped — spraying stray
lines across the silhouette. Isolated by rendering with hatch disabled. The cell path is now
rebuilt before outlining. My first guess (degenerate Voronoi cells near the poles) was wrong and
the guard I added for it was a measured no-op — 0 cells oversized — and was removed.

**Also:** drupes now carry their own hatch at a per-drupe angle. An earlier pass hatched the body
too, and with both horizontal they merged into corduroy and the tessellation vanished entirely.

**Still open:** the fruit is procedural (no source art exists above 46x50px). It reads as drawn
but not yet as the same hand as the tree. Will asked about generating replica hatched images —
no image model is available in this environment, and for a *rolling* object a generated sequence
would strobe unless perfectly consistent frame-to-frame, which generators do not give.

**Files:** `15_Off-Market/Concepts/Near_beach_palm_reveal/{pandanus_fruit.js,offmarket_beach_card.html}`

**Recurrence:** First

**Follow-up — 12:30 AEST:** Will still could not see the roll — "it bounces and then sort of
appears in its resting position". Cause: the roll was scroll-scrubbed, so its speed WAS the
reader's scroll speed. One wheel flick covers the ~830px span in well under a second, so it
teleported. My earlier trace stepped the scroll in gentle 90px increments with pauses, which
hid the problem completely — the bug only appears on a single fast jump.

Tried pure time-driving first; that fixed the speed but desynced it from the reader — jumping to
card 04 left the fruit starting a full viewport above the fold, so ~40% of the roll happened
off-screen (traced viewport y of -210 at t=200ms).

Landed on a **rate-limited follow**: progress chases a scroll-derived target but can move no
faster than 1/ROLL_MS per ms. Slow scroll feels scroll-linked; a fast flick still takes ~2.4s.
Also had to tighten the target range back — an "early start" variant put progress at 0.37 before
the reader had scrolled at all, which pushed most of the travel below the fold.

Verified both extremes: slow scroll keeps the fruit on screen throughout (viewport y 604->797->720
while x travels 1072->245 and R grows 11->153); fast flick stretches to ~2.4s. Remaining known
limit: flicking past an entire card in one jump still starts the roll off-screen above, because
the fruit's origin genuinely is a viewport away — it rolls in from the top edge.

**Follow-up — 13:05 AEST:** Will: "i have to keep scrolling down to see it rolling... what i want
is the page to move down automatically as the fruit rolls." Added a scripted scroll for this one
transition: when the fruit lands and the reader is still on card 03, the page eases itself to
card 04 over ROLL_MS+400.

It drives only the SCROLL — the fruit remains the same rate-limited scroll-follow, so the two
stay in step by construction instead of via two clocks that can drift.

Scroll hijacking is hostile if it traps anyone, so the guards are the feature: fires once, only
when card 03 is genuinely the card in view, never under `prefers-reduced-motion`, and any of
wheel/touchstart/keydown/pointerdown hands control back immediately. `scroll-behavior` is forced
to `auto` for the duration — per-frame `scrollTo` against CSS smooth scrolling makes each call
start its own easing and the two fight.

Verified: hands-off, scroll runs 0->900 over ~2.6s while the fruit travels x 1072->245 and grows
R 11->153; a wheel event mid-transition stops it dead and it does not resume; reduced-motion never
moves the page at all. Mobile suite still passes.

## [DOM-ZERO-STALE] Root cause + permanent fix — 20:05 AEST
**Symptom:** (extends the 18:55 entry) Not 21 of 81 — **every one of 3,002 active
listings** had a wrong `days_on_domain`, all understated, almost all by exactly 19
days. The field was last computed ~2026-07-14 and frozen since. 70 read 0 outright.
Every "N days listed" figure on the site was wrong, plus the `newest`/`longest_listed`
sorts, the `domMin/domMax` filter, the >60d "stale" cohort, seller copy ("After N days
we'd reset the campaign"), FB post copy, and the step-120 editorial selector
(`days_on_domain <= 7`), which kept re-qualifying old listings as new.
**Root cause:** `run_curlffi_suburb_scrape.py` — a recompute was added to
`save_to_mongodb()` on 2026-07-15 (logged then as V3-DOM-STALE), but the 2026-06-06
incremental-scrape change made that path unreachable for existing listings: `run()`
short-circuits known URLs into `_refresh_existing()`, which wrote only `last_updated`
and `price`. So the previous fix was correct code in a dead branch — the reason this
recurred. `first_listed_timestamp` is absolute, preserved on purpose, and present on
3,002/3,003 docs, so it is the correct source of truth.
**Fix:** Three layers.
  1. SOURCE — moved the recompute into `_refresh_existing()`, the path 100% of existing
     listings actually take, and added `first_listed_timestamp` to the `existing`
     projection in `run()` (without it the recompute would silently never fire).
  2. DATA — ran `scripts/backfill_days_on_domain.py --apply`: 3,000/3,002 corrected.
     "New this week" (editorial-complete) went 36 -> 4.
  3. SAFETY NET — new `check_stale_days_on_domain()` in `data_quality_validator.py`
     (nightly process 116, depends_on 103/104), auto-heals with a 1-day tolerance and
     records to `system_monitor.data_quality_errors`. Also fixed a latent bug there:
     the `land_size_sqm_corrected` early-`continue` skipped ALL checks on corrected
     docs, so it now only skips the two land-size checks. Verified end-to-end by
     corrupting a live Robina doc to 0 and confirming the validator restored it to 24.
  4. READ PATH — `decision-feed-v3.mjs` now derives days via `daysSinceListed(doc)`
     from `first_listed_timestamp`, treating a stored 0 as "unknown" rather than
     "listed today".
**Files:** /home/fields/Property_Data_Scraping/03_Gold_Coast/Gold_Coast_Wide_Currently_For_Sale_AND_Recently_Sold/run_curlffi_suburb_scrape.py,
scripts/data_quality_validator.py, scripts/backfill_days_on_domain.py (run),
Feilds_Website/01_Website/netlify/functions/decision-feed-v3.mjs
**Recurrence:** 2nd (first: V3-DOM-STALE, 2026-07-15 — fixed in an unreachable branch)

## [PRICE-FORMAT-ABBREV] Agent price strings rendered as "$1.795m" — 20:10 AEST
**Symptom:** Listing cards showed "Offers Above $1.795m". CLAUDE.md Rule 5 requires
`$1,250,000`, never `$1.25m`. The forbidden format was reaching the page through
third-party agent copy rendered verbatim.
**Root cause:** `decision-feed-v3.mjs` passed `doc.price` straight to `price_display`.
No normalisation existed — `parsePriceString()` only parses to a number for internal
maths, it never rewrites the display string.
**Fix:** Added `formatPriceDisplay()` to `shared-utils.mjs` — rewrites only the numeric
token ($1.795m -> $1,795,000, $980k -> $980,000, handles ranges and "Million"/"mil"),
preserves the agent's wording, and returns anything unparseable untouched. Applied at
the `price_display` assignment. "Auction", "Contact Agent" and already-formatted
figures pass through unchanged.
**Files:** netlify/functions/shared-utils.mjs, netlify/functions/decision-feed-v3.mjs
**Recurrence:** First

## [FONT-SOURCE-CONFLICT] theme.css and fonts.css disagreed on the body font — 20:15 AEST
**Symptom:** `styles/theme.css` set `body { font-family: "Poppins" }` while
`fonts.css` set `body { font-family: var(--font-body) }` (Inter). Whichever bundled
last won, so neither file could be trusted to say what the site renders in.
**Root cause:** Two files both claiming ownership of global typography.
**Fix:** Removed the font-family declaration from theme.css (it keeps colour tokens
only) and documented in fonts.css that it is the source of truth. NOTE: the initial
read of this — that Poppins was dead weight and `@fontsource/poppins` could be dropped
— was WRONG. Poppins is named explicitly by ~100 declarations across
PropertyReport/sections/*, AdjustmentExplainer, ExpandableExplainer, ValueDrivers and
ValuationScatterPlot. Removing those imports would have broken the seller mini-site's
typography. The site is genuinely three-font: Inter (body), IBM Plex Mono (labels),
Poppins (report/valuation components). Zero visual change from this fix.
**Files:** src/styles/theme.css, src/fonts.css
**Recurrence:** First

## [PRICE-FORMAT-ABBREV] Follow-up: our OWN formatters were the bigger offender — 20:40 AEST
**Symptom:** After shipping `formatPriceDisplay()` for agent copy, the live
`/for-sale-v3` page STILL showed abbreviated money — "Est. $2.6m – $3.3m". Caught by
screenshotting the live page rather than trusting the API fix.
**Root cause:** The remaining instances were not third-party copy at all — they were
Fields' own formatters. `formatPrice()` in both `decision-feed-v3.mjs:139` and
`decision-feed.mjs:203` deliberately emitted `$2.6m` for any value >= $1M, and both
files built price-bucket labels as `$${ceiling / 1_000_000}m`. A `formatPriceFull()`
producing the correct format already existed in the same file, unused by these paths.
This is the exact string in Will's original screenshot ("Est. $2.6m – $3.3m").
**Fix:** `formatPrice()` now returns the full `$2,600,000` form in both files, and the
bucket labels use `toLocaleString('en-AU')`. Fixes all three call sites per file at
once (estimated_range low/high, "Best buy under X" label, price-band label).
**Not changed:** `search-intent-explorer.mjs:888` (internal ops dashboard, not
public-facing) and `property-insights.mjs:502` (m² floor area, not money).
**Files:** netlify/functions/decision-feed-v3.mjs, netlify/functions/decision-feed.mjs
**Recurrence:** 2nd (same problem-id, same day — the first pass fixed agent strings but
missed our own formatters, because I verified the API payload instead of the page)

---

## [FRUIT-DRAWN-SPRITES] Rolling fruit now uses the real drawing — 14:20 AEST

**Symptom:** the resting fruit did not look like a pandanus fruit, and was far too large.

**Size:** 306px across, sized by feel. The drawing gives the honest number — the tree sits ~6m
off and the fruit is ~22px there, so a fruit a metre away is ~130-150px. Now 154px.

**Appearance:** Will supplied `Other images/Pandanas_Palm_Fruit.png`, a 1254x1254 pen-and-ink
pandanus head in the same hand as the tree. New `build_fruit_sprites.py` wraps its real drupe
texture onto a sphere and renders 24 frames of one barrel roll about the horizontal long axis
(`fruit_sprites.png`, 917 KB). `pandanus_fruit.js` draws the sprite and keeps the procedural
fruit only as the pre-load fallback.

Sprite sheets were previously argued against because separately-drawn frames strobe. That
objection does not apply here: every frame comes from ONE source image through ONE 3D mapping,
so consistency is structural rather than hoped for.

**Three things the source forced, each found by looking at the output:**
1. **Bracts hang across the fruit's face.** They would smear around the sphere as it rolled, so
   the texture is sampled from a strand-free patch of drupes (candidates A-D compared; the
   lower-right region won) rather than from the whole fruit.
2. **The drawing has its own baked lighting** (bright upper-left). Left in, the highlight rotates
   WITH the surface instead of staying where the light is. The patch is de-lit by dividing out a
   heavily blurred copy, then re-lit per frame.
3. **Mirror-tiling the texture put a visible reflected seam down every sphere.** A reflection is
   a symmetry the eye reads instantly in a pattern as regular as packed drupes. Replaced with
   forward tiling and a feathered cross-fade, plus a per-repeat lateral shift so the tiling does
   not line up in columns. Only the around-axis direction needs to wrap; the axial one is
   traversed once.

**Also:** the first sheet was 3.6 MB RGBA for what is greyscale ink plus an alpha cut-out —
three identical channels. Saved as LA and cut to 24 frames at 200px (it is only ever drawn at
~154px): **917 KB**.

**Files:** `15_Off-Market/Concepts/Near_beach_palm_reveal/{build_fruit_sprites.py,
pandanus_fruit.js,fruit_sprites.png,fruit_sprites.json,Other images/Pandanas_Palm_Fruit.png}`

**Recurrence:** First

## [PRICE-FORMAT-ABBREV] Third pass: the source was the editorial PROMPT — 21:05 AEST
**Symptom:** After both display-layer fixes deployed, 7 abbreviated-money strings
remained in the live feed payload — but not in any formatter. They were inside
AI-generated editorial stored in Mongo: `ai_analysis.verdict` on 3 properties
("listed ... at 'Offer Above $1.8m'") and `ai_analysis.headline` on 1
("Now open at $1.5m+. Here's what changed.").
**Root cause:** `config/property_editorial_prompt.md` — the master prompt for the
Editor Agent — had NO number-format rule, unlike `scripts/generate_feed_hooks.py`
which has carried one all along. Worse, the prompt's own worked examples used the
forbidden format 26 times ("$3M", "$2.2M", "$30K–$50K", "$1.85M to $2.50M"). Models
imitate examples over stated rules, so the prompt was actively teaching the breach.
**Fix:** Added Rule 8 to PART 5.5 (write money in full, always — including when
quoting an agent's listing price), with a correct/incorrect example pair. Then
normalised all 26 example figures in the prompt itself, protecting only the lines
that deliberately display the wrong format. 5 abbreviated strings remain, all inside
Rule 8's counter-examples, verified by inspection.
**Not fixed — needs a decision:** the 4 already-published properties still carry the
old text. Regenerating means re-running step 120 for them, which rewrites live
published editorial and costs Max tokens. Left for Will to approve rather than
silently rewriting published content.
**Files:** config/property_editorial_prompt.md
**Recurrence:** 3rd (same problem-id, same day: display layer for agent copy ->
our own formatters -> the generation prompt. Each pass fixed a real instance; only
checking the rendered page and then tracing the payload field-by-field found them all.)
