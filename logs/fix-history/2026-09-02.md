# Fix History — 2026-09-02 (AEST)

## [ORGANIC-OFFMARKET-CRM-BIND] Organic off-market owners: bind behaviour to CRM + tag PostHog person — late
**Symptom:** Google→/off-market owner-lookups already had device→address bound in
`crm_contacts.offmarket_home` (536 anonymous contacts, ~94% owner lookups), but their on-site
behaviour was NOT on the CRM (only 3/536 had `lead_web.activity`) and the address wasn't attributed
in PostHog itself — `property_address` was null, `lead_web.posthog_distinct_id` null, so
`lead_web_activity.py` skipped them and the crm-contact page showed no session/address.
**Fix:** Extended `offmarket_home_signal.py` (already owns this nightly write) to, per contact:
(1) stamp `lead_web.posthog_distinct_id` (+ distinct_ids, source=offmarket_organic) → lead_web_activity
harvests full behaviour → timeline/dwell/CTA/read on the crm-contact page, same as FB leads; (2) sticky
`property_address` = the bound address (only where absent — never clobbers a confirmed one); (3) tag the
PostHog PERSON via server-side `$identify $set` (public ingest key, mirrors lead-link-visit.mjs) with
`home_address`/`home_address_slug`/`home_confidence=medium_high`/`home_source`, gated by
`offmarket_home.ph_tagged_slug` so nightly re-runs skip unchanged persons. Rule 7b added
(sessions>0 but 0 contacts ⇒ raise). Scope = google single-address lookups only (Will's call — high
confidence; excludes deck-browsers). distinct_id → lead_web ONLY (never posthog_ids — crm_sync fork).
**Verified live:** ran it — **517 contacts bound + 517 PostHog persons tagged** from 560 sessions;
`lead_web_activity` 34→548 leads with behaviour. crm-contact page for 280/1 Vue Boulevard Robina shows
address + Website activity + 9-entry timeline. PostHog `persons` table: 517 with `home_confidence=medium_high`
(⚠ verify home_* on the PERSONS table/API, NOT events.person.properties — frozen at ingest). All fields
crm_sync-carried (`lead_web`, `property_address`, `offmarket_home`).
**Files:** `scripts/offmarket_home_signal.py`. Pushed 5de19e7.
**Recurrence:** First.

## [DECK-DWELL-IN-TIMELINE] Off-market DECK viewers' dwell/depth folded into the CRM timeline — later
**Symptom:** `/off-market` has two renderers — V5 (fires `page_engagement` w/ wall/engaged_seconds +
max_depth_pct) and the discovery DECKS (Discovery/V3/V4/OffMarketDeck) which fire a DIFFERENT schema
(`time_on_page.duration`, `deck_exit.reached_pct`, `card_dwell`) and NO page_engagement. So deck
viewers' timeline dwell came only from page-gap timing and depth was blank.
**Fix:** `lead_web_activity.build_timeline` now folds both schemas per pageview window — seconds =
max(page_engagement.wall/engaged, time_on_page.duration); depth = max(page_engagement.max_depth_pct,
deck_exit.reached_pct). Added `time_on_page`+`deck_exit` to TIMELINE_EVENTS and their props
(`duration`, `reached_pct`) to the query. ⚠ Deliberately EXCLUDED `scroll_depth` (highest-volume
event; its depth is already covered) — including it 504'd the cohort query on weight.
**⚠ Also fixed a latent bug it exposed:** a pageview with no MEASURED seconds fell back to the gap to
the next `$pageview`; when that "next" pageview is a return visit days later, the dwell read **30,922
min** (~21 days). Gap-based dwell is now capped at `DWELL_GAP_CAP_S=1800` (30 min, standard session
cap); measured seconds are never capped (a real long read stays real).
**Verified:** re-ran harvest (548/551) — deck view of 3 Avocet Avenue now shows 2.00 min @ 100% and
3.00 min @ 88%; the 30,922-min entry now caps at 30.00.
**Files:** `scripts/lead_web_activity.py`. Pushed 5ea7060.
**Recurrence:** First.


## [REPORT-LEAD-CRM-BIND] Leadpage property-report leads never joined to their CRM contact — 21:40 AEST
**Symptom:** Owner-Market carousel leads (and all Analyse-Your-Home *leadpage* leads) had
their device→address attribution captured on `property_reports.owner.posthog_distinct_id`,
but no CRM record logged their on-site behaviour. The `/api/v1/crm-contact` page for these
people showed "No linked on-site session yet", and `lead_web_activity.py` skipped them.
**Root cause:** Two docs for the same device existed but nothing joined them. `property_reports`
holds device↔address (written by `analyse-your-home-submit`). `crm_sync` separately creates a
`crm_contacts` doc for the same distinct_id once it qualifies on engagement — but ANONYMOUS:
`lead_web.posthog_distinct_id` null, no address. `lead_web_activity.py` only harvests contacts
where `lead_web.posthog_distinct_id` is set (the tokenised-click-through arm), so the leadpage
arm produced no behaviour log, and the crm-contact page's `lead_web` block stayed empty.
**Fix:** New `scripts/bind_report_leads_to_crm.py` — for every real (`is_test != True`)
`property_reports` lead with an `owner.posthog_distinct_id`, find the contact by
`posthog_ids: did` (seed one keyed by the deterministic `contact_id(did)` if absent so
crm_sync merges, never forks) and stamp `lead_web.posthog_distinct_id` (+ `distinct_ids`,
landing, report_slug, source) plus a sticky `property_address` / `source` / `lead_attribution`.
Also writes `owner.crm_contact_id` back onto the report. After that `lead_web_activity.py`
harvests the full pageview stream automatically and the crm-contact page renders it. Wired into
`nightly_lead_chain` after `crm_sync`, before `lead_web_activity`. Added `lead_attribution` to
`crm_sync.build_contact_doc` carry-forward so the hourly `replace_one` doesn't wipe it (same
LANDMINE as `lead_web`). Rule 7b: report-leads-with-a-did > 0 but 0 bound ⇒ raise.
**Verified:** 29 leadpage leads bound (1 contact seeded); `lead_web_activity` 7→31/33 bound
leads with activity; live `GET /api/v1/crm-contact?id=7dab…` for 38 Balmain St returns HTTP 200
rendering the home address + "Website activity → 6 pageviews … top: /off-market/38-balmain-…"
with the full page list (their own off-market page ×2, the Varsity article, market-intelligence).
Heartbeat `bind_report_leads_to_crm` self-registered on the Process Registry (status=success).
**Files:** `scripts/bind_report_leads_to_crm.py` (new), `scripts/nightly_lead_chain.py`,
`scripts/crm_sync.py`. Pushed to Will954633/Fields_Orchestrator
(1e4ce45, d27d57b, b8627a6). `crm-contact.mjs` needed no change (already tries string `_id`
before ObjectId).
**Recurrence:** First.

## [REPORT-LEAD-CRM-LINK] Live Leads tracker link column opens the CRM page for bound report leads — 22:05 AEST
**Symptom:** After the bind, the CRM behaviour page existed but wasn't reachable from the
tracker Will actually uses — the Live Leads sheet's link column (col O) only ever pointed at
the raw PostHog person feed.
**Root cause:** `refresh_posthog_links` only ever emitted `POSTHOG_PERSON_URL` and never
overwrote an existing cell.
**Fix:** `ayh_rows` now carries `owner.crm_contact_id`; `refresh_posthog_links` emits a signed
`=HYPERLINK(crm-contact,"CRM ↗")` (new `crm_contact_link()` helper, same HMAC scheme as
`priority_calls_to_sheet.crm_link`) for any lead with a bound contact, OVERWRITING an existing
PostHog link so pre-bind rows upgrade; skips when the cell already shows "CRM ↗" (idempotent —
verified: second run = "Nothing new"). Leads without a bound contact keep the PostHog person
link. Per Will's choice (repurpose the column, not add one). 28 AYH rows now link to their CRM
page.
**Files:** `scripts/live_leads_to_sheet.py`. Pushed c300f1f.
**Recurrence:** First.

## [LEAD-WEB-TIMELINE-AND-ARTICLE-READ] Timeline + dwell + article-read/CTA on the CRM page — 22:40 AEST
**Symptom:** The crm-contact page's "Pages" section was an aggregate count list. Will needed
(a) a reverse-chron timeline with per-page dwell in minutes (2dp), and (b) to know, per lead,
whether they clicked the off-market page's "The market has turned. Has your home? →" button
and whether they actually READ the market-update article.
**Root cause / finding:** No missing instrumentation — all signals were already captured:
`page_engagement` (engaged_seconds/wall_seconds/max_depth_pct, fired on unmount; 681 events/60d,
185 people), `article_view`, `scroll_depth` (max_depth), `article_cta_click`, and the button's
own `offmarket_market_article_open` (source=banner_market_turned). `lead_web_activity.py` was
only pulling `$pageview` aggregates, so none of it reached the CRM.
**Fix:** `lead_web_activity.py` — new `fetch_timeline()` pulls the chronological event stream for
the cohort; `build_timeline()` builds a reverse-chron timeline (dwell = page_engagement.wall_seconds
where present else gap to next $pageview; engaged_minutes + max_depth_pct attached BY TIME WINDOW,
not $pathname — on SPA unmount the engagement event's $pathname is already the next route),
`articles[]` (per article page: engaged + max depth + read flag = ≥30s & ≥50%), and
`cta_market_turned` (the banner button). `last_seen` from the full-event aggregate is the final
page's dwell fallback (timeline events omit $web_vitals so the last $pageview would else read 0).
`crm-contact.mjs` renders a Timeline block + a "Market-update article" block (Clicked CTA? Opened?
Actually read? with % scrolled + engaged mins).
**Verified live:** 38 Balmain crm-contact page — "Clicked 'The market has turned' CTA: No; Opened:
Yes 12:44; Actually read it: Yes — 100% scrolled, 2.65 min engaged" + 6-row reverse-chron timeline.
**⚠ Finding worth Will's attention:** `offmarket_market_article_open` (the "market has turned"
button) has fired ZERO times in 60 days across ALL 185 tracked visitors, despite V5 being the live
default in the 3 suburbs since 2026-08-21 and the capture being deployed (confirmed in the GitHub
file). Either the button is rarely reached (placement/visibility on the V5 page) or nobody's clicked
it — a UX question, not a tracking bug. None of the 4 leadpage leads clicked it; only 38 Balmain
read the market-update article, reached via /market-intelligence, not the button.
**Files:** `scripts/lead_web_activity.py`, `netlify/functions/crm-contact.mjs`. Pushed 5514572
(orch) + 09265b6 (website).
**Recurrence:** First.

## [FIND-AUTOOPEN-ARTICLE] /find landing now blinks the CTA then auto-opens the article — 2026-09-02 late
**Symptom:** Owner-Market FB ads send the address-first `/find/<suburb>` landing → on submit it
navigates to `/off-market/<slug>?from=find`, which smooth-scrolled to the "Your market update"
section but deliberately stopped there ("does NOT open the article"). Leads were reaching the
section but not reading — Will wants the article opened for them.
**Fix:** Extended the `?from=find` deep-link effect in `OffMarketV5.tsx`: after the scroll settles
it BLINKS the CTA (`#mu-open-link`, new `.muLinkBlink` keyframe — 3 fast opacity flashes) to anchor
the eye, then AUTO-OPENS the market article (`setShowMarket(true)`) so they start reading with the
full per-address page visible behind the modal. `?from=mailer` keeps guide-only (scroll, no open).
Honours prefers-reduced-motion (no blink, tighter timing). The auto-open fires
`offmarket_market_article_open` with **source='find_autoopen'** — a DISTINCT source, and
`lead_web_activity.build_timeline` excludes it from `cta_market_turned` so "did they DELIBERATELY
click the CTA?" stays truthful; the `offmarket_market_article_read` (modal close) still fires, so we
still learn whether they actually read it after the auto-open. Timing: land→hero (0), scroll (1400ms),
blink (2350ms), open (3400ms). `tsc --noEmit` clean.
**Files:** `src/pages/OffMarketPage/v5/OffMarketV5.tsx`, `OffMarketV5.module.css`,
`scripts/lead_web_activity.py`. Pushed f4e0a77 + f5d21f5 (website) + 1a163ba (orch).
**Recurrence:** First.

## [INTERNAL-OPTOUT-SILENCES-WILL] Will's own clicks never logged — he's opted out, not a button bug — 23:20 AEST
**Symptom:** Will reported clicking the "The market has turned. Has your home? →" button
"multiple times" yet `offmarket_market_article_open` had 0 events in 60d; I wrongly concluded
the button was "rarely reached / placement issue".
**Root cause:** `posthog.ts:283` hard-opts-out any browser flagged internal
(`window.posthog.opt_out_capturing()`), which stops ALL events leaving that browser. Internal =
`fields_internal=1` in localStorage OR host `vm.fieldsestate.com.au` OR referrer via the ops
dashboard (`/ops-hub-4417`). Will is the founder on flagged devices, so his entire sessions emit
nothing. Confirmed in PostHog: 60d `is_internal=true` = 1 event / 1 person vs `false` = 28,241 /
2,398. A live incognito test with `?fields_internal=0` (which clears the flag + calls
`opt_in_capturing`, posthog.ts:231) logged his click cleanly: `offmarket_market_article_open`,
`source=banner_market_turned`, `is_internal=false`, 22:47:30. **The button + its tracking work;
the zero was self-inflicted by the internal opt-out.** Retracted the placement theory.
**Fix:** none needed (documentation/method fix — verify from a non-internal browser before
concluding an event doesn't fire). **Method note:** a zero-count is evidence about who is being
measured, not about the feature (cf. Rule 8 for reads).
**Recurrence:** First.

## [REPORT-MODAL-READ] "Did they read the market-update article?" blind spot via the button — 23:40 AEST
**Symptom:** The banner button opens the market-update article in an iframe ReportModal
(`setShowMarket`), NOT the `/articles/` route, so the `page_engagement`/`scroll_depth` reading
signals never fired for that path — the button's article open was untracked for READING (click was
tracked). Will's incognito test confirmed: click logged, but "opened, didn't read" was unmeasurable.
**Root cause:** `ReportModal` (OffMarketV5.tsx) instrumented nothing but an Escape handler; the
market artifact loads in an `<iframe src=/api/v1/offmarket-artifact...>` — same-origin but never read.
**Fix:** Instrumented ReportModal — because the iframe is same-origin, a scroll listener on
`iframe.contentWindow` + a 1s engaged-time heartbeat (only while tab visible) accumulate
`max_depth_pct` + `engaged_seconds`; on close/unmount it fires `offmarket_market_article_read`
`{kind, slug, wall_seconds, engaged_seconds, max_depth_pct, doc_height_px}` (covers the market-update
AND valuation-report modals + both buttons — shared component). `lead_web_activity.build_timeline`
now emits a unified `activity.market_update` read signal combining BOTH paths — the `/articles/`
route (page_engagement) and the modal (offmarket_market_article_read, kind=market-update) — best
depth/engagement wins, with `via` = article_page|report_modal. `crm-contact.mjs` renders it (Opened?
via which path; Actually read? % scrolled + min engaged). Verified backend: 38 Balmain resolves
`market_update{via:article_page, 100%, 159s, read:true}`. Full `tsc --noEmit` clean.
**Files:** `src/pages/OffMarketPage/v5/OffMarketV5.tsx`, `netlify/functions/crm-contact.mjs`,
`scripts/lead_web_activity.py`. Pushed 06488f9 + ee06dd8 (website) + 8bc6115 (orch).
**Verified end-to-end 2026-09-02:** Will's incognito read-test (`?fields_internal=0`) fired
`offmarket_market_article_read` kind=market-update, **depth=100%, engaged=59s**, is_internal=false;
`build_timeline` folded it into `market_update{via:report_modal, 100%, 59s, read:true}` alongside
his `cta_market_turned` click. Summary renders "clicked 'market has turned' CTA; READ the
market-update article (100.0% deep, 0.98 min, via report_modal)". Click-only vs read now produce
clearly distinct signals. (Note: the read event fires on modal CLOSE/unmount — a still-open modal
shows no event yet; also allow for PostHog ingestion lag of a few seconds.)
**Recurrence:** First.

## [FRIDGE-ENGAGEMENT-LEDGER] Fridge-magnet interactions now logged to Mongo + CRM — 09:20 AEST
**Symptom:** Fridge-magnet (/fridge QR) engagement was PostHog-only — not durable, not CRM-joinable, no address→household attribution. Will could not tell if the magnets attract any engagement.
**Root cause:** No server-side write path; the static /fridge page only fired PostHog events.
**Fix:** New `netlify/functions/fridge-event.mjs` (routed `/api/v1/fridge-event` via `config.path`) writes a per-device ledger to `system_monitor.fridge_scans` (every land/open/click/address event, counted + logged) and, on address entry, find-or-creates the address's `crm_contacts` record with a `fridge_engagement` sub-doc back-attributing the device's full history. `fridge.js` mints `fields_device_token` + beacons every `emit()`. Added `fridge_engagement` to `crm_sync.py` carry-forward (as `or {}` — LANDMINE 2 avoided). Daily `fridge_engagement_report.py` (cron 08:10, Rule-7 heartbeat, 7b: zero scans = valid signal, raises only on DB error). Verified live end-to-end (ledger + CRM doc written correctly), then cleared test docs for a clean slate.
**Files:** netlify/functions/fridge-event.mjs (new), public/fridge/fridge.js (via concept deploy.py), 15_Off-Market/Concepts/Fridge_Magnet_Concept/fridge.js, scripts/crm_sync.py, scripts/fridge_engagement_report.py (new), crontab.
**Recurrence:** First

## [PRONTO-ORDER-ADMIN] Pronto order numbers + canonical handbook — 11:05 AEST
**Symptom:** No individual order number per Pronto job; Drive job folders unnumbered; no canonical VM home telling us how to run a new order (naming, manifest, print spec, pack-out) — batches scattered across mailer_v2 and 17_Direct_Letterbox.
**Root cause:** Fulfilment grew ad hoc from the first 2026-08-17 run; only a build runbook existed, no admin/order layer.
**Fix:** Adopted `PD-000N` order-number convention (appended to existing Drive folder names). Renamed the two live job folders → `..._PD-0001` / `..._PD-0002` via a one-off Node script reusing the gdrive MCP OAuth (MCP tools have no rename op). Created `fulfilment/README.md` (order handbook: supplier, numbering, Drive layout, filename convention, manifest schema, John's standing print spec, pack-out recipes incl. address-matched hand-written notes, stock tool, per-order checklist) and `fulfilment/ORDER_REGISTER.md` (PD-0001/0002 logged, next = PD-0003). Also updated PD-0002's Drive READ_ME with pack-out + post-shipment inventory (50 envelopes / 150 magnets remaining).
**Files:** fulfilment/README.md, fulfilment/ORDER_REGISTER.md (both pushed to Fields_Orchestrator); Drive folder renames; Drive READ_ME_print_spec_2026-08-26.txt.
**Recurrence:** First

## [PD-0003-NONOTE-AB] Owner-teaser no-note A/B batch + mail tracking system — 12:20 AEST
**Symptom:** Needed a new 50-address Pronto batch = same teaser+magnets as PD-0002 but NO hand-written note (A/B control for the note's effect on QR engagement), plus a durable record of every address mailed / what was sent / postage date.
**Root cause:** N/A (new capability). Gaps found en route: PD-0002 was never recorded as a work order (stock looked healthier than reality); lead selection was an ad-hoc /tmp file with no committed script; no mailed-address ledger existed (DB-only exclusion would silently re-mail PD-0002's 50).
**Fix:** (1) committed `select_teaser_leads.py` (reproducible ranked pull from organic_journeys, house-only, excludes already-mailed via manifests+work-orders+mail_log). (2) Registered A/B flow pair `Fields_OT`(note)/`Fields_OTN`(no-note) in fulfilment_flows.yaml; teaser+QR held byte-identical so the note is the only variable; attribution by disjoint slugs. (3) Built 77 teasers over 2 tranches (softened market → 44% pass on the easing-home copy guard), laid up top 50 as Fields_OTN.1, all QRs verified. (4) Stood up `system_monitor.mail_log` + `scripts/mail_log.py` (record/backfill/export/set-posted/mailed-slugs) + `scripts/mail_log_to_sheet.py` ("Mail Log" tab, Rule-7 heartbeat) + `fulfilment/MAIL_LOG.csv`; backfilled PD-0001/PD-0002/PD-0003 (150 pieces). (5) Recorded PD-0002+PD-0003 work orders, tagged PD-0001 — stock now truthfully shows -50 envelopes (PD-0003 needs restock). (6) Staged Drive folder Fields_OTN.1_2026-09-02_PD-0003 (PDF+manifest+READ_ME) for Will's review; NOT sent to John.
**Files:** scripts/mail_log.py, scripts/mail_log_to_sheet.py, config/fulfilment_flows.yaml, fulfilment/ORDER_REGISTER.md, fulfilment/MAIL_LOG.csv, 17_Direct_Letterbox/Owner_Subject_Article/{select_teaser_leads,combine_tranches,run_teaser_batch,assemble_teaser_layup}.py; Drive PD-0003 folder.
**Recurrence:** First

## [MAILER-AUTOOPEN-ARTICLE] Printed-QR mailer scans now auto-open the article — 12:55 AEST
**Symptom:** Teaser printed-QR (`/off-market/<slug>?from=mailer`, on PD-0002 + PD-0003) only SCROLLED to the market-update section; testing showed readers reached it but didn't click through to open the article.
**Root cause:** Deliberate earlier choice — `?from=mailer` was "guide only, no auto-open" while `?from=find` (FB /find landing) did scroll+blink+auto-open. Will asked to extend the /find behaviour to the mailer.
**Fix:** In `OffMarketV5.tsx` the auto-open block gate changed from `if (from === "find")` to `if (from === "find" || from === "mailer")`, so mailer scans now scroll → BLINK the CTA (`mu-open-link`) → AUTO-OPEN the ReportModal market article. Analytics source is per-pathway (`find_autoopen` vs `mailer_autoopen`) so deliberate clicks and the two entry paths stay separable. No reprint — the printed QR already carries `?from=mailer`; only page behaviour changed.
**Verify:** tsc clean; pushed (commit 9d7e7a4); polled live deploy until bundle carried `mailer_autoopen`; headless (Playwright) on live `…?from=mailer` → article auto-opened + CTA blinked TRUE, control (`?from=organic_control`) did NOT auto-open; screenshot confirms the modal open. Deploy + change logged.
**Files:** 01_Website/src/pages/OffMarketPage/v5/OffMarketV5.tsx
**Recurrence:** First (related: FIND-AUTOOPEN-ARTICLE)
**Note:** A/B side effect — both PD-0002 (note) and PD-0003 (no-note) mailer scans now auto-open, so "opened article" is no longer the differentiating metric; the note's effect must be read from downstream dwell/read-through, not open rate. Flagged to Will.

## [LIVINGMAP-MISSING-HOUSE-TILES] Living Map aerial blurs at Home zoom on Burleigh + Varsity — 12:55 AEST
**Symptom:** On `/off-market` (private property report) and `/property` pages, the interactive
aerial ("Living Map") background blurs into unrecognisable blobs once the zoom-in finishes,
while the parcel boundary/dot/labels stay crisp. Reported on 14 Treeview Drive, Burleigh Waters
(mobile screenshot).
**Root cause:** DATA build gap, not an engine bug. `precompute_living_map.py` fetches per-property
`house` (z20) + `street` (z17) Google Static Maps tiles and shares only `suburb` (z14) + `city`
(z10). The 2026-08-28 build saved **6,478 burleigh_waters + 6,185 varsity_lakes** docs with ONLY
the shared suburb+city tiles — the per-property house/street fetch failed for those two suburbs
(env/quota during that chunk; the key + z20 imagery both work today, verified). Robina (11,360)
built cleanly. With no house tile, the canvas engine's Home-level descent (z≈19.6) upscales the
z14 suburb tile by 2^(19.6−14) ≈ **48×** → total blur. The `#05060a` canvas fill + crisp vector
overlays are why only the raster blurs. **Self-heal trap:** the nightly off-market job filters
`living_map:{$exists:False}`, and these docs HAVE a living_map (just no house tile), so it would
NEVER repair them — permanently blurry until a forced backfill (Rule 7b shape: partial success
read as success).
**Fix:** (1) New surgical `scripts/backfill_living_map_tiles.py` — reuses the stored
`living_map.center`, re-fetches house(z20)+street(z17) only, `$set`s `living_map.tiles.{house,street}`;
does NOT recompute routes/POIs/comps (all fine) so it can't regress a good layer. Gated on
`living_map.tiles.house ∈ {null,""}`; Rule 7b assertion (eligible>0 & fixed==0 ⇒ raise); heartbeat
`backfill_living_map_tiles`. (2) Ran it for the reported property — 14 Treeview Drive now has both
tiles (house.jpg 337 KB / street.jpg 485 KB, blob HTTP 200 image/jpeg; z20 source visually sharp).
(3) Will approved the full backfill — ran over all 12,663: **12,456 fixed, 207 failed**. Varsity
Lakes + Robina now 100% clean. The 207 failures are all Burleigh Waters, all HTTP 403 in a burst
at the tail = the Static Maps key hit its **daily quota** at ~25k calls (403 returns Google's
over-quota PNG error tile; `fetch_static_tile` raises on it so NO garbage tile was saved — the 207
kept their old state cleanly). Retry scheduled for after the midnight-PDT quota reset (idempotent —
re-run only touches still-missing docs): `backfill_living_map_tiles.py --suburb burleigh_waters`. (4) Will approved a defensive engine clamp:
`LivingMapHero/engine.ts` now sets a per-property `zmax = min(ZMAX, maxNativeZoom + 1.3)` and
every zoom target (setZoom/zoomStep/goLevel/flyOut/compBack/fitBounds/startDescent) clamps to it,
so a missing house tile degrades to a slightly-soft street view (~2.5× upscale) instead of a 48×
blur. House-tile-present properties are unchanged (zmax stays 20.4). `tsc --noEmit` clean.
**Files:** `scripts/backfill_living_map_tiles.py` (new, pushed Fields_Orchestrator 47ccee4);
`01_Website/src/components/LivingMapHero/engine.ts` (pushed Website_Version_Feb_2026 1143928,
deploy logged 6a9790f).
**Recurrence:** First.
