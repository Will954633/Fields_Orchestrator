# Fix History — 2026-08-11

## [FIELDS-AUTOMATION-4-WORKFLOW-FAILURES] Four Sunday workflow failure emails — two real causes, and only one of them was a real failure — 14:39 AEST

**Symptom:** Will received four "All jobs have failed" emails from `Will954633/fields-automation` for the 2026-08-09 (Sunday) scheduled runs: Market Insight, Market Shift, Market Drivers, State of the Market. The same four workflows had all succeeded on 2026-08-02.

**Root cause — these were TWO unrelated problems, and the two halves are not equally serious:**

1. **Market Insight + Market Shift (a real, 3-week-old outage).** Both run the same script, `pipeline/run_market_insight.py`. Line 968 built a raw metered client — `anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)` — and the script never read `USE_CLAUDE_MAX` anywhere, despite both workflow files setting `USE_CLAUDE_MAX: "1"` and carrying a comment claiming `ANTHROPIC_API_KEY` was merely "an automatic fallback". It was in fact the *only* path. The metered Anthropic org has had no credit since 2026-07-20, so every run 400'd with `Your credit balance is too low` at the Selector agent. There was also a hard `if not ANTHROPIC_API_KEY: sys.exit(1)` precondition that would have blocked the keyless Max path even after the client was swapped.

2. **Market Drivers + State of the Market (not actually failures).** Both correctly did nothing: Market Drivers hit its own recency guard (`⏭️ Skipping — last generated 49 days ago (guard: 60 days)`), State of the Market likewise produced no new article, and `push_to_ghost.py` reported `No articles found matching criteria` for both. They then failed on an *unconditional* `git push`. `actions/checkout` pins the checkout to the SHA the run was **queued** at, not to current `main`. These four runs were queued 21:28–22:36 UTC but the single self-hosted runner only got to them at 23:36–23:39, and other workflows (Watch This Sale, Market Monitor) pushed commits in that window. So local `main` was strictly *behind* remote `main`, and `git push` was rejected as `non-fast-forward` **even with zero local commits** — pushing a branch that is an ancestor of the remote would rewind it, so git refuses. The reflog confirms it: at 09:36–09:38 AEST there are four checkouts resetting `main` backwards to `33e99389` and no `commit:` entries at all. **No content was lost by these two.**

**Fix:**
- `pipeline/run_market_insight.py` now calls `make_client(api_key=ANTHROPIC_API_KEY)` from the repo's existing `pipeline/claude_max_client.py` (the same helper `article_generator.py` has used all along), and the API-key precondition is now `if not ANTHROPIC_API_KEY and not _use_max()`. Verified `MaxClient.messages.create` is signature-compatible with both call sites (string content, no `tools`, `.content[0].text`).
- Added the `git pull --rebase --autostash origin main` guard — already proven in `market_insight.yml` since 2026-07-23 — to the **11 workflows that still lacked it**. All 13 now have it.

**Verification (not assumed — actually run):**
- Ran `run_market_insight.py --dry-run --force` with `USE_CLAUDE_MAX=1` on the VM as user `projects`. `--dry-run` executes the Selector agent, i.e. the exact call that was 400ing. It returned a full insight set and exited 0.
- Confirmed the GitHub Actions runner service runs as `projects` — the same user the test ran as — so the verified Max credential path is the runner's path.
- Dispatched a real `market_drivers.yml` run (31459081317) after pushing. **Green.** Its log shows the new guard on precisely the previously-fatal nothing-to-commit path: `Already up to date.` → `Everything up-to-date`.
- All 13 workflow files re-parsed with `yaml.safe_load`; `run_market_insight.py` byte-compiled.

**Files:** in `Will954633/fields-automation` (NOT `Fields_Orchestrator`) — `pipeline/run_market_insight.py`, and `.github/workflows/{annual_refresh,december_listing_paradox,how_it_sold,is_now_good_time,is_now_good_time_sell,market_drivers,market_monitor,state_of_market,update_pass_monthly,watch_this_sale,what_x_buys}.yml`. Pushed as ONE commit `b525180d` via `scripts/push_website_files.py --repo/--root`, all 12 md5-verified.

**Recurrence: 2nd for both halves — and both recurred because the 2026-07-23 fix was scoped too narrowly.** See `[FIELDS-AUTOMATION-GH-ACTIONS]` in `logs/fix-history/2026-07-23.md`:
- The push race was diagnosed correctly that day but the guard was applied to only **3 of 13** workflow files. The entry even reasoned about pre-emptively covering workflows that had not yet failed — it just stopped at the three that were in front of it. The two that failed on 08-09 were among the ten left untouched.
- The credit-balance half was written off as *"still blocked on Will topping up credits, nothing to fix in code"*. That was wrong, and it cost ~3 weeks of dead Market Insight / Market Shift output. There was code to fix: the workflows were already passing `USE_CLAUDE_MAX=1` and the repo already contained `claude_max_client.py`. The same day, `[APPRAISAL-EDITORIAL-PLACEHOLDER-IN-REAL-PDFS]` migrated a different generator off the same dead API for the same reason — the pattern was in hand and simply was not applied here.

**Lesson:** "blocked on billing" is a conclusion to test, not to record — check whether a credit-free path (Max) is already wired into the caller before declaring an outage external. And when a shared-runner defect is found in one workflow file, fix the class across every file that shares the pattern, not just the ones currently red.

---

## [FIELDS-AUTOMATION-METERED-CLIENT-SWEEP] Applied the "fix the class" lesson immediately — 5 more generators found on the dead metered API — 15:05 AEST

**Symptom:** None reported. Found by running the audit the entry above says to run (`grep -rn "anthropic.Anthropic(" --include=*.py`) instead of stopping at the four workflows Will was emailed about.

**Finding:** five more scripts built a raw metered client and so ignored `USE_CLAUDE_MAX`, exactly like `run_market_insight.py`: `run_state_of_market.py`, `run_annual_refresh.py`, `run_update_pass.py`, `run_light_rail.py`, `run_market_strength.py`. Two workflows (`annual_refresh.yml`, `update_pass_monthly.yml`) were also missing `USE_CLAUDE_MAX` from their `env:` block entirely. **`run_state_of_market.py` is the one that mattered** — it is on the weekly Sunday schedule and only escaped failing on 2026-08-09 because it happened to generate nothing that night; the moment it had an article to write it would have 400'd.

**Fix:** all five now use `claude_max_client.make_client(api_key=...)`; their `if not ANTHROPIC_API_KEY: sys.exit(1)` preconditions are now `and not _use_max()`; `USE_CLAUDE_MAX: "1"` added to the two workflows lacking it. `pipeline/` now has zero raw `anthropic.Anthropic(` constructions outside `claude_max_client.py` itself. `market_monitor.yml` is the only workflow without `USE_CLAUDE_MAX` and correctly so — `run_market_monitor.py` makes no Claude calls at all.

**⚠ Explicitly NOT fixed — three of the five are still broken, for a different reason.** `run_annual_refresh.py`, `run_update_pass.py` and `run_light_rail.py` each make a **server-side `web_search_20250305` tool call** for their research step. `MaxClient._Messages.create` cannot express `tools` through the one-shot `claude` CLI and deliberately routes any such request to `_fallback()` — i.e. straight back to the credit-dead metered API — and the call is **unguarded**, so it raises. Migrating them fixed their *writer* step, which they never reach. This is a strict improvement with no regression, but it is **not a fix**, and pretending otherwise is how the 07-23 entry went wrong. The Max-native replacement is `claude_max_client.research()` (agentic WebSearch billed to Max), but it returns plain text whereas these call `extract_sources(response)` on a structured response — so it needs real rework, not a swap. Left annotated in-place at each call site with a `⚠ KNOWN BLOCKER (2026-08-11)` comment so the next reader doesn't assume the migration covered it. Blast radius is low and non-urgent: `update_pass_monthly` is monthly (next run ~end of Aug), `annual_refresh` is dispatch/annual, and `light_rail` no longer has a workflow file in the repo at all.

**Verification:** all 13 workflows re-parsed with `yaml.safe_load`; all six migrated scripts byte-compiled **and** import-smoke-tested by running `--help` under `USE_CLAUDE_MAX=1` from `pipeline/` (compilation alone would not have caught a bad `from claude_max_client import ...` path). Only `run_market_insight.py` has been exercised end-to-end against a live Claude call; `run_state_of_market.py` and `run_market_strength.py` are pattern-identical and tool-free but have **not** been live-run.

**Files:** `Will954633/fields-automation` — `pipeline/run_{state_of_market,annual_refresh,update_pass,light_rail,market_strength}.py`, `.github/workflows/{annual_refresh,update_pass_monthly}.yml`. One commit `0fe917f3`, all 7 md5-verified.

**Recurrence:** 3rd for the metered-client class (2026-07-20 property_reports, 2026-07-23 appraisal generator, now the fields-automation generators). The pattern is always the same and the fix is always `make_client` — the failure is never that it is hard, only that the sweep is scoped to whatever is currently red.

---

## [FIELDS-AUTOMATION-LIVE-PROOF] Both fixes confirmed end-to-end on real dispatched runs, including the race itself — 15:40 AEST

On Will's instruction, re-ran the two workflows that had missed the week's content. Dispatched **together deliberately** — the runner is single-concurrency, so the second job queues and its checkout goes stale exactly as it did on 2026-08-09. That reproduces the race on purpose instead of assuming the guard works.

- **Market Insight** (31460959900) — **success**. Selector ran on Claude Max (the metered API is still dry, so it cannot have been anything else), 2 articles written, `Done. Pushed: 2 | Skipped: 0 | Errors: 0`, push `0fe917f3..98f5e6a4`.
- **Market Shift** (31460966294) — **success, and it hit the race.** Checked out at `0fe917f3`; Market Insight landed `98f5e6a4` while it queued; the new guard reported `Successfully rebased and updated refs/heads/main` and pushed `98f5e6a4..1cdd3aa4`. **Under the previous bare `git push` this is precisely the run that would have been rejected `non-fast-forward`.** Both halves of the fix are now proven on live runs, not by pattern-matching.

4 articles created (2 per workflow, both formats off the same delta report, all Varsity Lakes). All 4 are `status: draft` — they sit in the Article Manager for review and **nothing was published to the live site**. Note the two pairs are near-duplicates by design, not by bug: Market Insight writes the website-standard format and Market Shift the Facebook-optimised one, and both selected the same two top signals (asking-vs-valuation gap, fresh-listing supply drop).
