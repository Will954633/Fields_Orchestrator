# Fix History — 2026-09-15

## [MAILER-QR-LEAD-TOKEN] Mailer QR now carries a per-address lead token → scans bind to CRM — HH:MM AEST
**Symptom:** Will asked whether mailer opens are recorded against the property doc or a CRM record. Audit found they are recorded **nowhere durable** for anonymous scanners. Of the 7 genuine post-lodgement opens (Sep 8–15), only Noel Williamson (28 Riverwalk Ave) reached a `crm_contact` — because he'd first clicked a tokenised CRM email, binding his device. The other 6 exist **only as anonymous PostHog events** and will age out: the mailer QR (`/off-market/<slug>?from=mailer`) carried no per-recipient token, so `offmarket_home_signal.py` (google-referrer only) and `lead_web_activity.py` (identity-bound leads only) both skip them, and `mail_log` records the send but had no "opened" writeback. Gap already flagged in `fulfilment_flows.yaml`.
**Root cause:** The QR was address-attributable (slug) but not person-attributable — no `?lead=<token>`. The identity-join infra to fix it already existed (`root.tsx` global `useLeadIdentify` → `phIdentifyLead` → `lead-link-visit.mjs` → `lead_web` → `lead_web_activity.py`), but nothing minted or resolved a mailer token.
**Fix:** Option 1 (per Will) — token on the QR, resolved lazily so no empty CRM stubs are created up front. (1) `build_owner_mailer.py`: new `ensure_mailer_token(client, slug, address)` mints/reuses a stable per-slug `link_token` in `system_monitor.mailer_link_tokens`; threaded a `lead_token` param through `_qr_target`/`teaser_html`/`byline_frontqr_html`/`qr_panel_html`/`build_mail_html`, so the scanned/clicked QR is now `/off-market/<slug>?from=mailer&lead=<token>` (printed human-typed fallback URL stays clean). (2) `mail_log.py`: `record()` copies `link_token` from the registry onto each per-piece doc; added `opened_at`/`opened_distinct_id` to the schema + export. (3) `lead-link-visit.mjs`: on a `crm_contacts` token miss, falls back to `mail_log` then `mailer_link_tokens`; if the scanner's device already maps to a contact it enriches that (no duplicate), else adopts the address into a stable `_id:"mailer:<slug>"` contact (never sha256(distinct_id), so crm_sync never prunes it); then the existing `lead_web` bind runs and `mail_log` opened_* is stamped via `updateMany`. Backward-compatible: the new path only runs on a token miss, so existing email/SMS lead binding is untouched.
**Files:** `17_Direct_Letterbox/Owner_Subject_Article/build_owner_mailer.py`, `scripts/mail_log.py` (both pushed to Fields_Orchestrator); `01_Website/netlify/functions/lead-link-visit.mjs` (validated, awaiting Will's go for the prod deploy — no batch is generating now, so no urgency).
**Verification:** `py_compile` clean on both Python files; `_qr_target` unit-checked (`…?from=mailer&lead=abc123def`); `node --check` clean on the function. Confirmed end-to-end wiring by reading each hop (root.tsx:126 global handler, posthog.ts phIdentifyLead posts distinct_id, crm_sync carry-forward already includes link_token+lead_web, crm_sync does not prune non-hash _ids). Runtime bind not yet exercised against a real scan — first real confirmation will be the next tokenised batch.
**Recurrence:** First (of this specific token gap).
**Follow-up (same day, full attribution):** Will's requirement escalated to full ROI attribution — every recipient's device tagged to their address and their ENTIRE cross-site journey linked to it, to judge whether the mailer method is worth continuing. Confirmed the backbone already exists: `phIdentifyLead` calls `posthog.identify(token)` so every future visit from that browser is stored under the token, and `lead_web_activity.py`'s `all_ids()` already includes `link_token`, so the nightly harvest pulls the full journey onto the contact. Added two things: (1) `lead-link-visit.mjs` `identifyPerson()` now also `$set`s `home_address`, `home_address_slug`, `is_mailer_recipient`, `mailer_token` on the PostHog PERSON, so "all behaviour of mailer recipients" is a native person-property filter (was anonymous before). (2) New `scripts/mailer_attribution_report.py`: per-batch funnel sent→posted→OPENED→returned→engaged→converted with a per-address detail line, joining `mail_log` × PostHog off-market `?from=mailer` opens × each device's journey. Genuine-open filter = open timestamp ≥ piece posted_date (auto-excludes pre-lodgement QA scans — no hard-coded device id); single-attribution (each open credited to the most-recent off-market-scheme piece whose delivery cutoff precedes it), fixing a slug-shared double-count across batches; PD-0001's /your-home scheme is flagged out-of-scope. Set real lodgement dates from Will: PD-0002 (OT.1)=2026-09-07, PD-0003 (OTN.1)=2026-09-10. First run: **7 genuine opens / 100 posted (≈7%)** — PD-0002 4/50, PD-0003 3/50 (still delivering), 2 returned, 1 engaged, 0 converted; matches the manual PostHog analysis exactly. `job_run(cadence_hours=24)` self-registered on the Process Registry board; Rule 7b raises if mail_log is empty.
## [ARTICLE-WALKTHROUGH-EXEMPTION + FB-POST-FORCE-ATTRERROR] Will-walkthrough content exempt from the editorial gate; fixed a rate-cap crash — HH:MM AEST
**Symptom:** The one article with a live Will walkthrough (`comparing-median-house-prices`, titled "Why you should be careful comparing median house prices") could not be posted to Facebook — "you should" tripped `fb_post_article.py`'s no-advice gate, so the one piece with an on-camera walkthrough couldn't reach the channel where our content wins. Separately, trying to post once the daily cap is reached crashed with `AttributeError: 'Namespace' object has no attribute 'force'` instead of refusing cleanly.
**Root cause:** (1) No exemption existed for content Will has personally reviewed. (2) `fb_post_article.main()` referenced `args.force` in the rate-cap check but `--force` was never added to the argparser — latent since the cap shipped, and hidden by short-circuit evaluation (`posted_24h >= cap and not args.force`) until the cap was actually reached (2 posted today).
**Fix:** (1) Will ruling 2026-09-15 — a walkthrough IS his editorial sign-off, so walkthrough articles are **exempt from the automated content gate** in both `fb_post_article.py` and `article_approval.py` (breaches are still logged, never silently — just not blocking). Keyed on `content_articles.will_walkthrough`; set the flag + `will_walkthrough_video` on comparing-median-house-prices; recorded the rule in the articles brief §5. (2) Added `--force` to the argparser; the rate-cap path now prints the intended REFUSED message. Verified: the article now passes the gate as EXEMPT, and hitting the cap refuses cleanly.
**Files:** scripts/fb_post_article.py, scripts/article_approval.py, 16_General_Reinforcement_Learning/briefings/articles.md, content_articles.will_walkthrough (all pushed).
**Recurrence:** First.

**Files (follow-up):** `01_Website/netlify/functions/lead-link-visit.mjs` (person address tags — pushed `041c2f78`, Netlify auto-deploys, deploy logged), `scripts/mailer_attribution_report.py` (new — pushed). Website function is now LIVE; roll back by reverting `041c2f78` if needed.
**Fridge magnets (Will Q):** the report deliberately does NOT fold fridge scans into the per-address funnel — the fridge magnet is a GENERIC stock magnet (2 per envelope) with a static QR to `/fridge` carrying no slug/token, so a scan is NOT attributable to an address or batch (only becomes address-linked if the visitor self-types their address → `fridge-event.mjs` writes `crm_contacts.fridge_engagement`). Added a clearly-labelled aggregate-over-time block instead, reusing `fridge_engagement_report.summarise()`: all-time 9 devices · 5 opened · 7 engaged · 0 address-entered → 0 CRM households. Pushed `5b3e0497`.

## [OFFMARKET-V5-ANALYTICS-DARK] V5 off-market page shipped with no reading telemetry — HH:MM AEST
**Symptom:** Will asked for a deep dive on how people interact with /off-market V5 — where they drop off, where they spend time, and whether the founder videos (added 2026-09-12) had any views. PostHog showed only ~15 views/day of traffic but **no way to answer drop-off or time-on-page**: V5 fired zero view/scroll/section/exit events. Confirmed live: scroll_depth on /off-market collapsed from 68–150/wk (V4 era) to 0–10/wk the week V5 became default (2026-08-21); `offmarket_v4_view` and `v4_report_exit` went to a literal 0. Founder videos: 0 plays and 0 opens anywhere since going live (instrumentation verified correct — genuine zero, not a bug: low traffic × deep placement × ~25s median session).
**Root cause:** When V5 (`v5/OffMarketV5.tsx`) replaced V4 as the default off-market experience on 2026-08-21, it never ported V4's `useReportAnalytics` hook. V5 fired only a handful of discrete click events (`offmarket_market_article_open`, the video events) and NO page-view, scroll-depth, per-section dwell, or exit telemetry, and registered no `offmarket_arm` super-property. The V4 hook could not be reused verbatim because it derives its section order from V4's `PARTS` structure, which V5's flat rail ladder does not have. **Second occurrence of this class** — see [OFFMARKET-DECK-ANALYTICS-DARK] (DiscoveryDeck shipped analytics-dark the same way). A new off-market arm going live without carrying its telemetry is now a repeat pattern.
**Fix:** Wrote `v5/useV5Analytics.ts`, a V5-specific port of the V4 hook, and wired it into `OffMarketV5`. Emits `offmarket_v5_view` (mount, with valuation_method/decline_reason/has_range/n_comps so full and directional pages never pool), `scroll_depth` (page:"offmarket_v5", milestones 10/25/50/75/90/100, reusing the deck/V4 schema), `v5_section_read` {section,index,ms} (midpoint attribution → per-section times sum to session total; ≥1500ms floor), and `v5_report_exit` (sendBeacon: max_scroll_pct, engaged_seconds, wall_seconds, deepest_* vs exit_*, scrolled_back, seconds_by_section). Registers `offmarket_arm:"v5"` and unregisters on cleanup. SECTION_ORDER maps V5's ten ladder sections by DOM id (hero → v5-near → v5-new → v5-valuation → v5-market-update → rail-market → rail-news → rail-yourhome → v5-downloads → v5-founder-video), so the "Why I built Fields" video section reach is now measurable. Keeps the deep-scroll (>50%) Telegram beacon to `/api/v1/offmarket-intent-alert` (the function already accepts `deep_scroll` arm-agnostically — no server change). LAYOUT_VERSION="2026-09-15" + auto SECTION_SIG guard.
**Files:** `01_Website/src/pages/OffMarketPage/v5/useV5Analytics.ts` (new), `01_Website/src/pages/OffMarketPage/v5/OffMarketV5.tsx` (import + hook call). Pushed to Website_Version_Feb_2026 as `43e41b5e` (new hook) + `069dd3ca` (wire-in); Netlify auto-deploys.
**Verification:** `tsc -p tsconfig.app.json --noEmit` → 208 errors, under the ~210 baseline, none in the new/edited files. ⚠ Events CANNOT be verified headlessly — posthog-js sends nothing when `navigator.webdriver` is set (see offmarket_v4_reading_analytics memory). Must confirm against INGESTED events from real traffic over the next 24–48h: expect `offmarket_v5_view` ≈ off-market pageviews, and `scroll_depth`/`offmarket_arm="v5"` to resume. Netlify edge serves the old bundle ~1h post-deploy, so the first post-deploy events legitimately lack the new fields.
**Recurrence:** 2nd (class: off-market arm shipped without telemetry — first was [OFFMARKET-DECK-ANALYTICS-DARK], DiscoveryDeck).

## [PROPERTY-PAGE-EDITORIAL-REORG] /property redesign — card reorder, room-size insights, freshness-gated price-analysis merge — 09:05 AEST
**Symptom:** Will asked for a batch of /property (V2) changes: make the personalization card stand out + arrow CTA; hide the floor-plan image (keep the heading); add per-room size insight to the Measured-rooms table; delete "Orientation and outlook"; move The Property card into the floor-plan section, Condition & Value under the photos, Market Position below "What is wrong with it"; and merge the Price-analysis card's headline/description/"what this means" into the "8 sales behind that range" section (dropping its duplicate comparables).
**Root cause:** N/A — feature work. The four editorial cards were rendered as ONE block (`PropertyAnalysisV2`, looping `ai_analysis.insights[]` by index), so individual placement required splitting them.
**Fix:** Extracted `PropertyInsightCard` (one insight card) + `InsightCardSection` (standalone wrapper w/ section-read analytics); `PropertyAnalysisV2` now renders Verdict only. `PropertyPageV2` places The Property (idx0) into `PropertyGalleryV2` (new `propertyCard` slot where the floor-plan `<img>` was), Condition & Value (idx1) after the gallery, Market Position (idx3) after trade-offs; Price Analysis (idx2) is passed into `PropertyComparablesV2`. Gallery: removed the floor-plan `<img>` (heading kept), deleted the Orientation column, and added per-room notes — suburb-relative size via the existing `property-insights` `room_percentiles` engine (fetched with `usePropertyInsights`), a fixed-rule garage capacity note, and a build-to-land note (floor-area÷lot). Comparables merge is **freshness-gated**: shows the editorial headline/lede/"what this means" only when `editorial_fresh` (server-computed: `ai_analysis.generated_at >= valuation_data.computed_at`), else the neutral number-free lede. Costs section left as-is per Will.
**Files:** `01_Website/src/components/PropertyInsightCard/{PropertyInsightCard.tsx,InsightCardSection.tsx,InsightCardSection.module.css}` (new); edited `PropertyGalleryV2.tsx(+css)`, `PropertyAnalysisV2.tsx`, `PropertyComparablesV2.tsx(+css)`, `PropertyPageV2.tsx`, `PropertyPage.tsx`, `PersonalizationSlot.tsx(+css)`, `netlify/functions/properties-for-sale.mjs` (adds `editorial_fresh`). Pushed to Website_Version_Feb_2026 as `5dce059c`; screenshot-verified live (section order + room notes + merged section all correct).
**Recurrence:** First.

## [INSIGHTS-SLUG-400] property-insights API 400'd on slug URLs → room-size notes never loaded — 09:05 AEST
**Symptom:** After deploying the room-size notes, only the client-side garage/land/largest-room notes rendered; the suburb-relative percentile notes ("Larger than most…") were missing. Live console showed `GET /api/v1/property-insights/8-gum-court-burleigh-waters?suburb=Burleigh%20Waters → 400`.
**Root cause:** `property-insights.mjs` did `new ObjectId(propertyId)` and returned 400 "Invalid ID" on failure. The /property page routes by **url_slug**, not the Mongo ObjectId, so every insights fetch from a property page 400'd. (The endpoint's only prior caller — PropertyReport — passed an ObjectId, so it worked there and the slug path was never exercised.)
**Fix:** Resolve either form: `isObjId = /^[a-f0-9]{24}$/i.test(id)` → `{_id: ObjectId}` else `{url_slug: id}`, used in both the suburb-hint and fallback-scan `findOne`s. Verified live: `…/property-insights/8-gum-court-burleigh-waters` → 200 with 8 `room_percentiles`.
**Files:** `01_Website/netlify/functions/property-insights.mjs`. Pushed as `110eed94`.
**Recurrence:** First.

## [EDITORIAL-VALUATION-DESYNC] Published /property editorial contradicts the recomputed valuation on the same page (systemic) — 09:05 AEST
**Symptom:** On 8 Gum Court, Burleigh Waters the Price-analysis card claimed eight sales "cluster tightly between $1,900,000 and $2,200,000" citing 17 Pitta Place / 132 Acanthus Avenue, while the SAME page's valuation showed adjusted comps of $1,529,021–$1,710,781 (entirely different addresses) and a computed range of $1,468,251–$1,946,287. Merging the two sections (as requested) would have put the contradictory figures adjacent.
**Root cause:** `valuation_data` recomputes on its own precompute cadence (a batch ran 2026-09-09→14) while `ai_analysis` is frozen at generation time (8 Gum Court's was 2026-08-19), with **no link invalidating/regenerating editorial when the valuation changes**. Audit of live for_sale listings in robina/varsity_lakes/burleigh_waters with both published editorial + valuation: **63 of 68 (93%) have a valuation newer than their editorial**; ≥12 have >10% range contradictions; 7 cite comps no longer in the valuation. Worse sub-case: `2 Eagle Avenue` and `42 Mountain View Avenue` have their range now **suppressed by the >$2M envelope rule** (`range:null`) yet the editorial still quotes $2.8M–$3.3M figures the model deliberately withholds.
**Fix (partial):** (1) Shipped the **freshness gate** (`editorial_fresh`) so the merge never surfaces stale-vs-fresh contradictions on any page — stale pages fall back to the neutral lede. (2) Regenerated + published 8 Gum Court's editorial on Opus/Max (`--force`; auto-publish suppressed by `--force`, so published manually after confirming `_verify_outcome=minor_flags` and that the new headline "$1,530,000–$1,710,000" matches the live comps). **Still open:** a one-time regeneration sweep of the ~63 stale editorials + a lasting invalidation link (valuation recompute → re-trigger/flag editorial) + a guard that strips $ figures when the range is envelope-suppressed. Recommended as a follow-up task.
**Files:** frontend gate in the commits above; 8 Gum Court `ai_analysis` regenerated in `Gold_Coast.burleigh_waters`.
**Recurrence:** First (systemic finding).

## [EDITORIAL-VALUATION-DESYNC-DURABLE-LINK] Wired valuation-recompute → editorial regen + envelope-$ guard — HH:MM AEST
**Symptom:** Follow-up to `[EDITORIAL-VALUATION-DESYNC]` above (which shipped only the render-time `editorial_fresh` gate + one manual regen). 62 of 68 published editorials in the target suburbs still had `valuation_data.computed_at` newer than `ai_analysis.generated_at` — stale content on the page, hidden by the gate but not fixed. 3 of those are >$2M homes the valuation model envelope-SUPPRESSES (`confidence.range=null`, `directional_only=true`) yet whose editorial still quotes a specific $ band (2 Eagle Avenue "$2,810,000–$3,060,000"). No durable link re-triggered editorial when a valuation moved.
**Root cause:** `ai_analysis` is frozen at generation; `valuation_data` refreshes ~weekly per property even with unchanged inputs (`precompute_valuations.py` `_should_skip_valuation`, `VALUATION_MAX_AGE_DAYS=7`, "comparables keep arriving"). Nothing linked the two, and the generator emitted a synthesised subject range even when the model had withdrawn its answer.
**Fix:** (1) New detect-only pipeline step **123** `scripts/editorial_valuation_sync.py` — runs nightly after 18+120, compares `generated_at` vs `computed_at`, classifies drift (envelope_leak/envelope_flip/material >10% range-move or comp-set change/stale_no_baseline/minor) using a `valuation_snapshot` now recorded on each editorial, flags `ai_analysis.valuation_sync` + enqueues regen-worthy rows to `system_monitor.editorial_regen_queue`. Self-monitored via `job_run` (Rule 7/7b): raises if desync detected but zero enqueued, or if zero published+valued editorials could be read; no watermark (derived state, recomputed each run). (2) **Envelope-$ guard** in `generate_property_ai_analysis.py`: when `_valuation_suppressed(prop)`, a no-$ directive is prepended to the agent prompt and `store_analysis` runs `_scan_envelope_dollar_leak` — any leaked subject $ range/worth forces `needs_review` (never auto-published). `config/property_editorial_prompt.md` rule 9 strengthened to forbid adjusted-comp subject ranges. (3) `--stale-valuation` generator mode regenerates exactly the flagged set (to draft; Will verifies+publishes). (4) Registered 123 in `schedule_manager.always_run_processes` (the real schedule — YAML `execution_order` alone does not run it, per the 121 precedent). Detector `--dry-run` verified: 68 scanned, 62 flagged, matches the audit. Regeneration sweep is Will-run (~$10/property); the render-time `editorial_fresh` gate is untouched.
**Files:** `scripts/editorial_valuation_sync.py` (new), `scripts/backend_enrichment/generate_property_ai_analysis.py`, `config/property_editorial_prompt.md`, `config/process_commands.yaml`, `src/schedule_manager.py`, `docs/EDITORIAL_VALUATION_DESYNC_SWEEP_2026-09-15.md` (new sweep plan + affected list).
**Recurrence:** 2nd (same class as `[EDITORIAL-VALUATION-DESYNC]` earlier today — that was the acute render gate; this is the durable content-level link).

## [VALUATION-WALKTHROUGH-DEFAULT-ON] Flip guided valuation walkthrough to default-visible — 00:26 AEST
**Symptom:** Will reported the valuation walkthrough wasn't showing on /property/7-beaconsfield-drive-burleigh-waters. Nothing was broken — the feature was still gated behind `?walkthrough=1` (its first-deploy state), so the bare URL only showed the demand-test CTA stub. Verified headlessly: bare URL → CTA present but no avatar/video mount; `?walkthrough=1` → walkthrough mounts, 0 console errors. Property cleared every gate (reconciled $1,742,209 in-envelope, range $1,498,299–$1,986,118 → `#cmp-range`, 8 comparables w/ 19 adjustments on card 0 → `#cmp-adj-0`).
**Root cause:** N/A — deliberate flag left on after live sign-off ([VALUATION-WALKTHROUGH-BUILD]). Will signed off; flip requested.
**Fix:** One-line gate flip in PropertyComparablesV2.tsx: `flag === '1'` → `flag !== '0'`. Every qualifying property now shows the walkthrough by default; `?walkthrough=0` opts out (back to the demand-test CTA). Updated the stale gate comment. The `worthy` guard (`#cmp-adj-0` + `#cmp-range`) is unchanged, so directional-only / rangeless / comp-less properties still won't offer it.
**Verify:** tsc --noEmit + npm run build clean. Pushed (commit f8ac8eb8). Polled live bare URL post-deploy: mounts avatar + valuation-comparables video with 0 console errors; screenshot confirms the resting circular avatar left of the orange "Walk through the valuation step by step" CTA. Deploy + change logged (website-deploy-tracker / website-change-log).
**Files:** src/components/PropertyComparablesV2/PropertyComparablesV2.tsx → Will954633/Website_Version_Feb_2026.
**Recurrence:** First (follows [VALUATION-WALKTHROUGH-BUILD], [VALUATION-WALKTHROUGH-CHOREOGRAPHY-V2])

## [PIXEL-STATS-FIRST-BUCKET-MISREAD] Reported Meta pixel as under-capturing off a single hourly bucket — 17:59 AEST
**Symptom:** During FB-strategy scoping I stated as fact that the Meta pixel (`1491613936314260`) recorded "~50 PageViews + ~30 ViewContent in the last 28 days" vs PostHog's ~300+/month — a "~6× under-capture" — and made "fix pixel capture" a high-priority workstream in the scoping doc.
**Root cause:** The diagnostic queried `GET /{pixel}/stats`, which returns ~24h of **hourly** buckets (not a 28-day total, despite passing a `start` 28d back), and the code printed only `j2['data'][:1]` — the **first hourly bucket** (`PageView:50, ViewContent:30`) — which was reported as the whole-period total. Same failure class as [POSTHOG-SELF-TRAFFIC-MISREAD-AS-SCRAPER] and [OFFMARKET-DIRECT-TEST-VALUE-BUG]: a number read without confirming what it actually represents (Rule 8 applied to reads).
**Fix:** Re-queried `/stats` and summed all 23 returned buckets = **459 PageViews + 323 ViewContent in ONE day** (~12k/28d); in an identical 24h window the pixel logged 2.9× PostHog (459 vs 158). A background agent independently refuted the premise and confirmed: no consent gating, Stape 404 dead path (removed 2026-07-15, replaced by working first-party CAPI `meta-conversions.mjs`), CAPI backstop live, SPA PageViews tracked. The ~20-person website audience is a **match-rate/ITP** issue (AU-mobile Safari caps `_fbp` ~7d), not capture. Corrected §3b of the scoping doc, voided the (never-needed) Workstream 0, and marked the diagnosis line refuted. One real opposite-direction bug noted for later: PageView double-fires on initial load (`root.tsx:345` raw `fbq` no eventID + `root.tsx:90` deduped CAPI) — optional cleanup behind sign-off, not applied.
**Files:** 03_Facebook/Strategy/krista_video_funnel_scoping.md (corrected). No code changed. Lesson: never report a `/stats` figure as a period total — sum all buckets or use Events Manager / Insights API; and never state "capture is broken" from an unverified single-bucket read.
**Recurrence:** 3rd of the "metric misread as fact" family (prior: [POSTHOG-SELF-TRAFFIC-MISREAD-AS-SCRAPER], [OFFMARKET-DIRECT-TEST-VALUE-BUG])

## [MAYJUN-TRAFFIC-DROP-IS-ADS-OFF] "May/Jun analytics gap" was ads switched off, not a tracking outage — 18:46 AEST
**Symptom:** PostHog YTD unique visitors showed Mar 2,340 / Apr 2,078 / **May 89 / Jun 80** / Jul 1,209. I flagged May/Jun in the FB strategy doc as "almost certainly a tracking gap" and Will asked me to dig in.
**Root cause of the DIP:** Not a tracking bug — **all paid advertising was off in May and June.** Independent, contemporaneous evidence: FB Graph `insights` (account-level, monthly) shows Mar $1,717 / Apr $508 / **no May or June row at all (zero delivery)** / Jul $1,953; `system_monitor.google_ads_keywords` cost by period shows Mar $329 / Apr $495 / **May $0 / Jun $0** / Jul $0. Site traffic tracks paid spend ~1:1; with both channels dark, traffic fell to its organic/direct floor (~80 uniques/mo) and recovered in Jul when FB spend resumed. The "recovery" I first attributed to a code fix was just ads turning back on.
**Root cause of MY false flag:** jumped to "tracking outage" from the shape of the curve without checking the obvious driver (ad spend). Same jump-to-conclusion family as [PIXEL-STATS-FIRST-BUCKET-MISREAD] earlier today. Ruled out the tracking-bug hypotheses properly this time: unfiltered query showed the same drop (not a `filterTestAccounts` artifact); daily granularity put the decline at Apr 15–17, which *predates* the Apr 28 internal-opt-out commits (`b98e27a8`/`1acef586`), so those weren't the cause either; the Apr 15–17 taper matches April's already-declining ad spend ($508 vs March's $1,717).
**Fix:** Corrected the scoping doc §3c monthly table (added FB/Google spend columns; relabelled May/Jun as "ads off, real traffic drop, NOT a tracking gap") and added a strategic finding: **this site's traffic is almost entirely ad-dependent — ~80 uniques/mo organic floor** — so the video-qualification funnel only grows while cold prospecting runs continuously.
**Latent issue noted, not chased:** the internal-traffic opt-out (`opt_out_capturing_by_default: __ph_internal`, persistent via `localStorage 'fields_internal'`) is a real footgun if that param ever leaks into a shared/ad link — but the data shows it did NOT cause the May/Jun dip. Also spotted two stale collectors (`website_daily_metrics` stopped 2026-03-19, `google_ads_daily_metrics` empty, `ad_daily_metrics` only from July) — tangential, flagged for later.
**Files:** 03_Facebook/Strategy/krista_video_funnel_scoping.md. No code changed.
**Recurrence:** 4th "metric misread / jumped conclusion" this cycle — the lesson compounding: check the business driver (ad spend, releases) before blaming instrumentation.

## [STEP105-DEAD-BLOB-HOST-SILENT-ZERO] Photo analysis marks properties "processed" with zero analysed images — 23:59 AEST
**Symptom:** Step 105 (photo analysis) reported success but silently produced no analysis for some properties. On the 2026-09-14 run, 3 of 5 "successfully processed" properties completed in <1s having analysed 0 images (130 image downloads failed with DNS NameResolutionError for `fieldspropertyimages.blob.core.windows.net`), yet were marked done and graduated out of the unprocessed queue permanently.
**Root cause:** Two gaps vs the floor-plan client (step 106): (1) the photo client `ollama_client_single_image.py` fetched stored image URLs directly, never rewriting the retired Azure blob host to the live host via `to_live_url()` — the floor-plan client `openai_floorplan_client.py` already does this (its docstring records step 106 burning 54 analyses on dead-host errors before the same fix). (2) `worker_multi.py` wrote an empty `image_analysis` and returned success when a property had image URLs but zero were downloadable — a Rule 7b silent-zero. (Note: step 105 already runs on Claude/Gemini via `shared/claude_vision.py`, migrated 2026-06-11 — it does NOT use OpenAI; the "GPT 429" premise was stale.)
**Fix:** (1) `ollama_client_single_image._download_and_encode_image` now calls `_to_live_url(image_url)` before fetching (mirrors the floor-plan client). (2) `worker_multi.process_document` now returns False (retryable failure, no DB write) when a property has image URLs but produced zero analyses, instead of writing an empty analysis and marking success. `run_production.py` already exits non-zero when all attempts fail and none succeed, so a wholesale zero-run now surfaces.
**Files:** Property_Data_Scraping/03_Gold_Coast/Gold_Coast_Wide_Currently_For_Sale_AND_Recently_Sold/Ollama_Property_Analysis/ollama_client_single_image.py, .../worker_multi.py (tested in isolation — success path on 20 Chantilly Pl returned real Claude analyses, zero-path guard verified; DEPLOYED + pushed to Will954633/Property_Data_Scraping 2026-09-15. ⚠ side effect for sign-off: a property whose images are permanently unmirrored will now re-fail each run rather than be silently marked done — correct per 7b, but if it's the only unprocessed doc on a night, step 105 exits non-zero. Optional follow-up: stub-and-skip truly-dead properties like step 106 does.).
**Recurrence:** Nth of the SILENT-ZERO family (prior: [STEP105-DICT-IMAGES], [STEP111-SILENT-ZERO], [INDEXING-SILENT-ZERO], [STEP6-OVERPASS-406-SILENT-ZERO-VALUATIONS]); and 2nd dead-Azure-blob-host recovery after the step 106 `to_live_url` fix.

## [ARTICLES-FB-RANKING-AND-RETURN-ATTRIBUTION] FB metric fixed + per-article return attribution built — 03:20 AEST
**Symptom:** (a) Facebook post metrics on articles read `clicks:0 / fan_reach:1` on all 3 posted articles, so the articles domain (whose brief tells it to "rank on post_clicks" and "learn from FB") had a dead ranking signal. (b) The Engagements tab showed return viewers only by coarse channel bucket ("News"), not by which specific article brought someone back — but return viewers is now the domain's PRIMARY metric.
**Root cause:** (a) Two-part: the page has near-zero ORGANIC reach (1–3 fans/post) so organic `post_clicks` are structurally ~0 (1 link click across all posts), AND `post-performance-tracker.py` froze each post's insights at 72h (finalized posts skipped unless `--refresh-insights`; the 6-hourly cron omitted it). Live Graph-API check proved the clicks are real Meta values, not a bug — post_clicks works, only the impressions/reach metric family is Meta-deprecated. (b) Not a data gap — the article slug is already in PostHog `$pathname`; `engagement_funnel_to_sheet.py` computed ~90% of it internally but collapsed `/news/<suburb>`→"News" and truncated slugs.
**Fix:** (Fix A) added `--refresh-insights` to the 6-hourly `post-performance-tracker.py` cron so insights stop freezing. (Fix B) `article_performance.py` now records `fb_organic.fb_referral_sessions` (Facebook-referred on-site sessions from `organic_journeys`, which captures posts+shares+ads — real volume) and RANKS on that + paid-headline clicks instead of dead organic `post_clicks`. (Return attribution) added a "RETURN BY ARTICLE" block to the `Attr · Return` tab in `engagement_funnel_to_sheet.py` — per-article "came back to" + "return trigger" counts, un-truncated labels, `/news` suburb kept + lowercased, ObjectId→slug mapped. Both dry-run verified; additive; existing behaviour + the article_performance heartbeat untouched.
**Files:** crontab (post-performance-tracker line), scripts/article_performance.py, scripts/engagement_funnel_to_sheet.py (all pushed to Will954633/Fields_Orchestrator). ⚠ Volume is thin — treat both FB-referral and per-article return as ranking signals, not precise rates.
**Recurrence:** First.

## [SEO-HOUSES-FOR-SALE-STRUCTURED-DATA] ItemList schema + unique suburb copy on /houses-for-sale/:suburb — 18:30 AEST
**Symptom:** The suburb landing pages (`/houses-for-sale/:suburb`) rank ~pos 28 for transactional queries like "houses for sale varsity lakes" (3 impr, 0 clicks/90d, GSC). On-page fundamentals were sound (title/H1/canonical/SSR) but two gaps: (1) the only JSON-LD was a site-wide `RealEstateAgent` org block — no `ItemList`/listing markup for the actual properties; (2) only one H2, no unique editorial *about the suburb* — just listing cards that change nightly, so no durable unique text.
**Root cause:** Not a bug — a coverage gap. The template never emitted per-listing structured data, and the summary metrics (`priceGrowthYoY`, `daysOnMarket`, `absorptionRateMonths`) returned by `getMarketMetricsSummary` were fetched by the loader but only median/active were threaded into the page.
**Fix:** (1) Added `buildListingsJsonLd()` — an `ItemList` of up to 25 listings, each item multi-typed `['Product','SingleFamilyResidence']` so it carries a structured `PostalAddress` (with postcode — the geo signal), beds/baths, image, and a schema-valid `Offer` ONLY when the price string parses to a number (`parsePriceNumber`); "Contact Agent"/"EOI"/"Present All Offers" omit the offer rather than fabricate one. Verified live: 12 items, 5/12 with numeric offers. (2) Added an "About {suburb}" section — a report-voice market sentence built from live metrics (median 12m rolling, YoY %, days-on-market, months of supply) + an evergreen per-suburb factual blurb (geography/amenities, `SUBURB_CONTEXT` map) + a cross-link to `/market-intelligence/:suburb` + a provenance note. Editorial rules honoured (no advice/predictions, data cited, no forbidden words). Threaded `priceGrowthYoY`/`daysOnMarket`/`absorptionRateMonths` through `HousesForSaleData` + loader. Applies to all three suburb pages.
**Files:** src/pages/HousesForSalePage/HousesForSalePage.tsx, src/pages/HousesForSalePage/HousesForSalePage.module.css, src/routes/houses-for-sale.$suburb.tsx
**Verification:** tsc clean, `npm run build` OK, pushed (commit b63be1f), Netlify deployed. Live curl confirms 2 JSON-LD blocks (RealEstateAgent + ItemList) and the About section; site-inspector screenshot verified rendering, 0 console errors (1 unrelated whale_song.mp3 media abort).
**Recurrence:** First

## [EDITORIAL-PRICE-ABSENCE-DESYNC] 10 live property pages said "no price guide" on listings that publish one — 18:19 AEST
**Symptom:** 10 published `/property` pages carried copy stating the listing had no price, while the listing's `price` field held a real published figure. Live-verified as Googlebot: 9 Auriga Court served `<title>` "9 Auriga Ct — No Guide, But 8 Sales Say ~$1,900,000" against a $1,949,000 guide; 6 Moorhen Place "No Price Guide Since 1 Aug" against Offers Over $1,800,000; 14 Manakin, 24 Bittern, 126 Christine and 830 Legend Trail rendered the same class in body copy. 58 false strings across 10 properties, spread over `headline`, `sub_headline`, `meta_title`, `meta_description`, `verdict`, `insights[].h2/key_points/lifestyle_hook/what_this_means`, `faqs[].answer`, `next_steps[]` and `cta_valuation.hook`. Oldest (4 Pipit Parade) had been false for **43 days** — guide published 2026-08-03, editorial written 2026-07-30.
**Root cause:** Two layers. (1) `ai_analysis` is frozen at generation and nothing re-triggers it when the *agent publishes a price guide*. Every affected property launched as Auction/EOI/"Contact Agent" — where the absence framing was TRUE — and was later priced. (2) The durable link shipped earlier today, step 123 `editorial_valuation_sync.py`, **structurally cannot see this class**: it gates every check on `ai_analysis.generated_at` vs `valuation_data.computed_at`, and publishing a price guide does not touch `computed_at`. A page can be factually false while every valuation timestamp reads "fresh". This is the same multi-layer trap as Rule 6's `market_pulse` bug and was predicted by memory `editorial_price_absence_framing` ("the `verdict` layer was never swept" — it named 9 Auriga specifically).
**Fix:** (1) Corrected all 10 live pages + 2 not-yet-live docs (34 Beauty Point Drive `suppressed_waterfront`, 7 Winton Terrace `draft`) that would have published the same falsehood. Applied the absence-framing rule — delete the absence clause, lead with the evidence — keeping comparable RANGES, stating the published guide as fact, and passing the Seller Test on every headline (no adjudication of a named seller's ask). Originals preserved in `<field>_previous`; `ai_analysis.absence_reframed_at` + `reframed_reason` recorded. (2) Extended step 123 with a `price_contradiction` severity (`_price_contradictions`, `_walk_strings`), checked **before** the desync gate precisely because valuation timestamps cannot detect it. Placeholder-aware: "Auction"/"Contact Agent"/"Offers Close …"/"Present All Offers" are genuinely unpriced and do NOT fire, so legitimate absence framing is untouched (QLD POA s216(2)(c) bars a guide on auction listings). Skips `*_previous` archives and our own audit fields.
**Verification:** Post-fix sweep returns 0. Live Googlebot re-fetch of 6 pages: 0 absence phrases rendered, new titles serving (e.g. "9 Auriga Court — $1,949,000 Guide, 8 Sales Agree | Fields"). Detector replayed against the pre-fix backup: **10/10 fire**, 5 unpriced-campaign negative controls stay silent, positive control fires, audit-field control silent. Full `--dry-run`: 68 scanned, `price_contradiction: 0` (correct — all fixed), existing severities unchanged.
**Files:** `scripts/editorial_valuation_sync.py` (pushed 5e0353a), 12 property docs in `Gold_Coast.{robina,varsity_lakes,burleigh_waters}`
**Recurrence:** First for the price-drift trigger; 3rd in the `ai_analysis`-staleness class today (after `[EDITORIAL-VALUATION-DESYNC]` and `[EDITORIAL-VALUATION-DESYNC-DURABLE-LINK]`). The pattern: each fix closed one *input* to a frozen editorial. Valuation drift is now linked; price/listing-state drift is now linked; the general lesson is that `ai_analysis` has several independent inputs and each needs its own invalidation trigger.

## [STEP105-STUB-AND-SKIP] Permanently-gone images now stubbed (graduated out) instead of re-failing nightly — 00:20 AEST
**Symptom:** After the [STEP105-DEAD-BLOB-HOST-SILENT-ZERO] honest-zero fix, a property whose images are permanently unrecoverable (every URL 404s even after `to_live_url` rewrite — e.g. never mirrored) would re-fail every night and could turn step 105 red if it were the only unprocessed doc.
**Root cause:** The honest-zero fix treated ALL zero-analysis outcomes as retryable failures, not distinguishing "transient (retry)" from "permanently gone (will never recover)". Step 106 already stubs genuinely-dead-image cases; step 105 didn't.
**Fix:** `ollama_client_single_image._download_and_encode_image` now returns `(encoded|None, outcome)` where outcome ∈ ok/gone(404,410)/transient. `analyze_property_images` tallies `downloads_gone`/`downloads_transient` into metadata. `worker_multi.process_document` now returns SUCCESS/STUBBED/FAILED: when zero images analysed AND every attempted image is `gone` → `mongodb_client_multi.mark_stubbed_unrecoverable()` writes an auditable stub (`ollama_analysis.processed=True, skipped=True, skip_reason="images_unrecoverable"`) so it graduates out; any transient failure still returns FAILED (stays in queue, retries). `run_production.py` tallies + logs `stubbed` separately from `failed`; the exit-non-zero guard fires only on true failures with zero successes, so stubs never mark the step red but are always logged/counted (not a silent-zero).
**Files:** Property_Data_Scraping/03_Gold_Coast/Gold_Coast_Wide_Currently_For_Sale_AND_Recently_Sold/Ollama_Property_Analysis/{ollama_client_single_image.py, worker_multi.py, mongodb_client_multi.py, run_production.py} — pushed to Will954633/Property_Data_Scraping.
**Recurrence:** Follow-up to [STEP105-DEAD-BLOB-HOST-SILENT-ZERO] (same day). Tested all 3 paths (success/stubbed/transient) with a recording fake DB client.

## [DROP-OPENAI] Removed OpenAI dependency across pipeline; paused unused voice agent — 00:25 AEST
**Symptom:** OpenAI account credit-exhausted (429) for weeks. Will: "why do we need OpenAI / drop it."
**Root cause:** The property/valuation/editorial/vision pipeline was already migrated to Claude/Gemini (2026-06-11) but still carried dead/vestigial OpenAI code paths, one stale `OPENAI_API_KEY` gate that would silently disable a live feature, and one genuine dependency (the voice agent's STT/TTS/chat).
**Fix:** (a) Removed vestigial `OPENAI_API_KEY` (step117_satellite_analysis.py), dead `_get_openai_client` (on_demand_valuation.py — vision already routes via claude_vision), dormant OpenAI fallback (property_reports/inline_floor_plan.py — Gemini/Claude primary retained), and the stale `OPENAI_API_KEY` gate in property_reports/hero_photo.py (scoring already runs on vision_text; the gate would have disabled hero selection when the key is removed). Fixed stale `model="gpt-4o"` metadata. Tested classify/analyse/hero on Gemini/Claude — all work. Pushed to Will954633/Fields_Orchestrator. (b) Assessed the voice agent (fields-voice-agent.service): localhost-only :8090, no nginx route, no caller in website/scripts, 0 HTTP requests all-time in the journal — clearly not a live surface. Stopped + disabled it (reversible: `sudo systemctl enable --now fields-voice-agent`).
**Files:** scripts/step117_satellite_analysis.py, scripts/on_demand_valuation.py, scripts/property_reports/inline_floor_plan.py, scripts/property_reports/hero_photo.py; systemctl stop+disable fields-voice-agent.
**Recurrence:** First. OpenAI now fully dropped — no live component depends on it.

## [ARTICLES-NO-ONWARD-ROUTING] The article corpus had 2 internal links in total — 18:19 AEST
**Symptom:** 1 of 101 published articles linked to the address/AYH funnel, and across the entire corpus there were **2 internal `href`s** (`/disclaimer`, `/article`). Every article was a dead end. This defeats the articles brief's goal #6 ("educate each reader on what Fields does and route them onward — the McKinsey pattern") and, more importantly, the domain's new PRIMARY metric, RETURN VIEWERS.
**Root cause:** Never built. The article generator emits a self-contained body with a methodology note and a disclaimer; nothing ever appended an onward link. It was raised twice as a recommendation (REC-articles-007, then again as part (b) of REC-articles-009) and never actioned, because both times it was framed as something to ask Will about rather than something inside the domain's standing authorisation to rewrite/optimise existing published articles.
**Fix:** Appended a light "Keep reading" block to all 101 published articles, linking to the relevant `/news/<suburb>` hub (or `/news/gold-coast` where no single suburb resolves). Target resolved from tags first, then an unambiguous title mention, else Gold Coast — 26 Robina / 19 Burleigh Waters / 13 Varsity Lakes / 43 Gold Coast. Inserted before the trailing methodology/disclaimer `<hr>` where one exists, appended otherwise; idempotent via a `news-hub-link` marker.
**Why the /news hubs and not the conversion funnel:** this cycle's new RETURN-BY-ARTICLE data (built 2026-09-15) shows the hubs ARE the return engine — 17 of 21 content-page returners came back to a `/news/<suburb>` hub, versus 1 to an article. Routing to the thing that demonstrably brings people back serves the primary metric directly. The parasocial playbook also warns that overt CTAs trigger persuasion-knowledge and discount goodwill accumulated by prior pieces, so this is a "keep reading" link, not a sell.
**Verification:** 101/101 carry the block, 0 duplicates, all four hub URLs return 200 as Googlebot, and two live articles re-fetched confirm it renders (articles are DB-served, not build-baked, so no deploy was required). ⚠ Baseline for grading: return-visit lift is expected to be small and slow — 128 return visits over the last 60 days is the whole denominator.
**Files:** 101 documents in `system_monitor.content_articles` (`html`, `onward_routing_added_at`)
**Recurrence:** First

## [FRIDGE-SUBJECT-PROPERTY-V2] Per-address magnet → personalised fridge — 19:30 AEST
**Symptom:** New per-address ("My Market Update") magnet's QR went to /your-home/<slug>; Will wanted it to open the fridge animation pre-personalised to the subject property (news + off-market + walkthrough video launcher).
**Root cause:** Feature request, not a bug. The fridge (`/fridge`) was a generic shared landing that asks the visitor to pick suburb/enter address.
**Fix:** Additive `?slug=` mode in the fridge concept (`15_Off-Market/Concepts/Fridge_Magnet_Concept/` — index.html + fridge.css + fridge.js). When a slug is present: derive suburb+address, pre-fill "What's happening in <suburb>" (→/news/<suburb>) and "The latest on <address>" (→/off-market/<slug>), hide the redundant sold/for-sale/worth shelves, and reveal a circular-poster + play launcher → /news/<suburb>?play=1 (the deep-play long-form suburb walkthrough). Slug-less scan = today's exact 4-shelf generic fridge, so the ALREADY-POSTED generic magnets (QR = plain /fridge) are unaffected — verified live with a no-slug control. Analytics tagged fridge_arm:'v2' + subject_slug (slug-less stays v1). Deployed via deploy.py → push_website_files.py (commit f5337c41). Repointed the magnet generator QR to /fridge?slug=<slug> (no link key needed; /off-market/<slug> is public) and regenerated+pushed artwork.
**Files:** Fridge_Magnet_Concept/{index.html,fridge.css,fridge.js}; website public/fridge/{fridge.html,fridge.css,fridge.js}; 00_Run_Commands/Logo_Files/fridge_magnet/individualised/build_card_market_update.py + Fields_Magnet_MarketUpdate_25Huntingdale_90x55_{300dpi.png,PRINT.pdf}; verify/{subject_shot,prod_subject}.mjs.
**Gotcha:** `gh api ... --method PUT` (contents API) printed "unexpected end of JSON input" while the HTTP response was 200 OK and the write LANDED (gh 2.87.3 output-parsing quirk). Do NOT trust gh's exit message for contents PUT — verify by comparing `git hash-object <local>` against the remote `.sha`. Cost me several "failed" retries that had actually partly succeeded.
**Recurrence:** First

## [RL-DUE-FOR-GRADING-TZ-BOUNDARY] due-for-grading silently skips items due "today" — 19:40 AEST
**Symptom:** On 2026-09-13, REC-seo-001 and REC-seo-002 both carried `outcome_check_due: 2026-09-12T21:12 UTC` (past their due date) yet did NOT appear in `recommendations.py due-for-grading`; the SEO agent only found them by reading raw `feedback` output. A "closed loop that isn't" — if that command is the grading trigger, grading silently stops.
**Root cause:** `cmd_due_for_grading` compared `outcome_check_due` (a full UTC ISO timestamp = the exact instant `grade_in_days` after shipping) against the current UTC instant as strings (`{"$lte": _iso()}`). The recs shipped 2026-08-15T21:12 UTC, so due exactly 28d later at 2026-09-12T21:12 UTC = 2026-09-13T07:12 AEST. The SEO cycle ran at 2026-09-13T07:00 AEST (= 2026-09-12T21:00 UTC) — 12 minutes before the due instant — so `due <= now` was False. Everyone reasons about grading by DATE ("due 2026-09-12"); the code demanded a sub-day UTC instant pass, in a different timezone.
**Fix:** `due-for-grading` now fetches all `status=shipped` items and decides dueness by AEST *calendar date* (`_due_date_aest(due) <= today_aest`), so an item due on a date surfaces any time on/after that date in the operating timezone. Added a Rule 7b guard: shipped items with `outcome_check_due=None` can never surface, so they are now printed loudly (and warned on stderr in `--json`) instead of vanishing.
**Files:** `16_General_Reinforcement_Learning/recommendations.py` (new `_due_date_aest` helper; rewritten `cmd_due_for_grading`)
**Test:** Inserted 3 throwaway shipped docs — a boundary item (due instant 20 min in the future but AEST date = today), a genuinely-future item (+3d), and an undated item. OLD string-`$lte` query surfaced none of them; NEW logic surfaced only the boundary item, excluded the future item, and the guard flagged the undated one (stdout + stderr in JSON mode). Test docs deleted; command runs clean on real data ("nothing due" — all real shipped items are due 09-19+).
**Recurrence:** First

## [SEO-LANDING-PERF-NO-TIMESERIES] seo_landing_performance can't answer before/after — 19:40 AEST
**Symptom:** `system_monitor.seo_landing_performance` stored a single rolling 90-day snapshot, `delete_many({})`-then-insert every night, with only one `computed_at` and no date key. Grading any SEO recommendation needs two windows, so every grading required a bespoke ad-hoc GSC pull (the SEO agent wrote one by hand on 2026-09-13).
**Root cause:** The collector overwrote the whole collection each run, discarding all history.
**Fix:** Added a SEPARATE bounded history collection `system_monitor.seo_landing_performance_history`. The main collection is left byte-identical (still delete+insert), so all three existing readers (`16_General_Reinforcement_Learning/seo_signal.py`, `scripts/article_performance.py`, `scripts/brain2/seo_pilot_status.py` — all query by `dims` with no date filter) are 100% unaffected. Each run appends the compact gradeable signal — `__site_totals__` rows + `dims=page` rows with impressions ≥5 — tagged with `snapshot_date` (AEST date), `window` (start/end/days), `computed_at`. Idempotent per day (`delete_many({snapshot_date})` first); bounded by pruning `snapshot_date < today-180d`. Rule 7b: if the main snapshot had rows but none qualified for history (broken GSC pull), it prints a loud warning rather than silently skipping.
**Files:** `scripts/brain2/seo_landing_performance.py` (history constants + append/prune block in `main()`)
**Test:** Isolated logic test against current snapshot wrote 3339 history rows (3338 pages ≥5 impr + 1 site total = matches expected); simulated a second snapshot 28d prior and confirmed before/after is now answerable from stored data (site clicks 775→1551; per-page 13→27); confirmed per-day idempotency and that pruning removes >180d snapshots. Then ran the real collector end-to-end (`--days 90`): main snapshot rewrote to 16124 rows as before, history wrote 3339 rows for 2026-09-15. Confirmed main collection still carries all expected dims and both readers (`seo_signal.py`, `seo_pilot_status.py`) exit 0 afterward.
**Recurrence:** First
