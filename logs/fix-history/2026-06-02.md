# Fix History — 2026-06-02 (AEST)

## [PROCESS-TAB-SEASONALITY-MERGE] Merge duplicated Seasonality + Timing sections — 12:42 AEST
**Symptom:** On the new Process tab, the seasonality calendar strip rendered its own full section ("When does the southern Gold Coast sell for the most?") immediately followed by the timing segment as a second section ("Timing — When the southern Gold Coast has sold for the most") — the two duplicated the same content. Will asked for one merged "Seasonality and timing" section.
**Root cause:** `ProcessTab` rendered `<SeasonalityStrip>` (self-contained section) *and* the seasonality `ProcessPlanSegment` as two separate sections.
**Fix:** Folded the strip into the segment. `SeasonalityStrip` gains an `embedded` prop that returns only the calendar card (no section/heading/lede/citation); `ProcessPlanSection` gains a `leadSlot` rendered full-width above its two columns; `ProcessTab` passes the embedded strip as the seasonality segment's `leadSlot` instead of a separate block. Retitled the segment eyebrow → "Seasonality & timing", headline → "When does the southern Gold Coast sell for the most?", merged the strip's quantified lede (Nov +3.3% / Jan −1.4% / ~4.7pp spread, 18,978 sales) into the thesis, added "Before You List, Chapter 2" to the citation. One section now: heading → strip → (the data | the 3 timing questions) → one citation strip.
**Verification:** tsc clean; build clean; headless Chrome confirmed exactly one seasonality section (was two), 8 sections total, correct merged layout.
**Files:** website `Will954633/Website_Version_Feb_2026` commit `d6d3907` (4 files): `src/pages/YourHomePage/{components/SeasonalityStrip.tsx, components/ProcessPlanSection.tsx, tabs/ProcessTab.tsx, data/homeFixture.ts}`.
**Recurrence:** Follow-up to [PROCESS-TAB-V2] (same session).

## [YOURHOME-PHOTO-SCHEMA] Photo analysis never appeared on house mini-site — 12:30 AEST
**Symptom:** Entering 25 Huntingdale Crescent, Robina in "Analyse Your Home" produced a `/your-home/` report with NO photo-analysis data on the Your Home tab and none in the "Complete data record" drawer — the feature shipped 2026-06-01 ([YOURHOME-PHOTO-ANALYSIS]) showed nothing.
**Root cause:** Three compounding faults. (1) **Stale process** — `fields-property-report-poller` started 2026-06-01 11:21, *before* the photo-analysis fix landed (18:35), so reports it resolved (incl. this one at 02:14) ran pre-fix code with no `photo_analysis` emit at all. (2) **Schema mismatch** — the 06-01 fix was verified against mock data whose shape was invented, not real. `_photo_analysis_from()` and the frontend looked for buckets `overall/interior/structural/layout`, but the real `property_valuation_data` written by the photo step uses `property_overview/condition_summary/exterior/kitchen/bathrooms/bedrooms/living_areas/outdoor/renovation/property_metadata`. Only 3 buckets overlapped, metadata was read from `metadata` (real key `property_metadata` → all-null footnote), and standout/negative features were read from `overall.*` (real location `property_metadata.*`). So even with fresh code the result was a thin, near-empty card. (3) `scripts/property_reports/` is untracked in local git (pushed only via `gh api`).
**Fix:** Rewrote `_photo_analysis_from()` to read the six rich dict buckets + pull `standout`/`noted`/metadata (incl. prestige_tier, presentation/appeal scores, 16 images, professional) from `property_metadata`; the verbose per-room list buckets (`bathrooms/bedrooms/living_areas`) are skipped (their numbers roll up into `condition_summary`). Frontend: realigned `PHOTO_BUCKET_ORDER` + `PHOTO_FIELD_LABELS` to the real field names, read standout/noted from backend-provided arrays, added "Noted in photos" rows to the data-record drawer, skip noisy `visible` field. Restarted the poller (now loads fixed code for future entries), force-re-resolved the Huntingdale report — `property.photo_analysis` now carries all 6 buckets + 2 standout + 1 noted + full metadata. tsc clean.
**Files:** `scripts/property_reports/slot_resolver.py` (orchestrator `0a48a54`); website `src/pages/YourHomePage/{tabs/visionLabels.ts, tabs/YourHomeTab.tsx, components/DataRecordDrawer.tsx, data/homeFixture.ts}` (one commit `09613cab`).
**Recurrence:** Second photo-analysis surfacing attempt (first: 2026-06-01 [YOURHOME-PHOTO-ANALYSIS], which never took effect). Pattern: a feature "verified with mock data" whose mock shape was never checked against the live DB schema — verify resolvers against a real document, and restart the daemon that runs them.

## [PROCESS-TAB-V2] Interactive "build your plan" Process tab on house mini-site — 12:18 AEST
**Symptom:** Not a bug — a build. The house mini-site (`/your-home/:slug`) Process tab held only the seasonality strip. Will wanted the new interactive format: 8 selling-decision segments (prep ROI, timing, first two weeks, marketing, auction vs treaty, listing price, photography, staging), each showing the evidence and asking the seller to choose, with answers that persist and pre-load on return visits.
**Root cause:** N/A (greenfield feature). Spec authored at `09_Appraisals/Selling_Process.md/The-Process-Tab-Developer-Reference.md` over prior sessions (content built out, every numeric claim graded [A]/[B]/[C]/[D] against sources, storage + persistence decided).
**Fix:** Built the full feature.
- **Data:** added `references.ts` Source-Layer keys (Johnson/Tidwell/Villupuram 2020, Des Rosiers 2002, Virginia Tech, Zillow paint, VHT, Soleymanian & Qian 2024, Khezr 2015, Taylor 1999, Bucchianeri & Minson 2013, PropTrack 2024, NAR 2023 staging) with correct `hosted` flags; added `ProcessPlanSegment`/`PlanQuestionDef`/`SellingPlan` types + static `PROCESS_PLAN_SEGMENTS` (8 segments, corrected/tiered figures, `cite()` per claim) to `homeFixture.ts`.
- **Components:** `PlanQuestion.tsx`+`.module.css` (stateful single/multi/month/date/text question; debounced save; pre-fill; "Saved" indicator; device-token gated, degrades to local-only when no token), `ProcessPlanSection.tsx` (FearSection visual idiom + book chart + book-photo pair/single + interactive questions).
- **Tab:** rewrote `ProcessTab.tsx` — renders the segments, holds live answers map so cross-segment `visibleIf` reacts immediately (listing-price hides when Auction chosen); seasonality strip leads its timing segment.
- **Persistence:** answers stored on `system_monitor.property_reports.selling_plan` (NOT the pipeline-owned cadastral doc). New `property-plan-submit.mjs` (POST, device-token ownership check, targeted `$set` on `selling_plan.answers.<questionId>`, question-id whitelist, input caps, fresh client + close). `property-report.mjs` projection extended with `selling_plan`; `YourHomePage.tsx` `PropertyReportDoc.selling_plan` type + overlay into fixture.
- **Charts/photos:** reused existing book charts (ch8-1, ch7-1, ch3-2, ch4-1) and existing book photos (front-garden before/after, interior good/bad, open-home) — no new assets needed. ch5-4/ch7-4 deliberately NOT reused (carry pre-correction figures / unsourced 3.6× claim).
**Verification:** `tsc --noEmit` clean; `npm run build` clean (8.13s); headless Chrome against local dev server confirmed all 8 segments + 10 questions render with correct input types, both photo pairs + seasonality strip load, citation strips render, and `visibleIf` removes listing-price when Auction is selected. (Live `#process` screenshot not used — site-inspector can't reach non-default mini-site tabs; headless local click is the reliable check.)
**Files:** website repo `Will954633/Website_Version_Feb_2026` commit `9e6900e` (9 files): `src/pages/YourHomePage/{data/references.ts, data/homeFixture.ts, tabs/ProcessTab.tsx, YourHomePage.tsx, components/PlanQuestion.tsx, components/PlanQuestion.module.css, components/ProcessPlanSection.tsx}`, `netlify/functions/{property-report.mjs, property-plan-submit.mjs}`.
**Recurrence:** First (new feature).

## [SEASONALITY-CLEANUP] Consolidate 08_Seller-Book to one source of truth — 10:10 AEST
**Symptom:** `08_Seller-Book/Market_Data/` held three conflicting/misleading seasonality datasets alongside the new verified one — confusing and risky to reuse.
**Fix:** Removed superseded/broken files (Mar/Apr/May-baseline `SEASONAL_PRICE_ANALYSIS_FINDINGS.md` + `analyze_seasonal_price_impact.py` + `seasonal_matched_*.csv`; the broken `h3_*.csv` pipeline with nonsensical `pct_vs_annual_avg` and out-of-catchment suburbs). Renamed `seasonality_v2/` → `seasonality/`, pruned all non-canonical window/per-suburb diagnostic CSVs, gave the canonical three clean names (`seasonality_sales_dataset.csv` / `_monthly_summary.csv` / `_strata_detail.csv`). Updated `scripts/seasonality_analysis.py` so a **no-arg run = the canonical 2010–2025-excl-COVID run** and writes those exact filenames (overwrites in place). Added `Market_Data/README.md` (what/why/where + reproduce steps + what-was-removed table) and fixed stale refs in `seasonality/RUN_SUMMARY.md`. Kept `ACADEMIC_REFERENCES.md` (legit supporting lit) + `DATA_SOURCES_NOTE.md` (non-seasonality Article 3). Script pushed to orchestrator `19cf3de`; `Market_Data/` is VM-only data (not tracked on GitHub — regenerable via the script).
**Files:** `08_Seller-Book/Market_Data/` (README new, 9 files removed, folder + 3 CSVs renamed), `scripts/seasonality_analysis.py`.
**Recurrence:** Final step of the seasonality reconciliation series.

## [SEASONALITY-ALL-REPORTS] Strip on every mini-site report + FAQ split — 09:55 AEST
**Symptom:** The seasonality strip only rendered on the Merrimac demo fixture; real `/your-home/:slug` reports showed a PendingPlaceholder (gated on `slotStatus.seasonality === "approved"`, which stubFromDoc defaults to "pending").
**Root cause:** The strip was originally slot-gated awaiting a per-suburb resolver. But the verified figures are CATCHMENT-level (southern Gold Coast, 18,978 sales) — universal, identical for every report — so the gate was unnecessary.
**Fix:** Ungated the strip in `ProcessTab.tsx` — renders whenever `fixture.seasonality` exists (always, since stubFromDoc clones it from the fixture). Also shipped the coupled refactor: seller-concern Q&A moved from The Process tab to a new **FAQ tab** (`FaqTab.tsx`), so pushing ProcessTab required pushing FaqTab + YourHomePage together. Build verified; both tabs render.
**Files:** `src/pages/YourHomePage/tabs/ProcessTab.tsx`, `tabs/FaqTab.tsx` (new), `YourHomePage.tsx` — commit `0715f41`.
**Recurrence:** Part of the seasonality reconciliation series (below).

## [SEASONALITY-RECONCILE] Reconcile all surfaces to the SoT — 00:10 AEST
**Symptom:** Three conflicting seasonal figure sets (overstated article, fabricated book chart, placeholder mini-site strip).
**Fix:** Locked canonical figures (2010–2025 excl COVID 2019–2020, 18,978 sales, peak Nov +3.29%). Reconciled: (1) mini-site strip re-seeded + reframed catchment-level — pushed website `f1e581e`; (2) book chart `ch2-1` rebuilt from fake 3-suburb heatmap to a real catchment bar chart, regenerated PNG — pushed website; (3) **article `december-listing-paradox` corrected and PUBLISHED** — rebuilt HTML with canonical figures, single new catchment chart embedded (base64), advisory language removed for editorial compliance, title now "What 18,978 Gold Coast Sales Reveal". Updated `system_monitor.content_articles` directly (backup at `_db_backup_2026-06-02.json`), triggered Netlify build hook. Will approved going live ("a"). Scripts pushed to orchestrator `31fd09d`.
**GOTCHA (build env):** the Netlify build env lacks `COSMOS_CONNECTION_STRING`, so `scripts/fetch-articles.js` silently falls back to the committed `public/data/articles.json` — a DB-only article edit + build-hook does NOT update the static articles.json the client hydrates from (SSR page showed new text, hydrated H1 reverted to old). Fix: regenerate articles.json locally on the VM (`COSMOS_CONNECTION_STRING` from orchestrator .env — strip surrounding quotes; must start `mongodb://`) and commit `public/data/articles.json` to the website repo (commit `46d5d9f`). Verified live: hydrated H1 = "What 18,978 Gold Coast Sales Reveal", chart + table correct.
**Files:** website `homeFixture.ts`, `SeasonalityStrip.tsx`, `public/book-images/charts/ch2-1-monthly-heatmap.png`; orchestrator `scripts/seasonality_analysis.py`, `scripts/generate_book_charts.py`; draft `fields-automation/generated/december_listing_paradox/article_corrected_2026-06-02.md`.
**Recurrence:** Part of the 2nd seasonality correction (see SEASONALITY-SOT below).

## [SEASONALITY-SOT] Verified, reproducible seasonality source of truth — 23:40 AEST
**Symptom:** No auditable source of truth for the seasonal-timing figures used in the article (`december-listing-paradox`), the book chart (`ch2-1`), and the new mini-site strip. Will needs figures we can publish and stand behind.
**Root cause:** The corrected article's analysis script was lost (lived in `/tmp`). The only committed seasonal script used the discredited Mar/Apr/May baseline. The article's 13,585-sale sample was not reproducible from the live DB (clean `listing_status:sold` = only 2,710; single-path timeline probe = ~826 for 2020–2025).
**Fix:** Built `scripts/seasonality_analysis.py` — extracts per-property sale-history events from all three timeline paths (deduped), runs matched-cohort analysis with annual-average baseline + ±30% winsorization, and emits a publishable row-level CSV + monthly summary + strata detail. Verdict: the article's **narrative reproduces and is robust** (H2 > H1, Sep–Dec strongest, Jan–Apr weakest), but **magnitudes are overstated** (Dec +6.05% article vs +3.0–3.8% recomputed; May +2.29% vs ≈−0.5%) and the 13,585 sample is not reproducible (we get 8,620 for 2020–2025 / 21,507 for 2010–2025). Recommend adopting recomputed figures (2010–2025 window) as SoT.
**Files:** `scripts/seasonality_analysis.py` (new); outputs under `08_Seller-Book/Market_Data/seasonality_v2/` (dataset CSVs + `RUN_SUMMARY.md`). Decision pending before push + reconciling article/book/strip.
**Recurrence:** 2nd seasonality-data correction (first: 2026-03-23 baseline fix). Pattern: seasonal figures generated ad-hoc without a committed script — now fixed with a reproducible pipeline.

## [MINISITE-SEASONALITY] Per-suburb seasonality strip on Process tab — 22:40 AEST
**Symptom:** Opportunity-Report-v2 §1.5 ("Seasonality for Your Specific Suburb" mini-chart) was unbuilt. The house mini-site (`/your-home/:slug`) had no month-by-month seasonal view; the seasonal pattern only existed as a static book chart (`ch2-1-monthly-heatmap.png`).
**Root cause:** Feature not yet implemented — it was item 🟡7 (Phase 2) in the v2 build sequence.
**Fix:** Built a `SeasonalityStrip` component — a 12-month calendar strip (Jan→Dec) shaded by seasonal position (copper above annual average, teal below), peak month bordered, each month clickable to reveal its sold count + premium in a detail card. Placed it at the **top of The Process tab** (Will's call — timing is a process question, not a market-state one), not the Market tab as §1.5 originally suggested. Added `SeasonalityData`/`SeasonalityMonth` types + a `seasonality` slot to `SlotStatus`; the Merrimac reference fixture carries the published Robina-catchment (4226) pattern. Gated on the slot so real reports show a `PendingPlaceholder` until a per-suburb resolver computes the pattern — no reference-pattern leak. Current-month "now" dot added in a post-mount effect to avoid SSR hydration drift. Citations are clickable (Ngai & Tenreyro 2014; Miller/Sklarz/Real 2014) via the Source Layer registry. Framed strictly as a recurring pattern, not a forecast (editorial: no predictions/advice).
**Caveat / follow-up:** The monthly premium figures are the *illustrative* pattern already published in *Before You List* Ch.2 (the book chart comment marks them "Plausible seasonal data"), not yet computed from the live sold cohort. Safe because only the all-approved demo fixture renders them (status banner already says "example template"); real reports stay on the placeholder. Follow-up: wire a `seasonality` resolver that computes per-suburb monthly premiums + counts from the sold cohort (sourced via market-metrics, detrended).
**Files:** `src/pages/YourHomePage/components/SeasonalityStrip.tsx` (new), `.../components/SeasonalityStrip.module.css` (new), `.../data/homeFixture.ts`, `.../tabs/ProcessTab.tsx`, `.../YourHomePage.tsx` — repo `Will954633/Website_Version_Feb_2026`, commit `03ff161`.
**Recurrence:** First.
## [TRACK-502] Appraisal HTML reports 502 in ops view — 09:35 AEST
**Symptom:** `https://vm.fieldsestate.com.au/track/view/<id>` returned `502 Bad Gateway (nginx)`. All `/track/*` endpoints (viewer, pixel, status, logo, page) were down — appraisal HTML reports would not render.
**Root cause:** nginx had no `location /track/` block, so `/track/...` fell through to `location /` and was proxied to the ttyd terminal (`https://127.0.0.1:7681`), which failed the upstream SSL handshake → 502. The tracking server itself (Flask, `127.0.0.1:3051`) was healthy the whole time. The `/track/` proxy block was lost when `sites-enabled/fields-vm` was rewritten on 2026-05-26 (audio-library edit). Note: `sites-enabled/fields-vm` is a standalone regular file, NOT a symlink to `sites-available/` — editing sites-available has no effect.
**Fix:** Added `location /track/ { proxy_pass http://127.0.0.1:3051/; ... }` to `/etc/nginx/sites-enabled/fields-vm` (trailing slash strips the `/track` prefix → server.py routes `/view`, `/page`, `/status`, etc.). Reloaded nginx. Also moved a stray `fields-vm.bak.*` out of `sites-enabled/` (it was being loaded via `include sites-enabled/*` and caused a conflicting-server-name warning) into `/etc/nginx/backups/`. Verified: `/track/view/<id>` → 200 (renders "27 Huntingdale Crescent, Robina"), `/track/page/<id>/1` → 200 image/png, `/track/health` → 200.
**Files:** /etc/nginx/sites-enabled/fields-vm
**Recurrence:** First

## [MINISITE-FAQ-TAB] Split seller Q&A into dedicated FAQ tab — 09:34 AEST
**Symptom:** Not a bug — UX restructure. The House Mini-Site (`/your-home/:slug`) "The Process" tab was holding FAQ-style seller concerns (agent selection, settlement, sell-first/buy-first, cost of sale) rather than process/timing content.
**Root cause:** Sections were originally built as FearSection blocks under ProcessTab; the only genuine "process/timing" content (the §1.5 seasonality strip) shared the tab with the question-shaped sections.
**Fix:** Created `FaqTab.tsx` holding the `processSections` FearSection blocks + Research Library bibliography + editorial footer. Slimmed `ProcessTab.tsx` to the seasonality strip only (ready to grow further timing sections). Added a new "FAQ" tab to `YourHomePage.tsx` (TabId + TABS + render, placed before "What Happens Next"). Changed FearSection eyebrow "The process" → "Common question". Per Will's call, left the seasonality slot gate as-is (real reports still show the PendingPlaceholder; catchment data shows only on the Merrimac template until a per-suburb resolver exists). Verified: tsc --noEmit clean; live tab bar shows all 8 tabs incl. The Process + FAQ; page 200, 0 network errors; FAQ/seasonality content correctly isolated (0 leak onto home tab).
**Files:** Feilds_Website/01_Website/src/pages/YourHomePage/{YourHomePage.tsx, tabs/FaqTab.tsx, tabs/ProcessTab.tsx, components/FearSection.tsx}; scripts/push_faq_tab.py (new push helper). Website commit 5856a576 (Will954633/Website_Version_Feb_2026).
**Recurrence:** First

## [MINISITE-TAB-SCROLL] Mini-site tabs load at hero top instead of content — 10:06 AEST
**Symptom:** On the house mini-site (YourHomePage report), clicking any tab opened the page scrolled to the very top of the hero photo, forcing the user to scroll down to reach the tab bar and the content they navigated to.
**Root cause:** `switchTab` scrolled to the sticky tab bar on every tab change, but the hero remained the first thing in view on (re)render/hash navigation, and there was no per-tab distinction — every tab behaved the same and the seller lost context above the fold.
**Fix:** Reworked scroll logic so only the "Your Home" tab loads from the very top with the hero in view. Every other tab now scrolls to the tab section anchor (the status banner — the orange "under review" bar shown in the screenshot) pinned to the top of the viewport, with the hero scrolled off above. Added a `tabSectionRef` on the status banner, a `scrollToTabSection()` helper, home→`scrollTo(0)` vs other→anchor branch in `switchTab`, and a one-shot deep-link effect that drops to the tab section after async DB load when the URL targets a non-home tab.
**Files:** Feilds_Website/01_Website/src/pages/YourHomePage/YourHomePage.tsx (GitHub: src/pages/YourHomePage/YourHomePage.tsx @ 848dfed)
**Recurrence:** First

## [MINISITE-TAB-SCROLL-2] Tab scroll still landing on hero — ScrollRestoration override — 10:18 AEST
**Symptom:** After the first fix, every tab still loaded at the very top of the hero photo.
**Root cause:** root.tsx renders react-router's `<ScrollRestoration />`. `switchTab` changed the tab via `navigate('#id')`, which is a new router navigation. ScrollRestoration runs on each navigation and scrolls the window to top (no DOM element matches the hash) — firing AFTER switchTab's `scrollTo`, so it always won.
**Fix:** Stopped using router `navigate()` for tab changes. The hash is now updated with `window.history.replaceState(...)`, which keeps the URL shareable/deep-linkable but does NOT trigger a React Router navigation, so ScrollRestoration never fires and switchTab's scroll stays authoritative. Removed the now-unused `useNavigate` import/var.
**Files:** Feilds_Website/01_Website/src/pages/YourHomePage/YourHomePage.tsx (GitHub: src/pages/YourHomePage/YourHomePage.tsx @ 389bb1e)
**Recurrence:** 2nd (same-day follow-up to [MINISITE-TAB-SCROLL])

## [MINISITE-TAB-SCROLL-3] Tab scroll animated past hero (perceived as "loads on hero") — 10:34 AEST
**Symptom:** User still reported tabs "loading at the very top of the hero" after the ScrollRestoration fix. Headless test proved the END scroll position was correct (banner pinned to viewport top on both mobile + desktop), so the code worked — but `behavior: "smooth"` animated the scroll DOWN from the hero each click, which reads as "it opened on the hero and I had to wait/scroll".
**Root cause:** Smooth scroll animation. The user wants each tab to APPEAR already positioned at its content, not animate there from the top.
**Fix:** Changed switchTab scroll from `behavior: "smooth"` to `"auto"` (instant) for both home (top) and non-home (tab section). Verified end position via puppeteer against live site before this change (scrollY ~812-834, bannerTop=0).
**Files:** Feilds_Website/01_Website/src/pages/YourHomePage/YourHomePage.tsx (GitHub @ 1847133)
**Recurrence:** 3rd (same-day chain: MINISITE-TAB-SCROLL → -2 → -3)

## [GDRIVE-AUTH] Google Drive refresh token revoked again — 13:40 AEST
**Symptom:** Could not write to Google Drive (upload/create). Local OAuth `creds.refresh()` returned `invalid_grant: Token has been expired or revoked`; access token in `.gdrive-server-credentials.json` had expired 2026-05-28. The claude.ai-connected Drive connector is read-only ("insufficient authentication scopes"), so it was no fallback.
**Root cause:** The Google OAuth client is in "Testing" publishing status, so Google expires its refresh tokens ~every 7 days. This recurs (see GDRIVE history above + 2026-05 sitemap entry).
**Fix:** Ran the re-auth flow (`scripts/gdrive-reauth.py` logic) — generated consent URL, Will approved as will.simpson@blueoceans.com.au, exchanged the code for a fresh refresh token, rewrote `/home/fields/.gdrive-server-credentials.json` (chmod 600). Then uploaded the induction `.docx` via the Drive API with target `mimeType=application/vnd.google-apps.document` to create a native Google Doc ("House Mini-Site") with 56 inline images preserved.
**Permanent fix (pending):** Move the OAuth app to "Production"/published status so refresh tokens stop expiring weekly.
**Files:** 11_House_Mini_Site/upload_to_drive.py (new), 11_House_Mini_Site/capture_tabs.js (new), 11_House_Mini_Site/build_minisite_doc.py (new), /home/fields/.gdrive-server-credentials.json (token rewrite, not in git)
**Recurrence:** Nth (recurring — GDrive OAuth grant has expired multiple times)

## [CANONICAL-ATTRIBUTE-LAYER] Sample-based scarcity & positioning evidence base — 14:05 AEST
**Symptom:** Client-facing scarcity statements ("exceeds every house sold in the suburb this year", "only K of M sold") were neither defensible nor reproducible. The scarcity engine (`scarcity_features.py`) matched only on `valuation_data.subject_property.features.basic.*` — a field present for ~16% of core sold properties — so "1 of N" denominators were computed over an incomplete cohort. The same fact lived in 4+ fields (floor area), none authoritative, with no provenance recording where each value came from.
**Root cause:** Attribute fragmentation across the property doc + single-field matching + census framing of statements when we only ever had a Domain-scraped sample, not a census of all sales.
**Fix:** Built a standardised, provenance-tracked golden-record layer and reframed all rarity statements as sample-relative + disclosed:
- `config/canonical_attributes.yaml` — versioned data dictionary: per-attribute definition, unit, ordered source_priority paths, method→confidence map, scarcity thresholds (ported verbatim from FEATURE_RULES).
- `scripts/build_positioning_sample.py` — deterministic, reproducible representative sample (~67/core suburb = 201) over trailing 12mo, written to `system_monitor.sample_manifest` (seed + query + window + member ids); excludes vacant land; prints representativeness report (sample vs population).
- `scripts/property_reports/canonical_resolver.py` — walks source_priority per attribute, records provenance (source_field/method/model/confidence/date), applies floor-area/land sanity flip, computes scarcity hits, content-hash; writes `Gold_Coast.property_attributes`. Consolidation lifted pool coverage 16%→74%, floor area→48% on the sample with ZERO new API calls.
- `scripts/backfill_sample_extraction.py` — runs existing per-id `on_demand_valuation.py` (photo + floor-plan + georef + valuation) only where source images exist (40 recoverable targets, ~$10), then refreshes golden records. Media-less sold records flagged honestly, not silently extracted.
- `scripts/property_reports/sample_context.py` — sample-relative context: subject percentile vs typical sampled home + % of sample sharing the standout stack, with mandatory {sample_size, window, source} disclosure and small-n suppression; suburb→catchment fallback for non-core subjects.
- `scripts/property_reports/verify_claim.py` — reproduction harness: `--address` (evidence chain w/ provenance), `--scarcity` (sample + percentile + member addresses behind a %), `--reproduce` (re-resolve + content-hash diff). Verified byte-identical reproduction on 39 Manakin Ave.
- Rewired `compute_scarcity_stats()` in `generate_appraisal_report.py` to the sample engine; updated the editorial prompt + `positioning_narrative.py` rules to forbid census claims ("only one", "1 of X", "exceeds every") and require sample disclosure.
- Backup/reproducibility: existing `mongodump --uri --out` (no filters) already captures `property_attributes` + `sample_manifest` in the 02:00 GCS backup — no change needed.
**Files:** config/canonical_attributes.yaml (new), scripts/build_positioning_sample.py (new), scripts/property_reports/canonical_resolver.py (new), scripts/property_reports/sample_context.py (new), scripts/property_reports/verify_claim.py (new), scripts/backfill_sample_extraction.py (new), scripts/generate_appraisal_report.py (compute_scarcity_stats + editorial prompt), scripts/property_reports/positioning_narrative.py (scarcity rules)
**Recurrence:** First
