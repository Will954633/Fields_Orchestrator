## [VIDEO-ARCHIVE-STAGE-05] Video projects get an off-root archive stage; 5.8G backlog cleared — 07:40 AEST
**Symptom:** 5.4G of finished walkthrough video sat in 10_Market_Report/Video_Production on the 97G root disk (part-cause of yesterday's [MONGODB-BACKUP-DISK-FULL-SILENT]); volume video production is planned, with no defined end-of-life for a shipped project's footage.
**Root cause:** The editing pipeline had stages 00-04 (probe→transcribe→EDL→build→QC) but no archive stage — nothing said where finished projects go, so they accumulated on root.
**Fix:** Added stage 05 (`scripts/05_archive.py`): verified copy to /data/blobs/video_archive/ (738G disk, off-site nightly via the 03:00 gsutil rsync to gs://fields-blob-backup), byte-for-byte rsync--checksum verify REQUIRED before --purge deletes local, plus optional --drive leg to Will's Video_Archive Drive folder (1cIrw53ggePRR0eVZ0Jf8nq43NsHdAJ8D) with per-file size verification. Drive leg is best-effort by design: the shared OAuth token dies weekly (Testing mode) and must not gate the durable copy. --work mode archives the shared work/+qa/+out/ scratch under the current config slug (out/ included because social circle .movs ship nowhere else). Documented as mandatory Step 7 in RUNBOOK.md + README stage table. Ran it: 4 project bundles + Robina scratch (223 files, 5.8G) archived, verified, purged. Root 78%→72% (28G free). Drive push deferred — token currently dead (invalid_grant), flagged to Will; catch up with --drive-only after re-auth.
**Files:** 10_Market_Report/Video_Production/Video_Editing_Process/{scripts/05_archive.py,README.md,RUNBOOK.md}
**Recurrence:** First (sibling of the wrong-disk leak family — this one adds the missing lifecycle stage rather than another one-off move).

## [WALK-ARTICLE-MEDIAN-V2] Median-article walkthrough: films + caption clearance + animated median-row — 08:08 AEST
**Symptom:** Will's review of the median article walkthrough (built 2026-09-10 [WALK-ARTICLE-MEDIAN]): (1) no chapter/film structure like the suburb v2s; (2) captions rendered BEHIND the transport bar (stage caption top-anchored at innerHeight-72 grows down into the fixed bar at bottom:18); (3) wanted a red asterisk animated between 1.48/1.50 while he says "the middle is between two numbers", then those two numbers blinking 3× on "add them and divide by two"; (4) wanted the $12m drop VISUALISED: red X over $12m → it disappears → asterisk slides onto 1.48 (the new middle), holds, fades → red circle around 1.48 → blue "counting" arcs hopping the three numbers either side (1.20→1.34→1.40 and 1.55→1.55→1.50).
**Root cause:** n/a (feature) — except the caption bug: every stage placement anchored the caption by TOP near the viewport bottom, so multi-line chips grew down under the transport; and (found while verifying) `wkStageBox` mobile placed the whiteboard "under the head", which in ASIDE mode (head parked bottom-right) put the card off-screen behind the transport — the article walkthrough had never been mobile-verified.
**Fix:** engine — (1) 7 films on the article registry entry (`Why the median gets misread` @0 card:22.5, calc @25.5, traps @142/339.5/448/720, `How to read a median` @891); `wkFilms`/`wkChapters`/`wkBuildFilmPips`/`ctaFrom` taught the article path, so pips, title cards, ⏮⏭ and transport label all work. (2) stage + aside captions now BOTTOM-anchored (86px desktop / 78px mobile, above the transport); aside caption band stops short of the aside head; `wkHeadAtChart` resets `bottom:auto`; `wkBoxClamp` only treats the caption as a ceiling when it's actually above the box (its old assumption pinned the box off-screen once captions moved to the bottom). (3) `wkMedianRow()` renders the 8 prices as per-token `<span>`s inside a flex box-line; the red asterisk lives INSIDE the row (offset-positioned) so box growth/re-centring can't strand it; helpers `wkRowStar/StarTo/StarOff/Blink/Cross/Drop/Circle/Arcs` + beats at t=36.4/40/63.5/65.5/66.8/70/70.8/74.8/77.2 timed to the WALK_SEGMENTS_MEDIAN transcript. (4) mobile `wkStageBox` stages the card at top:96 when the head is parked low. CSS: `#walkLayer`-prefixed rules (class-only selectors LOSE to the existing `#walkLayer .wk-boxline` ID rules — first attempt silently didn't apply on mobile) for `.wk-numrow` (nowrap, mobile font shrink so all 8 tokens fit one line at 375px), `.wk-star` pop/slide/fade, `wkTokBlink` ×3.
**Verified:** headless dev (1440×900 + 375×812): 12 desktop checkpoints — 7 pips, film cards at 23/27/143.5, zero caption/transport overlaps at every checkpoint, star between→on 1.48→gone, $12m crossed then opacity 0, circle + both arc sets drawn; mobile row single-line with same sequence. Suburb regression (news/robina v2 fixture): 6 pips, stage/docked/close-recap captions clear, close-recap notebox on-screen, no JS errors. Production build clean; pushed 21592bd5 (one commit via push_website_files.py); live re-verified post-deploy.
**Files:** 01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts, 01_Website/src/components/MarketFlowProto/MarketFlowProto.css
**Recurrence:** First

## [WALK-ARTICLE-MEDIAN-AVG] Average section visualised like the median one — ✅ DEPLOYED 4a9a709c — 08:45 AEST
**Symptom:** Will's direction (same review round): in the "same numbers as an AVERAGE" section (t≈87-128), re-write the full row INCLUDING the $12m; circle the $12m as he names it; show "with $12m in → $2.75m" with "(median was $1.49m)" bracketed BESIDE it; cross out the $12m as he pulls it; show "drop the $12m → $1.43m (median was $1.48m)" directly below.
**Fix:** engine — average beats rebuilt (t:89 row, t:101 ring, t:104 answer+bracket, t:115 cross, t:118 answer2+bracket); new `wkRowRing()` and `wkRowCross()` made re-placeable via tag-clear so ring/X are re-drawn after each answer line re-centres the row; new `slinePair()` renders a smaller bracketed aside beside a box line. CSS: `.wk-bnote` (0.62em aside), `.wk-pairline` (nowrap + `clamp(15px,2vw,24px)` so the pair fits one row down to ~1024px; mobile wraps).
**Verified:** headless dev at 1440×900 and 1120×800 — ring on $12m, both answer lines with brackets beside, cross over the ringed $12m, re-placed marks stay aligned after each line append; clean clear at t:128.
**⚠ NOT DEPLOYED:** Will asked to hold and batch further walkthrough updates into ONE deploy (Netlify credit discipline). Local-only in `MarketFlowProto.engine.ts`/`.css`; batch-1 commit 21592bd5 (films/captions/median row) was already live before the hold and has been live-verified.
**Files:** 01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts, 01_Website/src/components/MarketFlowProto/MarketFlowProto.css
**Recurrence:** First

## [WALK-ARTICLE-MASKS-AND-POLISH] Article face masks + $12m ring flash + Trap 1 ink fixes + font-swap resync — ✅ DEPLOYED 4a9a709c — 09:20 AEST
**Symptom:** Will's continuing review round: (1) wanted the suburb-style circular face masks with play chip placed THROUGH the article (film 1 top-left of the H1, others at their sections); (2) wanted the $12m ringed in red at "included a $12m sale" and gone ~2s later; (3) Trap 1's red circle missed the chart data (drawn at fx 0.9, off in whitespace right of the "All attached dwellings 130" label); (4) Trap 1's "not prices rising — the MIX rising" note (fy 0.92) wrote across the docked talking head.
**Root cause (two real bugs found while building):** (a) injected masks vanished — the article BODY div hydrates/re-renders after the mount effect and wipes DOM injected into it (only the h1 mask, outside the body div, survived); (b) ink drawn in the same tick a note-box is created (seek catch-up) measures FALLBACK font metrics — Fields Hand only starts loading on first use, and the swap re-wraps the box, stranding the ink one line off (ring above the $12m).
**Fix:** engine — `wkMountArticleMasks()`: idempotent per-anchor injection (h1 + h2s in order map 1:1 onto the 7 films) re-run via MutationObserver on the article (survives body re-renders, can't self-loop); posters from film-start frames (`public/walkthrough/posters/median-house-prices_2026-09_f0..6.jpg`, ffmpeg); click delegation deep-starts `startWalk(films[k].t)`; masks hidden while `body.wk-active` (existing CSS). Median beats: ring flash t:49.4→fade t:51.6 (`wkRowRing` + `wkFadeTag`). Trap 1: new `wkCircleAtF()` (fraction-based radii that scale with the rendered image) circling the label at (0.70,0.147) measured off the PNG; MIX note moved to the top, replacing the now-tagged "how?" note (`t1how` fade). Font resync: `document.fonts` `loadingdone` listener + `fonts.ready` re-apply the current beat state instant, fixing all seek-into-section ink placement. CSS: standalone `.wfm-art` mask styling (the suburb rules are `.mktp`-scoped and never reached the article; first attempt rendered as giant unstyled squares), `.wfm-lead` 78px for the H1.
**Verified:** headless dev — 7 masks at h1+h2s (styled circles + gold play chip), click film-4 mask → walkthrough at t≈449 with masks hidden and correct film label; ring flash present at t=50.5, gone by 53.5, aligned to the $12m (223 vs 224 centre-y) even on immediate seek; Trap 1 circle wraps the label+line end, MIX note top with "how?" faded; full battery re-run (12 desktop + 3 mobile article checkpoints, avg sequence, Robina suburb regression) all clean, no JS errors; production build clean ×2.
**⚠ NOT DEPLOYED:** held with [WALK-ARTICLE-MEDIAN-AVG] for Will's batched single deploy. Local: engine, CSS, 7 poster jpgs.
**Files:** 01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts, .../MarketFlowProto.css, 01_Website/public/walkthrough/posters/median-house-prices_2026-09_f{0..6}.jpg (new)
**Recurrence:** First

## [WALK-ARTICLE-EX13] Trap 1 worked example: the 13 sales written out in full — ✅ DEPLOYED 4a9a709c — 10:05 AEST
**Symptom:** Will's direction (workshopped in markdown first): the "simplified example · 13 sales" section should WRITE OUT both data sets vertically, 2024 beside 2026, per-sale type tags, medians circled in each column, the larger homes that arrived in 2026 ringed as one group, and a blue arc drawn from the 2024 median's "2bd" tag to the 2026 median's "3bd" tag while he explains the mix change. Keep the old flow for revert.
**Fix:** engine — `ARTICLE_EX_V2` flag (false = old summary-card beats, kept verbatim inline); `wkExCard()` builds a fixed whiteboard grid (rank · 2024 val+tag · 2026 val+tag ×13 + two footer lines), BOTH columns laid out at creation with the 2026 column hidden per-cell — so revealing it never reflows and every circle/ring/arc drawn earlier stays aligned; `wkExReveal` staggers cells 95ms apart like handwriting; `wkExCircleMed`, `wkExRingTail` (rows 8–13 of 2026), `wkExMixArc` (wkArrow arc=0.5, blue, tag-top to tag-top); head kept, shrunk via new `wkStageAside(instant,scale)` 0.72. Beats: card 237 → circle '24 255 → reveal '26 264 → tail ring 273 → circle '26 286 → foot1 293 → mix arc 297 → foot2 308; Trap 2's scard clears it (`.wk-excard` added to clearWkInk). Data invented-but-consistent: 7th=$860k(2bd)/7th=$1,118k(3bd), +30.0% combined, 2bd +17.9%, 3bd +24.9%.
**Verified:** headless dev 1440×900 — all 8 beats screenshotted (columns, circles, ring, arc, footers), Trap 2 teardown clean; mobile 375×812 fits the whole table with head+captions clear; full battery (12 article + 6 avg checkpoints, masks, no JS errors) and production build clean.
**⚠ NOT DEPLOYED:** held with the rest of the batch.
**Files:** 01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts, .../MarketFlowProto.css
**Recurrence:** First

## [WALK-CHAT-CHANNELS] "Discuss with Will" chat chooser on the market-update walkthroughs — 09:01 AEST
**Symptom:** Feature (Will's request): the walkthrough offered Follow/Subscribe at the end but no way to open a conversation, and nothing on the page invited questions at each chapter.
**Root cause:** n/a — new capability.
**Fix:** (1) Engine: shared Messenger/WhatsApp/SMS chooser (`openChatMenu`, `#wkChatMenu`) with prefilled per-suburb message (`0416 529 481` / page `889412530933297`, same as ContactPage/OffMarketDeck); third gold "Chat with Will" pill in the walkthrough CTA band (pauses video, cancels the end-hold teardown while open); "Ask Will" chip beside every circular section film mask (topic = film title in the prefill), hidden while `body.wk-active`. PostHog: `walkthrough_chat_open` {source cta|section, topic}, `walkthrough_chat_channel` {channel, source}. (2) Caption clearance: bottom-anchored caption shared the band with the now-wider pill row — `walkTick` lifts it above `wkCtaBandTop()` while pills show, restores the staged base after (`cap.dataset.wkBase` set at each stage/aside placement). (3) `messenger-webhook.mjs`: `walkthrough_` ref branch (greeting reply, `messenger_leads` source `walkthrough_chat_ref`, Telegram ping) — an unknown ref previously got the OFF-MARKET "saved plan" auto-reply.
**Verified:** live headless desktop+mobile (390px): 5 chips beside masks, chooser hrefs correct (m.me ref `walkthrough_robina`, wa.me/sms prefills), Esc/outside close, CTA band 3 pills one row (mobile label collapses to "Chat"), video pauses on open, caption clears the band, zero console errors.
**Files:** 01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts, .../MarketFlowProto.css, 01_Website/netlify/functions/messenger-webhook.mjs. Commits dae1f7d1, 91247a7b.
**Recurrence:** First

## [WALK-HELD-BATCH-SHIPPED-EARLY] The two HELD article-walkthrough batches went live with the chat push — 09:05 AEST
**Symptom:** [WALK-ARTICLE-MEDIAN-AVG] + [WALK-ARTICLE-MASKS-AND-POLISH] were marked NOT DEPLOYED (Will's batched-single-deploy hold), local-only in engine.ts/.css + 7 poster jpgs.
**Root cause:** The chat-channels push (dae1f7d1) ships the LOCAL files wholesale — the held code rode along. Caught post-deploy from today's fix-history; the 7 posters had NOT been pushed, so `?walkthrough=1` article masks 404'd their images (plain article URL unaffected — masks are gated on the param).
**Fix:** Pushed the 7 posters (0b7c899a) to complete the batch; live-verified the gated article view (78px lead mask at H1, 62px masks at trap headings, posters rendering, launch pill intact). Net: everything previously held is now LIVE and verified — the two NOT-DEPLOYED notes above are superseded.
**Files:** 01_Website/public/walkthrough/posters/median-house-prices_2026-09_f{0..6}.jpg
**Recurrence:** First — rule for next time: before any wholesale file push, grep the last 2 fix-history files for "DEPLOY HELD" naming the same files.

**Batch-2 deploy note (10:35 AEST):** all three held entries above shipped as ONE commit 4a9a709c (engine + CSS + 7 poster jpgs) per Will's batching instruction; Netlify publish confirmed and re-verified LIVE headlessly: 7 face masks (h1+h2s), $12m ring flash at 0:50, median arcs + dropped $12m at 1:19, average ring/cross + both bracketed answer lines at 1:59, full 13-sale worked example (card, both median circles, tail ring, mix arc, footer) at 4:59, film label correct at 7:35, no JS errors.

## [ARCHIVE-DRIVE-SIZE-FIELD] Drive push verified -1 sizes; all 230 uploads were actually fine — 09:15 AEST
**Symptom:** First --drive-only push of the 5 archived video bundles reported "0 uploaded, 230 failed: Drive size mismatch -1 != N" after 30 min of transfer.
**Root cause:** Drive v3 finalise responses omit `size` unless the resumable-initiation URL asks via `fields=` — the verify read a missing key as -1 and condemned every successful upload. (Rule 7b in mirror image: the assertion itself was wrong, not the work.)
**Fix:** Initiation URL now passes `fields=id,name,size`; drive_upload made idempotent (same name+size in parent → skip; size mismatch → trash + re-upload) so re-pushes reconcile instead of duplicating. Re-ran all five bundles: 230/230 already present, correct sizes, 0 failed, 0 duplicated.
**Files:** 10_Market_Report/Video_Production/Video_Editing_Process/scripts/05_archive.py
**Recurrence:** First.

## [WALK-CHAT-COPY-HELP] Chat chooser subline — "I'm here to help." — 09:15 AEST
**Symptom:** Copy change (Will): append "I'm here to help." to the chooser subline.
**Root cause:** n/a.
**Fix:** One-line copy edit in `openChatMenu` (`wcm-sub`). Pushed fc0e3843, deploy polled ready, live-verified headless (subline text + screenshot).
**Files:** 01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts
**Recurrence:** First

## [WALK-ARTICLE-T3-CHART-DRIVE] Trap 3 chart driven live from the walkthrough + batch-3 polish — ✅ DEPLOYED 95949634 — 11:20 AEST
**Symptom:** Will's next batch: (1) worked-example tail ring must vanish before the $1,118k circle; (2) "different places"→"different cities"; (3) the Trap 3 scroll must begin right on the word "scroll" (was 3.4s late); (4) chart should settle PRESET to 12-month-only, then blink+asterisk the 12-month pill at "12 month median house prices", layer the 3-month series in (blink+asterisk) at "if I layer in the rolling 3 month median", switch the single-quarter pill on at "March quarter there"; (5) circle the $1.56M and $1.4M quarterly dots as each figure is spoken; (6) write "72 sales"/"55 sales" beside their dots as spoken, then "274 samples" (12-mo point) and "only 71 samples" (quarter dot) as spoken, clearing the earlier labels first; (7) caption transcription fix "robotic"→"erratic".
**How the cross-origin chart is driven:** the rolling-CI iframe lives on blobs.fieldsestate.com.au, so the engine can't touch its pills — added a postMessage remote ({wk:'series',on:{roll,qtr,m3}} partial-apply · {wk:'blink',key}) to BOTH live blob charts (`/data/blobs/article-charts/median_robina_rolling_ci.html`, `median_bw_asking_sqm.html`) and to both templates in `scripts/market_article_charts.py` so regens keep it; iframes cache-busted `?v=20260912` in `system_monitor.content_articles` (render-identical for readers — SHIPPED immediately, it's inert without the walkthrough). Pill/dot geometry measured off the generated SVG as viewBox fractions (T3PILLS/T3DOTS); asterisks are parent-side placeStar ink at pill top-right; beats re-assert the preset post-dock (t:468.2) to cover a late-loading iframe, t:0 and endWalk restore the reader default (all series on).
**Files:** 01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts (HELD), scripts/market_article_charts.py + both blob HTMLs + Mongo article ?v (LIVE, inert), logs.
**Verified:** headless dev — pills (roll,qtr,m3) read from INSIDE the iframe: (t,f,f) through the pill beats, m3 joins at 482.8, qtr at 512; stars 1→2; mar/jun rings at 517.2/521.3; "72/55 sales" beside dots then FADED with the summary at 532; "274 samples"/"only 71 samples" beside the 12-mo point and quarter dot at 544.8/548.8; close → pills restored (t,t,t); caption reads "erratic"; no JS errors; production build clean.
**⚠ Note for Will:** the handwritten "274/71 samples" follow the NARRATION; the article prose beside the chart now says "270 sales" (data re-captured since the video was recorded) — the two vintages are both on screen in that section.
**Recurrence:** First

## [WALK-ARTICLE-T4-CALLOUTS] Trap 4: timed "Asking price"/"Median price" arrow callouts — ✅ DEPLOYED 95949634 — 11:50 AEST
**Symptom:** Will's direction: as he says "is on that copper line" write "Asking price" with an arrow to the copper line (~Aug 2021), then "Median price" with an arrow to the sold line (~Aug 2022) as he names it.
**Fix:** engine — new generic `wkCalloutAt(sel,text,lfx,lfy,tfx,tfy,instant,tag)` (handwritten note + red arrow from its nearest edge to the target); targets measured off the BW chart SVG (copper path sampled via getPointAtLength at Aug-2021 → (0.7179,0.5487); nearest sold-median dot to Aug-2022 → (0.7623,0.6213)). Trap 4's untimed t:766 "copper = ASKING / green = SOLD" notes replaced by callouts at t:770.6 / t:778.3, same t4a/t4b tags so the existing t:799 fade still clears them.
**Verified:** headless dev — both callouts render with arrows on their lines at t=780.5, both gone by t=801 leaving only the "biggest gaps" note; screenshot checked.
**⚠ NOT DEPLOYED:** held with batch 3 (tail-ring fade, "different cities", scroll timing, T3 chart drive).
**Files:** 01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts
**Recurrence:** First

## [WALK-ARTICLE-T4-LINES-CUT-FLY-ASKWILL] Trap 4 verticals + "2026" audio cut + fly-over + Ask Will chips + seek hardening — ✅ DEPLOYED 95949634 — 13:40 AEST
**Symptom:** Will's continuing batch: (1) at "corrected or peaked" wipe all on-screen ink, then vertical dotted lines through the sold-median dots at 2021, Feb-2022 and Dec-2025; (2) he says "December 2026" but means Dec 2025 — cut the spoken "2026" from the video and captions; (3) at "come back down now recently" one ring around BOTH Aug-2026 line tips; (4) at "Take a look at that chart" return to the chart and do a zoomed left→right fly-over of the two lines, back out before "Trick number 4"; (5) "Ask Will" chip beside every article face mask, as on /news/robina.
**Fix:**
- **Video cut:** located "2026" via silence-detect + Gemini micro-slice transcription (`vlib.transcribe_audio`; word-level guesses verified by transcribing CANDIDATE CUTS, not by trusting slice timestamps — micro-slices hallucinate); final cut [812.10→813.15] (Δ1.05s) keeps "December when it peaked" — confirmed by transcribing the rendered file. New `median-house-prices_2026-09b.mp4` (918.35s, crf27, ~32MB) + 7 re-cut posters (new basename → masks pick them up automatically). Engine holds authored times on the ORIGINAL timeline; `MED_CUT`/`medT()` shift segs/beats/films/chapters at the registry so future authoring against the raw transcript still lines up. Caption text "December 2026"→"December".
- **T4 beats:** `wkWipeInk()` (fade-everything wipe that also RESETS the ink scroll-lock), `wkVLineAt()` (dashes as real subpaths so the write-on animation works) at the three measured dots; ring at (0.976,0.389) covering the asking end + last sold dot; fly-over via `wkT4Fly()` element-transform camera on the iframe (scale 2.2, diagonal translate pan 6.1s, hooks `walk._zoomEl` so `wkClearZoom` restores on any scene change; transient — skipped on seek replay).
- **Ask Will:** article masks get the suburb `wk-chat-ask` chip (standalone `.wca-art` CSS — suburb rules are `.mktp`-scoped); `openChatMenu` reused; `chatMsg`/`chatLinks` article-aware (msg names the article; m.me ref `walkthrough_median_article`, handled by messenger-webhook's `walkthrough_` branch).
- **Seek hardening (real bug):** jumping into a docked scene replays scroll+ink synchronously, so element-anchored ink measured PRE-scroll rects and landed off-screen (vlines invisible after scrub). `walkApplyBeats` now schedules ONE instant re-apply 750ms after any jump (mirrors the resize handler); wipe resets `inkFrozen`. Fixes all rect-anchored ink on article AND suburb walkthroughs.
**Verified:** headless dev — vlines at viewport y379 over the chart post-fix (were y5463); wipe leaves 0 ink; Aug-26 ring; fly-over frames left/mid/right show the lines filling the zoomed view, transform restored and Trick-4 card up after; cut file transcribes "in December when it peaked"; t105 beats unshifted, close card at 890.4, duration 918.35; 7 Ask Will chips, chooser opens with article message; full battery (12+6 checkpoints, T3 pills, suburb regression) green; production build clean.
**⚠ NOT DEPLOYED:** batch now = tail-ring fade · "different cities" · scroll-on-"scroll" · T3 chart drive · T4 callouts · T4 verticals+ring+fly-over · video cut (new mp4+posters) · Ask Will chips · seek hardening.
**Files:** engine.ts, MarketFlowProto.css, public/walkthrough/median-house-prices_2026-09b.mp4 (new), posters/median-house-prices_2026-09b_f{0..6}.jpg (new)
**Recurrence:** First

## [GC-EXPLORER-DATA] GC-wide leading-indicators dataset built; composite tail artifact caught — 13:13 AEST
**Symptom:** Not a bug initially — Will's go-ahead to build the "Which Signals Actually Lead Prices?" explorer data at Gold-Coast-wide level. During the build, the first-pass GC momentum series showed an 8.7% → 0.5% single-quarter collapse into 2026-Q1, initially reported to Will as "the coast-wide series is cooling much harder" — wrong.
**Root cause:** Composition artifact, two layers. (1) The per-suburb `rolling_12m_median_series` in `precomputed_indexed_prices` end at DIFFERENT quarters (3 suburbs at 2025-Q3, 31 at 2025-Q4, 16 at 2026-Q1, only the 3 targets reach 2026-Q2), so the composite's tail quarters were averages over a shrinking, cheaper subset — a mix change masquerading as a price fall. (2) The 3 target suburbs carry scattered interior missing quarters (robina: 9 within the 2017→ window), which got them excluded from the panel entirely by the coverage rule.
**Fix:** `scripts/build_gc_leading_indicators.py` (new): interpolates interior gaps ≤2 quarters (rolling medians are smooth), then drops any composite quarter carrying <95% of panel weight — tail truncates honestly at 2025-Q4 (+8.01%) instead of fabricating a cliff. Panel grew 50→61 suburbs. r recomputed at FIXED lags from the deep 2008–2026 pooled analysis (best-lag re-picking on 30 points produces spurious winners, e.g. clearance −0.91 @ +7q on n=18). Result: lending +4q r 0.81 (vs 0.76 pooled) — headline survives. `proj` dropped (pooled-validated only; engine guards on absence). Output `01_Website/public/data/leading_indicators_gc.json` staged locally; website push deferred to the GC page build. Guards: ≥40 suburbs, ≥28 momentum pts, lending r ≥0.5 else raise (Rule 7b spirit); wire into `run_monthly_market_precompute.sh` + job_run when the page ships.
**Files:** scripts/build_gc_leading_indicators.py (Orchestrator 90f42d4), 10_Market_Report/gc_wide_overview_data_scoping_2026-09-12.md (7f92ba1, c5217fe — addendum + artifact retraction).
**Upstream findings (not fixed here):** wide-suburb rolling series lag ~2 quarters behind the targets even after the 2026-09-01 refresh; target-suburb rolling series have interior missing quarters — both worth an upstream look in `precompute_union_prices.py` before the GC page ships.
**Recurrence:** First for this composite; kin to [EXPLORER median-level] composition findings 2026-09-11 (pooled-vs-suburb divergence) — composition effects in pooled series are now a repeat theme.

**Batch-3 deploy note (14:20 AEST):** shipped as ONE commit 95949634 (engine + CSS + median-house-prices_2026-09b.mp4 + 7 posters) and live-verified headlessly: new video serving (918.35s), 7 masks + 7 Ask Will chips, $12m ring flash, T3 pills preset (t,f,f)→all-on at "March quarter", T4 callouts (4 ink), 3 vertical markers with caption "and in December when it peaked" (cut confirmed live), Aug-26 ring, clean close — no JS errors. Also fixed en route: push_website_files.py false MISMATCH on >1MB files (contents API omits bytes) — now verifies large files by git blob sha; pushed to Fields_Orchestrator repo.

## [WALK-INK-VIEWPORT-DRIFT] Walkthrough ink now page-anchored (scroll space) + "Back to the walkthrough" pill — ⚠ DEPLOY HELD — 13:15 AEST
**Symptom:** Will's screenshot: viewers scrolling during a suburb walkthrough leave the drawn circles/arrows/handwriting floating over the wrong content. Root causes found: (1) ink was drawn in viewport coords and only compensated by a freeze-baseline translate set at the FIRST stroke of a scene — any stroke drawn after a user scroll landed offset by exactly the scroll delta; (2) the freeze listener only watched `window`, but on desktop (>900px) the scrolling element is the `.market-scroll` inner column (CSS-module class), so desktop user scrolls were never compensated at all; (3) during a held camera zoom the compensator was disabled (`!walk.zooming`), so ink detached 1:1 with scroll (confirmed live on production: ink moved 0px for a 200px scroll mid-zoom); (4) notes were clamped to viewport furniture (nav floor 110/118, CTA-band ceiling) at draw time, pinning off-screen-target ink onto whatever content was visible.
**Fix (engine):** SCROLL-SPACE ink. Every stroke/note stores (viewport coords + total scroll at draw time); `#wkInkWrap` carries a single `translate(−scroll)` transform recomputed per frame by the ONE writer `wkInkSync()`, composed there with the live camera affine (`wkCamAffine()` reads the svg's current viewBox + live rect, so scrolling through a held zoom stays glued — verified pixel-exact at 2.7×). `wkScrollNow()` sums window + every scrollable ancestor of ROOT (`wkScrollersOf`); the scroll listener is document-level capture-phase so inner-container scrolls are heard. Freeze machinery (`wkInkFreeze`/`inkBase`/`inkFrozen`) deleted. Viewport clamps in wkNote/wkNoteVB/wkAnnAt/placeClear/avoidNoteOverlap now gate on the target being on screen (`wkRectOnScreen`).
**Fix (UX — take-the-wheel):** a user scroll gesture (wheel/touchmove/scroll keys, filtered to exclude touches on the overlay itself) sets `walk.freeRoam`: scene-entry auto-scrolls in wkScrollTo/wkDock/wkTitleHighlight are suppressed (they still record `walk.anchorSel`), and a fixed gold pill "↑/↓ Back to the walkthrough" appears pointing toward the active section (map-app re-centre pattern). Follow mode resumes on pill tap, on transport/pip navigation, or automatically when the viewer scrolls the anchor back into view. Stage scenes (dim on) clear the anchor so the pill never shows over the whiteboard. PostHog: `walkthrough_scroll_away` {dir} on first pill show per departure, `walkthrough_return` {via: pill|self}.
**Verified (headless dev, mobile 390×844 + desktop 1440×900):** ink deltas relative to chart content exactly 0px after user scrolls on BOTH the mobile window scroller and the desktop `_marketScroll_*` inner scroller; pill shows with correct direction, click scrolls anchor back into view and hides; natural playback t≈62 reproduces Will's intended "50 days/≈25 + circles" composition on-chart; camera scene t=382–403 (2.7×) keeps circles pixel-on their bars before, during, and while scrolling through the hold (production comparison: prod detaches ink completely in this state); no JS errors; tsc + production build clean.
**Known remaining limitation (pre-existing, not a regression):** ink drawn while surrounding content is still hydrating/loading can be stranded by the layout shift (both builds); the existing seek-settle re-application covers the jump case.
**⚠ NOT DEPLOYED:** held with batch 3 + T4 callouts (same file). Ship together after Will's review.
**Files:** 01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts, 01_Website/src/components/MarketFlowProto/MarketFlowProto.css
**Recurrence:** First

## [ABS-TVD-GCCSA-FETCHER] Feature-add: ABS TVD Table 2 capitals medians fetcher — 13:30 AEST
**Symptom:** N/A (feature-add). "Gold Coast vs capitals" price-comparison chart needs quarterly median established-house transfer prices for Greater Sydney/Melbourne/Brisbane; no fetcher existed.
**Root cause:** N/A.
**Fix:** New `scripts/fetch_abs_tvd.py`. Uses the ABS Data API SDMX dataflow `ABS/RES_DWELL` ("Residential Dwellings: Unstratified Medians and Transfer Counts by Dwelling Type, GCCSA and Rest of State" — this IS TVD Table 2), one call for all 4 measures × 3 GCCSAs, full history 2002-Q1→. Prices scaled to whole dollars from each series' UNIT_MULT attribute (medians arrive in $ thousands). Writes single block via `$set` on `abs_tvd_gccsa` in `Gold_Coast.precomputed_macro_indicators` (`_id: macro_indicators`), `cosmos_retry`-wrapped. Rule 7/7b: `job_run("fetch_abs_tvd", cadence_hours=24*95)` heartbeat; raises if any city <20 quarters, if core fields missing, if latest median outside $100K–$10M (unit-scale guard), or if the fetch would go backwards/shrink vs the stored block. Ran once for real: 98 quarters per city to 2026-Q2 (Sydney $1,487,600 / Melbourne $850,000 / Brisbane $1,155,000 latest house medians). No cron added — quarterly wiring happens at page launch (10_Market_Report/gc_wide_overview_data_scoping_2026-09-12.md).
**Files:** scripts/fetch_abs_tvd.py (new; pushed 348ab88)
**Recurrence:** First

## [WALK-ARTICLE-T1-CIRCLE-FADE] Trap 1 label circle fades before the MIX text writes — ✅ DEPLOYED 6ef9ac08 — 14:55 AEST
**Symptom:** Will: the red circle (around "All attached dwellings 130") was still up when "not prices rising — the MIX rising" wrote over it — remove it just before the text.
**Fix:** `wkCircleAtF` gained an optional tag param (tags the drawn ink path); Trap 1's circle tagged `t1circ`, new beat t:192 fades it (`wkFadeTag`) one second before the t:193 text.
**Verified:** headless dev — circle present at t=190, gone with the text in at t=194.5.
**⚠ NOT DEPLOYED** per Will — start of a new held batch.
**Files:** 01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts
**Recurrence:** First

## [WALK-ARTICLE-EX13-ARC-TIMING] Mix arrow moved to "The middle property is now the three bedroom home" — ✅ DEPLOYED 6ef9ac08 — 15:05 AEST
**Symptom:** Will: the 2bd→3bd arrow drew too late (t:297, "largely just because that mix changed") — it should start at "The middle property…" and finish by "…three bedroom".
**Fix:** beat moved t:297 → t:286.4 (draws alongside the 2026 median circle; wkArrow's 0.6s shaft + head lands ≈288, on "three bedroom").
**Verified:** headless dev, LIVE playback through the moment — arrow fully landed at t≈288.8 while the caption still reads "The middle property is now the".
**⚠ NOT DEPLOYED** — held batch (with [WALK-ARTICLE-T1-CIRCLE-FADE]).
**Files:** 01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts
**Recurrence:** First

## [GC-OVERVIEW-PAGE-V1] Gold Coast city-wide overview live on /news — 14:05 AEST
**Symptom:** Feature build (Will's go-ahead): a Gold Coast option on the /news picker with a city-wide market panel — the licensing-clean v1 of the GC-wide overview scoped in 10_Market_Report/gc_wide_overview_data_scoping_2026-09-12.md.
**What shipped:**
  - Picker: 4th card "Gold Coast · CITY-WIDE · 61 suburbs tracked"; h1 now "Which market do you want to explore?". `/news/gold-coast` renders `GoldCoastOverview` (new component) in the market column — article rail shows scope-agnostic articles ("Latest Gold Coast research"), no condition pills (no market_pulse docs for GC), pulse fetch skipped.
  - Panel: hero stats (composite median $1,332,787 +8.0% YoY Q4-25, 2,878 active listings, 68 suburbs) · composite median chart · "GC vs the capitals" indexed race (our composite vs ABS TVD GCCSA established-house medians for Greater Sydney/Melbourne/Brisbane, indexed to 100 at Q3-17, CC BY attributed) · the leading-indicators explorer on `leading_indicators_gc.json` (lending r 0.81 @ +4q; matched-turn circles work unchanged).
  - Charts: hand-rolled inline SVG w/ crosshair+tooltip, direct end labels; 4-series palette (#166534/#b76749/#8b5cf6/#0284c7) validated for CVD+contrast (dataviz validator).
  - Data: `scripts/build_gc_overview_json.py` (new; shares `compute_gc_composite()` refactored out of build_gc_leading_indicators.py) → `public/data/gc_overview.json`; capitals from `abs_tvd_gccsa` (new fetcher, see [ABS-TVD-GCCSA-FETCHER]). `rateofsale` EXCLUDED from the GC explorer — its series is built from our 3-suburb listing histories, not a coast-wide input.
**Bugs caught during build:** (1) explorer CSS is scoped under `.liexp` — my slot lacked the class, so the r-scale rendered as unstyled stacked text; fixed by adding `liexp` to the slot div. (2) Price panel was titled "Price momentum (3-suburb avg)" from a hardcoded map in `IndicatorsExplorer.engine.ts` `subName()` — made it data-driven via `E.meta.price_labels` (fallback preserves suburb pages; Robina regression-checked: unchanged).
**Verified:** dev headless (desktop 1440 + mobile 390): picker card, hero stats, both charts + tooltips (hover screenshot), explorer matched-circles + r-scale styled, "Price momentum (Gold Coast)" label, Robina explorer unchanged; `npx tsc` + `npm run build` clean; console clean apart from pre-existing dev-only warnings. Live verify after deploy in progress.
**Files:** Website (commit `5d025bdf`, ONE batched push): src/components/GoldCoastOverview/{GoldCoastOverview.tsx,.module.css} (new), src/pages/NewsSplitPage/NewsSplitPage.tsx, src/components/IndicatorsExplorer/IndicatorsExplorer.engine.ts, public/data/{leading_indicators_gc.json,gc_overview.json}. Orchestrator: scripts/build_gc_overview_json.py (1f44551, new), scripts/build_gc_leading_indicators.py (3113e97).
**Deliberately deferred:** monthly auto-regeneration of the two JSON snapshots (same open follow-up as the parent leading_indicators.json — auto-refresh implies auto-publish, Will should choose); SQM-dependent charts (asking prices/stock/vacancy/rents) pending the ToS/permission decision; GC market_pulse verdicts.
**Recurrence:** First.

## [WALK-ARTICLE-T3-NOTE-POSITION] T3 summary notes pulled down into the chart area — ✅ DEPLOYED 6ef9ac08 — 15:20 AEST
**Symptom:** Will: the "12-mo: $1.5M → $1.492M — barely moves" handwriting rendered high over the article prose, not on the chart.
**Root cause:** the note anchored at fy 0.06 of the iframe — the same band as the pill ASTERISKS — so `avoidNoteOverlap` pushed it above the collision, off the chart entirely. Same anchor on the "Mar → Jun in ONE quarter" note.
**Fix:** both notes re-anchored to (0.30, 0.175) — top of the plot, below the pill row, clear of the stars (verified 70px into the 398px iframe, prose untouched).
**⚠ NOT DEPLOYED** — held batch (T1 circle fade, mix-arrow retiming, this).
**Files:** 01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts
**Recurrence:** First

## [WALK-ARTICLE-LOWERCASE-UNDERLINE] Trap 4 cards: cursive caps → lowercase + underline — ✅ DEPLOYED 6ef9ac08 — 15:45 AEST
**Symptom:** Will: "median SALE, not median LISTING" is illegible in the cursive font's upper case — write lowercase and underline key words instead (for this part).
**Fix:** `wkBoxNote` now parses `__word__` → `<u>` (underline 2.5px, offset 6px, CSS in MarketFlowProto.css); Trap 4's two cards rewritten: "want your home's worth? use __sold__, not asking" and "Trick 4 — median __sale__, not median __listing__".
**Verified:** headless dev — card renders lowercase with 2 underlines, fully legible (screenshot).
**⚠ NOT DEPLOYED** — held batch (T1 circle fade · mix-arrow timing · T3 note position · this).
**Files:** 01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts, .../MarketFlowProto.css
**Recurrence:** First

**Batch-4 deploy note (16:05 AEST):** shipped as ONE commit 6ef9ac08 (engine + CSS), live-verified headlessly: T1 circle gone with MIX text in at 3:14; mix arrow landed (4 ink) at 4:48 during "the middle property is now the"; T3 "barely moves" note 70px inside the iframe (top of chart, not over prose); Trick-4 card lowercase with 2 underlines; no JS errors.

## [WALK-HANDWRITING-NO-CAPS] Universal rule: handwritten emphasis = lowercase + underline, never caps — ✅ DEPLOYED eb05b5ed — 16:30 AEST
**Symptom:** Will (generalising the Trap 4 fix): cursive caps are illegible everywhere — convert ALL handwritten caps-for-emphasis to lowercase+underline and make it the standing rule for these videos.
**Fix:** engine — shared `wkHandText()` renders `__word__` → `<u>` and is now used by `wkNote`/`wkNoteVB`/`wkNoteAt`/`wkFreeNote`/`wkBoxNote` (every handwritten surface); 13 caps-emphasis occurrences converted across article AND suburb beats (middle/average/mix/above/one/before/built/other); acronyms (CBA/RBA/SQM) stay caps. CSS underline rule extended to `.wk-note u`. Rule codified in `Video_Editing_Process/RUNBOOK.md` and the walkthrough-pipeline memory so future beat authoring follows it.
**Verified:** headless dev — article t33/t90/t196/t407/t527 all lowercase with `<u>` present; suburb Robina v2 t436 "a signal that moves before prices" with underline; no JS errors; production build clean.
**⚠ NOT DEPLOYED** — new held batch.
**Files:** 01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts, .../MarketFlowProto.css, 10_Market_Report/Video_Production/Video_Editing_Process/RUNBOOK.md
**Recurrence:** First

## [WALK-VLINES-WEBKIT] T4 vertical lines invisible in Safari — WebKit-safe rebuild — ⚠ DEPLOY HELD — 16:55 AEST
**Symptom:** Will (incognito, Mac Safari): no vertical dotted lines through the whole T4 marker sequence, while the wipe and captions ran fine. Production Chrome (headless, incognito, 1800px) draws all three correctly — browser-specific.
**Root cause (high confidence):** the vlines were the ONLY ink built as many disjoint subpaths revealed via the getTotalLength/dashoffset write-on — WebKit computes multi-subpath getTotalLength wrong, so the dash reveal never painted (or threw, which would also stall the beat loop).
**Fix:** (1) vline = ONE subpath with a real `stroke-dasharray:2.5 8.5` (dotted look) revealed by opacity fade — no length API involved; (2) `wkDraw` hardened: getTotalLength in try/catch, invalid length → instant show (protects every pen stroke on WebKit); (3) `walkApplyBeats` wraps each beat run in try/catch so one bad draw can never stall the walkthrough.
**Verified:** Chrome dev — 3 dotted paths (dash 2.5 8.5, opacity 1, 262px) during playback; full battery (12+6 checkpoints, suburb regression) green; production build clean. Safari can only be confirmed on Will's machine post-deploy — no WebKit runtime exists on this VM.
**⚠ NOT DEPLOYED** — held with [WALK-HANDWRITING-NO-CAPS]. ⚠ This one needs a deploy for Will to confirm on Safari.
**Files:** 01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts
**Recurrence:** First (Safari-specific ink path)

## [WALK-VLINES-ZERO-BBOX] T4 vertical lines never painted for ANYONE — zero-width bbox × filter region — ✅ DEPLOYED eb05b5ed — 17:10 AEST
**Symptom:** Will (incognito, so current bundle): no vertical dotted lines in the Trap 4 sequence. Reproduced in headless Chrome once verification checked PIXELS, not geometry.
**Root cause:** a perfectly vertical path has a ZERO-WIDTH bounding box, and the `wkRough` filter's region is bbox-relative (`x:-25% width:150%`) — 150% of 0 = an empty filter region, so the browser renders the filtered path as NOTHING. Geometry, opacity and stroke all read correct — only paint is missing — which is why every one of my probe-based verifications (path counts, rects, computed opacity) "passed" while the feature was invisible in production through batches 3–4. My earlier "vlines fixed" conclusion after the ink-lock repair re-checked coordinates only; the ONE screenshot I actually read at the time was the one that exposed the ink-lock bug, and no pixel-check was repeated after. Second contributing bug found on the way: the seek/settle dev flake (already fixed) had been masking visual runs.
**Fix:** `wkVLineAt` rebuilt — single polyline with ±1.3px alternating x-jitter every ~52px (restores the hand-drawn feel AND gives the bbox real width), `filter="none"` (the bbox-relative filter can never be safe on near-vertical strokes), real `stroke-dasharray:"6 7"` at 3.2px with opacity fade-in — no reliance on the getTotalLength dash trick (also hardens WebKit). Plus two defensive guards that stay: `wkDraw` try/catch on getTotalLength (falls back to instant show), and beat `run()` wrapped so one bad draw can never stall the walkthrough.
**Verified:** headless Chrome dev, warmed layout, LIVE playback — three dashed red verticals VISIBLE in the screenshot (pixel check, not probes) through the 2021 / Feb-2022 / Dec-2025 dots; production build clean.
**Lesson (applies to every overlay verification):** assert PAINT, not geometry — a rect+opacity probe is the read-side equivalent of a heartbeat that reports success while doing nothing (Rule 7b). Screenshot-and-look is the only sufficient check for ink.
**⚠ NOT DEPLOYED — recommend shipping promptly:** this is a live defect (feature invisible to all viewers); held per Will's deploy gate, bundled with [WALK-HANDWRITING-NO-CAPS].
**Files:** 01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts
**Recurrence:** First

**Deploy note (17:35 AEST):** vline fix + no-caps rule shipped as ONE commit eb05b5ed and verified live BY PIXELS: incognito production playthrough screenshot shows all three dashed red verticals on the Trap 4 chart at 13:35; Trick-4 card lowercase with 2 underlines; no JS errors.

## [GC-OVERVIEW-STALE-TAIL+SAMPLE-SIZES] GC series current to Q2 2026; n in tooltips; houses-only captions — 15:10 AEST
**Symptom:** Will (1) required sample sizes on all hovered medians + explicit dwelling-type statements in captions (houses-only; units would need their own toggle), and (2) rejected the GC composite ending 2025-Q4 as unacceptable.
**Root cause (of 2):** The wide-suburb transaction history in `precomputed_indexed_prices` comes from Domain property-timeline events (`scraped_data.property_timeline.is_sold`) captured by a GC-wide sweep whose **last run was ~Nov 2025** — wide-suburb sold events collapse after Oct 2025 (helensvale Oct 26 → Nov 8 → Dec 1 → 0; pimpama/palm_beach identical). The nightly pipeline maintains sold data for the 8 tracked suburbs only, so 2026 quarters exist for 19 suburbs (Q1) / 3 suburbs (Q2). The composite's composition guard (correctly) truncated at the last full-panel quarter.
**Fix:**
  - `compute_gc_composite()` now also returns a **chain-linked extension**: past the last full-panel quarter, each quarter's growth is computed on the matched panel (suburbs holding BOTH adjacent quarters — drawn from ALL suburbs, not just the coverage-eligible set, since chaining is composition-safe by construction), rolled onto the level series. Points carry `n` (sum of rolling tx counts), `panel` (suburb count), `prov: true`. Tail: 2026-Q1 $1,357,885 (19 suburbs, n=1,886) → 2026-Q2 $1,357,092 (3 suburbs, n=566). Min panel 3.
  - Solid quarters now carry per-point `n` too; capitals points carry ABS transfer counts. Explorer `price.pooled` display series extended with chained momentum (2026-Q2 = 5.76%); **r stays computed on the solid series only**.
  - Frontend: provisional points render as a **dashed tail**; tooltips show `n` per series per point + a "provisional — k-suburb panel" footer; hero shows "(provisional, 3-suburb panel)"; both chart captions now open with **"Houses only — units are not included"** (a units view needs its own series + pill — no unit medians exist in `precomputed_indexed_prices`; follow-up).
**Verified:** dev headless hover screenshots (median: "Q2 26 · $1,357,092 · n 566 · provisional — 3-suburb panel"; race: all four series with n, GC dashed tail to Q2 26); tsc + build clean; live deploy in progress at time of writing (poller armed).
**Files:** Website commit `9dc9d806`: src/components/GoldCoastOverview/{.tsx,.module.css}, public/data/{gc_overview.json,leading_indicators_gc.json}. Orchestrator: scripts/build_gc_leading_indicators.py (b6aa1f12), scripts/build_gc_overview_json.py (8586a3b4).
**Real fix still open:** re-run the GC-wide sold/timeline capture (stopped Nov 2025) and put it on a cadence — scoped for Will in-session; not launched unilaterally (multi-thousand-page Domain sweep = load/ban-risk call).
**Recurrence:** First for this chart; the underlying "wide-suburb capture stopped and nothing noticed" is kin to the Rule-7 silent-failure family — the sweep had no heartbeat, so its death was invisible until a chart exposed it.

## [SMS-STOP-CALL-LIST-GAP] STOP reply left contact on priority call list; confirmation send silently failed — 17:24 AEST
**Symptom:** Contact +61452649132 (unnamed FB Buyer Brief lead, walkthrough batch 09-11) replied "Stop" at 16:16. Webhook set do_not_sms but the contact stayed on the Priority Calls tab (overdue CALL, due 09-01), and the designed unsubscribe-confirmation SMS never went out — failure visible only in Netlify console.log.
**Root cause:** (1) STOP handler in justcall-sms.mjs only set do_not_sms — never cleared follow_up_at or set priority_dismissed_at, and clearing follow_up_at alone would re-surface the contact in the tab's "New — reachable" section. (2) Confirmation send: JustCall adds the number to its platform Do Not Message list BEFORE the webhook fires, so on every REAL stop the API send bounces 400 "One of the numbers is on your do-not-text list"; sendSms errors were swallowed into console.error. The 09-05 test "passed" because it was a simulated webhook payload — JustCall never saw a real STOP for that number. There is no JustCall API to remove from DNM (only bulk-ADD exists), so the confirmation is unsendable programmatically; the platform block is itself the opt-out being enforced.
**Fix:** justcall-sms.mjs STOP path now also sets follow_up_at=null + priority_dismissed_at (full removal from both call-list sections, per Will 2026-09-12: STOP = full opt-out). Telegram STOP alert now reports the confirmation-send outcome: sent / "JustCall already auto-blocked (expected)" for the do-not-text bounce / ⚠️ FAILED with error text for anything else. Backfilled the 16:16 contact by hand (6a5c057aad73e69e9d68c54e): follow_up_at cleared, priority_dismissed_at set, timeline note added. Deployed 480cd3a6 + 71a9acc2, Netlify state=ready verified.
**Files:** netlify/functions/justcall-sms.mjs (website repo)
**Recurrence:** First

## [WALK-ARTICLE-GA] Walkthrough GA on the article + face mask on every /news start-here card — ✅ DEPLOYED 21bc47e8 — 18:20 AEST
**Symptom:** Will's direction: ship the walkthrough on the real article URL (no query param) and put the circular face mask over the top-right corner of the article's start-here card on every /news page — partly on, partly off the card.
**Fix:** (1) ArticlePage gate flipped: walkthrough default-ON, `?walkthrough=0` is the rollback; ArticleWalk hides its launch button for slugs the engine registry doesn't know (`mountArticleWalk` returns ok:true/false). (2) `?play=1` deep link auto-starts the article walkthrough (mirrors the suburb wkAutoStart: unmuted attempt → muted fallback + "Tap for sound" chip). (3) The LIVE /news page turned out to be NewsSplitPage (the split prototype has shipped) — its start-here card gets a 62px circular poster mask at top:-16px right:-14px with the gold play chip; click deep-links to the article `?play=1` (PostHog `walkthrough_card_mask_click` on the engine variant). Same mask also added to the engine's `startHereHtml` and MarketIntelligencePage's pillars so every start-here variant is covered.
**Verified LIVE (pixels + behaviour):** plain article URL → 7 masks + 7 Ask Will chips + launch button; `?walkthrough=0` → none; unregistered slug → none; `?play=1` → overlay active and PLAYING; mask present on all three /news suburbs half-off the corner (screenshot); mask click → article with walkthrough auto-playing; no JS errors.
**Files:** engine.ts, MarketFlowProto.css, ArticleWalk.tsx, ArticlePage.tsx, NewsSplitPage.tsx(+module.css), MarketIntelligencePage.tsx(+module.css) — commit 21bc47e8.
**Recurrence:** First

## [GC-WIDE-SOLD-BACKFILL] onthehouse sold capture extended to all 82 GC suburbs + monthly cron — 19:25 AEST
**Symptom:** Wide-suburb sold capture dead since Nov 2025 (root cause in [GC-OVERVIEW-STALE-TAIL+SAMPLE-SIZES]); Will directed restoring currency, Domain-first, onthehouse as the no-Bright-Data alternative.
**Investigation:** Domain GraphQL route (shared/domain_graphql.py) validated as design but **Bright Data's unlocker went down mid-investigation** — every relay call returns 200-empty (zone auth/billing fine; HTML fetches equally dead; worked 20:30 the night before). Two failure layers: (1) their relay began emitting an HTTP/2-illegal `proxy-connection: close` header → curl_cffi hard-fails curl(92) before retries; (2) with h1.1 forced, bodies come back empty. (1) is now hardened (h1.1 on the api.brightdata.com hop only, both shared modules); (2) is their outage — recovery monitor armed, warns at 20:00 if the nightly will fail.
**Fix (the actual capture):** pivoted to onthehouse (free, direct, no proxy; sold records carry beds/baths/type/landSize/lat-lon/VG records — also serves Will's segment drill-down ask):
  - `scripts/onthehouse/suburbs.py`: +WIDE (79 suburbs) / ALL_GC (82) — slugs taken from onthehouse's own suburb_profiles sitemap ∩ our Gold_Coast collections, so none are guessed. ⚠ Multi-postcode collision: dict-by-name kept `southport-4222` (0 results) over the correct `southport-4215` — caught via the 0-in-suburb tell, fixed, re-crawled (208 sold houses). cedar-creek-4207 verified correct.
  - `scripts/onthehouse_sold_sync.py`: `--all-gc` flag; GC-wide runs report as their OWN heartbeat `onthehouse_sold_sync_gc_wide` (744h cadence) + Rule-7b guard (raise on 0 suburbs reached or failures>successes). Seeded via record_job_result after the backfill; registry row verified (`self_registered: true`).
  - **Backfill run:** 82 suburbs deep, **6,779 sold houses seen / 6,014 new**, 1,541 price-withheld (23%, expected), **0 fetch failures**, ~35 min. `system_monitor.onthehouse_sold`: 858 → 7,085 docs. ⚠ Their index is a hard 12-month window — TODAY it still covered the entire Nov-2025 gap; a month later and that data would have been unreachable free.
  - **Cron:** `0 3 28-31 * *` + last-day guard (`date -d tomorrow` = 01) → fires 03:00 AEST on the actual last day of each month, outside orchestrator hours, the night before the 1st-of-month 05:00 precompute chain consumes it (Will's spec).
  - `scripts/precompute_union_prices.py`: `--all-gc` flag (staging run in progress at time of writing; promote + GC page regeneration to follow).
**Files:** scripts/onthehouse/suburbs.py (e30c5363), scripts/onthehouse_sold_sync.py (bd5c5635), shared/domain_fetch.py (39c02ddd), shared/domain_graphql.py (06ccc8e2), crontab.
**Recurrence:** First. Postcode mis-map is kin to [SQM-POSTCODE-MISMAP] 2026-09-07 — third postcode-identity incident; the 0-results tell caught both.

## [WALK-CARD-MASK-CLIP] News-card face clipped on the right — moved inside the card edge — ✅ DEPLOYED — 18:50 AEST
**Symptom:** Will: the circle mask was cut off on its right on /news (his viewport) — a column ancestor clips just past the card's right edge, so any right overhang is at the container's mercy.
**Fix:** mask anchored `right:6px` (was −14px overhang) in all three start-here variants (NewsSplitPage, MarketIntelligencePage pillars, engine sh-card) — still breaks over the TOP edge for the half-on/half-off look, but nothing can clip it horizontally at any width.
**Verified:** dev at 1200/1440/1800 and LIVE production at 1800 — circle fully visible, 6px inside right, 16px above top (screenshot).
**Recurrence:** First

## [UNION-PROMOTE-ROLLING-CLOBBER] --all-gc promote collapsed wide-suburb rolling series; fixed + overnight restore scheduled — 20:10 AEST
**Symptom:** After `precompute_union_prices.py --all-gc --promote` (31 suburbs staged with valid medians), spot-check showed wide suburbs' `rolling_12m_median_series` collapsed to the union window — helensvale/palm_beach/southport 36→**1 point**. Tracked suburbs unaffected (robina 120 pts intact); `indexed_series` merge correct everywhere (deep history + Q2 2026 appended).
**Root cause:** `promote_medians`' rolling merge drops live points lacking `ci_low` ("never recomputed on the union basis — leave it out rather than mix bases"). Every tracked-suburb point has union CIs from prior monthly promotes, so the branch never fired before; wide suburbs' points are all timeline-precompute-written (no CI) → all dropped. Compounding: `load_domain_history` under-reads wide-suburb history (helensvale 144 events vs 2,329 in the indexed precompute — likely `street_address`-field vintage differences; southport/coombabah/currumbin_waters read 0), so the union's own rolling replacement was short. **Also, my first read of the staging run was from a `| tail -40`-truncated log — wrong conclusions until the staging COLLECTION was queried directly (31 promoted-quality suburbs, not 3).**
**Impact:** None user-facing (no live page consumes wide rolling series; GC page serves baked JSON). GC generators NOT rerun — their Rule-7b guards would have refused the collapsed panel anyway (working as designed).
**Fix:** (1) promote now KEEPS pre-union rolling points, labelled `basis: "domain_timeline"` — mixed basis disclosed per point instead of destroyed (pushed c6e57d34). (2) Restore chain scheduled 00:30 AEST (after pipeline + onthehouse crons, per VM-capacity rule): rerun `precompute_indexed_price_data.py` (full timeline rebuild restores wide rolling) → `--all-gc --promote` with fixed merge → regenerate GC JSONs → validation prints. Website JSON push after morning review.
**Open (follow-up):** `load_domain_history` wide-suburb under-read (address-field vintage) — affects union coverage stats, not outputs, since rolling history is now preserved from the timeline basis. Update `run_monthly_market_precompute.sh` union line to `--all-gc` only after the restored chain is verified.
**Files:** scripts/precompute_union_prices.py (c6e57d34).
**Recurrence:** First. Kin to the tail-truncation family: diagnosing from a windowed log nearly shipped a wrong conclusion — query the artifact, not the log.

## [FOUNDER-VIDEO-SHIP] "Why I built Fields" video → /why-fields + off-market V5; blobs nginx lacked video MIME — 18:50 AEST
**Symptom:** (feature ship, with one real fix) Will's 84s founder video (Drive, 102MB HEVC .mov) needed captioning + placement on /why-fields and the off-market pages. First serve attempt from blobs.fieldsestate.com.au returned `Content-Type: application/octet-stream` for the mp4 — the blobs nginx site has a whitelist `types {}` block (jpg/png/webp/gif/html/css/js/pdf/json only), and mp4 wasn't in it; Safari can refuse to play video without a video/* type.
**Root cause:** HEVC .mov doesn't play in Chrome/Firefox (needed H.264 transcode), and the nginx types whitelist predates any video being served from blobs (walkthrough videos ship via the website repo's public/, not blobs). Also: Gemini/Vertex ASR timestamps (the Video_Editing_Process pipeline's transcriber) drifted up to 5s on an 84s single take — irregular, not linear, so chunked re-transcription + snap-to-silence couldn't fully rescue them; two segments came out degenerate (end<start).
**Fix:** Transcoded to H.264 1080p CRF23 +faststart (27MB). Captions: faster-whisper (isolated venv at scratchpad, base.en, word timestamps — timings verified against ffmpeg silencedetect and audio slices) for TIMING + the verified Gemini transcript for TEXT (whisper misheard "buyer analysis" as "bioanalysis" etc.); 23 blocks burned as ASS in house style via vlib.segments_to_ass. QC: 4 caption frames read multimodally, sync confirmed including final line inside 84.2s. Assets → /data/blobs/site-video/ (mp4+poster+srt). Added `video/mp4|webm|quicktime` + `text/vtt` to the blobs nginx types block, nginx -t, reload. New sections: WhyFieldsPage below hero (dark block continuation), OffMarketV5 below My downloads above footer (Will's chosen spot); both `preload="none"`, poster, one-shot `why_fields_video_play` PostHog capture. Pushed as ONE commit 85625b3e via push_website_files.py.
**Files:** src/pages/WhyFieldsPage/WhyFieldsPage.{tsx,module.css}, src/pages/OffMarketPage/v5/OffMarketV5.{tsx,module.css} (website repo); /etc/nginx/sites-available/blobs (VM config, not in a repo); /data/blobs/site-video/* (assets).
**Recurrence:** First. NOTE for future ASR: Gemini timestamps are unusable for caption sync on this pipeline's own evidence — consider adding faster-whisper alignment to Video_Editing_Process if caption drift appears in monthly videos (their flow re-transcribes the ASSEMBLED master, where the same drift mechanism applies).

## [WDM-VIDEO-CAPTION-OVERLAY] Why Data Matters launcher + both videos moved to /news-style page-rendered captions — 19:15 AEST
**Symptom:** (feature ship) Will: add "Why Data Matters" (120s, Drive, HEVC .mov 147MB) as a circle-mask launcher top-left of the V5 "Your valuation" rail title; click opens a full-rectangle lightbox (founder-player size) over a BLURRED page; captions for BOTH site videos must match the /news walkthrough black-and-white format.
**Root cause:** n/a (feature). Key design finding: the /news walkthrough serves a CAPTION-FREE mp4 and renders captions in HTML (`.wk-chip` in MarketFlowProto.css:360 — white 650-weight text in rgba(17,18,17,.88) pills, box-decoration-break: clone). Burned ASS can only approximate that; page-rendered chips match it exactly and survive any player size.
**Fix:** New shared `CaptionedVideo` component (components/CaptionedVideo/) — plain <video> + wk-chip-style caption bar driven by {start,end,text}[] cue JSON fetched from blobs, container-query font sizing, one-shot onFirstPlay. `Rail` gained an optional `avatar` slot. V5: circle launcher (face-crop poster + copper play badge) on the valuation rail head → `VideoModal` (fixed, scrim rgba(6,10,8,.55) + backdrop-filter blur(10px), Esc/scrim close, body scroll lock, autoplay-on-open — the opening click is the user gesture). Founder video on /why-fields + V5 switched to `why-i-built-fields-nocaps.mp4` + cues (blobs Cache-Control is immutable-1y, so the caption-free master got a NEW filename rather than overwriting the burned one). Cues: faster-whisper word timings + Gemini-verified text (whisper misheard "primary school" as "primary spill", "square metre" dropped); 35 cues (WDM) / 23 cues (founder). Assets on blobs/site-video/ (CORS * confirmed for the cue fetch). Commit 2a861da5, one build.
**Files:** src/components/CaptionedVideo/CaptionedVideo.{tsx,module.css} (new), src/components/Rail/Rail.tsx, src/pages/OffMarketPage/v5/OffMarketV5.{tsx,module.css}, src/pages/WhyFieldsPage/WhyFieldsPage.{tsx,module.css}; /data/blobs/site-video/why-data-matters.{mp4,srt,cues.json}, why-data-matters-{poster,circle}.jpg, why-i-built-fields-nocaps.mp4, why-i-built-fields.cues.json.
**Recurrence:** First. ⚠ If caption WORDING ever changes, write a NEW cues filename (immutable cache). ⚠ The WDM video says "it's this section..." (recorded for a listing-presentation context) — flagged to Will in session.

## [BRIGHTDATA-ZONE-407] CORRECTION: not an outage — zone proxy-auth rejected (407), needs dashboard check — 21:05 AEST
**Correction to [GC-WIDE-SOLD-BACKFILL]:** I earlier diagnosed the Domain/Bright Data failure as "their unlocker outage" based on 200-with-empty-body responses. That was wrong. Re-probed 21:00 AEST with header inspection: every relay request (Domain AND control URLs example.com / httpbin.org) returns **`x-brd-status-code: 407` (Proxy Authentication Required)**, and `zone/cost` shows **$0 billed for the whole day** = zero successful requests.
**Real signal:** the management API bearer token is VALID (`zone/cost` returns 200), but the Web Unlocker **zone** rejects proxy auth (407). Token-revoked would 401 everywhere (cf. [BRIGHTDATA-TOKEN-ROTATED] 2026-08-13); token-valid + zone-407 + $0-spend points at the ZONE/ACCOUNT: rotated zone password, billing hold, or paused/suspended zone. Not Domain-Akamai (control URLs fail identically), not a BD infra outage.
**Action:** requires Bright Data dashboard access (Will) — verify `web_unlocker1` zone is active, check billing/payment flags, check whether zone password rotated. If rotated, new credential into .env. Cannot be resolved from the VM.
**The h1.1 hardening (39c02ddd/06ccc8e2) still stands** — it fixed a real curl(92) crash on the malformed `proxy-connection` header and is correct regardless; it just isn't the whole story. Unaffected: onthehouse coast-wide capture (done, no BD) and the 00:30 restore chain (DB-only).
**Recurrence:** Second BD-credential incident (after [BRIGHTDATA-TOKEN-ROTATED]); first presenting as 407-zone rather than 401-token. Lesson (again): read `x-brd-status-code`, not just the body — an empty 200 body hid the 407 and produced a wrong root cause for ~2 hours.

## [BRIGHTDATA-ZONE-407] RESOLVED — account was out of credit — 21:15 AEST
Will topped up the Bright Data account. Re-probe: control URL x-brd-status 200 (real body), Domain GraphQL x-brd-status 200 ({"data":{"version":null}}), full search_listings for Helensvale sold returned 3,741 total / 200 on page. The 407 was account credit exhaustion (zero balance -> proxy auth refused), matching the "zone/account, not infra" diagnosis. Recovery monitors stopped. h1.1 hardening remains (correct independent of this). Tonight's 20:30 Domain step 101 likely failed pre-top-up; watchdog re-runs, next nightly clean.

## [LIVINGMAP-COORD-RESIDUE-SILENT-REASON] Living Map nightly built 0 for weeks — failure reason discarded, 4th coord path unread, backfill never scheduled — 21:30 AEST
**Symptom:** `precompute_living_map` heartbeat in error: "26 listings eligible but 0 living_maps built — upstream (cadastre / Static Maps key / valuation output) is broken". Every run since at least Aug 29 built 0–3; new listings (esp. units) shipping without a Living Map, which is the live default on mini-sites.
**Root cause:** Three gaps, none of them the components the error message named (Static Maps key, cadastre and valuation all verified healthy). (1) `build_one()` returned `(None, reason)` but `_flush` discarded the reason string — every failure was the silent "no parcel geometry and no coordinates" path, invisible in the log. (2) The coordinate fallback chain read 3 paths (`LATITUDE`, `georeference_data.coordinates`, `geocoded_coordinates`) but not `onthehouse_data.latitude/longitude`, where 9 of the 26 stuck docs carried a perfectly good point (Rule 8 case), nor `address_geocode`, which `backfill_parcel_boundary.py` writes. (3) `scripts/backfill_parcel_boundary.py` — built 2026-09-01 for exactly this residue — was never scheduled: no cron, no cadence, run manually on 2 properties ever. The residue regrew with each coordinate-less unit listing.
**Fix:** (1) `_flush` now prints `– address: reason` for returned-None failures; Rule 7b raise message reworded to point at the per-property lines instead of guessing at upstream services. (2) Fallback chain extended with `onthehouse_data` and `address_geocode` paths. (3) `backfill_parcel_boundary.py` cron'd nightly 00:45 (`--missing`, before the 01:15 precompute), heartbeat given `cadence_hours=24`. Ran both: backfill resolved 21/60 targets (39 no-match on complex unit formats); precompute then built 5 (was 0), heartbeat green, every remaining failure now named in the log. The 14 still-unmatched complex unit addresses (Level-prefix, dual-key, abbreviated street types) handed to a spawned patch task (6aa533ca602c7800d6f1f925) to extend address normalisation in `resolve_lotplan_by_address()`.
**Files:** scripts/precompute_living_map.py, scripts/backfill_parcel_boundary.py, crontab (00:45 entry). Pushed e6c1f2dc + 35aab994.
**Recurrence:** First for living_map specifically; Nth for the Rule 7b family "error message asserts a cause it never tested" ([INDEXING-SILENT-ZERO], [BUILDER-ENV-EXPORT-GAP]) and for Rule 8 wrong-field-name absence ([FIELD-NAME-GUESS-FALSE-ABSENCE]).

## [VM-NO-DISK-SNAPSHOTS] VM boot disk had zero backups — snapshot schedule enabled + freshness watcher — 21:55 AEST
**Symptom:** Systems check found no GCP disk snapshots and no snapshot schedule on either disk. The three data layers (Mongo→GCS, blobs→GCS, code→GitHub) cover data but not the machine: a boot-disk failure would lose all 83 .env credentials, config/settings.yaml, OAuth token files, the 131-entry crontab, 20 systemd units, nginx configs, venv, and the agent memory dir — days of rebuild and full credential re-issuance.
**Root cause:** VM-level backup was simply never set up; the backup story grew per-dataset (each after its own incident) and nothing owned the machine layer.
**Fix:** (Will approved the spend.) Created resource policy `fields-vm-daily-snapshot` (daily 18:00 UTC = 04:00 AEST, 14-day retention, keep-on-disk-delete, stored australia-southeast1), attached to the 100GB boot disk, and took an immediate first snapshot `fields-vm-initial-20260912` (46.4GB, READY). The 750GB blob disk is deliberately excluded — already mirrored nightly to gs://fields-blob-backup. Per Rule 7 the GCP schedule is an ongoing system that fails silently, so added `scripts/check_vm_snapshot.py`: asserts a READY boot-disk snapshot ≤36h old exists (7b: zero-or-stale raises), heartbeats `vm_snapshot_freshness` (cadence 24h, self-registered), cron 04:40 AEST; ran once, heartbeat green. Cost ~$2-3/mo at current churn.
**Files:** scripts/check_vm_snapshot.py (pushed 268603eb); crontab (04:40 entry); GCP resource policy + snapshot (not files).
**Recurrence:** First. Same family as every "no self-check" incident Rule 7 cites — this one just never had the process to begin with.
