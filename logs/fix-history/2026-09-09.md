
**Follow-up (07:15 AEST):** Made the Trap 4 asking-vs-sold chart interactive (hover tooltip). Static PNG cannot do hover; article HTML renders via dangerouslySetInnerHTML so inline <script> will not execute. Solution: `chart_bw_asking_sqm_html()` in market_article_charts.py generates a self-contained interactive HTML (inline SVG + vanilla JS, data baked in, no external libs) to /data/blobs/article-charts/median_bw_asking_sqm.html; article embeds it via <iframe> (with <noscript> PNG fallback). Title+legend moved INSIDE the SVG viewBox so the whole chart scales at one fixed aspect (820/470) and never clips at any width. **Infra:** added `text/html html htm; text/css css; application/javascript js;` to /etc/nginx/sites-available/blobs types block (was serving .html as application/octet-stream -> iframe downloaded instead of rendering); nginx -t + reload. No CSP / X-Frame-Options on site or blob host, ACAO:* -> cross-origin framing works. Verified live via headless: iframe renders inline (652x374, aspect ok), hover inside iframe on production shows correct tooltip. ⚠ blob host sets Cache-Control immutable max-age=1yr on .html too, so the iframe src carries ?v=20260908 — bump it when regenerating. Pushed (commit 0517654c), rebuilt, verified.

## [ARTICLE-DATE-HYDRATION-418] formatArticleDate had no timeZone → SSR/client date mismatch — 07:20 AEST
**Symptom:** After swapping the seasonality graphic in article `december-listing-paradox` and bumping its `updated_at` to `2026-09-08T21:08Z`, the live page threw React #418 (hydration text mismatch, `args[]=text`). Console went from 0 → 1 msg; page rendered fine but hydration failed.
**Root cause:** `src/services/articleService.ts::formatArticleDate` called `toLocaleDateString('en-AU', …)` with NO `timeZone`. It uses the runtime zone — UTC on the Netlify SSR server, local (AEST, UTC+10) on the client. `21:08Z` = "8 September" in UTC but "9 September" in AEST, so the "Updated <date>" line differed server vs client. Any article with a published/updated time in the UTC 14:00–24:00 window trips it. Not caused by the image swap — the timestamp bump exposed a latent bug.
**Fix:** Added `timeZone: 'Australia/Brisbane'` (Gold Coast zone, no DST) to the options in `formatArticleDate` so server and client always format the same calendar day. Fixes it site-wide (all article cards + article header use this one helper), not just this article. Pushed commit 7e90253 → Netlify auto-deploy; re-verified headless: #418 gone, Console 0.
**Files:** `Feilds_Website/01_Website/src/services/articleService.ts`
**Recurrence:** Nth (date-formatter hydration is a recurring class — see [HOUSE-MINI-SITE-003], [V3-HERO-DATE-HYDRATION], [GFONT-HYDRATION-418], [OFFMARKET-DECK-HYDRATION-418]). This is the first for the shared `formatArticleDate` helper. Standing lesson: EVERY `toLocale*` date call rendered under SSR must pass an explicit `timeZone`.

## [SEASONALITY-GRAPHIC-WIDTH] Book seasonality strip didn't fill article width — dead space from tight-bbox + overflowing text — 07:20 AEST
**Symptom:** The seasonality calendar strip (adopted into `december-listing-paradox` to replace the old matplotlib chart) rendered with the 12 month cards filling only ~68% of the image width; large dead space on the right made the months look small on the page.
**Root cause:** In `render_seasonality_strip.py` the title-lede and source lines were single long strings placed at x=0 with `ha="left"`; they overflowed past the card strip's right edge (axis x=12). `fig.savefig(..., bbox_inches="tight")` then expanded the saved PNG to include that overflowing text, so image width was governed by the text, not the cards (5444px vs the ~3894px card strip). The cards therefore never reached the image edge.
**Fix:** Wrapped the lede (`textwrap.wrap width=96`) and source (`width=130`) into multiple lines that stay within the card strip; also enlarged the render (13×6.4, cards 60% height, labels 22pt / values 19pt) to fill vertical space. Tight bbox now equals the card strip → months fill full width. Re-rendered PNG+PDF (book chart `ch2-1` and article share this asset), pushed new base64 into the article, rebuilt, verified headless: cards span full column width, matching the data table below.
**Files:** `08_Seller-Book/Market_Data/seasonality/render_seasonality_strip.py`; article DB doc `system_monitor.content_articles` slug `december-listing-paradox`
**Recurrence:** First

## [WATER-CLASS-TUPLE-SILENT-CORRUPTION] classify_water_relationship tuple return corrupted all 3 legs of renovation study — 07:45 AEST
**Symptom:** New `16_Valuation/Renovation_Premium/renovation_premium_study.py` first run: matched-pair leg found 0-3 matches out of 70+ treated per suburb; every hedonic p-value exactly 1.0 with CIs [-100%, +Inf]; r²=0.98 in a 136-row regression.
**Root cause:** `shared/waterfront.py:classify_water_relationship()` returns a `(class, reason)` TUPLE and the reason string is per-document (e.g. `('dry', 'osm_distance_to_water:265m')`). The study stored the tuple as the water-class value: (1) equality matching between treated and comps almost never fired → empty matched-pair leg; (2) `C(water)` in statsmodels exploded into hundreds of unique dummy levels → singular fit, leverage-1 rows, HC3 covariance → p=1.0/infinite CIs and fake r²; (3) `water == WATERFRONT` (tuple vs string) never true → 14 specialist-market waterfront sales silently leaked into the sample. One wrong assumption, three differently-shaped failures, none raising an error.
**Fix:** Take `classify_water_relationship(doc)[0]`; switched HC3→HC1 (robust to near-leverage-1 rows in small quarter×class cells). Re-run produced sane fits (r² 0.74-0.75) and 31/32/8 matched pairs per suburb.
**Files:** `16_Valuation/Renovation_Premium/renovation_premium_study.py`
**Recurrence:** First

## [CODESERVER-WATCHER-LEAK] Workbench dead again 2d after restart — leak rate is 2-day, not 32-day — 19:35 AEST
**Symptom:** `/healthz -> 0 after 30.0s`, 2 consecutive checks; guard at `level=critical … workbench_unhealthy streak=3`. Load 17.2 rising, 313Mi free RAM, **swap 7.9Gi of 8.0Gi consumed (131Mi left)**, 11 processes in `D` state split between `folio_wait_bit_common` (page-ins) and `rq_qos_wait` (block-queue saturation). Confirmed genuinely dead, not slow: local `curl /healthz` returned `000` after 25s. Disk fine at 86% — the 2026-09-07 backup reclaim held.
**Root cause (immediate):** `code-server` main (pid 4047094) was back to **5.55GB RSS after only 2 days** — it is the very process restarted on 2026-09-07, so **the leak rate is ~2.7GB/day, not the 32-day accumulation `[SLICE-HOG-STRUCTURAL-CODESERVER]` implied**. That correction matters: the weekly recycle recommended in that entry would have been far too slow and would not have prevented this. Slice sat at 9.67GB against `MemoryHigh=9G` with page cache at **548KB**. Six live Claude extension sessions (~0.22–0.34GB each), a `react-router build`, and `attribute_claim_poller` at 2.0GB after 5 days (a separate, slower leak outside the slice) made up the rest.
**Root cause (underlying, strong candidate — NOT yet proven):** `files.watcherExclude` was **NOT SET** in code-server's User settings, so the watcher covered the entire **24GB** workspace with no exclusions — including `logs/` (rewritten every minute by ~20 cron jobs), `data/` 5.1G, `10_Market_Report/` 5.3G, `artifacts/` 4.7G. VS Code holds watcher path state in the main server process, which is precisely the process leaking, and `max_user_watches` had already been raised to 124,699 to accommodate it. High-churn + huge tree + no exclusions is the textbook shape for this leak. **Stated as a hypothesis, not a finding** — it is consistent with every observation but unproven until the growth curve is measured over the next few days.
**Fix:** `systemctl restart code-server` + `systemctl restart fields-attribute-claim-poller`. Then, as prevention, added `files.watcherExclude` to `/home/projects/.local/share/code-server/User/settings.json` covering `node_modules`, `__pycache__`, `.git/objects`, `venv`, and the eight large/high-churn project dirs. Backup written alongside (`settings.json.bak.<ts>`); all 12 pre-existing keys verified preserved and the result validated as JSON. **Deliberately did NOT set `search.exclude` or `files.exclude`** — those change what you can find and see, whereas `watcherExclude` only stops live-watching. No extra restart forced; it takes effect on the next window reload or service start.
**Result:** RAM used 13Gi → 3.7Gi, available 2.0Gi → **11Gi**; **swap 7.9Gi → 3.1Gi**; slice anon 9.29GB → 33MB with page cache recovering; load 17.2 → 3.31; D-state 11 → 0; `/healthz` `000`/25s → **200 in 3.5ms**; external workbench 200 in 0.25s.
**How to falsify the hypothesis:** watch code-server main RSS over the next 48h. Previous rate was ~2.7GB/day. If it stays flat or grows markedly slower, the watcher was the leak. If it still reaches ~5GB in 2 days, it is not, and the next suspects are the accumulating Claude extension sessions (6 concurrent, none reaped) and the extension host.
**Files:** `/home/projects/.local/share/code-server/User/settings.json` (code-server user config — not repo-tracked, so no GitHub push applies; backup alongside).
**Recurrence:** Nth for the slice-thrash class and **2nd for the structural-hog shape** ([SLICE-HOG-STRUCTURAL-CODESERVER] 09-07, [SLICE-COHORT-THRASH-BACKTEST] 08-26, [SLICE-THRASH-MONGO-LIST] 08-24, ugrep 08-01/06, [VM-WEDGE-PREVENTION] 07-27). Four incidents in 16 days, four different triggers, one identical end state: slice page cache driven to zero. This is the first entry to attack a *cause* rather than restart the symptom.

## [WALK-V2-SECOND-VERSION] Second walkthrough at ?walkthrough=2 (Robina V3 cut) — 20:10 AEST
**Symptom:** Wanted a second version of the Robina walkthrough (a new video + captions cut, "V3") live at `?walkthrough=2` alongside the existing `?walkthrough=1`.
**Root cause / non-obvious part:** V3 is not a re-timed V1 — it's a different narration: longer (734.9s vs 641.8s), reworded throughout, the asking chart tells a different story (June-2026 asking $1.6M vs median $1.49M then dropping to match — not the Dec-2023-flip/Sept-2026 story), and it adds a NEW rental-yields closing section. So it needs its own beat choreography, not a remap.
**Fix:** Versioned the engine by `?walkthrough=` value: `walkVersion()` (1/2), per-version `WALK_SRC()` (v1 vs new `robina_2026-08_v3_dom.mp4`), per-version captions (`WALK_SEGMENTS` / `WALK_SEGMENTS_V2`) via a version-aware `walkCues()`, and per-version beats (`walkBeats()` / new `walkBeatsV3()`) dispatched in `startWalk`. Authored `walkBeatsV3()` to the V3 narration — reusing DOM/median/withdrawn/lending helpers, re-authoring the asking section (`wkAskV3Recent`/`wkAskV3Zoom`: circle the asking recent peak $1.6M + median $1.49M, arrow down to match, then magnify), and adding a rental-yields STAGE note-box. Re-encoded the editor's 90MB V3 mp4 to ~22MB (512² h264, faststart).
**Bugs hit & fixed during build:** (1) VM crashed running ffmpeg re-encode + `npm run build` concurrently — memory spike; recovered, then ran heavy steps sequentially (encode alone at `nice -15`, then build). (2) `walkCues` threw `Cannot read '0' of undefined` on v2 because my caption patch missed the `WALK_SEGMENTS[i+1]` ref (v1 array shorter than the 122-seg v2) — it silently killed the whole beat loop (transport shows, video stuck at 0:00). (3) Double-transform: zooming the asking chart BEFORE drawing the recent circles put the ink through the `#wkInkWrap` camera transform twice → off-screen; reordered to draw circles first, then `wkCamera` (V1 pattern).
**Fix:** Verified on dev — both `?walkthrough=1` (unchanged) and `?walkthrough=2` render correctly (DOM, median, asking June-2026 zoom with $1.6M/$1.49M glued, withdrawn, lending, yields stage, close recap). `npm run build` passes.
**Files:** `01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts` + new `public/walkthrough/robina_2026-08_v3_dom.mp4` (→ Website 08d7d65 / f0f8f53); `10_Market_Report/Video_Production/Video_Editing_Process/ASSET_REPLACEMENT.md` (→ Fields_Orchestrator). Verify-shooter `_shotwalkR.cjs` (waits for `seeked` — needed for the longer clip).
**Recurrence:** First for a second/versioned walkthrough. The walkCues-version-ref and zoom-order (double-transform) traps are now documented in ASSET_REPLACEMENT.md; VM concurrent-heavy-jobs crash is the recurring resource class (see slice-hog entries).

## [WALK-V3-WITHDRAWN-ZOOM] V3 withdrawn: zoom into 2025/2026 bars, keep numbers visible — 21:05 AEST
**Symptom (follow-up to WALK-V2-SECOND-VERSION):** In the V3 withdrawn-chart segment Will says "let's zoom in on 2025" — the chart should actually zoom right into the 2025/2026 bars while he speaks, with the on-screen "23"/"53" numbers staying visible.
**Root cause:** `wkCamera` only centred the focus HORIZONTALLY (scale about the chart's vertical centre). The withdrawn 2025/2026 bars sit low on the chart (near the bottom axis), so a centre-anchored scale pushed them — and their number labels — below the clipped view.
**Fix:** Added optional `foVy, VBH` params to `wkCamera` to centre the focus vertically too. V3 withdrawn beats now draw the "23"/"53" circles first (normal scale), then `wkCamera(DS,1000,925,2.7,secs,352,420)` magnifies the 2025/2026 cluster (annotate-then-zoom keeps the ink glued and in-frame). Tagged the intro "homes that didn't sell" note so it fades before the zoom.
**Fix:** Verified on dev at t392 — chart zoomed to ~2022-2026, red circles on 2025 (23) and 2026 (53), both handwritten numbers visible and scaled with the chart.
**Files:** `01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts` (→ Website 3155b4f); `10_Market_Report/Video_Production/Video_Editing_Process/WALKTHROUGH_EXPERIENCE.md` §3.1.16 (→ Fields_Orchestrator).
**Recurrence:** First for vertical camera focus. Camera annotate-then-zoom + vertical-focus rules now in the doc.

## [WALKTHROUGH-V3-TONGUE-OPEN] Trim tongue-out opening frames from walkthrough v3 — 21:03 AEST
**Symptom:** On `/news/robina?walkthrough=2`, the first ~0.2s of the walkthrough video showed Will with his tongue out (frames 0–5).
**Root cause:** The v3 cut's EDL in-point on the first take started a few frames before Will composed himself; no pipeline master exists for v3 (only the shipped mp4), so the trim was applied to the delivered file directly.
**Fix:** Re-encoded `robina_2026-08_v3_dom.mp4` with `-ss 0.23333` (7 frames @ 30fps), matched original bitrate (141kbps, size unchanged at 22.6MB). Audio safe — speech starts at 0.63s. Verified new first 12 frames clean via frame tile. Pushed via contents API (commit 3467694), deploy logged.
**Files:** `01_Website/public/walkthrough/robina_2026-08_v3_dom.mp4`
**Recurrence:** First

## [WALKTHROUGH-V3-ASKING-RERANGE-NO-ARROWS] Walkthrough ink changes: no connector arrows, asking 3yr re-range, lending wipe+peak — 21:20 AEST
**Symptom:** Not a bug — Will's direction on `/news/robina?walkthrough=2`: (1) when he says "if we zoom in on what's happened most recently" on the asking-expectations chart, the chart should switch to the 3-year range; (2) the two-circles-plus-connecting-arrow ruling (WALKTHROUGH_EXPERIENCE.md §3.1.11/§3.1.14) is retired — no arrow or line between compared circle pairs; (3) on the lending chart's 3-year switch, all on-screen text must wipe, and the late-2024 peak he speaks to gets circled.
**Root cause:** n/a (directed change).
**Fix:** `MarketFlowProto.engine.ts`: deleted `wkArrowVB` helper + all 9 call sites (DOM June→Aug, median peak→latest ×2 cuts, asking Dec-2023→Sept-2026, withdrawn 23→53 ×3 cuts, lending 20%→10%); v3 asking "b" beat now circles the drop-to-match point instead of arrowing to it; added t:297 beat (v3) setting `askingState.range="3yr"` + pill blink; moved `clearWkInk()` onto the lending 3y-switch beat in all three cuts (v1 t:495, v3 t:525, Varsity t:516) and added `wkAcPeak()` circling the lending line's min-y peak right after (t:498/531/523). Doc rules 11/14 rewritten/retired.
**Files:** `01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts` (commit aa97072), `10_Market_Report/Video_Production/Video_Editing_Process/WALKTHROUGH_EXPERIENCE.md` (commit 6feac6e)
**Recurrence:** First

## [WALK-VARSITY-LAKES] Varsity Lakes walkthrough added (/news/varsity-lakes?walkthrough=1) — 21:50 AEST
**Symptom:** Walkthrough-with-Will only existed for Robina; needed for Varsity Lakes (next: Burleigh Waters).
**Root cause:** N/A — new feature. Engine was Robina-hardcoded (`WALK_SRC`/captions/beats keyed only on `?walkthrough=` version, not the `/news/:suburb` slug).
**Fix:** Made the walkthrough **suburb-aware**. New `walkSuburb()` (reads `state.suburb`, set by `mountFlow`) + `walkConf()` registry keyed by (suburb, version) → `{src, segs, beats, end, chapters}`. `walkFlagOn()`/`WALK_SRC()`/`walkCues()`/`wkSkip()`/`startWalk` now registry-driven; hero gated on the suburb having an asset (so Burleigh shows no broken hero). Added `WALK_SEGMENTS_VARSITY` (112 caption segs from the editor) and authored `walkBeatsVarsity()`. Varsity narration is the **same STRUCTURE as Robina V3** so DOM/withdrawn/lending/stage beats reuse the V3 helpers (they read live chart geometry + values → circled numbers adapt automatically: DOM ≈23→35, withdrawn 23→53 pooled, lending 20→10 pooled). Two sections got bespoke beats because Varsity's story differs: **median** (Varsity is FLAT since December, no $ figure spoken → a "flat since December" note + sideways arrow, no peak/latest $ circles) and **asking** (Varsity's gap is falling, then Will LAYERS IN ROBINA — `askingState.suburbs.Robina=true; askRefresh()` — as the exemplar that already came down to match its median; after layering, `wkAskLines()` returns Robina's line first since `askingSeries()` orders Robina→Varsity, so the endpoint circle lands on Robina). Re-encoded the editor's 86MB master to a 21.8MB web mp4 (512² h264+aac, faststart). Also fixed a latent bug: `wkConceptDir(note,dir,i)` was called WITHOUT a tag, so `wkFadeTag()` removed the note text but left the concept ARROW lingering (visible stray down-arrow beside the asking "matched median" circle) — now pass the note's tag so the arrow fades with it.
**Files:** `01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts`, `01_Website/public/walkthrough/varsity-lakes_2026-08_dom.mp4` (new); docs `WALKTHROUGH_EXPERIENCE.md`, `ASSET_REPLACEMENT.md`.
**Recurrence:** First (Varsity). Burleigh Waters will reuse this exact path — add a `walkConf()` entry + captions + (reuse walkBeatsVarsity or a Burleigh variant).

## [WALK-CAMERA-SVG-TRANSFORMBOX] On-page chart camera zoom has never actually rendered — 21:50 AEST
**Symptom:** The `wkCamera()` chart zoom (used by Robina V3's asking beat `wkAskV3Zoom` and the withdrawn "zoom in on 2025" beat, and intended for Varsity's withdrawn segment) sets a transform but produces **no visible zoom**. Confirmed on **production** (`fieldsestate.com.au/news/robina?walkthrough=2`), not just dev — so it never worked; the earlier "verified zoomed" note was mistaken (the transform is *measured* as scaled, which is easy to confirm and easy to misread as working).
**Root cause:** Chrome computes an `<svg>` ROOT element's `transform-box` as `view-box`. A CSS `transform` on it then **measures** as scaled (`getBoundingClientRect().width` = 2.7× real) but **paints in viewBox user-space** → nothing visibly zooms. Diagnosed via headless: transform string + rect both said scale(2.7) while the screenshot showed the chart at 1×. The ink wrap (`#wkInkWrap`, a `<div>`) transforms fine — divs never had the quirk — which is why the drawn circles/notes appear to move correctly while the chart underneath does not.
**Fix (attempted, then reverted):** Forcing `transform-box: border-box` + `transform-origin:0 0` with the focus offset baked into `translate` DOES paint on Chrome — but only flakily (needs a layout flush after a transform already exists; setting it fresh in one tick still failed) and it de-glued the chart from the ink in testing. A transient wrapper-`<div>` around the svg (the robust idea — divs paint transforms reliably) failed because the wrapper collapsed to the wrong size. Rather than ship a half-working/janky zoom, **reverted `wkCamera`/`wkCameraOff` to their original (no-op-in-practice) form** so Robina is byte-for-byte unchanged, and **removed the withdrawn camera beat from `walkBeatsVarsity`** — the emphatic 23/53 circles already carry the "zoom in on 2025" narration, and the chart shows the full 2006→2026 history the narrator describes. Left a ⚠ comment in `wkCamera` documenting the known no-op + the fix to pursue.
**The proper fix (future, deliberate task):** render each chart svg inside a dedicated wrapper `<div>` with an EXPLICIT width/height (= the svg's measured box) and `transform-origin:0 0`, and point `wkCamera` at that div (compute the same focus→centre mapping used here; the ink wrap already uses it). Divs paint transforms reliably. Needs careful testing across all four charts (DOM/median/asking/withdrawn) + mobile, hence not bundled into the Varsity task.
**Files:** `01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts` (`wkCamera` comment + Varsity beat).
**Recurrence:** First time root-caused. The zoom has silently not worked since it was added; no user impact beyond "the chart doesn't magnify."

## [WALK-BURLEIGH-WATERS] Burleigh Waters walkthrough added — 22:40 AEST
**Symptom:** Third suburb walkthrough needed (/news/burleigh-waters).
**Root cause:** N/A — new feature; engine already suburb-aware from [WALK-VARSITY-LAKES].
**Fix:** Added `WALK_SEGMENTS_BURLEIGH` (112 caption segs), a `walkConf()` entry for "Burleigh Waters", and `walkBeatsBurleigh()`. Same STRUCTURE as Varsity/Robina-V3 so DOM/withdrawn/lending/stage beats reuse the shared helpers (numbers auto-adapt: DOM ≈28→42, withdrawn 23→53 pooled, lending 20→10 pooled). Median is FLAT (~$1.89M Dec → ~$1.9M) → same "flat since December" treatment as Varsity. Asking DIFFERS from Varsity: Burleigh's OWN asking has already converged to its OWN median (widest gap Dec-2025 peak → falls to match by September), so it's the Robina-V3 "own convergence" story ("asking now meets median"), NOT Varsity's layer-in-Robina. Re-encoded 84.7MB master → 21.6MB web mp4. All 5 sections verified live-geometry via headless screenshots.
**Files:** `01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts`, `01_Website/public/walkthrough/burleigh-waters_2026-08_dom.mp4` (new).
**Recurrence:** First. All three target suburbs now have walkthroughs.

## [WALK-CAMERA-VIEWBOX-FIX] Chart camera zoom FIXED (was a silent no-op) — 22:40 AEST
**Symptom:** The on-page chart "camera" zoom (`wkCamera`, used by the withdrawn "zoom in on 2025" beat and Robina V3's asking zoom) never visibly rendered — see prior entry [WALK-CAMERA-SVG-TRANSFORMBOX] where it was diagnosed and left as a documented no-op.
**Root cause:** Chrome computes an `<svg>` ROOT's `transform-box` as `view-box`, so a CSS transform on it measures as scaled (getBoundingClientRect) but PAINTS in viewBox user-space → no visible zoom. Neither `border-box`+origin-0,0 nor a transient wrapper-div painted reliably.
**Fix:** Zoom by ANIMATING THE SVG's `viewBox` ATTRIBUTE (the SVG-native zoom — always paints), NOT a CSS transform. `wkCamera` now shrinks the viewBox to the focus region ([foVx-VBW/2s, foVy-VBH/2s, VBW/s, VBH/s]) and the ink (which lives in a separate screen-space `<div>` #wkInkWrap that paints CSS transforms fine) gets the MATCHING screen affine (scale sc about origin 0,0, translate C) so ink stays glued. Both are tweened together each rAF frame (easeOutCubic) via new `_wkCamAnim`; `wkCameraOff` tweens the viewBox back to the stored `data-vb0`. Instant path (secs=0, headless-replay) snaps in one frame. VERIFIED on all three: Varsity withdrawn dsVB `783.75 286.38 312.50 131.25`, Burleigh same, Robina V3 `739.81 274.22 370.37 155.56` — the chart genuinely zooms into 2025/2026 now, bars enlarged, circles glued. Robina V3's asking zoom also revived (same code path). Withdrawn beat tuned to `wkCamera(DS,1000,940,3.2,...)` (tight on 2025/2026, "23"/"53" in frame).
**Also fixed [WALK-BURLEIGH-ARROWVB-THROW]:** `walkBeatsBurleigh`'s t356 beat was copied from a stale template that called `wkArrowVB(...)` — a function REMOVED 2026-09-09 (two compared points now just get two circles, nothing between). The undefined call THREW inside the rAF beat loop, killing `walkTick` (transport froze at 0:00, withdrawn+lending never rendered). Only surfaced under playback pageerror capture ("wkArrowVB is not defined"), not the paused-seek path. Removed the call → beat matches the clean Varsity/V3 form `wkPointCircle(...,"53",i)`. ⚠ LESSON: when cloning a beat set, grep the new calls against `function <name>` — a deleted helper is a runtime throw that silently kills the whole beat loop downstream, and it may not appear on the jump-replay code path (only on monotonic playback).
**Files:** `01_Website/src/components/MarketFlowProto/MarketFlowProto.engine.ts` (`wkCamera`/`wkCameraOff`/`_wkCamAnim`, Varsity+Burleigh withdrawn beats).
**Recurrence:** First real fix of the camera (supersedes [WALK-CAMERA-SVG-TRANSFORMBOX]'s "known no-op").
