
---

## [SELLER-BOOK-INTEGRITY] Agent-premium analysis audit + replication — 13:30 AEST

**Symptom:** *Before You List* Chapter 6 contains a "1,475 sales / 9.6% gap" finding (Figure 12 + "Commission Conversation" worked example). Local analysis files could not be located; chart code in `scripts/generate_book_charts.py:405-413` was synthetic (`np.random.normal()` around hardcoded means with seed 42). Will already flagged the underlying claim as MEDIUM-risk in `09_Appraisals/Scarcity_Legality/02_claim_audit.md` rows B17/B18.

**Root cause (article):** The article's analysis is real and reproducible. Replication against the live Gold_Coast DB returned 1,464 sales / 444 agents with bucket means within rounding of the published 89.8% / 95.8% / 92.2% / 91.7% / 86.2%. The article's specific numbers are not fabricated.

**Root cause (Figure 13 in seller book):** Figure 13's commission-tier scheme (+0% / +3% / +5% / +7% premium by commission rate) has no analytical basis — it is a hand-constructed illustration labelled "Source: Fields Estate, 2026".

**Root cause (chapter framing):** The chapter labels its buckets as "per year" — they are actually lifetime sales in the dataset window (~4.5 years). And the article's 9.6 pp gap shrinks to ~4.1 pp (2025-only window) or ~4.5 pp (time-detrended) because Domain valuations drifted dramatically across the data window (2021 sales ~−34% vs Domain; 2026 ~+5%), and 20+ bucket sales skew older than other buckets.

**Fix:**
1. Created `/08_Seller-Book/agent_analysis/` directory housing the recovered analysis:
   - `agent_premium_analysis.py` — replicates article methodology, idempotent, runs against live DB
   - `robustness_checks.py` — temporal mix, 2025+ restriction, time-detrended, outlier check
   - `METHODOLOGY.md` — methodology + caveats + honest interpretation
   - `README.md` — folder index
   - CSV + JSON outputs for both scripts
2. `COMMISSION_CONVERSATION_REPLACEMENT.md` — drop-in 2-page rewrite of section currently on book pages 68-69. Removes fabricated Figure 13 commission-tier table. Keeps chapter's existing 9.6-pp claim (chapter not being rewritten this print run). Adds defensible academic citations (Levitt & Syverson 2008, Bernheim & Meer 2013). Three Figure 13 replacement options provided; designer chooses.
3. The chapter's existing claims (Figure 12, 1,475 sales) remain in the print version; corrected analysis is preserved for the next book update.

**Files:**
- `08_Seller-Book/agent_analysis/agent_premium_analysis.py` (new)
- `08_Seller-Book/agent_analysis/robustness_checks.py` (new)
- `08_Seller-Book/agent_analysis/METHODOLOGY.md` (new)
- `08_Seller-Book/agent_analysis/README.md` (new)
- `08_Seller-Book/agent_analysis/COMMISSION_CONVERSATION_REPLACEMENT.md` (new)
- Analysis output CSVs + JSON

**Recurrence:** First. The lesson is broader than this section: published Fields-specific numbers should always be linked to a reproducible analysis script saved alongside the published artefact. Will's existing claim audit (B17/B18, C5) anticipated this exact risk.

---

## [WATCHDOG] Step 12 (Enrich Property Timeline) — 05:07 AEST

**Failure class:** UNKNOWN
**Cause:** No recognizable error pattern — check logs manually
**Action taken:** ESCALATED to Claude (id=watchdog_12_1778699225)
**Result:** queued — awaiting repair-agent or Claude

---

## [MINI-SITE-PHASE-0] Address-only gate + stub-state mini-site shipped — 17:51 AEST

**Symptom:** The existing `/analyse-your-home` page was a multi-step form (address → delivery method → optional name + timelines) and PostHog data showed sellers abandoning at the contact form. No path existed from a submitted address to a personalised mini-site at `/your-home/<slug>`.

**Root cause:** The funnel was never wired. The mini-site at `/your-home/13-terrace-court-merrimac` was a hand-coded fixture; nothing fed `system_monitor.property_reports`. The lead-capture page was optimised for the old delivery-method flow, not the new mini-site model.

**Fix:** Phase 0 of the House Mini-Site engine (per `11_House_Mini_Site/Development-Plan.md`). Five files atomically pushed in a single commit (no partial-deploy window):

1. **`netlify/functions/analyse-your-home-submit.mjs`** (NEW) — accepts `{ address, suburb, suburb_key, source }`, derives slug, creates a stub `property_reports` doc (schema v2), returns `{ slug, redirect_url, device_token }`. Validates service area against the seven southern Gold Coast suburbs.
2. **`netlify/functions/property-report.mjs`** (NEW) — `GET /api/v1/property-report?slug=...&device_token=...` returns the full doc, increments visit counter if device_token matches.
3. **`AnalyseYourHomePage.tsx`** (REWRITE) — single-field flow. Removed: delivery options, email/phone fields, optional name + timelines, success state. Added: on-select submit + localStorage persistence of device_token + redirect to `/your-home/<slug>`.
4. **`AnalyseYourHomePage.module.css`** — appended `.submittingState`, `.submitErrorWrap`, `.submitError`, `.submitRetry` for the new states.
5. **`YourHomePage.tsx`** (EXTEND) — when slug doesn't match the Terrace Court fixture, fetches from `/api/v1/property-report`, clones the fixture as a visual template, swaps in seller's actual address everywhere. New stub-state banner says "We've received your address... the figures shown below are an example template."

Smoke-tested: POST → 200 with slug; GET → full stub doc; `/your-home/<slug>` rendered with seller's actual address in hero, banner, share moment. Console: clean. Network errors: 0. Test doc deleted.

**Files:**
- `01_Website/netlify/functions/analyse-your-home-submit.mjs` (new)
- `01_Website/netlify/functions/property-report.mjs` (new)
- `01_Website/src/pages/AnalyseYourHomePage/AnalyseYourHomePage.tsx` (rewrite)
- `01_Website/src/pages/AnalyseYourHomePage/AnalyseYourHomePage.module.css` (append)
- `01_Website/src/pages/YourHomePage/YourHomePage.tsx` (extend)
- Commit: `f6707670dc8fc328c5f429a0d103230523a072c0`

**Recurrence:** First. The existing multi-step delivery-method form is now retired — its lead-capture endpoints (`/api/analyse-lead`, `/api/analyse-lead-address`) are no longer called by the frontend but remain in the codebase as dead code for now (can be removed in a follow-up cleanup pass once the new flow is validated).

---

## [MINI-SITE-PHASE-1-SLICE-1] #process tab + FearSection pattern + 3 authored sections — 18:06 AEST

**Symptom:** v0.3 mini-site had five tabs (Your Home, Valuation, Market, Positioning, Next). None of them addressed the structural seller fears identified in the Halo Strategy work (agent trust, settlement, tax, costs, FSBO, etc.) — the very questions sellers search for at 11pm.

**Root cause:** The fears tab was a Phase 1 deliverable in `11_House_Mini_Site/Development-Plan.md` but had not been built. The book (`Before You List`, v4.6) already contains the content for most of the 10 Halo fears the V4 print appraisal does not cover; the work was authoring that content into the universal thesis/applied-to-your-home pattern and wiring the new tab.

**Fix:** Phase 1 Slice 1 of the House Mini-Site engine. Six files atomically pushed:

1. **`components/CitationStrip.tsx`** (NEW) — foot-of-section attribution; wraps the existing `yh-source` utility with structured `{sources[], lastReviewed}` props.
2. **`components/ChartImage.tsx`** (NEW) — lazy-loaded `<figure>` for the 17 book chart PNGs already served at `/book-images/charts/`. Typed `FearChartId` enumerates valid IDs.
3. **`components/FearSection.tsx`** (NEW) — the universal block. Two-column grid (auto-fit, min 320px so it stacks on mobile). Left pane is "The data" with optional chart; right pane is "For your home" with optional StatBlocks. CitationStrip at the foot. Optional internal CTA at the bottom of the applied pane.
4. **`tabs/ProcessTab.tsx`** (NEW) — container. Renders intro section, maps `fixture.processSections[]` to FearSections, closes with an editorial-standard footer card ("We do not tell sellers what to do"). Placeholder copy if processSections is empty/undefined.
5. **`data/homeFixture.ts`** (EXTEND) — added `ProcessSection`, `StatBlock`, `FearChartId` types. `ReportFixture` now has optional `processSections?: ProcessSection[]`. TERRACE_COURT_13 fixture authored with three sections from the book:
   - Agent Selection (Halo #1, 10.7% volume) → Ch6 + chart ch6-1-agent-volume
   - Property Prep / Pre-Sale ROI (Halo #8) → Ch5 + chart ch5-4-presale-roi
   - Cost of Sale (Halo #10) → Ch6 & Ch7 + chart ch6-2-commission-comparison
6. **`YourHomePage.tsx`** (EXTEND) — added "The Process" tab between Positioning and What Happens Next. Tab id `process`, hash `#process`.

Editorial audit: ran greps on the 1,258-word new copy against forbidden adjectives (stunning, nestled, boasting, rare opportunity, robust market, generous, immaculate, must-see, unique opportunity), advice patterns (you should/must/need to), and predictions (prices will, market will, going to rise/fall). One hit: headline "What is this actually going to cost you?" — reworded to "What does this actually cost you?" Every dollar figure framed as a range or a generic example, never a single property valuation in a headline. Citation strip on every section.

Smoke-tested: live at `/your-home/13-terrace-court-merrimac#process`. Screenshot confirms tab nav has the new entry, three FearSection blocks render in the universal pattern, stat blocks show ("5–8×", "<1.2×", "$50K–$100K"), citations strip the foot of each, editorial footer renders in dark card. Console: clean. Network errors: 0.

**Files:**
- `01_Website/src/pages/YourHomePage/components/CitationStrip.tsx` (new)
- `01_Website/src/pages/YourHomePage/components/ChartImage.tsx` (new)
- `01_Website/src/pages/YourHomePage/components/FearSection.tsx` (new)
- `01_Website/src/pages/YourHomePage/tabs/ProcessTab.tsx` (new)
- `01_Website/src/pages/YourHomePage/data/homeFixture.ts` (extend — +118 lines)
- `01_Website/src/pages/YourHomePage/YourHomePage.tsx` (extend)
- Commit: `f0106160f8b9ac3167f3ace7c3ae9cbfd0f9a48e`

**Recurrence:** First. The remaining seven fear sections (Settlement, Sell-first-vs-buy-first, Tax, Tenant, Contract Terms, FSBO, Finance Fall-Through), the interactive CostOfSaleCalculator, and the avatar inference module land in Slice 2.

---

## [MINI-SITE-PHASE-1-SLICE-2-CONTENT] Seven more authored fear sections — 19:36 AEST

**Symptom:** Slice 1 shipped 3 of the 10 fear sections planned for the `#process` tab. The remaining seven (Settlement, Sell-first-or-buy-first, Tax/CGT, Tenant in Place, Contract Terms, FSBO, Finance Fall-Through) were still placeholdered.

**Root cause:** Content-authoring work that was deferred from Slice 1 to keep the first push tight while the FearSection pattern was validated.

**Fix:** Single-file extension of `01_Website/src/pages/YourHomePage/data/homeFixture.ts`. The `processSections[]` array on TERRACE_COURT_13 now contains all ten sections, reordered into seller-journey order:

1. Agent Selection (existing, Halo #1 — 10.7%)
2. **Settlement** (NEW, Halo #2 — 10.2%) — Ch8 + chart ch8-1, QLD Property Law Act 2023
3. **Sell first or buy first** (NEW, Halo #4 — 5.4%) — Ch8 + Merrimac DOM
4. **Tax / CGT** (NEW, Halo #3 — 6.0%) — ATO 4-question framework + 6-year rule + indexed-price growth; explicit "not advice" disclaimer
5. **Tenant in place** (NEW, Halo #5 — 5.3%) — investor vs owner-occupier pools + QLD Tenancies Act
6. Property Prep (existing, Halo #8)
7. Cost of Sale (existing, Halo #10)
8. **Contract Terms** (NEW, Halo #9 — 3.0%) — 4 standard clauses + 3 common surprises
9. **FSBO honest comparison** (NEW, Halo #13 — 2.7%) — Hendel/Nevo, Bernheim/Meer, Levitt/Syverson + crossover-point arithmetic
10. **Finance fall-through** (NEW, Halo #15 — 2.5%) — Ch7 + chart ch7-1, first-10-days curve + benchmarks

Each new section follows the same thesis/applied/citation pattern Slice 1 established. Charts used where the book has them (Settlement→ch8-1, Finance fall-through→ch7-1). Sections without a chart use prose-only thesis panes.

Editorial audit: 4,172 words of new copy passed against forbidden adjectives (stunning/nestled/boasting/etc.), forbidden phrases (rare opportunity/must-see/etc.), advice patterns (you should/must/need to), prediction patterns (market will, prices will, will rise/fall/etc.), and single-figure prices in headlines. Zero violations. Tax section carries an explicit "not advice" disclaimer with a partner-accountant referral.

Each section's applied pane is anchored to 13 Terrace Court where the property-specific framing adds something. Where it does not (e.g. Sell first or buy first speaks more to the seller's onward-purchase suburb than the subject), the section pivots to suburb-level data and explicit "we would discuss in the consultation" language.

Smoke-tested via puppeteer screenshot at `/your-home/13-terrace-court-merrimac#process`. All 10 section headlines render correctly, the tab nav has the new entry, layout flows cleanly through ~3,000 vertical pixels of content. Console clean, zero network errors.

**Files:**
- `01_Website/src/pages/YourHomePage/data/homeFixture.ts` (+277 lines)
- Commit: `3724c85b5dc3e293ff11838678a86a4051eefabc`

**Recurrence:** First. Remaining Phase 1 items (interactive CostOfSaleCalculator, avatar inference module, editorial-audit automation) deferred. Phase 1 content is now complete — the mini-site addresses every Halo top-15 concern.

---

## [MINI-SITE-PHASE-2] Buyers tab + Positioning split — 19:48 AEST

**Symptom:** v0.3 had buyer-persona cards living inside the Positioning tab, mixing the "who pays the premium" question with the "how do we win them with the listing" question. Per Design.md §4.3-4.4, those should be separate tabs sourced from V4 spreads S02 (Buyers) and S05 (Positioning).

**Root cause:** Phase 2 of the dev plan had not been built. The persona data was authored well but the tab split wasn't implemented.

**Fix:** Four files atomically pushed.

1. **`tabs/BuyersTab.tsx`** (NEW) — maps V4 S02 to the web. Four-section flow: scarcity thesis (headline + stat blocks + 2-paragraph argument), three persona cards (rich layout — numbered eyebrow, brief, why-this-home list, where-we'd-find-them strip), catchment (three location buckets with share + reasoning), campaign-math callout (active-vs-passive pool reach calculation, dark card). Closes with a back-reference to the Positioning tab.
2. **`tabs/PositioningTab.tsx`** (REFACTOR) — removed the "Buyer in mind" personas section (~60 lines). Added a companion-tab card that links back to Buyers and surfaces the persona count without duplicating data. Strategic frame, vocabulary, trade-offs, photography, and sample paragraph all retained.
3. **`data/homeFixture.ts`** (EXTEND) — new `BuyersData` type (thesis + catchment + campaignMath, all with optional StatBlocks). New `buyers?: BuyersData` field on ReportFixture. TERRACE_COURT_13 fixture extended with 468 words of Buyers-tab content: 4-of-142 scarcity thesis, 3 catchment buckets (southern GC upgraders ~half / Sydney+Melbourne return ~quarter / school-catchment families remainder), 28/72 active-vs-passive reach math.
4. **`YourHomePage.tsx`** (EXTEND) — added `#buyers` tab between `#market` and `#positioning`. Tab order: Your Home | Valuation | The Market | The Buyers | Positioning | The Process | What Happens Next.

Editorial audit: 2,630 words across new fixture + components. Zero forbidden adjectives, zero advice, zero predictions. Catchment shares phrased as approximations ("around half", "around a quarter", "the remainder") rather than fabricated single percentages where the data is back-tested but imprecise. Citation strip on the catchment block names the buyer-origin analysis and the open-home register as sources.

Persona data still lives in `fixture.positioning.personas` — single source of truth. BuyersTab consumes it as the primary view; PositioningTab back-references via the companion card. Future schema refactor could move personas onto `buyers.personas` but the current arrangement is non-duplicative and stable.

**Files:**
- `01_Website/src/pages/YourHomePage/tabs/BuyersTab.tsx` (new)
- `01_Website/src/pages/YourHomePage/tabs/PositioningTab.tsx` (refactor)
- `01_Website/src/pages/YourHomePage/data/homeFixture.ts` (extend)
- `01_Website/src/pages/YourHomePage/YourHomePage.tsx` (nav)
- Commit: `3ae20a580f52831391c4904b7ed2a052315ad7bb`

**Recurrence:** First. Remaining Phase 2 items (interactive competitor map via Mapbox, family-share button with scroll trigger, OG image generation, positioning-prose Python pipeline) deferred to follow-up slices.

---

## [WATCHDOG] Step 114 (Backfill URL Slugs) — 21:08 AEST

**Failure class:** UNKNOWN
**Cause:** No recognizable error pattern — check logs manually
**Action taken:** ESCALATED to Claude (id=None)
**Result:** queued — awaiting repair-agent or Claude

---

## [WATCHDOG] Step 101 (Scrape For-Sale Properties (Target Market)) — 21:08 AEST

**Failure class:** UNKNOWN
**Cause:** No recognizable error pattern — check logs manually
**Action taken:** ESCALATED to Claude (id=None)
**Result:** queued — awaiting repair-agent or Claude

---

## [MINI-SITE-PHASE-3] Slot resolver pipeline live — 21:10 AEST

**Symptom:** Phase 0 wired the funnel (address → stub doc → mini-site URL). Phase 1 + Phase 2 added editorial content. But every new submission still rendered the Terrace Court fixture with just the address swapped in. The valuation, the comps, the bed/bath/area, the market state — all template defaults. The mini-site was a demo, not a real personalised report.

**Root cause:** Phase 3 of the dev plan (slot resolver pipeline) had not been built. Real data lived in Gold_Coast.<suburb>, precomputed_market_charts, and precomputed_indexed_prices, but nothing was reading those collections and writing the results back into the property_reports doc.

**Fix:** Backend pipeline + frontend overlay shipped in one coordinated drop.

**Backend** (`scripts/property_reports/`, pushed to Will954633/Fields_Orchestrator `a26b577`):
- `slot_resolver.py` — SlotResolver class. One method per slot: `property_facts` (bed/bath/car/land/internal/property_type/photos), `subject_latlng`, `valuation_model_range` (median of bedroom-matched comps), `market_state` (DOM + indexed growth + active count), `recent_comparable_sales` (top-N with bedroom soft-match scoring), `competition_count`. All queries filter `listing_status` per project rule. Missing fields return None → frontend hides the block.
- `build_property_report.py` — CLI orchestrator. Reads stub doc, runs resolver, $sets the slot fields, transitions state to `under_review`, $pushes a `data_resolved` activity item. Idempotent with `--force`. Wraps the Mongo write in `cosmos_retry(label=...)` for RU resilience.
- `poller.py` — long-running loop. Every 15s, finds stub docs older than 5s (avoids racing the Netlify submit), processes each. SIGTERM-aware for clean systemd shutdown.
- `fields-property-report-poller.service` — systemd unit, Type=simple, Restart=always, runs as `projects` user with `PYTHONPATH=/home/fields/Fields_Orchestrator`. Now `active (running)` and `enabled` on the orchestrator VM.

**Frontend** (`src/pages/YourHomePage/YourHomePage.tsx`, pushed to Will954633/Website_Version_Feb_2026 `decf6c2`):
- `PropertyReportDoc` type extended with property/valuation/market/slots field shapes that mirror the resolver's output.
- `stubFromDoc()` rewritten as template-plus-overlay: clones Terrace Court fixture, then overlays any populated DB fields. Overlaid: address line + suburb, inventory (bed/bath/car/land/internal/yearBuilt + recomputed internal-to-land ratio), hero photo + gallery photos, valuation working range (mostLikelyLow/High + recommendedListingLow/High + confidence note with comp count), suburb market state (active listings count, sold cohort size, YoY growth), share-moment headline anchored to real competitor count.

**Verified end-to-end** against a real production submission (26 Colonsay Crescent, Merrimac):
- Submitted via `/api/v1/analyse-your-home-submit` at 21:05:55 AEST.
- Poller picked up the stub at 21:06:02 (7 seconds), ran the resolver, transitioned state to `under_review`.
- Resulting doc carries: bed=4, bath=3, car=3, land=931m², internal=205m², property_type=House, 7 photos (1 hero + 6 gallery from Domain + Azure Blob), lat/lng, model_range $1,001,250-$1,223,750 from 20 bedroom-matched comps, market state (median DOM 21, YoY +21.6%, 15 active, 638 sold txns), 13 competitors in the bracket, best comp 32 Merridown Drive sold 2025-10-30 for $1,335,000, 6 ranked recent comps.
- Live mini-site at `/your-home/26-colonsay-crescent-merrimac` renders the real inventory ("Measured, not estimated: 4, 3, 3, 931 m², 205 m², 0.22") + the standard "valuation is under review" banner.

**Known follow-ups (Phase 3 Slice 2):**
- The fixture template's `statTiles` and `headlineFinding` blocks are not yet overlaid — they continue to show Terrace Court defaults (6 / 3, 658m², 221m²) alongside the new "Measured, not estimated" block. Need a sweep through YourHomeTab to either overlay or hide template-bound stats.
- Recent comparable sales rendered on the Valuation tab still use the fixture's hand-coded `valuation.comps` (with `condition`, `notes`, `adjustedToSubject`). The resolver's lighter `recent_comps` are stored at `slots.recent_comps` but not yet rendered. Either upgrade the resolver to produce richer comps or downgrade the Valuation tab to consume the lighter shape during under-review.
- Valuation Tab's comp adjustments + confidence percentages still template defaults until the consultant signs off (state → final). This is by design for now — the final state replaces these.

**Files:**
- Orchestrator: `scripts/property_reports/__init__.py`, `slot_resolver.py`, `build_property_report.py`, `poller.py`, `fields-property-report-poller.service` (commit `a26b57748ff1`)
- Website: `01_Website/src/pages/YourHomePage/YourHomePage.tsx` (commit `decf6c2b5f84`)

**Recurrence:** First. The slot resolver is the central piece of the personalisation pipeline. Phase 3 Slice 2 will sweep through YourHomeTab + ValuationTab to overlay statTiles + comps. Phase 4 wires Mac's review UI (the consultant pass that transitions under_review → final) + the print appraisal trigger.
