
## [EMAIL-RESEND-MIGRATION] CRM-linked emailing: Gmail→Resend transport + closed the click→on-site identity loop — 20:20 AEST
**Symptom:** Will wanted a CRM-linked emailing system tracking opens, clicks, and ALL subsequent on-site behaviour back to the individual contact (he'd been emailing contacts from personal Outlook — untracked). Question was whether to build a "mini HubSpot".
**Root cause / findings (3 parallel Explore agents mapped the existing stack):** ~70% already existed — `crm_contacts` identity graph, first-party open pixel + click redirect (`email-track.mjs` → `email_sends`/`email_events` → CRM bump), PostHog device↔contact join, per-contact viewer (`crm-contact.mjs`). Real gaps: (1) sending was raw Gmail API in 13 functions — no SPF/DKIM/DMARC, no List-Unsubscribe, no bounce handling, ~500/day cap, 7-day refresh-token fragility; (2) the click→on-site loop only closed for ONE email (AYH fulfilment) because it was the only sender appending `?lead=<token>` to an instrumented landing page; only 3 pages ran `phIdentifyLead`; (3) `email_sends`/`email_events` had no reader UI.
**Fix — Phase 0 (deliverability foundation):** stood up Resend. Added `fieldsestate.com.au` (region ap-northeast-1) via full-access key; wrote DKIM + `send.` SPF/MX + a `p=none` DMARC to Netlify DNS via API (DNS is Netlify/NS1-hosted); domain verified. Send-only key stored in VM `.env` + Netlify site env (`RESEND_API_KEY`/`RESEND_FROM`/`RESEND_REPLY_TO`). Existing root SPF (secureserver) + M365 mail untouched (Resend isolates on `send.` subdomain).
**Fix — Part A (transport swap, all 13 senders):** new shared `netlify/functions/mailer.mjs` (`sendMail({to,subject,html,from,replyTo,tracked,type,meta,unsubscribe})`, Resend transport, optional first-party instrument()+`email_sends` logging, List-Unsubscribe header). `send-tracked-email.mjs` + all 12 direct-Gmail functions migrated onto it (lead-facing sends `tracked:true`, internal Will-notifications `tracked:false`/`unsubscribe:false`). Zero `gmail.googleapis.com` references remain; 7-day Gmail-token pain retired. (12 fns migrated by 3 parallel agents against the mailer contract; system-monitor.mjs by hand; all `node --check` clean.)
**Fix — Part B (Phase 1 identity loop):** `email-track.mjs` click handler now resolves/mints the recipient's `crm_contacts.link_token` (uuid hex, same shape as buyer_link_gen; carried forward by crm_sync) and appends `?lead=<token>` to first-party redirect targets. `App.tsx` new global `useLeadIdentify` hook fires `phIdentifyLead` on ANY route carrying `?lead=`, so every tracked email link — to /property, /for-sale, anywhere — binds the ensuing session to the contact (was 3 pages only). Verified live end-to-end: click → token minted → redirect `…/for-sale?lead=<token>` → CRM `email_clicked`+score.
**Bonus:** fixed latent `engagement_score` double-count in `crm_lead_sync.backfill()` (was `$inc` full email points every run while `$set`-ing counts; now tracks `email_engagement.score_applied` and incs only the delta).
**Files:** WEBSITE (Will954633/Website_Version_Feb_2026, commit 6c2971cc): netlify/functions/{mailer,email-track,send-tracked-email,ayh-lead-fulfil,five-property-friday,launch-form,lead-signup,subscribe,price-alert-subscribe,contact-message,analyse-lead,offmarket-notify,property-report-book-review,property-report-message-send,system-monitor}.mjs, src/App.tsx. ORCH: scripts/crm_lead_sync.py. DNS: 4 records on Netlify zone fieldsestate.com.au.
**Recurrence:** First for [EMAIL-RESEND-MIGRATION]. Phase 2 per-contact timeline done (below); campaign dashboard panel still pending.

## [EMAIL-CRM-TIMELINE] Phase 2 — per-contact email timeline on the CRM contact page — 20:45 AEST
**Symptom:** `email_sends`/`email_events` had NO reader UI anywhere (an Explore agent confirmed a repo-wide grep found zero consumers) — the open/click data existed but was invisible except as tag chips.
**Fix:** Extended `netlify/functions/crm-contact.mjs` (the HMAC-signed per-contact viewer opened from the Priority tab). Handler now queries `email_sends` for the contact's address (exact + lowercased `to`, newest first, limit 200) and their `email_events`, and a new `renderEmail()` renders an **Email** section: one row per send (date, subject, type) with opened-at + every link clicked (short path + time), plus a summary header (`N sent · X opens · Y clicks`) and the honest "opens are directional, clicks reliable" caveat. Interleaves with the existing website-activity timeline on the same page. No new endpoint/auth/app — one server-rendered file. `node --check` clean.
**Files:** WEBSITE (commit 644e6276): netlify/functions/crm-contact.mjs. Follow-up fix (9760d586): `email_sends.to` keeps original sender case but `crm_contacts.email` is lowercased — the exact/$in match returned 0 sends; switched to a case-insensitive anchored regex. Caught by live verification (showed "No emails sent" for a contact with 4).

## [EMAIL-PHASE3] Campaign dashboard + single-contact tracked send from the CRM — 21:05 AEST
**Symptom:** Phase 3 of the CRM-linked emailing build: (1) `email_sends`/`email_events` had no aggregate reader; (2) Will wanted to nurture leads through the tracked system instead of personal Outlook.
**Fix — dashboard:** `handleEmailCampaigns()` in system-monitor.mjs (GET `/api/monitor/email-campaigns`) aggregates email_sends by `type` joined to email_events (a send counts opened/clicked once), returns per-campaign + total open/click RATES. New `EmailCampaignsPanel.tsx` on the Ops → Marketing tab (colour-grades click rate; labels opens as directional). Verified live: 31 sent, 74% open, 16% click; fpf_shortlist 24/71%/12.5%.
**Fix — single-contact send (Will's choice: on the CRM page, contact-only):** new `crm-contact-send.mjs` (POST `/api/v1/crm-contact-send {id,k,subject,body}`) re-verifies the SAME HMAC key that gates the viewer and sends ONLY to the email on the contact identified by `id` — recipient is never taken from the request, so even a forwarded signed link can only email that one lead (blast radius = 1). Sends tracked via mailer, appends to `communications[]` (shows in history + email timeline), sets `last_contact_at`, refuses `do_not_email`. Compose box added to crm-contact.mjs (shown only when the contact has an email), vanilla inline JS reads id+k from the page URL, confirm() before send. Verified live: bad key → 404, valid → ok + recorded (type `crm_manual`, provider resend).
**Files:** WEBSITE: netlify/functions/{system-monitor,crm-contact,crm-contact-send}.mjs, src/pages/OpsPage/OpsPage.tsx, src/pages/OpsPage/panels/EmailCampaignsPanel.tsx (commits e340d644-area + 47b7d210).
**Recurrence:** First. Segment/bulk send deliberately deferred (needs throttling, dedupe vs do_not_email, per-send unsubscribe, confirm) — single-contact only for now.

## [EMAIL-COMPOSE-RICHTEXT] Rich-text compose — hyperlink words (like Outlook) — 22:00 AEST
**Symptom:** Will writes emails in Outlook where a word like "here"/"this link" is a hyperlink. The first compose box was a plain textarea that only auto-linked bare URLs, so "here" couldn't be a link (and only visible URLs got click-tracked).
**Fix:** Upgraded the compose box in crm-contact.mjs to a `contenteditable` rich editor with a toolbar (Bold, Italic, **Link**, Unlink) — select text → Link → paste URL → `execCommand('createLink')` wraps it in `<a href>`. Sends `innerHTML`. crm-contact-send.mjs now accepts rich HTML: added `sanitizeHtml()` (allowlist tags a/b/i/u/br/p/ul/ol/li/span/div/h3/h4; `<a>` keeps only http(s)|mailto href; strips all other attrs, on* handlers, javascript:/data: URIs, `<script>`/`<style>` blocks) + `buildHtml()` (rich HTML sanitized; plain text still escaped+autolinked). communications[] stores a tag-stripped plain-text version (history view escapes as text). mailer.instrument() then rewrites the sanitized `<a href>` links for click-tracking. **Verified live:** a body with "here" and "this link" as hyperlinks → BOTH captured as tracked links (`email_sends.links`); injected `<script>` stripped from the email. Sanitizer unit-tested (js-href, onclick, script, img/onerror all neutralised).
**Files:** WEBSITE: netlify/functions/{crm-contact,crm-contact-send}.mjs.
**Recurrence:** First.

## [CRM-SMS-CHANNEL] SMS as a second CRM comms channel (JustCall) — send + track on the timeline — 21:35 AEST
**Symptom:** Will wanted SMS send+track in the CRM mirroring email, all events on the contact timeline, via JustCall.
**Findings:** JustCall layer already existed — `justcall-sms.mjs` (inbound claim webhook + reusable `sendSms()`/`jcAuth()`), `justcall-call-webhook.mjs`, creds set (`JUSTCALL_API_KEY/SECRET/SMS_NUMBER/WEBHOOK_SECRET`), send API `POST api.justcall.io/v2.1/texts/new` (auth = `key:secret` raw colon-separated), `lead_sms_log`/`sms_claims` collections. So extended, not rebuilt.
**Legal gate (Spam Act 2003):** cold SMS is unlawful; SMS lawful only with consent. Will confirmed EVERY CRM number was self-submitted via FB lead forms = inferred consent. Gate = has phone AND source not appended/purchased (`id4me/pricefinder/appended/purchased/skip_trace`) AND not `do_not_sms`. Every message gets sender ID + "Reply STOP"; STOP auto-sets `do_not_sms`.
**Fix — send:** new `crm-contact-sms.mjs` (POST `/api/v1/crm-contact-sms {id,k,body}`), HMAC-gated like the email send, recipient ALWAYS the contact's own phone (E.164-normalised), consent gate above, sends via JustCall, logs to new `sms_messages` collection + `communications[]`, sets `last_contact_at`.
**Fix — track:** extended `justcall-sms.mjs` — non-incoming status callbacks update `sms_messages` delivery status by justcall_id (best-effort); incoming non-claim messages are matched to a crm_contact by phone variants and logged as an inbound reply to `sms_messages` + `communications[]` (+ Telegram), with STOP → `do_not_sms` + confirmation SMS. Claim flow unchanged.
**Fix — display:** `crm-contact.mjs` renders an **SMS** section (two-way history newest-first, out/in + status) + a consent-gated compose box (enabled for consented, disabled with reason otherwise), beside the Email section.
**Verified live:** bad key→404; opted-out send→409; simulated inbound reply + STOP logged to timeline and set do_not_sms; SMS section + enabled compose render for a consented (fb_lead_ad) contact. Real outbound not fired in test (would text/charge) — send path proven to the JustCall call, same function as the live claim flow; Will does the first real send.
**Files:** WEBSITE: netlify/functions/{crm-contact-sms,justcall-sms,crm-contact}.mjs.
**Recurrence:** First.

## [LEADS-MISSING-FROM-PRIORITY] Messenger + scattered-source leads were invisible to the Priority tab — 19:10 AEST
**Symptom:** Will: Facebook Messenger leads (and possibly others) are missing from the Priority tab of the Live Leads Tracker; every lead with an email/phone should be on it.
**Root cause:** Three gaps. (1) People who message the Page directly were captured in NO collection — the Messenger webhook (`messenger-webhook.mjs`) only logs `m.me/?ref=` ad-click referrals, and its target `system_monitor.messenger_leads` had never even been created; 17 real inbound conversations (Wayne Ineson, David Armstrong, Rochelle Collins, Klynt Parklands "looking for a 4-bed", …) lived only in the Meta inbox. (2) 8 contactable leads sat in small source collections (`campaign_leads`, `lead_signups`, `subscribers`, `five_property_friday_subscribers`) that were never funnelled into `crm_contacts`. (3) The Priority tab query only rendered `crm_contacts.follow_up_at` within 14 days (28 rows) — a deliberate "who to ring today" gate — so every reachable lead without a hand-set follow-up was invisible even when it was in the CRM.
**Fix:** (a) New `scripts/messenger_leads_sync.py` — pulls Page conversations via Graph API (Page token derived from `FACEBOOK_ADS_TOKEN`), upserts inbound threads into `crm_contacts` keyed by `messenger_psid`, merging onto an existing same-name form-lead contact when unambiguous (5 merged, 12 standalone) so nobody double-lists; stores a one-click Page-inbox thread link. (b) New `scripts/source_leads_sync.py` — sweeps the seven source collections and upserts any contactable lead (email-else-phone) not already in the CRM (test rows guarded). (c) `priority_calls_to_sheet.py` reworked into two sections (Will's choice, 2026-09-04): follow-ups due at top (unchanged), then a "New — reachable leads, no follow-up set" section listing every reachable `crm_contacts` (email OR phone OR messenger_psid) not dismissed. Ticking Done now also stamps `priority_dismissed_at` so a new-section lead drops off. Messenger rows render a `MESSENGER ↗` hyperlink in the How column. (d) Both syncs added to `nightly_lead_chain.py` after `crm_sync`; `priority_calls_to_sheet` now `needs` them. Both self-monitored (`job_run`, cadence 24h, Rule-7b zero-output assertions). Live tab now 77 rows (28 follow-ups + 48 reachable), 17 Messenger thread links; 3 heartbeats OK on the Process Registry.
**Files:** scripts/messenger_leads_sync.py (new), scripts/source_leads_sync.py (new), scripts/priority_calls_to_sheet.py, scripts/nightly_lead_chain.py.
**Recurrence:** First for [LEADS-MISSING-FROM-PRIORITY]. Related lineage: [OFFMARKET-WHATSAPP-MESSENGER] (webhook, 2026-07-31), [PRIORITY-CALLS-TAB] (tab origin, 2026-08-20), [LEAD-WORKLIST-MISSING-OFFMARKET] (a prior "a whole lead channel was invisible" incident).

## [PRIORITY-NOTE-CONTACTLESS-LOSS] "My notes" (col H) could be silently wiped for contactless rows — 17:30 AEST
**Symptom:** Will asked whether notes typed into column H of the Live Leads Tracker Priority tab would survive (earlier notes had "been deleted"). Earlier deletion was pre-2026-09-01 (the note-preservation feature `harvest_notes` did not exist yet; the tab is rebuilt nightly). Remaining latent gap: `harvest_notes`/`harvest_done` matched each sheet row to `crm_contacts` by email-else-phone only.
**Root cause:** A row with NO email and NO phone (AYH / off-market leads capture no contact details by design) could not be matched, so `contact_filter` returned None → note skipped → wiped on the nightly clear+rebuild. Email-only matching was also ambiguous if two contacts shared an email. Currently 0 contactless contacts have `follow_up_at`, so it was latent, not active loss.
**Fix:** Added the stable contact `_id` as the primary match key. It is already embedded in the CRM column's `=HYPERLINK(".../crm-contact?id=<ObjectId>&k=...")` formula; reading the grid with `valueRenderOption="FORMULA"` (was FORMATTED_VALUE, which shows only "Open ↗") exposes it. New shared `contact_filter()` prefers `{_id}` → email → phone; `_col_index()`/`_CRM_ID_RE` helpers. Both `harvest_done` and `harvest_notes` now use it. Falls back cleanly to email/phone when `REPORT_LINK_SECRET` is unset (CRM cell blank). Verified live: all 28 current rows now match via `_id`, 0 unmatched; full job run clean (0 spurious note writes, 28 rows).
**Files:** scripts/priority_calls_to_sheet.py
**Recurrence:** First

## [LEADFORM-PH-IDENTIFY-SKIP] Matched leads showed only email in PostHog, not name/phone — 16:45 AEST
**Symptom:** New buyer lead Kathy Davis (jpjonny@bigpond.com) was timestamp-matched to her anonymous PostHog session (distinct_id 01a069c8..., +13s), but searching "Kathy Davis" in PostHog returned nothing — only her email was linked to the person; name/phone were absent.
**Root cause:** Two writes raced. A Netlify function (geoip Ashburn VA = AWS us-east-1) identified the landing at 00:20:47 with email + is_lead + lead_source=five_property_friday but NO name/phone. Then leadform_ph_match.py ran at 00:21:09, saw the person already carried the same email, set `already_ours=True`, and its `ok = True if already_ours else _identify(...)` line SKIPPED _identify entirely — so name/phone were never sent. `posthog_match.identified` was recorded `true` regardless, masking the gap. `already_ours` was conflating "not a conflicting identity" with "nothing to enrich".
**Fix:** (1) match_lead now ALWAYS calls _identify (removed the already_ours skip) — enrichment is idempotent and must run even when the person already carries our email. (2) _identify split its payload: identity fields (email/name/phone/is_lead) stay in `$set` (must overwrite a thinner prior identify); provenance (lead_source, lead_match_method, first_lead_link_at) moved to `$set_once` (first-writer-wins) so we never clobber a richer or higher-certainty source — e.g. downgrading a token-certain identity to timestamp_inferred, or overwriting a specific lead_source with generic fb_lead_ad. Backfilled both existing matched leads (Kathy, Nikolas) — both now show name+phone in PostHog.
**Files:** scripts/leadform_ph_match.py
**Recurrence:** First

## [ROBINA-ARTICLE-DOM-RESYNC] Robina Aug update article DOM figures contradicted the rebuilt sell-now chart — 16:55 AEST
**Symptom:** `/articles/robina-market-update-august-2026` led with "homes taking longer to sell — median 34 days (June qtr), up from 24.5 a year ago, a third longer; quick-sale share 74.1%→45.7%" and embedded a static PNG drawing that climb. The rebuilt source-of-truth DOM chart (`Gold_Coast.precomputed_market_charts._id=robina_days_on_market`, houses-only) shows completed 2026-Q2 median = **25 days** (n=38) vs 2025-Q2 **21** (+4 YoY, not +9.5), quick-sale (<30d) 57.9% vs 70.2%, trailing-12m median 24 (n=211), typical 21. Article's 34/24.5/74→46 figures were stale/from a superseded source.
**Root cause:** Article written 2026-08-21 off an earlier DOM computation; the DOM precompute was later rebuilt (accurate chart) but the published article + its baked `robina_dom.png` were never resynced. Same class as `[OWNER-ARTICLE-DOM-HARDCODED-INVERTED]` — narrative diverges from its own chart because the article stores numbers, not a live query.
**Fix:** Rewrote the six DOM-bearing passages to the chart's figures, keeping the "lengthening" thesis (Will's call) but re-anchored on the genuine recent signal — trailing-3m median 50 (n=31) and the **incomplete** 2026-Q3 (62, n=22, dotted) — with explicit small-sample caveats; corrected quick-sale to 58%←70% and >90d share to "under 3%". Regenerated `robina_dom.png`: `chart_robina_dom` now draws the in-progress quarter dotted/hollow + a "typical 21 days" reference line so a 22-sale quarter can't read as a settled jump. Cache-busted img URL (`?v=20260904`; blob served `immutable`). Updated `content_articles` doc, triggered Netlify article rebuild.
**Files:** system_monitor.content_articles (6a87940e45c90537ba7917dd); scripts/market_article_charts.py (pushed 5b2e1c9)
**Recurrence:** 2nd (see [OWNER-ARTICLE-DOM-HARDCODED-INVERTED]) — published-article DOM figures drifting from the rebuilt chart. Standing gap: no automated check that article-embedded metrics still match their precompute source.
**Also flagged (not fixed here):** the `/market-intelligence/Robina/sell-now` page renders THREE contradictory DOM headlines — a "62" hero (the incomplete 2026-Q3 leaking into a headline), a "23 days" seller-conditions block, and the chart's ~24/25. The 62 hero is a page bug: an in-progress quarter shown as the current median.

## [SELLNOW-DOM-INCOMPLETE-QUARTER-HERO] Sell-now page showed 3 contradictory DOM numbers; hero was the incomplete quarter — 17:10 AEST
**Symptom:** `/market-intelligence/{suburb}/sell-now` rendered three different days-on-market figures: a hero "MEDIAN DAYS ON MARKET 62" labelled "(Q2 2026)", a seller-conditions block "23 days", and the chart's ~24/25. For Robina the 62 was flatly wrong.
**Root cause:** Two independent consumers took `timeline[timeline.length - 1]` as "current" DOM — but the last timeline entry is the **in-progress quarter** (`incomplete: true`; Robina 2026-Q3 = 62 on 22 sales). (1) `db.server.ts` getMarketMetricsSummary set the hero `daysOnMarket` from the timeline tail, and labelled it with the completed **price** quarter → "62 days (Q2 2026)". (2) `generate_market_pulse.py` set `data_snapshot.dom_median` from `timeline[-1]` too (the exact trap already fixed for sales volume 20 lines below, but DOM was missed) — the seller block's "23" was a *stale* run of that (22.5→round). Three sources, three numbers.
**Fix:** Headline is now the **trailing-12-month median** (the chart's own stable headline; Will's call) everywhere. `db.server.ts`: prefer `trailing_12m.median_days_on_market` → `latest_quarter_median` → last COMPLETE quarter, never the incomplete tail; added `daysOnMarketBasis` ("12 months to Aug 2026") so the hero/FAQ no longer inherit the price quarter label. `MarketMetricsPage.tsx`: hero label "(12 months)", DOM FAQ uses its own basis, source line notes DOM is a 12-month rolling median. `generate_market_pulse.py`: DOM block now reads `trailing_12m` (fallback last complete quarter), yoy from complete quarters only. Patched live `market_pulse` sell-now docs so block==hero==trailing-12m now (Robina 24, Burleigh 28 already, Varsity 22→19); corrected Robina summary's stale DOM clause (22.5/21.5 → 25/21, Q2 basis).
**Files:** src/lib/db.server.ts (13a54d2); src/pages/MarketMetricsPage/MarketMetricsPage.tsx (ac0e441); scripts/generate_market_pulse.py (7812936); system_monitor.market_pulse robina/varsity_lakes sell-now docs
**Recurrence:** Nth for the `timeline[-1]` incomplete-quarter class (see volume fix in generate_market_pulse.py, and 2026-09-04 [ROBINA-ARTICLE-DOM-RESYNC]). Pattern: any consumer reading the last timeline entry as "current" inherits the in-progress quarter. Standing gap: no lint/helper enforcing "current = last complete, headline = trailing-12m" across consumers.

## [AYH-HOME-SIGNAL-NOT-CAPTURED] "Analyse Your Home" typed address never reached the CRM — 17:10 AEST
**Symptom:** Rochelle Collins typed "43 Roundelay Drive, Varsity Lakes, QLD 4227" into Analyse Your Home on 2026-09-04 06:19 and read its /off-market page — an unambiguous own-home signal — yet the CRM had no home address for her (no offmarket_home/probable_address, PostHog person home_address=null). Will asked why we hadn't captured it.
**Root cause:** The typed address existed only as a PostHog `analyse_home_address_submit` event. Nothing bound it to the CRM: (1) the home-inference job offmarket_home_signal.py only captures google→/off-market lookups (referrer filter), and she arrived via the internal AYH flow; (2) the real-time endpoint analyse-lead-address.mjs matches contacts by `{posthog_ids: <did>}` only — an identified lead's device id lives in `lead_web.distinct_ids`, so it misses the real contact (and the /for-sale-v3 AYH flow doesn't call it at all). Same class as leadform_ph_match/lead-link-visit: never key identity off posthog_ids alone.
**Fix:** (A) Stamped `ayh_home` (address/slug/source/confidence:high) on Rochelle's real email-keyed contact + tagged her PostHog person home_address (high). (B) Added scripts/ayh_home_signal.py — nightly reconciliation job that reads PostHog `analyse_home_address_submit` events (90d) and binds each to the correct contact by ALL id sources (lead_web.posthog_distinct_id, lead_web.distinct_ids, posthog_ids, primary_posthog_id) then person email; writes ayh_home + sticky property_address + tags the person (high confidence, only when slug changed). Rule 7b assertion (events>0 but 0 binds => raise). Added `ayh_home` to crm_sync.py carry-forward allow-list. Wired into the 23:40 brain2 nightly chain after offmarket_home_signal.py; self-registers on Systems Health via job_run(cadence_hours=24). First run bound 19 previously-invisible owner home signals (0 orphans).
**Files:** scripts/ayh_home_signal.py (new), scripts/crm_sync.py, crontab
**Recurrence:** First
