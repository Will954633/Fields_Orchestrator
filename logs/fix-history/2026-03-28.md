## [DIRECTIONAL-VAL] Valuations above $2.5M now flagged as directional_only — 16:30 AEST
**Symptom:** Properties listed at $2.5M+ had `directional_only = True` set in precompute_valuations.py (line 2624) but the flag was never wired into the final output dict or the website. The website would either show a full valuation (incorrect) or show "Not Available" (too restrictive).
**Root cause:** The `directional_only` flag was set but never applied to the confidence/summary dicts in the return value. The website had no handling for the directional case.
**Fix:** (1) Python: already had directional_only post-processing at lines 3139-3147 — confirmed working. (2) TypeScript types: added `directional_only`, `directional_reason` to ConfidenceResult/ValuationSummary/ValuationMetadata, added `'directional'` to ConfidenceLevel. (3) HowToValuePage: directional properties bypass exclusion early-return, render with comps/range but no reconciled valuation and a directional disclaimer. (4) VerdictBanner: accepts `directionalOnly` prop, shows "Directional Range" heading, grey tag, disclaimer note, suppresses single-figure estimate.
**Files:** `src/types/valuation.ts`, `src/pages/HowToValuePage/HowToValuePage.tsx`, `src/components/VerdictBanner/VerdictBanner.tsx` (website); `07_Valuation_Comps/precompute_valuations.py` (already done, confirmed)
**Recurrence:** First

## [SUBURB-NORM] Market narrative API fails for suburbs with spaces — 15:30 AEST
**Symptom:** "Capital Gain Comparison" chart shows "Failed to fetch indexed price race data" error on Houses vs Units tab. Only affects suburbs with spaces in names (Varsity Lakes, Burleigh Waters).
**Root cause:** `market-narrative.mjs` line 41 normalised suburb with `.replace(/-/g, '_')` — only converted hyphens to underscores but not spaces. When AnimatedIndexedPriceChart passes "Varsity Lakes" (URL-encoded as `Varsity%20Lakes`), it became `varsity lakes` (with space) instead of `varsity_lakes`, so the DB collection lookup returned 0 documents → 404.
**Fix:** Changed to `decodeURIComponent(parts[...]).toLowerCase().replace(/[-\s]+/g, '_')` — handles both hyphens and spaces. Also reordered HousesVsUnitsSection to place Capital Gain Comparison chart first.
**Files:** `netlify/functions/market-narrative.mjs`, `src/components/HousesVsUnitsSection/HousesVsUnitsSection.tsx`
**Recurrence:** First

## [CHART-REWRITE] PropertyTypeRaceChart animated bubble → static line chart — 16:00 AEST
**Symptom:** "House Type vs Growth Rate" motion chart (animated bubble scatter) displayed incorrectly — all bubbles clustered at baseline, used hardcoded static data not from real transactions.
**Root cause:** Animated Framer Motion bubble chart used fake hardcoded quarterly prices from `propertyTypePriceData.ts`. Data was generic, not derived from actual sold transactions.
**Fix:** 1) Added `property-type-prices` chart type to `market-narrative.mjs` — queries real sold transactions, groups by House vs Unit and quarter, computes medians. 2) Rewrote `PropertyTypeRaceChart.tsx` as a Recharts LineChart with House (blue) and Unit (green) lines from real DB data.
**Files:** `netlify/functions/market-narrative.mjs`, `src/components/PropertyTypeRaceChart/PropertyTypeRaceChart.tsx`
**Recurrence:** First

## [SSR-LOADING] Eliminate Loading placeholders from market-metrics SSR — 23:10 AEST
**Symptom:** Google crawlers see 17 "Loading... data" placeholders in /market-metrics SSR HTML because chart components fetch data client-side.
**Root cause:** All chart components used client-side useEffect fetches with generic "Loading..." text as placeholder. SSR HTML contained no real data within chart containers.
**Fix:** Expanded db.server.ts getMarketMetricsSummary() to return full history arrays from precomputed collections. Created SsrDataContext to deliver SSR data to charts via React Context. 5 charts (MedianPrice, PriceGrowth, PriceGrowthQoQ, ActiveListings, DaysOnMarket) now initialise state from SSR data and skip client fetch. 12 remaining charts show descriptive text with real metric values. 4 CrashRisk charts show descriptive chart names.
**Files:** src/lib/db.server.ts, src/config/marketMetrics.ts, src/pages/MarketMetricsPage/MarketMetricsPage.tsx, src/components/MetricCharts/SsrDataContext.tsx (new), 13 MetricCharts/*.tsx, 4 CrashRiskCharts/*.tsx
**Recurrence:** First

## [DIRECTION-DYNAMIC] Market Direction page overhaul — dynamic content + forecast model showcase — 14:09 AEST
**Symptom:** Three issues: (1) CSV/JSON download buttons in Market Signals chart area looked unfinished. (2) "Rising" badge at top contradicted hardcoded "Flat"/"Plateau" text throughout the page. (3) Price forecast chart didn't render for any suburb (no data files), and the leading indicators research article was not referenced.
**Root cause:** DirectionSection had all commentary hardcoded as "plateauing/flat" regardless of actual market data. Forecast JSON files were never generated. MarketSignals component included download buttons that weren't ready for public use. The market-pulse API excluded `data_snapshot` from responses, preventing dynamic content rendering.
**Fix:** 
- Removed CSV/JSON download buttons from MarketSignals component
- Rewrote DirectionSection to derive ALL content dynamically from pulse data (MetricDashboard, VerdictClose, DecisionBlock, SectionBlock statuses, FAQ answers)
- Added ResearchCallout component linking to article 69b11e74852bb69028ccd914
- Added LeadingIndicatorStrip showing wage growth (r=0.940), consumer spending (r=0.914), lending (r=0.948), RBA rate (r=0.791) with live signal status
- Added leading/lagging indicator metadata (correlation, timing, type) to each MarketSignals card, sorted by forecasting rank
- Generated forecast JSON files for all 3 target suburbs (Robina, Varsity Lakes, Burleigh Waters) using weighted linear trend with dampening
- Fixed forecastData.ts to normalize suburb names with spaces
- Updated market-pulse.mjs to include data_snapshot for direction category
- Extended PulseData interface with PulseDataSnapshot type
- Verified and marked pulse verdicts as manually reviewed: Robina=rising, Varsity Lakes=plateauing, Burleigh Waters=plateauing
**Files:** DirectionSection.tsx, MarketSignals.tsx, CategorySummary.tsx, forecastData.ts, market-pulse.mjs, forecast_robina.json, forecast_varsity_lakes.json, forecast_burleigh_waters.json
**Recurrence:** First

## [OPS-API-DISPLAY-001] API health showing 0/4 buyer-facing due to query limit — 19:33 AEST
**Symptom:** OPS_STATUS.md showed 0/4 buyer-facing API endpoints healthy, all marked "stale" with "Last Checked: unknown", despite all 11 endpoints being healthy and checked every 30 minutes.
**Root cause:** `fetch_api_health()` in `refresh-ops-context.py` had `.limit(200)` which was insufficient — with 23 endpoints checked every 30 minutes, the 200-record window only captured the most recent ~4 checks (all internal endpoints). Buyer-facing endpoints were outside the window. Additionally, the Python-side sort happened after the limit, so Cosmos returned arbitrary 200 records.
**Fix:** Increased limit to 500 and added server-side `.sort("checked_at", -1)` to ensure latest records are fetched first. Also fixed the stale-cutoff comparison to normalize naive/aware datetimes consistently.
**Files:** `scripts/refresh-ops-context.py`
**Recurrence:** First

## [STEP116-EXIT-001] Step 116 exiting 1 on violations_found, making pipeline red — 19:35 AEST
**Symptom:** Daily pipeline showed as "failed" because step 116 (Data Quality Validator) found 8 `implausible_land_size` issues and exited 1. This also cascaded to skip step 107 (Database Audit). The pipeline was red not because anything was broken, but because the validator was working correctly.
**Root cause:** `main()` in `data_quality_validator.py` returned `1 if errors and not all(e.get('auto_fixed') for e in errors) else 0` — treating "found unfixed violations" the same as "crashed". No distinction between validator findings and execution failures.
**Fix:** Changed to always return 0 from `main()` (both `passed` and `violations_found` are normal operation). Added `try/except` wrapper in `__main__` block that prints `OUTCOME: execution_failed` and exits 1 only on uncaught exceptions. Added structured `OUTCOME:` line to stdout for orchestrator/OPS consumption.
**Files:** `scripts/data_quality_validator.py`
**Recurrence:** First (design issue from initial implementation)

## [DECISION-FEED] /for-sale Decision Feed concept created and documented — 14:15 AEST
**Symptom:** Facebook ad driving 1,237 clicks at $0.12 CPC to /for-sale, but high bounce rate. Discovery traffic landing on a directory-style grid page with no curated insight.
**Root cause:** Intent mismatch — Facebook users are in discovery mode (passive, curious) but /for-sale delivers a search experience (filters, grid, pagination). The existing AI analysis and valuation data is not surfaced in a way that engages low-intent mobile traffic.
**Fix:** Designed and built a "Decision Feed" concept — a vertical stream of ranked market judgements replacing the grid-first layout. Built working HTML mockup (for-sale-v2.html) with real property data/images. Created full product concept document and production-ready development specification. Key UX mechanics: tap-to-reveal, overpriced trust-builder at position 2, quiz/compare interaction cards, progress bar (Zeigarnik effect), opinionated copy leading with deltas not ranges.
**Files:** 06_Listing-Scroll-Concept/README.md, 06_Listing-Scroll-Concept/01-PRODUCT-CONCEPT.md, 06_Listing-Scroll-Concept/02-DEVELOPMENT-SPEC.md, 06_Listing-Scroll-Concept/for-sale-v2-mockup.html, public/for-sale-v2.html (live at fieldsestate.com.au)
**Recurrence:** First
