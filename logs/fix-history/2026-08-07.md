
---

## [WATCHDOG] Step 107 (Database Audit & Validation) — 00:31 AEST

**Failure class:** UNKNOWN
**Cause:** No recognizable error pattern — check logs manually
**Action taken:** ESCALATED to Claude (id=None)
**Result:** queued — awaiting repair-agent or Claude
# 2026-08-07

## [LISTING-IMAGE-HOSTS-ORB-BLOCKED] 82% of listing photos on hosts a browser refuses — 08:40 AEST
**Symptom:** Competitor cards rendered without photos. Diagnosed initially as "photos missing for
Burleigh Waters"; that was wrong — `images_uploaded_to_blob` is true on 98/99, 51/51, 56/56.
**Root cause:** TWO independent bugs, neither about photo coverage.
  1. **`competitor_matcher.py:884`** — `"imageSrc": _hero_image(c) if is_close else None`. The
     PHOTO was gated on a combinatorial match. Proof it was the gate and not the data: `8 Rice
     Place` appears in two reports built 37 seconds apart, with an image in one and none in the
     other, purely because `is_close` differed per subject. `differenceVsSubject` gating on
     is_close is correct — that is a CLAIM about similarity. A photograph is not.
  2. **Host blocking.** Measured in a real browser (curl returns 200 for all three — these are
     ORB/CORS failures curl cannot see):
         fieldspropertyimages.blob.core.windows.net   1,602 listings   BLOCKED
         bucket-api.domain.com.au                       711 listings   BLOCKED
         blobs.fieldsestate.com.au                      492 listings   loads
     **2,313 of 2,805 active listings (82%) carried a first image that renders broken anywhere it
     is used.** The CDN fronts the same blob store, so the Azure rewrite is a pure host swap —
     verified, identical path returns a 1600px image.
**Fix:** Removed the `is_close` gate. Added `servable_image_url()` rewriting the Azure host to the
CDN, applied on every `_hero_image` return path. `b.domainstatic.com.au` does NOT serve Domain's
`/v1/bucket/image/` path, so those are left as-is and our own store is preferred first.
**Not fixed, deliberately:** no photo backfill. Only **17 of 2,822 (0.6%)** active listings lack
`property_images`, and those carry zero `domain_image_urls` — there was nothing to scrape. Several
are malformed scrape artefacts (an "address" of "Coomera, QLD 4209 house for Sale, $1,202..."). The
nightly is working.
**Files:** scripts/property_reports/competitor_matcher.py
**Recurrence:** 17th of the family. Third instance of "curl said it was fine".

## [SCARCITY-MAP-COORDS] Matching homes were unmappable because the count never kept them — 08:20 AEST
**Symptom:** The V4 scarcity map plotted amenities instead of the homes the section is about. I had
concluded active listings carry no coordinates — 0 of 58 in Burleigh Waters.
**Root cause:** Two things, and the first was mine. (a) I queried lowercase `latitude`, which does
not exist on these documents; the coordinates live in **`LATITUDE`/`LONGITUDE`** (uppercase, from
the cadastral dataset), with `geocoded_coordinates` and `georeference_data` as fallbacks.
(b) `scarcity_features.count_active_matches` uses `count_documents`, so the matching set was never
retained and nothing downstream could place those homes anywhere.
**Fix:** Added `find_active_matches()` + `resolve_listing_coords()` to `scarcity_features.py`,
mirroring the counter's query EXACTLY so the dots can never disagree with the number printed beside
them. Kept separate from the counter because that count is the load-bearing honest number and must
not change shape. Verified: 21 of 21 matching listings resolve coordinates.
**Files:** scripts/property_reports/scarcity_features.py,
15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py
**Recurrence:** 18th.

## [AERIAL-TITLE-BOUNDARY] Hero aerial now marks the actual parcel — 08:15 AEST
**Symptom:** "A reader looking at a block of roofs cannot tell which one is theirs" (Will).
**Fix:** New `scripts/render_property_aerial.py`. Resolves LOT/PLAN from the document, fetches the
true parcel geometry from Queensland's public cadastral service, caches it on the document as
`cadastral_polygon` (so the API is hit once per property, ever), projects the rings into Static
Maps pixel space and draws the outline with a blurred dark underlay so it survives pale roofs.
Semi-transparent Fields logo top-left over a soft vignette. Correctness check: the fetched parcel
for 28 Wedgebill reports 603.0 m², matching the document's stored land size exactly.
**Colour:** renders `sun` / `gold` / `copper`. Default is **sun** — copper and gold sit in the same
hue family as the terracotta roofs that dominate these suburbs and start to vanish into the
roofline. Legibility beat brand primacy; Will to confirm.
**Files:** scripts/render_property_aerial.py, .../render_prototype_a.py

## [SLICE-EDIT-ATE-ADJACENT-FUNCTIONS] Index-slice patch silently deleted two functions — 08:55 AEST
**Symptom:** All five Prototype C pages failed to regenerate. `NameError: timeline_safe`.
**Root cause:** I rewrote `scarcity_map` with `s[:index(start)] + new + s[index('def month_year'):]`.
`TIMELINE_KINDS`, `_CONTACT_PROMISE`, `timeline_safe` and `full_date` sat between the two anchors
and were deleted with it — including the contact-promise guard added the night before.
**Why it went unnoticed:** my re-render loop ran `>/dev/null 2>&1`, so five consecutive failures
were silent and the stale HTML from the previous evening stayed on disk looking current. Only the
browser check caught it, via a 404 on an aerial filename that no longer existed.
**Fix:** Restored all four definitions; re-rendered; verified 16/16 pages clean. Never blind-slice
between two function anchors, and never suppress output on a loop whose failure is invisible.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py
**Recurrence:** 19th — and self-inflicted, like [ENVELOPE-BACKFILL-BLIND-SPOT].

## [LISTING-IMAGES-STALE-AZURE-URLS] 2,013 listings pointed at photos we already held — 09:40 AEST
**Symptom:** Competitor cards rendered without photos. My first diagnosis said "82% of listing
images sit on hosts a browser refuses to render" — directionally right, WRONG on mechanism, and it
understated how much was recoverable. Will pushed back ("all photos should have been downloaded and
stored locally on this VM or in google cloud") and was correct.
**Root cause:** `fieldspropertyimages.blob.core.windows.net` is not ORB-blocked — the **Azure
account was cancelled on 2026-05-28** (recorded in memory `gcs_blob_backup`, never applied to the
database). Those 1,568 URLs raise a connection error. Separately, 445 listings pointed at
`bucket-api.domain.com.au`, which DOES return 200 to a server but renders 0/3 in a browser
(hotlink/ORB) — that half of the original claim stands. Meanwhile the photographs were on this VM
the whole time: 296 GB under `/data/blobs/property-images`, served at `blobs.fieldsestate.com.au`,
mirrored nightly to `gs://fields-blob-backup`. Step 110 was migrated long ago and writes CDN URLs
correctly — the newest 60 listings are all clean. These were LEGACY rows the migration never
rewrote.
**Fix:** `scripts/repoint_listing_images.py` rebuilds `property_images` from the files actually on
disk. A URL is only written when the local file EXISTS — publishing a link we cannot serve is the
defect being fixed, and re-committing it in another form would be worse. Original array preserved
at `property_images_pre_repoint` so `--revert` is exact. Applied to 2,013 listings.
    blobs.fieldsestate.com.au    492 -> 2,505
    bucket-api.domain.com.au     711 ->   266
    fieldspropertyimages (dead) 1,602 ->    34
Verified 8/8 freshly repointed URLs render in a real browser at 1200-3240px.
**Checked, not broken:** step 110's idempotency gates on `images_uploaded_to_blob != True`, which
the repointed docs still carry — so the repoint does not cause 2,505 re-downloads.
**Files:** scripts/repoint_listing_images.py
**Recurrence:** 20th of the family, and the second where a MIGRATION was completed in
infrastructure but never applied to the data that referenced it.

## [PHOTO-BACKFILL-INVISIBLE-TO-STEP-110] 296 listings could never be picked up — 09:55 AEST
**Symptom:** After the repoint, ~300 active listings had unusable URLs and no local photo.
**Root cause:** Invisible to step 110 by construction. Its query skips
`images_uploaded_to_blob: True`, and on these documents that flag is wrongly True — the upload was
recorded against the cancelled Azure account. `property_images` is non-empty, so the 2026-07-30
gap-fix branch (which targets empty `property_images`) does not catch them either. They fell
between the two.
**Fix:** `scripts/backfill_missing_listing_photos.py` finds active listings with no photos on disk,
confirms a re-downloadable source exists, and clears the stale flag so the TESTED pipeline collects
them — deliberately not a second downloader, because two code paths writing the same blob layout is
how the layout drifts. 296 recoverable from `scraped_property_images`; 18 have no usable source at
all (malformed scrapes, e.g. an "address" of "Coomera, QLD 4209 house for Sale, $1,202...") and are
reported rather than silently counted as done. Both scripts self-report via `job_run`
(cadence 168h) per Rule 7.
**⚠ Gotcha found the hard way:** `download_images_to_blob.py --suburbs` expects "Name:postcode"
("Robina:4226"), NOT collection names. Passing collection names matches nothing and prints
"Properties found: 0" — which reads as success. Removed the flag; unfiltered is correct here
because step 110's own query already selects exactly this set (measured: 299 docs, 296 ours).
**Files:** scripts/backfill_missing_listing_photos.py
**Recurrence:** 21st.

## [OWNER-ARTICLE-PRODUCTIONISED] Owner-subject article: prototype -> generator — 09:04 AEST
**Symptom:** The owner-subject article existed only as four hand-written HTML/MD drafts
(2026-08-05, `15_Off-Market/Concepts/Article_Prototypes/`). Its generator, prompts and
guardrail linter were described in the README as living "in the session scratchpad" — they
were gone. No article could be produced for a second address, and the prototype's figures
had already drifted from the database (21 Indooroopilly adjusted $1,246,797 in the draft
vs $1,224,291 stored). Three of the README's five open blockers were still live in the copy.
**Root cause:** The drafts were authored by hand during an exploratory session and never
had a build path. Hand-authored copy cannot be regenerated, cannot be checked, and goes
stale silently against a database that is recomputed nightly.
**Fix:** Built `scripts/owner_article/` — a deterministic (non-LLM) generator, so the
numeric check below can be structural rather than a proof-read.
  - `factbook.py` — every figure must be MINTED (`fb.money`, `fb.pct`, `fb.word_count`,
    `fb.address`, `fb.date`) before it can appear in copy; `verify()` re-scans the finished
    text and fails the build on any figure with no mint record. This closes the class that
    produced "four of the eight" when it was six. Caught 7 real leaks on first run.
  - `guardrails.py` — advice / prediction / urgency / solicitation / confidence-grade /
    banned-word / no-money-in-headings linter. Zero BLOCK required to render.
  - `build_owner_article.py` — resolve subject, guards (listing_status, directional_only,
    design envelope on the adjusted-comp midpoint, live PropRadar mailability via
    `propradar.market_status`), radius-filtered comp selection, suburb rolling-median
    series, date-split movement, composition, checks, md + HTML render, aerial hero.
Blockers closed: (1) confidence grade removed — measured non-discriminating across 512
sold homes, high 56.0% vs medium 57.5%; (2) hard 2.0 km radius (comp-distance p90 = 2.04 km
over 1,197 sampled homes), widened in 0.5 km steps only if <4 comps remain and DISCLOSED
in the article; (4) numeric fact-check now exists.
Also fixed: the prototype hardcoded "+5.8% matches +5.8%" agreement copy — on a home where
the comps moved +3.5% against a suburb median of +6.8% that would have asserted agreement
that is not there. The characterisation is now derived per home. And `str.capitalize()` was
lower-casing street names mid-sentence (replaced with `_upper1`).
Hero image: satellite aerial with the true cadastral boundary, NOT a listing photo —
`domain_hero_image_url` is expired by Domain on delisting and curl cannot detect it
(memory `image_url_verification_orb`). Verified visually.
Generated 9 articles across all three suburbs, adjusted-midpoints $1.15M-$1.98M; all passed
the PropRadar guard; HTML verified in headless Chrome, zero console errors.
**Files:** scripts/owner_article/{build_owner_article.py,factbook.py,guardrails.py,macro_context.json},
15_Off-Market/Concepts/Owner_Article_v1/ (9 articles + README)
**Recurrence:** First

---

## [V3-CLAIM-BUTTON-NEVER-SEEN] SMS claim button hidden until after all three lines typed — 09:20 AEST
**Symptom:** The first real reader ever to press "Start building" on the V3 off-market deck
(6 Aug 2026, 20:08:46 UTC, `/off-market/11-highgate-lane-robina`, Samsung S22 from Google
search, PostHog session `019fd8ae-cb46-7106-96c3-e73311327d2f`) never sent the claim SMS.
Nothing errored: the prewarm fired, the mini-site built (`property_reports`, `under_review`,
complete 20:09:10), the outro ran clean with zero console errors, and the claim panel was
inserted at 20:09:12.767 carrying the correct link
`sms:+61440131629?body=SEND%2011%20Highgate%20Lane`.

**Root cause:** `.c-act` (the button and its note) is `opacity:0` until `#fx-claim` gets the
`ready` class, and `claim.js` only added it after ALL THREE lines had finished typing — 6.5s
after the panel appeared, 32.9s after the CTA press. The replay's last mutation is the final
"." of "We'll send a secure link to your mobile." at 20:09:18.864; the `ready` mutation, due
~0.4s later, never arrives. The button existed the whole time and was never visible.
Compounding it, `claim.js` shipped with **zero analytics**, so a press and a non-press were
indistinguishable — the only reason this is knowable is that session replay happened to be on.
No `$pageleave` and no `deck_exit` (which fires on `visibilitychange`), so the page stopped
executing abruptly rather than being backgrounded.

**Fix:** Reveal moved to the moment `"Your website is ready."` lands — 3.3s earlier, and the
offer is coherent at every frame (revealing it with the panel was tried first and rejected:
a lone button above three empty lines for 1.5s). Added `claim_panel_shown` and
`claim_sms_pressed`, carrying the deck's own `slug`/`suburb`/`arm` plus `surface`, over
`sendBeacon` because the `sms:` navigation backgrounds Android Chrome instantly. `ClaimConfig`
gained `slug`/`arm` so the events join onto `card_viewed`/`deck_exit` without a lookup.
Verified in headless Chrome emulating the same device: button fully opaque at 3.81s (was
7.1s), correct href, both events captured with the right props.

**Files:** 15_Off-Market/Page_Redesign_V3/outro/claim.js (canonical; synced via
sync_outro_to_site.sh), 15_Off-Market/Page_Redesign_V3/preview/deck.css,
15_Off-Market/Page_Redesign_V3/CLAIM_STEP.md,
01_Website/public/off-market-v3/{claim.js,outro.css},
01_Website/src/pages/OffMarketPage/discovery-v3/{useOutro.ts,DeckV3.tsx}
**Recurrence:** First (related but distinct: [V3-PORT-PHASE-3] built the panel,
[V3-SERVICE-AREA-GUARD] gated it)

## [V4-BRAND-AND-COMPLETE-PAGE] Real logo, rounded aerial, one complete page — 10:20 AEST
**Changes (Will's review):**
  1. **Real Fields mark**, replacing the text wordmark. Grass hero in the page header, where it sits
     on the birch background it was drawn for. The aerial stamp uses the **Birch (light)** variant
     instead — Grass is a dark green and disappears against a dark aerial, the same legibility
     problem that ruled out copper for the boundary line. Same mark, chosen per surface.
  2. **Aerial corners rounded to 14px.** 3px read as a square photo with its corners knocked off.
  3. **One COMPLETE page per property.** A/B/C were build stages (spine -> +evidence -> +living),
     never variants a reader would see. Publishing three links per address implied a choice that
     does not exist and buried the finished thing. `--prototype full` is now the default and writes
     `{slug}.html`; the stage flags survive only for isolating a layer during development. The index
     lists one link per address.
**Two defects caught by looking at the render:**
  * "PRIVATE REPORT" wrapped to two lines in the header at 390px.
  * The aerial was rendered 640x420 (1.52) into a `16/10` CSS box (1.60), so the top and bottom were
    cropped — and **Google's attribution sits on the bottom edge**. Clipping it is a licensing
    question, not a cosmetic one. Now rendered 640x400 so nothing is cropped. (The page also carries
    an independent text credit, so attribution never rested on the watermark alone.)
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py,
scripts/render_property_aerial.py

## [GOOGLE-OAUTH-7DAY-REAUTH] Two dead Google refresh tokens killed FPF, site email, Ads + GSC — 09:28 AEST
**Symptom:** Five Property Friday's 09:00 batch sent 0 of 7 (`gmail_token_unavailable`), and
`job_runs` showed four errors: `fpf_friday_batch`, `fpf_preflight_gmail` (`dead: invalid_grant`),
`search_intent_ads` and `search_intent_gsc` (both `invalid_grant: Token has been expired or revoked`).
All site email (9 Netlify functions) was down with it.
**Root cause:** TWO independent refresh tokens expired, not one — the recurring 7-day expiry from the
`fields-estate-ads` OAuth consent screen still sitting in **Testing** mode. Gmail
(`GMAIL_REFRESH_TOKEN`, sender rossmax06@gmail.com) and Ads/GSC/Indexing
(`GOOGLE_ADS_REFRESH_TOKEN` + `GOOGLE_INDEXING_REFRESH_TOKEN`) share the same desktop client
`6178359532-…` but are consented under *different Google accounts*, so they needed two separate
browser flows and could not be combined into one grant.
**Fix:** Re-consented both in Will's browser, exchanged codes → refresh tokens.
- Gmail: verified by **live test send BEFORE storing** (200 SENT, From: will@fieldsestate.com.au) —
  the `gmail.send` scope can't read the profile, so a live send is the only way to catch the
  wrong-account trap from [GMAIL-TOKEN-DEAD]. Written to VM `.env` + Netlify at **both** site
  (`?site_id=…`) and account scope (full-object PUT; two distinct value IDs confirmed the stores are
  genuinely separate and both were stale). Build hook fired so functions reload env at cold start.
- Ads/GSC: one grant covers adwords+webmasters+indexing. Confirmed the indexing scripts read
  `GOOGLE_ADS_CLIENT_ID/SECRET` (no separate indexing client) before overwriting, so one token fills
  both vars. Verified live: GSC `/sites` 200, `google_ads_manager.py list` returned all campaigns.
`.env` backed up to `.env.bak-<ts>` before edit. Google Drive's token was tested and is unaffected.
**Files:** .env (GMAIL_REFRESH_TOKEN, GOOGLE_ADS_REFRESH_TOKEN, GOOGLE_INDEXING_REFRESH_TOKEN),
Netlify env GMAIL_REFRESH_TOKEN (site + account scope)
**Recurrence:** 3rd — [GMAIL-TOKEN-DEAD] 2026-07-24, [GMAIL-NETLIFY-ENV] 2026-07-27, this one.
The monitoring added on 2026-07-24 worked exactly as designed (preflight caught it, Telegram fired),
but detection is not prevention: this WILL recur every ~7 days until the `fields-estate-ads` consent
screen is published to "In production". That remains the open permanent fix, now overdue and
widened in scope — it is not just a Gmail problem, it takes out paid search and SEO data too.

## [V4-SECTIONS-BUILT-BUT-NOT-RENDERED] Three finished sections were never on the page — 10:55 AEST
**Symptom:** Will asked whether the flow really was everything we had built. It was not.
**Root cause:** I built the page from the eight moments in the UI brief, but `emit_v4` and the fact
bundle already carried MORE than the brief listed, with real data per property. Nothing was broken
— the sections simply had no renderer, so they were invisible and looked unbuilt.
**Missing, now added, in canonical order:**
  * **Since the last recorded sale** (`gain`, card 07) — last sale, the honest "our series does not
    reach back that far" note, and the ten-year suburb movement.
  * **Who would actually want it** (`buyer`, card 09) — the buyer portrait, plus `value_drivers`
    split into *what carries the price* and *what draws them in*, which had never rendered anywhere.
  * **A private working plan** (Product/05_PAGE_FLOW.md section 10, prototyped in
    `Prototypes/build_working_plan.py`) — the five owner-only questions and the list of decisions
    that genuinely need a walkthrough. §0 names three questions the reader may be asking; without
    this the page answered one and gestured at another, which by our own rule ("forward cues must
    be honest") was a bait-and-switch.
**How they are built:** the `gain` and `buyer` sections IMPORT the emitter's own cards rather than
re-deriving them from the bundle. Two implementations of one section is how a page and its emitter
end up disagreeing about the same home.
**Carried over:** the load-bearing ordering guard from the deck card — `bought` can be a sale from
decades ago while `ten_year` covers ten years, so `cannot_reach` is rendered BETWEEN them or a
reader does arithmetic across both and gets a figure we have not evidenced.
**Page is now 13 sections.** Still unrendered and deliberately so: `positioning.frame_line` and
`filter_checklist` are internal framing for us, not reader-facing copy.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [OFFMARKET-DISCOVERY-UNPACK] Nightly crashed after building all 3000 decks — 09:45 AEST
**Symptom:** `offmarket_discovery_nightly` reported `ValueError: too many values to unpack (expected 3)`
and the job showed ERROR on the health board — despite the log reading `3000/3000 built=3000 failed=0`.
All the expensive work succeeded; only the final coverage assertion blew up.
**Root cause:** `indexed_homes()` and `reachable_homes()` were widened to yield a 4th field
(`valuation_data.computed_at`) — line 338 already destructured 4 (`for (s, le, sub, va) in homes`) —
but the coverage assertion at line 385 still unpacked exactly 3: `{s for s, _le, _sub in homes}`.
The generators' own docstring was also left stale, still promising a 3-tuple, which is what made the
mismatch easy to miss. Because the assertion runs LAST, the failure cost a full ~100-minute rebuild
every night while looking like a total failure when in fact nothing was wrong with the output.
**Fix:** `{s for s, *_ in homes}` — width-agnostic, so adding a 5th projected field can never
resurrect this. Corrected the `indexed_homes()` docstring to state all four yielded fields. Grepped
for sibling fixed-width unpacks of `homes`/`build_pool` — none remain. Verified against live data:
the old expression raises, the new one returns 500/500 slugs clean.
**Files:** 15_Off-Market/Page_Redesign_V2/offmarket_discovery_nightly.py
**Recurrence:** First. Pushed to Will954633/Fields_Orchestrator (7bd85a2), md5-verified against remote.

## [V4-MENU-AND-QR] Section menu + report-request QR — 11:40 AEST
**Added (Will's request):**
  * **Burger menu, top right.** Opens a panel listing every section with direct anchors. The nav is
    built by parsing the ASSEMBLED page for `<section id>` + its eyebrow, not from a hard-coded
    list — sections omit themselves on thin properties, and a fixed list would leave dead links
    pointing at blocks that never rendered. Plain hidden/aria toggle with Escape to close; no scroll
    lock, because locking the page to open a menu is a small version of the scroll-jacking the brief
    rules out.
  * **QR to request the report.** Encodes the SAME user-initiated deep link the live deck uses —
    `sms:+61416529481?&body=…`, matching `DiscoveryDeck.tsx`/`OffMarketDeck.tsx` rather than
    inventing a second mechanism. Body is "Download report for <short address>", per property.
    Rendered as an inline SVG data URI: no network request, no third-party QR service seeing our
    addresses, crisp at any size.
**⚠ A QR is useless on the device showing it** — you cannot scan your own screen. Desktop gets the
code; mobile gets the identical deep link as a tap ("Text me this report"). One action, the
affordance each device can actually use.
**Verified by decoding, not by eyeballing:** the SVG was rasterised through a browser and decoded
with OpenCV, returning
`sms:+61416529481?&body=Download%20report%20for%2028%20Wedgebill%20Parade`. A QR that renders but
does not scan looks identical to one that works — the same class of defect as an image URL that
returns 200 and never displays.
**⚠ Still needs a real-device test.** `?&body=` (a `?` immediately followed by `&`) is what
production uses and works for tap-links, but QR SCANNERS vary; `SMSTO:number:body` is the more
universally supported QR form. Matching production was the right default, but confirm on an actual
phone before this ships.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [V4-COPY-VERSIONING] v2 copy + a real revert path — 12:30 AEST
**Why:** iterating on prose was overwriting the previous wording with no way back, and "what did it
say before?" was being answered from memory or git archaeology.
**Fix:** a `VERSIONS` registry in the renderer holding ONLY the copy that differs between takes,
plus `--version` (default `LATEST`). The latest writes `{slug}.html`; any older version writes
`{slug}--vN.html` alongside rather than replacing, so two wordings can sit side by side. The index
ignores versioned files so the review path always shows the current take. Rule for the registry:
only add a key when it actually varies — everything else stays shared, or the versions drift apart
in ways nobody intended.
**v2 changes (Will):**
  * The three questions move to the owner's OWN words, first person. The first is now a worry
    ("I'm worried online valuations are wrong.") rather than a question we pose — closer to why
    someone searches their own address at all.
  * New section between the hero and the range: **"Two property sites. Two different values for the
    same home. Which one is right?"** — naming the two-portals problem BEFORE our figure appears, so
    the reader meets our range already knowing the question is *which sales, and what was changed*
    rather than *which number is bigger*. Without it our range is just a third unexplained number,
    which the copy says outright.
  * The privacy promise loses "This page starts with the first" — that framing referred to the old
    question set and no longer matched.
**Verified:** v2 renders all three new lines and the four new paragraphs; the v1 snapshot still
carries the original wording; 5 pages + the v1 snapshot all clean.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [V4-TIMING-SECTION] "Is now the right time?" — computed per suburb — 13:30 AEST
**Added:** a section answering the second of the three questions posed at the top, under that exact
headline. Macro (attributed, never adopted) + local figures + the seasonality chart + an explicit
refusal to advise.
**⚠ The defect this avoided.** The natural narrative — "listings rising while prices hold firm" — is
true in Varsity Lakes (+43.8%) and Robina (+5.5%) and **FALSE in Burleigh Waters, where active
listings are DOWN 27.3%**. Days on market also moves in opposite directions: faster in Burleigh
Waters (29 vs 37), slower in Robina (34 vs 24) and Varsity Lakes (26 vs 21). A hand-written
paragraph would have been contradicted by our own data on one suburb in three, so the copy is
composed from `market_pulse.data_snapshot` per suburb.
**A defect I DID ship and then caught:** the first logic said "those two readings do not point the
same way" for every case except faster-and-fewer — which made it false for Robina and Varsity Lakes,
where both readings move together. All four direction combinations now handled, and the copy states
which way each moved without characterising it as strength or weakness (that would be a prediction
in disguise, and "the market is strong / holding up / resilient" is banned outright).
**Seasonality** uses the canonical figures verbatim — 18,978 sales, 2010-2025 excluding 2019-2020,
H1 -0.18% / H2 +2.25%, November strongest — and is labelled CATCHMENT-wide, because per-suburb
months are too thin. These exact numbers matter: `december-listing-paradox` shipped overstated once
(Dec +6.05% vs the real +2.81%) and had to be corrected and republished.
**Verified:** banned-phrase scan clean on all five pages (no advice, no urgency, no timing framing,
no "strong/holding up/resilient").
**Also fixed — the instrument, not the copy:** the page checker flagged "None" as a leaked Python
value on all five pages. It was the English word opening a sentence ("None of that tells you what to
do"). Regex tightened to match `None` only when bounded by punctuation or end of line.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py,
15_Off-Market/Page_Redesign_V4/Product/10_MARKET_TIMING_PARAGRAPH.md

## [V4-SEASONALITY-REUSE] Replaced my ad-hoc chart with the existing component's treatment — 14:10 AEST
**Symptom:** I built a bespoke SVG bar chart for seasonality. Will pointed out the visual and its
loader already exist — `YourHomePage/components/SeasonalityStrip.tsx`, on the Process tab of the
`/your-home` mini-site — already reconciled to the canonical dataset and already carrying the
citations.
**Fix:** ported that treatment rather than keeping a second chart: month tiles with the value on
each, tinted copper above the annual average and sky/teal below at the component's own intensity
(`min(|pct|/6,1)*0.55`), peak outlined, current month dotted, below/above scale, tap-to-detail with
the sales count behind each month, and the academic framing verbatim — buyer concentration rather
than seller scarcity (Ngai & Tenreyro 2014; Miller, Sklarz & Real 2014). Counts come from
`seasonality_monthly_summary.csv` (`comparisons` column; Nov 1,512 matches the live page).
**Why it matters beyond tidiness:** two charts built from one dataset drift. The CSV still carries
the `article_pct` column — the overstated figures from the `december-listing-paradox` article that
had to be corrected and republished (Dec +6.05% against the real +2.81%). A second implementation is
exactly how those get picked up again.
**Also removed:** the timing paragraph restated the H1/H2 split in its own words. The strip already
states the figures, the window, the catchment caveat and the sources — saying it twice in different
words is how two numbers drift apart. The paragraph now hands off in one line.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [V4-MEDIAN-IN-CONTEXT] Suburb median placed against the national picture — 15:00 AEST
**Added:** a median block in the timing section — the rolling twelve-month median for the suburb,
its range and sample, the year-on-year change, and a quarterly bar chart carrying the sale count
behind each quarter.
**⚠ Leads with the ROLLING figure, never the latest quarter.** The data carries its own warning:
`latest_quarter_median_price_basis` literally reads *"name it as a quarterly figure wherever it
appears, never as 'the median house price'"*. And the quarters bounce — Robina runs $1,560k then
$1,410k on samples of 55 and 43 — which is why the mindset brief bars quarter-on-quarter claims for
Robina and Burleigh Waters. The bars are shown with their counts, quarters under 50 sales are faded,
and **no line is drawn through them**, with the copy saying why.
**The national framing** is the comparison Will asked for and is purely descriptive: national values
have fallen three months running; the suburb's rolling twelve-month median is up 10.2% (Varsity
Lakes), 6.9% (Burleigh Waters), 5.8% (Robina). *"The two are not in conflict — a national index and
one suburb measure different things."* Year-on-year is only stated in its rolling form, which is the
only defensible form per the brief.
**Two leaks caught by reading the render, not the code:**
  * **"Domain ∪ onthehouse"** — the stored basis string is internal set notation, fine in a data
    dictionary and meaningless to a homeowner. Now written in words.
  * **`Q3&nbsp;2024` displayed literally.** I passed an HTML entity through `E()` (html.escape),
    which turned `&` into `&amp;`. Replaced with a real U+00A0.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [V4-REBUILT-AN-EXISTING-CHART-AGAIN] Bar chart replaced with the real median series — 15:40 AEST
**Symptom:** Will: "why did you make this chart and not simply plug in the existing market metrics
chart from the market intelligence page we already have?"
**Root cause — mine, and a repeat.** I probed for a median-chart endpoint, found
`/api/market-narrative/.../charts/price-history` returned 15,343 RAW SALES rather than a series,
concluded the chart was computed client-side, and built bars from
`market_pulse.median_price_history` (8 quarters) instead of finding the component. The component
was `src/components/MetricCharts/MedianPriceChart.tsx`, fed by
`/.netlify/functions/market-insights`, and `OffMarketPage/MarketCharts.tsx` ALREADY imports it —
the off-market page renders this chart today.
**This is the second time in one sitting.** I had just written [V4-SEASONALITY-REUSE] about
rebuilding a component that already existed, then did the same thing one section later. One failed
endpoint probe is not evidence that a component does not exist; grep for the component.
**What was lost by rebuilding:** the endpoint returns **84 quarters** back to Q3 1993 with per-
quarter confidence intervals and a `reliable` flag, plus a 59-quarter rolling 12-month series and
the in-progress quarter. My version showed **8 quarters** and no rolling line — two years where the
live page shows thirty-three.
**Fix:** `market_insights()` reads the same endpoint (cached per suburb); `median_chart()` draws
both series over the full history. Palette follows THIS page rather than the market-intelligence
blue/green — the rolling median, which the copy tells the reader to trust, is in the accent, and the
noisier quarterly line is muted behind it, so the colour carries the same message as the words.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [FPF-STOP-NOT-HONOURED] Five Property Friday had no opt-out mechanism — 12:25 AEST
**Symptom:** Belinda Halstead replied STOP to the Five Property Friday email. Nothing in the
system acted on it: every FPF email footer says "Reply STOP to opt out", but there was no
suppression list, no unsubscribe path, and no code that could stop the next Friday send.
**Root cause:** `fpf_send.py` selects recipients purely on `fpf_status: "active"` (fb_leads) and
`status: "active"` (five_property_friday_subscribers). A STOP reply arrives as an ordinary Gmail
reply — no handler, no field written. Worse, `handle_lead()` unconditionally `$set`s
`fpf_status: "active"`, so even a hand-edited status would be resurrected by the same person
submitting the buyer-brief form again (a fresh `_id`, so no dedup against the old lead).
**Fix:** Added an email-keyed suppression list, `system_monitor.email_optouts` (`_id` = lowercased
address), checked at three points: `handle_lead()` (early return + marks the new lead
unsubscribed), both recipient loops in `friday_batch()` (counted as skipped), and `tracked_send()`
as a last-resort backstop returning `{ok:false, suppressed:true}` before any HTTP call — so no
future caller can email a suppressed address by forgetting a check. Keying on the EMAIL rather
than the lead means an opt-out outlives the lead record. `record_optout()` also deactivates the
address on every list it sits on (fb_leads, website subscribers, crm_contacts `do_not_email`).
New CLI for the next one: `python3 scripts/fpf_send.py --optout EMAIL --reason "..."`.
Applied to bhalstead1974@gmail.com. Verified: no longer matches the active-batch query,
`is_opted_out()` true case-insensitively, live `tracked_send()` suppressed before POST, control
address unaffected, 3 active leads remain.
**Files:** scripts/fpf_send.py
**Recurrence:** First

## [V4-CHART-SCRUB-AND-DEAD-CUES] Interactive median chart; forward cues re-chained — 16:20 AEST
**Added — chart scrubbing.** Pointer guideline, a dot on each series and a readout of both values at
that quarter. `pointer*` events cover mouse and touch in one path; `touch-action: pan-y` lets a
vertical swipe still scroll the page while a horizontal drag scrubs — capturing both axes would trap
the reader inside the chart, which is the scroll-jacking the brief rules out. Scrub points are
emitted as data rather than re-derived in JS, so the chart and the readout cannot disagree about a
quarter. Rolling values are matched by quarter LABEL, never by index: the rolling series is sparse
(59 entries against 84 quarters) and positional indexing would silently mis-pair them.
**Fixed — dead forward cues (Will).** "First, one sale nearby is worth looking at" pointed at
`#nearby`, which only renders when the property has an obvious comparable. **2 of 5 pages had that
dead anchor** — a link to nothing, and a question the page then never answers. Cues are now
re-chained after assembly against the sections that ACTUALLY rendered, taking the following
section's own question with them: where there is no nearby sale the cue becomes "See the strongest
sales" pointing at `#comps`. Same job `emit_v4._rechain` does for the deck. Verified: zero dead
anchors across all five pages.
**Fixed — tooltip clipped at the right edge.** It is positioned against `.mchart`, which is padded,
but I clamped against the SVG's own width. Now measures the offset between the two. Verified inside
the card at 2%, 50% and 99% on both desktop and mobile.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [V4-EXCLUDED-SALE] Duplicate cue replaced with the sale we left out — 16:50 AEST
**Symptom (Will):** on pages with no nearby-sale section, the re-chained cue read "See the strongest
sales" directly under a "See the strongest comparisons" button — same destination, same words,
twice. His suggestion: show something else, "like here's why X st was not included".
**We had the data.** `valuation_data.recent_sales` holds all 51 candidates with
`included_in_valuation` AND `verification.issues` — so the exclusion reason is RECORDED, not
inferred. New `#excluded` section shows the priciest nearby sale we did not use and why, in plain
English: 30 Aruma Avenue at **$4,600,000**, 2 km from 3 Fimiston Place, left out because once the
differences were priced in it still sat 58% above the middle of the set and read as an outlier.
**Only renders where a reason is RECORDED.** Most excluded sales simply were not among the eight
strongest by weight — true, uninteresting, and dressing it up as a rejection would misrepresent what
happened. Verified across all five properties; each has one, and the percentages differ per subject
because the cohort median does (30 Aruma is +58% against Fimiston, +34% against Whitehead).
**Editorial framing:** it is a real sale, not a bad one. "Leaving it in would have pulled the range
toward a home this one is not" — the same judgement the "sale up the road" card makes, in reverse.
**Cue de-duplication.** The re-chainer now skips any target the current section ALREADY links from a
control, so a cue can never restate a button beside it. The two affected pages now point at
`#excluded` with "And the sale we left out"; the other three keep pointing at `#nearby`.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [V4-SCROLL-GLIDE] In-page navigation animated instead of teleporting — 17:15 AEST
**Symptom (Will):** clicking a forward cue was "a very sharp instant flick" — no sense of gliding
from one section to the next, so the reader loses their mental map of where they are on the page.
**Fix:** intercept in-page anchors and animate the scroll with easeInOutCubic — gentle acceleration,
carry, long deceleration into the landing. Measured trajectory over 1.5k px:
`0 → 2 → 32 → 127 → 324 → 660 → 1056 → 1303 → 1434 → 1480 → 1494 → 1495`.
**Details that matter:**
  * **Duration scales with distance** (~0.55ms/px, clamped 480-1500ms). A fixed duration makes a
    short hop feel sluggish and a long move feel thrown.
  * **Lands clear of the sticky header** — offset by its measured height + 12px, so the section
    heading is never hidden under the chrome it just scrolled behind. Verified: 12px clearance on
    both desktop (55px header) and mobile (69px).
  * **A wheel or touch during the glide cancels it.** Never fight the reader for control of their
    own scroll.
  * **`prefers-reduced-motion` honoured** — instant jump for anyone who has asked the OS for less
    motion, which for them is the correct behaviour, not a degraded one. Verified: lands within
    60ms under emulation.
  * **History updated** with `pushState`, so back/forward and sharing a section link still work; and
    arriving with a hash already in the URL lands the same way rather than teleporting.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [BREAKGLASS-CONTEXT-GATE] Break-glass unreachable on every page it was built for — 12:45 AEST
**Symptom (Will):** the break-glass animation and Samantha handover run on the home page but not on
individual listing pages (`/property/24-tropicana-circuit-burleigh-waters`).
**Root cause:** not a wiring gap — a deliberate `{isHome && <BreakGlass />}` gate in
`SiteFooter.tsx`, added at ship time when home-only was the ask. Everything behind it had been
built for the other pages and shipped unused since 2026-08-04: `pageContext.js` carries five
contexts (property, suburb, listings, article, home), all five `ctx_*.mp3` voice lines are in
`public/break-glass/voice/`, and `samantha_chat/service.py:302` already branches on the context to
tell her which property the visitor is looking at. Only the home branch was reachable, which is the
weakest one — it opens on "no file selected" and she has to ask for an address.
**Fix:** replaced the `isHome` boolean with `hasPageContext(pathname)`, a new export in
`pageContext.js`. The gate now reads the same path table that selects the voice line, so a context
and the pages it covers are declared in one place — the .js/.tsx split exists precisely to avoid
two copies of one behaviour, and a duplicated path regex in the footer would have reintroduced it.
Pages with no context still get no box: the sequence announcing "no file selected" on /contact
would be a lie about the system's own state.
Also widened the property matcher to `/sold/` (same PropertyPage, same "property file located"
line), and fixed the readout: it fell back to `document.querySelector("h1")`, but a property page's
h1 is the *editorial headline* once the write-up is published (`PropertyPage.tsx:913`) — the
address h1 only renders when there is no editorial. So the screen would have typed an article
headline while the voice said "property file located". `pageContext.js` had anticipated this via
`el?.dataset?.ovrAddress` but nothing ever set it and `resolveContext()` is called with no element.
Both `PropertyPage` and `PropertySsrShell` now mark `data-ovr-address` on `<main>`, and the lookup
finds it globally.
**Verification:** `vite build` clean (the ~670KB sequence/audio/chat chunk stayed behind its
dynamic import — the box is now on far more pages, so the lazy boundary matters more than before,
not less). `tsc -p tsconfig.app.json` filtered to touched files: no new errors.
**Files:** src/components/BreakGlass/pageContext.js, pageContext.d.ts, README.md,
src/components/SiteFooter/SiteFooter.tsx, src/pages/PropertyPage/PropertyPage.tsx,
scripts/samantha_chat/README.md
**Recurrence:** First for the gate. Third BREAKGLASS-prefixed entry (BREAKGLASS-PULL,
BREAK-GLASS-FOOTER, BREAKGLASS-LIVE-VERIFY-INSTRUMENT all 2026-08-04/05) — all shipping this
feature, none the same defect.

## [V4-PORT-PLAN] Stop rebuilding what we own — 17:50 AEST
**The realisation (Will agreed):** the V4 page is substantially an ASSEMBLY of existing production
components plus new copy, not a new build. Established the hard way — three sections were rebuilt in
the prototype before anyone checked whether they existed. All three did:
  * Seasonality -> `YourHomePage/components/SeasonalityStrip.tsx`
  * Median price -> `components/MetricCharts/MedianPriceChart.tsx` (already imported by
    `OffMarketPage/MarketCharts.tsx`)
  * Comparables -> `YourHomePage/components/ValuationEvidence.tsx`, 1,068 lines
**Verified the valuation port is FREE, not merely possible.** `valuation_evidence_from_engine()`
reads straight off `valuation_data` — "no recomputation", per its own docstring — which is the same
field on every off-market document. Run against off-market subjects it returned 8 comparables each
with **15 photos**, 10 per-feature adjustments, weight %, distance, verified flag and narrative.
Same data, same resolver, same component. It also brings `EvidenceMap`, which the prototype has no
equivalent of.
**⚠ Two things must not port with it:** the "reviewed by a property consultant" line is a CONTACT
PROMISE — the class of copy already stripped from this page — and the statutory CMA block needs a
deliberate decision about whether it belongs on a page nobody requested.
**Written down:** `Design/03_PORT_PLAN.md` splits PORT from BUILD, with the rule that caused all
three: *before building any section, grep for the component; a failed API probe is not evidence that
a component does not exist.*
**In the prototype:** those three sections now carry a visible "Ported component" marker so nobody
— me included — invests further in them. Genuinely new work is six items: the two-portals preamble,
the excluded sale, the dispersion finding, the working plan, the timing copy, and the ordering.
**Files:** 15_Off-Market/Page_Redesign_V4/Design/03_PORT_PLAN.md,
15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [BREAKGLASS-PROSE-CASE] Samantha shouted the address in her opening line — 13:20 AEST
**Symptom:** on a property page her first message read "I have **24 TROPICANA CIRCUIT, BURLEIGH
WATERS, QLD 4220** loaded." Caught on the verification screenshot for
[BREAKGLASS-CONTEXT-GATE], not reported.
**Root cause:** `samanthaChat.js` built that sentence from `readout` — the *screen* string, which
is uppercase because it is typed into a terminal-style panel under the alarm. Correct there, wrong
in prose, and against house style (suburbs are title case, never shouted). The branch had been
dead code: `key === "home"` takes the other opener, and home was the only reachable context until
this morning. Widening the gate is what put it in front of visitors.
**Fix:** contexts now declare `label` in ordinary case as the single source, and `resolveContext`
derives `readout = label.toUpperCase()` for the screen. Storing the caps and lower-casing them back
could not work — "QLD 4220" does not survive the round trip. Home keeps an explicit readout
("AWAITING ADDRESS") since it has no label by design. `label` is threaded to the chat opener and
into the POST body; `service.py` prefers it over `readout` when telling her what the page is headed,
because she quotes that heading back and would otherwise shout the street name in every reply.
Both sides fall back to `readout` so an older client and a newer server still agree.
**Verification:** live re-run of the full sequence on the property page — readout typed to screen
matched `data-ovr-address` exactly, opener rendered in ordinary case.
**Files:** src/components/BreakGlass/pageContext.js, pageContext.d.ts, samanthaChat.js,
samanthaChat.d.ts, BreakGlass.tsx, scripts/samantha_chat/service.py
**Recurrence:** First.

## [BREAKGLASS-VERIFY-INSTRUMENT-2] Two more false "feature broken" readings — 13:25 AEST
**Symptom:** first verification run reported the property page had no box at all, and that the
sequence never fired on ANY page (readout null, chat absent everywhere). Both were the harness.
**Root causes:** (1) one `window.scrollTo(0, scrollHeight)` does not reach the footer on a long
page — lazy content keeps loading and moves the bottom, so the box's IntersectionObserver never
fired and the element genuinely was not in the DOM. The property page is 9,665px. (2) The bail is
`hidden` for `armDelay` = 1640ms after the smash; the harness waited a guessed 1200ms, focused a
hidden button, and the Enter went nowhere. (3) The chat panel is `.samc` — the selector guessed at
`[data-ovr-chat]`/`.ovr-chat`. (4) Sampling the readout at a fixed 9s caught it mid-type: it types
at 26ms/char and the beats do not start until the ~670KB sequence import lands.
**Fix (harness, not product):** scroll until `scrollHeight` stops growing; `waitForSelector` on
`[data-x="hit-handle"]:not([hidden])` rather than a guessed delay; poll the readout until it is
unchanged across three reads. Every assertion now waits on a real state.
**Recurrence:** Second — [BREAKGLASS-LIVE-VERIFY-INSTRUMENT] on 2026-08-05 logged three false
readings on this same feature. The pattern is consistent: this component is time- and
scroll-dependent, so any fixed `sleep` or single scroll in a check on it is a latent false negative.
Note the direction — all five failures made working code look broken, which is the safe direction.
The dangerous one is a check that goes quiet and reports success; see the standing note in
[[website_verification_gates]].
**Files:** scratchpad harnesses only — no product change.

## [V4-PAGE-CONTRADICTED-ITSELF] Two sections read the same two numbers opposite ways — 18:10 AEST
**Symptom:** on 11 Placid Court, the page said BOTH of these about the identical figures (26 days
against 21; 23 homes, +44%):
  * `#timing`  — "Both readings moved the same way: selling took longer than it did a year ago, and
    there is more on the market than there was a month ago."
  * `#now`     — "Both readings are true and they support opposite conclusions."
**Root cause:** `#now` carried an older hardcoded "Two true things point in different directions"
block. When I made the timing section derive that direction per suburb, I fixed the copy in one
place and left the other. For Burleigh Waters the two readings genuinely do diverge, so the page
looked right on the property I kept testing — and was wrong on the other two.
**Fix:** removed the block entirely rather than duplicating the direction logic. `timing_answer()`
owns days-on-market and active listings and gets the direction right per suburb; `#now` is about the
COMPETITOR SET, which is its distinct job. Two sections narrating one pair of numbers is how they
drift apart.
**Also fixed:** "1 homes a buyer could compare" — no pluralisation. And "the wider area" is now "the
wider comparison catchment", which is what the aperture actually is.
**Recurrence:** 22nd of the family, and the first where the page contradicted ITSELF rather than the
data. Found only because Will pointed at a section on a property I had not been testing against.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [OFFMARKET-INTENT-ALERT-NOISE] High-intent Telegram alerts fired on bots/self, not people — 14:20 AEST
**Symptom:** Will reported the "👀 High-intent off-market view" Telegrams were surfacing bot/non-human
activity rather than real prospects. All 30 signals ever recorded were `reached_end`; zero were `cta_click`.
**Root cause:** Three independent defects, none of them actually bots.
  1. `DiscoveryDeck.tsx` fired the alert from the per-card effect on `active >= total - 1`. On a
     **single-card deck that is true at index 0 on mount** — the alert fired before the visitor did
     anything (24 `deck_exit` events with `total_cards: 1` in the 30 days to today). On 3-card decks it
     tripped after ~8s of flicking (`019fcb4c` hit "100%" in 8s, `019fca5b` in 10s).
  2. `maxIdxRef` / `prevActiveRef` were never reset when `doc.slug` changed, so counters carried across
     SPA navigation between decks — PostHog holds `reached_pct` values of **111%, 113%, 133%**.
  3. `offmarket-intent-alert.mjs` applied NO filtering at all, while the CRM path has had
     `INTERNAL_IDS` + `BOT_CITIES` in `crm_sync.py` all along. Will's own distinct_id
     `019d03c0-…` — literally listed in `INTERNAL_IDS` — generated alerts. A 4th internal device
     (`019d24b3-da5d-…`, /ops ×26, for-sale-v2/v4b prototypes) was not even in that list.
**Note on the "bot" framing:** every user agent behind the 30 alerts is a real browser (Mobile Safari,
Chrome, Firefox, Google Search App), all Australian geo. PostHog's bot filter is UA-based and would
have removed **none** of them. The noise was internal devices + a trigger that fired on page mount.
**Fix:** Moved the `reached_end` evaluation out of the per-card effect into `emitExit`, where real dwell
is known, gated on `total >= 6` cards AND `>= 45s` on the deck (median deck dwell is 6s). Reset the
per-deck refs on slug change. Added a server-side `suppressionReason()` re-checking the same floors plus
`INTERNAL_DISTINCT_IDS` and `BOT_CITIES` (Netlify `context.geo.city`). Deliberate `cta_click`/`save_link`
still alert immediately with no floor. Every beacon is still RECORDED with `suppressed_reason` so the
floors can be re-tuned. Added the missing 4th internal id to `crm_sync.py`.
**Verified:** 10/10 unit cases on `suppressionReason`; replaying the 30 historical signals suppresses
**21** (13 deck_too_short, 5 internal, 3 dwell_too_short), leaving 9 genuine 8-9 card reads.
`npx vite build` clean; `tsc -p tsconfig.app.json` clean for the touched file.
**Files:** 01_Website/src/pages/OffMarketPage/discovery/DiscoveryDeck.tsx,
01_Website/netlify/functions/offmarket-intent-alert.mjs, scripts/crm_sync.py
**Recurrence:** First for this alert. But it is the Nth instance of the family "a new signal shipped
without the filtering the rest of the stack already had" — the filters existed in crm_sync.py and the
Netlify function simply never imported the idea.

## [V4-EVIDENCE-CARDS-BUILT] Comparable cards now render from the real resolver — 18:45 AEST
**Will:** "are you going to actually build in these components?" — fair. A placeholder saying "this
ships as ValuationEvidence" is a note, not a build, and it left a hole where the most important
evidence on the page belongs.
**What was actually possible.** A React component cannot drop into a Python HTML generator, but its
DATA can: `slot_resolver.valuation_evidence_from_engine()` returns the exact payload
`ValuationEvidence.tsx` consumes, and it runs unchanged on off-market documents. So the prototype
now renders that payload rather than a hand-rolled subset — rank badge, attributes, SOLD vs ADJUSTED
TO YOURS, distance/recency/weight %, verified flag, the engine's own narrative, photo strips, and an
expandable per-feature adjustment grid with the six weight factors as bars.
Result: **8 cards per property, 32 thumbnails, 0 broken**, 3 shown with the rest behind "See all 8".
Photos arrive as `{thumb, full}` already normalised to the CDN by the resolver — no host rewriting
needed.
**This is not a reversal of the port plan.** The design is not being decided here; the component
still ships. What changed is that the review artefact now reads end to end instead of having a gap
in the middle.
**Caught while building:** the intro promised "Tap any one for the line-by-line working" — but
outside the $1M-$2M envelope the resolver returns None (no reconciled range, by design), the page
falls back to simple cards with nothing to expand, and that line became a promise the page does not
keep. Now gated on the rich cards actually rendering. Verified consistent on all five: 30 Whitehead
(out of envelope) shows the plain line, the other four show the tap promise and have the tap.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [V4-TYPE-HIERARCHY] Headings gave the reader no structure — 19:20 AEST
**Symptom (Will):** "main heads and subheadings are all the same font and font size, combined with
the same page styling for every section — difficult for the reader to understand the relationship
of sections and where new sections start."
**Root cause, and it was a real bug not a taste call:** `h3` had **no `font-size` rule at all**. It
inherited the `h1,h2,h3` group — same serif, same weight, same letter-spacing — so it rendered as a
slightly smaller h2. Sections were divided only by a 1px `--line-2` hairline, which at 54px spacing
the eye does not register when every section is otherwise identical.
**Fix — told apart by FAMILY as well as size, so it reads at a glance:**
  * `SERIF = a section of the page` (h2, 28.5px). `SANS = a sub-part inside a section` (h3, 15px).
    An absolute rule rather than a size gradient that has to be measured.
  * Section rule strengthened to `--line`, with a 34px accent stub sitting on it — a visible "a new
    part starts here" that costs no vertical space and no colour beyond the one accent.
  * The eyebrow is the section's NAME, so it now reads as a label: accent-coloured, heavier,
    wider-tracked. It was muted grey fine print.
  * Spacing up to 66px top / 58px bottom.
**Also removed — stale placeholders.** The MedianPriceChart and SeasonalityStrip sections still
carried "Ported component — placeholder" labels above content I had since built for real (the full
median series with scrubbing; the seasonality strip with tap-to-detail). A placeholder label over
working content is simply wrong, and worse, it told the reviewer not to judge something that IS the
thing. All five pages now carry zero placeholders with the chart, strip and evidence cards all live.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [V4-PART-LEVEL] Fourteen peer sections, no parent — 19:45 AEST
**Symptom (Will):** "the subheading 'What has happened since the last recorded sale' appears as the
same size as the major heading 'Is now the right time to be selling'."
**The CSS was working; the STRUCTURE was wrong.** Both are `<h2>` — I made them peers, so they
rendered identically and correctly. The page carried **fourteen h2 sections and nothing above any of
them**. There was no hierarchy to perceive because none had been built: every section was a peer of
every other, so a reader had nothing to hang a map on. Adjusting font sizes could not have fixed
that — it would have made arbitrary sections look important.
**Fix — a level ABOVE sections, taken from the page's own opening copy.** The page names three
questions at the top and then answers them in order, so the parts are those questions:
  * **The number** — "Is the number attached to this home real?" (opens at `#which`)
  * **The timing** — "Is now the right time to be selling, or should I wait?" (opens at `#since`)
  * **What you can do about any of it** (opens at `#correct`)
Rendered as a chapter opening: full-bleed tone change to `--stone`, 38px serif (3.1rem desktop), the
question beneath, and the following section's rule suppressed so the part and its first section read
as one unit.
**Three levels, now measurable:** part 38px serif / section 28px serif / sub-part 15px sans.
**Deliberately NOT a progress indicator** — no "part 2 of 3", no bar. The brief rules that out
("turns curiosity into homework"). A part is a place, not a percentage.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [V4-PART-POSITION] "The timing" opened on the wrong section and echoed itself — 20:05 AEST
**Symptom (Will):** "'the timing' headline is in the wrong position."
**Two faults, both mine:**
  1. **Wrong boundary.** The part opened at `#since` — "What has happened since the last recorded
     sale" — which is about this home's VALUE, not about when to sell. It belongs to "The number".
     The part now opens at `#timing`, so it begins on the section that actually asks the question.
  2. **The part echoed itself.** Its subtitle is "Is now the right time to be selling, or should I
     wait?" and the section beneath carried the identical sentence as its `<h2>`, a screen apart.
     A part now suppresses a following `<h2>` that merely repeats its own subtitle — the part asks
     the question, the section answers it. Applied generically, so it cannot recur as parts move.
**Follow-on caught by looking:** with the h2 gone, the eyebrow "The second question" sat above body
text with nothing under it, and was restating POSITION in a part already named for that question.
Relabelled "The honest answer" — which is what the section actually contains.
**Structure now:**
  * The number — which · answer · comps · different · reliable · dispersion · since
  * The timing — timing · now · buyer
  * What you can do about any of it — correct · plan · next
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [V4-STALE-BUNDLE-RANGE] The headline range disagreed with its own evidence — 20:35 AEST
**How it surfaced:** Will asked why the reliability caveat had its own section. Making that section
conditional on a measured error rate exposed that TWO properties were taking the no-rate branch
while rendering ENGINE comparables underneath — which is impossible if the range came from the
engine.
**Root cause:** the renderer read the range from the CACHED bundle. Most bundles were built before
the off-market book was valued, so their `valuation` fell through to `exterior_evidence` and stayed
there, while the document itself now carries a high-confidence engine range:
    11 Placid    bundle $1,462,626-$2,061,774   live $1,470,491-$1,871,534
    3 Fimiston   bundle $1,736,360-$2,447,640   live $1,607,377-$2,045,752
**$400k apart at the top end on Fimiston** — and the number at the top of the page disagreed with
the comparables printed below it. The nightly bundle rebuild would eventually have corrected it,
which means it would have looked fine on whichever properties happened to be fresh.
**Fix:** the live `valuation_data.confidence` now overrides the bundle's valuation whenever it
carries a reconciled range. Reading the authoritative source rather than waiting for a cache to
catch up. Verified: all four in-envelope properties now show the engine range, the reliability
section and engine cards together; 30 Whitehead (outside the envelope) shows the fallback range with
no section and no cards.
**The original question, answered:** reliability is a SECTION where there is a measured error rate —
it is one of the brief's eight moments and carries the figure, the scale and the full test. Where
there is no rate there is only a caveat, and a caveat promoted to its own section reads as more
important than the number it qualifies. It is now a disclosure directly beneath the range, and the
"How reliable has this been?" button only renders when there is a section to reach.
**Recurrence:** 23rd of the family — a cached value presented as current.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [V4-RANGE-METHOD-CONTRADICTION] "44 sales built the range" vs "not built by that method" — 21:10 AEST
**Will's review, flagged as "the one major logical contradiction I would fix before almost anything
else":** the page said "44 relevant sales influenced the range. The 8 strongest carried most of the
weight" and, further down, "the range above was not built by the comparable-sales method." An
attentive reader thinks *"hang on, you just told me 44 sales built it"* — and the credibility
advantage the whole page rests on disappears.
**Two separate causes, both now fixed:**
  1. **Stale bundle range** — 4 of 5 properties were showing a cached `exterior_evidence` range
     while rendering engine comparables. Fixed earlier today ([V4-STALE-BUNDLE-RANGE]); the live
     valuation now wins.
  2. **The basis line was unconditional** — it claimed the comparables built the range even when
     they had not. Now rendered only when the engine produced the range. On a fallback range it is
     replaced by Will's own framing: *"This home sits outside the band our tested comparable-sales
     model operates in, so this is a broader evidence range rather than that model's output. The
     strongest nearby sales are still below — what we cannot responsibly do is attach our measured
     error rate to it."*
**Also:** the evidence section headline changes from "The sales behind that range" to "The strongest
sales near this home" when the engine did not build it, and adds "These sales inform the picture;
they did not generate the range above." The comparables become evidence rather than pretending to be
the generator — which, as Will noted, is the more impressive position: it shows Fields knows where
its model stops.
**⚠ My first fix targeted the wrong code path** and reported success while the contradiction
survived on 30 Whitehead. Caught only because the verification checked the RENDERED page rather than
trusting the edit. Verified clean on all five.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [V4-OPENING-V3] The portal disagreement becomes the inciting incident — 21:50 AEST
**Will's brief:** "the opening shouldn't merely mention the portal disagreement; it should make that
disagreement the inciting incident of the whole experience." Everything downstream depends on the
visitor feeling, within seconds, that the page understands why they came.
**v3 (v2 preserved and re-renderable via `--version v2`):**
  * The hook moves INTO the hero, immediately after the address confirms we have the right home:
    *"You've probably already seen two different values for this home."* / **"So which one should
    you believe?"**
  * The three questions are no longer raised up front. Raising three at once dilutes the one the
    visitor actually arrived with, and the page cannot answer the other two until it has earned the
    right to. They are introduced later, where they are about to be answered.
  * **"Why is the range this wide?" now sits directly beneath the number** — that is the real next
    thought, not "see the comparisons".
**The honest answer to "why so wide", and why it is not property-specific:** the range is a flat
±12% set from the method's MEASURED accuracy across 1,800+ sold homes. Inventing a bespoke reason
per property would be a better story and a false one. What IS property-specific is the list of
things we could not see, so both are said — separately.
**Three faults caught by reading the RENDERED page, all in copy written minutes earlier:**
  1. *"…internal condition, the quality of any renovation and the bathrooms"* — a category error.
     Condition cannot be seen from the street; a bathroom count is simply missing from the record.
     Now two sentences, and the second only appears when something IS missing.
  2. *"which sales pulled the answer to where it sits"* rendered on the FALLBACK property, where the
     sales pulled it nowhere — rebuilding, in new words, the contradiction fixed forty minutes
     earlier.
  3. The part subtitle *"Is the number attached to this home real?"* restated the hook two lines
     below it — orphaned copy carried over from the v2 question list.
**Acceptance test now runs across three property states** (Will's instruction), and this is the
standard for every copy/data-state change from here: clean live-engine (28 Wedgebill), fallback
outside the model band (30 Whitehead), incomplete facts (8 Lomond Court — bedrooms AND bathrooms
missing). All three openings read coherently without the reader knowing anything about the backend.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [V4-COPY-PASS-3-5] Differences in words; legislation demoted; refusal reframed — 22:20 AEST
**Items 3-5 of Will's review.**
**3 · Adjustment arithmetic collapsed.** The engine's narrative — "82.0 m² smaller land ($-36,900);
60.0 m² larger floor area ($+226,080)…" — is exact and unreadable as a default. Cards now lead with
two to four differences IN WORDS and no dollars ("a far smaller house · 1 fewer bedroom · a weaker
recorded condition · a larger block"), ranked by dollar impact so the biggest movers are the ones
named. The exact narrative and the full grid move inside "See all N adjustments". Both audiences
served.
**⚠ Two faults in that new code, caught by checking the words against the numbers:**
  * **Inverted polarity.** `diff` is subject minus comp, so a positive diff means the COMPARABLE has
    less. The card describes the comparable, so the sense inverts — the first version called a
    685 m² comparable "a smaller block" against a 603 m² subject. Exactly backwards, in the section
    whose entire job is showing our working. Verified after the fix against the raw adjustment rows.
  * **"A different position" led every card.** Position/street/beach adjustments are frequently the
    largest dollar mover, so the ranking put an uninformative phrase first on all three cards.
    Dropped from the word list; the dollars remain in the expanded working where the label names the
    specific factor.
**4 · Legislation demoted.** "three comparable sales is the statutory Statement of Information
standard in Victoria and the incoming NSW regime, so it is not a straw man" is argument aimed at a
critic, not at a homeowner — it made the page briefly read as a research rebuttal. Main path now
reads "The trouble with using only three sales is that the answer becomes highly sensitive to which
three are chosen." The statutory point moves into the disclosure for anyone checking the premise.
**5 · "We are not going to" reframed.** It read as refusal; an adviser should help someone decide.
What we decline is pretending the forecast settles it. Now: "None of that tells you what you should
do by itself. The useful question is how these conditions interact with your home, your timing, and
what you would need to buy next — which is where we can actually help."
**Verified across all three property states.**
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [GH-READBACK-RACE] md5 verification can false-negative straight after a PUT — 22:30 AEST
**Symptom:** both files in a push loop reported "✗ MISMATCH". Checking a moment later, local and
remote md5 were identical — the pushes had succeeded.
**Cause:** the loop read the file back immediately after the PUT. GitHub's contents API can serve a
STALE blob for a second or two after a write, so the compare runs against the previous version.
**Why it matters:** the md5-verify habit exists because `gh api` PUT can report success while
pushing wrong bytes (see `gh_api_large_file_gotcha`). But acting on a single failed compare by
re-pushing is its own risk — it can clobber a concurrent change. **Check, wait, check again**; only
treat it as a real mismatch after it persists.
**Files:** none — process note.

## [V4-THREE-ACTS] Structural pass — three acts, primary path 27% shorter — 23:10 AEST
**Range wording made statistically safer (Will).** "The measured spread of the method itself"
implies a defined prediction interval. It is not one — the flat ±12% contains the eventual sale
price 41-67% of the time depending on suburb. Now: "We set the width using how far estimates like
this one have historically missed the eventual sale price."
**Opening part label dropped.** A part titled "The number" above the hero made the reader parse the
page's taxonomy before its meaning. Acts II and III keep theirs, because there the reader IS
changing subject.
**Three acts:**
  * **ACT I** (no heading — the hero opens it) which · answer · nearby · comps · reliable ·
    dispersion. Ends where the visitor's original Google problem is answered. That closure is the
    reward.
  * **ACT II "What it means now"** since · different · timing · now · buyer · correct.
    `#different` moved OUT of Act I: it is about the property, not about whether the number can be
    believed.
  * **ACT III "What it would mean for you"** plan · next.
**The compression was one section, and measurement said so.** `timing` ran **994 words — a third of
the entire primary path** — walking through Westpac, the RBA, national indices, Brisbane, local DOM,
listing volume, rolling medians, quarterly caveats and fifteen years of seasonality before saying
anything useful. Rewritten to Will's reframe: *you don't need to predict the next six months to make
a good decision*, then three things you CAN know (how fast homes are selling · how much choice
buyers have · what selling would let you buy next), then "the third usually matters more than
whether a national index moves two per cent". **994 -> 176 words.** The forecasts, the median series
and the seasonality strip all moved intact into three drawers.
**Result: primary path 2,993 -> 2,175 words (27% shorter); drawers 1,616 -> 2,429. Nothing deleted.**
**⚠ I DELETED SIX FUNCTIONS AGAIN.** Slicing between `def timing_answer` and `def median_block`
took `evidence_cards`, `differences_in_words`, `_phrase`, `_ago`, `excluded_sale`,
`market_insights`, `median_chart` and two label tables with it — the identical mistake logged this
morning as [SLICE-EDIT-ATE-ADJACENT-FUNCTIONS]. Recovered from the last pushed copy on GitHub,
which is the argument for pushing after every verified step. **Never slice between two function
anchors.** Restoring twice then left duplicate definitions, which Python silently accepts — found by
counting definitions, not by the renders passing.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py

## [V4-STRUCTURAL-RESIDUES] Sequencing leftovers from the act restructure — 23:55 AEST
**Will's data-state check, answered first:** the figures I quoted (29 days vs 37; 32 homes, −27%)
were Burleigh Waters. 11 Placid correctly renders Varsity Lakes (26 vs 21; 23 homes, +44%). Both
verified against `market_pulse`. No stale-value bug.
**Root cause of the stale cues.** The rechainer left a cue alone whenever its target was "forward" —
but after the act restructure, forward is not the same as NEXT. The cue after `#comps` still pointed
at `#different`, now two acts away, so it asked a question the following section does not answer.
The page's whole pull mechanism depends on the arrow resolving immediately. Now always re-points at
the section that actually follows. Verified: all nine cues resolve.
**"Since then" moved into the hero** as a drawer on the last-sale line — the historical movement now
sits beside the fact that provokes the question, and **Act II opens on "This home"**, which is what
its heading promises.
**"This home" rewritten.** Was string-joined from anchors ("4 bedrooms and 914 m² of land and 255 m²
of internal living and a pool do not appear together often"). Now: *"It isn't any one feature. It's
the combination."* with the counts doing the persuading — "Of the 191 homes on the market across the
comparison area, 15 broadly match that combination. Only 4 also sit this close to a park, childcare
and a supermarket."
**Buyer claims de-escalated.** "Local family upgraders carry the price" asserted a fact we have no
buyer-origin data for; now framed as "One buyer group likely to value this combination more than
average". "To them, it's the one they've been waiting for" was advertising copy after an otherwise
disciplined page — replaced with the economic point.
**Closing handoff now names the whole journey** rather than only Act I, so the last question reads
as the one remaining piece.
**Also:** "the third usually matters more… the portals never touch it" softened to a claim we can
defend; "guessing at the half" → "the part" (false precision).
**Primary path 2,993 → 2,116 words.** Not chasing 35%; the remaining weight is spread thin.
**NEW — `check_invariants.py`.** Will: "successful render is necessary but no longer sufficient."
Asserts what a passing render cannot: no duplicate top-level definitions (Python takes the last
silently), every required function present exactly once, no dead anchors, and every forward cue
resolving to the section that actually follows. All three failures from today would have been caught
by it.
**Files:** 15_Off-Market/Page_Redesign_V4/Prototypes/render_prototype_a.py,
15_Off-Market/Page_Redesign_V4/Prototypes/check_invariants.py

## [SEO-SUBURB-PAGES-ORPHANED] /houses-for-sale/:suburb had zero internal inbound links — 18:45 AEST
**Symptom:** Will asked how SEO is going for the listings pages. Over 28d (8 Jul–4 Aug) the whole for-sale family took 231 of the site's 24,443 impressions (0.9%) and 3 of 569 clicks (0.5%). The three `/houses-for-sale/:suburb` pages have been shown for exactly 7 distinct queries since launch — all of them street addresses. No generic commercial term ("houses for sale robina" etc.) has ever surfaced them. GSC URL Inspection returned `referringUrls: None` for all three despite `Submitted and indexed`.
**Root cause:** Two independent faults.
(1) The suburb cross-link `<nav>` in `DecisionFeedV3Page.tsx` sits inside the `if (loading)` return. Once the route loader began seeding `initialFeed`, `loading` is `false` on both server and client whenever the feed API succeeds, so that branch stopped executing and the links rendered nowhere — an unintended regression from the SSR-seeding change, not a missing feature. The pages were left discoverable only via sitemap.
(2) Every other internal link that should have pointed at them pointed elsewhere: the homepage's three "Current listings — <suburb>" rails all linked to `/for-sale-v3`, and every `/property/*` breadcrumb linked to `/for-sale?suburb=X` — a page with no loader at all, whose SSR HTML contains zero listings, zero `/property/` links and an `<h1>` of "Properties". Its `ItemList` JSON-LD is built client-side so it is absent from server HTML too.
**Fix:** Extracted the suburb nav to a single `suburbSeoNav` const rendered on all three branches of `DecisionFeedV3Page` (loading, error, main), with a `.suburbSeoBar` wrapper for the main branch where it sits under the hero. Repointed the homepage rails per-suburb. Moved the property-page breadcrumb parent to `/houses-for-sale/<Suburb>` with anchor text "Houses for sale in X" instead of "X Properties". Added guarded helper `housesForSaleHref()` to `suburbNormalize.ts` — returns the canonical Title-Case-hyphen slug only for the three suburbs that actually have a landing page, else null so callers fall back to the old feed URL; without the guard, property pages in Nerang/Mermaid Beach/etc. would have linked into the route's soft-404 (HTTP 200 + noindex). Also aligned the BreadcrumbList JSON-LD in both `PropertyPage.tsx` and `routes/property.$id.tsx` to emit the identical URL to the rendered anchor, fixing a pre-existing `?suburb=Robina` (JSON-LD) vs `?suburb=robina` (anchor) casing mismatch.
**Verification:** `tsc -p tsconfig.app.json` — note bare `tsc -p tsconfig.json` checks ZERO files here (solution-style config, see memory `website_verification_gates`); the app config covers 518 src files. Codebase has 210 pre-existing errors; diffed against a GitHub-fetched baseline of the same six files: 0 new error messages, 0 removed, the 10 apparent deltas were PropertyPage line numbers shifting +21. `npm run build` clean; confirmed the three literal suburb hrefs plus `Houses for sale in ${suburbName}` compiled into the SERVER bundle. Unit-tested the helper: 8 casing variants of the 3 core suburbs map correctly, Nerang/Mermaid Beach/Gold Coast/Burleigh Heads/""/null/undefined all fall back.
**Files:** Feilds_Website/01_Website/src/utils/suburbNormalize.ts, src/pages/DecisionFeedV3Page/DecisionFeedV3Page.tsx, src/pages/DecisionFeedV3Page/DecisionFeedV3Page.module.css, src/pages/BrowsePage/BrowsePage.tsx, src/pages/PropertyPage/PropertyPage.tsx, src/routes/property.$id.tsx (single commit 2e212c34 via push_website_files.py)
**Recurrence:** First for this specific orphaning. 2nd of the broader class "a page renders only in a branch that no longer executes" — same shape as [GEO-BLOCK-INSPECTIONTOOL] (2026-07-31), where a guard's allowlist silently stopped covering the case it was written for. Pre-deploy GSC baseline saved to logs/seo_linking_baseline_2026-08-07.json for 7/14/28d comparison.

## [GPT-PEER-DUAL-KEY] Claude×GPT collaboration harness — funded GPT key shadowed by a dead env key — 19:05 AEST
**Symptom:** Building the Claude×GPT editorial pair harness in `16_General_Reinforcement_Learning/AI_Collaboration_Experiement`. A bare `gpt-5.6-terra` probe returned `OK` but every real prompt (~2k tokens) returned `429 insufficient_quota / credit_balance_exhausted`. After Will added $30 to the OpenAI console the 429 persisted, while raw calls with the token-file key succeeded 10/10 at the same size — so the failure looked like a billing/propagation problem for several rounds when it was not.
**Root cause:** Two different OpenAI keys exist on this VM for the same provider. `OPENAI_API_KEY` in the shell environment / `.env` (tail `…GQWLIA`) is credit-exhausted; the `GPT API:` entry in `00_Run_Commands/gh-token-29Mar.txt` (tail `…8LqCIA`) is the funded one Will tops up. `gpt_peer.load_api_key()` read env first, so every call went to the dead key. Two things kept this disguised: (1) a credit-exhausted account still answers tiny calls, because the quota reservation scales with prompt + `max_completion_tokens` — so a `"say OK"` smoke test returns 200 while a real prompt 429s, and my initial probe was exactly that shape; (2) the funded key is scoped to the non-default "Personal" org, so an explicit `OpenAI-Organization` header for the default "Fields Real Estate" org returns 401, which reads as a third unrelated fault. A mid-search binary probe also briefly reported a ~191k-token ceiling using the raw funded key, appearing to contradict the harness's own 429 — that contradiction was the clue that the two paths were using different credentials.
**Fix:** `resolve_route()` now reads the token file first and the environment second, invertible with `GPT_KEY_SOURCE=env`. Added `--health-check`, which pads to ~2k prompt tokens deliberately so a dead route cannot pass as healthy. Added an empty-reply guard that raises with `finish_reason` rather than returning `""` as a silent pass. OpenRouter (`openai/gpt-5.6-terra`) wired as fallback but its balance is **negative** ($13,748.48 credited / $13,748.62 used) so it 402s until topped up; direct is default. All three traps documented in the `resolve_route()` docstring and in `00_CONCEPT.md` §10.
**Files:** 16_General_Reinforcement_Learning/AI_Collaboration_Experiement/{00_CONCEPT.md, lib/gpt_peer.py, prompts/constitution.md, prompts/gpt_role.md}
**Recurrence:** First for the dual-key shadowing specifically. **5th for the broader class "provider credit exhausted, surfaced as an unrelated-looking failure"** — after [STEP105-117-ANTHROPIC] (2026-07-15), [EDITORIAL-MAX-ROUTING], [ARTICLE-WF-CREDIT], [HOW-IT-SOLD-MAX]. Every prior instance was resolved by migrating off the metered API to Claude Max; this one has no Max equivalent because the whole point is a non-Anthropic second model, so GPT work is inherently metered and will keep hitting this. Standing lesson: **never accept a small-prompt probe as proof a metered route is usable.**

## [BACKTEST-STAGE-INSTRUMENTATION] Trace where valuation bias enters the pipeline — 19:29 AEST
**Symptom:** The comparables method runs a median 6.2% low inside the design envelope, and the
honest 80% band is $600K wide on a $1.6M home. No instrument existed to say which stage caused it.
**Root cause:** `valuation_backtest.py` recorded only the final estimate. `included_points` is
discarded after weighting, so the raw pool, the adjusted pool and the weighted result were
indistinguishable in the output. A first attempt read the raw price from `sale_price`; the key on
an enriched point is `price`, so `raw_comp_median` came back null for every row.
**Fix:** Added `_stage_median()` and `_comp_spread()`, computing per-property stage medians, the
adjusted-comp IQR/CV, pool min/max and comp distance while `included_points` is still in scope, and
widened `--dump-errors` to carry them. Found: the pool sits 5.3% low before adjustment; the
adjustment step pulls the top of the pool down 6.1%, so 45% of homes sell above their dearest
ADJUSTED comp against 12% expected, and the sale lands at the 91st percentile of its own pool.
**Files:** scripts/valuation_backtest.py,
15_Off-Market/Page_Redesign_V4/Product/11_BAND_WIDTH_INVESTIGATION.md
**Recurrence:** First

## [OWNER-ARTICLE-MEDIAN-BYPASS] Posted owner articles used a Domain-only median and called it "independently measured" — 20:40 AEST
**Symptom:** `scripts/owner_article/build_owner_article.py` — the article POSTED to a homeowner's address — computed its own rolling 12-month suburb median instead of reading the promoted union pipeline, and understated every core suburb: Burleigh Waters $1,800,000 vs the correct $1,925,000 (**-$125,000**), Varsity Lakes $1,300,000 vs $1,400,000 (**-$100,000**), Robina $1,455,000 vs $1,490,000 (-$35,000). YoY was off by up to 1.2pp. The copy described the figure as "independently measured", which reads to an owner as *corroborated* when it meant the opposite.
**Root cause:** `suburb_median_series()` queried `Gold_Coast.<suburb>` `listing_status:"sold"` directly — Domain-only, the source we know misses 40-50% of sales — and excluded dwellings by substring match on `property_type`, a different classifier from the union pipeline's shared one. Where `property_type` is blank or worded differently, units and townhouses survived the filter and dragged the median down; that is why the old path returned MORE sales (BW n=179, VL n=153) yet a LOWER median than the union (n=167, n=111). It carried no CI and no provenance, and never referenced `precomputed_indexed_prices`, `median_source` or `median_computed_at`, so `check_union_median_integrity.py` — which guards only that document — could pass green while this path was wrong. The original docstring's reasoning was sound but over-applied: the rolling *series* is sparse and must not be indexed positionally, which argues against walking `rolling_12m_median_series`, not against reading the promoted *scalars* that `precompute_union_prices.py` writes for exactly this purpose.
**Fix:** `suburb_median_series()` now reads `Gold_Coast.precomputed_indexed_prices` and returns the promoted scalars (`rolling_12m_median_price`, `rolling_12m_yoy_pct`, `rolling_12m_median_sample_n`, CI low/high/margin). **Provenance is gated:** if `median_source != "domain_union_onthehouse"` or any scalar is missing it returns `None` and the caller omits the suburb-median passage entirely — omission, not fallback, because falling back to a weaker median is exactly how the $2,115,000/+23.6% Burleigh Waters retraction happened. Dropped "independently measured" for "across N sales matched between Domain and onthehouse". Added the 90% CI to the limits section, which is stronger disclosure than the bare `n` it had before.
**Verification:** all three core suburbs now return values identical to `logs/union-median-integrity.log` (Robina $1,490,000 n=265; Burleigh Waters $1,925,000 n=167; Varsity Lakes $1,400,000 n=111); a non-core suburb (Nerang) correctly returns `None` and omits the passage.
**Discovered by:** the Claude×GPT collaboration experiment — `gpt-5.6-terra` given a read-only shell on this VM found it unprompted in 41 tool calls while auditing the market-data pipeline, and correctly refused to confirm the surrounding "overwritten three times" claim on the grounds that code comments are not proof of a count. First substantive finding from that harness; see `16_General_Reinforcement_Learning/AI_Collaboration_Experiement/`.
**Files:** scripts/owner_article/build_owner_article.py
**Recurrence:** First for this file. **4th of the class "a second code path computes a metric the guarded pipeline already owns"** — after the three `precompute_indexed_price_data.py` `replace_one` reversions logged under `[UNION-MEDIANS-REVERTED-NIGHTLY]` and related IDs. The standing lesson those entries drew was "guard the document"; this one shows guarding the document is not enough, because a consumer that never reads the document cannot be protected by a check on it. **What actually generalises: every consumer of a guarded metric must read the guarded field and assert its provenance, or omit the claim.**

## [VMAGENT-LOOP-BUGS] Collaboration harness — three bugs that silently degraded or killed every audit run — 22:15 AEST
**Symptom:** Ran four GPT audit experiments concurrently (`E1_cost`, `E2_growth`, `E3_correctness`, `E4_unknown`). All four crashed at 43-50 tool calls — *after* the expensive investigation was complete but *before* producing any findings. Separately, three of the four reported in their own COVERAGE section that they could not inspect the database at all, and several burned tool calls on grep timeouts.
**Root cause:** Three independent bugs in `lib/vm_agent.py`, all mine:
1. **Missing tool outputs on budget exhaustion.** When `res.calls >= max_calls` the loop appended a "give your final answer" user message and `continue`d, without appending a `function_call_output` for the function calls still pending from that turn. The Responses API requires every `function_call` to be answered, so the next request 400'd with `No tool output found for function call`. This is what killed the runs, and it killed them at the worst possible moment — maximum spend, zero output.
2. **Wrong pymongo kwarg.** `coll.find(..., maxTimeMS=45000)` raised `Cursor.__init__() got an unexpected keyword argument 'maxTimeMS'` on every single `find`. pymongo's `find()` takes snake_case `max_time_ms`; only `count_documents` and `aggregate` take the camelCase form. So the Mongo tool worked for aggregations and silently failed for every document read. E3 and E4 both named this in MOST LIKELY BLIND SPOT — the agents correctly reported the harness was broken and I had shipped it anyway.
3. **No rate-limit backoff.** Four concurrent agents each resending 60-70k-token histories hit the org TPM ceiling (500,000/min); the SDK's default 2 retries were not enough.
**Fix:** (1) budget-exhausted branch now emits a `DENIED: tool budget exhausted` output for every pending call before the nudge; (2) `find()` uses `max_time_ms`; (3) added `_create()` with explicit exponential backoff on 429/5xx that deliberately does NOT retry `insufficient_quota` (out of money is not a wait-and-retry condition); (4) recursive `grep`/`rg` now auto-inject `--exclude-dir` for node_modules/.git/__pycache__/dist/build and the timeout rose 30s→60s, because the model was spending finite tool budget rediscovering that a repo-root grep times out; (5) added `finalize_from_transcript()` so a crashed run can be recovered by replaying its recorded tool calls as context and asking only for the final answer — this recovered all four runs for **$0.29 total** instead of re-running them.
**Verification:** all four salvaged runs produced findings; guard suite re-confirmed credential contents, `python3`, `rm`, `gh`, `curl`, Mongo `$out`/`$merge` and private-range HTTP still blocked.
**Files:** 16_General_Reinforcement_Learning/AI_Collaboration_Experiement/{lib/vm_agent.py, salvage.py}
**Recurrence:** First. **Standing lessons:** (a) an agentic tool loop must answer every tool call the model makes, on every exit path, or it fails at maximum cost; (b) record the transcript to disk as you go — the salvage path only existed because tool output was already durable; (c) **when an agent reports in its own output that a tool is broken, believe it** — E3 and E4 both said so plainly and that was the fastest route to the bug; (d) run tool-using agents serially unless the TPM ceiling has been checked, since cost and token-rate both grow quadratically in tool calls.

## [VALUATION-SUBJECTIVE-ADJ-NOISE] Three adjustment attributes carry no signal — 19:59 AEST
**Symptom:** Comparables method runs 6.2% low with an honest 80% band of $604K on a $1.6M home.
**Root cause:** Attribute ablation over 4,916 comparables shows the AI-derived quality adjustments
(kitchen, renovation_quality) have an optimal multiplier of **0.00** and renovation 0.25 — they add
noise. Physical attributes (floor_area, property_age) are calibrated at 1.00. The six-factor
weighting scheme also underperforms an unweighted median of the same adjusted comps.
**Fix:** Not yet applied to production — findings recorded. Dropping the three subjective
adjustments plus per-suburb offsets takes MAE 10.83%->9.52%, within-10% 59%->64%, and the 80% band
$588K->$501K. Also disproved: "fewer, higher-quality comps" (best-3 is WORSE than all-8) and
adaptive band width (loses to a flat band).
**Files:** scripts/valuation_backtest.py,
15_Off-Market/Page_Redesign_V4/Product/11_BAND_WIDTH_INVESTIGATION.md
**Recurrence:** First

## [VALUATION-PROXIMITY-ADJ-DEAD] beach_proximity + golf_course_backing never fire — 20:11 AEST
**Symptom:** Worst 15% of valuation errors mention canal/waterfront (11% vs 5%) and golf (7% vs 3%)
far more than the rest of the sample.
**Root cause:** Both `beach_proximity` and `golf_course_backing` adjustments fire on **0 of 4,916**
comparables — dead code. `water_views` fires on 1.1% but is worth a median $228,668 when it does.
Proximity value is not captured at all. Separately, unit-numbered addresses (MAE 18.0% vs 10.3%) and
homes missing 3+ of 4 core facts (MAE 19-26%) are being valued by the detached-house method.
**Fix:** Not yet applied — findings recorded with the property-level list. Stacked effect of
per-suburb offsets + dropping the 3 subjective adjustments + refusing units/incomplete homes:
MAE 10.83%->9.12%, within-10% 59%->65%, 80% band $588K->$479K on 96.4% of homes.
**Files:** 15_Off-Market/Page_Redesign_V4/Product/11_BAND_WIDTH_INVESTIGATION.md,
15_Off-Market/Page_Redesign_V4/Product/12_OUTLIER_LIST.md
**Recurrence:** First

## [BACKTEST-PROXIMITY-FEATURES-MISSING] Backtest omitted beach/golf features — 20:33 AEST
**Symptom:** `beach_proximity` and `golf_course_backing` fired on 0 of 4,916 backtest comparables.
I reported this as dead production code. It was not — production fires them on 99.1% and 2.9%.
**Root cause:** `valuation_backtest.py` builds its OWN `comp_features`/`subject_features` dicts
instead of reusing the production builders, and both omitted `beach_distance_km` and
`golf_course_backing`, so `calculate_adjustments()` took the `skipped` branch every time. The same
divergence made `renovation_quality` fire on 79.9% of backtest comps against 0.2% in production,
invalidating part of the earlier ablation.
**Fix:** Import `resolve_beach_distance` + `detect_golf_course_backing`; resolve beach distance onto
each comparable's basic features; pass both keys to `calculate_adjustments`. Firing rates now match
production within 0.7pp on every attribute. Also added `is_attached_dwelling()` — units/townhouses/
duplexes are excluded from accuracy reporting by default (14 survived `--property-type House` and
ran MAE 18.0% vs 10.3%), each exclusion printed with its reason.
**Result:** beach_proximity earns its keep and is UNDER-adjusted (best multiplier 1.5). water_views
is the strongest adjustment we have (+1.13pp on the 24 homes it touches). golf_course_backing hurts
on its 46 homes but corrects the centre — sample too small to act on.
**Files:** scripts/valuation_backtest.py,
15_Off-Market/Page_Redesign_V4/Product/11_BAND_WIDTH_INVESTIGATION.md
**Recurrence:** First

## [VALUATION-WATERVIEW-AS-WATERFRONT] Water views classified as waterfront — 20:42 AEST
**Symptom:** Homes flagged waterfront run median error +8.0%, MAE 13.5%, 73% over-valued, against
-0.6% / 9.4% / 48% for the rest. Will spotted it reviewing the outlier list: they had water VIEWS but
were not waterfront.
**Root cause:** `detect_waterfront()` signal 1 is `property_valuation_data.outdoor.water_views` — a
GPT-4 Vision read of the marketing photos, answering "can you see water?" not "does this parcel touch
water?". That flag is broad by design because it drives a SUPPRESSION gate (cheap false positives),
but `precompute_valuations.py` reads the SAME flag to pick the comparable cohort, where a false
positive pools a view-only home against genuine water-frontage sales. 69% of flagged homes (41 of 59)
are not waterfront by geometry; those run median error +10.4%, MAE 14.9%, 78% over-valued. Genuine
waterfront homes run +1.6% / 10.2% — the method handles them fine.
**Fix:** Added `classify_water_relationship()` to shared/waterfront.py — waterfront|water_view|dry,
with GEOMETRY (OSM water_features, satellite backs_onto, distance<=5m) deciding frontage and
photographs deciding only views. `detect_waterfront()` left broad deliberately. 24 Brooklyn Crescent
is the worked case: OSM said distance 21.5m, waterfront_type "none", canal_frontage False, backs_onto
residential_only — we flagged it waterfront off the photo signal and valued it 56% high.
⚠ BLOCKER: 53% of homes have no OSM water_features block; backfill is a prerequisite.
**Also:** `is_attached_dwelling()` now catches QLD GTP/BUP/CTS/SUP plan types and floor-to-land
ratio >0.70. 24 Brooklyn Crescent (131 m2 land, 122.4 m2 floor, ratio 0.93, freehold, no unit number,
property_type "House") passed every prior check.
**Files:** shared/waterfront.py, scripts/valuation_backtest.py,
15_Off-Market/Page_Redesign_V4/Product/11_BAND_WIDTH_INVESTIGATION.md
**Recurrence:** First

## [BUILDER-ENV-EXPORT-GAP] On-demand listing builder failed 11/11 for a week while reporting success — 23:40 AEST
**Symptom:** `system_monitor.property_build_requests` held 11 requests, all `status: failed`, every `error` reading "address not found among **0** live listings", spanning 2026-07-31 → 2026-08-07. `job_runs.listed_property_builder` reported `status: success, detail: "queue drained"` throughout. Consequence: every visitor opening `/off-market/:slug` for a home that is currently listed is 302'd to `/building/:slug` — a `noindex` page whose only capture is a phone field that has never produced a lead — and the real `/property` page it exists to build was never built. ~6 organic sessions/week land there, and they are the highest-intent cohort on the site (someone googling an address that just came to market).
**Root cause:** cron line 228 invoked `source /home/fields/Fields_Orchestrator/.env && … python3 scripts/build_listed_property.py --drain` **without `set -a`/`set +a`**, so nothing in `.env` was EXPORTED into the child process. `build_listed_property.py` was the one script in that group that never called `load_dotenv`/`load_env` itself (the six other cron lines missing `set -a` all self-load, which is why only this one broke — verified individually rather than assumed). Two things hid it: (1) `shared.db` still connected, because it falls back to `config/settings.yaml` for the connection string, so the job looked functional; (2) `BRIGHTDATA_API_KEY` was absent, so `run_curlffi_suburb_scrape.py:271` (`use_unlocker = bool(os.environ.get('BRIGHTDATA_API_KEY'))`) fetched Domain directly from this Akamai-blocked VM IP and returned 0 URLs — and the code treated "0 discovered" identically to "address genuinely not listed", so total infrastructure failure was reported as a fact about the addresses.
**Fix:** (1) `load_env()` called inside `build_listed_property.py` so it can never again depend on how the caller was invoked. (2) `build_one()` now returns `infra: True` when discovery yields **zero** URLs, with a message naming the scrape as the fault — a suburb we cover always has dozens of live listings, so an empty discovery is never a fact about one address. (3) `drain()` returns `{processed, infra_failures}` and `process_request()` now returns its result. (4) The `job_run` block **raises** when any infra failure occurred, so the heartbeat records ERROR instead of `"queue drained"`, and records `metrics={processed, infra_failures}`.
**Verification:** ran in the exact cron environment (`source .env` with NO `set -a`): discovery returned **59 URLs** (56 listed, expected 56 — matches `system_monitor.listing_coverage`) where it previously returned 0. Requeued the 11 discovery-failed requests and drained: **12 processed, 0 infrastructure failures, 2 pages successfully built**, 8 correctly resolved as no longer listed (expected — they were queued up to a week ago and have since gone under offer). Confirmed the honest not-found path is NOT flagged as infra.
**Files:** scripts/build_listed_property.py (crontab backup at logs/crontab-backups/crontab.bak-20260807-*-pre-setA-fix)
**Recurrence:** First for this script. The `set -a` gap exists on 6 other active cron lines but **none of them break**, because those scripts self-load — checked each rather than generalising. **Standing lesson: a script must load its own environment; relying on the caller makes correctness a property of the crontab.**

## [INDEXING-SILENT-ZERO] Google Indexing API submitted 0 URLs for 9 nights, discarded the error, and advanced its watermark — 23:45 AEST
**Symptom:** `logs/google-indexing.log` shows `Submitted 0/75`, `0/78`, `0/88`, `0/87`, `0/87`, `0/85`, `0/88`, `0/85`, `0/84` on nine consecutive nightly runs from 2026-07-29 — **757 URL submissions dropped**. Earlier runs in the same log succeeded (`109/109`, `110/110`). No error text, no alert, no heartbeat.
**Root cause:** three compounding defects in `cmd_submit_new()`. (1) It counted successes but **never printed `result["error"]`**, unlike `cmd_submit_all()` which does — so the cause was thrown away every night. (2) The progress line printed the ATTEMPT index (`Submitted {i+1}/{len(urls)}`), so a run failing every call still read as "Submitted 10/88 … 20/88" and only the final line revealed zero. (3) It wrote `indexing-last-run.json` **unconditionally**, so each night's failed batch fell outside the next night's "new/changed since last_run" window and was permanently excluded. And it had **no `job_run` heartbeat at all**, violating Rule 7 — which is the whole reason nine nights passed unnoticed.
**Underlying cause now identified (was previously unknowable):** with error printing added, every URL fails with `('invalid_scope: Bad Request', {'error': 'invalid_scope'})`. `SCOPES` in the script correctly declares `auth/indexing` + `auth/webmasters`, so the stored OAuth refresh token does not cover the declared scopes and needs re-consent. **This requires an interactive OAuth flow — it cannot be completed from a non-interactive session and is handed to Will.** The 757 dropped URLs will now be retried automatically once auth is restored, because the watermark was never advanced past them.
**Fix:** print the first 10 errors with their messages; show `ok / attempted` in progress rather than attempts alone; **refuse to advance the watermark and raise when `ok == 0`**; wrap the `submit-new` dispatch in `job_run("google_indexing_submit_new", cadence_hours=24)`.
**Verification:** ran `submit-new` — it now names `invalid_scope` on all 84 URLs, prints `REFUSING to advance the watermark`, raises, and `system_monitor.job_runs.google_indexing_submit_new` records `status: error` with `cadence_hours: 24`, so it will render as ERROR on the Process Registry page instead of being absent. Confirmed `logs/indexing-last-run.json` was NOT advanced.
**Files:** scripts/google_indexing.py
**Recurrence:** First for this script. **3rd today of the class "a scheduled job reported success or silence while doing nothing"** — after [BUILDER-ENV-EXPORT-GAP] (success while 11/11 failed) and the `offmarket_intel_poller` swallow-to-`done` pattern found in the same audit. **The generalisable fix, which I have now applied twice: `job_run` must wrap an ASSERTION that the work happened, not merely the absence of an exception.** A job that can complete having achieved nothing needs an explicit `raise` on the zero-output path.

## [RULE7B-OUTCOME-ASSERTION] Tightened Rule 7: heartbeats must assert an outcome, not just a clean exit — 00:20 AEST
**Symptom:** Three live jobs found today all HAD a Rule 7 heartbeat and were still invisible, because each reported success while achieving nothing: `build_listed_property` recorded `"queue drained"` through 11 consecutive total failures for a week; `google_indexing submit-new` submitted 0 URLs on 9 straight nights (757 dropped) and advanced its watermark each time; `gcp_cost_monitor` reported `MTD 0.00, forecast 0.00, top=n/a, 0 alert(s), status: success` against a real ~$410/month bill. `offmarket_intel_poller` is a fourth instance (swallows sub-resolver exceptions → `status: "done"` → never retried → 231 public deck pages permanently missing content).
**Root cause:** Rule 7 required every ongoing process to self-report, and `job_run` records success on any clean exit. It never required the report to **assert that the work happened**. A job that runs to completion having done nothing is therefore indistinguishable from one that worked — and every guard we had was a liveness check, not a correctness check.
**Fix:** Added **Rule 7b** to `CLAUDE.md` (both copies, synced): identify each process's zero-output path and `raise` on it, with three corollaries — (1) distinguish "no work to do" (success) from "could not do the work" (failure), and if the code cannot tell them apart that is the bug; (2) never advance a watermark/cursor/`last_run` on a failed run, because that makes one night's failure permanent; (3) record the error text rather than counting only successes. Also added to the shipping checklist: **load your own environment** (`load_env()` in the script, not `set -a` in the crontab), since `shared.db` falls back to `config/settings.yaml` and will connect regardless, making a credential-starved job look healthy. Applied 7b to `gcp_cost_monitor.analyse()`: it now raises `CostDataUnavailable` when the billing export returns zero rows or sums to $0.00 on day ≥ 2 — the missing-table case was already fatal, but the empty-result case was not, which is the whole reason a $76/month unmounted disk survived 8 days.
**Verification:** `gcp_cost_monitor` now reports **MTD $63.78 AUD, forecast $395.42, top=Compute Engine** where it previously reported $0.00.
**Files:** CLAUDE.md, /home/fields/CLAUDE.md, scripts/gcp_cost_monitor.py
**Recurrence:** This entry exists because the class recurred 4× in a single audit. Rule 7 (2026-07) closed "no self-check"; 7b closes "self-check that cannot fail".

## [GCP-IDLE-SPEND-REMOVED] Deleted the 400GB unmounted SSD and shut down two 170-day idle VMs — 00:30 AEST
**Symptom:** `fields-blob-storage` (400GB pd-balanced, **$76/month**, 18.6% of the fields-estate bill) had been attached-but-unmounted since the 2026-07-30 blob migration, 8 days past its own documented "delete after ~48h" follow-up, holding the region's SSD quota at 500/500. Separately `property-scraper` (e2-standard-2) and `searxng-server` (e2-micro) had run continuously since 17/19 February in project `property-data-scraping-477306` — a **different billing account**, invisible to all our GCP cost tooling — for a CEO/Codex system disabled 2026-03-31 (**~$113/month**).
**Verification BEFORE deleting anything** (Will asked the right question: backup or actual photos?): the disk holds our real property images, but as a stale subset. Mounted it **read-only** and established: newest file `2026-07-30 13:02` (the exact migration cutover) vs live `2026-08-07 09:04`; live has MORE files in every shared directory (property-images 1,086,604 vs 1,071,014; case-studies 446 vs 416); live additionally holds two whole directories created after cutover (`article-charts`, `off-market-v3`); and a 400-file random sample from the old disk was **0/400 missing** on the live disk. Off-VM copy confirmed current: cron `0 3 * * *` `gsutil -m rsync -r -d /data/blobs/ gs://fields-blob-backup/` ran 03:16 today copying 2026-08-06 photos.
**Backup gap closed first:** that rsync uses `-d`, so it is a MIRROR — a wipe of `/data/blobs` would propagate. GCS soft-delete was already enabled at 7 days (I had wrongly told Will the window was "under 24 hours"); **extended to 30 days** before touching the disk. Versioning left off: soft-delete covers this failure mode without needing lifecycle management of noncurrent versions.
**Snapshot deliberately skipped.** I had described a 400GB snapshot as "~$16 one-off" — wrong, snapshot storage is **recurring** (~$10/month for 295GB used). With the data verified present on the live disk AND in GCS with a 30-day window, a third stale copy at $10/month recurring was poor value. Stated to Will rather than done silently.
**Actions:** detached + deleted `fields-blob-storage`; confirmed `/data/blobs` still mounted on `/dev/sdc`, nginx active, blob HTTP 200, sample file readable AFTER the detach. Stopped both VMs; **deleted `searxng-server`** (0 bytes egress for 7 days, and its only repo references are a filename in a list at `scripts/ceo-context-export.py:789` plus a founder-truths doc, with the CEO crons commented out); **stopped but did NOT delete `property-scraper`**, retaining its 30GB boot disk (~$2.3/month) so the DR image survives — per `05_WHERE_WE_FALL_SHORT.md:149` the right model is a weekly start-scrape-compare-stop cycle, not a permanently-running instance.
**Result:** SSD quota **500/500 → 100/500**, which is what the July migration was blocked on. ~$189/month removed, of which $76 is on the fields-estate account (forecast should fall ~$395 → ~$319 as rows land) and $113 on the second billing account, which no cost tooling currently sees.
**Files:** infrastructure only (GCP disks/instances, GCS bucket policy)
**Recurrence:** First deliberate cleanup. **Follow-up still open:** `scripts/gcp_cost_monitor.py` has a single hardcoded `BILLING_ACCOUNT`, so project `property-data-scraping-477306` remains invisible — the next idle resource there will go unnoticed the same way.
