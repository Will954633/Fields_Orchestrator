
## [ARTICLE-EXCERPT-TRUNCATION] Article subtitles cut mid-word at 300 chars — 15:24 AEST
**Symptom:** The subtitle (dek) on the draft article "Sold in 35 Days: How a Renovated Burleigh Waters Duplex Reached $1,100,000" ended mid-word — "...this home landed above the g". `custom_excerpt` was exactly 300 chars. A sweep of `system_monitor.content_articles` found 13 more articles cut mid-word (8 at 300, 5 at 280) plus 4 carrying raw `**` markdown at the tail — 17 of 92 total, 12 of them published and live.
**Root cause:** Every excerpt write path used a raw character slice with no word-boundary awareness: `scripts/push_to_ghost.py` sliced `stripped[:300]` when deriving the excerpt from the first paragraph and re-sliced `custom_excerpt[:300]` on both the create and update writes to Mongo; `pipeline/run_market_strength.py`, `run_state_of_market.py` and `run_market_insight.py` each did `line[:280]`. The 280-char pipeline cut then hit the 300-char cut downstream, so a long lead paragraph was guaranteed to land mid-word. Bold lead paragraphs also leaked their `**` markers because the excerpt was taken from raw markdown.
**Fix:** Added `scripts/excerpt_utils.py` with `clip_excerpt(text, limit)` — strips markdown emphasis, returns the text unchanged when it fits, otherwise prefers the last complete sentence inside the limit (when it retains ≥60% of the allowance) and falls back to the last whole word plus an ellipsis so a trim is visible rather than silent. Guards a degenerate no-word-boundary input by hard-cutting, and is limit-inclusive of the ellipsis. Wired into all five write sites; `push_to_ghost.py` is the last gate before Mongo, so nothing truncated can reach the DB regardless of which pipeline produced it. Backfilled all 17 damaged excerpts by re-deriving from the article's first HTML paragraph and re-clipping; re-verified zero remaining.
**Files:** fields-automation: `scripts/excerpt_utils.py` (new), `scripts/push_to_ghost.py`, `pipeline/run_market_strength.py`, `pipeline/run_state_of_market.py`, `pipeline/run_market_insight.py`
**Recurrence:** First

## [HEALTH-BOARD-PAUSED-VS-DEAD] Paused jobs buried 3 real failures on the Systems Health board — 09:05 AEST
**Symptom:** Will asked for an audit of every heartbeat watcher on the Fields Systems Health sheet. Process Registry read **67% health, 5 ERROR + 33 STALE** — but almost all of it was noise. Of 32 flagged rows, 29 were jobs switched off ON PURPOSE (the 2026-07-30 `[RL-AGENTS-PAUSE]` and Home Owner funnel pauses, plus the 2026-07-29 weekly-SEO disable). Only 3 were real, and they were invisible inside the noise.
**Root cause:** `collect_self_reported_jobs()` — the generic Rule 7 heartbeat renderer — had no concept of "off on purpose". A paused job keeps its last heartbeat forever, so it ages into STALE (or freezes on a final ERROR) and alarms every day. `collect_process_registry()` already had `_REGISTRY_DISABLED` for exactly this, but the heartbeat path never got the equivalent. Net effect: the board cried wolf 29 times a day and the 3 genuine failures didn't stand out.
**The 3 real failures found underneath:**
1. `seo_dashboard` — dead since 2026-07-28 while cron fired daily (syslog confirms Aug 2/3/4/5). Its crontab line was the ONLY one of 83 using relative paths with no `cd`: `set -a && source .env && ...`. Cron runs from `$HOME` (`/home/projects`), where no `.env` exists → `source` fails → the `&&` chain short-circuits → the script never runs AND nothing is logged, anywhere. Silent by construction. Same class as `[[gh_api_large_file_gotcha]]` (relative paths + cwd reset); almost certainly collateral from the `[OPS-CRONTAB-WIPE]` restore.
2. `offmarket_home_signal` — failing nightly on `WriteError code 28: Cannot create field 'address' in element {offmarket_home: null}`. `crm_sync.py`'s hourly whole-doc REPLACE carries the field forward with a bare `.get()`, storing an explicit **null** for every contact that never had one; Mongo cannot create a dotted path inside a null, so every `$set` raised. 381 contacts affected. `physical_scan` (70) and `home_confirmed` (65) carried the identical unfired landmine — `minisite-visit.mjs:129` dotted-`$set`s into `physical_scan` from live traffic.
3. `ssd_quota_autoresize` — cron line lost in the `[OPS-CRONTAB-WIPE]` restore (absent from `crontab.restored-2026-07-30.bak`). Investigating showed it is also **obsolete**: a separate 750 GB `blobs-hdd` disk was attached and `/data/blobs` moved onto it (`/dev/sdc`, 738 G, 43% used), well past its `TARGET_BLOB=700`. It still targets `DEV=/dev/sdb` (the old 400 GB `fields-blob-storage`), so running it now would grow the wrong disk.
**Also found:** `search_intent_ads` was a FALSE ALARM — it reports `error` on "0 rows", but every Google Ads campaign is PAUSED/REMOVED so the search-terms report correctly returns nothing. And 5 weekly jobs (`fpf_preflight_gmail`, `fpf_friday_batch`, `propradar_ingest`, `fetch_abs_market_signals`, `fetch_macro_indicators`) declared no `cadence_hours` at all — running fine but effectively unmonitored, defaulted to a 24 h cadence they could never meet.
**Fix:**
- `main_site_health_check.py` — new `_PAUSED_JOBS` registry (29 entries, each with reason + pause date), the heartbeat-side twin of `_REGISTRY_DISABLED`; renders KNOWN-GAP (severity 0, `info=True`) instead of an alarm. **Safety valve:** a listed job that reports a run INSIDE its cadence is rendered normally with "LISTED AS PAUSED but ran Nh ago — remove it from `_PAUSED_JOBS`", so the registry can suppress a job only while it is genuinely silent, never while it is running or failing. Verified across 6 cases incl. paused-and-erroring-while-live → still ERROR.
- crontab — `seo_dashboard` line rewritten with `cd /home/fields/Fields_Orchestrator &&` + absolute script/log paths. Reproduced the old failure under a stripped `env -i` cron-like shell, then confirmed the new form runs.
- `crm_sync.py` — `offmarket_home` / `home_confirmed` / `physical_scan` now carry forward as `{}` not `None` (verified nothing anywhere does `is None`/`=== null` on them, so `{}` is behaviour-identical). `offmarket_home_signal.py` — added an idempotent pre-pass unsetting any explicit null. One-time repair cleared all 516 existing nulls across the 3 fields.
- `search-intent-collector.py` — new `_SRC_EMPTY_OK` set: 0 rows AND no source error = success-with-note, for `ads` only. Every real ads failure appends a `google-ads:` error, so an empty error list unambiguously means "API answered, nothing to report". 5/5 logic cases pass; `gsc` empty still ERRORs.
- `fpf_preflight_gmail.py`, `fpf_send.py`, `propradar/run_ingest_cron.py`, `fetch_abs_market_signals.py`, `fetch_macro_indicators.py` — `cadence_hours=168, stale_hours=192` + titles on BOTH success and error paths (8 call sites), so one missed week flags rather than the default 10.5 d.
- `ssd_quota_autoresize.py` — RETIRED banner, cron deliberately not reinstated, heartbeat doc deleted.
**Verified:** Process Registry self-reported rows went **63 → 32 OK / 28 KNOWN-GAP / 3 flagged**, and post-fix only genuine items remain. `offmarket_home_signal` re-run succeeds (229 contacts flagged from 248 sessions) where it previously raised.
**Miss worth recording:** my own first-pass audit under-reported by one. I judged staleness as `cadence × 1.5` and missed that the renderer honours an explicit `stale_hours` override — `samantha_seo_improvement` (cadence 168, stale_hours 180, age 188 h) read OK to me but STALE to the board. Re-audited every job carrying a `stale_hours` override; that was the only one mis-classified. It turned out to be a 29th deliberately-paused job (disabled 2026-07-29 by Will, whole weekly SEO process off — distinct from `run_seo_pilot_review.sh`, which is still live Mondays and ran fine Aug 3).
**Open (needs Will):** `home_owner_funnel_ledger_sync` still runs hourly feeding a reward ledger for a funnel whose checkpoint + wake-up loops are both paused — either retire it or un-pause the other two.
**Files:** scripts/main_site_health_check.py, scripts/crm_sync.py, scripts/offmarket_home_signal.py, scripts/search-intent-collector.py, scripts/fpf_preflight_gmail.py, scripts/fpf_send.py, scripts/propradar/run_ingest_cron.py, scripts/fetch_abs_market_signals.py, scripts/fetch_macro_indicators.py, scripts/ssd_quota_autoresize.py, projects crontab (backup: logs/crontab-backups/crontab.bak-20260805-081705)
**Recurrence:** First for the paused-vs-dead class. The cron-relative-path failure is Nth in the "silent cron" class (see `[OPS-CRONTAB-WIPE]`, sitemap push 2026-07-20→22).

## [HOWITSOLD-GAIN-AND-SEGMENT] Capital gain always 0% + sales benchmarked against the wrong median — 17:05 AEST
**Symptom:** Two defects in the How It Sold individual-home article pipeline. (1) Capital gain read +0.0% on 321 of 600 candidate properties, so the pipeline discarded gains like +260%, +441% and +4,459% and silently omitted the strongest fact in the article. (2) A 2-bedroom duplex at 2/5 Bottlewood Court was reported as "42.9% below the Burleigh Waters median" — it was measured against a houses-only median of $1,925,000. 214 of 600 candidates (36%) were non-house types benchmarked this way.
**Root cause:** (1) `_extract_price_history` returns every Sale event including the sale being written about, and the capital-gain loop took the first entry — the current sale — as the "last purchase", comparing the sale to itself. A date cutoff alone does not fix it: the timeline stores the CONTRACT date while `sold_date` is settlement, so the subject sale usually appears days-to-weeks BEFORE `sold_date`. (2) `median_sale_price` comes from `precomputed_indexed_prices`, which is houses-only, but selection only excludes the two exact strings "Apartment / Unit / Flat" and "Studio" — every Townhouse, Villa, Duplex and untyped record was compared to it. `property_type` is itself unreliable: 56 sold records are typed "House" while carrying a unit-prefixed address, including the Bottlewood duplex.
**Fix:** Added `_prior_sales()` to exclude the subject sale by price (the reliable discriminator) and by date. Added `segment_for()` + `MIN_SEGMENT_N=10` to `mongodb_client`, classifying by address unit-prefix first and `property_type` second; `get_suburb_context_and_price_history` now accumulates house and attached prices in the same single pass (no extra RU) and returns `segment_medians` plus per-bedroom sample counts. `assemble()` picks the segment-matched median and suppresses the comparison entirely below MIN_SEGMENT_N rather than borrowing the house median; thin bedroom buckets (some n=1) are suppressed too, which also gates the ±20% outlier flag that spends a GPT-4o call. The prompt now names what the median covers and states its transaction count, making the sample-size rule in `editorial_rules.md` satisfiable.
**Result (600-property candidate pool):** capital gain computed 92 → 412, falsely-zero 321 → 0. Median comparison: 366 house, 229 attached, 5 suppressed. 2/5 Bottlewood Court now reads "1.3% above the Burleigh Waters townhouse and villa median (based on 12 sales)" with a +44.7% capital gain that was previously dropped.
**Files:** fields-automation: `pipeline/mongodb_client.py`, `pipeline/data_assembler.py`, `pipeline/article_generator.py`
**Note:** The local checkout at /home/fields/fields-automation is ~5 months stale; `data_assembler.py` and `article_generator.py` were pulled fresh from GitHub before editing so remote work was not reverted. Verified by diffing against a clean remote pull before pushing.
**Recurrence:** First

## [BROWSE-RAIL-IMG-BYTES] Off-screen rail photos ate 68% of the bytes before LCP — 08:50 AEST
**Symptom:** Home page (`/`, the browse surface — the logo's destination) measured mobile LCP
~3.7-3.9s on a throttled profile (1.6Mbps 4G, 4x CPU). CLS 0, TTFB 41-57ms, FCP ~0.95s — server
and layout were both fine.
**Root cause:** the page is bandwidth-bound, and it was spending its bandwidth on images nobody
could see. 18 property images (527KB) were fetched **before** LCP with only 2 of them on screen —
68% of the 776KB that transferred pre-LCP. 776KB / 200KB/s ≈ 3.9s, matching measured LCP almost
exactly. The cause is that a `Rail` scrolls **horizontally**, so every card in it shares the
vertical offset of the visible ones. Chrome's native lazy-load threshold is measured vertically,
so `loading="lazy"` cards parked off to the RIGHT read as "near the viewport" and were fetched
immediately. 125 cards / 107 `<img>` were in the SSR HTML. On a 3x phone Chrome also picks the 2x
srcSet entry, so each wasted card cost ~51-63KB rather than ~26KB.
**Fix:** added `useNearViewport()` in `RailCard.tsx` — cards past the first few hold their photo
until an IntersectionObserver sees them. IO against the viewport DOES account for clipping by an
ancestor overflow container, so it gets the horizontal axis right where native lazy loading
cannot. Rails 1-2 keep `VISIBLE_PER_RAIL = 4` photos in SSR; rails 3+ are below the fold at every
viewport size and defer all of them. The `<a>` and all card text stay in the SSR HTML, so crawl
discovery is untouched (**verified: 81 unique `/property/` links before and after**; SSR `<img>`
107 -> 11). `.shot` already had `aspect-ratio` + a background, so the deferred state reserves
identical space — CLS stayed 0.
**Measured (median of 4, throttled 4G + 4x CPU):** LCP **3.76s -> 2.97s**; property images before
LCP **18 -> 7**; image bytes before LCP **527KB -> 188KB**; all bytes before LCP **776KB -> 439KB**;
DOM `<img>` 125 -> 13. Unthrottled mobile LCP is ~330ms. Still above the 2.5s "good" threshold on
the throttled profile — the remaining pre-LCP cost is the 4 SSR'd photos per rail on rails 1-2,
of which a 390px phone shows ~1.5.
**Also fixed:** the whale preload guard checked `inWhaleSeason()` but never `alreadySeen()`,
despite its own comment claiming it cost nothing "after they have had their whale" — so every
repeat visit through Jul-Sep re-warmed ~390KB for a moment that could not play again.
`alreadySeen()` is now exported and part of the guard.
**Two measurement traps hit on the way (both cost real time):**
  1. `fetchPriority` is emitted by React 19 SSR in **camelCase** (`fetchPriority="high"`), so a
     case-sensitive `grep 'fetchpriority='` finds nothing and looks like a missing priority hint.
     The LCP image was already `eager` + `fetchPriority=high`; that was never the bug.
  2. Sampling "the first N above-fold images" found only the blurred `LockedCard`s, which are
     lazy **by design**. Read the actual LCP element's attributes via the PerformanceObserver
     entry, not a positional guess.
  Also: blocking the whale appeared to cut LCP by 1.7s on a single run; across 4 runs each way the
  medians were 3.76s vs 3.87s — pure noise. Single-run A/B on this VM is not trustworthy.
**Files:** `src/components/Rail/RailCard.tsx`, `src/components/Rail/Rail.tsx`,
`src/pages/BrowsePage/BrowsePage.tsx`, `src/components/WhaleMoment/WhaleMoment.tsx`,
`src/components/WhaleMoment/useWhaleTrigger.ts`
**Recurrence:** First (related but distinct: `[BROWSE-TTFB-SERIAL]` 2026-08-03 fixed the *server*
half of this page's LCP; this is the *bytes* half.)

---

## [STEP-STDOUT-TRUNCATED] Every pipeline step silently lost its summary block — 09:05 AEST

**Symptom:** `logs/runs/*/stdout.log` — the path CLAUDE.md documents for auditing a step — ended
mid-output for every one of the 26 nightly steps, with no truncation marker. Step 101 lost 35 lines
(its entire per-suburb FINAL SUMMARY), step 18 lost its 233/307 counts, step 6 its valuation result,
step 107 stopped mid-Error #91 of 100. In the 2026-08-02 audit this was misdiagnosed as per-script
stdout buffering, and two steps (112, 117) were wrongly suspected of dying early.

**Root cause:** `src/task_executor.py` streams child stdout line-by-line into `stdout_file`, but the
closing "Drain remaining output" block appended whatever was still in the pipe to `stdout_lines` (memory)
and to `logs/orchestrator.log` only — it never wrote it to `stdout_file` before closing the handle. Any
output flushed in the final moments of a process was therefore absent from its per-step log.

**Fix:** write the drained tail to `stdout_file` as well, then flush. One block, no behaviour change
beyond completeness. `logs/orchestrator.log` remains the belt-and-braces copy.

**Files:** src/task_executor.py
**Recurrence:** First

---

## [HEALTH-PIPELINE-OUTCOMES] Health board judged pipeline steps on exit code, not on outcome — 09:05 AEST

**Symptom:** The Fields Systems Health sheet showed "Pipeline Processes 64%" and "26 ok / 0 failed" for
the 2026-08-04 run. That same run wrote 11 listings with ZERO photo analyses (and marked them processed,
so they can never retry), valued 1 property of 25, failed 73 of 73 timeline profile scrapes, cleared 303
published valuations, and left 117 listings in a terminal `under_contract` state. None of it appeared
anywhere on the board.

**Root cause:** `collect_*` for the Pipeline Processes page keyed entirely off `result.json.success`,
i.e. the step's exit code. Every nightly step exits 0 while absorbing failures internally, so the page was
structurally incapable of seeing lost work. Separately, a step defined in `execution_order` but absent
from every set in `src/schedule_manager.py` produces no `result.json` at all and so produced no row —
which is why step 121 (SEO sitemap resubmit) has never executed since it was created 2026-07-21 and
nothing noticed.

**Fix:** added `collect_pipeline_integrity()` with three families of check, wired into the existing
per-collector try/except so a bug in it cannot take down the audit:
1. **Schedule-membership drift** — reconciles `execution_order` against the three schedule sets; any
   orphan is an ERROR naming the step. Closes the step-121 class permanently.
2. **Per-step outcome assertions** (`_STEP_OUTCOME_CHECKS`) — asserts what a step *achieved*, parsed from
   its own log: step 105 dead-host image failures, 106 blob failures, 6 failed valuations + Overpass 406s,
   12 Pass-3 yield, 113 budget abort, 18 cleared valuations, 111 records written. A step whose log no
   longer matches its pattern renders UNKNOWN ("cannot verify"), never a silent OK.
3. **Terminal-state accumulation** — `under_contract` backlog with the age of the oldest record.
Also judged step 6 on its *failure* count rather than its success count (a bare "Successfully valued: 1"
reads green while 24 of 25 are lost), added `fields-samantha-chat` to the monitored systemd units, and
registered 9 active cron jobs that had no heartbeat, no registry row and no other coverage.

**Verified:** Pipeline Processes went 64% → 38% (4 → 11 errors) with every audit finding now surfaced,
sitting directly beside the unchanged "26 ok / 0 failed" row. Published live and confirmed on the sheet.

**Files:** scripts/main_site_health_check.py
**Recurrence:** First

## [MONITOR-FITNESS-PROBES] Retire dead ledger job + fix 4 probes that structurally could not work — 09:20 AEST
**Scope (Will):** get the Systems Health monitor fully operational and fit for the code stack AS IT IS TODAY, then stop — explicitly NOT to resolve the underlying issues the monitor surfaces. Everything below changes what the board can observe, not what the jobs do.
**1. `home_owner_funnel_ledger_sync` — RETIRED (Will's call).** It fed the Home Owner funnel reward ledger from two sources; both had stopped being useful. FB half: its only campaign (`120251771274010134`, "Leads TEST: Home Owner Hooks — SEQ ex-GC v2") has been PAUSED since 2026-07-30, so the ledger froze at data dates 2026-07-28..30 and every hourly run just re-upserted the same 120 `ad_stats` rows changing only `updated_at` (~15x/day, reporting success each time). PostHog half: **never worked at all** — zero `lab_*` events have EVER been fired (verified against PostHog all-time with no filters, while the same query returns real data for other events), so `funnel_event` rows have always been 0. The `EVENT_SPINE` was designed but the instrumentation was never shipped onto the landing pages. Cron commented with the reasoning, heartbeat doc deleted, RETIRED banner on the script warning that reviving it restores ad-level numbers only — the per-step drop-out analysis needs `lab_*` to exist first.
**2–5. Four Process Registry rows whose PROBE could not work** (the job was fine; the check was wrong):
- **Market Pulse reminder (1st)** + **auto-fallback (3rd)** — the registry looked for `pulse-reminder.log` / `market-pulse-auto.log`, which have never existed: both wrappers append to the SHARED `logs/market-pulse.log`, so a log-mtime probe cannot tell which of the two ran even in principle. Converted both to real heartbeats (`record_job_result` in the wrapper, cadence 744h / stale 800h — one missed month flags without crying stale across a normal 28–31 day gap) and deleted the two registry rows, since `collect_self_reported_jobs` renders heartbeats automatically. Seeded by BACKFILLING each job's true last run from the shared log (reminder 1 Aug 08:00, fallback 3 Aug 06:00) rather than firing a real Telegram reminder / content generation just to create a row; the detail field records that it was backfilled.
- **VM heartbeat writer** — read STALE ("log not updated in 3.0d") while perfectly healthy. `write_heartbeat.sh` sends all its own output to `/dev/null`, so the cron's `>> /tmp/vm-heartbeat.log` is only ever touched on an unhandled error and its mtime is meaningless. Repointed at `/tmp/vm-heartbeat.txt`, which the script rewrites every minute (`date > $TMP`). Deliberately NOT duplicating the end-to-end GCS check: `gs://fields-vm-watchdog/vm-heartbeat.txt` (confirmed 8s old mid-audit) is the OFF-VM watchdog's responsibility and it alerts Will directly, so this row's contract is narrower on purpose — "is the local writer still running".
- **Monthly market precompute chain** — read MISSING because the consolidated wrapper was created 2026-08-02, i.e. AFTER that month's run date, so its log legitimately does not exist yet (first run 1 Sep). Added an optional 5th `not_before` element to `_REGISTRY_LOG_JOBS`: a missing log before that date renders KNOWN-GAP "awaiting first scheduled run (due YYYY-MM-DD)" instead of MISSING. General mechanism — applies to any newly-added periodic job.
**Also:** ran `search-intent-collector.py --source ads` to exercise the `_SRC_EMPTY_OK` semantics fix from [HEALTH-BOARD-PAUSED-VS-DEAD] against the live API; heartbeat flipped from `error: 0 rows collected` to `success: 0 rows — no enabled Google Ads campaigns (expected while paused)`.
**Verified:** Process Registry **94% → 97%**, MISSING 3 → 0, STALE 1 → 0, ERROR 4 → 3. The 3 survivors are all genuine job failures the board is correctly reporting and are explicitly OUT OF SCOPE here: Brain2 nightly (the `offmarket_home` WriteError already fixed — its log predates the fix, clears tonight), CRM sync (PostHog 504 flakiness, known class), and `refresh_property_timelines` (Cosmos `CursorNotFound` on a long-running find — a real, separate, unfixed bug). Active crons 83 → 82.
**Files:** scripts/main_site_health_check.py, scripts/run_pulse_reminder.sh, scripts/run_market_pulse.sh, 03_Facebook/Home_Owner_Lead_Funnel_Search/ledger/ledger_sync.py, projects crontab (backup: logs/crontab-backups/)
**Recurrence:** First for the probe-fitness class. Companion to [HEALTH-BOARD-PAUSED-VS-DEAD] (same audit) and [HEALTH-PIPELINE-OUTCOMES] (same board).

## [PROPERTY-PAGE-UNCACHED] Property pages ran 3 uncached server calls per view — 09:30 AEST
**Symptom:** `/property/:slug` — the pages that carry the Google referral traffic — measured LCP
**5.5-5.8s unthrottled** and ~9.9s on throttled mobile. The initial paint hid it: FCP was ~1.7s
because the SSR shell (address, beds, price) renders fine; the actual analysis arrived seconds
later.
**Root cause:** three separate server round-trips, none of them cached, stacked in series.
  1. `routes/property.$id.tsx` had **no `headers()` export**, so the document served
     `cache-control: no-cache` with `cache-status: "Netlify Edge"; fwd=miss` — every visitor ran
     the loader live. Document TTFB 0.85-1.86s, against 41ms on `/`, which already had the export.
  2. `/api/v1/properties/for-sale/:slug` — also `no-cache`, TTFB a consistent **3.2-3.4s**.
  3. `/api/valuation/current_listings_comps` — also `no-cache`, **234KB** body at **2.4s**, and it
     only surfaced as the bottleneck once (1) and (2) were fixed.
  The **`[[headers]]` block in netlify.toml declaring `max-age=300` for `/api/v1/*` does not apply
  to Function responses** — those blocks cover static assets. Caching has to be set on the
  `Response` itself. That is why the intent was there but no endpoint honoured it.
**Not the cause (checked first, ruled out by measurement):** the single-property handler loops
serially over suburb collections doing `findOne({url_slug})`, which looks like the obvious
culprit. But `url_slug` **is** indexed on every suburb collection and each lookup is 1-6ms;
timing every DB operation the function performs totals **32ms**. The remaining ~3.15s is
`getFreshClient()` in `db.mjs` opening a **new Cosmos connection per invocation** — the deliberate
workaround for the Netlify/Mongo event-loop hang. Caching makes that handshake a per-window cost
instead of a per-request one; the workaround itself was left alone.
**Fix:** added the `/`-style `headers()` export to the property route
(`s-maxage=300, stale-while-revalidate=600`); set the same policy on the single-property and
valuation-comps responses. `jsonResponse()` in `db.mjs` gained an optional third `extraHeaders`
argument, defaulting to `{}` so all 55 existing callers are unchanged. Not-found responses cache
60s so a retired URL cannot reopen a connection on every hit.
**Measured (warm, median):** document TTFB **1.39s -> 0.03s**; single-property API **3.2s -> 0.037s**;
comps API **2.4s -> ~0.46s**; **unthrottled LCP 5.6s -> ~1.07s**; throttled-mobile LCP **2.5s (with
content at ~9.9s) -> ~1.18s**. CLS stayed 0. Rendered page carries 19,697 chars incl. the full
editorial; 0 console messages.
**Honest caveat — the first visitor still pays.** On a genuine cache miss the document is
**2.6-2.75s** (warm: 0.077s). Caching moves the cost, it does not remove it; the underlying
per-invocation connection setup is still there. A 5-minute window on a long-tail property page
means a fair share of organic entries will be cold. Removing that floor means either revisiting
`getFreshClient` or lengthening the TTL.
**Still open (deliberately not done here):** the analysis is **not in the SSR HTML** — only 1,090
chars of visible text are server-rendered, and the editorial arrives only via the client fetch.
Googlebot renders JS on a deferred pass, so this is an indexing-quality risk, not just a speed
one. Folding the payload into the SSR loader is the real fix and is its own change.
**Files:** `src/routes/property.$id.tsx`, `netlify/functions/properties-for-sale.mjs`,
`netlify/functions/valuation.mjs`, `netlify/functions/db.mjs`
**Recurrence:** Second — `[BROWSE-TTFB-SERIAL]` (2026-08-03) was this exact fix applied to the
browse surface. **The lesson did not generalise: no one checked which OTHER routes lacked a
`headers()` export.** Worth auditing the remaining routes and Functions for the same gap.

## [FRIDGE-CONCEPT-CSS-INHERIT] Fridge-door landing concept — 3 bugs only a live run caught — 09:35 AEST
**Symptom:** New concept prototype (`15_Off-Market/Concepts/Fridge_Magnet_Concept`). Frozen-frame screenshots looked correct at every door angle, but the real 3s run showed the door swinging open onto an empty interior — no option rows ever appeared. Separately the door rendered translucent mid-swing, and the palette came back green rather than black-and-white.
**Root cause:** (1) `--open` (the single custom property driving the whole sequence) was animated on `.door`; the shelves live in `.cavity`, a **sibling**, so they inherited nothing and stayed at 0. Posing a frozen frame sets `--open` on the parent, which is exactly what masked it. Same class of error hid the prompt fade (`.is-open .prompt` is a descendant selector against a sibling element). (2) `.cavity` sat at Z=0, exactly coplanar with the door's inner panel → z-fighting. (3) Door opened to 78°; projected width is `W·cos θ`, so it still covered 21% of the opening and cut every option label. (4) Palette used theme.css's grass ramp at its native 24% saturation.
**Fix:** animate `--open` on `.fridge` (the common ancestor); `.fridge.is-open ~ .prompt` sibling combinator; `.cavity { transform: translateZ(-8px) }`; `--angle: 100deg` (past perpendicular, so the door clears the opening); palette rebuilt at ~7% saturation; `.back` given `rotateY(180deg)` so backface-visibility doesn't hide it past 90°.
**Files:** `15_Off-Market/Concepts/Fridge_Magnet_Concept/{README.md,index.html,fridge.css,fridge.js,assets/*,verify/*}` (all new).
**Lesson:** frozen-frame screenshots verify **geometry**; only a live timed run verifies the **sequence**. `verify/live.mjs` exists for that and should run alongside `verify/shots.mjs` on any change. Reinforces the instrument-failure playbook in memory `website_verification_gates`.
**Recurrence:** First.

## [COMPS-DETAIL-EXPOSED] backtest discarded per-comparable adjustment detail — 19:40 AEST
**Symptom:** No way to show a reader the adjusted comparable sales behind a sold home. `precompute_property_valuation()` cannot be used on a sold property at all — its sold-comp filter tests only property type, price and a rolling 12-month window (the `$ne: _id` guard covers current listings), so the subject's own transaction returns as its own comparable with near-zero adjustment, "verified" status and top weight, and the valuation just reproduces the sale price.
**Root cause:** `valuation_backtest.backtest_single_property()` is the only path that builds a leak-free comp set (excludes subject by `_id`, drops every sale dated on/after the subject via `sold_before_subject()`), but it computed the full per-comp `adjustment_result` and then returned only summary accuracy metrics, discarding it.
**Fix:** Added `included_points`, `all_points` and `subject_features` to the return dict. Purely additive — existing callers read by key and are unaffected. Verified the accuracy harness still runs unchanged.
**Files:** `scripts/valuation_backtest.py`
**Related:** evidence written up in `11_House_Mini_Site/Adjusted-Comparables-Evidence.md`. Two traps recorded there: `total_adjustment_pct` is a fraction not a percentage, and time adjustment never fires on the backtest path (`time_adjust_sale_price()` hardcodes `utcnow()` as target when a sold subject needs its own sale quarter).
**Recurrence:** First

## [HOWITSOLD-ASKING-PRICE-CAMPAIGN] Articles presented reduced asking prices as the original guide — 21:15 AEST
**Symptom:** We published "$1,700,000 in Robina: How 16 Collingwood Avenue Beat the Suburb Median by 11.8%". That home opened as an Expression of Interest, was advertised at $1,949,000, cut to $1,749,000, and sold for $1,700,000 after 127 days. The article mentions none of that and refers to $1,749,000 — the REDUCED price — as "the guide" (9 mentions). Every sentence arguably true; overall impression wrong. A struggling campaign was published as a clean win.
**Root cause:** The How It Sold pipeline reads only `listing_price`, which holds the FINAL advertised price. It never read the `price_history[]` field (`{price_text, price_numeric, recorded_at, event: initial|change}`), so any campaign that reduced its price was invisible and the last guide looked like the first.
**Fix:** Added `_extract_asking_price_history()` to `data_assembler.py` (named distinctly — `price_history` in that module already means SALE history from property_timeline, a genuine collision) returning first/final asking, reduction and increase counts, change %, the event list, and any non-priced opening (Auction / Expressions of Interest). Exposed as `asking_history` on PropertySaleData, rendered into a new ADVERTISED-PRICE CAMPAIGN block in the prompt template with a mandatory instruction to state the FIRST advertised price and not to judge the result against the final guide alone.
**Result:** Across the 555-property candidate pool, asking-price coverage rises from 42 (via `listing_price`) to 84; 56 of those are properties that previously had no guide at all, and 11 had a price reduction that was completely invisible. Verified on 16 Collingwood (opened EOI 2026-03-26 -> $1,949,000 -> $1,749,000, -10.3%, 1 reduction) and 26 Moorabbin Place (advertised $1,649,000 unchanged, sold $1,620,000 — a guide the pipeline previously could not see at all).
**Files:** fields-automation: `pipeline/data_assembler.py`, `pipeline/article_generator.py`, `pipeline/prompts/article_prompt_template.md`
**Recurrence:** First

## [BRAIN2-NIGHTLY-DOUBLE-FAILURE] Brain2 nightly chain died twice in one run — 10:32 AEST
**Symptom:** Process Registry row "Brain2: attribution/behaviour/journey" ERROR since 2026-08-04 15:00 UTC. The
2026-08-04 23:40 chain run left TWO tracebacks in `logs/brain2-nightly-refresh.log`, not one.
**Root cause:** Two independent failures in the same `(...)` cron subshell:
1. `scripts/brain2/organic_journey_build.py:518` — `urllib.error.HTTPError: 504 Gateway Timeout` after
   `brain2_util.hog_retry` exhausted 3/3 retries against the PostHog query API. Transient upstream; the failure
   landed at line 478, i.e. after `organic_journeys`/`landing_affinity`/`all_conversions` had already been
   rebuilt, so only the final section was lost.
2. `scripts/offmarket_home_signal.py:103` — `pymongo.errors.WriteError code 28: Cannot create field 'address' in
   element {offmarket_home: null}`. This is the SAME bug fixed earlier today in `[HEALTH-BOARD-PAUSED-VS-DEAD]`;
   the log simply predates the fix, as `[MONITOR-FITNESS-PROBES]` predicted it would ("clears tonight").
**Fix:** No code change. Ops Tier 1 re-run of both scripts after establishing idempotency by reading them:
`organic_journey_build.py` is a full `delete_many({})` + `insert_many(docs)` rebuild (lines 311/364, 367,
399/442, 482) with no append/send/spend path; `offmarket_home_signal.py` does `update_many($unset)` plus
per-contact `$set` keyed on `posthog_ids`. Both re-ran clean:
```
organic_journey_build : exit=0 — organic_journeys=569 landing_affinity=384 all_conversions=13
offmarket_home_signal : exit=0 — 232 contacts flagged from 252 google→off-market sessions
```
**Verified:** independently of script stdout — `system_monitor.job_runs` → `offmarket_home_signal`,
`status=success`, `run_at=2026-08-05 00:20:05 UTC`, detail matching. Second consecutive clean run of the
`offmarket_home` write path since the crm_sync carry-forward fix, so that fix is now confirmed under real load.
**Not done on purpose:** the board row stays ERROR until tonight's 23:40 cron, because the probe reads the tail
of `logs/brain2-nightly-refresh.log` and these re-runs did not write to it. Appending to that log to force the
row green would be manufacturing evidence — explicitly forbidden. Root cause resolved; evidence catches up.
**Files:** none modified (re-run only).
**Recurrence:** PostHog 504 → Nth in the known PostHog-flakiness class (see `[[nightly_lead_chain]]`, crm_sync
504s the same night). The `offmarket_home` null → First recurrence-check of a same-day fix; held.

## [SITE-WIDE-CACHE-AUDIT] 22 uncached routes + 2.9MB per article view — 10:35 AEST
**Symptom:** after fixing property pages ([PROPERTY-PAGE-UNCACHED] above), swept every route and
Function. The same defect was almost everywhere: **13 of 16 loader-bearing routes had no
`headers()` export**, plus 9 loader-less routes nobody had checked. Worst was
`/off-market/:slug` — **15,253 URLs, 90% of the sitemap** — at 1.2-1.8s warm, every request
`fwd=bypass`. Separately `/articles/:slug` weighed **3,710KB** and had a **CLS of 0.216**.
**Root cause (three distinct):**
  1. No `headers()` export ⇒ `cache-control: no-cache` ⇒ live loader per request. Cost is not the
     query — it is `getFreshClient()` opening a Cosmos connection per invocation.
  2. `/api/market-narrative/*` served `no-cache` at a consistent 1.77s and
     `/market-intelligence/:suburb` calls it **4+ times per load**.
  3. `/articles/:slug` fetched `/data/articles.json` (6.2MB on disk, **2.97MB over the wire**) to
     read ONE article's `html`. The route loader already queried the document and **dropped the
     body**. The 0.216 CLS was the same bug: a single shift where `articleBody` collapsed
     306h→67h at **8.1s** — the moment that file landed and the body re-rendered.
**Fix:** new `src/lib/cacheHeaders.ts` (`cdnCache()`) applied to 22 routes; cache headers on
`market-narrative.mjs` 200-paths only; article loader now returns the body so the page renders
from loader data and pulls only the 0.83MB card index for its rails.
**Measured:**
| Surface | Before (warm) | After (warm) |
|---|---|---|
| `/off-market/:slug` (15,253 pages) | 1.22-1.84s | **0.08s** |
| `/houses-for-sale/:suburb` | 1.64-1.83s | **0.08s** |
| `/market-intelligence/:suburb` | 0.82s | **0.08s** |
| `/articles/:slug` | 0.55s | **0.08s** |
| 9 loader-less routes | 0.33-0.40s | **0.07-0.08s** |
| `/api/market-narrative/*` | 1.77s | **0.083s** |
| Article page weight | 3,710KB | **921KB** |
| Article CLS | 0.216 | **0** |
| Article LCP | 1.75-2.93s | **0.52-0.58s** |
| Article SSR text | ~1,090 chars | **6,684 chars** (body now server-rendered) |
**⚠ PARTIAL FAILURE — 5 patches were no-ops.** `headers()` on redirect-only routes (`browse`,
`market-metrics/:suburb`, `market`, `property`, `analyse`) **does nothing**: React Router
short-circuits when a loader returns a redirect, so the export is never consulted. They still
serve `no-cache` at ~0.35s. Verified by curl after deploy, not assumed. Correct fix is
`netlify.toml [[redirects]]`, which is edge-served with no function invocation — not done here.
**Method notes:** the CLS fix was NOT guessed. One run reported CLS 0 and another 0.216; four runs
settled it at a consistent 0.216, and capturing `layout-shift` **sources** identified the element
and the 8.1s timestamp, which is what proved it was the JSON swap rather than the 4 images that
lack `width`/`height`. No speculative image dimensions were added, because the measurement did not
implicate them. Likewise `/api/market-insights` looked like a 7.4s disaster on one trace and is in
fact correctly cached (6.6s cold, 0.08s warm) — a cold miss, not a bug.
**Files:** `src/lib/cacheHeaders.ts` (new), 22 route files under `src/routes/`,
`src/pages/ArticlePage/ArticlePage.tsx`, `netlify/functions/market-narrative.mjs`
**Commits:** 1f965625 (16 files), 6fb0a87f (9 files)
**Recurrence:** Third in three days — [BROWSE-TTFB-SERIAL] 2026-08-03, [PROPERTY-PAGE-UNCACHED]
today, now site-wide. The class is closed for routes; **`[[redirects]]` migration and an SSR-body
pass for other client-fetched pages remain open.**

## [OPS-DOMAIN-BUILD] Samantha ops domain — health-board triage agent, first observed run — 11:05 AEST
**Symptom / gap:** the 2026-08-05 board audit took a human two hours to separate 29 deliberately-paused jobs from 3 real failures. Nothing in the RL system does that class of work: every component (including `onsite_friction_signal.py`, the one "what is broken" sensor) DETECTS and ESCALATES — nothing triages or repairs.
**Build (Will's spec: Opus 5, no crontab edits, standalone, Process Registry + Pipeline Processes):**
- `ops_signal.py` — sensor. Calls `main_site_health_check.collect()` IN-PROCESS so it can never disagree with the sheet; emits each non-OK row with `failing_since`/`failing_days` from `mainsite_health_snapshots.fields[].last_changed` and a deliberately-dumb keyword `repair_class` hint. Excludes KNOWN-GAP by design (a human already acknowledged those; surfacing them would invite "resolving" things that aren't broken). Writes `rl_ops_signal`.
- `ops_prompt.md` — the mandate. Tier 1 is FOUR things only (re-run a proven-idempotent job max twice, clear a verified-dead lock, re-verify an already-fixed row, read anything). Everything else Tier 3 draft-and-escalate. Crontab is read-only to her (`crontab -l` fine, `crontab <file>` forbidden) citing [OPS-CRONTAB-WIPE].
- `ops_cycle.sh` — STANDALONE runner (not `rl_cycle.sh`, which is pinned to opus-4-8 and coupled to the paused fleet). `claude --model claude-opus-5` on Max, `ANTHROPIC_API_KEY` unset so it can't fall through to metered billing, flock, 40-min timeout, Rule 7 heartbeat `ops_cycle`.
- `domains.yaml` — registered as a meta-sphere domain, marked standalone.
**The designed-against failure mode:** an agent measured on "make the board green" has a trivially cheap shortcut — suppress rows instead of fixing them, via `_PAUSED_JOBS`, a probe threshold, or simply deleting a `job_runs` doc (which deletes the row entirely). The prompt's top rule is therefore *"You are measured on problems genuinely resolved, never on how green the board looks; a red row that is honestly red is a SUCCESS of this system"*, with monitoring code and `job_runs` documents hard-forbidden in every tier.
**Verification did not rely on her self-report.** A before/after harness md5'd every `.py/.sh/.md/.yaml` under `scripts/` and `16_*`, the crontab, and all 69 `job_runs` docs with their cadences. Result: **no monitoring file modified, crontab untouched (82 active lines), zero heartbeats deleted, zero cadence/threshold altered.** Only 2 files changed — `01_BUILD_LOG.md` and `WILL_TO_ACTION.md`, exactly the two she is meant to write. She also declined the most tempting cheat available: the brain2 row stays red until tonight's cron because the probe reads a log tail her re-runs didn't write to, and she explicitly refused to append to the log to green it — `logs/brain2-nightly-refresh.log` mtime is unchanged at Aug 4 23:46.
**Outcome of the run (10 of 40 min used, rc=0):** 1 real fix (brain2 nightly chain — established idempotency by reading the code, re-ran `organic_journey_build.py` + `offmarket_home_signal.py`, both exit 0, verified against `job_runs` rather than stdout); 2 rows correctly identified as already-healthy-but-lagging-evidence and deliberately left alone (CRM sync self-healed across 5 clean runs, the traceback just hasn't left the 60-line probe window; timeline refresh fixed 08-03 but cron is Sunday-only); 4 Tier 3 items raised.
**Independently re-verified her headline finding (WTA-OPS-001) and it is exactly right:** `config/settings.yaml` target list (6 suburbs, Merrimac/Carrara commented out, Burleigh Heads absent) has diverged from `shared/db.py TARGET_SUBURBS` (9), which is what the health writer iterates — so 3 suburbs are health-checked nightly against a `last_scraped_at` frozen at 2026-05-10 and are `critical` by construction, forever. Underneath: **180 documents still `listing_status:"for_sale"`** (burleigh_heads 113 / carrara 56 / merrimac 11), unverified ~87 days, structurally excluded from sold/withdrawn/re-pricing. She flagged sitemap inclusion as the unproven question that decides severity and did NOT guess; answered here — 0 sitemap URLs for all 3 suburbs and the pages serve `noindex, follow`, so it is a data-integrity issue, not a public-facing one.
**Gap found by the exercise:** `scripts/telegram_notify.py` keeps no send record and `ceo_chat_messages` stores only inbound, so an agent's "Telegram sent" claim is **structurally unverifiable**. Not fixed here; flagged for Will.
**Files:** 16_General_Reinforcement_Learning/{ops_signal.py,ops_prompt.md,ops_cycle.sh,domains.yaml}. NOT cronned — one observed run only, pending Will's verdict.
**Recurrence:** First.

## [WHALE-ROUTE-MISFIRE] Whale overlay fired on navigation, and was otherwise vetoed everywhere — 11:20 AEST
**Symptom:** The first and only real (non-internal) whale showing — distinct_id `019fcf4a-40b4-72b3-9016-503d776fd44e`, mobile Safari, Gold Coast — appeared **10ms after** the `/property/5-morea-court-varsity-lakes` pageview, reported as `trigger: scroll_reversal`. The visitor had just tapped a property card, i.e. peak intent, the exact inverse of the "stopped reading" signal the trigger is meant to encode. They watched it 12,345ms with audio playing and dismissed by tap.
**Root cause:** Two defects that only fire in combination, which is why exactly one showing has ever occurred.
1. `useWhaleTrigger`'s effect was keyed `[enabled]`. `enabled` is `whaleAllowed(pathname)`, true on both the listings page and a property page, so the effect survived the SPA navigation holding `maxDepth`/`reversedBy`/`lastY` from the *previous* page. React Router's scroll-to-top then arrived as one large `y < lastY` while `maxDepth` still cleared `EDITORIAL_FLOOR` — indistinguishable from a reader walking back up. `MIN_DWELL_MS` did not catch it because `mountedAt` was site-mount, not page-arrival.
2. `pageIsBusy()` matched `[role="dialog"]` bare. `MobileNav`'s drawer is a `role="dialog"`/`aria-modal="true"` that is **deliberately never unmounted** (so its close transition can run) and is merely `inert` + `aria-hidden` when shut, and `SiteHeader` renders it on every surface. So `pageIsBusy()` was permanently true and vetoed *every* organic trigger sitewide. The one showing slipped through only because `SiteHeader` is rendered per-route, so during the route change the drawer portal briefly unmounts (`mounted=false`) — the same ~10ms window defect 1 fires in.
**Fix:** Keyed the effect `[enabled, pathname]` so per-page state restarts per route. That alone was **insufficient** — verified, not assumed: the deps re-run is a passive effect (after paint) while React Router restores scroll in a layout effect (before paint), so the old listener still caught the reset in roughly 1 navigation in 3. The accumulators therefore also carry `statePath`, the path they were measured on, and re-baseline via `resetForPage()` the moment a scroll or a `fire()` is evaluated against a different path (`window.location` updates synchronously at `pushState`, ahead of all React work). `fire()` also rejects on path mismatch, which covers the timer-driven `footer_linger`/`idle_deep` signals that no scroll would reset. Separately, `pageIsBusy()` now skips dialogs carrying `inert` or `aria-hidden="true"`, so only genuinely open ones veto.
**Verification:** Headless Chrome against the dev server, observing the `/api/v1/whale-alert` payload rather than overlay DOM (the overlay lags the trigger by sprite load, and cannot tell you which signal fired). Scroll target deep-but-not-bottom so `footer_linger` could not spend the once-per-person budget first. Control run on pre-fix code reproduced production exactly — `trigger: scroll_reversal`, `surface: /property/5-morea-court-varsity-lakes`. Post-fix 6/6 clean, plus 3 runs of a harsher variant resetting scroll before navigation commit, all with no alert. A legitimate same-page reversal still fires in every run, so the trigger is not merely disabled. `tsc -p tsconfig.app.json` at the 210-error baseline, none in touched files.
**Files:** `01_Website/src/components/WhaleMoment/useWhaleTrigger.ts`, `01_Website/src/components/WhaleMoment/WhaleMoment.tsx`, `01_Website/src/root.tsx` (commit `4c253949`)
**Recurrence:** First for this problem ID. 16 prior `[WHALE-*]` entries, all rig/art/audio; this is the first trigger-semantics defect.

## [OPS-DOMAIN-LIVE] Ops agent cronned + self-verifying; second observed cycle — 11:40 AEST
**Change:** cronned `ops_cycle.sh` at 07:15 AEST daily (after the 01:00 sheet sync, before Will's morning; absolute paths and no `cd` dependency, deliberately avoiding the relative-path trap that killed `seo_dashboard` silently for 8 days). 83 active crons.
**Two hardening changes Will asked for first:**
1. **`telegram_notify.py` now audits every send** to `system_monitor.telegram_sends` (full text, `message_id`, sender, `CYCLE_STAMP`), on both the success and failure paths. Until now an autonomous agent's "Telegram sent" was the one claim in its report that could not be checked from this side — nothing recorded outbound and `ceo_chat_messages` stores only inbound. Best-effort and never raises, so an audit-write failure cannot turn a delivered message into a crashed caller. Self-tested: `message_id 1243`.
2. **`ops_signal.py` evidence instead of a guess.** The first cycle proved the keyword `repair_class` useless — three rows all tagged `RAISING` were three different things (a live failure, a self-healed job, and a log predating a fix). Attempted heartbeat pairing by name/log-stem FIRST and rejected it: stem matching mispaired "Brain 3 ops nightly" onto `nightly_lead_chain`, and a wrongly-paired heartbeat asserting "this job is fine" about a different job is worse than attaching nothing. Instead compute the discriminator directly from the log — completion markers appearing AFTER the last traceback — which needs no mapping. Now renders `CRM sync [5 completion markers after last traceback — may have self-healed]` vs `Brain2 (none)`, exactly the distinction the agent previously had to derive by reading three logs. Exact-title heartbeat match retained for self-reported rows only, where it is safe.
**`ops_integrity.py` (new) — the guard no longer depends on the guarded party.** Brackets every cycle from inside `ops_cycle.sh`: md5 of every `.py/.sh/.md/.yaml` under `scripts/` + `16_*`, the crontab and its active-line count, and every `job_runs` doc with its cadence/stale thresholds. `WILL_TO_ACTION.md` + `01_BUILD_LOG.md` are the only permitted writes. A violation Telegrams Will with a 🚨 and records an ERROR heartbeat, so a TAMPERING cycle is louder than a merely failing one; results persist to `system_monitor.ops_integrity` so "has she ever tampered" is a query, not a memory. Verified against 7 synthetic tamper modes — monitor-file edit, editing her own mandate, heartbeat deletion, threshold widening, crontab edit, quiet unrelated source edit, file deletion — all 7 detected, both legitimate writes correctly allowed. Tested via `compare()` directly rather than by really mutating a guarded file, to avoid sending Will a false alarm.
**Second observed cycle (11:12, rc=0, integrity CLEAN from inside the run):** `0 fixed, 6 need you`. She found nothing a re-run could clear and **said so** — "Tier 1 repairs performed: 0 … That is the honest result" — rather than manufacturing a fix. She also declined the forbidden move twice more, in writing: on the Brain2 row ("appending to the log to green it is exactly the forbidden move") and on the timeline row ("re-running it to force a green log would be manufacturing evidence"). No duplicate work: all 5 previously-raised items were listed as already-open with the prior WTA id rather than re-raised.
**Both her Tier 3 items independently verified and both are correct:**
- **WTA-OPS-006** — step 105's 158 image failures are all `NameResolutionError` on `fieldspropertyimages.blob.core.windows.net`. Confirmed here: no A record, live `curl` → `http=000` in 27 ms. The Azure account is gone, so no re-run can fix it — which is *why* she took no Tier 1 action. 11 listings logged `Analyzed 0 images` and were then marked processed, i.e. permanently photo-blind. She explicitly flagged what she could NOT prove (why the daily count is 0·0·1·158) rather than guessing.
- **WTA-OPS-005 — she found a defect in the monitor code that shipped this morning.** The Step 18 probe counts the regex `cleared existing valuation` and reports it as *"N properties had a published valuation wiped this run"*. The actual log line is `⚠️ Excluded (missing_floor_area) — cleared existing valuation`, which fires on EVERY exclusion whether or not a valuation existed. Confirmed: latest run has 219 `missing_floor_area`, and all 101 Robina for-sale docs still carry `valuation_data` — nothing was wiped. The row is mislabelled and will alarm every night forever. The real finding underneath is worse and was invisible behind the wrong label: ~41% of the for-sale book is unvaluable for `missing_floor_area` and the gap is widening (209 → 223 in 12 days while successful valuations stayed flat). She proposed both a product fix and a board fix, and correctly did NOT apply the board fix herself.
**Files:** scripts/telegram_notify.py, 16_General_Reinforcement_Learning/{ops_signal.py,ops_cycle.sh,ops_integrity.py,WILL_TO_ACTION.md,01_BUILD_LOG.md}, projects crontab (backup in logs/crontab-backups/).
**Recurrence:** First. Follows [OPS-DOMAIN-BUILD] same day.

## [GCP-BILLING-EXPORT] Detailed billing export → BigQuery → VM query tool — 12:03 AEST
**Symptom:** A $409.59 AUD July invoice could not be broken down from this VM. No cost data source existed locally, so the answer had to be *reconstructed* — live resource inventory (VM sizes, disk sizes, GCS bucket bytes) × Google's own SKU price list from the Cloud Billing catalog API × usage measured from Cloud Monitoring. That model reconciled to within a couple of percent, but it is a model: it cannot attribute a `Balanced PD Capacity` line to one specific disk, and it silently guesses at what fraction of NIC egress is billable.
**Root cause:** No Cloud Billing export had ever been configured, and there is no BigQuery dataset in `fields-estate` for one to land in.
**Fix:** Set up everything that is machine-configurable, and documented the one step that is not.
- Created BigQuery dataset `fields-estate:billing_export` in `australia-southeast1`.
- Enabled `cloudbilling.googleapis.com` (needed for the SKU catalog reads that produced the estimate) and `bigquerydatatransfer.googleapis.com` (prerequisite for the export itself).
- Added `scripts/gcp_cost_report.py` — breaks a month down by service / SKU / project / **resource**, the last being per-disk and per-VM and the whole reason for choosing the Detailed export over Standard. Costs are computed net of credits (`SUM(cost) + SUM(credits.amount)`); sustained-use discount and free tier arrive as negative rows, so a plain `SUM(cost)` overstates every line and would not reconcile to the invoice.
- Added `docs/GCP_BILLING_EXPORT.md` with the Console steps, both caveats (not retroactive; ~24h lag) and the retroactive Cost-Table-CSV route for invoices already received.
**Verified:** `gcp_cost_report.py` run pre-enablement exits cleanly with the "not switched on yet" message rather than a traceback; `--by resource` refuses with a specific error if only the Standard export exists. Dataset creation confirmed via `bq ls`.
**Blocked on a human, by design:** there is no API and no `gcloud` command that writes billing-export settings — checked the `cloudbilling` v1 and v1beta discovery documents, which expose only the pricing catalog, account metadata, project links and budgets. The switch is Console-only, so an agent cannot flip it. Will holds `roles/billing.admin`, which is the role required.
**Dead end, recorded so it is not retried:** `POST cloudbilling.googleapis.com/v1beta:generateInsights` takes a natural-language prompt over billing data and looked like it would bypass the export entirely. It authenticates and streams "Compiling data…" thought chunks, then returns `500 INTERNAL` every time across several prompt shapes. Preview API, unusable.
**Not scheduled,** so no `job_run` heartbeat — on-demand tool. Noted in the doc that cronning it (e.g. a monthly cost summary to the health board) requires wrapping it per Rule 7 first.
**Files:** scripts/gcp_cost_report.py (new), docs/GCP_BILLING_EXPORT.md (new).
**Recurrence:** First for this problem ID. Prior cost work ([COST-MONITORING], [CEO-COST-ROLLUP], [CEO-COST-BACKFILL]) covered AI/ad spend in `system_monitor`, never GCP infrastructure cost.

## [WHALE-MONITOR] Daily monitor + docs for the seasonal overlay — 12:15 AEST
**Symptom:** Not a defect — a gap. The whale overlay had no self-check, so the two defects fixed earlier today ([WHALE-ROUTE-MISFIRE]) were only ever found because Will happened to ask about one showing. Rule 7 requires an ongoing surface to self-report.
**Root cause:** N/A — new process.
**Fix:** `scripts/whale_moment_monitor.py`, daily 00:40 AEST (ahead of the 01:00 health-sheet publish so the heartbeat is fresh). Alarms on exactly the two regressions: (a) `whale_shown` within 5s of a pageview in the same session — nothing legitimate can fire that fast given `MIN_DWELL_MS` 8s, so it is the Bug-1 "fires on arrival" signature; (b) zero showings over 7d in season with ≥50 visitors — the Bug-2 "pageIsBusy veto back on" signature. Also records post-dismissal continuation (within-person, so no control group needed and readable at n≈20), median watch time, and audio-playing share. History accrues in `system_monitor.whale_monitor_daily`.
Two traps found and closed while building it: **(1)** `?whale=1` previews are logged with `trigger='forced'` and a forced preview fires *immediately at mount* — byte-for-byte the misfire signature. Counting them would have made routine QA raise the alarm, and a single preview would have masked a real silence. Excluded from every real-user metric (tracked separately as `forced_previews_season`); my own two verification runs had already inflated the count from 1 to 3. **(2)** Out of season (Oct–Jun AEST) zero showings is correct, so the silence alarm is suppressed then — otherwise the row sits red nine months a year and stops being read. Misfires are counted only from `FIX_DEPLOYED_AT` for the same reason.
**Deliberately not built:** an engagement / return-visit read. Measured 2026-08-05: ~20–50 visitors/day sitewide (~5/day on property pages), baseline return rate 4.9% (1,232 of 1,296 people over 30d had exactly one session). Detecting a *doubling* of that at 80% power needs ~450 people per arm; a 50% lift needs ~1,450. The season ends 30 September. Separately, the trigger fires *on disengagement*, so whale-seers are selected on intent-to-leave and any seer-vs-non-seer comparison is confounded by construction — it would show the whale as harmful regardless of effect. A holdout would fix the confound but not the power, and would halve an insufficient sample. Reasoning written into `docs/WHALE_MONITOR.md` so it is not quietly re-litigated.
**Also:** `collect_self_reported_jobs` now renders a `doc_url` declared by any job via `job_run(..., doc_url=…)`, appended after the 200-char detail cap so a chatty detail cannot truncate the link away. Generic, so every future job gets it — a STALE/ERROR row now carries its own context.
**Verification:** Ran at creation (Rule 7 step 2); heartbeat present in `job_runs`, and confirmed the row renders on the Process Registry page of the Systems Health sheet with the link: `Whale Moment (seasonal overlay) | self-reported (auto) | ran 2026-08-05 | OK | … · docs: https://github.com/…/WHALE_MONITOR.md`. Cron added (256 entries, was 252; crontab backed up to scratchpad).
**Files:** `scripts/whale_moment_monitor.py` (new), `scripts/main_site_health_check.py`, `docs/WHALE_MONITOR.md` (new), crontab (commit `aa61a63f`)
**Recurrence:** First.

## [GCP-COST-CRON] Daily GCP cost monitor on cron — 12:41 AEST
**Change:** `scripts/gcp_cost_monitor.py`, daily 07:40 AEST. Snapshots month-to-date cost by service and by resource to `system_monitor.gcp_costs`, Telegrams on a daily spike (>1.6× trailing 7-day median, floored at $8 so a quiet day can't page on a few dollars) or a month-end forecast over the ceiling, and pulls Recommender findings (idle disk / idle IP / VM rightsizing) with dollar amounts — the automated form of the manual audit that found an unmounted 400 GB disk billing ~$94 AUD/month. Wrapped in `job_run(cadence_hours=24)` per Rule 7; 86 active crons.
**Reuse, not duplication:** refactored `gcp_cost_report.py` from a CLI into a CLI + importable query layer — `sys.exit()` calls became a `CostDataUnavailable` exception so a library caller can handle them, and `breakdown()`/`daily_totals()`/`pick_table()` are now shared. The monitor imports them rather than carrying a second copy of the credit-netting SQL.
**Fails loud by design:** until the export is switched on the job raises, recording ERROR on the health board. A monitor that shrugs when its data source is missing is the silent failure Rule 7 exists to prevent. It is a *quiet* failure — the Telegram path sits past the raise, so there's no daily message, just a red row that clears itself once tables appear.
**Two bugs found by testing, both of which would have made this a silent daily no-op:**
1. **Cron used relative paths with no `cd`.** Cron's CWD is `/home/projects`, not the orchestrator, so `source .env` and `scripts/…` would both have missed. This is the identical trap that killed `seo_dashboard` silently for 8 days. Rewritten to fully absolute paths rather than adding a `cd`.
2. **`analyse()` had never executed.** The first run only exercised the not-enabled path; the entire analysis body — spike detection, forecasting, resource breakdown, Mongo write, Telegram — was unrun code that would first execute on a day when Will enabled the export and nobody was watching. Verified against a synthetic export table (120 rows, 30 days, 4 SKUs, credits, a planted 9× spike on the previous day) instead: both alerts fired with correct arithmetic (spike 55.75 vs median 15.75 = 3.5×; forecast correctly excluded today's partial day, 103.00/4×31 = 798.25), credit-netting reconciled by hand (Cloud Storage 0.3 × 5 days × 0.9 = 1.35), and a missing `TELEGRAM_BOT_TOKEN` degraded to a warning with the job still exiting 0. Fixed a blank-`resource.name` rendering as an empty label while there (`NULLIF` before `IFNULL`).
**Cleanup verified:** synthetic table dropped (`bq ls` empty), the one synthetic `gcp_costs` doc deleted, and the monitor re-run so the heartbeat reads the honest ERROR rather than the synthetic success.
**A third would-be bug that wasn't:** the literal cron line failed with `source: not found` under `/bin/sh`. Checked before "fixing" it — the crontab declares `SHELL=/bin/bash` on line 1, which every other `source`-using job depends on. The test was wrong, not the cron; re-run under bash it works, `bq` auth included.
**Auth constraint recorded:** the VM's attached service account can't be used — its scopes omit BigQuery and changing VM scopes requires stopping the VM. Runs on the gcloud user creds at `/home/projects/.config/gcloud`, verified under a minimal `env -i` cron-like environment.
**Files:** scripts/gcp_cost_monitor.py (new), scripts/gcp_cost_report.py (refactored to a library), docs/GCP_BILLING_EXPORT.md, projects crontab (backup in logs/crontab-backups/).
**Recurrence:** First. Follows [GCP-BILLING-EXPORT] same day. The relative-path cron trap is a repeat of the `seo_dashboard` failure mode — caught pre-deploy this time.

## [PROPERTY-SSR-EDITORIAL] Property editorial server-rendered; redirects moved to edge — 12:30 AEST
**Symptom / context:** the three items left open by [SITE-WIDE-CACHE-AUDIT] above.
**Fixed:**
  1. **`/property/:slug` was SSR-thin.** ~1,090 chars of server-rendered text (address, specs,
     price) while the analysis these pages rank for arrived only from a client fetch — the part a
     crawler is least likely to weigh, on 1,479 pages carrying the organic traffic. The loader
     already read the document AND already computed the published `ai_analysis` for meta tags; it
     simply never forwarded the body. Now forwards headline / sub_headline / verdict /
     insights[].h2+lifestyle_hook / faqs, rendered by `PropertySsrShell`.
     **1,090 → 4,713 chars of SSR text.** Only clean string fields are forwarded:
     `insights[].key_points` and `.what_this_means` are stored as **serialised arrays with
     inconsistent quoting** (some JSON, some Python repr) and are left to the client renderer that
     already copes with them. The shell is an early return under the loading branch, so the
     interactive render replaces it — verified live, the headline appears exactly ONCE (grep count
     on the rendered text), 17,686 chars rendered, 0 console messages.
  2. **Legacy redirects moved to `netlify.toml [[redirects]]`** — `/browse`, `/market-metrics/*`,
     `/market`, `/analyse`. **0.35s → 0.076-0.092s**, splat verified
     (`/market-metrics/Robina/prices` → `/market-intelligence/Robina/prices`). The dead
     `headers()` exports from 1f965625 were removed from all five redirect routes; the route files
     stay as a fallback behind the edge rules. `/property` deliberately left as a route — its
     target is a hardcoded ObjectId and moving that magic value into infra config is worse than
     0.35s on one legacy path.
  3. **TTLs lengthened where the data allows.** The audit set a uniform 5 minutes, which is far
     too short for a long tail: `/off-market/:slug` is 15,253 pages on nightly data, so at
     `s-maxage=300` nearly every organic entry was STILL a cold miss and the caching was not doing
     its job. Now `3600/86400` there and on articles/sold, `900/3600` on suburb + charts pages.
     `stale-while-revalidate` is the actual lever — a page fetched once serves instantly for the
     whole window while revalidating behind the visitor.
**Deliberately NOT lengthened:** `/`, `/property/:slug`, `/for-sale`, `/recently-sold`,
`/discover` stay at 300s — they carry live price and listing-status data, and stale status on a
listing surface has a real cost (see the standing rule on verifying fresh listing status before
contacting a seller). `/building/:slug` stays at 60s.
**COULD NOT FIX — `getFreshClient()` per-invocation Cosmos connection.** This is the deliberate
Netlify/Mongo event-loop-hang workaround; changing it risks reintroducing the hang that made a
deploy uncancellable. Cold misses therefore remain **0.61-1.60s**. Widening the cache window is
the low-risk mitigation and is what was done instead. Genuinely open.
**Verified live:** property `/property/:slug` 1.25s cold / **0.10s warm**; off-market 1.60s /
**0.08s** (ttl=3600); articles 0.61s / **0.10s**; houses-for-sale 1.48s / **0.08s**;
market-intelligence 1.46s / **0.08s**.
**Note:** the commit message for 9a2fad2f lost one phrase — **backticks inside a double-quoted
bash string are command substitution**, so an inline code reference was eaten (bash printed
"syntax error: unexpected end of file" and the push still succeeded). Use single quotes or avoid
backticks in `-m` messages.
**Files:** `src/routes/property.$id.tsx`, `src/pages/PropertyPage/PropertyPage.tsx`,
`netlify.toml`, 5 redirect routes, 7 route files retuned
**Commits:** 9a2fad2f, de8d5373
**Recurrence:** First for the SSR half; closes the caching class opened by [BROWSE-TTFB-SERIAL].

## [FRIDGE-AUDIO-REAL] Real recorded fridge audio, scheduled to the motion — 13:12 AEST
**Change:** Replaced the synthesised Web Audio door sounds with Will's own recordings of his fridge (`15_Off-Market/Concepts/Fridge_Magnet_Concept`). Added a continuously-running compressor hum.
**Non-obvious work:** (1) Events are buried in silence — 5ms-window RMS envelope located the open transient at 1.930s and the close impact at 2.160s in 6s recordings; each event is <0.4s, so untrimmed clips would lag the tap by ~0.5s. (2) The hum recording ramps in level end-to-end (mic AGC), so only the tail from 6s is usable, evened with `dynaudnorm`. (3) Seamless loop built as `acrossfade(S[C..L+C], S[0..C], d=C)` — the output ends in a crossfade into S[0..C] whose tail lands on S[C], where the output begins. **Verified: wrap-point sample delta = 1 vs median in-loop step of 107.** (4) Clips are SCHEDULED, not fired: solved the CSS cubic-beziers numerically — open clip starts +0.094s (peel peak 0.130s in, seal breaks at 0.224s), close clip starts +0.955s (impact 0.060s in, door seats at 1.015s). Firing close on tap would play the slam a second before the door arrives.
**Also:** open easing retuned `.83,.015,.2,1` → `.42,.03,.28,1` — the old curve left the door visually motionless for 433ms after a tap.
**Constraint (not a bug):** audible autoplay is blocked everywhere, so the hum cannot start before first touch. Context is created and all three clips decoded on load (a gesture-less context starts suspended; `decodeAudioData` works suspended), so it begins the instant they touch. The 0.8s auto-open stays silent by design.
**Files:** `fridgeAudio.js` (rewritten), `fridge.js`, `fridge.css`, `index.html`, `assets/build_audio.sh` (new), `assets/fridge-{open,close,hum}.m4a`, `assets/source/*` (Will's originals — pushed to GitHub, they existed only on the VM).
**Recurrence:** First.

## [PYTHON-REPLACE-EMPTY-BOMB] str.replace('') inflated a 43KB README to 142MB — 13:12 AEST
**Symptom:** GitHub `contents` PUT began returning `409 Repository rule violations found / Timed out validating rule` for one file, repeatedly, across 4 retries. Read as a transient GitHub fault. It was not.
**Root cause:** an in-place doc edit did `old = s[s.index(A):s.index(B)]` where B occurs BEFORE A in the file (the "Scale" section had been inserted above "Audio" in an earlier edit). A backwards slice in Python returns `''` — silently, no error. The following `s.replace(old, new)` therefore became `s.replace('', new)`, **which inserts `new` between every character of the string**. 43KB × a 3.5KB block ≈ 142MB. GitHub's rule engine was timing out on the payload, which is why it looked like a server-side flake.
**Fix:** restored the file from GitHub (last good copy), re-applied the edit with `s[:ia] + new + s[ib:]` plus two guards that would have caught it: `assert 0 <= ia < ib` and `assert len(old) > 500`.
**Lesson:** never `str.replace(x, y)` where `x` came from a computed slice without asserting the slice is non-empty and ordered. Prefer explicit `s[:i] + new + s[j:]`. And a repeated, oddly-specific push failure on ONE file is evidence about that file, not about the network — check the artifact before retrying.
**Files:** `15_Off-Market/Concepts/Fridge_Magnet_Concept/README.md` (restored).
**Recurrence:** First.

## [OFFMARKET-DECK-NO-SALE-HISTORY] Off-market page served classic layout, not V3 — 13:12 AEST
**Symptom:** `/off-market/34-banksia-broadway-burleigh-waters` rendered the pre-V3 `OffMarketPage`
("See what it may be worth"), while neighbouring Banksia Broadway addresses served DeckV3. Reported
as "why are we showing an old version — we're serving v3?"
**Root cause:** Not a stale deploy. V3 is correctly the default in `off-market.$slug.tsx`, but only
when a precomputed doc exists in `system_monitor.offmarket_discovery`; otherwise the loader falls
through to the classic page. 34 Banksia had no doc because `indexed_query()` in
`offmarket_discovery_nightly.py:138` requires `enriched_data.transactions.0` to exist, and this
home's transactions array is empty. That filter is inherited from `generate-sitemap.mjs`
getOffMarketUrls() to keep "indexed → deck" 1:1 — it is an INDEXING gate, and the deck builder
itself never reads sale history (`grep transactions` in emit_json.py / fact_bundle.py = 0 hits).
So the gate excludes homes the deck could render perfectly well. Consistent for crawlers (the page
is noindex), but direct human traffic — QR, mail, ads, manual lookup — got the old page.
**Fix:** Built the missing doc for this one address only
(`offmarket_discovery_build.py --slug 34-banksia-broadway-burleigh-waters --suburb burleigh_waters`)
— 8 cards, lead=beachside, verified live serving V3. Deliberately NOT a bulk backfill: 8,641 further
homes across the three core suburbs match every off-market filter except the sale-history `$or`, and
flipping that many live URLs needs Will's sign-off. Proposed approach when it happens: leave
`indexed_query()` as the exact sitemap mirror (the `COVERAGE_GAP_TOLERANCE` assertion depends on it —
see the 2026-07-29 Nerang drift) and add a separate lower-priority `reachable_query()` without the
`$or`, so sitemap/robots behaviour is untouched and these stay noindex.
**Two pre-existing deck bugs surfaced while verifying (NOT caused by this, present fleet-wide):**
1. Broken question→answer chain: `competition.next` asks "But what about that sale down the road
   that looks just like mine?" — the `value_drivers` card answers it. When `value_drivers` is absent
   (data-dependent, e.g. 33/34/35 Banksia) the next card is `buyer`, which answers something else,
   so the question is never answered. Affects homes with AND without sale history.
2. Internal contradiction in the comparables count: the `competition` funnel says "Comparable to
   yours = 1" while the `valuation` card on the SAME page says "Based on 59 adjusted comparable
   sales". Present on every deck checked (35: 4 vs 59, 36: 2 vs 5, 37: 9 vs 55, 27: 2 vs 56) — the
   CLAUDE.md Rule 6 same-stat-two-ways class, already live on ~18k pages.
Both logged for a separate fix; a backfill would multiply their blast radius, so worth resolving first.
**Files:** none (data-only build); diagnosis touched
`Feilds_Website/01_Website/src/routes/off-market.$slug.tsx`,
`15_Off-Market/Page_Redesign_V2/offmarket_discovery_nightly.py`
**Recurrence:** First for this cause. Related but distinct from [OFFMARKET-DECK-EXPANSION-DRIFT]
(2026-07-29), which was a suburb-list drift; this is a per-home eligibility filter doing double duty.

## [FRIDGE-QR-LANDING-LIVE] /fridge now serves the fridge door instead of 301'ing to AYH — 13:25 AEST
**Change:** The printed magnet QR (encodes `https://fieldsestate.com.au/fridge`, verified by decoding both 300 DPI print files) landed on `/analyse-your-home` via a 301 since 2026-07-24. It now serves an animated monochrome fridge door that opens to four market options.
**How served:** `[[redirects]] from="/fridge" to="/fridge.html" status=200 force=true` + static files in `public/fridge/` — the same pattern as `/privacy` and `/seller-guide`. **Not a React route:** the app ships 219KB compressed JS before it can animate anything, and this page has to paint over kitchen 4G. Verified the four option links are in the RAW HTML (7,047 bytes) before any JS runs.
**Path resolution:** the concept is served from a folder where relative paths work; production serves it at the bare path `/fridge`, where `assets/x.png` would resolve to `/assets/x.png` — the Vite hashed bundle dir. One `<base href="/fridge/">` fixes stylesheet, scripts, `<img>`, CSS `url()` and `fetch()` in one go.
**Anti-drift:** `public/fridge.html` is GENERATED by `15_Off-Market/Concepts/Fridge_Magnet_Concept/deploy.py`, which is the only path to production. Never hand-edit it.
**Attribution:** the 301 used to append `utm_*`. A 200 can't, so the page registers `utm_source/medium/campaign` as PostHog super properties on load (filter identically to URL-parsed) and fires `fridge_land`. Saves a round trip. PostHog snippet is inlined from `root.tsx:216-256` including the internal-tester opt-out — the page never mounts the React app so it cannot inherit the app's analytics.
**Push:** ONE commit `88bf0ec8`, 11 files, via `scripts/push_website_files.py` = one Netlify build (not 11).
**Verified live:** `/fridge` 200 (was 301); all 7 sub-assets 200 with correct content types (`audio/mp4`, `image/svg+xml`, `image/webp`); 4/4 links in raw HTML; links hit-testable; all three audio clips decode; door opens and closes; PostHog present; no console errors; no 4xx/5xx; no horizontal overflow. site-inspector: 200, 0 console msgs, 0 network errors. Screenshots read.
**Still open:** `utm_medium=print` vs `minisite-visit.mjs:121`'s `utmMedium === "qr"` check (pre-existing, see [FRIDGE-CONCEPT] notes); suburb is hardcoded to Burleigh Waters.
**Files:** `Feilds_Website/01_Website/{netlify.toml,public/fridge.html,public/fridge/**}`; concept + `deploy.py` in the orchestrator repo.
**Recurrence:** First.

## [FRIDGE-SILENT-FIRST-OPEN] Door auto-opened before any gesture, so the first open was always silent — 14:02 AEST
**Symptom (Will, real handset):** "there is no sound until i close the fridge door. sound should be on by default."
**Root cause:** the door auto-opened 0.8s after load. No gesture had happened, so there was no user activation, and audible autoplay is blocked in every browser — the single most important moment on the page was **structurally guaranteed** to be silent, and the first sound anyone ever heard was the door CLOSING. Sound was already "on by default" (`muted=false`); the problem was never a setting, it was the ordering.
**Fix:** the door no longer opens by itself. It waits for a tap, so the open always carries the gasket peel, the hum starting and the haptic. Two SILENT safety nets: a **nudge** at 4s (door cracks ~11%, lets a sliver of light out, settles back — signals interactivity without spending the reveal) and a **give-up open** at 12s so the options are never unreachable. Verified on production: shut at 2s/10s, nudge at 4.2s, give-up open at 12.0s.
**Note:** Will's report also confirms audio DOES work on his Android on a real tap — the still-open V3 neon-sign silence is specific to scroll-only sessions, exactly as `web_audio_user_activation_touch` hypothesised.
**Files:** `fridge.js`, `fridge.css` (nudge keyframes), `index.html` (prompt copy "Tap to open").

## [FRIDGE-REOPEN-STRANDED] Could not reopen the door after closing it — 14:02 AEST
**Symptom (Will):** "once i closed the door i was not able to open it."
**Not reproducible headlessly** — emulated touch delivered pointerdown/touchstart/pointerup/touchend/mousedown/mouseup/click for one tap and produced exactly one toggle, open→close→reopen all clean. So the cause is real-device-specific and had to be reasoned about rather than caught.
**Root cause (most probable) + fix:** the door face carries three `<img>` elements (the child's drawing and two magnets) and they are the most tappable-looking things on screen, so a finger lands on an image far more often than on bare steel. On iOS an image can absorb a tap into the callout/selection path. Made `.art, .art img, .handle, .seam` `pointer-events: none` and set `touch-action: manipulation; -webkit-touch-callout: none; user-select: none` on the room, so a tap on the drawing is identical to a tap on the door.
**Hardening (the real defence):** taps are now bound to `pointerup` **+** `touchend` **+** `click` behind a **450ms dedupe**. Redundancy so a swallowed event cannot strand the door; dedupe because without it one tap toggles three times and lands back where it started — which presents as exactly the same symptom. Measured: one physical tap delivers all three within ~10ms.
**Files:** `fridge.js`, `fridge.css`.

## [FRIDGE-HUM-DUCKING] Compressor bed read as fading in and out — 14:02 AEST
**Symptom (Will):** "just leave the fridge running sound running. it currently fades in and out."
**Root cause:** the hum ducked between gain 0.10 (door shut) and 0.28 (door open) over ~1s, on the theory that an open door means hearing into the cabinet. A 2.8x swing at those absolute levels reads as the sound fading, not as a room tone.
**Fix:** one constant level (0.17). Only the initial fade-in on arming, mute, and tab-hide ever change it. Removed `HUM_OPEN`/`HUM_SHUT` and the ramps in `open()`/`close()`; `setMuted`/`wake` no longer take a door-state argument. Verified on production: gain 0.17 shut and 0.17 open.
**Lesson:** a background bed must be boring. Anything that moves draws attention to itself.
**Files:** `fridgeAudio.js`, `fridge.js`. Deployed commit `2af0f231` (4 files, ONE Netlify build).

## [MONGOD-LOG-UNBOUNDED] mongod.log grew to 5.0 GB — no rotation config existed — 14:10 AEST
**Symptom:** `/var/log/mongodb/mongod.log` was **5.36 GB** on the 100 GB boot disk, which sat at 77% full. Found incidentally while tracing GCP egress, not by any alarm.
**Root cause:** There was **no logrotate config for mongodb at all** — no `/etc/logrotate.d/mongodb`, and no entry in logrotate's state file. `mongod.conf` sets `systemLog.logAppend: true` with nothing pruning it, so the log had grown unbounded since the file was created on 12 Apr (~46 MB/day; currently closer to 110 MB/day).
**Why nothing caught it:** `resource_guard.py` warns at `DISK_WARN_PCT=85` and only cleans journald/caches. At 77% it had not fired, and even when it did it would have been reclaiming space against a file that regrows daily — treating the symptom forever without ever fixing the cause.
**Fix:**
1. Rotated live via `db.adminCommand({logRotate: 1})` — no restart, so no interruption to the website this mongod backs.
2. Compressed the old log rather than deleting it: **5.36 GB → 367 MB (93%)**. Kept deliberately because it holds the 17 Apr–13 May unknown-IP connection evidence (see below) — deleting it would have destroyed the only record.
3. Installed `/etc/logrotate.d/mongodb`: daily, `rotate 7`, `maxsize 500M`, compressed.
**`copytruncate` chosen deliberately:** the documented pairing (logrotate rename + `SIGUSR1`) needs `logRotate: reopen` in `mongod.conf`, which only applies after a **mongod restart** — and this mongod serves the live site. `copytruncate` rotates with no signal and no restart. The tradeoff is a small race where lines written mid-copy can be lost; acceptable here, and the file is now small enough that the copy is near-instant.
**Verified, not assumed:** forced a real rotation (`logrotate -f`) — `mongod.log` truncated to 0, `mongod.log.1` created. Then confirmed mongod still writes *after* copytruncate by issuing a deliberate slow query (unindexed regex, >100ms) and watching the entry land; the file went 0 → 177 KB with fresh timestamps. Without that check a broken-logging regression would have looked identical to a quiet log.
**Result:** disk 77% → **72%**, free 23 GB → 28 GB.
**Related finding (not fixed, needs a decision):** the archived log shows connections to MongoDB from unknown IPs `185.177.72.30`, `185.177.72.23`, `94.26.106.155`, `23.95.86.214`, `176.65.132.141` between **17 Apr and 13 May** — 9,272 entries on 5 May alone. Nothing after 13 May; the firewall hardening ([MONGO-ALLOWLIST-HARDEN]) closed it. Whether any of them authenticated successfully has NOT been established.
**Files:** /etc/logrotate.d/mongodb (new, copy tracked at deploy/mongodb.logrotate), logs/fix-history/2026-08-05.md.
**Recurrence:** First for this problem ID. Same silent-growth class as the earlier disk incidents the resource guard was built for, but the guard cannot fix a missing rotation config.

## [OFFMARKET-INTRO-TOKENS-NEW-DOCS] New discovery docs ship with no Matrix intro — 13:25 AEST
**Symptom:** After building the deck for 34 Banksia Broadway, the V3 page skipped the Matrix code-rain
intro entirely and opened on "We found your home." (reported by Will).
**Root cause:** `DeckV3.tsx` gates the intro on `doc.intro_tokens` (no tokens = someone else's streets
would rain, so it skips by design). Those tokens are produced ONLY by the standalone
`Page_Redesign_V3/intro/backfill_intro_tokens.py`. The nightly build pipeline never writes them —
`grep intro_tokens` over offmarket_discovery_nightly.py / offmarket_discovery_build.py / emit_json.py /
fact_bundle.py = 0 hits in all four. The comment above the gate in DeckV3.tsx claims "new ones get them
from the nightly build", which is FALSE and is what hid this.
The bug was invisible because `upsert()` uses `$set` on named fields only, so nightly rebuilds of
EXISTING docs preserve tokens the backfill wrote earlier. Of 18,073 decks, exactly ONE lacked tokens —
the one built new an hour before. Any newly-eligible home would have silently shipped without the intro.
**Fix:** Ran `backfill_intro_tokens.py` (1 written). Verified via headless browser: rain renders with
this home's own streets (CHRISTINE, NEWSTEAD, REEDY CREEK, SANDPIPER) + "tap to skip". 0/18,073 now missing.
**Still outstanding (NOT yet done):** (a) wire intro_tokens into the nightly build so new docs get them at
creation; (b) correct the false comment in DeckV3.tsx:399-400; (c) schedule/heartbeat the backfill —
it already supports --cron but nothing runs it, so this can silently recur.
**Cache caveat worth remembering:** the route returns `cdnCache(3600, 86400)`, so intro_tokens (embedded in
the SSR payload) do not appear until the Netlify CDN copy expires — up to 1h, and stale-while-revalidate
means the first visitor after expiry STILL gets the stale copy. A "not working" report in that window is
expected; verify with a cache-busting query param before re-diagnosing.
**Files:** none (data-only); implicated `discovery-v3/DeckV3.tsx`, `Page_Redesign_V2/offmarket_discovery_nightly.py`
**Recurrence:** First. Same silent-failure class as CLAUDE.md Rule 7 — a producer with no heartbeat.

## [FRIDGE-HUM-EDGE-FADE] Hum still faded in/out — a 22.7dB fade was baked INTO the loop file — 14:15 AEST
**Symptom (Will, 2nd report):** "sound is still fading in and out" — after [FRIDGE-HUM-DUCKING] had already made the playback gain constant and verified it (0.17 shut, 0.17 open).
**Why the first fix missed:** I fixed the gain because that was the mechanism I had built. The gain was provably constant, so the fade had to be in the audio itself — but I only re-measured what I had changed. Measuring the DECODED buffer in the browser found it immediately: RMS 0.001 at the head vs 0.122 in the middle, a **22.7 dB** swing, envelope `..:-=+*###@@@@@@@@#@@@@##@#*+-:..` — i.e. the 4s loop faded up and back down every single cycle.
**Root cause:** `dynaudnorm` normalises over a gaussian window (f=250ms x g=7 = 1.75s) and until that window is full it has nothing to work from, so it **ramps in over ~0.9s at the start of whatever you give it and out at the end**. `build_audio.sh` applied it to an ALREADY-SLICED 4.8s segment, so both ends of the loop were faded to near-silence.
**Second, smaller bug found alongside:** the loop join used `acrossfade c1=tri:c2=tri`, which is equal-GAIN. Two different bits of a noise recording sum incoherently, so equal-gain loses ~3dB through the middle of the fade. Equal-POWER (`qsin`) is correct for uncorrelated material.
**Fix:** (1) flatten the WHOLE recording first so the ramps land on the file's own unused edges; (2) `acrossfade ... c1=qsin:c2=qsin`; (3) dynaudnorm alone still left 3.6-3.9dB of drift (peak-based, and it smooths its own gain curve), so flattening is now **deterministic**: measure the RMS envelope, divide it out with ~1s smoothing (slow drift goes, short-term texture stays), normalise to a **target RMS, not to peak** — normalising to peak first dropped the bed 23dB (RMS 3671 -> 250) and would have made it inaudible; (4) the flattest 6.6s window is now SEARCHED for, not chosen by eye.
**Gate added:** `build_audio.sh` now decodes its own output and **fails the build** above 2.5dB total swing or 1.5dB edge-vs-middle. This class of defect was only caught by a human listening on a phone, twice.
**Verified live:** 6s loop, head **-0.1dB** and tail **-0.4dB** vs middle, 2.0dB total swing (was 22.7dB).
**Lesson:** when a fix doesn't work, re-measure the OUTPUT, not the thing you changed. And any filter with a lookahead/window ramps at the edges of its input — normalise before slicing, never after.
**Files:** `assets/build_audio.sh`, `assets/fridge-hum.m4a`. Deployed `de95f4c7`.
**Recurrence:** Second in this file's history ([FRIDGE-HUM-DUCKING] was the same symptom, different cause).

## [FRIDGE-BADGE-SECRET-DOOR] Manufacturer badge + secret door to the homepage — 15:14 AEST
**Added:** (1) the Fields icon debossed into the door top-left like a manufacturer mark; (2) a small unlabelled control that drops the false back of the fridge to reveal the homepage, then navigates there.
**Badge:** a CSS `mask` rather than an `<img>`, so the glyph is filled with the same metal gradient as the door and lit by the same light — an image sits ON the door, a mask is cut INTO it. Debossed = shadow on the top edge, highlight on the bottom (the opposite of raised).
**⚠ Two CSS-mask traps, both silent:** (a) the shorthand order is `<image> <position> / <size> <repeat>` — writing `no-repeat` before the position invalidates the whole declaration; (b) `mask-mode` defaults to `match-source`, which for a raster image means the **ALPHA** channel. The first mask was exported as 8-bit greyscale with no alpha, so alpha was 255 everywhere and the badge rendered as a **solid square**. Ship RGBA with the glyph in the alpha channel.
**⚠ The secret button is NOT on the inside of the door, which is what was asked for — it cannot be.** On a portrait phone a left-hinged door that opens away from a fixed head-on camera is edge-on at 90° (the button measured **5px wide**) and swung clear out of frame past that (the inner face measured screen x **-113..25** at 100°). Compression is |cos θ|, so ~90° is the worst possible place. Measured six angle × perspective-origin combinations plus camera dollies and narrower stages; none got the target above 17px while keeping the cavity on screen (340px fridge in a 390px viewport leaves no room for a 100° door). Moved to where a real fridge puts unlabelled controls: the top of the cabinet beside the lamp. **Genuinely useful geometry note kept in the CSS:** a foreshortened surface compresses ~0.41x, so a control that should LOOK small on screen must be an ellipse in local space — that trick works, it just couldn't save this one.
**Third pointer-events casualty:** `.door`'s own un-rotated box covered the stage and won hit-testing against its 3D-transformed children, so the button was unclickable even where visible. Same root cause as `.fridge` earlier. Faces now carry the events (`.door {pointer-events:none}`, `.face/.back/.edge {auto}`).
**Reveal is a captured still, not a live `<iframe>`:** an iframe fires the homepage's own PostHog (phantom sessions with odd referrers), costs a full page load per fridge visit, and — decisive — can reveal a BLANK box if it hasn't painted, which would ruin the one moment it exists for. `assets/build_home_shot.mjs` regenerates it; ⚠ it goes stale when the homepage changes.
**Cascade gotcha:** the prompt kept reading "Tap to close" through the reveal. `opacity: 0` was being **ignored** because the `.is-open` rule applies `promptIn` with `fill-mode: both`, and a filling animation OUTRANKS a normal declaration. The fix is `animation: none`, not more specificity. Separately `.muteBtn` sits outside `.stage`, so no sibling combinator can reach it from `.fridge` — driven from a body class.
**Also:** latch click isolated from the close recording's contact transient (0.16s, high-passed at 900Hz so the door-mass thud is gone and only the plastic click survives).
**Verified on production:** badge renders the glyph; secret button hittable at 19px; `is-vault` fires; prompt/mute/secret all fade to 0 during the reveal; navigates to `https://fieldsestate.com.au/`; no console errors, no 4xx.
**Files:** `index.html`, `fridge.css`, `fridge.js`, `fridgeAudio.js`, `assets/{fields-icon-mask.png,home.webp,fridge-latch.m4a,build_home_shot.mjs,build_audio.sh}`, `deploy.py`.
**Recurrence:** First. (pointer-events-on-parent is 2nd — see [FRIDGE-CONCEPT-CSS-INHERIT].)
