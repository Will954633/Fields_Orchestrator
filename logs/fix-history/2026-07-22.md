
---

## [MARKET-METRICS-DATA-AUDIT] Market Pulse rewrite blocked on 4 upstream data bugs found during /market-metrics review — 16:40 AEST
**Context:** Will asked for a full review/update of data, analysis and insights across all core suburbs on /market-metrics. Ran `manual_market_pulse.py --show-data` across Robina/Burleigh Waters/Varsity Lakes as a read-only audit before rewriting the 21 Market Pulse summaries, and found 3 fixable bugs plus 1 structural data-lag (not a bug) — logged individually below.

## [ABS-API-HOST-MIGRATED] 5 of 6 market signal indicators silently N/A for weeks — 16:40 AEST
**Symptom:** `fetch_abs_market_signals.py` (weekly Monday cron) has been throwing `NameResolutionError` for `api.data.abs.gov.au` on every run for at least the last several weeks per the log. Wage Growth, Consumer Spending, Housing Lending, Dwelling Supply and CPI Inflation all wrote `null`/`NEUTRAL` to `system_monitor.market_signals` — only ASX (a non-ABS Yahoo Finance source) kept working. This silently broke the Market Signals chart and the market_pulse "6 indicators" block for every suburb, including the #1 research-backed indicator (wages lead prices, r=0.940).
**Root cause:** ABS retired the `api.data.abs.gov.au` SDMX API hostname — confirmed via `dig` against 8.8.8.8 and 1.1.1.1 directly (not a VM/GCP DNS issue): the hostname returns NOERROR with zero A records from every public resolver. The replacement host is `data.api.abs.gov.au`, and it additionally requires the `/rest/` path prefix (`/rest/data/{dataflow}/{key}` instead of the old `/data/{dataflow}/{key}`) plus a browser-like User-Agent (bare/no-UA requests to the CDN return a 403 "Request blocked" WAF page).
**Fix:** Updated `ABS_BASE` to `https://data.api.abs.gov.au/rest/data` and added a Chrome User-Agent header to the `requests.get()` call in `fetch_abs_data()`. Verified with `--dry-run` (all 5 indicators returned real values) then ran for real — `system_monitor.market_signals` now shows Wage Growth 3.4% UP/BULLISH, Consumer Spending $22.3B UP/BULLISH, Housing Lending $12.2B DOWN/BEARISH, Dwelling Supply 8,020 DOWN/BULLISH, CPI 4.2% DOWN/BULLISH. Confirmed downstream via `manual_market_pulse.py --show-data`.
**Files:** scripts/fetch_abs_market_signals.py (`Fields_Orchestrator`, commit `c56fc47`)
**Recurrence:** First.

## [PRICE-EVENTS-SUBURB-REGEX] Price Adjustments showing 0/0 for Burleigh Waters & Varsity Lakes — 16:40 AEST
**Symptom:** `manual_market_pulse.py --show-data` and the auto-fallback `generate_market_pulse.py` both showed "0 reductions out of 0 events" for Burleigh Waters and Varsity Lakes, while Robina correctly showed 5/50. Raw `price_history` data exists for both suburbs (148 and 164 docs respectively), so the underlying data wasn't missing.
**Root cause:** Both scripts query `system_monitor.price_change_events` with `{"suburb": {"$regex": suburb.replace("_", " "), "$options": "i"}}` — replacing the underscore with a space before building the regex. But `price_change_events.suburb` is actually stored with underscores (`"burleigh_waters"`, `"varsity_lakes"`), so the space-converted regex matched zero documents. Robina has no underscore in its name so the bug never showed up for it.
**Fix:** Removed the `.replace("_", " ")` call in both scripts — regex now matches the suburb key as stored. Verified: Burleigh Waters and Varsity Lakes now show 6/50 events each.
**Files:** scripts/manual_market_pulse.py, scripts/generate_market_pulse.py (`Fields_Orchestrator`, commits `a23d333`, `08c6b88`)
**Recurrence:** First.

## [SALES-VOLUME-PARTIAL-QUARTER-YOY] Sales Volume chart showed a fake -82% to -90% YoY "collapse" for the current in-progress quarter — 16:40 AEST
**Symptom:** `precomputed_market_charts` sales_volume `timeline` array showed Q3 2026 (only 3 weeks old at time of audit) with 4-8 sales and a -82% to -90% YoY change per suburb, with no flag distinguishing it from a genuine completed-quarter figure. This is the same failure pattern as the March 2026 crash-risk phantom-surge incident (see [[crash_risk_chart_data_review]] memory) — a partial quarter compared against a full prior-year quarter reads as a fabricated market-crash signal.
**Root cause:** `calculate_sales_volume_data()` in `precompute_market_charts.py` already excluded the in-progress quarter from the *headline* `yoy_change` field (via `complete_timeline` at the end of the function), but the per-quarter `timeline` array entries still computed and displayed a YoY figure for the in-progress quarter with no marker.
**Fix:** Added `is_in_progress` boolean to each timeline entry (computed via the existing `get_quarter_key()` helper) and suppressed `yoy_change` (set to `null`) for the in-progress quarter's own entry. Re-ran `precompute_market_charts.py --charts volume` for all 8 target suburbs. Confirmed: Q3 2026 entries now show `yoy_change: null, is_in_progress: true`; the headline `yoy_change` (already using the last complete quarter) is unchanged and correctly shows a real, material Q2 2026 YoY decline (Robina -46.6%, Burleigh Waters -41.7%, Varsity Lakes -52.9%) — a genuine finding to carry into the Market Pulse rewrite, not a data artifact.
**Files:** 08_Market_Narrative_Engine/precompute_market_charts.py (`Website_Version_Feb_2026`, commit `d0a45d2`)
**Recurrence:** 2nd occurrence of this failure class (1st was [CRASH-RISK-SALES-VOLUME], March 2026, per `crash_risk_chart_data_review` memory) — different chart output, same root pattern (unflagged partial-quarter data reads as a real trend). Consider adding an explicit "quarter in progress" guard as a standing pattern across all quarterly charts, not just this one.

## [INDEXED-PRICE-Q2-LAG] Median Price series stuck at Q1 2026 — 16:40 AEST — ⚠️ SUPERSEDED, see [PROPERTY-TIMELINE-REFRESH-DEAD] below
**Symptom:** `precomputed_indexed_prices` (feeds Median Price / Price Growth charts + market_pulse) tops out at Q1 2026 for all 3 core suburbs despite running nightly, even though raw `listing_status:sold` data shows 37-41 house sales per core suburb in Q2 2026 (ended 3 weeks before this audit).
**Investigation (16:40 pass — WRONG CONCLUSION, corrected 19:15):** Checked `property_timeline` capture ratio vs `listing_status:sold` (25-40% at Q1, near-zero at Q2) and concluded this was Domain's own publishing lag not yet having caught up. **Will pushed back and asked me to verify that claim rather than assume it.** Traced one specific Q2 sale (21 Indooroopilly Court, Robina, sold 2026-05-05 $1,260,000) end to end and found the real cause was entirely different — see [PROPERTY-TIMELINE-REFRESH-DEAD].
**Fix:** See below — this entry is kept for the record of the wrong initial diagnosis, not as the final root cause.
**Recurrence:** N/A.

## [PROPERTY-TIMELINE-REFRESH-DEAD] The Q2 median "lag" was never Domain's — it was our own dead cron job + blocked scraper — 19:15 AEST
**Symptom:** Same as above (Median Price stuck at Q1 2026). Corrected root cause after Will questioned the "Domain publishing lag" explanation.
**Investigation:** Traced 21 Indooroopilly Court, Robina (sold 2026-05-05, $1,260,000) end to end. (1) Its `scraped_data.property_timeline` had no entry past 2016 and `timeline_updated_at: None`. (2) The dedicated weekly job that's supposed to refresh this field, `scripts/refresh_property_timelines.py` (cron `0 1 * * 0`), has **never once run successfully** — its crontab entry is missing `cd /home/fields/Fields_Orchestrator &&` before `. .env` (every other crontab entry either uses absolute paths or has an explicit `cd`). Reproduced exactly: cron runs from `/home/projects` (the crontab owner's home), `. .env` fails instantly (no `.env` there), and since the command is `&&`-chained the script and its log redirect never execute — zero log file has ever existed, anywhere. (3) Even fixing that wouldn't have been enough: the script's fetch method (raw `curl_cffi`, `impersonate="chrome120"`) is blocked by Domain's bot protection on `/property-profile/` URLs — 3/3 manual requests returned a 2.5KB anti-bot challenge stub, no `__NEXT_DATA__`. (4) Called `shared/domain_fetch.fetch_html` (Bright Data Web Unlocker — the same tool already used elsewhere in this codebase for the identical Akamai-class block, see [[sold_data_architecture]]) on the identical URL and got the full 402KB real page back immediately — containing the property's sale, dated 2026-04-20 on Domain's own site. **Domain has had this data for three months; nothing was ever lagging on their end.**
**Fix:** (1) `scripts/refresh_property_timelines.py` — swapped `fetch_timeline()` from raw `curl_cffi` to `shared.domain_fetch.fetch_html`; added `--sold-since-days N` flag for a targeted catch-up run (only recently-sold properties, not the full ~12K-property suburb stock) instead of only the existing indiscriminate `--stale-only`. (2) Crontab — added the missing `cd /home/fields/Fields_Orchestrator &&` and switched the relative `. .env`/log path to be safe under the corrected cwd; capped the standing weekly job at `--limit 500` so it can't run unbounded through the full suburb stock on Cosmos/Bright Data cost. (3) Ran `--sold-since-days 120` for the 3 core suburbs as a one-off catch-up to unblock this session's Q2 median work — see run output in this session; re-ran `precompute_indexed_price_data.py` after.
**Files:** scripts/refresh_property_timelines.py (`Fields_Orchestrator`); crontab (not in git — VM-local).
**Recurrence:** First (this specific dead-cron pattern). Related-but-distinct from the March 2026 Selenium→curl_cffi migration that originally created the need for this script (per its own docstring) — the migration created the gap, this cron bug meant the gap-filler itself never ran.

---

## [SITEMAP-HEALTH-MONITORING] Added "Sitemap" tracking to Main Site Health — daily push failure now alerts instead of going silent — 09:40 AEST
**Why:** Direct follow-up to [SITEMAP-PUSH-SILENT-FAILURE] below — the push had been failing silently for 2+ days because nothing checked whether the VM's daily `regenerate-sitemap.sh` cron actually succeeded (only that it ran and exited 0, which it always did despite the push failing). Will asked for a standing check + Telegram/Samantha alert so this class of failure can't recur invisibly, pointing at the existing "Main Site Health" Google Sheet (`1Oa7uZv0shzsxftDYJJ3WErxhr7OZMf_SOxRFawbSgTk`) as the model.
**What:** Added a new "Sitemap" page to `main_site_health_check.py` (feeds both the sheet and the Telegram alert already wired up in `main_site_health_to_sheet.py` — no new alert code needed, it's generic over all pages). New helper `last_commit_for_path(repo, path)` checks the actual last-commit date of `public/sitemap.xml` and `public/news-sitemap.xml` on GitHub via `gh api repos/{repo}/commits?path={path}&per_page=1` — judged against the existing 2-day "scrape" cadence threshold (same tolerance used for other once-daily jobs in this file). STALE or ERROR here will show up in the Dashboard tab and trigger the existing Telegram alert path (`maybe_alert()` in `main_site_health_to_sheet.py`) automatically, same as any other tracked page — this monitors PUSH SUCCESS specifically, not GSC-side crawl/index status (that stays a separate weekly job, `seo_indexation_check.py`).
**Gotcha hit while building this:** `gh api` silently switches its default method from GET to POST the moment any `-f`/`-F` flag is present, unless `--method GET` is explicit — my first version 404'd on the exact commits-by-path query because of this. Fixed by using the query-string form (`commits?path=...&per_page=1`) instead of repeated `-f` flags, sidestepping the footgun entirely.
**Verified:** ran `main_site_health_check.py --no-snapshot` locally — correctly showed `public/sitemap.xml` OK (commit from today, post-fix) and `public/news-sitemap.xml` STALE (5.8 days since its last real commit — legitimately true, since there have been no new articles to add in that window; this is a separate, expected condition, not a recurrence of the push bug).
**Runs automatically:** no new cron needed — hooks into the existing `main_site_health_to_sheet.py` job (crontab: `0 1 * * *`, daily), which already writes to the sheet and fires the Telegram alert on any breach.
**Files:** scripts/main_site_health_check.py (commit `7f09492`, `Fields_Orchestrator`)
**Recurrence:** N/A (new capability)

## [SITEMAP-PUSH-SILENT-FAILURE] Sitemap.xml frozen at 2026-07-20 snapshot for 2 days — Google indexing new property pages against stale data — 08:20 AEST
**Symptom:** Will reported a property page published "yesterday" (14 Elfin Street, Robina) wasn't appearing anywhere in Google search results, while other pages had indexed much faster. Asked me to find the problem, believing it was in the sitemap/exposure mechanism.
**Investigation:** Confirmed the page itself was fine — live, 200, SSR-rendering full content, valid `<link rel="canonical">`, no noindex, present in the live sitemap.xml, robots.txt open. GSC URL Inspection showed `coverageState: "Discovered – currently not indexed"`, `lastCrawlTime: null` — Google knew the URL existed but had never fetched it. Sampled 5 more properties from the same batch: 3 already indexed (crawled 2026-07-16/07-20, i.e. BEFORE their editorial was even published — the underlying page was already known), 2 came back "URL is unknown to Google" entirely.
**Root cause (two compounding factors):**
  1. **Silently broken push pipeline.** `system_monitor.seo_indexation_snapshots` history showed sitemap URL count 170 (07-16) → 1,690 (07-19) → 18,686 (07-21) — an 11x jump exactly coinciding with the off-market paid report system going live 2026-07-20 (adds `/off-market/<slug>` URLs, deliberately `index, follow`). This pushed `public/sitemap.xml` to ~3.9MB. The VM's daily cron (`regenerate-sitemap.sh`, 06:15 AEST) regenerates the file correctly every day but its `gh api ... --field content=$(base64 ...)` push step has been failing EVERY DAY since 2026-07-20 with `Argument list too long` (base64 payload as a CLI arg blew past the 2MB `ARG_MAX`) — this is the exact documented `gh_api_large_file_gotcha` class of bug, just not applied to this particular script. Because the failing command was the first operand of a `cmd && echo ...` list, bash's `set -e` didn't abort the script, so it printed "Done." and exited 0 every day — completely silent. Last successful push was `e87535b` at 2026-07-20T08:05:17Z. Every property published/updated/scraped/status-changed since then (the entire 76-property backlog batch, 14 Elfin Street's own editorial, likely under_contract/withdrawn transitions) was invisible to whatever Google fetched from the live sitemap.
  2. **Crawl-budget dilution (secondary, not fixed — flagging for a decision).** The 16,866 new `/off-market/` URLs now outnumber the 1,731 actual `/property/` pages ~10:1 in the same public sitemap. This reproduces the exact "low domain authority + high URL count → Google won't spend crawl budget" constraint already documented as this site's core SEO bottleneck since March 2026 (see `[[seo_indexation_baseline]]`) — just via a different page type than the sold-page dump that caused it originally. Worth a deliberate decision on whether `/off-market/` should get the same `noindex` treatment already applied to the analogous `/your-home/` mini-site path — did not make this call unilaterally since it's a product/SEO policy tradeoff, not an unambiguous bug.
**Fix:** Rewrote the push step in `regenerate-sitemap.sh` to build the PUT body as JSON on disk and pass via `--input` (the established large-file pattern) instead of `--field content=`. Also replaced the "skip if unchanged" check — which compared against the Contents API's `.content` field, silently empty for any file >~1MB, a second latent bug — with a git blob-SHA comparison against the already-fetched `.sha`, which works at any file size. Verified live: ran the script manually, it generated 19,224 URLs and pushed successfully (commit `4425a3b`) where it had failed 100% of the time since 07-20. Waited for Netlify to redeploy and confirmed the live `sitemap.xml` now shows `lastmod=2026-07-21` for 14-elfin-street-robina (previously stuck at `2026-07-19`). Resubmitted to GSC.
**Bonus fix (surfaced during this investigation, unrelated to sitemap):** last night's orchestrator run (07-21 23:20 AEST) had step 112 (property type classification) crash with `TypeError: 'NoneType' object is not iterable` in `shared/claude_vision.py:151` (`resp.content` was `None` on some OpenRouter response, and the code assumed it was always an iterable list) — this bug existed since the vision-OpenRouter migration but was masked because every call was previously failing on the dead direct-API-key 400 first. My 2026-07-21 OpenRouter routing fix made calls succeed, which is what newly exposed this. Fixed by guarding `for b in (resp.content or [])`. Verified live: reprocessed 15 Robina properties through step 112, 0 errors (previously crashed on this exact class of response). This is why the watchdog escalation below fired.
**Files:** `01_Website/scripts/regenerate-sitemap.sh` (commit `4425a3b`, `Website_Version_Feb_2026`), `shared/claude_vision.py` (commit `6777be1`, `Fields_Orchestrator`)
**Recurrence:** Nth instance of the `gh api` large-file class of bug (see `[[gh_api_large_file_gotcha]]` memory) — this specific script just hadn't been touched since the sitemap grew past the threshold. Nth instance of "dead direct-API-key masked a real bug underneath" (see 2026-07-21 `[NIGHTLY-EDITORIAL-SATELLITE-CASCADE]`) — underscores that fixing a masking failure can immediately surface a second, previously-invisible one; always re-verify the newly-unblocked path actually completes cleanly, not just that the original error is gone.

## [WATCHDOG] Step 112 (Classify Property Type (Claude Vision)) — 02:01 AEST

**Failure class:** CODE_BUG
**Cause:** Matched error pattern: TypeError:\s+
**Action taken:** ESCALATED to Claude (id=watchdog_112_1784649664)
**Result:** RESOLVED — see [SITEMAP-PUSH-SILENT-FAILURE] above (same date). Fixed `shared/claude_vision.py:151` (null `resp.content` guard), verified live against 15 properties, 0 errors.

## [OFFMARKET-LADDER] New value-first "ladder" arm on /off-market + negative-latitude coord bug — 08:30 AEST
**What:** Shipped a third A/B arm of `offmarket_gate_v1` (`ladder`) — a value-first, scroll-driven load sequence for homeowners who Google their own address (Recognition aerial → facts we hold → nearby street sales → market-context range → live suburb market → three free doors). Replaces the paywall-first teaser entirely for that arm; the "open the deep-dive" door reuses the free `analyse-your-home-submit` build (no $15 gate). Ships DORMANT behind `?arm=ladder` — rich/minimal arms and the Square payment path are byte-unchanged, so real traffic is unaffected until a `ladder` variant is added to the PostHog flag.
**Symptom (caught by mandatory visual verification):** first deploy rendered a blank hero image and skipped the "your street" comps + range on every address; market tiles + doors were fine.
**Root cause:** the off-market loader reused the file's shared `toNum()` helper (rejects values `<= 0`) to parse `lat`/`lon`. AU latitudes are negative (~-28), so `lat` always resolved to `null` → the Mapbox satellite hero URL never built AND `getNearbySoldComps` (gated on `lat != null`) never ran. Market/doors worked because they don't need coordinates. Invisible to tsc + production build.
**Fix:** added a coordinate-safe parser `toCoord()` (accepts negatives, rejects only NaN/0) for lat/lon. Verified live via headless screenshots on a rich address (5 Chantilly Place — real satellite hero + 6 nearby comps + $1,360,000–$1,870,000 market-context range) and a thin cadastral-style unit (1/19 Camphor Wood Court — same experience, degraded honestly with no beds chips). Confirmed the `rich` arm is visually identical and the $15 checkout path untouched.
**Files:** `01_Website/src/pages/OffMarketPage/{OffMarketLadder.tsx,OffMarketLadder.module.css,OffMarketPage.tsx,types.ts}`, `01_Website/src/routes/off-market.$slug.tsx`, `01_Website/src/lib/db.server.ts`, `01_Website/netlify/functions/offmarket-ladder-lead.mjs` (commits `fb21dd81` feature + `cf0b930b` lat fix, `Website_Version_Feb_2026`)
**Recurrence:** First for the coordinate-vs-`toNum` sign bug. Lesson: a positive-only numeric parser must never be reused for coordinates — silent, and only the mandatory screenshot caught it.
**Known v1 follow-up (not a bug):** `getNearbySoldComps` selects nearest sold homes with no property-type/bed filter, so a small unit can show house comps + an inflated range (Camphor Wood unit showed $1.23M–$1.64M for a former $229k unit). Filter comps by property_type / bed band before enabling the flag for real unit traffic.

## [OFFMARKET-LADDER-DECK] Swipe card-deck arms (light + dark) + Standout beat + deck hero collapse fix — 09:06 AEST
**What:** Added two more A/B arms to the off-market ladder (Will's request): a one-card-at-a-time SWIPE DECK (Next button + pointer/touch drag + arrow keys, progress dots, edge resistance) in two themes — `ladder_deck` (light Fields scheme) and `ladder_dark` (dark storyboard palette). Also added the missing "What makes your home stand out" beat to ALL ladder arms as an HONEST, sample-relative comparison (subject beds/baths/land vs the nearby sold set, positive-only — e.g. "907 m² block — larger than 4 of 5 nearby sales"), never the storyboard's illustrative features (we don't hold feature data cheaply). Refactored shared data + door logic into `ladderShared.ts` (computeLadder + useLadderDoors); added `land` to `getNearbySoldComps` for the standout. Ships DORMANT (`?arm=deck` / `?arm=dark`).
**Symptom (caught by screenshot verification):** the deck hero card (card 0) rendered completely blank — no aerial, no address — while every content card rendered fine.
**Root cause:** the hero card's image + text are `position:absolute` (no in-flow height); the deck root used `min-height:100dvh`, so the flex height-chain (root → viewport `flex:1` → track `100%` → slide `100%`) never resolved to a definite pixel height, and the hero — having no intrinsic content height — collapsed to 0. Content cards survived because their in-flow content gave them height (they just weren't vertically centred).
**Fix:** definite `height:100dvh` on the deck root + `min-height:0` on the flex viewport, so the percentage-height chain resolves. Hero now fills the card (aerial + address), content cards centre. Verified live via headless screenshots: dark + light hero cards render the aerial + address; dark content cards render in the dark palette; both decks 6/6 cards, 0 page errors; scroll arm shows the honest Standout beat.
**Files:** `01_Website/src/pages/OffMarketPage/{OffMarketDeck.tsx,OffMarketDeck.module.css,ladderShared.ts,OffMarketLadder.tsx,OffMarketLadder.module.css,OffMarketPage.tsx,types.ts}`, `01_Website/src/lib/db.server.ts` (commits `5ed57a21` feature + `8b46f126` hero fix, `Website_Version_Feb_2026`)
**Recurrence:** First for this hero-collapse; Nth of the classic "percentage-height needs a definite-height ancestor; `min-height` isn't definite for `%` children" flex gotcha. Lesson (again): a card whose only children are absolutely-positioned must have its own height guaranteed — and the mandatory screenshot is what caught a bug tsc + build can't see.

## [OFFMARKET-DECK-HOOK-ORDER] Deck Stage-1 — hook-first card order + market-pulse verdict + like-for-like comps + retire scroll — 10:05 AEST
**What (Will's direction — keep both decks, kill the scroll, hook the user early):** Re-sequenced the swipe deck to lead with the money hook: recognition → **wealth reveal** ("last sold $670k in 2016" anchor, then "homes like yours nearby are selling for $1.4M–$1.87M") → your-street proof → **market verdict** → standout → doors. Wired the precomputed monthly **market-pulse** (`/api/v1/market-pulse`, `categories['sell-now']`) into the market card — client fetch, so card leads with "Right now, Robina is a sellers' market." + the honest summary; falls back to plain tiles if the fetch fails. Added a **like-for-like comp filter** to `getNearbySoldComps` (strata/unit-ness via address `N/M` prefix + property_type, and bed band ±1) so a unit never shows house comps and the wealth-reveal range stays honest — this both hardens the new lead hook and fixes the earlier unit-inflation follow-up. **Retired the scroll arm** (`?arm=ladder`→`ladder_deck`; deleted `OffMarketLadder.tsx`/`.module.css`); decks only now (`deck` + `dark`). Verified live via screenshots on the dark deck: wealth-reveal card + "sellers' market" verdict render correctly; range moved to $1.4M–$1.87M under the like-for-like filter.
**Files:** `01_Website/src/pages/OffMarketPage/{OffMarketDeck.tsx,OffMarketDeck.module.css,OffMarketPage.tsx}`, `01_Website/src/routes/off-market.$slug.tsx`, `01_Website/src/lib/db.server.ts`; deleted `OffMarketLadder.{tsx,module.css}` (commit `2274cba4`, `Website_Version_Feb_2026`). Still dormant behind `?arm=`.
**Recurrence:** N/A (feature iteration). Part of a staged program — next: S2 fast no-LLM "intel" endpoint (real scarcity/competition streamed into the standout card), S3 pre-warm build on engagement + no-LLM vs LLM build A/B + Opus→`claude-sonnet-5` swap.

## [OFFMARKET-DECK-INTEL] Deck Stage-2 — real scarcity/competition streamed into card 5 (fast, no-LLM) — 10:45 AEST
**What (Will's "hook early, compute in the background" plan):** Card 5 ("what makes your home stand out") now streams the REAL competition ratio — e.g. **"12 of 231 comparable listings nearby truly compete with your home"** — computed on the VM in ~1-4s with NO LLM, while the visitor swipes the instant cards. Pipeline: deck fires `GET /api/v1/offmarket-intel?slug=&suburb=` once the visitor swipes past the hero (engagement) → `offmarket-intel.mjs` marks the slug pending in `system_monitor.offmarket_intel` (queue, mirrors `valuation_requests`) → **`fields-offmarket-intel-poller`** (new systemd service, `scripts/offmarket_intel_poller.py`, 3s interval) computes it by REUSING the existing deterministic resolvers `resolve_scarcity_features` + `resolve_competitor_map` (funnel `close_tier` = N compete, `active_total` = TOTAL) — no Opus, no vision, Mongo-only → writes `result` + `status:done` → deck polls (1.6s) and fills the card. Result cached 14 days (instant on repeat; stale triggers a background refresh). The instant sample-relative standout ("907 m² block — larger than 5 of 6 nearby sales") stays as the fallback + supporting evidence, and a shimmer shows while intel computes — so the card is never blank. Does NOT touch the mini-site build pipeline (`slot_resolver`), so zero risk to solicited reports.
**Verified end-to-end (live):** endpoint deploy → queue → poller → `{competition:{n_compete:12, n_total:231}}` for 5 Chantilly Place; dark-deck card 5 renders "12 of 231" + the compete caption, 0 page errors.
**Files:** website `netlify/functions/offmarket-intel.mjs` (new), `src/pages/OffMarketPage/OffMarketDeck.tsx`+`.module.css` (commit `542cf4f6`); orchestrator `scripts/offmarket_intel_poller.py` (commit `59c45456`) + systemd unit `/etc/systemd/system/fields-offmarket-intel-poller.service` (VM-only). Still dormant behind `?arm=`.
**Recurrence:** N/A. Note for enabling: `active_matching_full_stack` (27 here) is a wide-catchment count and is NOT surfaced as a scarcity headline (not scarce enough); the `close_tier`/`active_total` ratio is the honest hook. `notable_features` labels ("Bedrooms/Land size/Pool") are generic — not shown as tags yet.

## [OFFMARKET-DECK-S3AB] Deck Stage-3a/3b — mini-site model Opus→Sonnet-5 + pre-warm build on engagement + build_mode passthrough — 11:05 AEST
**What (Will: "use claude-sonnet-5 not opus" + pre-warm "worth it if it improves engagement"):**
(3a) The mini-site NARRATIVE model is now `claude-sonnet-5` instead of `claude-opus-4-7`. Minimal additive change: `_claude_backend.py:71` now honours `EDITORIAL_MODEL` on EVERY backend (was Vertex-only) — `model = os.environ.get("EDITORIAL_MODEL") or (default_model if backend != "vertex" else "claude-sonnet-5")` — then set `Environment=EDITORIAL_MODEL=anthropic/claude-sonnet-5` on `fields-property-report-poller` (restarted). The build routes narratives via **OpenRouter** (`ANTHROPIC_BACKEND=openrouter` takes precedence over Max); the OpenRouter shim auto-prefixes `anthropic/`, and `anthropic/claude-sonnet-5` is the proven-working id (same one the nightly editorial already uses). Verified in code: `get_client_and_model('claude-opus-4-7')` → `('_OpenRouterAnthropic', 'anthropic/claude-sonnet-5')`. Unset EDITORIAL_MODEL = byte-identical to before. VISION stages are a separate path (own model envs) — untouched. NOTE: this applies to ALL mini-site builds (solicited seller reports too), consistent with the site-wide opus→sonnet-5 cost migration.
(3b) The deck **pre-warms** the mini-site build once the visitor swipes to card 3+ (`doors.prewarm()`, fire-and-forget POST to `analyse-your-home-submit`, once, skipped when `fields_internal`), so the free deep-dive is (partly) built by the time they tap it. `analyse-your-home-submit.mjs` now whitelists a `build_mode` ('full'|'no_llm') body field and stamps it on the stub (first-write-wins) for the deep-dive build A/B; deck resolves build_mode from `?build=` / `offmarket_build_mode` flag / default 'full'.
**Files:** website `src/pages/OffMarketPage/{OffMarketDeck.tsx,ladderShared.ts}`, `netlify/functions/analyse-your-home-submit.mjs` (commit `075e4c38`); orchestrator `scripts/property_reports/_claude_backend.py` (commit `0b68c3cb`) + VM-only unit edit on `fields-property-report-poller`. Dormant behind `?arm=`.
**Recurrence:** N/A. **S3c NOT built yet:** `build_mode=no_llm` currently produces a FULL build (resolver ignores it until the additive `slot_resolver` gate + scarcity-decouple land). Checkpointing with Will before modifying the core build (runs every solicited report) + deciding the lean-report presentation.

## [OFFMARKET-DECK-S3C] Deck Stage-3c — no_llm build gate BUILT (additive, dormant) + testing findings that reshape it — 11:40 AEST
**What:** Added the `build_mode=no_llm` gate in `slot_resolver.py` (commit `7fc61501`, dormant): `self.no_llm` in `__init__`; each vision + narrative resolve call wrapped `None if self.no_llm else resolve_xxx(...)` (no re-indentation, so the existing `if result:` guards skip the slot); positioning/personas/buyers gated by one `and not self.no_llm` on their shared guard; the scarcity block gets a `no_llm` branch that persists the DETERMINISTIC evidence (`featureEvidence` + `soldCohortPremiums`) without the Opus prose hero. Full build (`build_mode` absent/`full`) is byte-identical (verified: py_compile + a full report path unchanged). `EDITORIAL_MODEL` model swap (S3a) is live for ALL builds.
**Verified working via real no_llm builds (then cleaned up the test docs):** `build_mode=no_llm` is honored end-to-end; vision correctly skipped; scarcity decouple works (`featureEvidence` = 2 features persisted, slot approved); competition ratio present (close_tier 18/231); positioning_thesis/walking/statutory/your_street/market approved.
**THREE findings that reshape the no_llm design (checkpointing with Will):**
1. **Not actually fast.** A subject-found off-market build took ~3-4 min even with ALL LLM skipped — the bottleneck is NETWORK-heavy deterministic stages (POI Overpass, which was 429-ing; on-demand valuation catchment cache; occupancy Bright Data; competitor Nominatim geocoding), not the narratives. So "no_llm" ≠ "fast build" for off-market; the pre-warm (S3b) is what actually hides the latency.
2. **Thinner valuation.** Skipping the floor-plan VISION means no derived `floor_area` → the engine excludes the subject (comps=0) → valuation falls to Tier-3 `thin`/"Indicative only" ($1.43M–$1.74M band) instead of the reconciled engine figure. So a truly zero-LLM (no-vision) build has a low-confidence valuation; the sensible "lean" definition is **skip narratives, KEEP vision** (needed for the data).
3. **Frontend has no lean-report mode.** `slot_status` is a strict two-state gate (approved→content, else→"Consultant review in progress" placeholder); the "Right Buyer" tab is a static array (always shows); the banner reads "under review" for any DB doc (the "signed off" banner is unreachable). So a clean data-only report needs a real (but `isNoLlm`-gated → safe) frontend refactor: hide the narrative placeholders, drop the buyers tab, add a "data report" banner, suppress the PositionAtAGlance "under review" badges.
**Recommendation to Will:** the pre-warmed FULL sonnet-5 deep-dive (live) already delivers a warm, cheaper deep-dive; the no_llm variant's premise (much faster/cheaper) is weaker than hoped (network-bound, thinner data) and needs the frontend refactor. Lean toward PARKING the no_llm A/B (gate kept dormant/ready), or if pursued, define it "skip narratives + keep vision" and do the frontend refactor as a focused follow-up. Awaiting Will's call.
**Files:** `scripts/property_reports/slot_resolver.py` (commit `7fc61501`, `Fields_Orchestrator`). Dormant — nothing in real traffic sets `build_mode=no_llm` (deck defaults `full`, no flag enabled).

## [OFFMARKET-HERO-PHOTO] Deck hero uses real full-res home photo (like the mini-site) instead of satellite-first + fixes two data-integrity bugs found via live testing — 13:15 AEST
**What (Will's request):** the deck hero was always showing the Mapbox satellite tile even though a real photo existed, because the off-market loader grabbed the FIRST raw photo URL with no quality upgrade — often a dead/thumbnail Domain `rimh2` URL. Added `bestHeroPhoto(doc)` in `src/lib/db.server.ts`, replicating the mini-site's `shared-utils.mjs` `toFullResUrl`/`upgradePhotoQuality` logic (rewrites `rimh2.domainstatic.com.au` thumbnails → full-res `bucket-api.domain.com.au` originals; prefers our `blobs.fieldsestate.com.au` mirror; skips dead Azure hosts and un-rewritable legacy `hpg-unique-data` URLs) — not imported directly since `shared-utils.mjs` pulls in `db.mjs`/mongodb. Hero now: real photo when one resolves live → Mapbox satellite only as fallback. Verified live: 5 Glenferrie Drive (`?arm=dark`) now shows a real 2000px full-res photo; 5 Chantilly Place (whose only photo is a dead 2016 `hpg-unique-data` URL) correctly still falls back to satellite — the degradation is honest, not a bug.

**Two real bugs found via this live testing, both fixed:**
1. **Nominal-transfer defense-in-depth (website):** added a `NOMINAL_TRANSFER_FLOOR=$50,000` so `lastSale`/`firstRecord` (and their derived growth %) are never anchored on a junk-priced entry — the full unfiltered sale history table is untouched, only the derived figures are protected.
2. **SITEWIDE DATA BUG (orchestrator) — rentals stored as sales.** 5 Glenferrie showed "Last recorded sale **$695** · June 2022" — a **weekly rent listing**, not a sale (confirmed against the live Domain property-profile page, which Will supplied). Root cause: `scripts/enrich_cadastral.py`'s `extract_transactions()` had `is_sale = ev.get('is_major_event') or ev.get('category') == 'Sale'` — `is_major_event` is `true` for BOTH `category:"Sale"` and `category:"Rental"` raw timeline events, so the `or` let any rental listing that happened to be a property's most recent major event get stored as its "last sale." **Measured impact: 3,719 of 17,366 already-enriched cadastral/off-market docs (21.4%) had a rental price as their newest stored transaction** — corrupting last-sale price, growth %, and `capital_gain` for roughly 1 in 5 off-market properties across the 3 target suburbs (the ONLY population `enrich_cadastral.py` ever touches — `for_sale`/`sold` properties use a different, unaffected pipeline; `occupancy_classifier.py`'s tenanted-detection gate already had its own correct category check, unaffected). This directly threatens "smarter with data" credibility on exactly the surface (off-market wealth-reveal) built earlier today.
   **Fix:** changed the check to `is_sale = ev.get('category') == 'Sale'` (the only two category values sitewide are `'Sale'`/`'Rental'` — confirmed by a full survey before changing the condition, so no genuine sale category was at risk of being dropped).
   **Backfill:** wrote `scripts/backfill_rental_transaction_bug.py` — a pure, deterministic RE-DERIVATION from already-stored `scraped_data.property_timeline` (no scraping, no external calls); only rewrites `enriched_data.transactions`/`enriched_data.capital_gain` on docs where the currently-stored newest transaction is provably a rental (cross-checked against the raw timeline event). Dry-run verified on a 5-doc Robina sample first. Ran live across all Gold_Coast suburb collections: **3,719/3,719 fixed in 24s** (varsity_lakes 1,461, robina 1,422, burleigh_waters 836 — zero in any other collection, confirming the scoped blast radius). Verified 5 Glenferrie Drive directly in the DB and live on the deck: now shows the genuine **$326,167 · Aug 2003** sale.
**Files:** website `src/lib/db.server.ts` (`bestHeroPhoto`), `src/routes/off-market.$slug.tsx` (hero wiring + nominal-transfer floor) — commits `8b7c4905`/`1f29f881`/`efb8fd20`; orchestrator `scripts/enrich_cadastral.py` (root-cause fix, commit `4157c9d2`) + new `scripts/backfill_rental_transaction_bug.py` (commit `2a3218f3`).
**Recurrence:** First for the rental/sale conflation. Lesson: `is_major_event` was never a sale-vs-rental signal — it only marks "notable enough to show in a property timeline," true for both; a boolean OR beside a category check silently defeats the category check whenever the boolean is near-universally true. Also reinforces the value of the mandatory visual-verification step — this was found because a screenshot Will was reviewing showed an obviously-wrong dollar figure, not because any automated check caught it.

## [OFFMARKET-DECK-CARD2-COMPS-SUMMARY] Card 2 — replace redundant "last sold" line with a computed comps-summary sentence — 13:11 AEST
**What (Will's request):** card 2 ("What it's worth now") repeated the last-sale fact already shown on card 1 ("It last sold for $X in <date>") instead of explaining WHY the range is credible. Replaced that line with `compsSummarySentence(data, v)` (new, `ladderShared.ts`) — a plain-language description of the actual comps behind the range: the subject's own bed/bath count (the comps are matched around it via the loader's bed-band ±1 + unit-likeness filter, so describing them via the subject's spec is accurate to how they were selected), the comps' lot-size spread vs the subject's own lot, and how far away they are. Degrades clause-by-clause when data is missing (e.g. no land data on any comp → drops the lot-size clause entirely rather than showing an empty/wrong range); never fabricates a min-max from fewer than 2 data points. The compliance caveat ("market-context range… not a valuation… full report") stays underneath, unchanged.
**Verified live** on two addresses: 5 Glenferrie Drive → *"These are 3 bedroom, 2 bathroom homes on similar lot sizes to yours (400 m²–544 m² compared to your 463 m²), located between 70 m and 260 m from your home."*; 5 Chantilly Place → *"…on similar lot sizes to yours (530 m²–977 m² compared to your 907 m²), located between 130 m and 280 m from your home."* Removed the now-orphaned `.revealAnchor` CSS rule.
**Files:** `01_Website/src/pages/OffMarketPage/{ladderShared.ts,OffMarketDeck.tsx,OffMarketDeck.module.css}` (commits `2e0a224a`/`48ed12dd`/`ee2a1115`, `Website_Version_Feb_2026`). Dormant — deck still behind `?arm=`.
**Recurrence:** N/A (feature iteration).

## [OFFMARKET-DECK-CARD3-CAPITAL-GAIN] New card 3 — animated capital-gain fan chart, purchase price to today's range — 14:11 AEST
**What (Will's request):** a new card between the wealth-reveal (card 2) and "your street" — an animated two-line chart of capital gain since purchase. Title: *"So you're potentially realising a gain of between $X and $Y. That's an excellent result for homes in your area."* (Will's literal copy, flagged below.) Loaded the `dataviz` skill first per its trigger rule.
**Data reality checked before building:** the suburb price-index data (`Gold_Coast.precomputed_indexed_prices`, same source as the Market Intelligence pages) is **quarterly, not monthly** (39 quarters, Q3 2016 → Q1 2026, confirmed identical coverage across all 3 core suburbs). Built honestly around that — no fabricated monthly granularity.
**How low/high are plotted (Will's open question):** two lines share ONE origin (purchase price at purchase date) and diverge to the SAME low/high endpoints already shown on card 2 (the comps-based market-context range) — not two independent model outputs. The suburb's real quarterly index gives the trajectory's SHAPE (cumulative % growth per quarter since the purchase quarter); each line is the purchase price times `1 + suburb_growth(q) × (target_growth / suburb_total_growth)` — a proportional rescale that preserves the real market's ups/downs while landing exactly on the two evidence-based endpoints. Endpoints are pinned exactly (no float drift). Falls back to a straight ramp only if the suburb barely moved over the period (near-zero denominator guard).
**Honesty gates (`computeCapitalGain`, `ladderShared.ts`) — card is silently absent when any fail:** no genuine last sale; purchase predates suburb-index coverage (~mid-2016, ±45-day grace); fewer than 3 quarters of span; either scenario isn't actually a gain (rangeLow or rangeHigh ≤ purchase price). No partial/fabricated chart ever renders.
**Chart build:** new `CapitalGainChart.tsx`, hand-rolled SVG (no charting library — single small chart in a swipe card, not a dashboard). Straight polyline segments through the real quarterly points (no spline smoothing — would imply false precision). Mark specs per the dataviz skill: 2px round-cap/join lines, ~10% opacity area wash, 8-10px end dots with a 2px surface ring, direct end-labels (value at the line end, text in ink not data-color), minimal inline key instead of a legend box. Colors reuse the deck's EXISTING 2-step accent ramp (`--accent`/`--accent2`, the same pair already driving card 2's range bar) rather than introducing a new categorical hue — correct read per the skill (this is an ordered low/high split of one magnitude, not two unrelated identities). Animates via SVG `pathLength=1` + `stroke-dashoffset` draw-on, replays every time the visitor swipes back to the card (idx-driven `active` prop → remount via a `playKey`, not a one-shot IntersectionObserver). Respects `prefers-reduced-motion`.
**Verified live** on 5 Chantilly Place (purchased Oct 2016, right at the coverage boundary): *"…gain of between $730,000 and $1,200,000…"*, chart runs $670,000 → $1,400,000/$1,870,000 with real visible quarterly texture, 0 page errors, both themes correct. Verified the gate on 5 Glenferrie Drive (purchased 2003, predates coverage): card correctly absent.
**Compliance flag (not blocking, noted to Will):** "That's an excellent result for homes in your area" is subjective/unbenchmarked boosterism — implemented literally as Will's authored copy; could be swapped for a comparative, data-backed line (e.g. vs suburb median growth) if he wants it defensible under the site's factual-accuracy rules.
**Files:** `01_Website/src/lib/db.server.ts` (`priceSeriesWithDates` on `getMarketMetricsSummary`), `src/routes/off-market.$slug.tsx`, `src/pages/OffMarketPage/{types.ts,ladderShared.ts,OffMarketDeck.tsx,OffMarketDeck.module.css}`, new `src/pages/OffMarketPage/CapitalGainChart.tsx` (commit `985f2d0d`, `Website_Version_Feb_2026`). Dormant — deck still behind `?arm=`.
**Recurrence:** N/A (feature iteration).

## [OFFMARKET-DECK-CARD3-LONG-HISTORY] Card 3 follow-up — switched to a real 40+ year suburb series so pre-2016 purchases aren't excluded — 14:23 AEST
**What (Will asked "do we have annual data going back to 2010 or 2000?" after noticing the card didn't show for 5 Glenferrie, purchased 2003):** measured first — **52.8% of off-market properties (last sale before ~mid-2016) were being silently excluded** from the new capital-gain card because the source it used (`precomputed_indexed_prices`) only starts Q3 2016. Rather than add a lower-fidelity straight-line fallback for older purchases, found a BETTER real source already in the DB: **`Gold_Coast.suburb_median_prices`** (House; genuine quarterly medians, not annual — Robina/Varsity Lakes back to 1983, Burleigh Waters to 1973). Added `getSuburbPriceHistory(suburbCollection)` (`db.server.ts`) and rewired `computeCapitalGain` to use it instead — the honesty gate (purchase must be within the series' own coverage) needed no logic change, it just became far more permissive automatically once fed the longer series. House-only (no per-suburb Unit series exists in either source — consistent with the pre-existing limitation on the market card, not a new compromise).
**Verified live:** 5 Glenferrie Drive (purchased Aug 2003, $330,000) now shows the card — *"…gain of between $820,000 and $1,010,000…"*, chart runs 22 real years with genuine visible texture (a flat GFC-era 2008-2012 stretch, then the 2021-2023 boom), landing exactly on card 2's range ($1,150,000/$1,340,000 — consistency verified). 0 page errors.
**Files:** `01_Website/src/lib/db.server.ts`, `src/routes/off-market.$slug.tsx`, `src/pages/OffMarketPage/{types.ts,ladderShared.ts}` (commit `9c324283`, `Website_Version_Feb_2026`). Dormant — deck still behind `?arm=`.
**Recurrence:** N/A. Lesson: before building a fallback/degradation path for a data gap, check whether a better real source already exists — `suburb_median_prices` had been sitting there the whole time, unused by this card, and is strictly better (longer, still genuinely quarterly) than the source picked on the first pass.

## [OFFMARKET-DECK-GAIN-VERDICT] Capital-gain "since you purchased" verdict — real RBA term-deposit + ABS CPI benchmark replacing the suburb-median comparison — 15:22 AEST
**What (Will's request, following up on the "That's an excellent result for your area" line):** Will flagged that the line needed its own prominent section at the bottom of the card, DYNAMIC (computed, not static), and — critically — objected to comparing against the suburb median: since that's roughly a 50/50 split by construction, half of every visitor would be told they "underperformed," which is a bad policy for a card meant to make owners feel proud of a genuine gain. Will proposed comparing against bank interest / stock market returns instead; landed on **term deposit + CPI (inflation)** as the two benchmarks.

**Data sourcing (checked before building, per Will's ask "should we check if we're already pulling that"):** found **two of three pieces already live** on a weekly cron — RBA cash rate (`scripts/fetch_macro_indicators.py`) and QLD CPI (`scripts/fetch_abs_market_signals.py`), both feeding the public Market Intelligence pages already — but both artificially capped to recent history (cash rate 2015+, CPI 2020+). Investigated real source depth directly: RBA's F4 table (1-year term deposit, series `FRDIRBTD10K1Y`) has genuine MONTHLY data back to **Dec 1981**; ABS's CPI Data API (same dataflow/key already live: `1.10001.10.3.Q`, QLD All Groups) has genuine QUARTERLY data back to **1948-Q3** — both far deeper than any purchase date we'll see, so no fallback source was needed.
**Backend:** extended `scripts/fetch_macro_indicators.py` (Fields_Orchestrator) — new `fetch_rba_term_deposit_1y()` (F4 CSV, column position verified against the Series ID header each run rather than hardcoded, so an RBA column reorder fails loudly not silently) and `fetch_cpi_qld()` (ABS Data API, reuses the exact dataflow/key already proven in the sibling script, deliberately NOT duplicated with different query params). Also widened the cash-rate filter to the CSV's actual full extent (~2011+; the RBA daily F1 CSV itself is capped there, not an artificial choice in our script). Ran live: 179 quarters term deposit (1981-Q4–2026-Q2), 311 quarters CPI (1948-Q3–2026-Q1), written to `Gold_Coast.precomputed_macro_indicators`.
**Frontend:** `getMacroReferenceSeries()` (`db.server.ts`) reads the new fields; threaded through the off-market loader → `PropertyData.termDepositSeries`/`cpiSeries`. `computeGainVerdict()` (`ladderShared.ts`) compounds the term-deposit trajectory quarter-by-quarter from the purchase quarter (real historical rate each step, not a flat average) and computes the CPI trajectory as a direct index ratio (exact, no compounding needed); converts both to CAGR alongside the property's own implied CAGR range (from the gain-chart's rangeLow/rangeHigh), then buckets into **4 honest tiers**: `excellent` (beats the bank even at the low end) → `solid` (beats the bank only at the high end) → `modest_vs_bank` (below the bank but still ahead of inflation) → `modest_vs_cpi` (below/at inflation too, rare). Gated off entirely (no verdict shown) when either benchmark series doesn't reach back to the purchase date, or when there's no chart at all — same honesty-gate pattern as the rest of the deck. UI: moved out of the card title into its own bottom section (`.verdictBlock`, a divider + a larger bold headline + a smaller substantiating line), per Will's "own space, prominent font" spec.
**Verified live** with two real, independently-computed examples, both landing in the "excellent" tier (expected — this benchmark is a bar Gold Coast property clears the vast majority of the time, by design, unlike the suburb-median comparison it replaces): 5 Chantilly Place (Oct 2016, $670k) → *"…7.8%–11.1% a year — well ahead of a term deposit (around 2.5% a year) and comfortably ahead of inflation (around 3.2% a year)."* 5 Glenferrie Drive (Aug 2003, $330k) → *"…5.7%–6.4% a year — well ahead of a term deposit (around 3.7% a year) and comfortably ahead of inflation (around 2.9% a year)."* 0 page errors on either.
**False-alarm note (own tooling bug, not a product bug):** mid-verification a `tail -60` truncation in my own test script made it LOOK like both the range and gain cards had vanished from Glenferrie; re-ran without truncation and confirmed everything was rendering correctly the whole time. Worth remembering: always capture full page text to a file rather than piping through `tail` when the expected content could exceed the truncation window.
**Files:** orchestrator `scripts/fetch_macro_indicators.py` (commit `c27aa4b1`, `Fields_Orchestrator`); website `src/lib/db.server.ts`, `src/routes/off-market.$slug.tsx`, `src/pages/OffMarketPage/{types.ts,ladderShared.ts,OffMarketDeck.tsx,OffMarketDeck.module.css}` (commit `bb4d8936`, `Website_Version_Feb_2026`). Dormant — deck still behind `?arm=`.
**Recurrence:** N/A (feature iteration). Reinforces the earlier lesson: check for a real source before building a synthetic one — here TWO of three needed pieces were already live, just capped, and widening a working fetch was far less work than either had first assumed.

---

## [SYSTEMS-HEALTH-CONSOLIDATION] Business-wide process audit → single "Fields Systems Health" sheet (was website-only "Main Site Health") — 17:10 AEST
**Context:** Will asked for a comprehensive audit of every business process (website, orchestrator, ads, leads tracker, article/editorial gen, sitemap submission, mini-site) to confirm nothing fails silently, and for all of it consolidated into one Google Sheet as a single source of truth (the sheet already existed for the main website only — `1Oa7uZv0shzsxftDYJJ3WErxhr7OZMf_SOxRFawbSgTk`). Used the same-day market-intelligence audit above as the motivating example: those 4 bugs mapped onto specific *monitoring* gaps, not just data staleness.
**What the audit found (research phase, 3 parallel agents):** of ~50 cron/systemd jobs + 14 off-VM GitHub Actions workflows, only ~8 had any failure alerting; ~40 were log-only or had zero exception handling. `telegram_notify.py`'s `send_message()` called `sys.exit(1)` on failure — a `SystemExit`, not caught by the `except Exception` almost every caller used, so a Telegram outage silently crashed the calling script instead of being logged. 3 of 14 `fields-automation` GitHub Actions workflows were failing silently (Light Rail Stage 3: 5 straight weeks; Market Shift: 2 runs; Market Insight: most recent run) — no workflow has a failure-notification step. `worker-agent/run-worker-agent.sh` called `telegram_notify.py --message` — a flag that doesn't exist (positional arg) — so it had never actually sent a notification. The mini-site had its own separate "Mini-Site Health" sheet with zero Telegram alerting (asymmetric vs. the main site).
**Fix (implementation, following `/home/projects/.claude/plans/synchronous-dazzling-tiger.md`):**
1. `telegram_notify.py`: `send_message()` now raises `TelegramSendError` (a normal Exception) instead of `sys.exit(1)`, so existing `except Exception` callers across the fleet now actually catch alert failures instead of crashing uncaught. Updated `main_site_health_to_sheet.py`'s `maybe_alert()` to match.
2. `worker-agent/run-worker-agent.sh`: removed the non-existent `--message` flag (3 call sites) — Worker Agent notifications now actually send.
3. New `scripts/job_status.py` (`record_job_result()` → `system_monitor.job_runs`) + instrumented `fetch_abs_market_signals.py` and `fetch_macro_indicators.py` to self-report success/failure and indicator counts — the only two scripts where a DNS-class failure degrades silently (writes a doc either way) with no other observable side effect to check instead.
4. `scripts/main_site_health_check.py`: added 5 new pages — **Process Registry** (every cron job/systemd daemon/GitHub Actions runner, ~50 rows, via a new generic `log_freshness_check()` helper for the ~30 jobs with no other check anywhere, `systemctl_is_active()` for the 15 always-on daemons), **GitHub Actions** (14 `fields-automation` workflows via `gh api`, surfaces the 3 real failures), **Market Signals Fetch** (reads the new `job_runs` records), **Leads & CRM**, **Ads & Compliance** (best-effort `ad_decisions` Rule-3 compliance signal + compliance-nightly log check). Also fixed the pre-existing "Indexed prices" row's cadence judge (was "nightly", the job is monthly — would cry STALE almost every day) and added 3 new correctness checks tied directly to the market-intelligence audit above: quarter-completeness (compares `precomputed_indexed_prices`' latest complete quarter against raw `sold_date` counts — grace-windowed to 2 quarters back, not 1, after confirming via [INDEXED-PRICE-Q2-LAG] that a 1-quarter lag is normal Domain-publishing-lag, not a bug — a 1-quarter grace window would have cried wolf every month), in-progress-quarter flag correctness on the sales-volume chart, and price-reductions correctness (raw `price_history` vs. `price_change_events` count).
5. Merged the mini-site into the same sheet: refactored `minisite_health_to_sheet.py::build_workbook()` to accept an existing `wb` (optional param); `main_site_health_to_sheet.py::main()` now calls it against its own workbook before one upload, and `maybe_alert()` folds mini-site breach counts into the same Telegram digest (mini-site had none before). Renamed the Drive file "Main Site Health" → "Fields Systems Health" (same file id/URL). Renamed the old standalone "Mini-Site Health" sheet to mark it archived (could not trash it — the service account has edit/rename but not trash permission on that specific file; flagged to Will to trash manually if wanted). Commented out the now-redundant `30 0 * * * minisite_health_to_sheet.py` crontab line (its work runs inside the 01:00 job now); script kept runnable standalone for debugging.
**Verified:** `main_site_health_check.py --json` ran end-to-end against live Mongo — Process Registry correctly surfaced 2 genuinely-broken jobs (Google Ads metrics collector: `RefreshError: invalid_grant` on every run since credential expiry; `google_indexing.py`: `pymongo.errors.InvalidURI` on every run — both confirmed by direct log inspection, not false positives), all 15 systemd daemons + the GitHub Actions runner correctly read as active/online, and the GitHub Actions page correctly flagged the 3 known-broken workflows. Caught and fixed two of my own bugs during verification: (a) `log_freshness_check()` originally grepped a fixed 20KB byte-window for failure patterns, which caught OLD resolved tracebacks still within that window on 3 logs whose most recent run was clean (`mongo-allowlist-refresh.log`, `marketing-stage.log`, `brain2-nightly-refresh.log`) — fixed by restricting the pattern search to the last 60 lines, not the whole byte window; (b) the quarter-completeness check's raw-sold-count query used `datetime` objects against `sold_date`, which is stored as a plain `"YYYY-MM-DD"` string — the query silently matched zero documents every time (confirmed directly: datetime-range query returned 0, string-range query returned 74 for the same window) — fixed to compare ISO date strings. `main_site_health_to_sheet.py --no-alert` built and uploaded a 50-tab workbook (13 main-site pages + Mini-Site Dashboard + 35 per-home tabs) to the live sheet successfully.
**Files:** `scripts/telegram_notify.py`, `worker-agent/run-worker-agent.sh`, `scripts/job_status.py` (new), `scripts/fetch_abs_market_signals.py`, `scripts/fetch_macro_indicators.py`, `scripts/main_site_health_check.py`, `scripts/main_site_health_to_sheet.py`, `scripts/minisite_health_to_sheet.py` (all `Fields_Orchestrator`); crontab (VM, not in git).
**Recurrence:** First for the consolidation. The `sys.exit`-vs-`Exception` alerting bug is a variant of a recurring class (masking failures compound — see 2026-07-21 `[NIGHTLY-EDITORIAL-SATELLITE-CASCADE]` and 2026-07-21 `[SITEMAP-PUSH-SILENT-FAILURE]`'s bonus fix) — worth remembering that a monitoring system's own alert-delivery path needs the same scrutiny as the things it monitors.

---

## [SAMANTHA-SYSTEMS-HEALTH-TASK] Samantha now reads + fixes/escalates the Fields Systems Health sheet every run — 17:19 AEST
**Symptom:** The Fields Systems Health sheet (consolidated same day, see `[SYSTEMS-HEALTH-CONSOLIDATION]` above) had a Telegram breach alert wired into its own nightly build (`main_site_health_to_sheet.py`), but nothing proactively triaged what it surfaced — a human had to notice the alert and act, and Samantha (the nightly co-CEO agent) had no standing instruction to read it, fix durable/safe gaps herself, or escalate the rest.
**Root cause:** Samantha's task list (`daily_tasks.md`) predates the sheet's consolidation into a business-wide board; her only related behaviour was the generic "Blockers & self-recovery" protocol, which is reactive (fixes whatever she happens to trip over) rather than a standing check of this specific source of truth.
**Fix:** Added a new **Task 0.5 — Fields Systems Health check** section to `daily_tasks.md` (between the existing Task 0 lead-worklist review and Task 1): every run she reads the sheet (or re-runs `main_site_health_check.py --json` live if Drive OAuth is down — a known weekly-expiry issue), triages rows (skip OK/KNOWN-GAP, informational-only UNKNOWN-FRESHNESS, act on ERROR/STALE/MISSING), traces each to root cause, fixes it durably (safe/reversible fixes only, per her existing autonomy tier, with a mandatory fix-history entry so the fix is traceable and doesn't regress) or — if she can't fix it or is unsure whether it's safe — Telegrams Will immediately (`telegram_notify.py`) with the specifics AND records it in her run's report/Google Doc under a "Systems Health" section. Also added a one-line pointer in `charter.md`'s "Load" step (run-loop step 1) and updated the report-structure line in `daily_run.py`'s `_build_prompt()` so the checkpoint report explicitly includes the Task 0.5 section.
**Files:** `scripts/samantha/daily_tasks.md`, `scripts/samantha/charter.md`, `scripts/samantha/daily_run.py`.
**Recurrence:** First.

---

## [SYSTEMS-HEALTH-WATCHES-ITSELF] The checker had no check on itself — fixed with an independent watchdog + crash safety net — 17:25 AEST
**Symptom:** Will asked directly: "does this system itself have its own tracking and status alerts... it needs to be checked itself." Correct — the only thing in `[SYSTEMS-HEALTH-CONSOLIDATION]`'s Process Registry claiming to cover `main_site_health_to_sheet.py` (the 01:00 AEST job that builds the whole sheet) was a static row reading *"this sheet, self-evidently running"* — a no-op. If that script crashed before writing anything, that row never gets written either, so nothing would ever say so. `main()` also had no top-level try/except at all — any uncaught exception (a Mongo timeout, a bug in one of the same day's new collectors) would kill the run silently: no sheet update, no Telegram alert (never reaches `maybe_alert()`), nothing but a traceback sitting in a log file nobody was watching.
**Fix:**
1. **Crash safety net** — `main_site_health_to_sheet.py`'s `__main__` block now wraps `main()` in try/except; on any exception it calls `_alert_own_crash()`, which sends a direct Telegram alert (deliberately not importing anything from `main_site_health_check.py`/`minisite_health_check.py` — those are exactly what might have just crashed) before re-raising so the cron log still gets the full traceback and a non-zero exit code.
2. **Collector isolation** — `main_site_health_check.py`'s `collect()` now wraps each of the 5 new business-wide collector functions (Process Registry, GitHub Actions, Market Signals Fetch, Leads & CRM, Ads & Compliance — added same day, the least battle-tested code in the file) individually in try/except; a bug in any ONE degrades to a single ERROR row on that page instead of killing the entire audit and producing zero rows for every other page.
3. **Independent watchdog** — new `scripts/check_systems_health_ran.py`, scheduled separately at 07:00 AEST (6h after the 01:00 job): checks (a) `logs/main-site-health.log` is fresh and its tail actually contains a "Synced" success line, and (b) the Google Sheet's own Drive `modifiedTime` is recent — independent proof the write landed, not just that the process didn't crash before printing something. Alerts via Telegram directly on either check failing. Deliberately a separate script with no imports from the checker it's watching.
4. **Closed the loop back into the sheet** — replaced the old no-op Process Registry row with one that reads this new watchdog's own log (`logs/systems-health-runcheck.log`), so its result is visible in the sheet too, not just in Telegram.
**Verified:** ran the watchdog live — correctly read OK on both checks (log fresh, sheet modified 0.4h ago). Simulated a missing log file directly — correctly returned FAIL with the right detail. Simulated a full crash of `main_site_health_to_sheet.py` (monkeypatched `run_audit` to raise) — `_alert_own_crash()` fired for real and sent an actual Telegram message to Will's chat ID, confirming the safety net works end-to-end (flagged live to Will since it was a real send, not a dry run). Re-ran the full checker afterward — new watchdog row shows OK, no regressions on the other 12 pages.
**Bonus fix (found while cross-checking a concurrent session's work):** noticed `[PROPERTY-TIMELINE-REFRESH-DEAD]` (this file, 19:15 entry) described fixing the `refresh_property_timelines.py` crontab's missing `cd` — but the live crontab still had the broken version. Root cause: my own earlier crontab edit this session (16:52, for the mini-site-merge cron cleanup) captured its `crontab -l` snapshot before that fix and reinstalled from it, silently clobbering it. Reapplied the fix directly from that entry's description (`cd /home/fields/Fields_Orchestrator &&` prefix + `--limit 500` cap) — confirmed via diff that only that one line changed.
**Files:** `scripts/main_site_health_to_sheet.py`, `scripts/main_site_health_check.py`, `scripts/check_systems_health_ran.py` (new), all `Fields_Orchestrator`; crontab (VM, not in git — added the 07:00 watchdog entry, restored the `refresh_property_timelines.py` fix).
**Recurrence:** First for "the checker has no check on itself." Second instance today of a crontab edit silently clobbering a concurrent fix (see bonus fix above) — worth a standing habit of diffing crontab before/after any edit, which this session already did each time, but not of re-checking whether *another* change landed on the live crontab between snapshot and reinstall in a genuinely concurrent-editing session.

---

## [NEW-LISTING-EDITORIAL-INDEXATION] New Sheet page: confirms new-listing editorial actually runs + whether Google indexed the result — 19:56 AEST
**Context:** Will asked directly whether new listings' AI editorial gets confirmed as complete (including anything stuck pending human review in the ops dashboard) and whether Google has actually indexed the resulting published pages — flagging anything wrong for Samantha to investigate. Neither existed: the sheet's existing "AI editorial status" row (Property Page tab) is a suburb-wide aggregate over ALL for-sale listings, not scoped to new listings, and only alarms on `failed_factcheck > 0` — a growing `needs_review` backlog produces zero alarm anywhere in this codebase (health check or ops dashboard), confirmed by direct code read of `system-monitor.mjs`'s `handleEditorialDrafts` and `EditorialReviewPanel.tsx`. On the Google side, nothing anywhere checked actual indexation for specific published pages — `google_indexing.py` only *submits* URLs (Indexing API) and reports traffic (Search Analytics), neither tells you if Google indexed a given page; the weekly `seo_indexation_check.py` only random-samples the whole sitemap.
**What already existed and got reused rather than rebuilt:** a working, already-proven GSC URL Inspection credential (`.gcp-floor-plan-vision.json`, `webmasters` scope) via `seo_indexation_check.py`'s `_svc()`/`inspect()` — confirmed live-working via a real snapshot in `system_monitor.seo_indexation_snapshots` from earlier today. The per-URL `inspect()` function has zero dependency on that script's random sampler, so it was trivially reusable for a specific, targeted list instead of a sample.
**Built:**
1. New `scripts/new_listing_indexation.py` — imports `_svc`/`inspect`/`SITE` from `seo_indexation_check.py` directly (no auth reimplementation). `check_and_cache()` persists one doc per property slug to a new `system_monitor.new_listing_indexation` collection with a `confirmed_indexed` flag — once a page is confirmed indexed, it's never re-queried again, which is the quota-conservation mechanism (shares the same ~2000/day GSC quota as the weekly sampler).
2. New `collect_new_listings_editorial()` in `main_site_health_check.py`, new page "New Listings: Editorial & SEO". Scoped to match `generate_property_ai_analysis.py`'s own nightly processing exactly (House-only, the same 4 target suburbs — robina/varsity_lakes/burleigh_waters/merrimac — days_on_domain ≤14, a bit wider than the nightly 7-day window to catch stragglers). Flags: (a) `days_on_domain > 7` with no `ai_analysis` at all (should've been picked up by step 120, wasn't); (b) stuck in `needs_review` beyond `days_on_domain > 10` (7-day window + 3 days' grace — the pre-gate is only missing steps 105/106, which should normally clear within a night or two); (c) `published` articles, once ≥4 days old (grace period — checking sooner is just "Google hasn't crawled it yet" noise, not a real signal), get GSC-checked and flagged if still not indexed, with the coverage state and check count in the detail so Samantha has something concrete to investigate. Capped at 30 GSC calls/run as a safety valve (expected real volume is a handful/night).
**Verified live:** first real run found a genuine, longstanding problem on the first pass — "38 Glen Eagles Drive, Robina" (`ai_analysis.published_at` 2026-03-28, `days_on_domain: 7` — the DOM counter reset independent of when we published) still shows `Crawled - currently not indexed` per GSC, `lastCrawlTime` also stuck at 2026-03-28 — Google crawled it once four months ago and has neither indexed it nor recrawled it since. Two other published articles checked in the same run came back `Submitted and indexed` (correctly OK, no alarm). Re-ran immediately after: cache correctly skipped both confirmed-indexed pages (0 new GSC calls for them) and only re-checked the still-failing one (`checks: 2`) — confirms the quota-conservation logic works. Full sheet build (`main_site_health_to_sheet.py --no-alert`) picked up the new 14th main-site tab correctly.
**Files:** `scripts/new_listing_indexation.py` (new), `scripts/main_site_health_check.py` (all `Fields_Orchestrator`).
**Recurrence:** N/A (new capability). Note for Samantha/Will: the Glen Eagles Drive finding is a real, unresolved indexation problem as of this entry, not a demo artifact — worth investigating why Google crawled once and stopped (thin/duplicate-content signal? canonical issue? crawl-budget dilution per the existing `seo_indexation_baseline` finding?).

---

## [GDRIVE-OAUTH-REAUTH] Manual re-consent to unblock Drive/Docs (from_will.py, running_doc.py, drive_comment.py) — 20:16 AEST
**Symptom:** `from_will.py --peek` (and the `google-drive` MCP tools) failed with `google.auth.exceptions.RefreshError: invalid_grant: Token has been expired or revoked.` — the known weekly OAuth expiry (see `[[gdrive_oauth_7day_expiry]]` memory) had recurred, discovered live during a first trial run of an interactive ("Claude Code channel") Samantha session.
**Root cause:** the gdrive OAuth app (client `6178359532-…`) is still in Testing mode in Google Cloud, so Google revokes the refresh token ~7 days after the last consent — confirmed directly this time: the new token's `refresh_token_expires_in` came back `604799` (~7.0 days) on the token-exchange response.
**Fix:** Will was present live in the session, so did a manual browser re-consent rather than waiting for it to be escalated async: built the consent URL (`redirect_uri=http://localhost&access_type=offline&prompt=consent`, same client + scopes as the existing token: `drive` + `spreadsheets`), Will approved in-browser and pasted back the failed `localhost/?code=...` redirect, exchanged the code at `oauth2.googleapis.com/token`, and wrote the new token to `/home/fields/.gdrive-server-credentials.json` in the same shape the scripts expect. Verified immediately: `from_will.py --peek` succeeded and surfaced a real, previously-unprocessed "From Will" note (posted 2026-07-22T10:10Z, before this fix) — confirming the fix is live, not just a token that parses.
**This is NOT a permanent fix** — same 7-day expiry will recur until Will publishes the OAuth consent screen to production (Google Cloud Console → APIs & Services → OAuth consent screen → Publish app). That remains open and cannot be done by Samantha/Claude Code (requires Will's Google Cloud Console access + a judgment call about the verification requirements of going to production).
**Files:** `/home/fields/.gdrive-server-credentials.json` (credential, not in git).
**Recurrence:** Nth — same class as the 2026-06-11 and prior instances of `[[gdrive_oauth_7day_expiry]]`. Worth noting for next time: the manual re-consent itself takes under 2 minutes end-to-end when Will is present, so treating it as a live, on-demand fix (as done here) rather than only an async Telegram escalation is a reasonable interim workaround until the consent screen is published.

---

## [GOOGLE-INDEXING-INVALIDURI] google_indexing.py InvalidURI + invalid_scope — two real bugs, not the credential expiry they looked like — 20:41 AEST
**Symptom:** `google_indexing.py submit-new` (cron `0 1 * * *`) had been throwing `pymongo.errors.InvalidURI: Invalid URI scheme` on every run (flagged in the Systems Health Process Registry, same-day `[SYSTEMS-HEALTH-CONSOLIDATION]` entry assumed this was a credential problem alongside the Google Ads collector's `invalid_grant`).
**Root cause 1 (InvalidURI):** the crontab entry does `source .env && ...` with NO `set -a`, so `source`-ing `.env` sets shell variables but does NOT export them — `os.environ.get("COSMOS_CONNECTION_STRING")` was `None` when the Python process started (reproduced directly: confirmed `None` via the exact cron invocation). The script's own `load_env()` fallback then reads `.env` itself, but its parser did `value.strip()` only — not stripping the surrounding double-quotes `.env` wraps values in (`COSMOS_CONNECTION_STRING="mongodb://..."`) — so the URI passed to `MongoClient()` had a literal leading `"` character, which pymongo correctly rejects as an invalid scheme. `main_site_health_to_sheet.py`'s equivalent loader already does this correctly (`val.strip().strip('"')`) — `google_indexing.py`'s was just missing it.
**Root cause 2 (invalid_scope, found during verification):** `GOOGLE_ADS_REFRESH_TOKEN` and `GOOGLE_INDEXING_REFRESH_TOKEN` in `.env` are the literal same token string — both reuse one OAuth grant under the same client (`6178359532-…`, the same client backing Drive/Sheets access) — and that grant's actual scope is full `webmasters`, not `webmasters.readonly`. `google_indexing.py`'s `SCOPES` constant asked for `.readonly` specifically; passing a scope to `google.oauth2.credentials.Credentials` that doesn't match what the refresh token actually carries makes Google's token endpoint reject the refresh with `invalid_scope`. Fixed by aligning the constant to `webmasters` (matches `seo_indexation_check.py`'s already-correct scope for the same shared grant).
**Also discovered live:** this is the SAME OAuth client (`6178359532-…`) as the Drive/Sheets token in `[[gdrive_oauth_7day_expiry]]` — meaning the Google Ads metrics collector's `invalid_grant` (flagged same day in `[SYSTEMS-HEALTH-CONSOLIDATION]`) and this indexing job's failure share one root credential. Will was present in this session, so instead of two separate escalations, did ONE combined browser re-consent covering all five scopes needed across Drive+Sheets+Ads+Indexing+Search Console at once, wrote the new refresh token to both `.gdrive-server-credentials.json` and `.env` (`GOOGLE_ADS_REFRESH_TOKEN`/`GOOGLE_INDEXING_REFRESH_TOKEN`), and verified all three independently: `google_indexing.py status` (real Search Console data returned), `google-ads-metrics-collector.py --dry-run` (real keyword data returned), and Drive/Docs access (`from_will.py` — see prior entry). Ran `google_indexing.py submit-new` for real afterward to clear whatever backlog had silently accumulated.
**Same 7-day Testing-mode caveat applies** to this consolidated token as the Drive one above — will need re-auth again in ~7 days until the consent screen is published to production.
**Files:** `scripts/google_indexing.py` (two one-line fixes); `.env`, `/home/fields/.gdrive-server-credentials.json` (credentials, not in git).
**Recurrence:** First for both bugs. Lesson: a Process-Registry row reading as a credential-expiry error is worth actually reproducing before assuming — half of what looked like "the same known OAuth expiry" here was in fact two unrelated, durable code bugs (missing `set -a` interaction + quote-stripping + scope mismatch) that would have kept failing even with a permanently-valid token.

---

## [LEAD-DISMISSAL-NOT-DURABLE] lead_intelligence.py had no way to keep a manually-resolved lead resolved — 20:47 AEST
**Symptom:** Will confirmed directly that the "Dee" lead (13 Terrace Court, Merrimac — the exact stale-status worked example already documented in `daily_tasks.md`) has now listed and should be dropped. Setting `lead_worklist.priority`/`reason` by hand would have been silently overwritten the next time `lead_intelligence.py` runs, since its write loop unconditionally recomputes and `$set`s `priority`/`reason`/`signals` for every lead, every run — there was no concept of "a human already resolved this, don't re-score it."
**Root cause:** the pipeline was designed as a pure nightly recompute with no persistence layer for human decisions layered on top of it.
**Fix:** `lead_intelligence.py`'s write loop now pre-fetches `lead_key`s with `dismissed: true` from `lead_worklist` before writing, and skips re-setting `priority`/`reason`/`signals` for those (still refreshes `property`/`occupancy`/enrichment fields, so a dismissed lead's data doesn't go stale, it just stays out of the priority view). Marked Dee's lead `dismissed: true`, `dismissed_reason: "now_listed"`, `dismissed_by: "will_direct_confirmation"`. Documented the convention in `daily_tasks.md`'s fresh-status-verify section so this is now the standing way to resolve a lead durably (whether verified fresh or told directly by Will), not just a one-off patch.
**Files:** `scripts/samantha/lead_intelligence.py`, `scripts/samantha/daily_tasks.md`.
**Recurrence:** First.

---

## [LEAD-WORKLIST-MISSING-OFFMARKET] Task 0's actual lead pipeline had ZERO off-market coverage — 20:54 AEST
**Context:** Will flagged the Live Leads Tracker sheet (`1mRjT_PmjTepF1rDajJlM553Umy47dKa4fHOclrzAKFs`, built 2026-07-21, see `[[live_leads_tracker_sheet]]`) and asked that Samantha's memory/behaviour reflect it. Checking `lead_intelligence.py` (the actual source of Task 0's `system_monitor.lead_worklist`, which Samantha reviews every run) against that sheet's sources found it collects from `analyse_leads`/`launch_leads`/`leads`/`fb_leads`/`property_reports` — but has **zero ingestion of off-market report leads** (neither `offmarket_orders` purchases nor the PostHog `offmarket_report_view` events that are the ONLY signal for everyone who merely viewed). This is exactly the "owner googles their own off-market address" scenario the opportunity-chasing doctrine in `daily_tasks.md` calls the single most valuable trail to chase — and it was entirely invisible to Task 0.
**Fix:** Extended `lead_intelligence.py::collect_leads()` (now takes `gc_db` too) to also ingest: (1) `offmarket_orders` purchases (keyed by email like every other source — a real payment is a strong identity signal, dedup-by-person is correct here), and (2) PostHog `offmarket_report_view` events (reusing the exact HogQL query pattern from `live_leads_to_sheet.py`, filtered to Australia + non-bot-city + non-internal). Deliberately did NOT duplicate `live_leads_to_sheet.py`'s bespoke investor/occupancy filter — this pipeline already has its own generic `_enrich_occupancy` step applied uniformly to every lead type downstream, so letting that run on the new source is more consistent and less code than reimplementing the filter.
**Bug found + fixed while wiring this in:** the existing `add()` helper keys anonymous (no-email) leads by NORMALISED ADDRESS — correct for the existing sources (unifies one person's multiple touchpoints on the same property), but WRONG for anonymous page-view leads, where two different visitors of the same property must stay two different leads. Added an optional `key_override` param to `add()` (default `None`, zero behaviour change for every existing caller) and keyed off-market VIEW leads by `posthog_distinct_id` instead.
**Verified live (`--dry-run --no-fresh`):** lead count went from 48 (no off-market source) to 107 with it wired in — 59 new leads, all correctly triaged `low` priority (anonymous views with no stated pre-market signal don't inflate to high/medium, matching the existing scoring philosophy) — none of the 59 wrongly appeared as `high`. 53/59 (90%) resolved a full address via the slug→Gold_Coast lookup; the remaining 6 hit a pre-existing Gold_Coast data-completeness gap (the matched property doc itself has no `address` field) and degraded gracefully to an empty string rather than crashing or fabricating one — a separate, minor, out-of-scope data-quality item, not a bug in this change. Also caught mid-testing: my own test invocation's `source .env` (no `set -a`) reproduced the EXACT SAME class of bug as `[GOOGLE-INDEXING-INVALIDURI]` above — `POSTHOG_API_KEY` wasn't exported, so `posthog_query()` failed silently-caught (by design — a PostHog outage shouldn't break the whole lead run) until re-run with `set -a`.
**Files:** `scripts/samantha/lead_intelligence.py`.
**Recurrence:** First. Lesson (ties to the entry above): "we have a leads sheet, make sure Samantha knows about it" turned out to mean more than a memory note — the sheet and Samantha's actual scored pipeline were two different, divergent systems over the same underlying leads, and only one of them was being reviewed every run.

---

## [AD-ATTRIBUTION-ARCHITECTURE-DECISION] fb-attribution-builder.py confirmed superseded, not accidentally broken — plus a monitoring gap on its replacement — 21:21 AEST
**Context:** Will asked Samantha to investigate whether `fb-attribution-builder.py`'s dead cron (found earlier today, see the Google-Indexing entries above) was an intentional retirement (superseded by Brain 2) or an accidental break, and to make a decision on current ad-data architecture.
**Investigation:** `ad_attribution_build.py` (Brain 2, cron 23:40 AEST, confirmed running nightly) writes `system_monitor.ad_downstream` — a richer per-ad document than the old `ad_attribution` ever had (adds `unique_visitors`, `converters`, `conversion_rate_pct`, `entry_pages`, `content_consumed`, folded-in ad lifetime metrics). Separately, `scripts/ad-flow-report.py` — the tool Samantha's own `daily_tasks.md` names FIRST for "ad → on-site flow" — queries PostHog live at request time with zero dependency on either precomputed collection, so it's never stale regardless of any cron's health. Together these fully cover, and exceed, what the dead collection provided.
**Decision:** `ad_attribution`/`fb-attribution-builder.py` is SUPERSEDED — leaving its cron disabled is correct, do not resurrect it. Real residual risk found: three scripts (`ad-review-dump.py`, `ad-experiment-log.py`, `ceo-query-broker.py` — none in this VM's crontab, last touched Mar 13/13/31) still read the dead collection directly and would show stale/misleading numbers if anyone runs them. `ceo-query-broker.py` matters most since it feeds CEO-agent context, but its own cron entry on THIS VM (`ceo-context-export.py`) is also commented out — CEO agent compute may run on the separate property-scraper VM (35.201.6.222) instead, not verifiable from here. Flagged to Will as his call — migrate these 3 scripts to `ad_downstream`, or retire them; not fixed in this session.
**Fixed now (low-risk, clearly correct regardless of the above):** updated the `fb_ads_experimentation_playbook.md` memory (was pointing every ad-review session at the dead collection) to reference `ad_downstream` + `ad-flow-report.py` instead, with an explicit "do not re-enable" note. Added a NEW Systems Health check — `main_site_health_check.py::collect_ads_compliance()` now also checks `ad_downstream` freshness, so if THIS (now-current) pipeline ever silently dies the same way `ad_attribution` did, it gets caught instead of going unnoticed for 4+ months again. Caught + fixed my own cadence-mismatch bug while building it: first used `judge(ts, "nightly", now_utc, expected_last_run(now_utc))`, but `expected_last_run()` assumes the orchestrator's 20:30 AEST checkpoint — since this job runs at 23:40, that produced a false STALE every evening between 20:30-23:40. Fixed to use the existing `"scrape"` cadence (2-day tolerance, no `last_run` dependency) — the same tolerance this file already uses for other once-daily jobs, and the same class of bug as the "Indexed prices" cadence fix earlier today.
**Files:** `scripts/main_site_health_check.py`; `fb_ads_experimentation_playbook.md` (memory, not in git).
**Recurrence:** Nth for the cadence-mismatch bug class (3rd instance today: Indexed prices, this, and worth watching for more — any check using `"nightly"` + `expected_last_run()` against a job that ISN'T actually cadenced around 20:30 AEST will false-alarm). First for the architecture decision itself.

---

## [SAMANTHA-TASK-BOARD-STOPPING-DEFINITION] Task board made the concrete source of truth for "what's next" and "when to stop" — 21:24 AEST
**Context:** Will asked (a) whether Samantha needs a task-list spreadsheet to track what she's found/done during a session, using it when unsure what to do next, and (b) for an actual, checkable definition of when she should stop — "keep running until no more significant value" (added earlier today) was directionally right but not concrete enough to follow consistently. He also asked whether she checks his running doc periodically DURING a session (only checked once at session-start today, not periodically) for notes he adds while she's actively working.
**Finding:** The sheet Will linked (`1xy2w8ATjaOCAelEi0BBcKonZbE9FQXNWyAosfkot6jo`) is not a new ask — it's her EXISTING "Samantha — Task Board," Backlog tab already has Priority (P0–P3) + Status columns, exactly the mechanism he was describing. It had simply gone stale (last touched 2026-07-16) despite a huge amount of work happening in the days since — a real instance of the exact problem he's naming, not a missing feature.
**Fix:** Extended `charter.md`'s Task board section: (1) log new findings to the Backlog AS DISCOVERED, not just remembered/reported; (2) when unsure what to do next, read the Backlog and pull the next open item rather than improvising; (3) concrete stopping definition — stop only when Backlog has no open items you can act on AND nothing this cycle surfaced a new one AND nothing is blocked purely on Will's input; (4) re-check `from_will.py` roughly every 20 minutes of active work, not just once at session start. Cross-referenced from the "keep running" autonomy note added earlier today so the two policies read as one coherent rule, not two competing ones.
**Files:** `scripts/samantha/charter.md`.
**Recurrence:** First.
