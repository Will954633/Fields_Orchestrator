# Fix History — 2026-05-18

## [COVERAGE-AUDIT-001] Address-level coverage audit script + bungled-upload verification — 15:45 AEST

**Symptom:** No tooling existed to audit per-address data coverage across the cadastral baseline. Will spot-checked 1 Glenside Dr Robina and found no photo. Also unverified status on the 2026-05-15 apr01 mongodump merge — did the recovered image URLs actually land on the right records and are they live?

**Root cause:** Coverage was tracked per-listing in the ops dashboard (enrichment % of active listings), not per-cadastral-address. Recovery from the IMAGE-OUTAGE-001 blob shutdown was tracked in fix logs but never measured against the cadastral baseline.

**Fix:** New `scripts/address_coverage_audit.py` joins every cadastral record in `Gold_Coast.{robina, burleigh_waters, varsity_lakes}` against every data dimension:
- listing presence (ever listed / active / sold)
- image sources: `scraped_data_v2`, `scraped_data_apr01_recovered`, Phase-2 sidecars, `property_images*`, `scraped_property_images`
- floor plans, photo analysis, floor-plan analysis, satellite analysis
- valuation (reconciled + CatBoost + Domain)

Three modes: `--summary` (per-suburb markdown table), `--csv` (per-address rows), `--address "..."` (single lookup). Optional `--check-urls N` does live HEAD requests against random sample.

**Findings from first run:**

| Suburb | Total cadastral | Any image | apr01 recovered | v2 scraped | v2 failed | Listed but no image |
|---|---:|---:|---:|---:|---:|---:|
| Robina | 11,953 | 53.8% | 52.2% | 20.7% | **4,981** | 47 |
| Burleigh Waters | 7,007 | 79.1% | 78.8% | **0** | 0 | 20 |
| Varsity Lakes | 7,805 | 72.9% | 72.2% | 11.0% | 0 | 43 |

- **apr01 mongodump merge succeeded:** 17,393 records across target suburbs gained recovered images. 8/8 sampled URLs live (Domain CDN, not the dead Azure blob).
- **v2 re-scrape uneven:** Robina ran with 67% failure rate (4,981 failed / 7,451 attempted), Burleigh Waters never ran v2, Varsity Lakes barely started (~11% coverage).
- **Phase-2 sidecars added zero rows** to target suburbs — Phase 1 `_id` match captured everything matchable.
- **110 currently-listed properties (47+20+43) still have ZERO image** across all sources — the actual gap to close.
- 1 Glenside Dr Robina specifically: 40,900 m² cadastral lot, never listed, v2 scrape was attempted and failed. Expected — no listing means no image source.

**Outputs:**
- `logs/coverage/address_audit_2026-05-18.md` (summary)
- `logs/coverage/addresses_{robina,burleigh_waters,varsity_lakes}.csv` (every address × every dimension)

**Files:** `scripts/address_coverage_audit.py` (new, pushed to Will954633/Fields_Orchestrator commit `cee4eb7`)

**Recurrence:** First — no prior address-level coverage audit existed.

**Next:**
- Investigate Robina v2-scrape failure mode (4,981 failures — Akamai? rate limit? bad URLs?). → see COVERAGE-AUDIT-002
- Decide whether to re-run v2 scrape for Burleigh Waters (currently 0% v2, riding entirely on apr01 dump).
- Triage the 110 listed-but-no-image records — these break property pages on the live site.

---

## [COVERAGE-AUDIT-002] Robina backfill test batch — root cause of v2-scrape failures is wrong cadastral postcodes — 15:55 AEST

**Symptom:** 4,981 Robina cadastral records show `scraped_v2_failed_at`. Unknown whether this is a Bright Data / Akamai problem (retryable), a Domain-side absence (no profile page), or a URL-construction bug.

**Test:** New `scripts/test_robina_backfill_batch.py` reuses the production scraper's URL builder + parser on a fixed batch of 10 IDs (5 never-attempted + 5 previously-failed). Bright Data, parse, write all happen per-record with full before/after snapshot.

**Results (10 Robina records, none with image coverage):**

| Status | Count | Note |
|---|---:|---|
| SUCCESS | 2 | Both had `parsed_image_count=0` — Domain has the profile page but no listing photos |
| PARSE_FAIL | 8 | All returned Domain's `__NEXT_DATA__` page `/_error` with `statusCode: 404` — the slugged URL doesn't exist on Domain |

**Root cause of the parse-fails:** Inspected the failing URLs by manually retrying with `postcode=4226`:

| Original URL (from cadastral) | Result | Retried URL (4226) | Result |
|---|---|---|---|
| `703-glades-drive-robina-qld-4213` | 404 | `703-glades-drive-robina-qld-4226` | PROPERTY_EXISTS |
| `2-pearwood-lane-robina-qld-4227` | 404 | `2-pearwood-lane-robina-qld-4226` | PROPERTY_EXISTS |
| `839-legend-trail-robina-qld-4213` | 404 | `839-legend-trail-robina-qld-4226` | PROPERTY_EXISTS |
| `38-st-ives-drive-robina-qld-4226` | 404 | (same) | 404 (genuine — slug not on Domain) |

The cadastral baseline has 3,268 Robina records (28% of 11,760) with non-4226 postcodes — split 4227:1317, 4218:896, 4213:599, 4220:456. These postcodes are adjacent suburb codes (Varsity Lakes, Mermaid Waters, Mudgeeraba, Burleigh) that the cadastral data assigns to addresses Domain considers part of Robina (golf course estates, suburb edges). **1,650 of these 3,268 wrong-postcode records are flagged `scraped_v2_failed_at`** — i.e. ~33% of the 4,981 v2-failures are recoverable by retrying with postcode 4226.

**Critical secondary finding:** Both SUCCESS cases returned `parsed_image_count=0`. Cadastral-only addresses (never listed) often have a Domain profile page but no listing photos — we can't backfill images for those from Domain. The image-coverage ceiling for cadastral-only addresses is set by satellite analysis, not listing scrapes.

**Fix proposed (not yet applied):**
1. Update `scripts/scrape_property_profiles.py:build_profile_url` to try `qld-4226` as a fallback when `LOCALITY=ROBINA` regardless of cadastral POSTCODE, OR
2. Normalise cadastral `POSTCODE=4226` wherever `LOCALITY=ROBINA` AND POSTCODE in {4213,4218,4220,4227} (one-shot data fix), then re-run v2 scrape with `--retry-failed --only-missing-images --suburbs robina`.
3. Mark `scraped_v2_failed_reason='domain_404'` instead of `'parse'` when we detect `/_error` status, so we stop confusing structural absence with retryable failure.

**Files:**
- `scripts/test_robina_backfill_batch.py` (new, pushed to Will954633/Fields_Orchestrator commit `56cc083`)
- `logs/coverage/robina_test_batch_20260518_1553.json` (full per-record before/after)
- `/tmp/robina_test_batch_ids.json` (the 10 test IDs, retained for re-runs)

**Recurrence:** First diagnosis of v2-scrape failure root cause.
