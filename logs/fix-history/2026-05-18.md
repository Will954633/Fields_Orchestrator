# Fix History — 2026-05-18

## [COVERAGE-AUDIT-001] Address-level coverage audit script + bungled-upload verification — 15:45 AEST

**Symptom:** No tooling existed to audit per-address data coverage across the cadastral baseline. Will spot-checked 1 Glenside Dr Robina and found no photo. Also unverified status on the 2026-05-15 apr01 mongodump merge — did the recovered image URLs actually land on the right records and are they live?

**Root cause:** Coverage was tracked per-listing in the ops dashboard (enrichment % of active listings), not per-cadastral-address. Recovery from the IMAGE-OUTAGE-001 blob shutdown was tracked in fix logs but never measured against the cadastral baseline.

**Fix:** New `scripts/address_coverage_audit.py` joins every cadastral record in `Gold_Coast.{robina, burleigh_waters, varsity_lakes}` against every data dimension:
- listing presence (ever listed / active / sold)
- image sources: `scraped_data_v2`, `scraped_data_apr01_recovered`, Phase-2 sidecars, `property_images*`, `scraped_property_images`
- floor plans, photo analysis, floor-plan analysis, satellite analysis
- valuation (reconciled + CatBoost + Domain)

Three modes: `--summary` (per-suburb markdown table), `--csv` (per-address rows), `--address "..."` (single lookup). Optional `--check-urls N` does live HEAD requests against random sample.

**Findings from first run:**

| Suburb | Total cadastral | Any image | apr01 recovered | v2 scraped | v2 failed | Listed but no image |
|---|---:|---:|---:|---:|---:|---:|
| Robina | 11,953 | 53.8% | 52.2% | 20.7% | **4,981** | 47 |
| Burleigh Waters | 7,007 | 79.1% | 78.8% | **0** | 0 | 20 |
| Varsity Lakes | 7,805 | 72.9% | 72.2% | 11.0% | 0 | 43 |

- **apr01 mongodump merge succeeded:** 17,393 records across target suburbs gained recovered images. 8/8 sampled URLs live (Domain CDN, not the dead Azure blob).
- **v2 re-scrape uneven:** Robina ran with 67% failure rate (4,981 failed / 7,451 attempted), Burleigh Waters never ran v2, Varsity Lakes barely started (~11% coverage).
- **Phase-2 sidecars added zero rows** to target suburbs — Phase 1 `_id` match captured everything matchable.
- **110 currently-listed properties (47+20+43) still have ZERO image** across all sources — the actual gap to close.
- 1 Glenside Dr Robina specifically: 40,900 m² cadastral lot, never listed, v2 scrape was attempted and failed. Expected — no listing means no image source.

**Outputs:**
- `logs/coverage/address_audit_2026-05-18.md` (summary)
- `logs/coverage/addresses_{robina,burleigh_waters,varsity_lakes}.csv` (every address × every dimension)

**Files:** `scripts/address_coverage_audit.py` (new, pushed to Will954633/Fields_Orchestrator commit `cee4eb7`)

**Recurrence:** First — no prior address-level coverage audit existed.

**Next:**
- Investigate Robina v2-scrape failure mode (4,981 failures — Akamai? rate limit? bad URLs?). → see COVERAGE-AUDIT-002
- Decide whether to re-run v2 scrape for Burleigh Waters (currently 0% v2, riding entirely on apr01 dump).
- Triage the 110 listed-but-no-image records — these break property pages on the live site.

---

## [COVERAGE-AUDIT-002] Robina backfill test batch — root cause of v2-scrape failures is wrong cadastral postcodes — 15:55 AEST

**Symptom:** 4,981 Robina cadastral records show `scraped_v2_failed_at`. Unknown whether this is a Bright Data / Akamai problem (retryable), a Domain-side absence (no profile page), or a URL-construction bug.

**Test:** New `scripts/test_robina_backfill_batch.py` reuses the production scraper's URL builder + parser on a fixed batch of 10 IDs (5 never-attempted + 5 previously-failed). Bright Data, parse, write all happen per-record with full before/after snapshot.

**Results (10 Robina records, none with image coverage):**

| Status | Count | Note |
|---|---:|---|
| SUCCESS | 2 | Both had `parsed_image_count=0` — Domain has the profile page but no listing photos |
| PARSE_FAIL | 8 | All returned Domain's `__NEXT_DATA__` page `/_error` with `statusCode: 404` — the slugged URL doesn't exist on Domain |

**Root cause of the parse-fails:** Inspected the failing URLs by manually retrying with `postcode=4226`:

| Original URL (from cadastral) | Result | Retried URL (4226) | Result |
|---|---|---|---|
| `703-glades-drive-robina-qld-4213` | 404 | `703-glades-drive-robina-qld-4226` | PROPERTY_EXISTS |
| `2-pearwood-lane-robina-qld-4227` | 404 | `2-pearwood-lane-robina-qld-4226` | PROPERTY_EXISTS |
| `839-legend-trail-robina-qld-4213` | 404 | `839-legend-trail-robina-qld-4226` | PROPERTY_EXISTS |
| `38-st-ives-drive-robina-qld-4226` | 404 | (same) | 404 (genuine — slug not on Domain) |

The cadastral baseline has 3,268 Robina records (28% of 11,760) with non-4226 postcodes — split 4227:1317, 4218:896, 4213:599, 4220:456. These postcodes are adjacent suburb codes (Varsity Lakes, Mermaid Waters, Mudgeeraba, Burleigh) that the cadastral data assigns to addresses Domain considers part of Robina (golf course estates, suburb edges). **1,650 of these 3,268 wrong-postcode records are flagged `scraped_v2_failed_at`** — i.e. ~33% of the 4,981 v2-failures are recoverable by retrying with postcode 4226.

**Critical secondary finding:** Both SUCCESS cases returned `parsed_image_count=0`. Cadastral-only addresses (never listed) often have a Domain profile page but no listing photos — we can't backfill images for those from Domain. The image-coverage ceiling for cadastral-only addresses is set by satellite analysis, not listing scrapes.

**Fix proposed (not yet applied):**
1. Update `scripts/scrape_property_profiles.py:build_profile_url` to try `qld-4226` as a fallback when `LOCALITY=ROBINA` regardless of cadastral POSTCODE, OR
2. Normalise cadastral `POSTCODE=4226` wherever `LOCALITY=ROBINA` AND POSTCODE in {4213,4218,4220,4227} (one-shot data fix), then re-run v2 scrape with `--retry-failed --only-missing-images --suburbs robina`.
3. Mark `scraped_v2_failed_reason='domain_404'` instead of `'parse'` when we detect `/_error` status, so we stop confusing structural absence with retryable failure.

**Files:**
- `scripts/test_robina_backfill_batch.py` (new, pushed to Will954633/Fields_Orchestrator commit `56cc083`)
- `logs/coverage/robina_test_batch_20260518_1553.json` (full per-record before/after)
- `/tmp/robina_test_batch_ids.json` (the 10 test IDs, retained for re-runs)

**Recurrence:** First diagnosis of v2-scrape failure root cause.

---

## [COVERAGE-AUDIT-003] Robina postcode normalisation + targeted v2 rescrape applied — 16:53 AEST

**Symptom (from COVERAGE-AUDIT-002):** 3,268 Robina cadastral records (28%) carried Australia Post postcodes 4213/4218/4220/4227 instead of Robina's canonical 4226, causing v2-scrape URLs to 404 on Domain.

**Fix applied:**
1. `scripts/fix_robina_postcodes.py` — bulk normalised `POSTCODE` to `4226` on all 3,268 records. Original preserved in `POSTCODE_original_cadastral` sidecar + `postcode_normalised_at` + `postcode_normalised_reason="domain_url_compat_robina"`. Stale `scraped_v2_failed_at` cleared on same update. Idempotent + reversible via `--revert`.
2. `scripts/rescrape_postcode_fixed_robina.py` — thin wrapper around `scrape_property_profiles.py`'s scrape_one(). Scoped strictly to the 1,968 records that were postcode-fixed AND missing a Domain hero image. Ran 20 workers / 1.5s dispatch over 49 minutes.

**Results:**

| Metric | Pre-fix | Post-fix | Δ |
|---|---:|---:|---:|
| Robina records with any image | 6,436 (53.8%) | **8,089 (67.7%)** | **+1,653** |
| Robina records with `scraped_data_v2` | 2,470 (20.7%) | 4,238 (35.5%) | +1,768 |
| Robina records with v2 images | 2,086 | **3,811** | +1,725 |
| v2-failed records | 4,981 | 3,536 | −1,445 |
| Postcode-fixed cohort: now has v2 hero | n/a | 1,766 / 1,968 (89.7%) | — |
| Postcode-fixed cohort: still v2-failed | n/a | 202 (10.3%) | — |

Compare to the pre-fix test batch (8/10 = 80% parse-fail rate on the same cohort). The postcode normalisation took success rate from ~20% to 89.7% on the targeted cohort.

**What the 1,766 successes added per record:** scraped_data_v2 with image_urls + hero_image_url + valuation (low/mid/upper) + comparable_sales (up to 6) + timeline + rental_estimate + land/internal area + bedrooms/bathrooms/parking. 1,725/1,766 came with actual listing photos; 41 had Domain profiles but no photos (cadastral-only).

**The 202 residual failures:** Domain genuinely doesn't have profile pages for those addresses even with the correct postcode (newly-built or sub-divided lots not yet indexed by Domain).

**Verification:**
- `scripts/verify_postcode_fix_results.py` — quick check on the cohort.
- `python3 scripts/address_coverage_audit.py --suburbs robina` — refreshed global audit, written to `logs/coverage/address_audit_2026-05-18.md`.

**Files:**
- `scripts/fix_robina_postcodes.py` (new, pushed `89ad248`)
- `scripts/rescrape_postcode_fixed_robina.py` (new, pushed `d647417`)
- `scripts/verify_postcode_fix_results.py` (new, pushed `cd1004a`)
- `logs/coverage/rescrape_postcode_fixed_20260518_1603.log` (full run log)

**Recurrence:** First — postcode normalisation hadn't been done before. Same pattern may exist for other suburbs with overlapping AusPost zones (e.g. Burleigh/Burleigh Waters straddle 4220, Varsity/Robina straddle 4227). To survey: `python3 scripts/address_coverage_audit.py --suburbs burleigh_waters varsity_lakes` after running similar normalisation.

**Next:**
- Re-run nightly pipeline so the new v2 data flows through to the live website (property pages will start showing the 1,725 newly-recovered photo sets).
- Consider applying same normalisation to Burleigh Waters (which still has 0 v2 coverage) and Varsity Lakes (11%).
- Triage the 47 listed-but-no-image Robina records — these are a different problem (currently for sale but somehow zero images across all sources).

---

## [COVERAGE-AUDIT-004] Floor plans extracted from v2 image arrays via GPT-4o-mini vision — 18:08 AEST

**Symptom:** Floor-plan coverage on cadastral records was 393/11,953 = 3.3% in Robina, with the prevailing assumption that floor plans live only on listing pages (not Domain property-profile pages). Will pointed out the assumption was wrong: Domain's profile page for 16 Tullamarine Dr embeds the floor plan as image 29 of 29 in the `media({"categories":["IMAGE"]})` array, untagged. The data was sitting in the v2 scrape results we already had — we just weren't extracting it.

**Diagnosis:** Domain doesn't tag floor plans separately (`category: "property"` for all images). Listing agents upload them in arbitrary positions — most often near the end, but not reliably last. A 6-record visual sample showed 1/6 last images = floor plan; expanding to multi-listing aggregations (Legend Trail records with 100+ images), floor plans can be buried at position 121/161. Position heuristics fail; visual classification is required.

**Fix:** Built `scripts/extract_floor_plans_from_v2_images.py` — GPT-4o-mini vision classifier with structured "YES/NO architectural floor plan?" prompt at `detail=low`. Per-record URL dedup; global thread-safe URL cache to skip API calls on shared images across records (saves ~5-15% of calls on duplicate-heavy datasets like golf-estate cohorts). Output written to non-destructive `floor_plans_v2_extracted[]` field + `floor_plans_v2_classifier` metadata (model, prompt version, per-candidate verdicts). Idempotent (skip if `floor_plans_v2_extracted_at` set; `--force` to override). Concurrent (`--workers` for records, `--image-workers` per record).

**Test (20 records, `--tail 3`):** 4/20 records found ≥1 floor plan (20% recall), 5 images extracted. **Precision: 5/5 manually verified = 100%.** Established the classifier works; recall capped by the last-3 search window.

**Full run (`--all --all-images --workers 20 --image-workers 8`):**
- Eligible: 2,736 Robina records with ≥5 v2 images
- Runtime: 27.5 min (1,652s)
- Total images classified: 102,565 (5,762 cache hits = 5.6% saved)
- API calls: 96,803 gpt-4o-mini vision at `detail=low` ≈ ~$14.50 estimate
- Records with ≥1 floor plan found: **1,578 (58%)**
- Floor plan images extracted: **2,723**
- Errors: 0
- Precision (12 random samples spot-checked): **12/12 = 100%** — every sample was a genuine architectural floor plan, several included site plans too

**Coverage delta (Robina):**

| Source | Records with FP | % of total cadastral |
|---|---:|---:|
| floor_plans (legacy, mostly dead Azure URLs) | 393 | 3.3% |
| floor_plans_original (live Domain CDN) | 390 | 3.3% |
| **floor_plans_v2_extracted (NEW)** | **1,588** | **13.3%** |
| **Union (any source)** | **1,850** | **15.5%** (vs 3.3% before) |

**Key win — coverage for never-listed cadastral records:** 1,448 floor plans now exist for properties that have never been listed on Domain. Previously zero data for these.

**Files:**
- `scripts/extract_floor_plans_from_v2_images.py` (new, pushed to Will954633/Fields_Orchestrator commit `2f1b4b2`)
- `logs/coverage/fp_extraction_full_20260518_1740.log` (full HTTP-traced run log)
- `logs/coverage/fp_extraction_robina_20260518_1808.json` (per-record results)

**Recurrence:** First — no prior attempt at extracting floor plans from v2 image arrays.

**Next:**
- Update the `address_coverage_audit.py` to count `floor_plans_v2_extracted` as a valid floor-plan source.
- Consider running on Burleigh Waters and Varsity Lakes once their v2 coverage is improved (currently 0% and 11% respectively).
- Wire the extracted floor plans through the live website — they need to be readable by the property page (probably write to `floor_plans_original` or extend `property.mjs` fallback chain).
- Fix the floor_plans → dead-Azure-URL website bug (parallel to the IMAGE-OUTAGE-001 photo fix, but never applied to floor plans).

## [COVERAGE-AUDIT-005] Floor-plan extraction expanded to ALL image sources, not just v2 — 20:22 AEST

**Symptom:** After COVERAGE-AUDIT-004 hit 1,850 (15.5%) FP coverage on Robina, audit revealed 5,332 records (~66% of the image-bearing pool) had never been through the classifier. The original eligibility query only saw `scraped_data_v2.image_urls`, so 4,177 records whose images came from the apr01 mongodump recovery (or `property_images_original` / `scraped_property_images`) were silently skipped. Will's hypothesis: many properties without v2 scrape data still have floor plans buried in the apr01-recovered photo arrays.

**Root cause:** Extractor query and image-loading logic were narrow to one source. The audit's "any image" count (8,089) was 3x larger than the classifier's input pool (2,736) — a hidden mismatch.

**Fix:** Added `--all-images` flag to `scripts/extract_floor_plans_from_v2_images.py`:
1. Eligibility query widened to `$or` across 6 image-source fields (v2 + apr01 phase 1/2/3 + property_images_original + scraped_property_images).
2. Per-record image collection replaced single-source read with union+dedup helper `collect_image_urls()` (preserves first-seen order across all sources).
3. Projection extended to include all 6 source fields.
4. Two stale `urls` references inside `process_record()` cleaned up — caught by initial 20-rec smoke (20/20 EXCEPTION → fixed → 8/20 FP found, 0 errors).

**Verification:**
- Smoke (20 records, `--all-images`): 8/20 found FPs, 12 extracted, 0 errors, 75s.
- Full run (`--all --all-images`, 8,088 records, 20×8 workers): completed 20:22 AEST.
  - Runtime: 82.6 min (4,956s)
  - Images classified: **289,614** (2.8x the COVERAGE-AUDIT-004 volume)
  - API calls: ~$23 estimate at gpt-4o-mini detail=low
  - Records with ≥1 FP found: **3,195 (40% of cohort)**
  - Total FP images extracted: **6,755** (2.5x prior run)
  - Errors: 0
  - **Precision (12 random samples from apr01-only cohort spot-checked): 12/12 = 100%** — every sample was a real architectural floor plan, many with site plans included.

**Coverage delta (Robina, cumulative across runs):**

| Source | Records with FP | % of 11,954 cadastral |
|---|---:|---:|
| floor_plans (legacy, dead Azure URLs) | 393 | 3.3% |
| floor_plans_original (Domain CDN) | 390 | 3.3% |
| **floor_plans_v2_extracted (now from all sources)** | **3,195** | **26.7%** |
| **Union (any source)** | **3,311** | **27.7%** (vs 15.5% post-CA-004, 3.3% pre-extraction) |

8.4x lift on Robina FP coverage in one day; **+1,459 floor plans** added on top of CA-004's gains, the majority on records that were never listed on Domain and only have apr01-recovered images.

**Files:**
- `scripts/extract_floor_plans_from_v2_images.py` (added `--all-images` flag, union-source query/projection, helper `collect_image_urls()`)
- `logs/coverage/fp_extraction_v2_all_20260518_1859.log` (full HTTP-traced run log)
- `logs/coverage/fp_extraction_robina_20260518_2022.json` (per-record results — 2.2MB)

**Recurrence:** First. CA-004 was the v2-only version; this is the all-sources expansion. Same classifier, same prompt, same 100% precision held across a different image-source distribution.

**Next:**
- Re-run `address_coverage_audit.py` to surface the new 27.7% Robina total in the official audit.
- Apply same `--all-images` run to the 7 other target suburbs (Burleigh Waters, Varsity Lakes, Mudgeeraba, Reedy Creek, Carrara, Worongary, Merrimac). Same playbook, estimated total ~$100–150 cost, multiplies the win across ~35K properties.

## [PATH-A-VALUATION] Auto-run valuation engine for new appraisal pipeline subjects — 16:15 AEST
**Symptom:** Page 10 of every appraisal PDF rendered "Analyst review required" forever. Homeowner-submitted subjects (e.g. 12 Beaconsfield Drive, Tusmore Place, 1 Glenside Drive) have no `valuation_data` because they have never been on the for-sale list, so the nightly batch precompute_valuations.py skips them, so §03 receipts had no comp cards to render.

**Root cause:** Two issues stacked.
1. No pipeline step existed to compute valuation_data for an on-demand subject. The batch script's per-subject entry point `precompute_property_valuation()` was reachable but had no wrapper, no process_commands entry, no bridge integration.
2. Even after wiring the engine, §03 receipts came back empty. `data_pull.section_03_receipts` reads `valuation_data.comparables[]`, but the engine writes the actually-used sold comps to `valuation_data.recent_sales[]` (the `comparables[]` field is reserved for current listings used as comps, which is almost always empty for residential subjects). Schema mismatch between engine output and report consumer.

**Fix:**
1. Built `scripts/run_subject_valuation.py` — wrapper around `precompute_property_valuation()` that loads catchment-only sold data (~30s vs 131s full GC) and writes back to `Gold_Coast.<suburb>.<subject_id>.valuation_data`.
2. Added process 301 to `config/process_commands.yaml` invoking the wrapper with `--pipeline-id`.
3. Updated bridge sync (`property_reports_to_appraisal_pipeline_bridge.py`) and `analyse-lead.mjs` to queue 301 then 300 (1s offset, FIFO sequential) for any new pipeline record whose subject lacks `valuation_data`.
4. Updated `data_pull.section_03_receipts` to read from `recent_sales[]` ∪ `comparables[]` filtered to `included_in_valuation=True`, mapped engine field names (`adjustment_result.adjusted_price`, `original_sale_price`) to the card schema, capped adjustment rows at 7 + rolled remainder into a "smaller adjustments (net)" line, normalised address (strip "QLD 4220" tail).
5. Added §03 receipts compact-variant CSS overrides in `09_Appraisals/Version_Four/preview.html` (template uses inline styles, so selectors had to target the inline-styled `.comp-card` table cells with `!important`).

**Verification:** Test pipeline 6a0aaac3b65866386eac3b96 on subject 12 Beaconsfield Drive Burleigh Waters. After auto-301+300: range $1.88M–$2.40M (confidence: high), page 10 renders 2 comp cards (37 Beaconsfield $2.14M adjusted, 23 Kestrel $2.30M adjusted) each with 8 adjustment rows, footer aligned, zero overflow, zero hard breaches.

**Files:**
- `scripts/run_subject_valuation.py` (new, ~230 lines)
- `config/process_commands.yaml` (process 301 added)
- `scripts/property_reports_to_appraisal_pipeline_bridge.py` (queues 301 before 300)
- `scripts/appraisal_template/data_pull.py` (section_03_receipts schema fix + row cap + address normalisation)
- `09_Appraisals/Version_Four/preview.html` (compact-variant overrides for 03_receipts comp cards)

**Recurrence:** First. Schema mismatch is the kind of bug that would silently regress if the engine schema changes — caption now explicitly names `valuation_data.recent_sales` so any future schema drift surfaces in the audit trail.

## [MINI-SITE-DAY1] Kill Merrimac fallbacks across `/your-home/` mini-site — 18:30 AEST
**Symptom:** Every submitted property at `/analyse-your-home` rendered Merrimac-specific content on `/your-home/<slug>` — "Yours is one of only four. Out of 142 homes for sale across Merrimac, Robina, and Varsity Lakes...", "387 m to All Saints", Merrimac sold cohort references, hardcoded Federal Place / Vanderbilt Court / Lockwood / Bayford activity feed items, "13-terrace-court-merrimac/share-card.jpg" share image — regardless of which suburb / address was submitted. User reaction: "it's just building for the same Merrimac home every time."

**Root cause:** The page was built as a "fixture + thin overlay" scaffold. `stubFromDoc()` overlaid only ~7 fields on top of the TERRACE_COURT_13 Merrimac fixture (address, bed/bath/car/land, hero photo, valuation range, 2 market numbers). The remaining ~17 sections — scarcity headline, buyers thesis/catchment/campaignMath, positioning frame/vocabulary/tradeOffs/photography/sampleParagraph, valuation.comps[], hero.headline/subhead/chips, shareMoment, activity[], inventory.condition/orientation/layout, POIs, statTiles — all rendered the Merrimac fixture verbatim. The slot resolver writes real bed/bath/photos/valuation, but had no mechanism to surface "this section isn't real yet" so the fixture leaked through everywhere.

**Fix:** Day 1 of the dynamic-content rollout. Introduced a `slotStatus: { scarcity, buyers, positioning, comps, market_narrative }` field on the fixture, seeded by `stubFromDoc` from `doc.slot_status` (API field — defaults to all-pending until the slot resolvers + analyst approval flow lands on Days 2-14). Built `PendingPlaceholder` component (copper accent, pulsing dot, "Live within 3 business days" ETA). Every Merrimac-bleeding section in every tab now renders `<PendingPlaceholder>` when its slot isn't approved.

Specific changes:
- `HeroSection` — headline/subhead gated on scarcity. Chips derived from inventory (bed/bath/land/internal/car) instead of fixture's curated mix of inventory + scarcity facts.
- `YourHomeTab` — `statTiles` computed from inventory. `inventory.condition/orientation/layout` gated on scarcity. POI table gated on scarcity. ShareMoment hidden until scarcity approved. ActivityFeed initial state changed from `fixture.activity` → `[]`; empty state shows PendingPlaceholder. `normaliseLive` relaxed to accept API items missing `body`.
- `MarketTab` — competitor map + match cards gated on `comps`. Scarcity headline/combinatorial/cohort premiums gated on `scarcity`. Market narrative paragraph gated on `market_narrative`. Hardcoded "Merrimac, Robina, and Varsity Lakes" → dynamic `address.suburb`.
- `BuyersTab` — entire tab gated on `buyers`. Personas additionally gated on `positioning`. CitationStrip Merrimac references → dynamic suburb.
- `PositioningTab` — entire tab gated on `positioning`.
- `ValuationTab` — comp table gated on `comps`. "Merrimac's last-18-month cohort" → dynamic suburb.

**Verification:** Screenshotted `https://fieldsestate.com.au/your-home/5-cedarwood-crescent-robina`. Zero matches for `Merrimac`, `All Saints`, `Federal Place`, `Vanderbilt`, `Lockwood`, `Bayford`, `Kingary`, `Cypress Place`, `142 homes`, `13-terrace-court-merrimac` in the rendered page-text. Real values render correctly: hero "Your report for 5 Cedarwood Crescent, Robina, QLD 4226", chips "3 bd / 2 ba, 409 m² block, 221 m² internal, 2 car", 5 real Domain CDN photos. Pending placeholders show for feature stack + walking distance + market + buyers + positioning + comps. Page is now honest.

**Files:**
- `01_Website/src/pages/YourHomePage/components/PendingPlaceholder.tsx` (new)
- `01_Website/src/pages/YourHomePage/components/PendingPlaceholder.module.css` (new)
- `01_Website/src/pages/YourHomePage/components/HeroSection.tsx`
- `01_Website/src/pages/YourHomePage/data/homeFixture.ts` (SlotStatus type + fixture default)
- `01_Website/src/pages/YourHomePage/YourHomePage.tsx` (PropertyReportDoc.slot_status + stubFromDoc seeding)
- `01_Website/src/pages/YourHomePage/tabs/YourHomeTab.tsx`
- `01_Website/src/pages/YourHomePage/tabs/MarketTab.tsx`
- `01_Website/src/pages/YourHomePage/tabs/BuyersTab.tsx`
- `01_Website/src/pages/YourHomePage/tabs/PositioningTab.tsx`
- `01_Website/src/pages/YourHomePage/tabs/ValuationTab.tsx`

**Commits:** a59f8dd6 (initial Day 1 push) + 10994aac (Hero/Activity/ShareMoment patch) on Will954633/Website_Version_Feb_2026

**Recurrence:** First. Day 1 of the 14-day mini-site dynamic-content build plan. Days 2-14 build the resolver chain that promotes each slot from "pending" → "approved" with real per-property content.

## [MINI-SITE-DAY2] Wire valuation engine comps into mini-site — 19:00 AEST
**Symptom:** Mini-site Valuation tab showed "Consultant review in progress" placeholder for every property — no real comp cards even when the precompute_valuations engine had run and written `valuation_data.recent_sales[]` to the Gold_Coast doc.

**Root cause:** The SlotResolver poller pulled basic property facts (bed/bath/photos) from Gold_Coast and a simple model_range from a 20-sale bedroom-matched median, but had no method to surface the engine's per-comp adjustments. The engine output (`valuation_data.recent_sales[]` with `included_in_valuation=True`, `adjustment_result.adjusted_price`, weights, narratives) sat unused on the Gold_Coast doc. Mini-site frontend had no field to read because there was no field to project.

**Fix:** Day 2 of the dynamic-content rollout. Built the end-to-end wiring:
1. `SlotResolver.valuation_comps_from_engine()` (new method, scripts/property_reports/slot_resolver.py) — reads `subject.valuation_data.recent_sales`, filters to `included_in_valuation=True`, sorts by weight, maps engine schema → mini-site Comp[] shape: `{ address, soldPrice, soldDate, land, internal, bedrooms, bathrooms, condition, notes, adjustedToSubject, weight_pct }`. Engine fields used: `original_sale_price`, `sale_date` (handles epoch ms + ISO string), `features.basic.{land_size_sqm,floor_area_sqm,bedrooms,bathrooms}`, `adjustment_result.adjusted_price`, `weight.normalized`/`weight.raw_weight`, `narrative` (truncated to 280 chars), address normalised (strip state+postcode tail).
2. `resolve_all()` now calls the method; on success writes `valuation.comps` + auto-promotes `slot_status.comps = "approved"` + stamps `valuation.comps_resolved_at`. On empty result (engine exclusion), writes empty array + leaves `slot_status.comps = "pending"` so the page shows the pending placeholder instead of a broken empty table.
3. `netlify/functions/property-report.mjs` — added `slot_status: 1` to the projection so the field reaches the frontend.
4. `YourHomePage.tsx` — extended `PropertyReportDoc` type with `valuation.comps[]` + `valuation.comps_resolved_at`. Added overlay in `stubFromDoc`: `cloned.valuation.comps = doc.valuation.comps`. Engine comps override the fixture's hardcoded Merrimac comps.
5. `ValuationTab.tsx` — added `fmt$()` null-safe formatter, `fmtArea()`, `fmtDateShort()` ("2026-02-04" → "Feb 2026"). Loop key changed from `comp.address` to `${address}-${idx}` (engine sometimes produces same-address sales across years).

Interim: slot is auto-promoted to "approved" on resolver success. Day 14 moves the promotion gate behind the ops dashboard analyst sign-off (per the Day-0 requirement that analyst review must drive both V4 PDF + mini-site simultaneously).

**Verification:** Created synthetic property_report `41-olympus-drive-robina-test` linked to existing valued listing 41 Olympus Drive Robina (subject_id=690bd7eb8b8f5465926068f0). Reset to stub state. Poller resolved in next cycle (~15s). Result: 7 comps written, `slot_status.comps=approved`, model_range $1,426,500–$1,743,500. Live API `/api/v1/property-report?slug=41-olympus-drive-robina-test` returned `slot_status.comps=approved` + 7 comp records. Screenshot of `/your-home/41-olympus-drive-robina-test#valuation` shows real comp table with 38 Nardoo Street ($1,585,000 → $1,525,618 adjusted), 18 Fan Road, 4 Springvale Street, 24 Springvale Street, 81 Thorngate Drive, 4 Sawgrass Place — each with line-itemised adjustment narrative, suburb-specific rates, weights ranging 14% → smaller. Test record cleaned up post-verification.

**Files:**
- `scripts/property_reports/slot_resolver.py` (new `valuation_comps_from_engine` method + `resolve_all` wiring)
- `01_Website/netlify/functions/property-report.mjs` (slot_status projection)
- `01_Website/src/pages/YourHomePage/YourHomePage.tsx` (PropertyReportDoc.valuation.comps type + stubFromDoc overlay)
- `01_Website/src/pages/YourHomePage/tabs/ValuationTab.tsx` (null-safe formatters)

**Commits:**
- Will954633/Fields_Orchestrator: `e815eea0` (slot_resolver)
- Will954633/Website_Version_Feb_2026: `d7ff10c3` (YourHomePage), `81728fbe` (ValuationTab), `3aa3d112` (API)

**Recurrence:** First. Day 2 of the 14-day mini-site dynamic-content build plan. Day 3 expands the deterministic wins — full inventory (internalArea, yearBuilt, propertyType), activity timeline, expanded shareMoment — all without LLM calls.

## [MINI-SITE-DAY3] Nullable inventory + property type/year built + richer activity feed — 19:15 AEST
**Symptom:** Two residual Merrimac bleeds: (1) hero chip "221 m² internal" rendered on Cedarwood/Olympus pages even though those properties have no `total_floor_area` in the DB (the Merrimac fixture's `inventory.internalArea=221` was leaking through because `stubFromDoc` only overlaid the field when the doc provided it); (2) hero address read "5 Cedarwood Crescent, Robina, QLD 4226, Robina QLD 4226" — the full address string was stored in `cloned.address.line1` and then re-concatenated with the suburb+state+postcode in HeroSection. Activity feed showed only 2 cards (`stub_created` + `data_resolved`) — sparse and uninteresting compared with the 5-event Merrimac fixture feed it replaced.

**Root cause:** (1) `stubFromDoc` used `if (p.X != null) cloned.inventory.X = p.X` — when doc returned null/missing, cloned kept the Merrimac default. (2) `analyse-your-home-submit.mjs` stored the full address verbatim, then `stubFromDoc` set `line1: doc.address` without stripping the suburb/state/postcode tail. (3) `build_property_report.py` emitted exactly one `data_resolved` event regardless of what the resolver actually accomplished.

**Fix:**
1. Made every `ReportFixture.inventory` field nullable in the type (bedrooms, bathrooms, carSpaces, landArea, internalArea, internalToLandRatio, condition, orientation; yearBuilt was already nullable; added `propertyType: string | null`).
2. `stubFromDoc` now explicitly assigns from doc OR null — no more inherited Merrimac defaults. Also explicitly nulls `condition`, `orientation`, `layout` (which the scarcity slot will populate later).
3. Added address normalisation in `stubFromDoc`: strip `, QLD 4226` tail + `, Robina` suffix from `doc.address` to derive a clean street-only `line1`. Postcode derived from the regex match.
4. `YourHomeTab` inventory card now conditionally renders each field — bedrooms/bathrooms/carSpaces/landArea/internalArea/internalToLandRatio/yearBuilt/propertyType all disappear if null instead of showing "null m²" or staying at Merrimac defaults.
5. Stat tiles + hero chips extended to include `propertyType` and `yearBuilt` when present.
6. `build_property_report.py` now emits up to 3 activity events per resolution:
   - `data_resolved` (always — same headline as before)
   - `valuation` (when `valuation.comps` was written by the engine — headline names the comp count + working range)
   - `market_state` (when ≥3 photos pulled — "We pulled N photos and the satellite view")

Events $pushed reversed-with-position-0 so newest ends up at top of the activity feed.

**Verification:** Created test stub `41-olympus-drive-robina-day3` (property_id pointing to a fully-valued Olympus Drive Robina listing with `propertyType=House`, no `year_built`). Poller resolved in next 15s cycle. Live page screenshot confirmed:
- Address: "41 Olympus Drive" (clean line1, no double-suburb)
- Hero chips: `4 bd / 2 ba · 512 m² block · 130 m² internal · 2 car · House` — propertyType present, yearBuilt absent (gracefully)
- Stat tiles: 6 real fields, no Merrimac data
- Inventory card: real values + "Property type: House" row, no condition/orientation Merrimac rows
- Activity feed: 4 stacked events with copper accents, none Merrimac
- Pending placeholders on scarcity-dependent sections (feature stack, walking distance) — unchanged from Day 1
- Zero matches for `Merrimac`, `All Saints`, `Terrace Court`, `221 m²`, `9 / 10`, `North-facing`, `Kingary`, `Federal Place`, `Vanderbilt` in rendered text

Test record cleaned up.

**Files:**
- `scripts/property_reports/build_property_report.py` (activity events × 3)
- `01_Website/src/pages/YourHomePage/YourHomePage.tsx` (address normalisation + explicit-null inventory overlay)
- `01_Website/src/pages/YourHomePage/data/homeFixture.ts` (nullable inventory type + propertyType field)
- `01_Website/src/pages/YourHomePage/tabs/YourHomeTab.tsx` (conditional inventory rows + propertyType + yearBuilt stat tiles)
- `01_Website/src/pages/YourHomePage/components/HeroSection.tsx` (propertyType + yearBuilt chips)

**Commits:**
- Will954633/Fields_Orchestrator: `cf79920e` (activity events)
- Will954633/Website_Version_Feb_2026: `870d1c91` (YourHomePage), `63212776` (homeFixture), `13e3c402` (YourHomeTab), `9493d645` (HeroSection)

**Recurrence:** First. Day 3 of 14 — deterministic wins. No LLM calls. Day 4 next: GPT-4o-mini hero photo scoring + Google Places walking-distance resolver.

## [MINI-SITE-DAY4] AI hero photo selection + walking distance resolver — 21:32 AEST
**Symptom:** Two missing deterministic data wins in the mini-site: (1) hero photo was just "first scraped image" — often a poor framing, sometimes an interior shot or floor plan; (2) the Walking Distance section showed a PendingPlaceholder for every property because the existing slot resolver wrote nothing to `pois[]` — the Merrimac fixture's All Saints / Kingary Wetland Reserve POI list never appeared either (Day 1 gating prevented that bleed).

**Root cause:** No mechanism existed for either. Day 0 plan: GPT-4o-mini for photo scoring + Google Places for POI lookup. Tried Mapbox Geocoding for POIs first — failed (it returns street names containing the keyword, not actual POI buildings). Pivoted to OpenStreetMap Overpass API with structured amenity tags + kept Mapbox Directions for walking-route distances.

**Fix:**
1. New module `scripts/property_reports/hero_photo.py` — calls OpenAI GPT-4o-mini vision API with up to 8 photos at "low" detail (~85 tokens each). System prompt scores each on hero-suitability (wide exterior framing, good lighting, daytime, clean composition vs floor plans / interior details / single-feature shots). Returns highest-scored URL + reason + per-photo scores. Cost ~$0.001-0.002 per resolution. Falls back to scraper hero on any failure.

2. New module `scripts/property_reports/walking_distances.py` — Overpass query for 5 POI categories: primary school (amenity=school + isced:level=1), secondary school (isced:level=2/3), supermarket (shop=supermarket), park (leisure=park), beach (natural=beach). Each within configured radius (2-15km). For each candidate, Mapbox Directions `walking` profile returns actual route distance (not straight-line). Skips POIs walking >2× MAX_WALK_METRES (16km) since they aren't really "walkable" anyway.

3. `SlotResolver.property_facts` rewritten — collects all candidate URLs (scraper hero + property_images), dedupes, calls `score_and_pick_hero`. AI-picked photo promoted to role=hero with `meta={picked_by:"ai", score, reason, model}` audit trail. Remaining photos become gallery in original order.

4. `SlotResolver.resolve_all` extended — when subject has lat/lng, calls `resolve_pois`. Writes to `property_reports.<slug>.pois[]` + `slot_status.walking_distance="approved"`.

5. Frontend: new `walking_distance` slot in `SlotStatus` type. `PropertyReportDoc` extended with `pois[]` + `slot_status.walking_distance`. `stubFromDoc` overlays `doc.pois` directly. `YourHomeTab` decoupled the POI table (gated on `walking_distance`) from the scarcity narrative (still gated on `scarcity`) — table appears as soon as Mapbox resolves, narrative waits for analyst review.

6. API projection: `pois: 1` added to property-report.mjs.

**Verification on 41 Olympus Drive Robina (after `--force` re-resolve):**
- Hero AI-picked: score 80, reason "Good exterior shot with ample yard space, but twilight photo" — twilight exterior beat interior shots in the candidate set
- 5 POIs resolved: Arcadia College 1242m · Scottsdale Parklands 1251m · Aldi 1873m · Australian Industry Trade College 2030m · Miami Beach 7012m
- `slot_status.walking_distance=approved`
- Live page renders the table with real metres + "Source: Mapbox walking routes — actual route distance, not straight-line"
- Zero Merrimac POI bleed

**Files:**
- `scripts/property_reports/hero_photo.py` (new, ~140 lines)
- `scripts/property_reports/walking_distances.py` (new, ~220 lines)
- `scripts/property_reports/slot_resolver.py` (property_facts rewritten + POI wiring in resolve_all)
- `01_Website/src/pages/YourHomePage/data/homeFixture.ts` (walking_distance slot)
- `01_Website/src/pages/YourHomePage/YourHomePage.tsx` (PropertyReportDoc.pois + stubFromDoc overlay)
- `01_Website/src/pages/YourHomePage/tabs/YourHomeTab.tsx` (POI table gated on walking_distance, narrative on scarcity)
- `01_Website/netlify/functions/property-report.mjs` (pois projection)

**Commits:**
- Will954633/Fields_Orchestrator: `819b2d27` (hero_photo), `1d383399` (walking_distances), `19974610` (slot_resolver)
- Will954633/Website_Version_Feb_2026: `ed78a6f9` · `4eeebbbf` · `cad5c8c8` · `922c1a09`

**Cost per resolution:** ~$0.002 (GPT-4o-mini) + 5-10 Mapbox Directions calls (well within free tier) + Overpass (free). Total well under $0.01.

**Recurrence:** First. Day 4 of 14. Day 5-6 next: market context narrative resolver (Opus on suburb numbers — first narrative LLM day).

## [MINI-SITE-DAY5] Opus market narrative + competitor_matches slot separation — 21:50 AEST
**Symptom:** Mini-site MarketTab showed only generic "Wages lead prices in the Gold Coast cohort..." narrative — same paragraph for every property, no suburb-specific interpretation of the numbers shown above. Also discovered (during Day 5 verification) that the Competition map + MatchCards section was rendering Merrimac fixture competitor data ("Federal Place Robina", "Bellfield Cct" etc.) on every property — Day 1 had conflated the `comps` slot (valuation comps) with competitor matches.

**Root cause:** No resolver existed for per-suburb market interpretation. Day 0 plan: Opus narrative grounded in the suburb's transaction count, YoY median, days-on-market, active stock — with editorial guardrails. For the competitor-matches bleed: Day 1 used a single `comps` slot to gate both the valuation comps table AND the competitor map, then Day 2 auto-promoted `comps` when the engine ran — which un-gated the competitor map even though no live-competitor resolver exists.

**Fix:**
1. New module `scripts/property_reports/market_narrative.py` — calls Anthropic Opus 4.7 with a system prompt encoding the editorial rules (no advice, no predictions, no forbidden words, exact figures, conditional language, cite Abelson et al. 2005 for wage-price linkage). Renders the market dict as a structured facts block. 3 retries with backoff [2, 5, 12]s. Validator rejects outputs with advice patterns ("you should"), prediction patterns ("prices will rise"), forbidden words ("stunning", "nestled", "boasting", "rare opportunity", "robust market"), or length outside 50-1200 chars.
2. `SlotResolver.resolve_all` extended — after market_state runs, calls `resolve_market_narrative` with the market dict + suburb + bedroom band. On success writes `market_narrative` doc with `{text, generated_at, model, inputs_snapshot, attempt}` + `slot_status.market_narrative="approved"`. On final failure writes `market_narrative_error` + slot status = "error".
3. Frontend: `PropertyReportDoc.market_narrative` typed, `stubFromDoc` lifts `doc.market_narrative.text` to `fixture.marketNarrative`. MarketTab renders the narrative in a copper-tinted "What the data describes" card when slot approved, error placeholder if slot=error, pending placeholder otherwise.
4. **Competitor-matches slot separation** (discovered during verification, fixed in same Day 5 batch) — added `competitor_matches: "pending" | "approved" | "error"` to SlotStatus, distinct from `comps`. MarketTab Competition section + MatchCards now gate on `competitor_matches`. Slot stays pending until a future resolver (Day 12 buyers work) populates live competitor data. This kills the Merrimac fixture's "Federal Place Robina", "Bellfield Cct", "Sienna Lane Mermaid Waters" competitor cards that were leaking through every property page.

**Verification on 41 Olympus Drive Robina:** Force-resolved with new code. Opus generated 551-char narrative on attempt 1:

> *"Your Robina cohort recorded 2,373 sold transactions over the past 24 months against 76 active listings currently on market, indicating turnover depth relative to standing stock. The rolling 12-month median sits at $1,450,000, up 6.5% year-on-year, with the latest quarter median at $1,520,000. Median days-on-market has lengthened to 26 from a historical 22 (a 9-day YoY shift). If wage growth holds, Abelson et al. (2005) findings (r=0.940, 1.71× income elasticity) suggest continued price support, though the days-on-market drift warrants attention."*

Editorial validation passed: no advice, no predictions, conditional tense, exact figures, Abelson citation, no forbidden words. Live API returns `slot_status.market_narrative=approved` + the text. Screenshot of `#market` tab shows the narrative in the copper-tinted "What the data describes" card directly under the stat grid.

**Known transient issues (non-blocking):**
- OpenAI vision API got a "Timeout while downloading" the Domain CDN URL during this re-resolve. Hero photo fell back to the scraper's pick (already correct in DB). Worth a Day 6 follow-up: longer timeout, base64 pre-download, or skip-and-keep-prior-hero on download timeout.
- Free Overpass endpoint returned 504 on one query — recovered, got 4/5 POIs (Miami Beach didn't resolve this round). Self-hosted Overpass or retry on 5xx for Day 6.

**Files:**
- `scripts/property_reports/market_narrative.py` (new, ~180 lines)
- `scripts/property_reports/slot_resolver.py` (market_narrative wiring after market_state)
- `01_Website/src/pages/YourHomePage/data/homeFixture.ts` (marketNarrative field + competitor_matches slot)
- `01_Website/src/pages/YourHomePage/YourHomePage.tsx` (market_narrative consumer + competitor_matches seeding)
- `01_Website/src/pages/YourHomePage/tabs/MarketTab.tsx` (narrative card render + competitor_matches gating)
- `01_Website/netlify/functions/property-report.mjs` (market_narrative projection)

**Commits:**
- Will954633/Fields_Orchestrator: `08b327c9` (market_narrative), `c2392dc7` (slot_resolver)
- Will954633/Website_Version_Feb_2026: `43be97e2`, `ea7ff04b`, `bea90231`, `39f2c865` (Day 5 main); `f664b851`, `8f391948`, `c7e83780` (competitor_matches slot separation)

**Cost:** Opus 4.7 generation ~$0.04 per property (input ~150 tokens + output ~150 tokens). One-time per resolution.

**Recurrence:** First — establishes the narrative-LLM pattern (system prompt + structured input + retry + validator + audit trail) that Days 6-13 reuse for scarcity, positioning, buyers narratives.

**Day 5 also closes the architectural decision pattern that drives Days 6-13:**
- Each narrative slot has its own resolver module
- Each resolver follows the same shape: `resolve_X(inputs) → {text, generated_at, model, inputs_snapshot, attempt} | None`
- SlotResolver.resolve_all calls each, writes result + slot_status
- Frontend reads slot status, renders narrative or PendingPlaceholder
- Editorial validator is in the resolver, not the frontend — invalid outputs never reach the page

## [MINI-SITE-DAY6] Resilience: hero photo base64 pre-download + Overpass mirror retry — 22:02 AEST
**Symptom:** Two transient failures observed during Day 5 verification:
1. OpenAI vision API returned `Timeout while downloading https://bucket-api.domain.com.au/...` — the Domain CDN occasionally serves images too slowly for OpenAI's internal URL-fetch timeout. Result: hero AI-pick silently skipped, page fell back to the scraper's first photo.
2. Overpass API returned `HTTP 504 Gateway Timeout` on the first query — free public endpoint at `overpass-api.de` rate-limits under load. Result: 4/5 POIs instead of 5 (Miami Beach dropped).

**Root cause:** Both resolvers depended on third-party endpoints with hard timeouts and no retry logic. OpenAI was being asked to download Domain CDN URLs, putting two networks (us → OpenAI → Domain) in the critical path. Overpass had no failover.

**Fix:**
1. `hero_photo.py` now pre-downloads each candidate photo with 8s per-image timeout, then sends to OpenAI as base64 data URLs. Downloads run in parallel (ThreadPoolExecutor, max 8 concurrent). Photos that fail to fetch are dropped from the scoring set; the resolver only fails entirely if ZERO photos download. 4MB safety cap on individual image size. Per-image timeout means a single slow photo doesn't block the whole resolver.
2. `walking_distances.py` now rotates through three Overpass mirrors with retry backoff [1, 3, 6]s between attempts on 5xx / network errors:
   - `overpass-api.de` (main)
   - `overpass.kumi.systems` (Kumi mirror)
   - `overpass.openstreetmap.ru` (Russia mirror)
   4xx responses fail fast (query is malformed). 5xx and timeouts retry up to 3× per endpoint then fall through to the next.

**Verification on 41 Olympus Drive Robina (post-fix):**
- Hero pick succeeded via base64: 8 photos downloaded in parallel (~5s), scoring took ~6s, total 14.5s end-to-end. Same selection (twilight exterior, score 80) — confirms the score function is deterministic given the same inputs.
- Walking distances: 5 POIs resolved (Arcadia College 1242m, Scottsdale Parklands 1251m, Aldi 1873m, AITC 2030m, Miami Beach 7012m). Total 47.9s — slower than Day 4 (~30s) because of the retry attempts, but reliably returns all 5 now.

**Trade-offs accepted:**
- Hero resolver wall-time grew from ~7s to ~15s because of the pre-download step. Acceptable given this runs once at submission, asynchronously.
- Walking resolver wall-time grew because retries serialize when the first endpoint times out. Could be mitigated by racing all three endpoints in parallel and taking the first response — left as future work.

**Files:**
- `scripts/property_reports/hero_photo.py` (base64 pre-download + parallel fetch)
- `scripts/property_reports/walking_distances.py` (Overpass mirror list + retry backoff)

**Commits:**
- Will954633/Fields_Orchestrator: `d708ba52` (hero_photo), `317786ab` (walking_distances)

**Recurrence:** First — closes the two known transients from Day 4-5. Day 7 starts scarcity, the largest narrative resolver.

## [MINI-SITE-DAY7-8] Scarcity feature extraction + cohort premiums — 22:32 AEST
**Symptom:** Scarcity section ("What makes your home rare — measured") still rendered Merrimac fixture data: "Six bedrooms, dual living, a pool, and a cul-de-sac head against permanent bushland..." with hardcoded premium percentages (+11.3% six-bed, +6.8% bushland, +4.1% pool etc.). No per-property feature extraction; no real cohort-derived premiums.

**Root cause:** No resolver existed. Day 0 plan: feature stack identification (which features make this home uncommon) + combinatorial match count (how many other actives carry the same stack) + cohort premiums (what each feature commanded historically).

**Fix (Days 7-8):**
1. `scripts/property_reports/scarcity_features.py` — 11 feature rules with thresholds (5+ bed, 6+ bed, 3+ bath, 900m²+ land, 1500m²+ land, 250m²+ floor, pool, water views, within 2km/1km beach, two-storey, premium finish). Each rule has a predicate over `valuation_data.subject_property.features.basic` (the engine's per-property feature dict, the same data that drives the valuation), a render fn for human display, a mongo clause for counting actives, and an optional `supersedes` (so 6+ bed replaces 5+ bed in the notable list). Counting uses the same engine-feature path (`valuation_data.subject_property.features.basic.*`) which has ~70% coverage on active listings; 174 of ~250 actives across the 9-suburb catchment have engine features.

2. `scripts/property_reports/cohort_premiums.py` — For each notable feature, loads up to 500 sold properties per catchment suburb with engine features + sale prices, partitions by has-feature vs not, computes median sale price of each partition, returns `premium_pct = (m_with - m_without) / m_without * 100`. Reliability flag: requires both partitions ≥ 20 sales AND `|premium_pct| ≥ 2%` (anything within ±2% is noise).

3. `SlotResolver.resolve_all` — after `market_state` runs, calls `resolve_scarcity_features` then `compute_cohort_premiums`. Writes a structured `scarcity_features` doc with `{notable_features, active_matching_full_stack, active_listings_total, catchment_suburbs, cohort_premiums, features_basic_snapshot}` to `property_reports.<slug>.scarcity_features`. Slot stays pending — Day 9's Opus narrative reads this structured data and produces the user-facing headline + premium table.

**Bonus fix shipped same batch:** Walking distance resolver had no network-level circuit breaker. When the Overpass endpoint was unreachable (`Errno 101 Network is unreachable`), the resolver kept retrying 5 categories × 3 endpoints × 3 attempts = 45 retries with 6s backoffs = ~5 minutes of stuck wall-time. Added a circuit breaker that detects `network is unreachable`, `name or service not known`, or `temporary failure in name resolution` in the exception string and aborts all retries immediately. Combined with tightened backoffs [1, 3] (was [1, 3, 6]) and 12s per-request timeout (was 25s), the worst-case walking_distances wall-time is now ~30s. Verified during a Day 8 test where Overpass was genuinely blocked from the VM — resolver completed cleanly in 10s instead of hanging.

**Verification on 12 Beaconsfield Drive Burleigh Waters (premium subject):**
- 5 notable features: 3+ bathrooms, Extra-large block (1562 m²), Large internal (289 m²), Pool, Two-storey
- Combinatorial match: **2 of 174** active listings in catchment match the full stack
- Cohort premiums (5):
  - Pool: **+32.6%** (✓ reliable, n_with=50, n_without=49) — strong, well-sampled
  - Two-storey: **-14.0%** (✓ reliable, n_with=39, n_without=46) — single-storey premium in this market
  - 3+ bathrooms: +46.6% (✗ unreliable, n_with=19 < 20)
  - 250m²+ floor: +46.8% (✗ unreliable, n_with=16)
  - 1500m²+ land: +12.8% (✗ unreliable, n_with=4)

Honest signal: pool clearly pays in southern GC; two-storey actually sells for less; the +46% bath/floor premiums are real but undersampled.

**Tests on three subjects gave matching counts that pass the smell test:**
- 41 Olympus Drive (modest): 16/174 (just premium finish)
- 12 Beaconsfield Drive: 2/174 (full premium stack)
- 38 Beaconsfield Drive (6 bed): 5/174 (6+ bed + pool)
- 5 Pintail Cres (pool + near beach): 13/174 (common stack)

**Files:**
- `scripts/property_reports/scarcity_features.py` (new, ~260 lines)
- `scripts/property_reports/cohort_premiums.py` (new, ~180 lines)
- `scripts/property_reports/slot_resolver.py` (chained both into resolve_all)
- `scripts/property_reports/walking_distances.py` (circuit breaker + tighter timeouts)

**Commits:**
- Will954633/Fields_Orchestrator: `(Day 7)` scarcity_features + slot_resolver, `4a61d5cb` (Day 8 cohort_premiums), `75674a91` (Day 8 slot_resolver), `9ef55ed2` (network circuit breaker)

**Recurrence:** First. Scarcity is the largest single slot. Day 9 next: Opus narrative grounded in this structured data — the headline scarcity claim, the cohort premium table caption, the walking-distance monopoly line. Same editorial guardrail + retry pattern Day 5 established.
