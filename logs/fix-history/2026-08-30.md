## [LIVING-MAP-COORD-FIELD-BLIND] Living Map precompute built 0 of 51 — read only LATITUDE — 06:15 AEST
**Symptom:** `precompute_living_map` heartbeat ERROR since the 2026-08-30 01:15 run:
`RuntimeError: 51 listings eligible but 0 living_maps built (51 failed)`. Rule 7b assertion
firing correctly — Living Map has been live and ON BY DEFAULT on /property since 2026-08-28,
so every one of those 51 for-sale listings was serving the page without its hero map.
**Root cause:** `build_one()` (scripts/precompute_living_map.py:643) takes the center point from
`doc["LATITUDE"]/["LONGITUDE"]` only. When there is no cadastre geometry it returns
`None, "no parcel geometry and no coordinates"`. But coordinates are written by three different
enrichments under three different paths. Measured on the 51 eligible docs: `LATITUDE` = 2,
`georeference_data.coordinates.latitude` = 1, `geocoded_coordinates.latitude` = 36, none = 15.
So 36 listings with a perfectly good point on file were reported as having no coordinates at all.
Proven by calling `build_one` directly on two of them (14b Pinehurst Place, Robina and
4204/12-14 Executive Drive, Burleigh Waters): `None -> no parcel geometry and no coordinates`
before the change, `BUILT` after, same docs, same key. This is CLAUDE.md Rule 8 in the code
itself — a guessed-single field name read as absence of the data.
**Fix:** Added the two missing fallbacks to the center-point chain, in order
LATITUDE -> georeference_data.coordinates -> geocoded_coordinates, keeping the genuine
zero-output path (no coordinates from ANY source) intact. Re-ran the job: **33 built, 15 failed,
heartbeat success**. The 15 residual are all units/townhouses/duplexes that have no coordinates
under any of the three paths — an upstream georeferencing gap for unit-numbered addresses, left
honestly red rather than silenced. NOT weakened: the Rule 7b assertion is unchanged, so if those
15 stay uncoordinated the row will legitimately go red again and should be fixed upstream.
**Files:** scripts/precompute_living_map.py
**Recurrence:** First (same *class* as [FIELD-NAME-GUESS-FALSE-ABSENCE] 2026-08-09)

## [FB-MULTISELECT-LIST-POISONS-SHEET] One multi-select lead failed the whole Live Leads Tracker — 06:20 AEST
**Symptom:** `nightly_lead_chain` ERROR: `live_leads_to_sheet=failed` (exit 1 after 14s),
which `blocked` both dependent steps (`leads_came_to_market`, `leads_prune_nonleads`) — 3 of
8 chain steps not ok. The Live Leads Tracker sheet got NO rows at all.
**Root cause:** A Facebook lead-ad **multi-select** answer is returned as a LIST, not a string.
`live_leads_to_sheet.py:202` maps `suburb_address` straight from `fields.get("area")`, so one
lead carried `["burleigh_waters", "open_to_all_three", "varsity_lakes", "robina"]` into a cell.
The Sheets API rejects a nested list and fails the ENTIRE batch write, not the offending cell:
`Invalid values[3][7]: list_value {...}` — index 7 is the "Suburb / Address" column. Reproduced
deterministically by running the script by hand; it failed identically every time. So a single
lead answering a multi-select question takes down the whole tracker and two downstream steps,
every night, until that lead ages out of the window.
**Fix:** Added a `_cell()` coercion applied to every value in `row_values()` — lists/tuples/sets
join with ", ", None becomes "", anything else non-scalar is str()'d. Fixed at the boundary
rather than on the one field, so no future field of any shape can poison the batch the same way.
Re-ran: **exit 0, 33 rows added, 15 PostHog links filled.** The two blocked steps were left for
tonight's scheduled chain rather than re-run by hand — one prunes rows and the other can alert,
and Tier 1 forbids re-running anything that deletes or contacts.
**Files:** scripts/live_leads_to_sheet.py
**Recurrence:** First for this failure; the underlying trap is the 3rd in the family
(see memory `fb_lead_field_name_mismatches` — FB lead field shapes/names differ from ours).
**Noted, not fixed:** the same run printed `(telegram send-audit failed: Cannot use MongoClient
after close)` — the audit write happens after the client is closed. Cosmetic; the notification
itself sent.

## [INSIGHTS-NULL-ROOM-NAME-POISONS-POOL] One unnamed room failed step 15 and the nightly run — 06:35 AEST
**Symptom:** Step 15 (Calculate Property Insights) ERROR, failing the whole nightly run
(2026-08-29, and previously 2026-08-26 via [WATCHDOG]): `RuntimeError: Error ratio 18.96%
exceeds threshold; marking failure.` — 140 of 738 properties failing with the bare string
`'NoneType' object has no attribute 'lower'`. The board showed `Step 15 :: no error message`
because the traceback goes to stdout while `result.json.error_message` stayed empty.
**Root cause:** `get_room_area()` read `room.get('room_name', '').lower()`. The `''` default
only applies when the key is ABSENT; floor-plan analysis writes an explicit **null** for a room
it could not name. Exactly ONE document in the three suburbs has one — 53 Easthill Drive,
Robina: `{"room_name": null, "room_type": "powder_room"}` (verified with `$type: "null"`, not a
bare `null` match, which also matches missing). Because `get_room_area` is called across the
**whole for-sale comparison pool** (lines 256 and 378), not just the subject, that single room
raised inside the calculation of 140 different subject properties. One bad record, 19% error
ratio, entire pipeline step red. Same shape as [FB-MULTISELECT-LIST-POISONS-SHEET] today.
**Fix:** Two parts, neither of which touches the 5% threshold — lowering that would have been
the forbidden "make the check stop complaining" move.
  1. `(room.get('room_name') or '')` in both the keyword and fallback-largest-bedroom branches.
  2. The `except Exception` handler now prints the property address and the full traceback
     instead of `str(exc)` alone. It had been swallowing 140 failures a night behind a message
     that named neither a line nor a field, in a ~140-line try body — which is why this took a
     reproduction to find rather than a read. CLAUDE.md Rule 7b: record the error text.
**Verified:** full re-run **exit 0, zero errors**, 738 properties processed, 59 with insights.
**Files:** scripts/backend_enrichment/calculate_property_insights.py
**Recurrence:** 2nd (step 15 also failed 2026-08-26, logged as [WATCHDOG] with no root cause)
**Left for Will:** `result.json.error_message` is empty for any step whose traceback goes to
stdout, so the board renders "no error message" on a step that stated its reason clearly. That
is health-board-adjacent code and I did not touch it.

## [FEED-V3-COSMOS-THROTTLE-500] decision-feed-v3 500s during nightly write window — 14:30 AEST
**Symptom:** `/api/v1/properties/decision-feed-v3` returned HTTP 500 (78b body) at 22:00:02 AEST 2026-08-29, recovered by the next keep-warm probe at 22:05. Telegram alert fired (paid Buyer Brief carousel funnel depends on this endpoint). Same signature previously on 2026-07-30 and 2026-08-18.
**Root cause:** The handler runs an unbounded `find().toArray()` across all three FEATURED_SUBURBS collections (robina, varsity_lakes, burleigh_waters). At 21:58:59 AEST pipeline Step 12 (Enrich Property Timeline) began writing to those exact collections. On Cosmos serverless (~5000 RU/s burst) the heavy read landed on the write burst → 429/16500 TooManyRequests. The read had no retry, so a single transient throttle surfaced as an uncaught 500. db.mjs already exports `withCosmosRetry()` (backs off RetryAfterMs on 16500/TooManyRequests/429) but this handler never used it.
**Fix:** Imported `withCosmosRetry` in decision-feed-v3.mjs and wrapped each per-suburb query in it. Transient throttles now back off and retry instead of 500ing. No happy-path behaviour change (verified `node --check`).
**Files:** Feilds_Website/01_Website/netlify/functions/decision-feed-v3.mjs (repo: Website_Version_Feb_2026 @ 048e4886)
**Recurrence:** First for this problem-id; third occurrence of the underlying throttle-500 symptom (2026-07-30, 2026-08-18 were the same collision, never root-caused/fixed until now).

## [SEO-QUERY-DIMENSION-BLINDNESS] Every SEO cycle has been reading 9% of Search Console and calling it the channel — 07:00 AEST
**Symptom:** `system_monitor.seo_landing_performance` reported the site taking 5,334 impressions / 78 clicks over 90 days. Search Console's own site total for the identical window is 56,341 impressions / 1,168 clicks. The SEO domain's standing brief records "the channel is SMALL: 3,494 impressions / 52 clicks... Nothing reaches significance" as a governing fact, and every priority set between 2026-07 and 2026-08 was ranked on that number.
**Root cause:** `gsc_pull()` requested `dimensions: ["page","query","device"]` and nothing else. Google does not bucket anonymized (low-volume / potentially identifying) queries — it **drops the rows entirely**. So the query-dimension pull is a ~9.5% sample of impressions and ~6.7% of clicks *by construction*, and every consumer summed those rows to get per-page and site totals. The failure is invisible from inside the data: the numbers are internally consistent, just an order of magnitude small. Two published conclusions were wrong as a direct result — `/off-market/` was written up on 2026-08-23 as "the worst impressions-to-clicks ratio of any large template on the site" at 0.72% CTR when it is in fact the site's engine (34,588 impr, 801 clicks, **2.32% CTR**, the best of any large template), and REC-seo-004 told Will that `/for-sale-v3` sat at position 41.3 "and ranks for none of it" when it is at **position 13.0 with 649 impressions**.
**Fix:** `gsc_pull()` now issues three pulls and tags every row with a `dims` discriminator — `page` (authoritative per-page clicks/impressions/position), `page,query,device` (query attribution only, never to be summed), `__site_totals__` (one row, exact site totals from the `date` dimension, immune to both). The collector logs all three totals and the coverage ratio on every run so the gap can never again be invisible in the log. Bing rows tagged likewise. Added a Rule 7b zero-row assertion (the site has continuous organic traffic, so zero rows is a broken pull, not an empty result) and batched the inserts at 500 — the page dimension alone is 12,480 rows at 90 days and a single `insert_many` that size exhausts Cosmos RU. Updated all four consumers to filter on `dims` so they neither double-count the new rows nor keep reading the sample: `seo_signal.py`, `articles_signal.py`, `article_performance.py`, `seo_pilot_status.py`. `seo_signal.py` also gained a per-template rollup, which is what actually shows the shape of the channel now that 12,480 URLs rank.
**Files:** `scripts/brain2/seo_landing_performance.py`, `16_General_Reinforcement_Learning/seo_signal.py`, `16_General_Reinforcement_Learning/articles_signal.py`, `scripts/article_performance.py`, `scripts/brain2/seo_pilot_status.py`
**Recurrence:** First. Same class as `[FIELD-NAME-GUESS-FALSE-ABSENCE]` (2026-08-09) and CLAUDE.md Rule 8 — a query returned a small number and the number was believed instead of the query being checked. Rule 8 covers absence; this is the same error against *magnitude*, where there is no zero to prompt suspicion.

## [SEO-SIGNAL-AFFINITY-KEY-MISMATCH] seo_signal's CONVERTING arm had never fired once — 07:00 AEST
**Symptom:** `seo_signal.py` printed `CONVERTING (0):` on every run since the sensor was built. The domain mandate names CONVERTING as the arm worth most ("a page ranking for a query near the 26x address-search milestone is worth most"), and it was permanently empty.
**Root cause:** `organic_landing_affinity` is keyed by **path** (`/property/x`); Search Console pages are **absolute URLs** (`https://fieldsestate.com.au/property/x`). The join did `affinity.get(pg)` with `pg` the absolute URL against a dict keyed by path, so it never matched a single document. `converters` was therefore 0 on every row and the flag could not fire. Nothing errored — a dict miss returns `{}`, which reads as "this page has no conversions", which is a perfectly plausible answer for most pages.
**Fix:** normalised both sides through a `_path_of()` helper that strips the origin, query string and fragment and collapses the trailing slash. CONVERTING now returns 13 pages, including `/off-market/3-avocet-avenue-burleigh-waters` (pos 6.2, CTR 5.9%, 1 converter) and `/market-intelligence/Robina/overview` (317 impr, 1 converter).
**Files:** `16_General_Reinforcement_Learning/seo_signal.py`
**Recurrence:** First.

## [EDITORIAL-ABBREVIATED-CURRENCY-IN-TITLES] Rule 5 number format breached in 60 live SERP fields — 07:00 AEST
**Symptom:** 46 properties' `ai_analysis.meta_title` / `meta_description` used `$1.4M`, `$965K`, `$1.849M` where CLAUDE.md Rule 5 mandates `$1,400,000`. Because REC-seo-001 shipped the editorial hook into the live `<title>`, these were the SERP copy itself — e.g. `<title>10 Belmore Close — $965K to $1.849M. What Bridges It? | Fields</title>`.
**Root cause:** generated at editorial time; the generator has no number-format assertion. Flagged by the `articles` domain on 2026-08-23 and handed to SEO rather than fixed unilaterally, because titles/metas are the SEO brief's authority.
**Fix:** deterministic, meaning-preserving expansion of the abbreviated figures only — no rewording. `editorial_compliance_check.py` `abbreviated_currency` 32 -> 0. Verified live on two pages. Every before/after saved to `logs/backups/meta_abbrev_currency_20260830.json`. Two titles land at 66 chars, accepted: a compliant title Google may truncate beats a live rule breach, and the address stays front-loaded. The **durable** fix is a generation-time assertion and was sent to `articles` as a directive — otherwise the next generation run re-emits the abbreviated form.
**Files:** MongoDB `Gold_Coast.{robina,varsity_lakes,burleigh_waters}` `ai_analysis.meta_title` / `meta_description` (60 fields, 46 docs); no code changed.
**Recurrence:** First.

## [EDITORIAL-FALSE-CONFIDENCE-RANGE] Live copy calls the flat ±12% band a confidence interval — 07:00 AEST
**Symptom:** live on `/for-sale-v3`: "the like-for-like evidence reconciles to about $1,183,000 today, **within a high-confidence range** of roughly $1,041,000 to $1,325,000". That band is exactly the estimate ±12%.
**Root cause:** the ±12% range is a flat percentage the code applies to the point estimate, not a statistic derived from a distribution. CLAUDE.md's Valuation System section states it contains the actual sale price **61%** of the time (67% inside the design envelope), that it is "NOT a statistical CI, and the code says so", and that we must "never call it a '90% confidence range'" — a true 90% band needs ±26.4%. The editorial generator has no assertion against the phrasing. Scale: **65 of 359** properties carrying `ai_analysis` match `(high[- ])?confidence (range|interval|band)|\d\d% confidence` — 31 active `for_sale`, 22 sold, 8 withdrawn, 4 under contract; phrasings are `confidence range` 42, `confidence interval` 12, `high-confidence range` 10, and one literal `90% confidence`.
**Fix:** added a `false_confidence_range` check to `scripts/editorial_compliance_check.py` (catches 2 on live *published* editorial today). Deliberately did **not** mass-rewrite 65 documents of generated prose — per the `articles` domain's own 2026-08-23 reasoning, the durable fix is a generation-time assertion, not a regex over live copy. Sent to `articles` as an evidenced directive with the exact pattern, counts and the CLAUDE.md citation.
**Files:** `scripts/editorial_compliance_check.py`
**Recurrence:** First. Related to `[SEO-STALE-META-AND-FORECAST-PROMISE]` (2026-08-23) — generated copy asserting a certainty the underlying method does not support.

## [OWNER-MARKET-AD-SUBURB-OVERRIDES-TYPED-ADDRESS] Per-suburb ad forced the wrong suburb into the address resolver — 09:__ AEST
**Symptom:** 3 of the 4 paid Owner-Market address leads (14d, $105) came back `om_kind: unresolved`, `om_link: None`. Each was texted "I'm finalising your link and will text it through shortly"; only one ever received a follow-up (manually, from Will). Two people have now been waiting 2–3 days for a link that no process was going to produce.
**Root cause:** Two independent faults. (1) `owner_market_sms.py` looked up `forms[form_id] -> (suburb_key, suburb_name)` and passed the **ad's** suburb to the resolver regardless of what the lead actually typed. The Burleigh Waters ad is seen by Burleigh Heads owners, so `2/9 cedar court Burleigh Heads` was searched inside `burleigh_waters` and could never match — it sits in `Gold_Coast.address_search_index` verbatim under `suburb_key: burleigh_heads`. (2) Token-strict street matching: `6 Campelle st varsity` failed against `6 CAMPELLES AVENUE VARSITY LAKES` on the missing plural and the street-type abbreviation, though the property is in the index exactly. This is the same fragility recorded in memory `address_search_matcher_and_abandon_capture`.
**Fix:** Added `resolve_address_locality()` — parses the typed string into a street number (unit-aware, `2/9` -> `9`) plus candidate name tokens with singular/plural variants, queries `address_search_index` on the structured `street_no`/`street_name` fields, and scores candidates so the suburb the person **wrote** outranks the suburb the **ad** was for (the form's suburb survives only as the weakest tiebreak, and as the fallback when nothing matches — i.e. previous behaviour). Verified against all four real leads: `2/9 cedar court Burleigh Heads` and `6 Campelle st varsity` now resolve to their canonical index rows; `21 Gardendale crescent` and the built-in Robina test address are unchanged (no regression); `18 glauca street burleigh heads` still does not resolve — Glauca Street runs 1–15 in the index, number 18 is genuinely absent, so that one is a real data/typo gap and is reported as such rather than papered over.
**Also fixed (Rule 7b, same file):** the heartbeat recorded `success` on a batch where every address failed, because the "finalising your link" holding SMS still increments `sent`. Added an all-unresolved raise, and put `awaiting_manual_link` into `beat.metrics` — `om_needs_manual_link` was written by exactly one file and **read by none**, which is why 3 promised-and-forgotten leads were invisible to the health board.
**Not done deliberately:** the two now-resolvable links were **not** sent. Contacting a real person is never autonomous; raised as REC-ads-006 for Will. One of the two (6 Campelles Ave) is `listing_status: for_sale` with another agency, so POA constraints apply to anything beyond fulfilling the analysis she asked for.
**Files:** `03_Facebook/Owner_Market_Carousel/owner_market_sms.py`
**Recurrence:** First for this ID. Same class as `[TELEGRAM-GENERIC-NOTIFY-NO-CONTACT]` and `[LEAD-BUYER-VS-SELLER-MISLABEL]` — lead handling driven by which ad/form it came from rather than by what the lead actually said.

## [EDITORIAL-FALSE-CONFIDENCE-BAND-AT-SOURCE] Prompt taught the model to call the flat ±12% band a confidence range — 11:20 AEST
**Symptom:** 20 published property pages described the valuation band as a "high-confidence range", "confidence range", "confidence interval" or — on 10 Glen Eagles Drive — a "90% confidence range", the exact phrase CLAUDE.md names as forbidden. The band is a flat ±12% of the reconciled estimate: arithmetic, not a distribution, containing the actual sale price 61% of the time (67% inside the design envelope). A true 90% band needs ±26.4%. These were false statements of fact on live public copy, not style wobbles.
**Root cause:** The prompt taught it, in two places. (1) Rule 9's own exemplar sentence read "...eight verified comparables, **medium confidence**, compiled from public sale records" — modelling a confidence tier inside the very sentence that states the range. (2) PART 7B "CONFIDENCE TIERS" listed "Fields valuation range" under **HIGH CONFIDENCE**, so the model read "the range is high confidence" and published exactly that. The tiers were meant as guidance about which inputs to build on; nothing said they were not labels to publish.
**Fix:** Three layers. (a) Removed "medium confidence" from rule 9's exemplar. (b) Added rule **9a** banning confidence/interval/band/percentage language on the band, with the measured 61%/±26.4% figures and WRONG/CORRECT pairs drawn from the real live breaches. (c) Annotated PART 7B that the tiers are writer guidance, never published labels. Then, because prompt text alone has already failed to hold the number-format rule, added `_strip_false_confidence()` — a deterministic phrase-level rewrite applied in `_normalize_money_formats()` at save time, exactly like `_expand_abbreviated_money()`. It rewrites how the band is *described* and never touches figures. Verified against all 12 real live phrasings.
**Backfill:** Applied the same scrub to live published editorial — 19 docs, 39 strings. Re-scan: **0 remaining**; `editorial_compliance_check.py` no longer fires `false_confidence_range` (4 `stale_price` findings remain, unrelated and untouched). Full before/after backup: `logs/backups/false_confidence_published_20260830.json`.
**Not done deliberately:** did NOT mass-rewrite the 45 non-published (rejected/archived/suppressed/draft) copies — they are not public and the generator fix covers regeneration. Also did NOT rewrite the separate Rule 9 breach visible in the same passages (valuations stated to the dollar, e.g. "$1,726,668"); a regex cannot fix that without changing meaning, so it is reported below rather than papered over.
**Files:** `config/property_editorial_prompt.md`, `scripts/backend_enrichment/generate_property_ai_analysis.py`
**Recurrence:** 2nd — `[EDITORIAL-FALSE-CONFIDENCE-RANGE]` (2026-08-30, seo) added the *detector*; this adds the *generation-time fix* the detector's author correctly said was the durable answer. Same shape as `[EDITORIAL-NUMBER-FORMAT]` (2026-07-24): prompt rule ignored until a deterministic save-time rewrite backed it.

## [META-TITLE-STREET-NUMBER-DROPPED] Headline specialist dropped the street number from meta_title — 11:35 AEST
**Symptom:** 29 of 237 generated `meta_title`s did not lead with the property's street number; 6 were published. "114 Florabella Drive" shipped as "Florabella Dr Sales Hit $1,170,000", "2 Eagle Avenue" as "Eagle Ave — ...", and "4/44 Frascott Avenue" as "44 Frascott Ave" — which is a different property. Exact-address queries are the strongest organic position the site holds (/articles/ avg position 9.4), and the street number is the most discriminating token in that query.
**Root cause:** The Sabri headline specialist optimises `meta_title` for click tension against a ~60-char budget and treats the street number as droppable. Nothing asserted otherwise at generation.
**Fix:** Added `_ensure_meta_title_street_number(meta_title, address)`, called at both `meta_title` assignment sites (draft-1 and the Sabri re-run). It repairs only the safe case — the title opens with the street NAME, so the number is simply missing — and otherwise leaves the title alone with a warning, so it can never mangle a title it does not understand.
**Backfill:** 3 of the 6 published titles repaired mechanically (114 Florabella Dr, 2 Eagle Ave — the case seo raised — and 126 Acanthus Ave). Backup: `logs/backups/meta_title_street_number_20260830.json`. The other 3 are hook-led titles that never contained the street name; a mechanical prepend would produce nonsense, so they are left for regeneration.
**Files:** `scripts/backend_enrichment/generate_property_ai_analysis.py`
**Recurrence:** First.

## [ADDRESS-SEARCH-STREET-TYPE-ZERO] Contracted street types and number ranges returned zero results — 11:15 AEST
**Symptom:** Visitors typing their own address on `/analyse-your-home` got no result and kept retyping. The onsite friction sensor flagged 3 HIGH retry loops in 7d, the worst being 41 searches converging on "6/2-18 Beachcomber Ct, Burleigh Waters QLD" with 0 submits. That exact string returned `[]` from the live API; "6/2 Beachcomber Court" returned the property immediately.
**Root cause:** Two independent gaps in the same all-tokens-must-match matcher.
1. **Street-number ranges.** Complexes are addressed by range on Domain/Google ("2-18 Beachcomber Ct") but 86,213 of 86,223 index rows store the lower bound alone ("6/2 BEACHCOMBER COURT"). The tokenizer split "2-18" into two required tokens, so "18" could never match and the query zeroed.
2. **Contracted street types.** The index stores street types in full (10,618 COURT / 0 CT; 4,579 PARADE / 0 PDE). An abbreviation that is a *prefix* of the full word survives the char-wise `\s*` regex for free (AVE→AVENUE, DR→DRIVE, ST→STREET), but a *contraction* cannot: `C\s*T` never matches COURT. Measured on 400 random index addresses, the same address typed with an abbreviated street type matched **246/388 before, 388/388 after** — i.e. 37% of abbreviated-street-type queries were silently returning zero.
**Fix:** In `tokenizeAddressQuery`, collapse `(\d+)-(\d+)` to its lower bound (safe both ways — the 10 rows that do store the range still contain the lower number). Added `STREET_TYPE_ALIASES` + `tokenPattern()`, which expands a contracted street type to an **alternation** (`(?:CT|COURT)`) rather than substituting, so "ST" still matches the 534 rows where it means Saint. Widening only: it can add matches, never remove one. `scoreCandidate` credits the alias as a whole-word hit so the expanded match also ranks. Mirrored in the client-side progressive-narrowing filter, which otherwise re-filters out exactly what the server just matched.
**Files:** `01_Website/netlify/functions/address-search.mjs`, `01_Website/src/services/addressSearchService.ts` (one commit, 057b9680, one Netlify build)
**Verification:** `npm run build` passed; 400-address regression (400/400 full addresses still match, Glen Eagles case intact); live after deploy — "6/2-18 Beachcomber Ct, Burleigh Waters QLD" now returns "6/2 Beachcomber Court, Burleigh Waters" as the top result; "12 Sunshine Pde", "5 Smith Rd Bonogin" now return results; "120 Gleneagles drive, Robina", "5 fulham place robina", "14 Ray" unchanged. `/analyse-your-home` screenshotted clean (0 console errors).
**Why it matters:** `searched_address` is the 45.7x-predictive milestone in the reward ledger and `submitted_address` is the reward itself — this failure sat on the single highest-value action on the site.
**Recurrence:** 3rd — same matcher class as [ADDRESS-SEARCH-GLENEAGLES] (2026-07-30, one-word vs two-word street) and the 2026-08-28 filler-token fix ("Queensland"/"Australia"). Each was found by a real visitor failing, not by a test. The matcher has no fixture suite; that is the actual recurring cause.

## [VALUATION-BAND-STALE-AT-ROOT] CLAUDE.md described a valuation band retired 3 weeks earlier — 16:40 AEST
**Symptom:** `config/property_editorial_prompt.md` rule 9a, shipped this morning by the articles domain to STOP false confidence language on live property copy, justified itself with four factual claims that were all false: the band is "a flat ±12%", "arithmetic, not statistics", "contains the actual sale price 61% of the time", "a genuine 90% band would need ±26.4%". The seo domain's `false_confidence_range` detector, shipped the same morning, cited the same figures.
**Root cause:** `CLAUDE.md:455` was never updated when the band changed on 2026-08-07. Production has emitted a per-suburb **empirical 80% band** since 2026-08-08 (`valuation_data.confidence.range_basis.kind = "empirical_80_band"`, `precompute_valuations.py:2141-2185`). Two domains, working carefully and independently on the same day, both sourced their justification from the root doc and both shipped a false one — they had no reason to distrust it. The adjacent accuracy bullet was stale the same way (MAE 10.5%/8.2%/59%, Robina n=278) against the governing 8.05%/6.44%/69% (n=581).
**Fix:** Corrected at the root rather than at the two copies. `CLAUDE.md:455` now states the per-suburb empirical 80% band (VL ±11.2%, Robina ±12.2%, BW ±14.0%, n=581), names it a measured coverage band and NOT a confidence interval, gives the emitting code path, and explicitly marks the ±12%/61%/±26.4% figures as retired and forbidden to propagate. `CLAUDE.md:457` updated to the governing accuracy figures with their source file. Prompt rule 9a and the PART 7B annotation rewritten to the same facts. **The prohibition itself was correct and is unchanged** — only its stated justification was wrong; an empirical 80% band is still not a confidence interval.
**Files:** `CLAUDE.md`, `config/property_editorial_prompt.md`
**Verification:** `grep -n "26.4\|61% of the time\|flat ±12" ` over both files returns only the deliberate "these are retired" warning line. Figures cross-checked against `16_Valuation/accuracy/2026-08-08-figures.md` (MAE 8.05%, median 6.44%, within-10% 69%, n=581) and against 100/101 live valued for-sale listings carrying `range_basis.measured_on = "2026-08-08"`.
**Why it matters:** internal config only — no reader-facing surface carried the stale figures (`ReliabilitySection.tsx:17` correctly says "the OLD flat ±12%"). But 61% and ±26.4% are accuracy claims sitting one copy-paste from public copy, and the prompt was teaching the generator that the most defensible number this business owns is meaningless arithmetic. The real band is the stronger claim.
**Recurrence:** First for this line. Found by the valuation RL domain (REC-valuation-004); fixed by Samantha rather than spending a weekly-brief slot on a factual correction.
