# Fix History — 2026-08-13

## [RENTAL-AS-SALE-PARSERS] Closed the deferred half of the rental-as-sale class — 7 parsers hardened — 07:37 AEST
**Symptom:** None visible. This is the item `[RENTAL-AS-SALE-DEEP]` (2026-08-10) deliberately left
undone: the price parsers that accept a weekly rent as a sale price. Latent, not active.

**Root cause:** Every price parser in the estate was written independently. There was no shared price
helper and **no rent guard anywhere in the codebase** (`grep` over `shared/` and
`scripts/property_reports/` returns only occupancy-classifier rent *estimates*). Each parser strips
`$`, `,` and text and converts what remains, so `"$750 per week"` reduces to `750`.

**What the earlier record got wrong.** I re-tested all 13 parsers by executing them rather than
reading them, and the 2026-08-10 entry understates the exposure in two ways:
1. It says **"two accept the literal string `$750 per week`"**. **Five do** — `slot_resolver`,
   `cohort_premiums`, `appraisal_data_pull`, `comparable_feed`, `refresh_property_reports`.
2. Two of its named parsers were **tested at the wrong function**. In `comparable_feed` and
   `refresh_property_reports`, `parse_price` is string-only *by design*; the real entry point is
   `coerce_price`, which is permissive on **both** the numeric and the string form. So the exposure
   reaches the comparable feed and the nightly report refresher, not just the valuation engine.
3. `assemble.num` was **miscounted as a parser**. It is a *formatter* — it returns the string
   `"950,000"` — and does not belong in this class at all.

Net: 7 real entry points, not 6.

**The floor is measured, not guessed.** I did not inherit the price floor from the previous entry.
Across every Sale-category timeline event in the three target suburbs:

| band | n | median year |
|---|---|---|
| < $1,000 | **0** | — |
| $1,000–$5,000 | **0** | — |
| $5,000–$20,000 | 56 | 1978 |
| $20,000–$100,000 | 4,246 | 1990 |

There is not one genuine sale below $5,000 in our history, so the $1,000 floor carries 5x headroom
and cannot touch the 56 real 1970s sales — the trap the previous entry correctly flagged. Note the
floor is **not** the primary defence and cannot be: luxury Gold Coast weekly rents reach $3k–$5k and
would clear any floor set low enough to preserve 1970s sales. The **string markers** do the
discriminating work; the floor only catches bare numerics with no plausible sale reading.

**Fix:** new `shared/price.py` — `looks_like_rent()` (string markers: `per week`, `pw`, `p/w`,
`p.w.`, `/wk`, `weekly`, `fortnightly`, `per month`) and `sale_price_or_none(raw, parsed)`, which
takes the **untouched** raw value so the markers are still visible (every parser strips them before
converting). Wired into all 7 entry points.

**Verified:**
- **Guard works:** all 7 parsers now return `None` for all 8 rent forms tested
  (`750`, `"750"`, `"$750 per week"`, `"$750 pw"`, `"$750 p/w"`, `"$1,200 PER WEEK"`, `"$750/wk"`,
  `"$850 weekly"`). Previously 7/7 accepted at least one. Sale values unchanged across
  `950000`, `950000.0`, `"$950,000"`, `"$1,250,000"`, `"$1.4m"`, `8500`, `"$8,500"`.
- **Provably a no-op on current data** (the point of doing it this way): the guard rejects **0 of
  2,859** real production price values (`sold.sale_price` 1,465 · `sold.price` 358 ·
  `enriched_data.transactions.price` 825 · `for_sale.price` 211) and **0 of 8,049** price-ish strings
  across 105 `system_monitor.property_reports` documents. No published valuation can change.
- All 8 files compile; all 7 modules import cleanly.

**Also confirmed while here (the data half of the class is closed):** the `--deep` backfill's result
holds and is not re-accumulating. Re-checked **all 90 suburb collections** (the 2026-08-10 run
covered only the 3 target suburbs): 0 rental entries, 0 affected documents. Unfiltered re-check of
the 3 target suburbs across 17,039 enriched docs: 0. The source writer is genuinely fixed —
`enrich_cadastral.py` `extract_transactions()` requires `category == 'Sale'`.

**Two pre-existing quirks found but NOT fixed** (unrelated to rentals, no evidence of live harm —
recorded so they are not lost):
- `slot_resolver._parse_price` / `cohort_premiums._parse_price` strip all non-digits, so a range
  `"$8,500 - $9,500"` becomes **85009500**. Any range string is silently mangled, not rejected.
- `slot_resolver`, `cohort_premiums` and `appraisal_data_pull` return `None` for `"$1.4m"` — the
  abbreviated-millions form the other four handle.

**⚠ Not pushed — needs a decision (see session notes):** the house valuation engine lives at
`/home/fields/Feilds_Website/07_Valuation_Comps/precompute_valuations.py`, which is **in no local git
repo**. GitHub's copy at `Fields_Orchestrator/07_Valuation_Comps/precompute_valuations.py` is an
orphan: **254 lines divergent** (4,356 vs 4,480 lines), with content on GitHub that no longer exists
locally — so it is not merely stale, it has diverged. The live local file is authoritative (it is
what the pipeline runs and what CLAUDE.md names), but I did not overwrite a 254-line diff I did not
author. The guard IS applied to the live file; only the GitHub sync is outstanding.

**Files:** `shared/price.py` (new); `scripts/property_reports/{slot_resolver,cohort_premiums,comparable_feed}.py`;
`scripts/{generate_appraisal_report,appraisal_data_pull,refresh_property_reports}.py`;
`/home/fields/Feilds_Website/07_Valuation_Comps/precompute_valuations.py` (local only — see above)
**Recurrence:** 3rd of the rental-as-sale class (2026-07-22 visible symptom → 2026-08-10 data depth →
this, the parsers). Each prior fix was scoped to what was *actively* broken; this one closes the
class rather than the instance.

## [DR-OUT-OF-TREE-BLINDSPOT] The valuation engine ran from a directory no backup check scanned — 08:05 AEST
**Symptom:** GitHub's copy of the house valuation engine was **254 lines divergent** from the file
that actually runs (4,356 vs 4,480 lines) — and nothing anywhere reported it.

**What the divergence actually was:** the 2026-08-12 `[VALUATION-UNKNOWN-ASYMMETRY]` fix (unknown
attributes silently marking subjects down, worth up to $120k on water views). It edited the live file
and never reached GitHub. Confirmed by diffing the hunks against that entry — the `_LEGACY_UNKNOWN`
guards are exactly it. **Shipped behind `VALUATION_UNKNOWN_HARDENING=1`, default OFF**, so syncing it
changes no production behaviour; that entry verified the default path unchanged (MAE 9.1% both sides).

**Root cause — three failures stacked, none of which alone would have done it:**
1. **The live engine lives outside every repo `check_unpushed_code.py` scans.** `REPOS` covers
   `/home/fields/Fields_Orchestrator` and `/home/fields/Property_Data_Scraping`.
   `/home/fields/Feilds_Website/` — where the engine actually runs, per CLAUDE.md — is scanned by
   **nothing**, and has no local `.git` at all.
2. **Someone worked around (1) by hand-copying the file into the orchestrator repo** at a mirror path
   and pushing it. A copy is a thing that can drift, and it did.
3. **That mirror was later deleted from the working tree, and the deletion was silent.**
   `code_files()` filters with `os.path.isfile()`, so a tracked-but-missing file is skipped with no
   output. GitHub's copy then looked authoritative while the file that ran lived somewhere else.
   Rule 8 applied to backups: an empty result asserted nothing.

**Fix (`scripts/check_unpushed_code.py`):**
- **`EXTRA_FILES`** — a first-class concept of an out-of-tree live file that claims a path inside a
  repo. It points at the **LIVE path, not a copy**, which is the whole point: there is no second file
  to drift. `classify_extra()` compares by content hash; `push_repo()` takes a `localmap` so the
  bytes come from the live file while the commit lands at the repo path.
- **`ghost_files()`** — tracked paths with no file on disk are now printed instead of silently
  dropped. Reported, not alerted (most ghosts are ordinary uncommitted deletions), but a stale mirror
  can no longer hide in the gap.
- **`secret_hits()` refactored to take an absolute path** rather than `(repo, rel)`, so out-of-tree
  files go through the same credential gate. A bypass route must not also bypass the secret scan.

**Verified — the guard fires, rather than just happening to read clean once:**
| case | result |
|---|---|
| unchanged live file | IN SYNC |
| drifted copy | **MOD** (caught) |
| live file vanished | **MISSING** (caught — not silently skipped) |
| path absent on GitHub | **NEW** |
| secret in an out-of-tree file | **BLOCKS** |

Then synced: commit `025d5dc8`, byte-verified, and the checker now reports the engine in sync.

**Follow-up (08:40, actioned on Will's go-ahead) — one live file now has exactly one backup path.**
Deleted the superseded root-level mirror `precompute_valuations.py` from the DR repo
(commit `029b0fbc`). Checked before deleting rather than trusting the description: same lineage,
older snapshot (3,879 vs 4,480 lines), and the canonical is a **strict superset** — all 58 of the
old copy's functions exist in the canonical's 63, so nothing unique was lost. Nothing in the estate
references the root path. Recoverable from history.

**Two flaws found in my OWN fix while verifying the deletion** — both the same class of error this
entry is about, caught only because I re-read the output instead of trusting one clean run:
1. **`ghost_files()` read the LOCAL index** (`git ls-files -d`). That is the exact source this
   file's own docstring calls untrustworthy — nothing commits locally and `git push` hangs, so the
   index has drifted. Now reads `origin/main`, which is what actually holds the backup. The switch
   immediately surfaced one more real ghost the local index had missed (`scripts/email.py`).
2. **A path covered by `EXTRA_FILES` was listed as a ghost too**, so the valuation engine printed
   "✓ in sync" and "GONE" on the same run — a contradiction no action could ever clear. That is how
   a report stops being read (the same complaint `is_external_link()` documents). Covered paths are
   now excluded.

**How the second mirror was found:** the new ghost report surfaced it on its first run — a
**second, older mirror** of the same engine at the repo ROOT, `precompute_valuations.py` (171,651B,
last touched 2026-08-05, vs 198,984B at the `07_Valuation_Comps/` path). Two backup copies of one
live file, both stale, is how the "which copy is real?" trap gets rebuilt. Deleted — see the
follow-up above. The check paid for itself on the run that introduced it.

**Final state, verified end-to-end:**
| case | result |
|---|---|
| unchanged live file | IN SYNC |
| drifted copy | MOD |
| live file vanished | MISSING |
| path absent on GitHub | NEW |
| secret in an out-of-tree file | BLOCKS |
| EXTRA_FILES path suppressed from ghosts | YES |
| root duplicate gone | YES |

**Files:** `scripts/check_unpushed_code.py`,
`/home/fields/Feilds_Website/07_Valuation_Comps/precompute_valuations.py` (synced),
GitHub `Fields_Orchestrator/precompute_valuations.py` (deleted, `029b0fbc`)
**Recurrence:** 2nd of the git-DR-gap class (`[GIT-DR-GAP]` 2026-07-15 created this checker; this is
the hole it was never given the concepts to see).

## [V4-DEPTH-MISLABELLED] `deepest_section` measured where readers LEFT, not how far they got — 10:35 AEST
**Symptom:** Analysing the first 3 days of V4 reading telemetry (13 exit events on detached
houses), the three deepest reads on the site — 100% scroll, 94-193s engaged, 9-11 sections —
all reported `deepest_section: "hero"`, `deepest_index: 0`. The three shallowest sessions
reported the same thing. The field could not distinguish a complete read from a bounce.
**Root cause:** `sendExit()` assigned `deepest_section: s.lastSection`. `lastSection` is
reassigned on EVERY section change (`settle()`), so it tracks the current section and moves
backwards. Readers who reached the bottom and scrolled back up to re-read the hero — which is
what all three deep readers did — recorded `hero`. Depth is a high-water mark and was never
tracked as one. The same wrong value was sent to the `offmarket-intent-alert` Telegram beacon,
so Will's deep-read alerts have been reporting the exit point as the depth reached.
**Fix:** Track `deepestSection`/`deepestIndex` as an explicit max in `settle()`. Exit event now
carries `deepest_*` (furthest reached), `exit_*` (where they left — the drop-off answer, which
is what the old field actually held) and `scrolled_back` (deep then back up; a re-read signal
worth having on its own). Alert payload carries both.
**Files:** `01_Website/src/pages/OffMarketPage/v4/useReportAnalytics.ts`
**Recurrence:** First. Related: [WHATS-CHANGED-SHIPPED] 2026-08-10 shipped this telemetry.

## [WHATS-CHANGED-WATCH-UNPUBLISHED] Resolving a watch published nothing; RBA decision went stale on ~21,800 pages — 10:50 AEST
**Symptom:** Every V4 page still read "**Coming up — 11 August 2026 (tomorrow)** — The Reserve
Bank hands down its next cash rate decision" on 13 August, two days after the decision. It is
the second-most-reached section in the reading data (8/13 house sessions, 11.3% of reading time).
**Root cause:** Three separate faults, each masking the next.
  1. The generated `whatsChangedData.ts` had not been re-emitted since 2026-08-10. The
     expired-watch guard in `watch_points()` drops a stale watch at GENERATION time, not at
     render time, so an un-regenerated file keeps serving the stale line indefinitely.
  2. `events.yaml` documents that writing `outcome:` "converts it into a normal dated event and
     it moves into the timeline". That is not implemented — `watch_points()` simply `continue`s
     on a resolved watch and nothing else reads it. Resolving the watch alone would have removed
     the stale line and published nothing, silently breaking the promise the watch made
     ("we will re-read the numbers against it and update this page").
  3. Adding the outcome as a second cash-rate event ALSO published nothing. Both it and the
     existing 1 May `cash_rate_rises` pair with `days_on_market`, so `_dedupe_readings` saw the
     same "34 days" figure twice and kept the earlier bullet. Every local metric is already
     spoken for, so a second cash-rate event can never carry a reading of its own.
  Then, once re-anchored to 11 August, it was dropped a THIRD time — by the synthetic
  `**Now** — homes are taking 34 days to sell` suburb point, dated `now - 30d`, a value that
  exists only for sorting. "Earlier wins" is sound between two macro events; against a point
  with no real date it ranked a bare restatement above a dated fact carrying the same figure.
**Fix:** Verified the actual outcome against RBA primary sources first (MR 2026-19: held at
4.35%; next decision 29 September; our Feb/Mar/May-to-4.35% record confirmed correct). Collapsed
to ONE cash-rate event re-anchored to 2026-08-11 stating the hold; resolved the `rba_2026_08_11`
watch and queued `rba_2026_09_29`; gave `_dedupe_readings` a kind-rank so a `macro` event
outranks a `suburb` restatement regardless of date. Documented in both files that resolving a
watch and publishing its result are two separate writes that must happen together.
**Files:** `15_Off-Market/Whats_Changed/events.yaml`,
`15_Off-Market/Whats_Changed/whats_changed.py`,
`01_Website/src/pages/OffMarketPage/v4/whatsChangedData.ts` (regenerated)
**Recurrence:** First for the publish path. The stale-timeline risk was anticipated in
[WHATS-CHANGED-SHIPPED] 2026-08-10 ("REGENERATE MONTHLY"); nothing enforces it — see note below.

## [APPRAISAL-SELF-SERVE-VARIANT] `--self-serve` appraisal variant for off-market report requests — 07:57 AEST
**Symptom:** Not a bug — new capability. The V4 off-market page is to offer the Property
Positioning Report as a self-serve download. The generator's two recommendation pages
(11 + 18) are gated on `recommendation.listing_price` + `target_sale_price`, which are
analyst judgement entered by hand in the ops dashboard. Only **6 of 135** pipeline records
have them, so a self-serve render would have shipped two "Analyst review required"
placeholder pages to the reader.
**Root cause:** Two distinct manual gates exist, not one, and they are data-presence checks
rather than pipeline stages. Gate 1 (`data_pull.py:559`, §03R derived range) already falls
through to the engine at `valuation_data.confidence.range` — the *same* field the V4 page
reads in `buildValuation()` — so it self-passes on any V4-eligible property and needed no
change. Gate 2 (`data_pull.py:861`, recommendation pages) cannot be engine-derived: a
listing price is a market judgement, and the code says so.
**Fix:** Added an additive `--self-serve` flag following the existing `--positioning`
precedent. It replaces pages 11 + 18 with copy stating why no listing price is published
(the judgement needs a physical inspection) plus a consultant CTA (Will Simpson, 0416 529
481). Deliberately does NOT synthesise a listing price from the engine range — that would
be advice, contrary to editorial Rule 5, on a page whose reader has had no inspection.
The engine-derived range on pages 9-10 is untouched, since the off-market page already
publishes that same range to that same reader.
Verified the default path is unmodified: rendered subject `690bd7da8b8f546592602972` with
the committed code and with the change, both HTML outputs md5 `916334c2f55e7f5e9b23dd...`
— byte-for-byte identical.
Four samples rendered across all three V4 suburbs (Chantilly/Cheltenham Robina, Fimiston
Burleigh Waters, Placid Varsity Lakes), 0 overflow on all. Pages 11 + 18 visually
inspected as PNG. One `tight` warning on Cheltenham is `05_right`, pre-existing and
data-dependent, unrelated to this change.
**Files:** `scripts/appraisal_template/render.py` (SECTION_RECOMMENDATION_TEMPLATE
self_serve branch + `render_section_recommendation_html(self_serve=)`),
`scripts/generate_appraisal_v4.py` (`render_appraisal(self_serve=)`, `--self-serve` CLI,
`report_variant == "self_serve"` pipeline hook)
**Recurrence:** First.
**Not yet built (feasibility findings, see session notes):** delivery is the open problem —
PDFs average **11.87 MB**, generation is queued through a single-threaded 30s-interval
FIFO poller (18-40s render), artefacts live on the VM filesystem only, and pre-generating
all 8,015 eligible pages would need ~95 GB against 27 GB free. Only **29.6%** of V4 slugs
(8,015 / 27,110) currently carry an engine range, so the page section must gate on
`valuation.method === "engine"`.

## [STRATA-LOT-CLAIM] Apartments claimed the whole complex's land parcel as "Largest lot currently for sale" — 08:19 AEST
**Symptom:** Live, public, on property cards and the property page. `186/325 Reedy Creek Road`
(Apartment) carried `"Largest lot currently for sale (82620m²)"` — it had inherited its
complex's 8.26-hectare parcel via `enriched_data.lot_size_sqm`. Two Varsity Lakes townhouses
*both* claimed to be the single "Largest lot currently for sale (1250m²)" on the same suburb
page. A Surfers Paradise unit claimed the largest lot at **11m²**. 10 strata documents across
9 suburbs were affected. Because the card's `rarity_label` derivation iterates
`['lot_size','floor_area',…]` and breaks on the first hit, the worst label was always the one
promoted to the badge.

**Root cause:** Two independent defects in `calculate_rarity_insights()`.
1. `lot_size_sqm` on a strata dwelling is the *scheme's* parcel, not the dwelling's land, but
   the lot branch (`:242`) ran for every property type. Nothing gated it.
2. `_rank_among()` compared with strict `>`, so two properties sharing the top value both
   scored rank 1 and both received the `only_one` label — the twin 1250m² collision. The
   kitchen and master-bedroom branches already used `>=`; the lot/floor-area path did not.

Compounding: the comparison pool was untyped, so **houses were also ranked against complex
parcels** and under-credited — `2a Pipers Point` read "2nd largest lot" because an apartment
block outranked it.

**Fix:** Added `has_own_lot(doc)`, used both to gate the subject and to filter the comparison
pool inside `extract_lot`. A dwelling qualifies only if neither `property_type` nor
`classified_property_type` says strata and the address carries no unit-number prefix; docs with
no type at all are treated as strata. The two type fields disagree often (17 active listings
typed `House` are classified attached), so either one is enough to disqualify. Changed
`_rank_among` to `>=`.

Verified by running the real function over all 211 active listings in the 3 target suburbs,
before vs after, no writes: **5 of 99 own-lot houses changed and every change was an upgrade or
a new insight — zero houses lost a claim** (`2a Pipers Point` 2nd → Largest 2090m²,
`24 Tropicana Circuit` 3rd → Largest 3594m²). All 3 false unit claims disappeared. Backfilled
the 10 stored documents to `rarity_insights: []` (rollback JSON saved); 5 of them sit outside
`TARGET_SUBURBS` and would never have self-healed on a nightly run. Confirmed clean on the live
`/api/v1/properties/for-sale` response.

**Files:** `scripts/backend_enrichment/calculate_property_insights.py`

**Still open (deliberately out of scope — verified, not patched):**
- **Peer cohort is untyped and is not "currently for sale".** `:516` passes `suburb_properties`,
  not `active_properties` — Robina ranks against 310 properties of which only 107 are for sale,
  so every "currently for sale" label is ~65% false. Affects houses. Units still get
  kitchen/bedroom/floor-area claims ranked against houses; only the *lot* claim is fixed here.
- **`suburb_statistics` has no unit rows** — 50 docs, 100% `property_type: "House"` (and
  `suburb_median_prices` 76/76 House). Step 14 must emit unit/townhouse rows before `:444` can
  become type-aware; until then units can only be suppressed, not compared correctly.
- **`enrich_cadastral.py:298`** — reported as a house-bias bug, but the line is *inert*: it
  queries title-case `'Robina'` against a snake_case collection, so it never matches and the
  untyped fallback at `:300` always wins. The script has never written a non-empty
  `property_insights` (all 515 stamped docs hold `{}`). 0 documents affected today, but **1,798
  attached dwellings are queued** to receive house-derived bedroom percentiles on its next run.
  Also found dead: `compute_capital_gain()` returns `None` always (same key-format bug plus it
  reads `quarterly_medians` where docs carry `data`) — a Rule 7b silent zero.
- **`step117_satellite_analysis.py`** — asks for lot shape / usable yard / neighbour setback at
  zoom 19 on strata parcels. 426 unit-like docs already carry values; five units in one Glenside
  Drive tower returned five *contradictory* answers from one satellite tile. `pin_confirmed`
  cannot catch this (it detects wrong-lot, not wrong-granularity) and is `None` on 94% of records.
  Valuation does not read those three fields but *does* read `pool_visible`/`water_proximity`,
  so a tower's common pool becomes a unit's premium.
- **Dead copy** `/home/fields/Feilds_Website/03_For_Sale_Coverage/calculate_property_insights.py`
  is byte-identical in the buggy function and is a re-introduction hazard.

**Recurrence:** First (prior `[VALUE-DRIVERS-ENRICHMENT]` touched rarity insights but not the
strata/lot defect).

## [V4-COVER-BLOCK-BURIED-HOOK] "What's changed" cost the page its hook; moved to Part 03 — 11:40 AEST
**Symptom:** Will asked whether the block should move so "So which one should you believe?" is
visible on open. Measured on the live page: the block ran 1,309-1,747px on mobile (1.6-2.1 full
screens) and pushed `#which` to 2.6-3.1 screens down — 12.9% (engine) / 16.9% (declined) of
scroll. Seven of the first thirteen detached-house sessions never passed 10%, so the question the
page exists to ask sat past the point most sessions stopped.
**Root cause:** Not a defect — a placement decision (2026-08-10) made before there was any reading
data, on sound reasoning (hero states the record, this states what happened since, both are the
reader's own history). What was missing was the cost side. ⚠ THE METRIC THAT HID IT: the block
ranked 2nd of 14 by SHARE OF TOTAL READING TIME, which reads as "engaging". Ranked by median
dwell per reader who actually reached it, it is 13th of 14 (5.0s vs dispersion 29.3s). Reach is
confounded with position — anything in the cover is reached by 100% of sessions — so total-time
rank measures placement, not quality. Only the per-reader figure is position-independent.
**Fix:** Block moved beside `timing` in Part 03 ("the market a move would happen in" — this is
that market with dates on it). Cover keeps a one-line anchor, because the block is the only part
of the page that differs between visits and a returning reader must still find it. Extracted
`selectTimeline()` so the anchor and the section cannot disagree about whether content exists —
same invariant as `partHasContent()` for dividers. `parts.ts` updated (also the SECTION_ORDER
`useReportAnalytics` derives `deepest_index` from, so document order had to follow).
**Measured after:** `#which` 12.9%->4.9% (engine mobile) and 16.9%->7.5% (declined mobile) — 3.1
screens down to 1.2. `#answer` 15.9%->7.9%. `#dispersion`, the highest-dwell section, 50.8%->42.5%.
**Two copy defects caught in verification, both on the one surface every visitor sees:**
  - The count included the watch point (a scheduled RBA meeting is not a change) — read
    "3 dated changes" on a page carrying two. Now counts past-dated points only and names the
    forward item separately, which also restored the "one date ahead" clause that had been gated
    on `lead` and so only ever appeared in the 7 days before a decision.
  - The scope list said "this home, {suburb}, and the wider market" unconditionally. Most homes
    have NO per-home point — 29 Andromeda Parade has none, both its points are national — so it
    asserted something had changed about the home when nothing had (Rule 5). Scope is now derived
    from the kinds actually present, and correctly drops the suburb layer for attached dwellings,
    where the house-only local clause is stripped. Verified across four pages covering all three
    shapes.
**Files:** `01_Website/src/pages/OffMarketPage/v4/{OffMarketV4.tsx,WhatsChangedSection.tsx,
WhatsChangedSection.module.css,parts.ts,HeroSection.tsx}`
**Recurrence:** First. Same session as [V4-DEPTH-MISLABELLED] — both came out of the first read of
V4 telemetry, and both were measurement-interpretation failures rather than code failures.

## [APPRAISAL-GENERIC-HERO-FALLBACK] Report could ship a photo of a different house — removed — 08:14 AEST
**Symptom:** Both image chains in the V4 appraisal generator ended by copying assets of
**13 Terrace Court** — a real, unrelated home — onto a cover printed with the subject's
address. `--strict-photos` flagged it but only on the analyst path, by convention, after
the PDF already existed. The planned self-serve download has no human in the loop, so a
fallback cover would have gone straight to an owner, or into the post.
Separately, when listing photos were missing the hero fell to `local_cadastral`, which is
frequently an **uncentred aerial**: 5 Chantilly Place rendered with five roofs in frame and
no way to tell which was number 5.
**Root cause:** The chain's last resort was a static placeholder rather than a subject-derived
image. The generator also never used `render_property_aerial.py` — the parcel-centred,
lot-outlined aerial that the V4 `/off-market` hero has used all along (`aerial_boundary_url`,
~473 rendered). Chantilly **already had one on disk**; the appraisal simply never looked.
**Fix:**
1. Deleted both generic fallbacks (`fallback_hero`, `fallback_sat`). Exhausting either chain
   now raises and **no report is produced** — a report showing someone else's home is worse
   than no report. Comment left at the site so it is not reintroduced.
2. Ported the boundary aerial into both chains via a new `boundary_aerial()` helper: prefers
   the cached blob, only pays for a Static Maps fetch when absent. Ranked above
   `local_cadastral` (resolves the ambiguity) and above raw `auto_static_maps` (which centres
   on the geocoded address at fixed zoom 19 with no outline).
3. Guarded against the image appearing **twice** — both chains can resolve to the boundary
   aerial, which put the identical picture on the cover and again in the satellite section.
   Satellite now skips it when the cover took it.
4. Partial `.html` is unlinked on the raise, so a failed render leaves no artifact for a
   wrapping script or the tracking server to serve.
5. Repointed `--strict-photos` at `local_cadastral`, the remaining weak-but-valid source.
   The placeholder case it used to guard can no longer occur.
**Verification:** Chantilly re-rendered `cover_hero=boundary_aerial` — lot outlined in Fields
sun, parcel-centred, unambiguous (inspected as PNG). All four samples 0 overflow, no
duplicate image. Fail path tested against 14B Pinehurst Place (no imagery of any kind):
exits 1, produces **0 artifacts**. Default non-self-serve path re-verified working.
⚠ Default output is intentionally NO LONGER byte-identical to the previous baseline — the
image chain change is global, by design. The `--self-serve` flag alone remains additive.
**Files:** `scripts/generate_appraisal_v4.py`
**Recurrence:** First for the fallback itself. Third in the family of "a real image that is
not this property reached a reader" — cf. `[FLOOR-PLAN-DEBRAND]` and the V4 prototype's
`domain_hero_image_url` 404 finding, both of which were caught by looking at the picture
rather than the code path.
**Open:** the Google Static Maps attribution ("Imagery ©2026 Airbus, Vexcel") is clipped
mid-word by the cover crop. Google's terms require it to stay legible — needs a layout fix
before these covers are used commercially.

## [APPRAISAL-AERIAL-ATTRIBUTION-CLIP] Google imagery attribution sliced on aerial covers — 09:41 AEST
**Symptom:** With the boundary aerial now serving as cover hero, Google's mandatory imagery
attribution rendered as "Imagery ©2026 Airbus, Vexce" — clipped mid-word. Static Maps terms
require it to remain legible, so these covers were not fit to print commercially.
**Root cause:** Two independent crops, and I fixed the first before finding the second.
(a) The shared aerial is 1.455:1; the cover band is 210mm x 68% of 297mm = 1.040:1, so
`background-size: cover` scaled by height and took **14.3% off each side** — and Google
stamps the attribution bottom-RIGHT. (b) Matching the aspect (640x615) fixed the horizontal
clip but the text was then sliced *horizontally through the glyphs* by the band's bottom edge.
Neither `background-position: right bottom` (verified present in the generated HTML, no
competing rule, no print media query) nor requesting a deliberately taller 640x680 image
moved it. I could not make the CSS background mechanics give a reliable guarantee.
**Fix:** Stopped depending on them. `boundary_aerial(cover=True)` now crops Google's baked-in
attribution strip (bottom 40px at scale=2) out of the raster, and the hero block injects the
attribution into the cover as real HTML text — 6pt mono, white at 82% on the existing
darkening gradient, `z-index: 6` so it always sits under the terracotta address card (z-index 7).
Attribution is still displayed, as the licence requires, and is now legible by construction
rather than by luck. ⚠ The crop and the re-display are a pair: never crop without re-drawing it.
**Verification:** Zoomed the region at 300 DPI and read it — "Imagery ©2026 Airbus, Vexcel
Imaging US, Inc." renders complete and crisp. Full cover inspected, no collision with the
address card. Confirmed the attribution line is injected ONLY for `boundary_aerial` covers
(1 of 4 samples) and is absent on the three Domain-photo covers, which carry no Google
imagery. All four render 0 overflow; default non-self-serve path re-verified.
**Files:** `scripts/generate_appraisal_v4.py`
**Recurrence:** First. Second time in this session that reading the rendered picture caught
what the code path did not — cf. `[APPRAISAL-GENERIC-HERO-FALLBACK]`.
**Note:** the cover-aspect aerial is cached separately as `boundary_cover.png` beside the
shared `boundary.png`, so re-renders cost no Static Maps call. Two aerials per property now.

## [DR-WEBSITE-UNCOVERED] 983 live website files were compared against nothing — and 2 files were backed up NOWHERE — 09:20 AEST
**Symptom:** `/home/fields/Feilds_Website/` has no local `.git` and is not in `REPOS`, so nothing
ever compared the live website source against `Website_Version_Feb_2026`. Backup rested entirely on
whoever made a change remembering to `gh api` it.

**Fix:** `EXTRA_TREES` in `check_unpushed_code.py` — directory-scale version of `EXTRA_FILES`. One
recursive `git/trees` call for the remote side (1,502 blobs, verified not truncated) plus one batched
`git hash-object` for the local side: **two subprocesses for ~1,000 files**, not an API call each.

**⚠ REPORT-ONLY, deliberately — this must never auto-push, for two independent reasons:**
1. **A website push triggers a Netlify deploy.** Deploys cost credits and a sitemap build hang is
   UNCANCELLABLE. A backup checker must not be able to deploy the site.
2. **Direction is unknown.** A hash mismatch says local != remote, NOT that local is newer. For the
   orchestrator that inference is safe (edit-local-then-push is the only workflow). Here it is not:
   all 3 real diffs look like the LOCAL copy is BEHIND. Auto-pushing would have regressed the site.
   The output says `DIFF … (direction unknown)` rather than `MOD` for exactly this reason.

**Result: 905 in sync · 3 differ · 23 local-only.** Generated artifacts excluded (`.react-router`
typegen 50 files, `public/data/articles*.json` written by `scripts/fetch-articles.js`) — they drift
on every build and are noise, not signal.

**The real find — a circular assumption between two checks.** `is_external_link()` skips symlinks
pointing outside the repo, on the stated reasoning that the file "is version-controlled where it
actually lives ... already backed up in Website_Version_Feb_2026". That comment was the ONLY
evidence for the claim. Checked it: **8 of the 10 BreakGlass workbench symlinks are genuinely in the
website repo. `sirens.js` (14,134B) and `powerAudio.js` (8,682B) are in NO repo at all.** The
orchestrator check skipped them as "backed up over there"; the website repo never had them. Each
check assumed the other covered it, so 22.7KB of source sat unbacked-up and invisible.

They are unimported (only referenced in sibling comments), which is why no build ever failed on
them — and why nothing surfaced the gap.

**Fix:** `link_is_really_backed_up()` — resolves the symlink target, maps it through `EXTRA_TREES`,
and confirms the path is actually present in that remote's tree. An unverified assumption is not a
backup. Orphans now report as `⛔ ORPHAN`, count as real gaps, and raise a Telegram problem.

**Verified:**
- `↔ 8/10 symlink(s) VERIFIED backed up there` + 2 ORPHAN lines — the split is now measured, not asserted.
- Rule 7b failure paths all raise rather than read clean: wrong local path → UNKNOWN · empty local dir
  → UNKNOWN · nonexistent remote repo → UNKNOWN · truncated remote tree → explicit refusal.
- A tree that cannot be compared appends to `problems`, so silence still means "checked", never
  "could not tell".

**Left for a decision (NOT actioned):**
- **The 2 orphaned files.** Backing them up via the website repo costs a Netlify deploy for code
  nothing imports; the alternative is storing them in the orchestrator repo. Will's call.
- **The 3 `DIFF` files** — `src/main.tsx` (`react-router` vs `react-router-dom`),
  `src/components/MarketIntelligenceSection/index.ts` (quote style only),
  `public/off-market-v3/intro.js` (comment text; generated from `matrix-intro.html`). All look like
  local-behind-remote, none is a backup gap, and resolving them needs a human.

**Files:** `scripts/check_unpushed_code.py`
**Recurrence:** 3rd of the git-DR-gap class today ([GIT-DR-GAP] 2026-07-15 →
[DR-OUT-OF-TREE-BLINDSPOT] → this). The pattern each time: the check covered what someone remembered
to point it at, and the gap lived exactly where a comment claimed coverage without proving it.

## [LISTING-IMAGE-DERIVATIVES] Listing pages ship full-res originals into an 800px slot — 09:30 AEST
**Symptom:** The On-Market V2 prototype's gallery weighs ~46 MB (largest single photo 10.6 MB). It
declares `width="800" height="600"` on every image and is then handed a ~3,000px original.
**Root cause:** Not a prototype fault — inherited. `blobs.fieldsestate.com.au` stores exactly one
rendition per photo (whatever `mirror_full_res_photos.py` / `download_images_to_blob.py` downloaded)
and nginx serves it as a static file with no transform layer of any kind. Lazy-loading past the
first four was a bandage on bytes that should never have been sent.
**Fix:** Pre-generated WebP renditions written BESIDE the original, named by width
(`03.jpg` -> `03.960.webp`) so the URL is derivable by string substitution — no schema change, no
second lookup, no service on the request path. New `shared/image_derivatives.py` (480/960/1600,
q80, never upscale, EXIF-transposed, only reports renditions that were actually written) and
`scripts/backfill_image_derivatives.py` (idempotent, `--suburbs/--limit/--workers/--dry-run`,
wrapped in `job_run('image_derivatives', cadence_hours=24)` with a Rule 7b assertion on the
zero-output path). Originals are never modified and no document is written.
**Measured:** 120 photos sampled from 62,873 for_sale files across the three target suburbs —
median width 1,618px, mean 731 KB. A 14-photo gallery goes **10.0 MB -> 1.8 MB (82%)**; on the first
real listing backfilled, **11.3 MB -> 1.1 MB (90%)**. Derivatives cost ~49% of source bytes, so
~21 GB scoped to for_sale in the three target suburbs — `/data/blobs` is its own 738 GB disk with
376 GB free. ⚠ `df -h /data` reports the 97 GB root and is misleading; the store is `/dev/sdc`
mounted at `/data/blobs`.
**Throughput trap:** the naive form (full decode, three independent LANCZOS passes, single-threaded)
ran at ~10 photos/min — ~7 hours for Robina alone. `Image.draft()` before `load()` plus a
progressive 1600->960->480 chain plus 4 threads took it to ~93 photos/min (~9x). ⚠ The true source
width must be read BEFORE `draft()` shrinks the raster, or a 3,000px photo looks too narrow for
1600w and silently loses its largest rendition.
**Verification:** wide and narrow fixtures (2000px -> 3 renditions at exact widths; 320px -> none,
correctly); idempotent re-run rewrites nothing (50 photos, 0 written / 50 skipped); missing original
returns None, distinct from `{}`; all three widths 200 `image/webp` over HTTPS with correct byte
counts; a missing derivative 404s cleanly (which is why `srcset` must be built from what exists).
The 7b assertion was executed against an empty suburb and confirmed to raise, not merely be present.
**Files:** `shared/image_derivatives.py` (new), `scripts/backfill_image_derivatives.py` (new),
`15_On_Market/03_Audit/IMAGE_DERIVATIVES_SPEC.md` (new), `15_On_Market/prototype/README.md`
**Recurrence:** First.
**Not done yet:** the website side — a rendition helper in `netlify/functions/shared-utils.mjs`
alongside `toFullResUrl()`, and `srcset`/`sizes` on the property page with the original as `src`.
Until that ships, the derivatives exist but nothing requests them. Backfill so far is Robina only.

## [APPRAISAL-FALSE-FEATURE-CLAIMS] Reports asserted features properties do not have — 10:52 AEST
**Symptom:** Will spotted two false claims in sample reports: 5 Chantilly Place described as
having "a permanent bushland boundary" (it adjoins a golf course, 71 m away) and 16 Cheltenham
Drive as having "a cul-de-sac head position" (it faces Cheltenham Drive, a main road).
Investigation found the scope was far wider: **184 of 185 rendered PDFs (99%)** contain at
least one false-feature string — `bushland boundary` 184, `Six bedrooms` 179, `quiet
cul-de-sac` 161, `13 Terrace` 42.
**Root cause — three independent defects:**
1. **Vision read by substring.** `pick_highlight.py` and `personas.py` each derived features
   from `satellite_analysis.categories.*` (one un-versioned GPT call, no confidence) via
   substring match. `green_space_proximity` is a PROXIMITY field; the disjunction
   `or "bushland" in green` turned the value `bushland_adjacent` into a BOUNDARY claim, on a
   home whose own `backs_onto` said `["residential_only"]` — the report contradicted its own
   source field. The derivation was DUPLICATED across the two files, so fixing one would have
   left the other wrong.
2. **Hardcoded 13 Terrace Court copy.** §05's three "lever" rows and §02's persona match-bar
   labels were literals in `render.py`'s templates — `data_pull` computed `presentation_rows`
   that the template ignored. Every report for every property asserted a pool, a bushland
   boundary, a rear deck and a quiet cul-de-sac. The label "Six bedrooms" was 13TC's own
   bedroom count on a row whose key is the generic `bedrooms_high` (beds >= 5).
3. **Authoritative data ignored.** `osm_location_features.road_classification.is_cul_de_sac`
   sat on the same documents and said `false` for both homes. `canonical_attributes.yaml`
   scores `gpt_satellite` at 0.70, the lowest non-degenerate tier; neither caller consulted it.
**Fix:** New `scripts/appraisal_template/location_facts.py` — a single verified resolver
porting the V3 /off-market evidence standard, which never used vision and never made these
mistakes: OSM road classification for cul-de-sac, and `green_space.classify()` (a NAMED OSM
feature + a MEASURED edge distance + a threshold: <=25 m "backs onto", <=80 m "adjoins") for
boundaries. "Bushland" now requires the OSM feature to actually BE bushland/nature reserve AND
to back on; a golf course and a park are named for what they are. Both callers rewired to it.
Removed the bushland rarity candidate entirely — it is uncountable across the sold cohort
(green-space relations are geometric, not stored), so any "N of 542" figure printed beside it
measured a different population than the claim. §05 rows and §02 labels now render from data.
**Verification:** Chantilly now reads "adjoins Palmer Gold Coast (Robina Woods) Golf Club",
Cheltenham "backs onto Belmore Close Reserve" — both true and both specific. Persona dots now
track reality (Chantilly Pool 4/5 filled, Cul-de-sac and Bushland empty; Cheltenham all empty).
All four samples probe CLEAN for every false-claim string; 0 overflow.
**⚠ KNOWN GAP — cul-de-sac is now never claimed, and that is not the same as fixed.**
`is_cul_de_sac` is `True` on **0 of 2,808** populated docs and missing on 27,343. Robina
demonstrably has cul-de-sacs, so the field is broken or never-positive, NOT authoritative.
Vision claimed cul-de-sac on 215 of 558 eligible docs and OSM contradicted **100%** of them —
but with an all-False field that comparison cannot distinguish "vision was wrong" from "OSM is
empty". The safe outcome holds either way (we assert nothing we cannot substantiate), but
restoring cul-de-sac as a real feature needs a genuine derivation — dead-end road geometry or
OSM `noexit`/`cul_de_sac` tagging — not this field. V3's `scarcity_features.py` has no
cul-de-sac rule either, which is consistent.
**Files:** `scripts/appraisal_template/location_facts.py` (new),
`scripts/appraisal_template/pick_highlight.py`, `scripts/appraisal_template/personas.py`,
`scripts/appraisal_template/data_pull.py`, `scripts/appraisal_template/render.py`
**Recurrence:** First for false features. Third this session in the family of "content that is
not about this property reached the reader" — cf. `[APPRAISAL-GENERIC-HERO-FALLBACK]`. All
three were found by reading the rendered output, not the code path.
**⚠ The 185 existing PDFs on disk are NOT regenerated and 99% still carry false claims. Do not
print or post any of them without re-rendering.**

## [DERIVATIVE-CORRUPT-AS-SKIPPED] My own 7b violation: undecodable originals counted as "no work needed" — 10:12 AEST
**Symptom:** The first full Robina backfill reported `written=9557 skipped=429 missing=0` — clean.
The log underneath it carried three `decode failed` lines that appear in no counter.
**Root cause:** `make_derivatives()` returned `{}` on a decode failure, which is the identical value
it returns for a photo that legitimately needs no rendition (source narrower than every target).
`_one()` could not tell them apart and classified both as `skipped`. This is exactly the shape
Rule 7b exists to prevent, written by me in the same session that quoted the rule in the spec — a
job reporting success while silently discarding failures.
**Fix:** New `DecodeError`, raised on an unreadable source, giving four distinct outcomes:
`None` = original absent · `DecodeError` = present but not an image · `{}` = decoded, nothing needed ·
`{w: url}` = written. Backfill counts `corrupt` separately, reports it in `beat.metrics`, and raises
when it exceeds 2% of photos seen — a slow bleed never trips the existing zero-output checks because
the healthy majority keeps `written`/`skipped` non-zero. Per-width encode failures are still
swallowed (one bad rendition is recoverable by fallback; an unreadable source is not).
**Second finding — a real data defect, not a counting one:** those 3 files are **HTML pages saved
with a .jpg extension** (~200 KB each), served today as photos of live listings.
⚠ **CORRECTED 2026-08-13 11:55** — I first wrote that these were "error pages" from "a failed HTTP
response". They are not. Every one is a **Matterport 3D Showcase page for the correct property**:
`property_images_original[46]` on 5 Chelsea Place, Robina is `https://my.matterport.com/show?m=…`
(carries an `auth=Bearer` token — treat as a credential). The fetch SUCCEEDED; the URL was simply
never an image. Root cause is the absence of any content-type/magic-byte check on write, plus a
source list that contains virtual-tour links. The full-target-market backfill found **6** such files,
not 4. See `15_On_Market/HANDOFF_two_live_defects.md`. **Not fixed here** — `download_images_to_blob.py` /
`mirror_full_res_photos.py` need a magic-byte check on write, and the 4 known files need re-fetching.
Scope beyond Robina unmeasured.
**Also:** `make_derivatives` now short-circuits before decoding when every rendition already exists.
A no-op re-run over Robina went from a full JPEG decode per photo to **2m00s for 4,219 photos**.
**Verification:** all four outcomes asserted against real fixtures — corrupt→`corrupt`,
good→`skipped`, narrow→`skipped`, absent→`missing`; `DecodeError` confirmed raised. Re-ran the full
Robina pass under the heartbeat: `4219 photos, 0 written, 4216 skipped, 0 missing, 3 corrupt`, and
confirmed the row in `system_monitor.job_runs` (status success, cadence 24h, corrupt count carried
into `metrics`).
**Files:** `shared/image_derivatives.py`, `scripts/backfill_image_derivatives.py`
**Recurrence:** First for this job. Nth for the pattern — cf. `[BUILDER-ENV-EXPORT-GAP]`,
`[INDEXING-SILENT-ZERO]`, `offmarket_intel_poller`, all in CLAUDE.md Rule 7b.

## [WATERFRONT-DETECTOR-DEPTH-BUG] Canonical detector read the OSM signal at the wrong depth — 11:38 AEST
**Symptom:** 11 Placid Court, Varsity Lakes — 22.2 m from Silvabank Lake, `waterfront_type:
'lakefront'` — was not flagged waterfront, so the self-serve report published a valuation range
of $1,748,105–$2,189,566 at confidence "medium", built from comparables that every one carry
`water_views: False`. Waterfront has been out of scope since 2026-07-26 precisely because the
comparable-sales model values a waterfront home against dry blocks and produces a materially
wrong range.
**Root cause:** `shared/waterfront.py::detect_waterfront` line 164 read
`doc.get('waterfront_premium_eligible')` at the **TOP LEVEL**. The field has always lived at
`osm_location_features.water_features.waterfront_premium_eligible`. The detector was written to
consume exactly this evidence and looked one level too shallow, so it silently found nothing.
The OSM water block was backfilled 2026-08-08, thirteen days AFTER this module was written on
2026-07-26, and lake homes were the documented blind spot that backfill existed to close.
**Scale (measured, 3 core suburbs):** 2,011 properties carry OSM waterfront geometry; only 63
were flagged. **1,948 missed (97%)** — 1,945 lakefront, 3 riverfront, every one within 30 m of
water (43 <=10 m, 652 10-20 m, 1,253 20-30 m; `LAKE_FRONT_M = 30`, a conservative threshold).
**252 of the missed carry a published valuation range built from dry comps.**
**Fix:** Added Signal 4 to `detect_waterfront` reading the nested OSM block, using
`waterfront_premium_eligible` as-is since it already encodes the conservative distance
thresholds. Ranked first in the reason order — it is the only signal with a measured metre
distance behind it. Verified: Placid -> True (reason `osm_water_frontage`); three dry controls
(golf-course adjoin, two park boundaries) -> False. No stored flags were flipped.
**Also added:** a waterfront guard on the **self-serve** appraisal path only. It refuses to
render and produces 0 artifacts. Analyst-driven renders are deliberately still permitted —
Will can see the property is waterfront and exercise the judgement the policy assumes; the
self-serve path removes that judgement, which is why it needs the hard stop.
**⚠ RUNTIME IMPACT — READ BEFORE THE NEXT NIGHTLY.** `detect_waterfront` is consumed by
`generate_property_ai_analysis.py` and `generate_sold_analysis.py`. With this fix those
generators will now treat ~1,948 additional homes as out-of-scope and skip their editorial.
That is policy-correct but a large swing in one night. `precompute_valuations.py` and the
website key off the persisted `is_waterfront` flag, which has NOT been changed, so valuations
and indexing are unaffected until someone runs the backfill tool.
**Files:** `shared/waterfront.py`, `scripts/generate_appraisal_v4.py`
**Recurrence:** First for this field. Fourth this session in the family of "the data was there
and the code looked in the wrong place" — cf. `[APPRAISAL-FALSE-FEATURE-CLAIMS]` (OSM
`is_cul_de_sac` ignored) and `[APPRAISAL-GENERIC-HERO-FALLBACK]` (`aerial_boundary_url`
ignored). Directly the Rule 8 failure mode: a lookup returning nothing is a fact about the
path you typed, not about the data.
**NOT done (deliberately, pending Will):** no `is_waterfront` flags flipped, no de-indexing, no
editorial suppression backfill. Flipping ~1,948 properties would noindex them, drop them from
the sitemap and withhold their valuations — a large change to the indexed surface across all
three core suburbs.

## [OFFMARKET-SELF-SERVE-REPORT-LIVE] Report request section shipped to V4 /off-market — 11:58 AEST
**Symptom:** Not a bug — new capability. Will's call: stop refining the report and get it in
front of readers, because there is no point perfecting a document nobody downloads.
**What shipped:**
- `ReportSection.tsx` on the V4 page, immediately after `DispersionSection` (Will's placement:
  dispersion has just argued a single number cannot be trusted, so a written report resolves the
  problem the reader is holding). Registered in all three required places — `parts.ts`
  `PARTS[0].sections`, `ReportHeader.tsx` `TITLES`, and `useReportAnalytics` `SECTION_ORDER`
  derives automatically. Missing the first would have silently corrupted `deepest_index`.
- `netlify/functions/offmarket-report-request.mjs` — POST enqueue / GET status.
- `scripts/offmarket_report_poller.py` + `fields-offmarket-report-poller.service` (15s poll).
- `--no-flatten-cover` on the generator.
**Delivery problem, solved cheaply.** The blockers were an 11.87 MB average PDF, a 30s serial
global queue, and VM-only storage. All three dissolved:
1. `/data/blobs` is served directly by nginx as blobs.fieldsestate.com.au with `application/pdf`
   already in its type map — publishing is a file write, no upload code, no CDN work.
2. A dedicated queue instead of `trigger_requests`, which is global, strictly serial and shared
   with the nightly scrape; a visitor could have sat behind a 20-minute job.
3. **17 MB -> 1.2 MB.** `--no-flatten-cover` skips the 300 DPI print raster (17->12 MB), then a
   Ghostscript `/printer` pass takes it to 1.2 MB with no quality loss visible on the cover or
   the comparable photography at 300 DPI inspection. ⚠ The flatten exists for the Officeworks
   CMYK pink-hero fix — anything being PRINTED must keep it.
**No contact capture, deliberately (Will, 2026-08-13).** No email, name, phone or device token.
The page's load-bearing promise is that nothing is sent to us — corrections stay in
localStorage, plan answers never leave the device, `NoFigureSection` states there is no form. A
capture here would have made all of it untrue and broken claims-register C13b. The reader asks,
the PDF appears, nobody can contact them.
**Guards:** section self-gates on `valuation.method === "engine"` (the report's spine is the
derived range); waterfront refused server-side in the generator, because a client-side check
would be a suggestion rather than a rule; `HEADLESS_UA` regex copied from
`offmarket-intent-alert.mjs` — a crawler walking ~21,800 off-market URLs would otherwise wedge
the render queue for weeks; join-don't-duplicate on pending/processing; 7-day PDF cache reuse.
**Self-monitoring (Rule 7 + 7b):** `job_run("offmarket_report_poller", cadence_hours=24)`, one
heartbeat per CLAIMED job rather than per idle tick, and it **raises when a cycle claims a job
and publishes nothing** — otherwise the poller would report success while every visitor's
request failed. Verified live: heartbeat present with `self_registered: true`, `detail: "1
report(s) published"`. Stale `processing` claims older than 900s are reclaimed so a restart
cannot strand a visitor on a spinner.
**Verified end to end:** three cases through the live service — dry property published
(200 OK, `application/pdf`, 1,233,395 bytes, ~35s); waterfront refused with the guard message;
unknown slug failed with an actionable error. `tsc -p tsconfig.json` clean (bare `tsc` checks
zero files), `npm run build` clean in 13.9s. All pushed files md5-verified against the remote.
**Instrumentation:** `offmarket_report_requested` fires on the CLICK — that is the demand
signal that decides whether further investment is justified — plus `_ready`, `_downloaded`,
`_failed`.
**Files:** `scripts/offmarket_report_poller.py` (new),
`/etc/systemd/system/fields-offmarket-report-poller.service` (new, not in git),
`scripts/generate_appraisal_v4.py`, website: `netlify/functions/offmarket-report-request.mjs`
(new), `src/pages/OffMarketPage/v4/{ReportSection.tsx (new),OffMarketV4.tsx,parts.ts,
ReportHeader.tsx,ClosingSections.module.css}`
**Recurrence:** First.

## [PROPERTY-ANALYTICS-CENSORED] Property-page engagement metrics were survivor-biased and double-counted — 11:40 AEST
**Symptom:** Asked whether PostHog logging was rich enough to decide an A/B test of the On-Market V2
page, the answer was no — and the existing numbers were not merely thin, they were wrong.
**Root cause (three, all measured before fixing):**
1. **No exit capture.** `time_on_page` is a `setTimeout` ladder (values are only 10/30/60/120/300)
   and no final duration is ever emitted; there is no `visibilitychange`/`pagehide` handler on the
   page. Sessions that emitted `time_on_page` had a **median 49s** on page; those that emitted none
   had a **median 3s — and that was 34% of all sessions**. The metric silently drops the fastest
   visitors, so a change that makes MORE people bounce instantly makes the average look BETTER.
   ⚠ My own earlier power calculation (mean 43.9s, sd 46.5) was computed over these ladder markers,
   not durations, and was therefore invalid. Corrected before it was acted on.
2. **`property_view` double-fired.** The effect depended on `[id, property?.suburb]` and `suburb` is
   undefined until the client fetch resolves, so it ran twice — 199 of 253 session+property pairs had
   exactly 2 events, ~2-4s apart, the first with no suburb. Every count built on it was ~2x inflated
   and any suburb filter dropped half the data. The same dependency also tore down and rebuilt the
   scroll/time instruments mid-visit, restarting the clock from fetch-resolve and allowing duplicate
   scroll milestones.
3. **Variant assignment races the early events.** `$feature/*` is present on 99% of `time_on_page`
   but only **66% of `property_view` and 38% of `$pageview`** — they fire before flags load.
**Fix:** New `phTrackPageEngagement()` in `src/utils/posthog.ts` — continuous `max_depth_pct` and
`engaged_seconds` (gated on document visibility, so a hidden tab is not counted as reading), emitted
via the existing `phCaptureOnExit`/sendBeacon on `visibilitychange`, `pagehide` AND SPA unmount. Both
tracking effects on `PropertyPage.tsx` now depend on `[id]` only, with suburb read through a ref so a
late fetch enriches events without re-running the effect; `property_view` waits on `phOnFlagsReady`
with a 2s fallback so a blocked SDK loses the variant rather than the event.
⚠ `page_engagement` may emit more than once per visit (tab hidden then restored). **Take the MAX per
session+page+property_id — never sum, never average raw rows.** Documented on the helper.
**Also created:** PostHog flag `property_page_v2` (id 817007), control/v2 50/50 at 100% rollout. No
code branches on it yet, so it is inert — deliberately, so an **A/A period can validate balance and
instrumentation before any traffic is spent on the real comparison.**
**Verification:** `npx vite build` clean (42s). `npx tsc -p tsconfig.app.json --noEmit` filtered to
the touched files — the 10 errors in `PropertyPage.tsx` are all pre-existing (lines 637+, none at the
edited sites), none in `posthog.ts`. ⚠ Not yet verified in a browser — `page_engagement` has not been
observed arriving in PostHog. That is the next gate before trusting it.
**Files:** `src/utils/posthog.ts`, `src/pages/PropertyPage/PropertyPage.tsx`
**Recurrence:** First. Related in kind to `[DERIVATIVE-CORRUPT-AS-SKIPPED]` earlier today — both are
a measurement that reports success while discarding the cases that matter.

## [COVER-PHOTO-AGENCY-WATERMARK] Competing agency's watermark on report covers — 11:45 AEST
**Symptom:** An audit of 43 pre-warmed covers found `1 Highgate Lane` carrying
**"PRD nationwide Robina"** across the photo, directly beneath the Fields logo. Scraped Domain
listing photos belong to the listing agency and frequently carry its watermark. A second issue
in the same audit: listing photo #1 is often a kitchen, lounge or deck rather than the home.
**Root cause:** The hero chain prefers `auto_apr01` / `auto_scraped` (Domain CDN photos) and had
no notion of whether a photo was fit to publish — only whether it downloaded and exceeded 2 KB.
**Scale (measured over 149 real hero images):** 11% are unfit; **4.0% carry competing-agency
branding** — roughly 310 covers across the 7,730 eligible properties.
**Fix:** New `scripts/appraisal_template/cover_photo_gate.py` — one Google Vision call
(LOGO_DETECTION + TEXT_DETECTION + LABEL_DETECTION) per photo, reusing the service-account auth
already built for `floor_plan_debrand.py`. Rejects agency logos, agency names from the existing
`floor_plan_agencies.txt`, marketing text, phone numbers, and interiors. Rejected photos fall
through to the **boundary aerial**, which is ours, never watermarked, and always the subject lot.
Deliberately does NOT inpaint: `floor_plan_debrand` can, because a floor plan is a line drawing
with margins; content-aware removal on photography is far riskier and a smudged patch is worse
than no photo.
**Two calibration errors of mine, both found by looking at the rejected images:**
1. `pool > exterior` rejected four GOOD covers — pool in front of a clearly visible two-storey
   block — because Vision labels those frames "resort"/"leisure"/"condominium", none of which
   were in the exterior vocabulary. The vocabulary was the bug, not the photos. Rule removed;
   a frame with no building at all is still caught by `no_exterior_signal`.
2. `logo:Android` was rejecting every Street View hero. That is Google's own required
   attribution, not a competitor's watermark. Added `ALLOWED_LOGOS`.
   Recalibration moved publishable from 63.1% -> 89.3% over the same 149 images. **The first
   measurement (37% unfit) was mostly my own over-rejection, not a defect in the data.**
**Cost control:** branding is a property of the LISTING, not the frame — every photo in a PRD
listing carries PRD's mark. Testing all 8 candidates cost 16 Vision calls to reach the same
answer (~$550 across the catchment). Now short-circuits to the aerial on the first branding
rejection: **16 calls -> 1**, ~$60 for the full catchment. Composition rejections (interior)
still try the next photo, since that is per-frame. Verdicts cached on (subject_id, image hash)
in `system_monitor.cover_photo_verdicts`.
**Fails OPEN.** An unreachable Vision API accepts the photo and records
`reason: vision_unavailable`, rather than silently demoting every cover in the catchment to an
aerial the moment a key expires.
**Verified:** 6/6 on hand-labelled cases (PRD watermark, two interiors, two facades, one pool
photo I had mislabelled myself — the gate was right and I was wrong). `1 Highgate Lane`
re-renders to `cover_hero=boundary_aerial`.
**Files:** `scripts/appraisal_template/cover_photo_gate.py` (new),
`scripts/generate_appraisal_v4.py`, `scripts/build_cover_contact_sheet.py` (new),
`scripts/prewarm_offmarket_covers.py`
**Recurrence:** First. Fifth this session in the family of "content that is not about this
property, or not ours, reached the reader" — and again found by looking at the rendered output
rather than the code.
**Audit tool:** `python3 scripts/build_cover_contact_sheet.py` tiles every cover with its hero
source, live at `https://vm.fieldsestate.com.au/concepts/off-market/Cover_Audit/index.html`.
Pre-gate covers preserved at `/data/blobs/off-market-reports/covers_pregate/` for comparison.

## [UNIT-NUM-ALIAS-EATS-FLOOR-AREA] A price sanity band applied to floor areas — 14:05 AEST
**Symptom:** building the statutory comparable set, the backtest reported floor area known on
**0 of 3,005** attached Robina homes. A direct count says **20.1%** carry one. The zero was
mine, not the data's.
**Root cause:** `unit_valuation.py` had `_num = sale_price`, with the comment *"internal alias;
every call site gets the sanity band"*. `sale_price` enforces `MIN_SALE = 20_000` to keep weekly
RENTS out of the comparable pool. A floor area is a number around 100. Every floor area in the
system fell under the price floor and returned `None` — silently, with no error, at every call
site that measured a non-price quantity.
**What it actually cost.** Not just the new code:
  * `impute_floor_area()` is **live on the page data path** (`unit_page_data.py:160`,
    `render_unit_report.py:214`). It builds its sample through `_num`, so the sample was always
    empty, `len(rows) < 2` always tripped, and **it returned None for every unit ever asked**.
    Its own docstring advertised *"5.2% median error on leave-one-out testing (n=424)"* — a
    figure this code could not have produced in the state it shipped in.
  * Comparable `floor` values were always None, so the floor-area term was inert.
**A second, independent bug in the same function.** `impute_floor_area` passed
`{**scope, "bedrooms": beds}` into the **Mongo query** — the top-level field, which fills 53.5%
of attached stock against 57.1% for the coalesced value. That is the exact anti-pattern
documented in a ⚠ block on `comparables()` twenty lines above it. Either bug alone was fatal, so
neither was ever observed.
**Fix:** `_num` is now a real numeric coercion with no band (and rejects `bool`, which is an
`int` subclass — a `True` bathroom is not 1 bathroom). The one genuine PRICE call site inside
`_sales_in` was moved to `sale_price` explicitly, so the rental guard is unchanged. Bedrooms are
now matched in Python on the coalesced value.
**Files:** `15_Off-Market/Units/scripts/unit_valuation.py`
**The lesson, which is the same one as [RENTAL-AS-SALE]:** a sanity band is a claim about a
QUANTITY, not a safety feature you can apply everywhere. Aliasing one to a general-purpose name
(`_num`) spreads a price assumption to every quantity in the module, and reads as prudence in
review. Both bugs in this family were invisible because the failure mode is an empty result, and
an empty result and a wrong question produce identical output (Rule 8).
**Recurrence:** 2nd of the price-band-on-a-non-price family (1st: `[RENTAL-AS-SALE]`, where the
band was MISSING on one branch; here it was PRESENT on branches that should not have had it).

## [SPAWN-TASK-HANDOFF] Cross-session task handoff queue (spawned_tasks) — 11:52 AEST
**Symptom:** Work identified mid-session but out of scope for that session ("two live defects I
found but haven't touched") died with the transcript. There was no way to hand a bounded task to
a separate Claude session and get the answer back.
**Root cause:** Every existing queue is per-domain and keyed to fixed ids — `trigger_requests`
resolves `process_id` against `config/process_commands.yaml` and is strictly serial; the CEO path
runs Codex on a remote VM. None can carry a free-form brief.
**Fix:** New dedicated queue `system_monitor.spawned_tasks` (same precedent as
`offmarket_report_requests`, and for the same reason). `spawn_task.py` enqueues a brief and
REJECTS thin ones; `spawn_worker.py` runs each as a headless `claude -p` session on Max
(concurrency 2, model pinned `claude-opus-4-8`, env recipe from `deep_research.py:_clean_env`);
`spawn_status.py` reads results back. Scopes are `investigate` (no Write/Edit) and `patch`
(git worktree only). There is deliberately no `deploy` scope. Rule 7b assertion: a session that
writes no result file, or reports "resolved" with an empty worktree, is recorded as FAILED.
CLAUDE.md gains Rule 9 (never name parallelisable work without dispatching it).
**Files:** scripts/spawn_task.py (new), scripts/spawn_worker.py (new), scripts/spawn_status.py
(new), /etc/systemd/system/fields-spawn-worker.service (new), CLAUDE.md
**Recurrence:** First

## [SPAWN-GH-TOKEN-ESCAPE] Spawned sessions had push access to production repos — 12:05 AEST
**Symptom:** `spawn_worker.py` documented a "no-push guarantee" — spawned sessions supposedly
could not write to GitHub. The first task ever queued was one asking a spawned session to test
that claim on itself. It came back authenticated as Will954633 with `push:true, admin:true` on
BOTH `Website_Version_Feb_2026` and `Fields_Orchestrator`.
**Root cause:** The control was `GH_CONFIG_DIR=/nonexistent`. But `gh` reads `GITHUB_TOKEN` /
`GH_TOKEN` in PREFERENCE to its config dir, and `spawn_worker.py`'s own `load_dotenv(.env)` lifts
`GITHUB_TOKEN` (.env line 39) into `os.environ`, from which `_child_env()` copied it — the strip
list held only 4 Anthropic/Claude keys. The redirect was applied exactly as designed and bought
nothing. The docstring also claimed `git push` hanging on this VM was a second backstop; it is
not — this fleet writes to GitHub via `gh api PUT contents`, which that hang does not touch.
**Fix:** Strip `GITHUB_TOKEN`/`GH_TOKEN`/`GH_ENTERPRISE_TOKEN` in `_child_env()`; demote the
`GH_CONFIG_DIR` redirect to defence-in-depth in both code and docstring. Verified after the fix:
`gh auth status` → "not logged into any GitHub hosts", `gh api user` → rc=4. Confirmed no
`~/.git-credentials` and no `credential.helper` fallback.
**Note:** an unattended full-autonomy path still exists elsewhere —
`worker-agent/run-worker-agent.sh:73` runs `claude -p --dangerously-skip-permissions` on a 6000s
timeout. Not touched here; flagged for review.
**Files:** scripts/spawn_worker.py
**Recurrence:** First

## [SPAWN-STALE-CLAIM-TZ] Naive datetime made every in-flight task look 10h stale — 11:58 AEST
**Symptom:** First real spawned task was reclaimed as "stale" 30 seconds after it started, and a
second `claude -p` session was launched on top of the first — two concurrent sessions, same task.
**Root cause:** pymongo returns naive datetimes. `started_at.timestamp()` on a naive value
interprets it as LOCAL time (Brisbane, UTC+10), so it evaluates ~36000s in the past and always
sits below a UTC-derived cutoff, however large `STALE_CLAIM_SECONDS` is.
**Fix:** `_as_utc()` helper attaches `timezone.utc` to naive values before comparison.
**Note:** the identical comparison exists in `scripts/offmarket_report_poller.py:poll_once`
(`started.timestamp() < cutoff`). It is latent there only because that poller is strictly serial,
so no job is ever mid-flight when the sweep runs. Worth fixing before that poller is ever made
concurrent.
**Files:** scripts/spawn_worker.py
**Recurrence:** First

## [PROPERTY-SOFT404-CLOSURE] Closing measurement: /property sitemap soft-404s are at zero — 12:05 AEST
**Symptom:** `15_On_Market/HANDOFF_two_live_defects.md` carried this forward as an open defect:
`2026-08-08.md:176,204,215` recorded **115 of 1,493** `/property` sitemap URLs (7.7%) returning
HTTP 200 while rendering "Property Not Found" with `noindex`. A prior session sampled 250 URLs,
got 0, and could only bound it at ≤1.5% (~22 URLs) — so it still read as partly open.
**Root cause of the *re-report*, not of the defect:** the defect was already fixed by
`[PROPERTY-SOFT404-UNIT-SLUGS]` (commit `97a89526`, `effectivePropertyAddress()`), but **no closing
fix-history entry was ever written**. With nothing recording closure, the 08-08 entry remained the
most recent word on it and the defect was inherited as live by two subsequent sessions.
**Fix:** no code change — this is the missing measurement and the missing entry. Checked
**every** `/property` URL in the live sitemap (not a sample), fetched as Googlebot, flagged on both
`sitemap_robots_invariant.unhealthy()` and the literal "Property Not Found" marker:
```
total /property sitemap URLs: 1508
{"checked": 1508, "flagged": 0, "violations": []}
```
**115 → 0 of 1,508, exhaustive.** Closed.
**Also verified — the inverse problem** (`2026-08-08.md:27`, 11 `/property` pages earning Google
impressions but absent from the sitemap): also already fixed, by commit `5a65cfb1`.
`generate-sitemap.mjs:getPropertyUrls()` matched `listing_status: 'for_sale'` exactly while
`getOffMarketUrls()` excluded `{$nin: ['for_sale','under_contract']}` — `under_contract` fell in the
gap between the two, and `property.$id.tsx`'s noindex gate never tests `listing_status`, so those
pages served indexable with no sitemap entry. Both named examples (17 Pitta Place, 180 Christine
Avenue) are in the live sitemap now. Ran invariant B exhaustively over the whole family rather than
the nightly's 40-URL sample: of 1,276 `/property` pages earning impressions in GSC 2026-07-14..08-10,
**226 are absent from the sitemap and all 226 serve `noindex, follow`** — 0 violations. They are the
waterfront exclusion and the editorial gate working as designed, not wrongly excluded.
**⚠ Gap this exposed in the monitor itself:** `scripts/sitemap_robots_invariant.py` invariant A uses
`DEFAULT_SAMPLE = 25` **per family** — it only ever tests 25 of 1,508 `/property` URLs a night. The
original 7.7% breakage was caught by luck on the first run; at that sample rate the nightly needs
~4% prevalence to catch anything reliably. Re-running the monitor would NOT have answered this
question, which is why an exhaustive one-off was needed. Not fixed here.
**Note:** 2,861 impressions / 58 clicks per month sit on those 226 `noindex` pages. Correct per
current policy, but it is measurable demand being deliberately declined — a policy question for
Will, not a defect.
**Files:** none (measurement + this entry)
**Recurrence:** First

## [BLOB-NON-IMAGE-WRITE] Any HTTP 200 became a "photo" — 3D tour pages stored as .jpg — 12:20 AEST
**Symptom:** Files under `/data/blobs/property-images/` carrying a `.jpg` extension and ~200 KB of
HTML. Each is a **Matterport 3D Showcase page** for the correct property, so each affected listing's
gallery renders one broken image. Found while auditing image derivatives; first mis-described as
"corrupt error pages" — they are not corrupt and the fetch never failed.
**⚠ Scope corrected twice.** Reported first as 6 files. A full sweep of **all 1,127,148**
image-extension files across **every** blob container found **10**, on 10 distinct properties, all
still live in `property_images` today. The 4 extra were missed because the earlier check covered only
`for_sale` in the three target suburbs: **2 are in Reedy Creek** (outside the target set) and 2 are
`listing_status: sold` documents whose blobs still sit under the `for_sale/` prefix. Every other
container — `cadastral` (461,098), `gold_coast` (309,828), `sold` (40,127), `aerial` (15,393),
`case-studies`, `off-market-reports`, `article-charts` — is **clean at 0**.
Affected: robina 4, varsity_lakes 3, reedy_creek 2, burleigh_waters 1.
**Root cause:** The virtual-tour URL is *in Domain's source image list*
(`property_images_original[46]` on 5 Chelsea Pl, Robina is a `my.matterport.com/show?m=…`).
`download_images_to_blob.py:download_single_image` returns `resp.content` for **any** HTTP 200 with
no content-type or magic-byte check, and `blob_storage.upload` was called with a hard-coded
`content_type='image/jpeg'`. Extension, declared type and HTTP status all agreed it was an image;
only the bytes disagreed, and nothing read them.
**Fix:** Guarded the **choke point**, not the two scripts the defect was found in. `blob_storage.upload`
now sniffs magic bytes whenever `content_type` starts with `image/` and **refuses** the write
otherwise, returning `None` — which every caller already treats as a failed upload and drops from the
list, so an impostor can never reach a gallery. This covers all **11** image-uploading callers
(`image_derivatives`, `step117_satellite_analysis`, `run_satellite_bulk`, `backfill_sold_fullres_photos`,
`archive_gold_coast_images`, `mirror_full_res_photos`, the `property_reports/*` writers) rather than
the 2 named in the handoff. Added `sniff_image_format()` covering JPEG/PNG/GIF/BMP/TIFF/ICO/WEBP/
AVIF/HEIC, plus `is_non_image_url()` in the downloader to skip known tour and video hosts before the
fetch — which also keeps Matterport's `auth=Bearer` token off the wire.
**⚠ Validation that mattered:** a naive "must start with `\xff\xd8\xff`" check would have been a
disaster. Sniffing 4,000 randomly sampled live blobs: **3,050 are WEBP, 806 JPEG, 135 PNG, 9 GIF** —
i.e. **76% of files named `.jpg` and served as `image/jpeg` are actually WEBP**. Neither the extension
nor the declared content-type carries information here. Measured **0 false rejections** across those
4,000, and all 6 known impostors correctly refused.
**Blast radius bounded — the exposure is fully materialised, not growing.** Histogram of **all
364,148** URLs in `property_images_original` across `Gold_Coast`: 205,736 `bucket-api.domain.com.au`,
158,247 `rimh2.domainstatic.com.au`, **10 `my.matterport.com`**, **8
`www.domain.com.au/virtual-viewer/`**. Only two non-CDN hosts exist in the entire corpus. The 10
Matterport URLs map **1:1 onto the 10 bad blobs** — there is no undownloaded backlog. The 8
virtual-viewer URLs **never produced a blob** (that fetch fails and is dropped), so Matterport is the
only host that actually trips the defect; the skiplist covers virtual-viewer by **path** anyway, since
it is one upstream change away from returning 200 and we must not blanket-block `www.domain.com.au`.
Residual exposure is new listings only, at ~0.003%.
**Not done — needs Will's call:** repairing the 10 existing blobs. `scripts/repair_non_image_blobs.py`
is written and compiles (`--scan` / `--dry-run` / `--apply`); it matches by **URL, never by index**
(the arrays are already not 1:1 — 48 originals vs 47 stored), moves bad blobs to `.notanimage` rather
than deleting, redacts Bearer tokens in all output, and can optionally preserve the discarded
Matterport link as `virtual_tour_url` instead of throwing away genuinely useful content. **Held**
because an A/A period is live on PostHog flag `property_page_v2` (id 817007) and changing a property
page's photo list mid-window adds a confound.
**Files:** shared/blob_storage.py, scripts/download_images_to_blob.py,
scripts/repair_non_image_blobs.py (new)
**Recurrence:** First

## [UNIT-FLAG-OR-TRUE] `or True` rewrote 11,565 docs nightly on a serverless tier — 12:25 AEST
**Symptom:** `flag_unit_indexable.py` (daily 05:00) issued a `$set` for every attached dwelling it
evaluated, every run — ~11,565 writes a night to persist a value that had almost always not changed.
**Root cause:** `if bool(d.get("unit_indexable")) != ok or True:` — the trailing `or True` makes the
change test unconditionally true. Found incidentally while tracing sitemap counts.
**Fix:** Removed `or True`. Also widened the comparison to include `unit_indexable_reason`: simply
deleting `or True` would have introduced a *stale reason* bug, because the same `$set` writes `reason`
and `ok` can stay constant while `why` changes. Dry-run before and after both report **4,980
indexable of 11,565 evaluated** — decisions are unchanged, only the redundant writes are gone.
**Files:** 15_Off-Market/Units/scripts/flag_unit_indexable.py
**Recurrence:** First

## [STATUTORY-COMPS-SET] A second, statutory comparable set on unit pages — 15:40 AEST
**Why:** Will asked how a published range of $707,000–$930,000 is justified when the
comparables table under it spans $802,516–$875,548, and whether comps older than six months
sit right with the Property Occupations Act. Both are fair, and the honest answer to the
second was that we were ASSERTING the older same-building sale is better evidence without
having measured it against the alternative.
**Built:** a second set on the same page — same bedroom count, within 5km, settled in the last
six months, each sale adjusted to the subject (POA 2014 (Qld) Sch 2). Shown ALONGSIDE the
same-complex set, never instead of it, and it never feeds the range.
**The measurement, which is the point.** Leakage-free, both methods predicting the same 1,604
sales with only prior sales visible, scored by the SAME primitives (`load`, `build_index`,
`deflate_to`, `predict` imported from the existing backtest, not reimplemented):
```
                          median    MAE    within-10%
  sales in this building    5.8%    9.4%     67.2%
  recent sales within 5km   9.0%   14.6%     54.7%
```
Same-complex is closer on only 54.4% of individual sales, but the MAE gap (9.4 vs 14.6) is far
wider than that split implies: the statutory set is not uniformly worse, it is much worse **in
the tail**. It is also far more dispersed — the adjusted sales span a median 43% of their own
median, because "a 2-bedroom attached dwelling within 5km" covers a beachside apartment in
Burleigh Waters and a townhouse in western Robina alike.
**Distance needed geometry we did not have.** Per-unit coordinates exist on **0.7%** of indexed
attached stock. Geocoding 4,967 unit addresses would be slow, paid and *wrong in a specific way*
— a geocoder resolves `12/45 Smith St` to the street, so every home in a tower gets a different
point implying precision we lack. The scheme centroid is not a workaround, it is the CORRECT
geometry: every home in one building shares one location. `ingest_scheme_centroids.py` located
**1,964 of 1,964 schemes (100%)**, covering 100% of indexed units. Verified against real
geography (Robina→Burleigh Waters 4.04km, Robina→Varsity Lakes 2.39km).
⚠ `returnCentroid=true` is a documented ArcGIS parameter this service SILENTLY IGNORES —
HTTP 200, well-formed features, `"centroid": null` on every one. Geometry is fetched and the
centroid computed here (shoelace, area-weighted; a vertex mean drifts on curved boundaries).
**Two rejected shortcuts, recorded so they are not re-proposed:**
  * *Accept a 3-bed comp for a 2-bed subject, adjusted by the bedroom step.* The step looks
    stable (~1.22-1.25x) but those medians conflate bedroom count with floor area, car spaces,
    aspect and building age — the "adjustment" would be mostly confound. Same bedroom count is
    REQUIRED; where three cannot be found we decline.
  * *Rank comparables by closeness to the expected price.* That selects the evidence to agree
    with the answer and reports the agreement as accuracy. Ranking is distance / floor area /
    bathrooms / recency only — every term verifiable without knowing any sale price.
**Files:** `15_Off-Market/Units/scripts/{ingest_scheme_centroids,statutory_comparables,
precompute_statutory_comparables,backtest_statutory_comparables}.py`,
`v4/StatutoryCompsSection.{tsx,module.css}`, `OffMarketV4.tsx`, `types.ts`,
`db.server.ts`, `off-market.$slug.tsx`, `research/STATUTORY_COMPARABLES.md`
**Recurrence:** first.

## [RANGE-VS-EVIDENCE-COPY] The band exceeded every comparable shown — 15:55 AEST
**Symptom (Will's):** the page published $707,000–$930,000 while the comparables table beneath
it ran $802,516–$875,548. Nothing on the page supported the top number.
**Root cause:** two different quantities rendered as one range. `low`/`high` are the point
estimate ± the measured error band; the table shows the adjusted sales. The band exceeds the
evidence spread on **81% of pages** (median 1.2x). Presenting them as a single range implies
the sales support the edges. They do not, and a reader who checked was right to ask.
**Fix:** the two are now stated separately and in order — *the sales, adjusted to this home, ran
$803,000–$876,000 · our central estimate is $819,000 · tested against 625 Robina attached sales
an estimate like this lands within ±13.6% four times in five, so the honest range is
$707,000–$930,000.* Added `adjusted_low`/`adjusted_high` to the valuation record (computed over
the comps the point is actually derived from, not the 8 displayed). The accuracy section no
longer repeats the band's derivation but explains why it is WIDER than the sales rather than
drawn from them.
**Files:** `unit_valuation.py`, `precompute_unit_valuations.py`, `UnitValuationSection.tsx`,
`UnitValuationSection.module.css`, `types.ts`, `db.server.ts`
**Recurrence:** first. Approved by Will before shipping — it is copy on ~5,000 live pages.

## [STATUTORY-ASSERTION-SCOPE] My own Rule 7b guard broke Rule 8 — 16:05 AEST
**Symptom:** first two runs of `precompute_statutory_comparables.py` both aborted on their own
assertions. Neither was a real regression; both were defects in the guard.
**Run 1:** availability 54.5% "outside the 55-98% band measured at 86.3%". The 86.3% was
measured over INDEXED units (4,980). The job runs over ALL attached stock (11,565), which
includes 4,143 dwellings with no bedroom count and 1,086 with no scheme — neither of which has
a page. **The guard compared a figure from one population against a threshold measured on
another** — precisely the "scope the sample" rule (Rule 8 #5) it exists to enforce.
**Run 2:** "0 indexed units seen". `PROJ` is imported from `unit_page_data` and does not carry
`unit_indexable`, so every document read as not-indexed.
**Fix:** track indexed and total separately, project `unit_indexable` explicitly, assert on the
indexed subset — the population that is actually published.
**Worth keeping:** both failures were the assertions doing their job. A guard that has never
fired is a guard that has never been tested, and each of these would otherwise have been a
silent wrong number in a heartbeat metric.
**Files:** `15_Off-Market/Units/scripts/precompute_statutory_comparables.py`
**Recurrence:** first for the scope error; the projection omission is the same shape as
`[BUILDER-ENV-EXPORT-GAP]` — a field absent from a projection is indistinguishable from a field
absent from the data.

## [SITEMAP-LOG-INVISIBLE-MANUAL] Manual sitemap runs logged nothing; cron entries undated — 12:40 AEST
**Symptom:** `logs/sitemap-regen.log` recorded the 06:15 run writing 12,430 URLs while the LIVE
sitemap served 17,338 — an apparent ~4,900-URL discrepancy, on top of an off-market count that had
swung 15,351 → 10,793 → 15,703 across recent runs. This read as a nondeterministic generator, and an
earlier note claiming `[OFFMARKET-MULTILOT-OSCILLATOR]` was "confirmed stable" looked wrong.
**Root cause — the discrepancy was an artefact, not a defect.** Every count change was a dated,
deliberate code change: `1ab93aba` (−4,558, the `[OFFMARKET-UNIT-SITEMAP-MISMATCH]` fix removing
noindex-serving unit URLs) and `e526c995` (+4,908, attached dwellings made indexable via
`unit_indexable`), the latter followed by a **manual** `regenerate-sitemap.sh` at 09:19. Both moved
sitemap and robots *into* agreement. The real oscillator was fixed 2026-08-07 and has stayed fixed —
**the 08-08 "confirmed stable" claim still holds.** What made it look otherwise: (a) the
`>> sitemap-regen.log` redirect lives in the **crontab line**, not the script, so the manual run
wrote nothing at all; (b) the log has **no timestamps**, so entries could not be ordered against
commits; (c) the commit message hardcodes `[VM cron]`, so the manual run's own commit lied about its
provenance. Three independent gaps, all in the audit trail rather than the generator.
**Fix:** `regenerate-sitemap.sh` now writes timestamped marker lines (`START` / `COUNTS` / `EXIT rc=`)
to the log from **inside** the script, so a run is recorded however it was invoked, and detects its
invocation source from `$PPID` — the commit message now reads `[VM cron]` or `[VM manual:…]`
truthfully. No crontab change needed; cron's verbose stdout still flows through the existing redirect.
**Verified:** `bash -n` clean; invocation labelling exercised (correctly reports `manual:bash` under a
shell parent).
**⚠ Not fixed — needs Will's call:** the push guards only catch `PROP_COUNT < 100` and
`OFFMARKET_COUNT == 0`. **Both ±4,500 URL moves sailed straight through.** A percentage-movement guard
was recommended but not added, because a threshold tight enough to catch drift would have blocked two
*legitimate* changes — that is a policy decision, not a code one.
**Also unmeasured:** the 06:15 cron has never autonomously produced 15,703 (today's 4,980
`unit_indexable` flags came from a manual run; the 05:00 flag cron was added today and has not fired).
First end-to-end autonomous proof is 2026-08-14 05:00 → 06:15 → 07:00. Until then "stable at 15,703"
is a prediction, not a measurement.
**Files:** Feilds_Website/01_Website/scripts/regenerate-sitemap.sh
**Recurrence:** First

## [DR-WEBSITE-UNCOVERED-FOLLOWUP] Orphans backed up; the 3 website diffs diagnosed — 09:55 AEST
**Orphans fixed (Will's call: orchestrator repo, no deploy).** `sirens.js` + `powerAudio.js` pushed
to `15_Off-Market/Concepts/Break_glass_emergency/workbench/` (their own symlink paths). Chosen over
the website repo because both are unimported — pushing them there would spend a Netlify deploy on
dead code.

`link_is_really_backed_up()` gained a second branch: content backed up into THIS repo at the
symlink's own path. `git hash-object` follows the symlink, so it compares **content** — if the live
file is later edited, the copy goes **STALE** and says so, instead of vouching for an out-of-date
backup forever. Verified both branches: unchanged → `(True, "content backed up in this repo")`;
simulated post-backup edit → `(False, "STALE — holds an OLDER copy; re-push to refresh")`.
Symlink status is now **10/10 VERIFIED**, up from 8/10.

**The 3 website diffs — diagnosed, and my earlier read was WRONG.** I had said all 3 looked like
local-behind-remote. Checking commit dates against local mtimes reversed two of them:

| file | remote last commit | local mtime | direction |
|---|---|---|---|
| `src/main.tsx` | 2026-02-23 (initial import) | 2026-03-15 | **local NEWER** |
| `MarketIntelligenceSection/index.ts` | 2026-02-23 (initial import) | 2026-03-15 | **local NEWER** |
| `public/off-market-v3/intro.js` | 2026-08-04 18:44 AEST | 2026-08-04 18:52 | **local newer by 8 min** |

Corroborated, not inferred from mtime alone: `package.json` declares `react-router: ^7.13.1` and
**no `react-router-dom` dependency at all**; 41 source files import `"react-router"`, **zero** import
`react-router-dom`. The REMOTE `main.tsx` imports a package that is not a dependency — it is the
pre-migration copy. Local is correct. This is exactly why the checker prints `DIFF (direction
unknown)` and refuses to auto-push: an automated "sync" in either direction would have been wrong
for at least one of these.

**Severity: all three are cosmetic or dead.**
- `src/main.tsx` is **vestigial**. `react-router.config.ts` sets `ssr: true, appDirectory: "src"`, and
  `entry.client.tsx` / `root.tsx` / `routes.ts` all exist — framework mode never compiles `main.tsx`
  (or the `index.html` that loads it). That is why a remote copy importing a non-dependency has never
  broken a build.
- `MarketIntelligenceSection/index.ts` — single vs double quotes, semantically identical.
- `public/off-market-v3/intro.js` — comment text only. Verified functionally identical by diffing
  with comments and blank lines stripped.

**Recommendation (not actioned):** push all three, **bundled with the next website change** rather
than as a deploy of their own — the fix is worth doing but not worth a dedicated Netlify deploy. It
makes the DR copy self-consistent and returns the tree baseline to 908/0, so any future `DIFF` line
is real signal rather than known noise. Separately, `main.tsx` + `index.html` look like dead
pre-migration files worth deleting outright; that is a bigger call and should not ride along with
this.

**Files:** `scripts/check_unpushed_code.py`; GitHub `Fields_Orchestrator/…/workbench/{sirens,powerAudio}.js` (new)
**Recurrence:** follow-up to `[DR-WEBSITE-UNCOVERED]` above.

## [SPAWN-ENV-FILE-ESCAPE] Stripping credentials from the child env was not a boundary — 12:20 AEST
**Symptom:** After [SPAWN-GH-TOKEN-ESCAPE] was "fixed" by stripping GITHUB_TOKEN from the child
env, an adversarial review found spawned sessions could still obtain every production credential.
**Root cause:** `.env` is mode 644 and readable by the same uid the session runs as — and the
prompt ITSELF instructed `set -a && source .env`. A benign session following its own instructions
re-acquired GitHub admin+push, Netlify, Facebook Ads, Google Ads, OpenAI and the rest. Env
stripping cannot be a boundary while the process can read the file the env came from. Third time
this control was asserted at one layer while the capability leaked through another.
**Fix:** Real sandbox. Every session now runs under `unshare -rm` (unprivileged user + mount
namespace) with `/dev/null` bind-mounted over `.env`, so the file cannot be read at all. Cosmos
still works deliberately — `shared.db` falls back to `config/settings.yaml`, which is left visible,
so sessions keep DB access and lose API tokens. `investigate` scope additionally remounts
`/home/fields` read-only: Bash is granted, so the live tree (~1,000 dirty files, one
`git checkout .` from unrecoverable) was never protected by withholding Write/Edit. Sessions write
results to /tmp, which stays writable. The `source .env` line is removed from the prompt and
replaced with an explicit statement that credentials are absent by design.
**Files:** scripts/spawn_worker.py
**Recurrence:** 3rd (see [SPAWN-GH-TOKEN-ESCAPE] same day) — the first control to be tested rather
than asserted.

## [SPAWN-DIFFSTAT-HEAD1] Patch verification always passed; every worktree leaked — 12:24 AEST
**Symptom:** The Rule 7b guard that downgrades a patch task reporting "resolved" with an empty
worktree could never fire, and no worktree or branch was ever cleaned up.
**Root cause:** `_worktree_diffstat` diffed `HEAD~1..HEAD`. In a worktree created at HEAD that is
the repo's last PRE-EXISTING commit, not the session's work — measured non-empty (7 files, 348
insertions) on a worktree where nothing had been done. So `keep=bool(diffstat)` was always True and
`_cleanup_worktree` returned immediately every time.
**Fix:** Capture the base SHA at worktree creation and diff `base..HEAD` plus porcelain status.
**Files:** scripts/spawn_worker.py
**Recurrence:** First

## [SPAWN-BRANCH-COLLISION] Patch retries failed 100% and blamed the wrong thing — 12:26 AEST
**Symptom:** Any retried patch task died with "worktree setup failed".
**Root cause:** Branch `spawn/<task_id>` was reused across attempts, and (per the entry above)
never deleted, so attempt 2 hit "a branch named X already exists". The recorded error blamed
worktree setup rather than the original stall. `_cleanup_worktree` also used `subprocess.run`
without `check=True`, so its `except Exception` was unreachable and failures were silent.
**Fix:** Per-attempt branch/worktree (`spawn/<id>/a<n>`), `-B` instead of `-b`, `git worktree
prune`, and explicit returncode logging on cleanup.
**Files:** scripts/spawn_worker.py
**Recurrence:** First

## [SPAWN-HEARTBEAT-OVERWRITE] Concurrent tasks erased each other's health status — 12:30 AEST
**Symptom:** With concurrency 2, a failed task finishing seconds before a successful one left the
Process Registry reading `success` with the failure's traceback gone.
**Root cause:** `job_status.record_job_result` does `replace_one({"job": job}, ...)` — one document
per job name, last writer wins. The worker heartbeated per task under the fixed name
`spawn_worker`. It also heartbeated ONLY when a task was claimed, so a worker that died with an
empty queue read OK for `168 × 1.5 = 252` hours.
**Fix:** One heartbeat for the WORKER, every 30 min, `cadence_hours=1, stale_hours=3`, carrying
pending/running/completed_24h/failed_24h. Rule 7b assertion: raise when tasks failed in 24h and
none succeeded (an upstream outage, not per-item failure). Per-task records stay in
`spawned_tasks`, which is keyed per task and cannot collide.
**Files:** scripts/spawn_worker.py
**Recurrence:** First

## [SPAWN-QUOTA-DRAIN] A Max quota outage would fail the entire queue permanently — 12:33 AEST
**Symptom (predicted, not yet observed):** On hitting the 5-hour Max limit, `claude -p` exits
non-zero in seconds. The loop would claim the next two tasks, fail them in seconds, and drain the
whole pending queue to `failed` within two poll cycles — each error reading like a bad brief, none
retried.
**Root cause:** Only `returncode` and result-file presence were inspected; no quota detection,
despite the established pattern in `scripts/samantha/max_client.py`. Direct analogue of
`[INDEXING-SILENT-ZERO]`: an upstream outage consumed as if it were per-item failure.
**Fix:** Match quota markers in the session log; on a hit, requeue the task (decrementing
`attempts`), and pause claiming for 30 min.
**Files:** scripts/spawn_worker.py
**Recurrence:** First

## [SPAWN-RETRY-DEAD] MAX_ATTEMPTS governed nothing for real failures — 12:35 AEST
**Symptom:** `MAX_ATTEMPTS = 2` implied a retry that never happened; a CLI blip or Cosmos timeout
was terminal on the first try.
**Root cause:** Every failure path wrote `status: "failed"`, and `claim_pending` only ever selects
`status: "pending"`. `attempts` governed only the crash-reclaim path.
**Fix:** Requeue genuinely transient failures (no result file AND non-zero exit) while
`attempts < MAX_ATTEMPTS`. A session that ran and reported a bad outcome is NOT transient —
re-running it would burn another 45 minutes to reach the same answer.
**Files:** scripts/spawn_worker.py
**Recurrence:** First

## [SPAWN-BLOCKED-AS-SUCCESS] "I achieved nothing" recorded as ✅ — 12:37 AEST
**Symptom:** A session writing `{"outcome": "blocked"}` — which the prompt explicitly tells it to
do — was marked `completed`, sent a ✅ Telegram and recorded a success heartbeat. `{"outcome":
"banana"}` passed too, as did a bare list or string (the latter raised AttributeError outside the
try, stranding the task in `running` until the stale sweep).
**Root cause:** `ok = bool(result) and rc == 0` never validated the outcome value, the summary, or
the JSON type.
**Fix:** `ok` now requires `outcome in (resolved, diagnosed, could_not_reproduce)`, a non-empty
`summary`, and `isinstance(result, dict)`.
**Files:** scripts/spawn_worker.py
**Recurrence:** First

## [SPAWN-TIMEOUT-UNBOUNDED] Long timeouts reintroduced the duplicate-run bug — 12:39 AEST
**Symptom:** `--timeout-s` was unbounded while the stale sweep used a fixed 5400s constant, so any
task above that would be reclaimed while a worker thread still held it — the reclaim then
`rmtree`s the worktree the live session is editing and deletes its result file.
**Fix:** Clamp `--timeout-s` to 300..5400 at enqueue, and derive the stale cutoff per task from its
own `timeout_s + 900s` margin rather than a global constant. Also added: `{status, created_at}`
index on the queue, 30-day artifact pruning, a load-average ceiling and a 20:15–02:00 AEST quiet
window so sessions never overlap the nightly pipeline (the profile behind the memory lockouts that
forced the e2-standard-4 upsize).
**Files:** scripts/spawn_worker.py, scripts/spawn_task.py
**Recurrence:** 2nd (see [SPAWN-STALE-CLAIM-TZ] same day)

## [SPAWN-BRIEF-FICTION] Brief validator checked shape, not existence — 12:12 AEST
**Symptom:** The first example brief written for spawn_task.py cited
`python3 scripts/check_sitemap_urls.py`, which does not exist, and passed every validation.
**Root cause:** All checks were length/placeholder based. Nothing verified that cited paths were
real, so a session would have spent 45 minutes chasing a command that never existed.
**Fix:** `_check_repro` / `_check_known_files` verify every cited path resolves (against the
orchestrator repo, the website checkout, or absolute) and that the repro binary is on PATH.
Deliberately static — executing an arbitrary repro at enqueue time would be worse than the problem.
`--skip-path-check` is the documented exception. On first run it immediately caught a SECOND
invention in the same brief: `01_Website/src/pages/PropertyPage.tsx`, where `PropertyPage` is a
directory and the real file is `.../PropertyPage/PropertyPage.tsx`. Also fixed: `shlex.split`
leaves shell separators glued to words (`.env;`), which failed a valid multi-command repro.
**Files:** scripts/spawn_task.py, CLAUDE.md
**Recurrence:** First

## [WORKER-AGENT-GATE] Retired full-autonomy launcher was one keystroke from live — 12:45 AEST
**Symptom:** `worker-agent/run-worker-agent.sh:73` runs `claude -p --dangerously-skip-permissions`
for 6000s with `set -a && source .env` — all 83 credentials exported into a session with the
permission engine disabled.
**Root cause:** Retired 2026-04-27 by commenting out the cron line; the script itself was left
armed and is still maintained (last touched 2026-07-22), so it reads as live. Audit confirmed by
live probe that the inherited GITHUB_TOKEN has admin:true AND push:true on both production repos.
Its stated "hard limits" live in WORKER_AGENT.md and are enforced by nothing; the prompt is also
built by concatenating `07_Focus/*.md` raw, so anything written there becomes an instruction to a
permissionless agent.
**Fix:** Hard gate at the top requiring `WORKER_AGENT_I_MEAN_IT=1`, with a header documenting the
real blast radius and pointing at the sandboxed successor. Not deleted — deliverables/ has
historical value and deletion is Will's call.
**Still open for Will:** (1) delete the commented cron line at `/var/spool/cron/crontabs/projects:99`,
which has propagated into 6+ crontab backups that could be restored wholesale; (2) scope the
GitHub PAT down from `admin` to `contents:write` — that is live risk today for every consumer
of .env, not just this script.
**Files:** worker-agent/run-worker-agent.sh
**Recurrence:** First

## [SALE-COUNTED-TWICE] One sale, two dates, counted twice — 16:40 AEST
**Symptom:** the first statutory comparable table I inspected listed **1/29 Mountain Ash Circuit
twice**, same month, same $1,300,000.
**Root cause:** a sale reaches us from several sources that disagree on the DATE. That dwelling
carries `sold_date` **2026-05-18** with `sale_price` $1,300,000, and a `property_timeline` event
on **2026-05-11** for the same $1,300,000 — seven days apart. Every module collapsed its sale
list with `set()`, which compares the whole `(date, price)` tuple, so two records of ONE sale
survive as two sales.
**Why it is worse than a missing comparable:** the sale is silently **double-weighted in the
median** while the comparable count tells the reader the evidence is broader than it is. A
missing comp is visible; a duplicated one is not.
**Fix:** `dedupe_sales()` in `unit_valuation.py`, keyed on **price within a 180-day window** —
not on date. Two genuine sales of one dwelling at an identical price within six months do not
happen; two records of one sale disagreeing by days demonstrably do. Keeps the EARLIEST date
(the later figure is typically settlement; the price was agreed at contract). Wired into all
four sale-collapsing sites: `statutory_comparables._sales_of`, `build_unit_content.sales_of`,
`backtest_unit_valuation.load`, and via those the statutory backtest.
**Scale:** 125 of 19,947 sale events (0.63%), 125 homes. The recent-sale pool fell 123 -> 109
with no loss of coverage (statutory availability held at exactly 86.3%).
**⚠ THE PUBLISHED ACCURACY FIGURES MOVED AND HAD TO BE RE-MEASURED.** These render verbatim on
~5,000 live pages ("tested against 596 Robina attached sales"), so they cannot be left stale:
```
                    before (dup + no floor area)      after (deduped, floor area working)
  robina            n=625  median 6.27  band 13.63    n=596  median 6.4  band 13.6  <10% 67.4
  varsity_lakes     n=992  median 4.95  band 14.82    n=967  median 4.8  band 14.6  <10% 69.1
  burleigh_waters   n=167  median 10.84 band 20.32    n=155  median 11.3 band 21.9  <10% 46.5
  statutory (all)   n=1604 median 9.0   <10% 54.7     n=1542 median 9.1  <10% 54.1
```
Burleigh Waters stays below the WEAK_WITHIN10=55 publication bar, so no page changed status.
Every citation of the old figures was updated in the same pass — code, component docblocks,
types, loader and `research/STATUTORY_COMPARABLES.md`.
**Files:** `unit_valuation.py`, `statutory_comparables.py`, `build_unit_content.py`,
`backtest_unit_valuation.py`, `precompute_statutory_comparables.py`, plus the citation updates.
**Recurrence:** first. Related in shape to `[RENTAL-AS-SALE]` and
`[UNIT-NUM-ALIAS-EATS-FLOOR-AREA]`: all three are "what counts as a sale" defects that produce a
plausible number rather than an error.

## [SPAWN-MOUNT-SHADOW] Sandbox v3 defeated — a plain bind shadows the nested credential hide — 12:55 AEST
**Symptom:** The `unshare -rm` sandbox from [SPAWN-ENV-FILE-ESCAPE] was tested by an adversarial
spawned session, which read the real `.env` in full (8,132 bytes, all ~40 live secrets), extracted
`GITHUB_TOKEN` and authenticated to GitHub as Will954633 — from inside the supposedly sealed
investigate scope.
**Root cause:** Mount ordering. `_sandbox_wrap()` did `mount --bind /dev/null .env` FIRST, then for
investigate scope `mount --bind /home/fields /home/fields` + `remount,ro`. A plain (non-recursive)
bind of a subtree does not carry nested mounts, so the fresh whole-tree bind overlaid and hid the
`/dev/null` mount, presenting the real file underneath. `findmnt .env` showed SOURCE
`devtmpfs[/null]` — the bind existed — while `stat .env` returned a regular 8,132-byte file. The
tell was visible and contradictory, and only a live probe surfaced it. Note the leak was unique to
`investigate`, the MORE restricted scope, because `patch` performs no second bind.
**Fix:** Bind `/dev/null` over `.env` LAST, after the read-only tree bind, so the credential hide is
the topmost mount (verified: `--rbind` on the tree also works; last-bind is the smaller change).
Added a runtime self-check inside the namespace that aborts with exit 91 before starting the
session if `.env` is non-empty, and worker handling that treats 91 as an infrastructure fault —
requeue the task, do not spend an attempt, pause claiming.
**Files:** scripts/spawn_worker.py
**Recurrence:** 4th version of this control ([SPAWN-GH-TOKEN-ESCAPE], [SPAWN-ENV-FILE-ESCAPE], this).
**Lesson:** every version of this boundary that was reasoned about was wrong; every version that was
probed got fixed. The self-check exists so the system stops asserting and starts observing.

## [TWO-TABLES-ONE-SALE-TWO-DATES] The two comparable tables contradicted each other — 17:25 AEST
**Symptom:** caught by reading the DEPLOYED page, not by any test. On
`/off-market/187-easthill-drive-robina`, **159 Easthill Drive's $1,425,000 sale appeared in both
comparable tables with different dates and different adjusted figures**:
```
  same-complex table   2026-04   $1,425,000 -> $1,425,000
  statutory table      2026-03   $1,425,000 -> $1,523,676
```
Same sale, same price, one page, $98,676 apart. A reader comparing the two tables — which is
exactly what showing both invites — sees a contradiction and has no way to resolve it.
**Root cause:** the two pipelines disagreed on which date is canonical for a sale recorded twice.
`_sales_in` picks the most recent event for a dwelling and took the LATER of the duplicate pair;
`dedupe_sales` (added earlier today) collapses the pair and keeps the EARLIER. Different date ->
different index quarter -> different time adjustment.
**This is the same underlying data defect as [SALE-COUNTED-TWICE], and fixing that one is what
made this one visible.** Deduping the statutory pool moved its date while the same-complex path
kept the old one, so a previously-hidden inconsistency surfaced as a visible disagreement.
**Fix:** `_sales_in` now runs `dedupe_sales` over the events BEFORE taking the most recent, so
both pipelines report the same canonical date. Verified: 159 Easthill is now 2026-03-09 ->
$1,523,676 in both.
**Files:** `15_Off-Market/Units/scripts/unit_valuation.py`
**Worth noting:** the guard that would have caught this does not exist. `check_renderer_consistency.py`
compares the two RENDERERS against one data source; nothing compares two data sources that
describe the same sale. Adding both comparable sets to one page created that requirement, and it
was only found by reading the rendered output.
**Recurrence:** 4th in the "what counts as a sale" family today ([RENTAL-AS-SALE],
[UNIT-NUM-ALIAS-EATS-FLOOR-AREA], [SALE-COUNTED-TWICE], this).

## [SPAWN-SANDBOX-SURFACE] Sandbox v4 defeated five more ways — hiding one file is not a boundary — 13:10 AEST
**Symptom:** After the mount-ordering fix, a second adversarial spawned session confirmed `.env`
itself was correctly `/dev/null` — then defeated the sandbox five other independent ways, three of
which handed over the same production credentials the fix was meant to protect.
**Root cause:** Five distinct holes, all from protecting one path instead of a surface:
  (a) the session runs as uid 0 inside the user namespace and could simply `umount` the bind
      (exit 0), re-exposing the real 8,132-byte file — the exact inode from the previous finding;
  (b) `.env.bak-20260721165131` and `.env.bak-20260807-092602` sat unshadowed in the SAME directory,
      full copies of every secret;
  (c) sibling project env files were wide open — `Feilds_Website/01_Website/.env` carries live
      SQUARE PRODUCTION tokens, `fields-automation/scripts/.env` the Cosmos primary key and
      ANTHROPIC_API_KEY;
  (d) `/proc/<ppid>/environ` leaked the spawn worker's own environment (POSTHOG_ALL_ACCESS_KEY,
      GOOGLE_VISION_SA_KEY);
  (e) `/home/projects/.config/gh/hosts.yml` is mode 664 with the GitHub oauth_token in plaintext.
The session authenticated to GitHub as Will954633 via a token read from an unshadowed backup.
**Fix:** `_credential_paths()` sweeps every `.env*` / `*.env` under /home/fields (34 files) plus
gh's hosts.yml and any .git-credentials, and binds /dev/null over ALL of them, after the tree bind.
`unshare -rmpf --mount-proc` adds a private PID namespace with a fresh /proc, closing (d).
`setpriv --bounding-set=-sys_admin` drops CAP_SYS_ADMIN before the session starts, so umount and
remount are impossible for it and everything it spawns, closing (a). The exit-91 self-check now
tests the whole credential set, not one file. Verified by live probe: .env 0 bytes, .env.bak 0,
website .env 0, gh hosts.yml 0, 4 processes visible, umount blocked, writes read-only, Cosmos
reachable.
**Files:** scripts/spawn_worker.py
**Recurrence:** 4th version of this control ([SPAWN-GH-TOKEN-ESCAPE], [SPAWN-ENV-FILE-ESCAPE],
[SPAWN-MOUNT-SHADOW], this).
**⚠ SEPARATE FINDINGS FOR WILL — these are VM-wide hygiene, not spawn-system bugs, and are live
risk today for every process on this box, not just spawned sessions:**
  1. `/home/projects/.config/gh/hosts.yml` is world-readable (664) with a plaintext GitHub token.
  2. Two `.env.bak-*` files sit beside .env containing every current secret.
  3. `Feilds_Website/01_Website/.env` contains live Square PRODUCTION access tokens.
  4. The GitHub PAT carries `admin:true` on both production repos where `contents:write` would do.
**Lesson:** the write boundary held under every attack including uid 0; the credential boundary
failed every time it was reasoned about and got fixed every time it was probed. Treat the former as
proven and the latter as defence-in-depth.

## [WEBSITE-VESTIGIAL-ENTRYPOINT] Reference audit before deleting main.tsx + index.html — 10:20 AEST
**Not a fix — a pre-deletion check, recorded so the evidence isn't re-gathered.** Asked to confirm
what references the pre-migration SPA entry point before removing it.

**Decisive evidence that both are dead:** `build/client/index.html` **does not exist**. The build is
`npx react-router build` publishing `build/client` with `ssr: true` — the SSR build never consumes
the source `index.html`, so the `<script src="/src/main.tsx">` inside it is never a build input.
That is measured from the actual build output, not inferred from the config.

**Every reference, and what it is:**
| reference | verdict |
|---|---|
| `index.html:88` → `/src/main.tsx` | the dead file referencing the dead file |
| `dev-harness/index.html` → `./main.tsx` | a **different** main.tsx (`dev-harness/main.tsx`); unaffected |
| `01_New_Versions/August_2026/*` | the August rebuild prototype's own `index.html`; unrelated |
| `netlify.toml:265` `[[headers]] for = "/index.html"` | dead rule — no such file is ever published |
| `src/utils/gtagConversions.ts:5` | **stale comment**: "Requires the Google Ads tag loaded in index.html". The tag lives in `root.tsx` |
| `vite.config.ts` | no `rollupOptions.input`, no html entry at all |
| `scripts/`, `netlify/` | no references |

**Checked for a hidden dependency and found none — but nearly a false alarm.** `index.html` carries
tags `root.tsx` does not: it still has the **Contentsquare** loader. That looked like "deleting this
kills tracking" until checked — `root.tsx:303` records *"Contentsquare removed 2026-07-16 — redundant
with PostHog session replay + heatmaps"*. It is deliberately retired, absent from the shipped bundle,
and the copy in `index.html` is dead code inside a dead file. Every other tag
(`AW-17993173310`, `G-5C5VS27X42`, `fbq`, `gtag`) is present in `root.tsx`; `root.tsx` additionally
has `posthog`, which `index.html` never did.

**Conclusion: safe to delete `index.html` + `src/main.tsx`.** Nothing that builds, ships or deploys
reads either. Two tidy-ups belong with it: the dead `netlify.toml` `/index.html` headers rule, and
the stale `gtagConversions.ts` comment pointing at a file that will no longer exist.

**Recommendation:** bundle the deletion with the same next website change as the 3 `DIFF` pushes —
same single Netlify deploy, no dedicated one. Note deleting `main.tsx` also resolves one of those 3
diffs outright.

## [VM-CREDENTIAL-HYGIENE] Credential exposure surface reduced across the VM — 12:40 AEST
**Symptom:** Adversarial testing of the spawn sandbox surfaced four credential-exposure problems
that were NOT spawn-system bugs — they were live for every process on this box.
**Fix (all applied, Will approved):**
  1. `/home/projects/.config/gh/hosts.yml` was mode 664 (world-readable) with the GitHub
     oauth_token in plaintext → `chmod 600`. ⚠ `gh` may rewrite this on `gh auth` operations;
     re-check after any auth change.
  2. `.env.bak-20260721165131` (77 vars) and `.env.bak-20260807-092602` (83 vars) — full copies of
     every current secret sitting beside `.env` → deleted. Both were already 600, so the exposure
     was to same-uid processes, which is exactly what the sandbox had to defend against.
  3. `Feilds_Website/01_Website/.env`, `Fields_Orchestrator/.env`, `fields-automation/pipeline/.env`
     → `chmod 600`. (The automation one is reached via a symlink from `scripts/.env`; the symlink's
     own 777 mode is irrelevant on Linux — the target is what counts.)
  4. `worker-agent/run-worker-agent.sh` deleted locally and from GitHub, and its commented cron line
     removed from `crontab -l` (311 → 310 lines, 98 active entries verified intact). Backups at
     `/home/fields/backups/2026-08-13-security/`. CLAUDE.md Rule 9 updated — the file it warned
     against no longer exists.
**⚠ STILL OPEN — needs Will (cannot be done from here, PAT creation is UI-only):**
  The GitHub PAT has `admin: true` on both production repos. Measured write surface across the whole
  fleet is far narrower: 6 `PUT` calls, all to `contents`, plus read-only `actions/runners` and
  `actions/workflows` on fields-automation. Nothing pushes `.github/workflows`. So a fine-grained PAT
  scoped to **Contents: Read+Write; Actions: Read; Metadata: Read** over Website_Version_Feb_2026,
  Fields_Orchestrator, fields-automation (+ fields-ceo-sandbox, fields-local-photography if still
  used) would cover every current consumer with no `admin`.
**⚠ CORRECTION TO A STATED ASSUMPTION:** Will said Square was not in use, so the plan was to strip the
Square production tokens. It is NOT unused — `netlify/functions/square.mjs` and `square-webhook.mjs`
implement the **$15 off-market paid report unlock**, wired live into `src/routes/off-market.$slug.tsx`.
Tokens left in place; file permissions tightened only. Likely reading: no payment has ever come
through (pre-revenue), but the code path is live in production. Rotation still worth considering
since the file was previously group/world-readable.
**Files:** CLAUDE.md, worker-agent/run-worker-agent.sh (deleted), crontab
**Recurrence:** First

## [INTERNAL-FLAG-NO-UNSET] Internal-tester flag was permanent and silenced the browser entirely — 12:40 AEST
**Symptom:** Will loaded a property page to verify the new `page_engagement` event and produced
**zero** events. He assumed his IP was filtered.
**Root cause:** Not IP. PostHog's `test_account_filters` are query-time only and discard nothing at
ingestion. The cause is `fields_internal` in localStorage: set permanently by visiting any URL with
`?fields_internal=1`, read by BOTH the pre-init inline script in `root.tsx` (feeding
`opt_out_capturing_by_default`) and `phCaptureUtmAndReferrer` (calling `opt_out_capturing()`), and
**with no un-set path anywhere in the codebase**. Any device that ever opened such a link — a demo
laptop, a phone, a forwarded link — goes permanently silent, and because it sends nothing you cannot
even count how many real visitors were caught.
**Fix:** `?fields_internal=0` now clears it, handled in BOTH places. The inline script must clear the
key too, because it runs first and feeds `opt_out_capturing_by_default` — clearing it only in
`posthog.ts` would still leave `init()` opted out for the whole session. `opt_in_capturing()` is
called as well, since PostHog persists its own opt-out separately from our key.
**Files:** `src/utils/posthog.ts`, `src/root.tsx`
**Recurrence:** First.

## [PROPERTY-SRCSET] Listing photos now served as pre-generated WebP renditions — 12:45 AEST
**Symptom:** 12,979 derivatives existed on disk across the three target suburbs and nothing requested
them; pages still shipped full-res originals.
**Fix:** `buildPhotoSrcSet()` / `withPhotoSrcSets()` in `netlify/functions/shared-utils.mjs`, emitted
additively from `property.mjs` as `property_images_srcset` (the existing `property_images` string
array is untouched, so every other consumer keeps working), and consumed on the hero `<img>` with
`sizes="100vw"`. The original remains `src`, so a photo with no rendition still renders.
⚠ The helper refuses to guess: it emits only the widths in `image_derivative_widths` on the listing
document — the INTERSECTION across every photo — because a 404 inside `srcset` does NOT fall back to
`src`, it fails the image. Non-blob URLs, empty/missing widths and extension-less URLs all return ''.
**Verification:** unit-tested all six edge cases (3 widths, 2 widths, empty array, undefined, Domain
CDN URL, no extension). `npx vite build` clean. ⚠ **Not yet verified live** — no browser check that
a real page requests the .webp and the bytes drop.
**Files:** `netlify/functions/shared-utils.mjs`, `netlify/functions/property.mjs`,
`src/pages/PropertyPage/PropertyPage.tsx`
**Recurrence:** First. Completes step 4 of `15_On_Market/03_Audit/IMAGE_DERIVATIVES_SPEC.md`.

## [GENRL-WEEKLY-REDESIGN] General RL rebuilt around a weekly cadence and a capped recommendation ledger — 12:50 AEST
**Symptom:** The General RL system (`16_General_Reinforcement_Learning/`) was paused by Will on 2026-07-30 after two days of operation. In 48 hours it produced 27 cycles and 31 human-decision items; `WILL_TO_ACTION.md` reached 1,773 lines with 48 items OPEN (84% of everything ever raised), including two pairs of duplicate findings (WTA-OPS-007≡023, WTA-OPS-008≡014). Will's report: "its cycles were way too fast" and it "becomes out of touch with what I'm doing."
**Root cause:** Three faults, only one of which was cadence.
  1. *Self-pacing.* `cycle_pacer.py` allowed 14 runs/day PER DOMAIN; dispatchers polled every 20 min 08:00-22:00; every prompt ended "CHAIN 20-45 min if actionable work in hand". Six domains each held their own Telegram line to Will. The pacer optimised compute (cheap) and spent attention (scarce).
  2. *No closure.* The output was an append-only essay ledger with no cap, ranking, expiry or dedup. Items revised themselves in place via addenda, so knowing what was currently true required reading each item's revision history.
  3. *The loop never closed.* No domain read `logs/fix-history/` as INPUT (only wrote to it); no domain learned Will's verdicts; and the `rl_<domain>_actions` collections that five prompts were told to consult to avoid repeating themselves **had no writer at all** — no Python creates them. It was an idea generator, not a learning system.
**Fix:**
  - `recommendations.py` (new) — ledger in `system_monitor.rl_recommendations` enforcing MAX_OPEN_PER_DOMAIN=2 in code (`propose` refuses a third), verdict+REASON capture, `feedback --domain` to replay Will's reasons to the domain next cycle, and `ship`/`due-for-grading`/`grade`/`stats` for outcome grading. A 48-item backlog is now unrepresentable.
  - `fix_digest.py` (new) — indexes `logs/fix-history/` (~1,000 lines/day) down to one line per entry with `--domain` filtering, `--full ID` drill-down and `--recurring`. Pure-Python matching (shell grep is ugrep on this VM). 7-day window: 263 lines vs ~7,000 raw.
  - `_CYCLE_CONTRACT.md` (new) — one shared contract prepended to every domain mandate at run time, so it cannot drift per-domain; states it overrides the mandate where they disagree (the mandates still carried self-pacing instructions).
  - `weekly_cycle.sh` (new) — fixed-cadence runner. Refreshes the sensor, prepends the contract, retries ONCE on transient auth failure, and applies Rule 7b: a missing cycle doc is an error regardless of exit code. Carries the ops_integrity tamper baseline for the ops domain.
  - `samantha_weekly.sh` + `samantha_weekly_prompt.md` (new) — the single channel to Will. Records his verdicts from `ceo_chat_messages`, dedupes across domains, challenges evidence against N, and writes a brief capped at 5 decisions. Alerts Will directly if it fails, since a silent week is the exact failure being designed out.
  - Six mandates edited: self-pacing steps removed, Telegram/WTA redirected to the ledger, ops identity corrected (it opened "You are Samantha" while Samantha is now the synthesiser above it).
  - 14 files moved to `_retired/` with a README. Two were actively misleading: `rl_selftest.py` verified cron wiring by substring-matching `crontab -l`, and the PAUSED lines are comments containing those exact strings, so every cron check passed green while nothing was scheduled; `cycle_state.py` wrote the same state doc as `cycle_pacer.py --job geo` with no coordination, and `geo_cycle.sh` never exported `PACER_JOB` (default `"seo"`), so a geo agent could have silently written seo's pacer state.
  - Crontab: daily `ops_cycle.sh` commented out; 7 weekly lines added (6 domains Sun 06:00-11:00 staggered hourly, Samantha Sun 16:00). Backed up to `logs/crontab_backups/` first; active job lines 99 -> 105, verified, nothing lost.
**Correction made during the work:** I initially wrote into two files that the 2026-08-13 ops OAuth failure "still logged a clean heartbeat". That was wrong — it correctly recorded `status: error`. The real defects are that `detail` read only `claude -p rc=1` (byte-identical to the 08-09 max-turns failure, so "never started" and "ran out of road" are indistinguishable) and that the health board only rebuilds at 01:00, an 18-hour blind window. Both files were corrected.
**Files:** 16_General_Reinforcement_Learning/{recommendations.py, fix_digest.py, _CYCLE_CONTRACT.md, weekly_cycle.sh, samantha_weekly.sh, samantha_weekly_prompt.md, domains.yaml, geo_prompt.md, seo_prompt.md, ads_prompt.md, articles_prompt.md, onsite_prompt.md, ops_prompt.md, _retired/*}, crontab
**Recurrence:** First

## [DECLINE-LEAVES-THE-OLD-FIGURE] A withdrawn valuation kept its old range — 18:05 AEST
**Symptom:** after re-running the valuation precompute, 31 records still carried the PREVIOUS
run's accuracy sample size. 6 of them are attached, `unit_indexable: true` live pages.
**Root cause:** `$set` writes only the fields it names. When a dwelling that HAD a range stops
earning one, `precompute_unit_valuations` writes `method: "declined"`, `decline_reason` and
`publishable: false` — and every range field from the previous run **survives untouched**. So a
record simultaneously states it has no valuation and carries a full one:
```
  71-98-university-drive-varsity-lakes
    method=declined  decline_reason=comparables_too_old  publishable=False
    point=$579,550   low=$493,660   accuracy.n=992   (992 = the PRE-dedupe sample size)
```
The trigger was today's `dedupe_sales`: collapsing duplicate-dated sales moved comparable dates,
pushing these past `MAX_UPLIFT`, so a previously-valued home became a decline.
**Did anything render it?** No. `UnitValuationSection` gates on `publishable`, so the section
returned null. **But that is one consumer being careful, not the data being right.** Any future
reader of `point` that does not also check `publishable` — the markdown report path reads the
record directly — would publish a figure from a superseded method.
**This is the market_pulse failure exactly, which is why CLAUDE.md Rule 6 exists:** a partial
`$set` touches only the fields it names, one content layer goes stale, and it is invisible until
it contradicts the rest of the page. Rule 6 was written after a `narrative.pillars` field sat
stale for three weeks showing three contradicting absorption rates.
**Fix:** the decline branch now `$unset`s every figure field (`low`, `high`, `point`, `band_pct`,
`n_comps`, `n_available`, `adjusted_low`, `adjusted_high`, `comparables`, `band_basis`,
`accuracy`, `dropped_*`). A declined record now carries no number at all.
**Files:** `15_Off-Market/Units/scripts/precompute_unit_valuations.py`
**Recurrence:** first in this collection; 2nd occurrence of the partial-`$set` staleness class
(1st: `market_pulse` `narrative.pillars`, 2026-07-23, which made Rule 6 mandatory).

## [PS-TRUNCATION-MISDIAGNOSIS] I read a truncated process list as absence — 17:50 AEST
**Not a code defect — a reasoning error worth recording because it cost real work.**
**What happened:** checking whether a background precompute chain was alive, I ran
`ps -eo etime,cmd | grep "[p]ython3 -u " | head -2`. The two long-running daemons filled both
lines. I read the absence of the precompute as the chain having died, reported that to Will,
started a replacement chain, and **ran two valuation precomputes concurrently against the same
collection for ~10 minutes**, burning Cosmos RU. I also spent time investigating the VM Resource
Guard for a kill that never happened, and told Will the nightly crons might be at risk. They are
not.
**No data harm:** the job is an idempotent upsert and both runs write identical values.
**The point:** this is Rule 8 — *a query returning zero is evidence about the question you asked*
— applied all day to MongoDB and then broken against `ps`. `head -2` on an unsorted process list
is a filter, not a census, and an empty filtered result is indistinguishable from an empty
system. The same sentence covers `db_fields.py --check`, a projection that omits a field, and a
piped `head`.
**What to do instead:** assert on a sentinel the process itself writes (`chain.done`), or count
without truncating (`pgrep -c -f <exact>`). Both were available and neither was used.
**Recurrence:** first as a recorded reasoning error; conceptually the 6th instance today of
"empty result read as fact about the world rather than about the question".

## [GITHUB-PAT-SCOPED] Production PAT replaced with a contents-scoped fine-grained token — 12:55 AEST
**Symptom:** The `GITHUB_TOKEN` in `.env` — read by every process on this VM and inherited by any
child — carried `admin` on both production repos, far beyond the `gh api PUT contents` workflow it
exists for.
**Fix:** Will generated a fine-grained PAT (Contents: RW, Actions: RO, Metadata: RO over the five
Fields repos); swapped into `.env` (backup at `/home/fields/backups/2026-08-13-security/.env.pre-token-swap`,
mode 600). Verified after swap: admin-only endpoints (`/keys`, `/actions/permissions`, `/hooks`) all
return **403**, while `contents` read+write and `actions/workflows` read succeed — a real file was
pushed and md5-verified with the new token.
**⚠ Method note:** the first scope check was WRONG and nearly caused a false alarm. `GET /repos/{o}/{r}`
returns `permissions` for the authenticated **user**, not the token, so a correctly-scoped
fine-grained PAT still reports `admin: true` there. Admin-only ENDPOINTS are the valid test. Same
class as every other failure today: the obvious check measured the wrong thing.
**⚠ STILL OPEN FOR WILL:** (1) revoke the OLD PAT at github.com/settings/personal-access-tokens —
it remains valid until you do, and it is the one with `admin`; (2) `gh`'s own
`/home/projects/.config/gh/hosts.yml` holds a **SEPARATE** oauth token (confirmed different from
`.env`), which was world-readable until today — re-run `gh auth login` to rotate it.
**Files:** .env (not committed), scripts/check_property_sitemap_urls.py
**Recurrence:** First

## [SITEMAP-404-NOT-REPRODUCED] "115 of 1,493 /property URLs render Property Not Found" does not reproduce — 12:50 AEST
**Symptom (reported):** an earlier session reported 115 of 1,493 published `/property/` sitemap URLs
rendering "Property Not Found". No reproduction command was recorded.
**Finding: it does not reproduce. 0 of 1,508 fail today.** Swept every published `/property/` URL:
all return HTTP 200 with real SSR'd content (~40 KB, correct `<title>`, address and valuation
present). A random 250-URL sample of the much larger `/off-market/` class (15,703 URLs) is also
100% clean.
**Detection was validated rather than assumed:** a deliberately non-existent property URL returns
HTTP **404** with `<title>Property Not Found</title>` — so the checker demonstrably catches the
failure it is looking for. Without that control, "0 failures" would have been indistinguishable
from a broken test.
**Most likely explanations, in order:** (a) already fixed — the sitemap regenerated since (count
moved 1,493 → 1,508, lastmod 2026-08-12); (b) the original measurement was wrong. Cannot
distinguish without the original method, which was never recorded. **This is exactly the cost Rule 9
and the brief-validation gate exist to prevent** — a defect reported without a repro cannot later be
confirmed OR dismissed.
**Fix:** wrote `scripts/check_property_sitemap_urls.py` — the missing repro. Sweeps any sitemap URL
class (`--pattern`), random-samples large ones (`--sample`), asserts a minimum body size so it cannot
silently pass everything if SSR breaks, and exits non-zero when dead URLs are published (so it can
gate a job).
**Files:** scripts/check_property_sitemap_urls.py
**Recurrence:** First

## [SQUARE-PAYMENT-PATH-DORMANT] Square production token live for a payment path that 502s — 12:58 AEST
**Symptom:** `Feilds_Website/01_Website/.env` holds live Square PRODUCTION credentials
(`SQUARE_ACCESS_TOKEN`, `SQUARE_WEBHOOK_SIGNATURE_KEY`) in a file that was group-readable until today.
**Findings (corrects assumptions on BOTH sides):**
  - It is NOT unused code, as first assumed: `netlify/functions/square.mjs` implements a real
    $15 AUD off-market report unlock, wired into `src/routes/off-market.$slug.tsx`.
  - But it is NOT live either, as Will said: `/.netlify/functions/square` returns **HTTP 502** in
    production, and there is no visible payment CTA on the rendered page (the one "$15" match was a
    false positive — "$150,000" in valuation copy).
  - The page DOES ship Square's `appId` (`sq0idp-…`) and `locationId` in its loader payload. That is
    NOT a leak — the Web Payments SDK requires them client-side and they are public by design. The
    server-side ACCESS TOKEN is the secret, and it is not exposed to the browser.
  - `SQUARE_*` does not appear in the orchestrator `.env` at all — the credential lives only in the
    website env (and in Netlify's own env vars, which is what production actually reads).
**Action taken:** file permissions tightened to 600. Credentials left in place — deleting them
without disabling the code path would change production behaviour, and the path is a product
decision, not a bug.
**⚠ NEEDS WILL:** revoke/rotate the Square production access token at developer.squareup.com (only
you can). Since no payment has ever completed and the endpoint 502s, there is no operational risk in
revoking. If you want the feature gone entirely rather than dormant, say so — that is a code change
plus removing the Netlify env vars, not a credential rotation.
**Files:** /home/fields/Feilds_Website/01_Website/.env (permissions only)
**Recurrence:** First

## [WEBSITE-VESTIGIAL-ENTRYPOINT-SHIPPED] Dead SPA entry point removed — one commit, one deploy — 12:55 AEST
**Shipped** the bundle recommended above as a single commit `77f29fcb` → one Netlify deploy:
- **Deleted** `index.html` + `src/main.tsx` (pre-migration Vite SPA entry).
- **Deleted** the `netlify.toml` `[[headers]] for = "/index.html"` rule, which matched no published file.
- **Fixed** the `gtagConversions.ts` header comment (Google Ads tag is in `root.tsx`, not `index.html`).
- **Synced** `MarketIntelligenceSection/index.ts` and `public/off-market-v3/intro.js`, where local was
  newer than the remote.

`push_website_files.py` cannot express a deletion (it only builds blobs), so this used a one-off
git-trees commit with `"sha": None` entries. It **refuses to run if a path marked for deletion is not
in the remote tree** — a no-op deletion would otherwise report success having removed nothing.

**Verified BEFORE pushing:** ran `npx react-router build` locally with both files already deleted —
`✓ built in 22.48s`, 174 client assets, and `build/client/index.html` still absent exactly as before.
The build is unaffected by their removal, which is the claim that mattered.

**Verified AFTER deploying:** deploy state `ready`, no error.
- `/` — renders fully: nav, hero, all listing rails populated with live data, market intelligence,
  news, footer. **0 console messages.**
- `/for-sale` — **212 properties**, cards, images, prices, pagination. **0 console messages.**
- Both network errors are pre-existing and unrelated: the `whale_song.mp3` easter-egg media abort,
  and a Google Ads conversion beacon. That beacon is `AW-17993173310` **firing live** — independent
  confirmation that the Google Ads tag loads from `root.tsx`, exactly as the corrected comment says.

**A false alarm I nearly filed.** The first `/for-sale` capture showed skeleton loaders instead of
cards. Rather than assume it was benign, I screenshotted the **deploy permalink from before my
commit** and compared: it rendered 212 properties. So the skeletons were real, not normal — but not
mine either. Two things were true at once: (a) I had omitted `--wait`, so the capture caught
mid-hydration, and (b) an **unrelated concurrent deploy by Will** (`c1d8ddaa`, "emit srcset from the
serializer the property page actually calls", touching `properties-for-sale.mjs` — the very function
that page calls) was mid-build 18 seconds after my push. Re-running against my own deploy permalink
with `--wait 8000` rendered all 212 properties identically to the pre-change deploy. Page text was
4,734 bytes vs 4,724 pre-change, same content markers.

**The lesson, and it is this session's lesson again:** deploy permalinks make "did MY change cause
this?" an answerable question instead of a guess. Compare against the commit before yours rather than
reasoning about whether a symptom "looks related".

**Website tree baseline is now 911 in sync · 0 differ** (was 905 · 3). Any future `DIFF` is signal.

**Files:** website `index.html` (deleted), `src/main.tsx` (deleted), `netlify.toml`,
`src/utils/gtagConversions.ts`, `src/components/MarketIntelligenceSection/index.ts`,
`public/off-market-v3/intro.js`
**Recurrence:** First.

## [BRIGHTDATA-TOKEN-ROTATED] Bright Data token rotated — Domain.com.au ingestion restored after 2.5 days — 13:30 AEST
**Symptom:** Every Domain.com.au fetch on this VM returned `HTTP 401: Token expired` since 2026-08-11. `shared/domain_fetch.py` is the single choke point for 12 modules, so no new listings were discovered, no withdrawals detected, no sold records backfilled and no price changes captured. 9 of the 15 actionable rows on the Fields Systems Health board resolved to this one cause. Raised as WTA-OPS-022 (2026-08-12, unread) and re-raised as REC-ops-001 under the new ledger.
**Root cause:** The Bright Data API token expired at the vendor's end. Not an unset-env fallback (`BRIGHTDATA_API_KEY` was set and read, zone `web_unlocker2`), not a timeout (failed in 0.6s), not Akamai (we never reached Domain).
**Fix:** Will rotated the token in the Bright Data console. Two false starts worth recording:
  1. The first pointer to `00_Run_Commands/gh-token-29Mar.txt` yielded a token whose SHA-256 fingerprint was **byte-identical** to the one already in `.env` (`d784432a3493`), and a direct call to `api.brightdata.com/request` returned `401 Token expired` — confirming the file had not been updated, rather than our code misreading it.
  2. After the rotation, the same line 28 still held the old value — but the API error changed from `Token expired` to `Invalid token`, proving the rotation HAD happened at the vendor end. The new token had been appended as an **unlabelled continuation on line 29**, which the naive `startswith('bright data')` parser skipped. Masked-value inspection of every line in the file surfaced it (fingerprint `bb0d6ae2c58a`).
  Updated `.env` line 122 (with an assertion that exactly one `BRIGHTDATA_API_KEY=` line existed; `.env` backed up first). Verified three ways: direct API call `HTTP 200`; `fetch_html()` on the Robina for-sale search returned **1,967,403 bytes containing `__NEXT_DATA__`** (the payload the scraper parses).
**Deliberately NOT done:** did not re-run step 101 by hand. It is a ~75-minute `heavy_write` job and the nightly pipeline runs it at 20:30 anyway; a scrape captures *current* state, so tonight's run recovers the book without a duplicate RU spend. What is genuinely unrecoverable is listing transitions that began and ended inside the outage window — no re-run fixes that.
**Wider point (from the ops cycle, worth acting on):** this is the **sixth** credential-expiry outage — Gmail twice, GitHub, the Google 7-day OAuth, a two-month Facebook Ads blackout, now Bright Data. **None of these credentials has expiry monitoring.** Rule 7 proves a process *ran*; nothing proves the secrets it depends on are still *valid*. Also noted: `run_curlffi_suburb_scrape.py` returns 2 on its zero-discovery path *before* writing its heartbeat, so a hard outage renders as "may not be firing" rather than ERROR.
**Files:** .env (line 122, not committed), logs/fix-history/2026-08-13.md
**Recurrence:** 1st for Bright Data specifically; 6th credential-expiry outage overall

## [PHOTO-API-BEARER-LEAK] Matterport auth token served on the public property API — 13:05 AEST
**Symptom:** `https://fieldsestate.com.au/api/property?id=6a5a05bdf52d8b60206405b2` returned a
Matterport 3D tour URL **carrying its access credential** —
`my.matterport.com/show?m=EuQybhRW3PX&auth=Bearer%20…` — twice, at `property_images[25]` and
`property_images_srcset[25].url`, on a live for-sale listing (7/3-17 Camphor Wood Court, Robina).
**Root cause:** `isLiveImageUrl` in `shared-utils.mjs` (and six drifted local copies of the same
predicate) rejected only the DEAD Azure host. It never checked that a URL was an IMAGE at all.
Domain's image array contains the tour URL, it lands in `property_images_original`, and
`extractPhotos` pushes that array into the photo sources — so the tour URL was emitted as a photo.
**⚠ Why it stayed invisible:** the rendered HTML did NOT paint it (curl of the page: 0 matterport
hits), so any page-level check would have passed. The API is public regardless of what the page
shows. Verified against the API directly, which is the only check that could have caught it.
**⚠ The reported blast radius was wrong in BOTH directions.** The handoff said 3 files; there were
**7 files / 9 call sites** all carrying the same duplicated predicate — `property.mjs` (×2),
`decision-feed.mjs`, `decision-feed-v3.mjs`, `analyse-property.mjs`, `properties-for-sale.mjs` (×2),
`recently-sold.mjs` (×2). And the "30-photo cap masks 9 of the 10 corrupt blobs" mitigation is
accidental, not a guard: `decision-feed.mjs` returns photos UNCAPPED.
**Fix:** `isLiveImageUrl` now rejects tour/video hosts (browser-side mirror of `NON_IMAGE_URL_HOSTS`
in `download_images_to_blob.py:102`) AND any URL containing `auth=bearer` — the latter catches a host
nobody has enumerated yet, which a host list alone can never do. All 9 local copies deleted in favour
of the shared helper. Behavioural test: 7/7 including the exact leaked URL; all 7 modules import-check
clean.
**⚠ NEEDS WILL — ROTATE THE MATTERPORT CREDENTIAL.** The fix stops us SERVING it; it does not
un-publish it. That token was publicly retrievable from the API and must be treated as compromised.
It belongs to the listing agent's Matterport account, not ours — so this likely needs to go back to
whoever supplied the tour.
**Files:** netlify/functions/{shared-utils,property,decision-feed,decision-feed-v3,analyse-property,
properties-for-sale,recently-sold}.mjs
**Recurrence:** First

## [CORRUPT-BLOBS-HTML-AS-JPEG] 10 tour pages stored and served as .jpg photos — 13:10 AEST
**Symptom:** 10 blobs under `/data/blobs/property-images/` carry a `.jpg` extension and are served by
nginx as `Content-Type: image/jpeg`, but contain complete Matterport 3D Showcase **HTML pages**
(`file` reports "HTML document, ASCII text"; no image magic bytes). Browsers render a broken tile.
**Count correction:** the reported 6 was measured over `for_sale` in the three target suburbs only.
Widening to all statuses/collections found **10** — 2 more in reedy_creek, 2 on now-sold listings.
**Live impact today: 1 page**, not 10 — nine sit past the `maxImages = 30` cap in `extractPhotos`.
That cap is incidental, not protective (see the entry above).
**Root cause:** `download_single_image()` got HTTP 200 for a tour URL and
`blob_storage.upload(..., content_type='image/jpeg')` wrote the HTML verbatim. Nothing inspected the
bytes. Rule 7b shape: **a successful fetch is not a successful image.**
**Write path already fixed** (verified in sync with GitHub): `shared/blob_storage.py:120-127` refuses
non-image bytes declared `image/*`; `download_images_to_blob.py:102-116` skips tour hosts before
fetching. Step 110 is in `always_run_processes` (`src/schedule_manager.py:77`) so it runs nightly —
CLAUDE.md's "Sunday only" for step 110 is WRONG and should be corrected.
**⚠ THE DETECTOR EXISTS BUT IS SCHEDULED NOWHERE:** `scripts/backfill_image_derivatives.py:193-195`
already raises when corrupt exceeds 2% of photos seen, and self-registers `cadence_hours=24` — so it
will flip to STALE on the Process Registry having never run again. Wiring it into cron turns a dead
detector into a live guard, and byte-checks every referenced photo as a side effect (which also closes
the residual: an exhaustive magic-byte scan of all 1,127,515 blobs was not completed — ~4 hours).
**Files:** (cleanup pending via scripts/repair_non_image_blobs.py --apply --keep-tour)
**Recurrence:** First

## [CORRUPT-BLOBS-REPAIRED] 10 HTML-as-JPEG blobs removed from service — 13:20 AEST
**Fix applied:** all 10 verified non-images by magic-byte sniff, their URL `$pull`ed from
`property_images` (URL match, never index — the arrays are not 1:1 with `*_original`), and the blob
renamed to `.notanimage` (reversible, not deleted). Verified after: all three spot-checked URLs now
return **404** instead of `200 image/jpeg`, and the live-impact listing still serves 25 photos with
no Bearer, no matterport, no `.notanimage` reference.
**⚠ `scripts/repair_non_image_blobs.py` HAS A BUG — it found all 10 and repaired none**, reporting
"could not resolve a document … left in place" for every one. The documents ARE resolvable: the
corrupt URL sits at `property_images[24]` on the very doc it could not find. Its path→document
resolver is broken. The repair above was done directly instead. Fix the resolver before relying on
that script again — as written it is a detector that silently declines to act, which is the Rule 7b
failure shape in a maintenance tool.
**Also:** an un-scoped `--dry-run` walks all 1,127,515 blobs (~4h). Always pass `--root`.
**Files:** 10 blobs + 10 Gold_Coast documents
**Recurrence:** First

## [DERIVATIVES-JOB-SCHEDULED] Dead corrupt-original detector wired into cron — 13:21 AEST
**Symptom:** `scripts/backfill_image_derivatives.py` carries a Rule 7b assertion that raises when
corrupt originals exceed 2% of photos seen (`:193-195`) and self-registers `cadence_hours=24` — but
was scheduled NOWHERE. It would have shown STALE on the Process Registry forever, having never run.
**Fix:** cron at 03:30 daily (clear of the 20:30 pipeline and the 02:00 mongodump). This turns an
existing detector into a live guard and byte-checks every referenced photo as a side effect, which
also closes the residual from the manual sweep (an exhaustive scan of all 1.1M blobs was not
completed, so impostors from an unlisted host remain unmeasured).
**Files:** crontab (backup: /home/fields/backups/2026-08-13-security/crontab-pre-derivatives.bak)
**Recurrence:** First

## [SANDBOX-PRIVILEGE-VERIFIED] Spawned sessions cannot sudo or edit cron — 13:25 AEST
**Context:** noticed the crontab had gained 8 RL weekly-cycle entries mid-session and checked whether
a spawned session could have done it. `crontab` and `sudo` live OUTSIDE /home/fields, so the
read-only remount does not cover them — a real gap worth testing rather than assuming.
**Result — the boundary holds, for a reason worth recording:** inside the sandbox `sudo -n` fails,
`crontab -l` fails, and `sudo systemctl` fails. setuid binaries are ineffective inside an
unprivileged user namespace, and CAP_SYS_ADMIN is dropped. **Outside** the sandbox, user `projects`
has `NOPASSWD: ALL` — i.e. full root. So the namespace is carrying real weight: an unsandboxed
headless session on this box would have had root.
**⚠ UNEXPLAINED, FOR WILL:** there were **7 `crontab REPLACE` events today; only 2 were mine**
(12:35 worker-agent removal, 13:21 derivatives). The other five — 11:20, 12:03, 12:21, 12:46,
12:53 — added 8 RL weekly-cycle entries with a comment reading
"[SUPERSEDED 2026-08-13] daily ops_cycle.sh -> weekly_cycle.sh ops (Sun 06:00)". That is deliberate
RL-schedule consolidation authored today by something that is not this session and not its spawned
sessions (proven above). Most likely a concurrent Claude session. **Please confirm that was you or
another session** — memory records all RL crons as PAUSED 2026-07-30, so they are now live again.
**Recurrence:** First

## [SITEMAP-MONITOR-FROZEN-SAMPLE] Hardcoded seed meant the monitor checked the same 25 URLs forever — 13:35 AEST
**Symptom:** `sitemap_robots_invariant.py` has run nightly since 2026-08-08 reporting clean. Its
invariant-A sample is `DEFAULT_SAMPLE = 25` per family — 25 of 1,508 `/property` URLs a night — which
looked merely small. It was worse than small.
**Root cause:** `run()` built its RNG as `random.Random(20260808)` — a **hardcoded constant**. The
sampler therefore drew the **same 25 URLs every single night, forever**. The reassuring maths in the
script's own comment ("at 26% prevalence, P(miss) over 25 draws is ~0.0006") is true for ONE night and
then stops accruing: repeated runs added **zero** additional coverage, and any defect outside those 25
URLs was never checked no matter how long the monitor ran. ~98% of each family was permanently blind.
The intent — "a violation is reproducible" — was sound; it was just bought at the price of all
longitudinal coverage.
**Fix:** seed from **today's date** (`int(date.today().strftime("%Y%m%d"))`), with `--seed` to replay
any past draw and the seed recorded in the heartbeat metrics. Same-day reruns stay perfectly
reproducible, which is what reproducibility was actually for, while each night draws fresh.
**Measured, not asserted:**
```
5 nights, OLD (constant seed):  25 distinct URLs ever checked
5 nights, NEW (date seed):     120 distinct;  7 nights: 169;  30 nights: 603 of 1508 (40%)
P(catch) a 1%-prevalence defect:  OLD 22.2% and CAPPED THERE FOREVER
                                  NEW 22.2% night 1 -> 82.8% at 7 nights -> 99.9% at 30
```
No extra fetches per night. Live-verified: default seed = today, `--seed 20260813` replays it
identically, `--seed 20260814` draws differently; all runs 0 violations.
**Why not the exhaustive 1,508-URL check considered earlier:** unnecessary. The blind spot was the
frozen seed, not the sample size; fixing the seed costs nothing and an exhaustive nightly sweep would
add ~1,500 fetches for less benefit.
**Files:** scripts/sitemap_robots_invariant.py
**Recurrence:** First

## [SITEMAP-OSCILLATION-DETECTOR] Shape, not magnitude — a guard that can tell the two apart — 13:40 AEST
**Symptom:** The sitemap push guards only catch `PROP_COUNT < 100` and `OFFMARKET_COUNT == 0`. Two
±4,500-URL moves sailed straight through, and so did the genuine Aug 1–7 oscillation.
**Why the obvious fix was REJECTED:** a percentage-movement threshold is *anti-correlated* with what
we need. Measured: the harmful oscillator was **±2.6%**, while the two legitimate deploys were
**−29.7%** and **+45.5%**, and ordinary churn is ±0.04%. The bad event is an order of magnitude
SMALLER than the good ones. Any threshold catching 2.6% blocks both legitimate deploys; any threshold
permitting 45% never sees the oscillator. **Magnitude cannot separate them** — building that guard
would have blocked good deploys and missed the bug.
**Fix:** `scripts/sitemap_count_stability.py` keys on **shape** instead. A real change moves once and
stays; an oscillator reverses direction with comparable magnitude every run. So it fires only when the
last two deltas have **opposite signs** AND the smaller is ≥50% of the larger AND the move exceeds
0.3% of total. Counts are parsed from the sitemap commit messages, which already encode them — full
retrospective history immediately, no waiting for a log to fill. It **alerts, never blocks**.
**Backtested on the real 12-commit series:**
```
08-04  [15253, 15654, 15259]  ⚠ OSCILLATION +395 then -401 (2.63%)
08-05  [15642, 15253, 15654]  ⚠ OSCILLATION -401 then +389 (2.49%)
08-06  [15252, 15642, 15253]  ⚠ OSCILLATION +389 then -390 (2.56%)
08-08  [10793, 15351, 15252]  quiet   <- the legitimate -4,558
08-13  [15703, 10795, 10795]  quiet   <- the legitimate +4,908
```
3 true positives on the oscillator nights, 0 false positives on either legitimate ±4,500 move.
Heartbeat + rule 7b zero-output assertion (fewer than 3 parsed commits = blind, raises, never "clean").
**Files:** scripts/sitemap_count_stability.py (new)
**Recurrence:** First

## [REPAIR-KEEPTOUR-WOULD-RELEAK] My own --keep-tour flag would have re-published a credential — 13:45 AEST
**Symptom:** `repair_non_image_blobs.py --keep-tour` was written to preserve the discarded Matterport
link as `virtual_tour_url` rather than throw away useful content. I recommended using it.
**Root cause:** those URLs carry `auth=Bearer <token>`. `[PHOTO-API-BEARER-LEAK]` (found the same day
by a concurrent session) established that the token was already being served on the **public property
API**, because anything in a photo array is emitted by the API whether or not the rendered page paints
it. Writing the raw URL into a new field would have **re-created that leak under a different key** —
moving a credential, not removing it. The repair as actually executed did not pass `--keep-tour`,
so this never fired; it was latent in a script already on GitHub.
**Fix:** `--keep-tour` now stores `redact(source)`. The tour ID — the useful part — survives; the
credential does not. Verified: token and JWT prefix both stripped, `m=EuQybhRW3PX` preserved.
**Note:** a working tour link needs a token re-issued by the Matterport account owner (the listing
agent), which is not ours to persist. Redacting loses nothing we were entitled to keep.
**Files:** scripts/repair_non_image_blobs.py
**Recurrence:** First

## [GEO-LLMSTXT-404-AND-STALE] /llms.txt was a 404 and the served copy carried two wrong facts — 13:20 AEST
**Symptom:** `https://fieldsestate.com.au/llms.txt` — the conventional path AI crawlers request — returned the SPA "Page Not Found" shell (noindex). The file existed only at `/.well-known/llms.txt`. That served copy pointed every "Key Pages" link at `/market-metrics/...`, which has 301-redirected to `/market-intelligence/...` since 2026-07-31, and told AI crawlers our valuations come "with confidence intervals".
**Root cause:** llms.txt was originally shipped to `.well-known/` only, and was never revisited when the market-metrics → market-intelligence route move landed. The "confidence intervals" line predates the CLAUDE.md valuation-envelope rules, which are explicit that the ±12% band is NOT a confidence interval.
**Fix:** Created `public/llms.txt` at root and updated `public/.well-known/llms.txt` to the same corrected content: all URLs repointed to `/market-intelligence/...` (301→200 verified), and the valuation paragraph rewritten to describe the comparable-sales method and state plainly that the published range is an indicative band, not a statistical confidence interval. This is the file most likely to be quoted verbatim by a generative engine, so a false statistical claim in it is the worst place to leave one.
**Files:** `01_Website/public/llms.txt` (new), `01_Website/public/.well-known/llms.txt`
**Verification:** `npm run build` passed; ONE commit `55cc0d26` via `push_website_files.py` (2 files, 1 build), md5-verified; deploy logged (`6a7d3a0d87ad14b0c20c91f0`); live check `https://fieldsestate.com.au/llms.txt` → 200 with 6 corrected URLs.
**Recurrence:** First

## [GEO-BING-ZERO-20-DAYS] Bing and Copilot have sent zero traffic since 2026-07-23 — found, not yet fixed — 13:20 AEST
**Symptom:** Bing Webmaster first-party: 488 impressions / 9 clicks over 2026-07-16→22, then **0 impressions and 0 clicks on all 20 days 2026-07-23→08-11**. Our own `organic_journeys` referrers agree independently: `www.bing.com` ~4 sessions/wk through W30, then 0 in W31/W32/W33; `copilot.microsoft.com` 0 since W31. Google moved the opposite way (68→151 sessions/wk), so it is Bing-specific, not a site-wide traffic loss. Bing's `InIndex` has fallen monotonically 2,247 (07-25) → 1,998 (08-12) and its `AllOtherCodes` crawl bucket climbed from ~20/day to +350-400/day starting 07-28.
**Root cause:** Not established — this entry records the finding, not a fix. Bingbot itself is served fine (CrawledPages 100-130/day, Code2xx flat ~2,380, BlockedByRobotsTxt 0, and 20/20 random sitemap URLs return 200 to a Bingbot UA), so this is a serving/index decision, not a crawl fault. Two candidates match the timing: (a) `netlify/edge-functions/geo-block.js`, shipped 2026-07-21 19:49, which returns 403 + a `noindex` page to every non-AU/NZ IP while exempting crawlers by user-agent — cloaking-shaped to an engine that spot-checks a URL from a US IP with a browser UA; (b) the two Netlify hard-pauses, `[NETLIFY-CREDIT-OUTAGE]` 2026-07-23 19:35 (the same day as the cliff) and `[SITE-PAUSED-NETLIFY]` 2026-07-29 17:51 (the day before the AllOtherCodes spike). Hypothesis (a) cannot be tested from this VM at all — the geo-block is by construction invisible to any AU-IP request.
**Fix:** None applied. Remedy for either candidate is a site-wide edge-function or Netlify change, so it was raised for Will as **REC-geo-001** rather than shipped. Also recorded: a Bing SERP scrape was tried as corroboration and **discarded** — the control query `site:domain.com.au` returned unrelated Visual Studio pages, so the scrape gets a bot-fallback SERP and proves nothing.
**Files:** none changed. `16_General_Reinforcement_Learning/cycles/2026-W33/2026-08-13/geo_cycle_20260813_1320.md`
**Recurrence:** First

## [ORPHANED-VALUATIONS] A job cannot correct what it stopped evaluating — 18:35 AEST
**Symptom:** after the `$unset` fix, 25 records still carried pre-dedupe accuracy figures. A full
sweep found **88** such records.
**Root cause:** `precompute_unit_valuations` writes a record for every dwelling it EVALUATES, and
it evaluates only attached dwellings. When a dwelling stops being attached its record is never
visited again — so it is never updated AND never cleared. These 88 are precisely the
non-dwellings that left the attached bucket this morning when `classify_dwelling` was fixed to
stop returning `"attached"` for Land, Industrial, Development Site, Leisure and Farm.
**Reachability:** none today. The route only loads a unit valuation when the dwelling is
attached. As with `[DECLINE-LEAVES-THE-OLD-FIGURE]`, that is a consumer being careful rather than
the data being right.
**Fix:** the job now sweeps records whose slug is not in the current attached set. Safe because
every field is derived and regenerated each run. Target set verified before deleting: all 88
classified `non_dwelling`, zero classified attached. Guarded to full runs only — `--suburb` or
`--limit` would make "not in the live set" mean "not in this slice" and delete the rest.
**This is the third variant of one root problem today, and the distinction is worth keeping:**
```
  [SALE-COUNTED-TWICE]              wrong input      -> a wrong number
  [DECLINE-LEAVES-THE-OLD-FIGURE]   record visited, fields not named by $set  -> stale field
  [ORPHANED-VALUATIONS]             record no longer visited at all           -> stale record
```
A partial `$set` and an unvisited record fail identically from the outside, and neither throws.
**Files:** `15_Off-Market/Units/scripts/precompute_unit_valuations.py`
**Verified after the run:** 11,565 records; accuracy sample sizes in use are exactly
{596, 967, 155}; 0 declined records carrying a figure; 0 stale statutory records; the
consistency check reports 0 contradictions across 206 overlapping pages of 5,791 compared.
**Recurrence:** first.

## [SECTION-READ-NULL-REF] section_read never fired — ref read before the node existed — 14:55 AEST
**Symptom:** New per-section dwell instrumentation shipped, built clean, type-checked clean, and
emitted **zero** `section_read` events on both arms in production.
**Root cause:** `useSectionRead` used `useRef` and read `ref.current` inside a `useEffect` whose deps
were `[enabled]`. Both instrumented sections render **conditionally, after their data resolves** — the
V2 comparables block returns `null` until comps load, and the control's Valuation Guide sits inside a
`{cond && (…)}`. So the effect ran once on mount, found `ref.current === null`, attached no
IntersectionObserver, and never re-ran. Nothing threw; the code simply did nothing.
**Fix:** state-backed callback ref (`useState` + `useCallback`), with `node` in the dependency array,
so the effect re-runs the moment the element attaches.
**Why it was not caught earlier:** every gate passed — `vite build` clean, `tsc` clean, no console
errors, component visibly rendering. **A missing event has no failure signature.** It was only found
by querying PostHog for the event by `distinct_id` after driving both arms in a real browser, i.e. by
asserting the outcome rather than the absence of an error. Rule 7b applied to front-end telemetry.
⚠ Also note: two earlier "it isn't working" conclusions in the same session were WRONG and caused by
(a) PostHog ingestion lag of ~2 minutes and (b) querying `properties.reason` when the field is
`exit_reason`. Re-query after a delay and check the field name before concluding an event is missing.
**Files:** `src/utils/useSectionRead.ts`
**Recurrence:** First.

## [AERIAL-LOTPLAN-ZERO-PADDING] 757 aerials refused for a padding convention — 19:20 AEST
**Symptom:** the indexed-unit aerial batch finished `rendered 3737 · skipped 0 · failed 757`.
Every one of the 757 had recorded the same reason on its document: `"no parcel geometry"`.
**That reason was false.** All 757 sit in schemes that ARE in `Gold_Coast.complexes` with a
cadastral centroid — this morning's `ingest_scheme_centroids` located **1,964 of 1,964 schemes
(100%)** by reading their rings. The geometry plainly exists.
**Root cause, in two layers, each of which alone was enough to lose the parcel:**
1. **Zero padding.** `scheme_lotplan_for` builds the common-property key as `0{plan}` and
   `fetch_polygon` does an EXACT match on `lotplan`. The cadastre stores the common property of
   BUP100135 as **`00000BUP100135`** — five digits. Some plans use the unpadded form, which is
   why the fallback worked for thousands of homes and failed for exactly these: **a padding
   convention that varies by plan is indistinguishable from missing data.**
2. **`lot_area` is 0.0 on many strata plans.** Once the first fix queried by `plan` and ranked
   parcels by the reported area, SP280574 still failed — every one of its parcels reports zero
   area while carrying perfectly good rings. Ranking on the attribute discards those plans a
   second time for a second wrong reason.
**Fix:** `fetch_scheme_polygon(plan)` queries by `plan` (padding-agnostic) and ranks candidates by
**computed shoelace ring area**, never by the `lot_area` attribute. Tried only after the
dwelling's own lot fails, so townhouses keep their own 195 m2 boundary and only apartments fall
back to the scheme footprint — the caption distinction this feature exists for is untouched.
**Verified before re-running:** 17 of 17 sampled previously-failed dwellings across all three
suburbs now resolve geometry; 0 still failing.
**Files:** `scripts/render_property_aerial.py`
**How it was found — worth keeping:** the batch log is written without `-u`, so it was 0 bytes
with the file still held open while the job ran, and I could not diagnose from it. I first
claimed the gap was "batch progress, not a data gap" on the strength of ONE property rendering on
demand. That property turned out to be one of the 142 the batch had not reached, not one of the
757 that failed — a sample of one, drawn from the wrong population, used to characterise the
whole. The failure reason was in Mongo the entire time (`aerial_boundary_failed`), and reading it
took one query.
**Recurrence:** first. Same family as `[FIELD-NAME-GUESS-FALSE-ABSENCE]` — a lookup that returns
nothing is a fact about the key you built, not about the data.

## [OFFMARKET-REPORT-502-CACHED-CLIENT] Request endpoint 502'd on every call — 15:10 AEST
**Symptom:** Will clicked "Get the report" on 3 Springvale Street and saw *"We could not prepare
a report for this address. That usually means the evidence behind it is not strong enough for us
to put in writing."* The property was fine — it has an engine range of $1,339,512–$1,711,768 and
rendered successfully as recently as 2026-07-20. No request record existed in Mongo at all.
**Root cause — CORRECTED 16:40 AEST, my first diagnosis was wrong.** The helpers in db.mjs
return **`{ client, db }`**, not a db. I wrote `const db = await getSystemMonitorDb()` and then
`db.collection(...)`, so `db.collection` was **undefined** — a plain TypeError, unhandled,
which Netlify surfaces as "error decoding lambda response". Correct usage is
`const { db } = await getSystemMonitorDb()`.
I originally recorded this as the 2026-05-30 cached-client event-loop hang. It was not: 11
other functions use the cached helpers today and are not hanging. The fix (getFreshClient +
close) is still right — the cached-client risk is real and documented — but it is not what
broke this endpoint, and the log said so incorrectly.
**Original (incorrect) diagnosis, kept for the record:** imported `getSystemMonitorDb()`
— the CACHED Mongo client. `GET /api/v1/offmarket-report-request` returned
`502 error decoding lambda response: unexpected end of JSON input` on every call. This is a
documented landmine: the MongoDB driver's SDAM monitor uses `setTimeout` heartbeats that are
never `.unref()`'d, so a cached client keeps the function's Node event loop alive and Netlify's
streaming runtime never finalises the response. It hung the ENTIRE website API layer for ~33s
per request on 2026-05-30, which is exactly why `db.mjs` carries `getFreshClient()`. The warning
is written in the memory file and in db.mjs, and I used the cached helper anyway.
**Fix:** `getFreshClient()` + `await client.close()` in a `finally`, with the whole body wrapped
so an exception returns JSON rather than nothing — an unhandled rejection produces no body at
all, which is what Netlify reports as "error decoding lambda response".
**Second fix — the copy lied.** A crash was reported to the reader as a judgement about their
property. The page now distinguishes `unavailable` (the generator genuinely declined on the
evidence — waterfront, or no image that is actually the home) from `internal` (our fault), and
the latter says so and offers a retry. Never let infrastructure failure masquerade as an
authoritative statement about someone's home.
**Verified live:** GET -> 200 `{"status":"not_requested"}`; POST -> `{"status":"pending",
request_id}`; queued with `source: offmarket_v4`, `country: AU`; poller took it
pending -> processing -> completed in ~40s alongside the 3-worker pre-warm; PDF published to
`blobs.fieldsestate.com.au/off-market-reports/3-springvale-street-robina/20260813T045931.pdf`.
`tsc -p tsconfig.json` and `npm run build` clean; pushed files md5-verified.
**⚠ Blast radius:** the endpoint had been 502ing since it first deployed, so EVERY reader who
clicked since go-live got the misleading message and no report. Nothing was recorded, so no
requests were lost — but the completion funnel for that period is empty by construction.
`offmarket_report_requested` still fired on the click, so intent is measurable; nothing
downstream of it is.
**Files:** `netlify/functions/offmarket-report-request.mjs`,
`src/pages/OffMarketPage/v4/ReportSection.tsx`
**Recurrence:** Second for the cached-client hang — first was `[NETLIFY-MONGO-EVENTLOOP-HANG]`
2026-05-30. The guard rail (a `getFreshClient` helper plus a memory note) existed and did not
stop it, because nothing prevents a new function importing the cached helper. Worth a lint rule
banning `getSystemMonitorDb`/`getGoldCoastDb` inside `netlify/functions/*.mjs`.

## [V2-FLAG-NEVER-RENDERED] The A/B never ran — phGetFlag read at render time — 15:40 AEST
**Symptom:** A forced-`v2` browser session emitted `section_read` with **`arm: control`** and no
`comparables` section at all. The experiment was live, assigning visitors, and rendering the control
to both arms.
**Root cause:** `const v2Arm = phGetFlag('property_page_v2') === 'v2'` evaluated during render.
`phGetFlag` is **not reactive** — flags resolve asynchronously, so it returns undefined on first
paint, and nothing triggers a re-render when they arrive. The section gate therefore always saw
`false`, and the `arm` label baked into the `section_read` props was captured as `control` for
everyone. I had explicitly chosen the render-time read over state to avoid a layout flash; that
reasoning was wrong, because it traded a few hundred ms of flash for the experiment silently not
running at all.
**Fix:** variant held in `useState`, set inside `phOnFlagsReady`. Control is the default until flags
land.
**Verification:** found by driving a forced-`v2` session and reading the emitted `arm` back out of
PostHog — NOT by looking at the page, which rendered fine and simply rendered the wrong arm.
**Files:** `src/pages/PropertyPage/PropertyPage.tsx`
**Recurrence:** Second flag-timing defect today — cf. `[PROPERTY-ANALYTICS-CENSORED]`, where
`$feature/*` was missing from 66% of `property_view` events for the same underlying reason: flags
are async and anything read or emitted before they land is wrong.
**Related, same session:** `[SECTION-READ-NULL-REF]` (hook read a ref before the node existed) and a
React #310 outage from a hook placed below an early return. Three defects in one feature, all of
which passed `vite build` and `tsc` cleanly. **Front-end correctness gates do not cover timing,
mounting, or whether an event actually left the browser** — only driving the real page and querying
the data does.

## [AERIAL-RENDERER-DISAGREED-WITH-THE-INDEX] 110 indexed pages the renderer refused — 19:55 AEST
**Symptom:** after the padding fix took aerial coverage to 97.1%, 142 indexed unit pages still
had none. 110 of them carried `listing_status: "sold"`.
**Root cause:** `batch_render_aerials` filters `listing_status: {$nin: ["sold", "for_sale"]}`.
That is correct for HOUSES — a sold house belongs to the recently-sold surface, not the
off-market one. It is wrong for units: `flag_unit_indexable` deliberately ALLOWS sold dwellings,
because a sale history is what earns an off-market page at all; only `for_sale` and
`under_contract` are declined. So the flag said "publish this page" and the renderer said "not
eligible", and 110 live pages could never receive their hero image.
**One decision, two rules, in two languages of the same codebase — the sitemap/robots divergence
in a different costume.** `unit_indexable` exists precisely so publication has ONE definition
(`flag_unit_indexable.py`); intersecting it with an older rule that disagrees defeats the point.
**Fix:** under `--indexable-only` the `listing_status` exclusion is dropped, because
`unit_indexable` has already made that judgement.
**Second fix in the same file:** the eligibility query was written out TWICE, verbatim — once for
the `--dry-run` cost estimate and once for the run. Patching one and not the other is how an
estimate quietly stops describing the job it estimates. Extracted to a single `build_query(args)`
used by both.
**Result: 4,980 of 4,980 indexed unit pages now carry an aerial (100%).** 110 rendered, 0 failed.
**Files:** `scripts/batch_render_aerials.py`
**Recurrence:** 2nd of the duplicated-policy family ([OFFMARKET-UNIT-SITEMAP-MISMATCH] was the
first); 1st for the estimate/run duplication.

## [AERIAL-SCOPE-MISSING-CAPTION-RISK] 32 images whose caption could claim the wrong land — 20:05 AEST
**Symptom:** 32 indexed pages had an aerial and a URL but no `aerial_boundary_scope`, rendered
before that field existed.
**Why it matters more than the count suggests:** `HeroSection` branches on scope. Without
`scope === "scheme"` it falls through to *"Title boundary from the Queensland cadastre — lot X"*,
which tells the reader the outline is THEIRS. For an apartment the outline is the whole
building's common property. **3 of the 32 are `building_units`** — apartments — so up to three
live pages could have drawn a boundary around forty neighbours and captioned it as the reader's
own lot. That is the precise defect the scheme/lot distinction was built to prevent, arriving
through a missing field rather than through wrong logic.
**Root cause:** the stored `cadastral_polygon` on all 32 has `boundary_scope: None`, so the scope
could not be back-derived from what was cached — the polygon predates the field too.
**Fix:** cleared the image, the URL and the stale polygon on those 32 and re-rendered from
scratch, so the outline and the caption are produced together by the current code.
**Files:** data-only (no code change; the current renderer already sets scope on every path)
**Recurrence:** first. Worth noting the shape: a NEW field added to an EXISTING pipeline leaves
every pre-existing record in a state no code path will ever revisit — the same class as
[ORPHANED-VALUATIONS], where the job could not correct what it no longer evaluates.

## [TRACKING-SERVER-PII-LEAK] /track/list served 151 client email addresses publicly — 16:05 AEST
**Symptom:** Found while checking whether the tracking server could safely be proxied under
fieldsestate.com.au for the self-serve report viewer. `https://vm.fieldsestate.com.au/track/list`
was **completely unauthenticated** and returned up to 50 tracking records containing real client
names, **email addresses**, property addresses and full engagement summaries. Confirmed live by
fetching it: the first record returned a real client's gmail address and home address.
`/track/status/<id>` returned the **entire document** — `{"_id": 0}` was the only projection —
including `recipient_email` and the raw `events` array.
**Scale — CORRECTED 17:05 AEST. My first figure was wrong and overstated it ~75x.**
`system_monitor.email_tracking` holds 154 records, 151 with a NON-EMPTY `recipient_email`. I
reported that as "151 with a real email address" and then as "151 client email addresses are
already out". I had not looked at the values. They are:
    preview@fieldsestate.com.au   137   <- generator placeholder, minted for the QR pre-render
    test@fieldsestate.com.au        4   <- internal
    t@x.com / e2e@x.com             2   <- throwaway test addresses
    rossmax06@gmail.com             7   |  REAL, and both belong to the same person (Dee)
    miss.dee.dcruz@gmail.com        1   |
**Actual exposure: 2 addresses, 1 person**, plus her first name and one property address
(13 Terrace Court, Merrimac). `/list` exposed the 50 most recent records; `/status` exposed the
full document for any id, and ids appear in printed QR codes and emailed links.
⚠ THE LESSON: a populated field is not evidence of what it contains. `count_documents` on
`$nin: [None, ""]` answers "how many rows have something here", not "how many are real client
emails" — I supplied that second meaning myself and put it in this log as fact. This is Rule 8
inverted: the rule says a zero result is a fact about the name you typed, not about the data;
the same holds for a non-zero one. Look at the values before characterising them.
**Root cause:** the tracking server had **no authentication of any kind** — grep for
`OPS_AUTH_TOKEN|Bearer|auth` across `tracking-server/server.py` returned nothing. It was built as
an internal tool on an internal-sounding hostname; `vm.fieldsestate.com.au` is public.
**Fix:** added a bearer-token guard (`TRACKING_ADMIN_TOKEN`, generated and written to `.env`).
- `/list` -> 401 without the token. **Closed by default**: `_admin_ok` returns False when no
  token is configured, so a missing env var fails shut rather than open.
- `/status/<id>` -> projects out `recipient_email` and `events` for unauthenticated callers.
  Holding an id proves you have the link, not that you may read the recipient's contact details
  or their IP/user-agent history.
- Reader routes (`/view`, `/page`, `/event`, `/download`, `/scan`, `/pixel`) stay open — they are
  the product, reached with a 12-char unguessable id.
**Verified live:** `/list` 401; `/status` returns no `recipient_email`, no `events`; `/view` 200;
`/page` 200. Service restarted and active.
**⚠ Exposure window:** the service has existed since 2026-04-10. nginx access logs retain 14 days
(back to 30 July) and show `/track/list` requested exactly TWICE, both from this VM's own egress
IP (34.40.230.132, curl/7.81.0) — my own request that found the leak (200) and my check after the
fix (401). No third-party access in the observable window; the preceding ~3.5 months are
unknowable. The path is not linked from anywhere, but the ops host serves no robots.txt (404) and
sends no `X-Robots-Tag` on /track/, so nothing prevented crawling either.
**Provenance of the real addresses:** typed in by Will. `send_report.py` takes `--to` as a
required CLI argument and is the ONLY writer of `recipient_email` besides the tracking server
reading it back. Nothing scrapes or imports addresses into this collection. The Dee records are
all 2026-04-10 against 13 Terrace Court, matching `2026-04-10_13-terrace-court_dee_v2.pdf`.
**⚠ Still visible to an id-holder:** `recipient_name` and `property_address` on `/status`. That
is defensible (they hold the link) but a forwarded link leaks the recipient's name to whoever
receives it. Flagged rather than changed.
**Files:** `tracking-server/server.py`, `.env` (token, not committed)
**Recurrence:** First for this service. Same family as [[ops_dashboard_secret_path]] — an
internal tool on a public hostname with no auth, assumed private because the URL was obscure.

## [POSTHOG-SELF-TRAFFIC-MISREAD-AS-SCRAPER] Suspected scraper bot was our own VM — 16:20 AEST
**Symptom:** Person `019ff9b1-405a-7d32-bc95-187bad9feb4b` looked like a scraper: spoofed
iPhone UA, `?cb=<epoch>` cache-buster on every URL, 35s burst, throwaway ID each visit, and
(confirmed by Will in session replay) unmistakably programmatic scroll.
**Root cause:** Not a third party. `$ip` on all 106 such events = **34.40.230.132**, which is
this box (`fields-orchestrator-vm`, verified live via ifconfig.me + GCP metadata). It is our own
headless-browser verification traffic (`site-inspector.js` / `verify_market_metrics_live.py`
family) writing into production analytics. 177 events / 21 phantom persons in 30 days.
GeoIP renders australia-southeast1 as "Sydney", which is what made it read as external.
Also note the pasted value was a **distinct_id**, not a person UUID — `persons` lookup on it
returns [] (person UUID is `b3cb3a2e-3f02-5cf8-ade3-7c091cc3cb3c`). See [[posthog_internal_tester_labelling]].
**Fix:** Added `{"key":"$ip","type":"event","value":["34.40.230.132"],"operator":"is_not"}` to
the project's `test_account_filters` (existing two rules re-sent — PATCH replaces the array).
One rule covers all future VM runs, vs. labelling each throwaway person by hand. Deliberately
did NOT block the IP at the edge: that would break our own mandatory visual-verification step.
**Files:** PostHog project 348370 `test_account_filters` (API config, no repo file)
**Recurrence:** First.

## [SECTION-READ-TALL-SECTION] section_read silently skipped every long section — 16:30 AEST
**Symptom:** After the null-ref fix, `section_read` fired for `valuation_guide` on both arms but
NEVER for `comparables`, despite that section demonstrably rendering (the v2 arm measured 17,882px
vs the control's 15,332px — the 2,550px difference IS the section).
**Root cause:** `IntersectionObserver` threshold `0.35`. The comparables block is ~2,550px tall; on
an 844px mobile viewport, 35% of it is 892px — **taller than the screen**. The ratio can never be
satisfied, so the callback never fired for exactly the longest, most important sections. The shorter
`valuation_guide` cleared the bar, which is what made the failure look selective rather than broken.
⚠ The code comment beside the threshold explicitly reasoned about this failure mode ("a section
taller than the viewport can never reach 50%") and then picked 0.35, which has the same defect. The
hazard was identified and the fix was still wrong.
**Fix:** `threshold: 0` — any intersection starts the dwell timer. The timer, not the ratio, is what
distinguishes reading from scrolling past, so a ratio threshold buys nothing here.
**Files:** `src/utils/useSectionRead.ts`
**Recurrence:** Third defect in this one hook (cf. `[SECTION-READ-NULL-REF]`, and the React #310
outage from hook placement). Every one passed `vite build` and `tsc`. A telemetry event that does not
fire has no failure signature — the only gate that works is driving the real page and querying for
the event by `distinct_id`.

## [OFFMARKET-REPORT-TRACKED-VIEWER] Self-serve report delivered via the tracked viewer — 16:15 AEST
**Why:** Will asked whether he'd get a Telegram on download, whether re-opens were visible, and
what page a reader was on. A raw PDF answers none of those — it is invisible the moment it lands
on a device. The tracking server already recorded all three for POSTED appraisals; self-serve
simply wasn't wired to it.
**Shipped:**
- Poller mints an `email_tracking` record per report and returns
  `viewer_url = https://fieldsestate.com.au/track/view/<id>`. ⚠ **No contact details** —
  `recipient_email` is None and `recipient_name` is "the Owner". `sync_crm_engagement` returns
  early without an email, so a reader promised nobody would contact them does NOT silently
  become a CRM contact.
- `netlify.toml` proxies **the whole `/track/*` prefix** (status 200) to the VM. Proxying only
  `/view` would have rendered a page whose every image and beacon 404s: viewer.html addresses
  pages as `/track/page/<id>/<n>` and builds its event endpoint from
  `window.location.origin + "/track"`.
- Section links to the viewer ("Read your report"), falling back to `pdf_url` for reports built
  before tracking existed. The raw download stays inside the viewer.
- **Telegram throttle** (`notify_policy: "quiet"`): every non-heartbeat event used to notify, so
  a 20-page read would have sent ~20 messages. Quiet records notify only on `viewer_opened`,
  `session_end`, `conversation_requested`, `feedback_submitted`. Everything is still RECORDED —
  pages, dwell, re-opens — the policy governs only what reaches a phone. Posted appraisals are
  unaffected and stay loud.
**Two bugs found by testing, both mine:**
1. `shutil` was never imported — the durable viewer copy raised `name 'shutil' is not defined`
   on every request.
2. **The stale-claim reclaim fired instantly.** Cosmos returns naive datetimes and
   `naive.timestamp()` interprets them as LOCAL time (AEST, UTC+10), so a job started seconds
   ago looked ten hours old. Combined with (1) it produced a spin loop, and on its own it would
   have handed a still-rendering job to a second worker. Now compares in explicit UTC.
**Verified end to end on 16 Cheltenham Drive:** request -> render -> published (2.39 MB);
viewer and page images both HTTP 200 **through fieldsestate.com.au** (proxy works);
`total_viewer_opens: 3` across three visits (re-opens tracked), `pages_viewed: [1, 2, 11]`,
`last_session_duration: 142`; `recipient_email: None` confirmed.
⚠ The `/event` contract is `{"tracking_id", "type", "data"}` — my first test used `event` and
silently 400'd, which is why page events appeared missing.
**Files:** `scripts/offmarket_report_poller.py`, `tracking-server/server.py`,
`netlify.toml`, `netlify/functions/offmarket-report-request.mjs`,
`src/pages/OffMarketPage/v4/ReportSection.tsx`
**Recurrence:** First.

## [ARTICLE-TELEGRAM-APPROVAL] Telegram approve/reject-with-feedback gate for article drafts — 16:15 AEST
**Symptom:** Every article needs Will's explicit approval before publishing (his rule, 2026-07-29) but there was no mechanism, so drafts accumulated (28 in `content_articles`) and articles became the most constrained domain. Publishing had also silently stopped this week through workflow errors.
**Root cause:** The gate was a policy with no plumbing. Will had to be asked ad hoc, in-session, so nothing moved between sessions.
**Fix:** `scripts/article_approval.py` — `propose` sends the draft to Telegram with tap-able YES/NO keys; `poll` (cron */5 07-22) reads the reply and publishes or records feedback.
  Two design constraints drove the implementation and are documented in the module docstring:
  1. **Reply keyboard, NOT inline buttons.** `ceo-telegram-bridge.py:1345` requests `allowed_updates: ["message","edited_message"]`, so it never receives `callback_query` — inline buttons would fire into a void. A reply keyboard sends ordinary text the bridge already captures into `ceo_chat_messages`. (The bridge uses the same trick in `workflow_reply_markup()`.)
  2. **Never poll getUpdates.** Telegram permits one consumer of an update offset and the CEO bridge owns it; a second poller would silently steal Will's messages. Verdicts are read from `ceo_chat_messages`, the pattern `fb_approval.py` established.
  Deliberate divergence from `fb_approval.py`: it accepts a bare YES/NO when only one item is pending. This does NOT — an untokened "no" is ambiguous about which draft and carries no reason, and the **rejection reason is the entire point**. Will's feedback is written to `will_feedback` on the article and replayed to the articles domain, exactly like a recommendation verdict; an approval flow recording only yes/no teaches the domain nothing and it re-proposes the same drafts.
  Also patched `scripts/telegram_notify.py`: `send_message()` gained an optional `reply_markup` (backward compatible), and the Markdown-parse-failure retry path now carries the keyboard through — otherwise a parse error would silently drop the buttons.
**Verified:** proposed a real draft (`6a7bb57115379c7bd04625b9`, "What Does $1,500,000 Buy in Robina in 2026?"), message delivered as token #A941 with keyboard; `poll --dry-run` correctly actioned 0 before any reply.
**Note on crontab safety:** the first install attempt used a wrong anchor string; the assertion aborted and the crontab was left untouched at 109 active jobs (verified before/after). Reinstalled against the correct line — 109 → 110.
**Files:** scripts/article_approval.py (new), scripts/telegram_notify.py, 16_General_Reinforcement_Learning/briefings/{articles,seo,geo}.md, crontab
**Recurrence:** First

**Follow-up (16:40 AEST):** Will asked for the sessions labelled, not just IP-filtered. Queried
all-time events where `$ip = 34.40.230.132` -> **25 person UUIDs** (Apr 7 through today), set
`is_internal_tester: true` on each via the persons management API. Verified: 26 persons now
carry the flag (the 25 + one pre-existing from 2026-07-24). Note these are person UUIDs read
straight off `events.person_id` — no distinct_id->UUID resolution needed, unlike the path in
[[posthog_internal_tester_labelling]].
**⚠ Still open:** labelling and `test_account_filters` both act at QUERY time — PostHog still
ingests every event. Source fix not yet shipped: `isInternal` in `src/utils/posthog.ts` does not
test `navigator.webdriver`, so headless Chrome gets a fresh profile each launch and captures.

## [WHATS-CHANGED-HOUSE-FIGURES-ON-UNIT-PAGES] Unit owners shown the house market — 21:10 AEST
**Symptom:** the What's-Changed section on a unit page read *"how long homes take to sell —
currently a median of **34 days**"*. The Robina **attached** median is **20 days**. 34 is the
house figure.
**Not cosmetic — it inverted the story.** The same section told a Robina unit owner their median
was *"broadly unchanged, $1,490,000, **-1.7%** on the quarter"*. The attached series says
**$990,000, +4.2%**. Unit owners were reading the opposite direction of their own market, on the
one section of the page whose whole promise is "here is what changed for you".
**Root cause, in two halves:**
1. `watch_points()` CONCATENATED the local reading into the point's own `text`
   (`text += f" {reading}"`). Once inside `text` it cannot be filtered per dwelling, so the
   house figure reached every unit page regardless of the houseLocal machinery added on
   2026-08-10.
2. The macro events did keep `house_local` separate and the component correctly dropped it for
   units — but dropping was the whole strategy, so units got NO local clause at all. The fix
   for the first defect had made the second one invisible: a suppressed line looks like a
   design choice, not a gap.
**Fix — answer the question with the right number rather than going quiet.** We already hold the
attached equivalents in `unit_market_series`. Added `unit_reading()` and
`unit_watch_metric_now()`, emitted `unitLocal` beside `houseLocal`, and the watch point now
emits its reading as a field instead of baking it into prose. `WhatsChangedSection.forDwelling()`
picks by `dwellingClass` instead of deleting.
```
  house : "homes in Robina have taken longer to sell — 34 days in 2026-Q2 against 26 in Q1"
  unit  : "attached homes in Robina have taken a median of 20 days to sell"
  house : "median house price broadly unchanged — $1,490,000, -1.7%"
  unit  : "median unit price is higher — $990,000, +4.2%"
```
**Files:** `15_Off-Market/Whats_Changed/whats_changed.py`,
`v4/WhatsChangedSection.tsx`, regenerated `v4/whatsChangedData.ts`
**⚠ `whatsChangedData.ts` IS GENERATED** — the header says so. A hand-fix to it on 2026-08-10 was
silently undone on 2026-08-12 when the generator next ran. This change is in the generator.
**Recurrence:** 3rd house-figure-on-a-unit-page defect (1st: rarity claim counted the detached
market; 2nd: TimingSection/MedianTrend labels). The pattern each time is that the unit path was
built by SUPPRESSING house content rather than substituting unit content, and suppression leaves
no visible hole to notice.

## [ONTHEHOUSE-UNIT-ATTRIBUTES] The bedroom data existed; we had never fetched it — 22:10 AEST
**The wrong conclusion first.** Asked why 4,590 attached dwellings have no bedroom count, I said
it was an evidence ceiling: "we can't write a page about a home we have no transaction for". That
reasoning was wrong twice over. The unit valuation is built from OTHER homes' sales in the same
building, so the subject's own history is irrelevant; and the real blocker is **bedrooms**, missing
on 79% of them. Will pushed back and pointed at an onthehouse URL.
**What is actually there.** 1/1 Acacia Court, Robina — a home with no bedroom count of ours —
carries `beds: 2, baths: 2, floorSize: 138` on onthehouse, along with car spaces, year built and
an exact per-unit lat/lon. Their sitemap publishes every property URL with address and id
embedded (11,077 Robina / 8,099 Varsity Lakes / 6,512 Burleigh Waters), and `/property/` is not
disallowed in robots.txt. So the data was public and free the whole time.
**⚠ THE MATCHER WAS THE ENTIRE RISK, AND MY FIRST ONE WAS WRONG.**
A throwaway sampler matched on substring containment. `61 Investigator Dr` matched INSIDE the
slug `4608-61-investigator-drive`, so unit 4608 was about to be assigned the BUILDING's bedrooms.
Three of twelve inspected rows were building-level. It reported "20 of 25 resolved" and looked
like a success. Writing it would have corrupted bedrooms, which feed valuations, comparables AND
the bedroom-matched price index — strictly worse than having no data at all.
**The shipped matcher** is exact on a normalised `(unit, street-number, street-name, suburb)`
tuple with no fuzzy fallback. Unit-tested on the failure case (`4608/61` must NOT equal `61`), the
known positive, ranged street numbers (`112/2-4 Riverwalk Ave`, which the first version returned
None for), and unit-vs-no-unit. `beds: 0` is rejected — it is onthehouse's "unknown", not a studio.
**Held-out validation before any write** (`validate_onthehouse_match.py`): resolve dwellings whose
bedroom count we ALREADY hold and compare. **40 of 40 agreed, 0 disagreed, 0 empty; the 5
comparable floor areas matched exactly.** Honest bound: 40/40 puts the true error rate under
roughly 7%, not at zero.
**Provenance on every write** (`bedrooms_source`, `bathrooms_source`, `floor_area_source`) with the
source URL and the address actually matched, so any row can be audited or reverted.
**Files:** `15_Off-Market/Units/scripts/onthehouse_attributes.py`,
`15_Off-Market/Units/scripts/validate_onthehouse_match.py`
**Recurrence:** first. The lesson is the one from `[FIELD-NAME-GUESS-FALSE-ABSENCE]` in a new
costume: **"we don't have it" is a claim about where you looked.** I had checked our five bedroom
paths and `backfill_beds_baths.py` (which only MOVES values we already hold) and concluded the
data did not exist. It existed on a public site we already scrape for other purposes.

## [PKILL-MATCHES-ITS-OWN-SHELL] Killed my own session four times — 22:15 AEST
**Not a code defect. A habit that cost real time today, recorded so it stops.**
`pkill -f <pattern>` matches the FULL COMMAND LINE of every process — including the shell that is
running the `pkill`, because that shell's command line contains the pattern. Four times today a
`pkill -f "precompute_..."` / `pkill -f batch_render_aerials` killed the invoking shell (exit 144)
BEFORE the rest of the command ran, so the edit or re-run I had chained after it silently never
happened. Once it also left me believing a job had died when it had not.
**Instead:** target explicit PIDs from a prior `pgrep`, or use the harness's own task control. If
a pattern must be used, exclude the invoker (`pgrep -f X | grep -v $$`).
**Related, same root:** `ps -eo … | grep … | head -2` truncated a process list and I read the
truncation as absence, reported a chain as dead, and started a duplicate run. See
`[PS-TRUNCATION-MISDIAGNOSIS]`. Both are Rule 8 against the shell rather than the database: **an
empty or truncated result is a fact about the question, not about the world.**

**Source fix shipped (17:05 AEST):** commit `602af03` on Website_Version_Feb_2026,
`src/utils/posthog.ts`. Added `navigator.webdriver === true` to the `isInternal` set (which
already hard opts-out via `opt_out_capturing()`), plus a `forcedExternal` guard so an explicit
`?fields_internal=0` beats every automatic rule — without it the headless check is recomputed
each page load and the escape hatch could never win. Also declared `opt_in_capturing?` on the
`window.posthog` type (pre-existing TS2551, benign at runtime behind `?.`; confirmed present in
the committed version before touching it).
**Verified:** md5 local==remote after push; deployed minified chunk contains OUR expression
(`e.get("fields_internal")==="0",u=navigator.webdriver===!0`), not vendor code; headless probe
=> `has_opted_out true`, with `?fields_internal=0` => `false`; **0 VM-origin events ingested**
in the 15 min since, vs 37 in the 30 min before; real non-VM traffic still capturing normally.
**⚠ Caveat — what is NOT proven:** the opted-IN control probe also ingested 0 events, because
posthog-js flushes nothing from a headless browser that closes (see [[offmarket_v4_reading_analytics]]).
So "capture still works when overridden" rests on the SDK flag, not on observed ingestion.
Also could not attribute the final 06:33-06:35Z burst (37 events, `?cb=` + scroll signature) —
it landed around CDN propagation and the originating script was never identified.

## [ARTICLE-LICENCE-WRONG-CORPUS] 68 articles stated the wrong QLD licence number; 40 were live — 17:10 AEST
**Symptom:** Every article's legal disclaimer read "Fields Real Estate (Licence No. 4832971)". Will's actual licence is **4832972**. Found because Will queried an unrelated "Social Caption" heading in a draft sent for approval, and reading that draft surfaced the disclaimer.
**Root cause:** Not established. The number is not in the `fields-automation` repo (GitHub code search: 0 matches) and not in local config, so nothing appears to template it. A plausible mechanism is the generator reproducing the number from our own older published articles — self-reinforcing, which would explain why the earlier fix did not stop it.
**Recurrence — this is the second surface, not the first sighting.** `logs/fix-history/2026-06-21.md` records: *"the QLD licence number was wrong (4832971) on three public pages"* and *"Licence correction 4832971→4832972 on DisclaimerPage, AboutPage, EditorialPolicyPage."* That fix corrected the three React pages and never touched the 100-document article corpus, and nothing was added to stop new articles carrying it. **28 of the 68 bad articles were created in August 2026, the most recent 2026-08-11** — i.e. it kept happening for ~7 weeks after being "fixed".
**Fix (corpus):** 92 documents updated in one pass, each backed up first to `content_articles_backup_20260813` (fields touched only: html, author, author_slug):
  - licence `4832971` → `4832972` in 68 articles. Verified after: 0 wrong / 68 correct.
  - Removed a stray `<h2>Social Caption</h2>` + paragraph block from 6 articles (1 published). It is a social-media caption the generator emits alongside the article that was being appended into the reader-facing body instead of stored separately. Will: *"Seems odd. Would rather it removed."*
  - `author`/`author_slug` → `Will Simpson`/`will` on 74 articles (were 73 "Fields Research", 1 null). Will: *"every article should be authored by me."* Matched the field values of a known-good article rather than inventing a mapping — `author_image` is left empty because the byline photo resolves from the slug.
  - Verified live on a published article: wrong licence absent, correct present, Social Caption gone, byline "Will Simpson".
**⚠ STILL OPEN:** the generator source. The corpus edit is cleanup; article 101 may still arrive with 4832971. Handed to the articles domain via its brief.
**Files:** system_monitor.content_articles (92 docs), content_articles_backup_20260813 (new), 16_General_Reinforcement_Learning/briefings/articles.md
**Recurrence:** 2nd (first: 2026-06-21, website pages only)

## [ARTICLE-REVISE-LOOP] Rejection now triggers an automatic revision and resubmission — 17:15 AEST
**Symptom:** The approval gate recorded a rejection and stopped. Feedback sat on the article until the weekly articles cycle, so an iterative draft loop took a week per round. Will: *"my reply should have either samantha or the sub-domain agent make the fix as per my feedback and resubmit draft to telegram for my approval."*
**Fix:** `scripts/article_revise.py` — on rejection, `article_approval.py` launches it detached (the poller must return before the next cron tick). It runs a headless Claude Max agent with Will's verbatim feedback, edits the draft HTML, and re-proposes via `article_approval.py propose`. Three guards, because this is an agent editing live content in a loop: a **3-round cap** (then parked `needs_human`, so reject→revise→reject cannot run away), it **never publishes** (Will's tap remains the only path live, preserving the 2026-07-29 rule), and every round **snapshots prior HTML** to `content_article_revisions`. The prompt hard-codes licence 4832972 and names 4832971 as the known-bad value so a revision cannot reintroduce it.
**Also:** added a live preview link to the approval message. No preview route was needed — `db.server.ts findArticle()` resolves by slug or `_id` without filtering on status, so a draft already renders as the real page with hero image. Verified HTTP 200.
**Files:** scripts/article_revise.py (new), scripts/article_approval.py
**Recurrence:** First

## [ARTICLE-PUBLISH-NO-DEPLOY] Approving an article published it but left it unlisted — 17:35 AEST
**Symptom:** `article_approval.py` set `status: "published"` on Will's YES and reported success. Caught before it shipped: the article would render at its own URL but never appear in the /articles index.
**Root cause:** Two different read paths. A single article is served by `db.server.ts findArticle()`, which reads Mongo live and does not filter on status — so a status flip is enough. The article LIST is served from `/data/articles.json`, generated **at build time** by `scripts/fetch-articles.js` from the `published-articles` endpoint. Without a Netlify build the article is published-but-unlisted: reachable only by someone who already holds the URL, and absent from the feed — which is indistinguishable from publishing having silently failed.
**Fix:** `_publish()` now POSTs the Netlify build hook after the database write and returns `(ok, warning)` rather than a bare bool, so the three states are distinguishable in the Telegram reply: published + deploy triggered; published but deploy failed (states plainly that it will not appear in the list until a build runs); or the write itself failed. One build per approval (~15 credits) is the correct cost, and approvals arrive one at a time via Telegram so this cannot stampede.
**Files:** scripts/article_approval.py
**Recurrence:** First

## [ARTICLE-REVISE-VERIFIED] Revision loop proven end to end — 17:40 AEST
**Verified** with a live run rather than assumed: seeded feedback ("opening paragraph too long, split it"), ran `article_revise.py --id`, and confirmed the agent rewrote the opener into two paragraphs, **left status at `draft`** (did not publish), incremented `revision_round` to 1, wrote one snapshot to `content_article_revisions`, preserved author and the corrected licence number, and re-proposed to Telegram as a new token. The full reject → revise → resubmit cycle works.
**Files:** none changed (verification only)
**Recurrence:** n/a

## [ARTICLES-JSON-STALE-AUTHOR] Author fix was invisible on the article list — build-time artefact, not the database — 18:05 AEST
**Symptom:** After correcting `author` to "Will Simpson" on all 100 articles in Mongo and verifying it on a live article page, Will reported articles still not showing him as author. He was right and my verification was aimed at the wrong surface.
**Root cause:** Author appears on TWO surfaces fed differently. The article PAGE is served by `db.server.ts findArticle()`, which reads Mongo live — so the fix applied instantly, and diffing the rendered author card against Will's reference article showed byte-identical markup (same avatar asset, same class). The article LIST/cards are served from `/data/articles.json`, generated **at build time** by `scripts/fetch-articles.js`. That file still held **55 "Fields Research" vs 17 "Will Simpson"** — frozen at the last deploy.
**Fix:** POSTed the Netlify build hook; `articles.json` regenerated to **72/72 "Will Simpson", 0 "Fields Research"**, confirmed by re-fetching the live file.
**The general lesson, worth more than the fix:** on this site "the database is correct" and "the site shows it" are two separate claims and must be verified separately. Anything served from `articles.json` is stale until a build runs. This is the SECOND time this trap appeared in one session — `[ARTICLE-PUBLISH-NO-DEPLOY]` (17:35) was the same mechanism in the publish path. Having fixed it there, I did not ask where else build-time artefacts sit; that is what left the author fix half-done.
**Files:** none (data + deploy only)
**Recurrence:** 2nd occurrence of the build-time-staleness class today

## [TELEGRAM-INLINE-BUTTONS] Approval buttons never appeared — wrong keyboard type — 17:25 AEST
**Symptom:** Will: "there are no buttons to tap" on the article approval message.
**Root cause — not a send failure.** I inspected the outgoing payload and `reply_markup` was transmitted correctly. The fault was the KIND of keyboard: a *reply* keyboard replaces the client's on-screen keyboard, hides behind the keyboard-toggle icon, and `one_time_keyboard: true` collapses it after a single use — so it is easy to never see. Inline buttons attach to the message itself and stay visible. I had originally chosen a reply keyboard specifically because `ceo-telegram-bridge.py` requested `allowed_updates: ["message","edited_message"]` and so never received `callback_query`; that constraint was real, but the right response was to lift it rather than accept an invisible control.
**Fix (bridge, additive):** `allowed_updates` now includes `callback_query`; `extract_message_text()` translates a callback into the ordinary `(message, text)` shape so every downstream consumer — allowed-chat check, session handling, `ceo_chat_messages` storage — treats a tap exactly like typed text; `answerCallbackQuery` is called first so the client spinner clears, wrapped so a failure there can never drop the update (a stuck spinner is cosmetic, a dropped approval is not). Note `message.from` on a callback is the BOT, so the human is taken from `callback.from` — otherwise the message is attributed to the bot.
**Fix (approval):** `_keyboard()` now emits `inline_keyboard` with "✅ Publish" / "✏️ Request changes", carrying the same `YES <tok>` / `NO <tok>` payload the poller already parses — so no verdict logic changed.
**Also added:** the bridge now short-circuits approval tokens (`APPROVAL_TOKEN_RE`) — it stores them and stays silent, leaving `article_approval.py` to act and confirm, rather than double-messaging Will for one action.
**Files:** scripts/ceo-telegram-bridge.py (backed up to logs/code_backups/), scripts/article_approval.py
**Recurrence:** First

## [BRIDGE-WAKE-PATH-BROKEN] I broke the founder-message wake this morning and it went unnoticed — 17:28 AEST
**Symptom:** Every Telegram message Will sent since ~15:00 answered "⚠️ Saved — but I couldn't start Samantha automatically just now."
**Root cause — self-inflicted, same session.** Retiring the daily RL machinery moved `conductor_cycle.sh` to `_retired/`, but `ceo-telegram-bridge.py:167` still pointed at the old path. My post-retirement reference check grepped for `cycle_pacer|cycle_state.py|rl_cycle.sh|rl_dispatch.sh|ops_cycle.sh` and **did not include `conductor_cycle.sh`**, and it searched the RL folder while the bridge lives in `scripts/`. Both halves of the check were too narrow.
**Fix:** the wake is now conditional on the runner existing (`os.path.exists`). When it does not, the reply states the truth — Samantha is weekly (Sunday 16:00) and reads her inbox first, with article approvals actioned within 5 minutes as the exception — instead of reporting an error for something working as designed. If an interactive runner is ever restored at that path, instant wake resumes automatically with no further change.
**Lesson:** when retiring files, grep the WHOLE repo for each filename, not a hand-written subset of patterns scoped to one directory.
**Files:** scripts/ceo-telegram-bridge.py
**Recurrence:** First

## [ONTHEHOUSE-HOUSES-BACKFILL] Extending the onthehouse backfill past units — and three matcher bugs it exposed — 23:40 AEST
**Symptom:** the units backfill covers ATTACHED dwellings only. Will asked whether detached
homes we hold as cadastral-only records also have onthehouse pages. They do: 15,545 non-attached
dwellings across the three suburbs, 6,206 (40%) missing bedrooms, floor area or land size.

**⚠ THE FIRST DESIGN WOULD HAVE THROWN AWAY 84% OF THE YIELD.**
Copying the units script's shape, selection AND writing were both gated on a missing BEDROOM
count: if onthehouse published no beds, the page was discarded whole. Measured on 40 random
never-listed Burleigh Waters houses, only **3 of 40 had a bedroom count** — but many carried a
floor area, and floor area is missing on **37% of houses and 90% of units**. We were insisting on
the field they publish least in order to discard the field we are shortest of. Selecting and
writing per-field took the yield from **8% to 50%** on the same sample. The units script has the
same defect and under-fills for the same reason — a re-run with `--scope attached` recovers it.

**⚠ A `--limit 3` SMOKE TEST REPORTED A TOTAL FAILURE THAT WAS NOT ONE.** `--limit` took the
first N in cursor order, which front-loads the oldest cadastral stubs — precisely the records
onthehouse publishes least about. 3 of 3 came back empty and tripped the Rule 7b "resolved
NOTHING" guard. A random 40 yielded 8%. `--limit` now shuffles first: an ordered sample of a
sorted population measures the worst slice and reads as a broken pipeline.

**Three matcher bugs, all of which cause MISSES (never wrong matches), so all invisible unless
measured.** Together they cost ~1,400 recoverable matches:
1. **The suburb suffix defeated street-type canonicalisation.** Type normalisation applies to the
   last token; while a trailing `robina` was attached the last token was the suburb, so
   `1/1 Acacia Court Robina` stayed `acacia-court` and never met `acacia-ct`. The suburb now comes
   off first. This broke every address stored with its suburb, which is most of ours.
2. **`saint` vs `st`.** onthehouse publishes the same street as BOTH `st-ives-dr` and
   `saint-ives-dr`. `saint` now folds to `st` — except as the final token, where `St` means Street
   (`12 Saint Ives Dr` and `12 Ives St` must not converge).
3. **Double-dash ranges.** `7-8--14-st-ives-dr` is unit 7 at 8-14. The naive split produced an
   empty part and silently reparsed it as unit 7 at number 8.
Also `Cr`/`Cres`, `trail`/`trl`, `parkway`/`pkwy`, `quay`/`qys`, `point`/`pnt`.

**The structural fix that makes the whole thing safe:** the page is now the AUTHORITY and the
sitemap only a RECALL device. Every property page carries its own subject under
`propertyDetail.property` with a parsed address; `subject_of()` reads that and the caller requires
the returned address to equal the one requested. A wrong URL therefore produces **no write**
rather than a wrong write — which is what lets the index be generous about genuinely ambiguous
slugs (`2-4-riverwalk-ave` is either unit 2 at number 4, or the ranged address 2-4; unresolvable
from the slug, trivially resolved by the page). This also fixes a latent hazard: a page carries a
dozen NEIGHBOURS' `shortAddress` blocks and on `4-dexter-cl-robina` the subject is the
**fourteenth**, so any scan-and-take-the-first reads a different house's bedrooms.

**⚠ HOUSES VALIDATE AT 87.5%, NOT THE 100% UNITS GOT — AND THAT IS NOT A MATCHER FAULT.**
Held-out against 40 known-bedroom houses: 35 agree, 5 disagree, addresses matching exactly. The
disagreement is between SOURCES, and our own are no better: **our five bedroom paths disagree with
each other on 16.2% of non-attached dwellings, versus 1.7% of attached.** onthehouse disagreeing
with us 12.5% of the time sits *below* our own internal noise floor. One of the five "mismatches"
(6 Medinde Court) is onthehouse agreeing with our own `scraped_data_v2` while `bedrooms_of()`
returned a staler path. House bedroom counts are genuinely unstable (extensions, study-vs-bedroom
judgement); unit counts are not. Writing only where we hold NOTHING is therefore clearly
net-positive, and per-field provenance makes every row reversible.

**⚠ VALIDATION IS BIASED OPTIMISTIC AND MUST BE READ THAT WAY.** Ground truth only exists for
homes that HAVE been listed, but the target population has NEVER been listed. The 8%/50% yield
figures above are the honest measure of the real population; the 87.5% accuracy is measured on an
easier one.

**Fix:** new shared `scripts/onthehouse_match.py` (one matcher, not one per script),
`scripts/test_onthehouse_match.py` (27 cases, every trap that has actually occurred, no network),
`scripts/onthehouse_backfill.py` (per-field fill-only writes, provenance per field, Rule 7b
assertions on every zero-output path, `--wait-for` so only ONE crawler is on the site at a time).
**Files:** scripts/onthehouse_match.py, scripts/test_onthehouse_match.py, scripts/onthehouse_backfill.py
**Recurrence:** Second — see [ONTHEHOUSE-UNIT-ATTRIBUTES] earlier today. Same lesson twice:
**"we don't have it" is a claim about where you looked**, and the sampling you use to check
yourself is itself a claim that needs testing.

## [FB-ORGANIC-INSIGHTS] Organic post insights + article↔post linkage + article posting path — 18:05 AEST
**Symptom:** Will wants to trial posting published articles organically and measure which ones
work before putting any paid budget behind a winner. That was not measurable: all 16 documents
in `system_monitor.fb_page_posts` read `engagement: {likes:0, comments:0, shares:0}` and no
views/reach/clicks had ever been collected. There was also no way to tell which post promoted
which article, and no script to post an article at all.
**Root cause:** `scripts/post-performance-tracker.py` only read the post's `likes`/`comments`/
`shares` edges. It never called the Graph API `/insights` edge, so every distribution metric
(clicks, reach, reactions by type) was simply absent — the zeros were real but they were the
only numbers we had, and on a ~26-follower page reactions are almost always zero. Zero
reactions and zero distribution are indistinguishable in that shape, which is exactly the
Rule 7b failure mode applied to reads.
**Fix:** Three pieces.
1. *Insights collection.* `fetch_post_insights()` requests `INSIGHT_METRICS` from
   `/{post_id}/insights` and stores them in a new `insights` sub-document
   (`{metrics, unavailable, api_version, collected_at}`) — the legacy `engagement` field is
   left untouched. Facebook rejects an ENTIRE batch if one metric name is invalid, so the
   batch is tried first and on failure we degrade to one request per metric; a refused metric
   is recorded in `unavailable` with its reason and printed, never reported as a zero.
   ⚠ **Verified live today: the post-level impressions/reach family is gone.**
   `post_impressions`, `post_impressions_unique`, `post_impressions_organic`,
   `post_engaged_users`, `post_clicks_unique` and `post_negative_feedback` all return
   `(#100) The value must be a valid insights metric` on **both v18.0 and v23.0**, while
   `/me/permissions` shows `read_insights: granted` — so this is Meta's deprecation, not a
   token problem, and no permission fix will bring them back. What DOES return data:
   `post_clicks`, `post_clicks_by_type`, `post_reactions_by_type_total`,
   `post_reactions_like_total`, `post_activity_by_action_type`, `post_fan_reach` (reach among
   page fans — the only reach-ish number left) and the `post_video_*` family. We still request
   the dead ones so they reappear automatically if Meta restores or renames them.
   **Consequence for the trial: rank articles on `post_clicks` (+ `post_fan_reach`), not reach.**
2. *Article linkage.* `article_id` / `article_slug` on `fb_page_posts`, plus
   `--backfill-articles` which parses `/article/<id>` and `/articles/<slug>` out of the `link`
   field. The two 2026-03-05 posts carry pre-migration Ghost ids, so the resolver also tries
   `ghost_id` — without that both resolved to a slug of None. Posts with no article link are
   left untouched; we never guess which article an un-linked post was about.
3. *Posting path.* New `scripts/fb_post_article.py` — refuses anything not
   `status: "published"`, composes the post from the article's own hook (`custom_excerpt`,
   else first `<p>` of `html` stripped to text) + title + `/articles/<slug>` + tagline, reuses
   `post_to_page()` from `fb-page-post.py` rather than duplicating Graph API code, and writes
   an `fb_page_posts` doc with `article_id`/`article_slug` so the tracker picks it up. A
   Rule 5 editorial gate (`check_editorial`) refuses rather than silently sanitises: forbidden
   words, advice patterns, prediction patterns, `$1.25m` shorthand, uncapitalised suburbs, and
   a single valuation paired with a street address in the opening line. Two false positives
   were found and fixed by running it over all 73 published articles: URLs are excluded from
   the check (every slug is lowercase, so every link read as an uncapitalised suburb), and a
   recorded SALE price at an address is a fact rather than a valuation — the ban is on our
   estimate of worth, so sold context exempts it unless the headline uses valuation language
   ("worth", "valued at", "estimate", "could sell for"). Final: 71/73 pass, and the 2
   refusals are real breaches ("you need to" in a sell article, "$1.1M" shorthand).
**Verified:** `--refresh-insights` over all 16 posts → real numbers, e.g. `post_clicks=1,
post_clicks_by_type={link clicks:1}, post_fan_reach=2`; all 16 docs now carry `insights`.
Backfill linked exactly 2 posts, 14 untouched. `--dry-run` on
`what-does-1-500-000-buy-in-robina-in-2026` composes a 432-char post, editorial check PASS.
Draft refusal exits 1 with the status named. Nothing was posted to Facebook — Will posts.
**Files:** scripts/post-performance-tracker.py, scripts/fb_post_article.py (new)
**Recurrence:** First for this problem. Related in kind to Rule 7b:
a metric that is absent and a metric that is zero must not render identically.

## [CONTENT-CORPUS-UNJOINED] Ad headlines and their outcomes had never been joined — 22:40 AEST
**Symptom:** Fields' content learnings were unreadable by any generator. `ad_profiles`
(203 ads with lifetime delivery metrics, ~200 of them literally article titles used as ad
copy) and `ad_semantic_annotations` (92 ads classified by hook_type / emotional lever /
message theme) sat in the same database, joinable on `ad_id`, and **had never once been
joined** — so no one could ask "which hook type actually earned its clicks?". Separately,
the entire homeowner-funnel verdict set (2 copy archetypes, ~36 dead angles, ~26 operating
laws) existed only as prose across a 557-line ledger, 29 cycle docs and a 910-line test
summary.
**Root cause:** Not a bug — a missing artefact. Both halves were written by different
processes for different purposes and nothing ever reconciled them. The prose half compounds
it: prose cannot be queried, so dead angles were at permanent risk of being re-tested and
contradicted findings could sit side by side unnoticed (three do — see below).
**Fix:** Built two scripts.
`scripts/build_hook_corpus.py` joins the two collections (92/92 match on `ad_id`, zero
orphans) into `system_monitor.content_hook_corpus` + `content_hook_aggregates`. Evidence
discipline is the point, not the code: every aggregate is impression-weighted (sum clicks /
sum impressions, never a mean of per-ad CTRs), carries `n_ads` and `total_impressions`, and
is stamped `insufficient_evidence: true` below 500 impressions or 3 ads. `campaign_objective`
is recorded on every row and an `objective_mix` on every aggregate, plus a second set of
aggregates held *within* one objective — because `drafts/marketing-test-summary.md` documents
an earlier "property stories beat market commentary" conclusion produced by comparing an
OFFSITE_CONVERSIONS campaign to a POST_ENGAGEMENT one, and later retracted.
`scripts/build_content_learnings.py` writes 80 structured docs to
`system_monitor.content_learnings` (3 archetype, 26 law, 15 caution, 36 dead_angle), each
with finding, evidence (numbers + spend + n), source file, date, and an honest `confidence`
plus `confidence_reason`. Both have `--show` modes for an agent to read at cycle start.
`16_General_Reinforcement_Learning/CONTENT_LEARNINGS.md` documents query paths and the ten
limitations. Source Markdown untouched (read-only).
**What the join revealed (the reason this mattered):**
- **Zero lead-optimised ads are annotated.** All 92 sit in OUTCOME_ENGAGEMENT (57),
  OUTCOME_TRAFFIC (33) or OUTCOME_AWARENESS (2). The account's **108 OUTCOME_LEADS ads —
  including all 43 homeowner-funnel angles — are entirely unannotated**, and 0 attributable
  leads join to the corpus. So the whole hook taxonomy is evidence about CLICKS only, which
  is precisely the metric `law_personal_open_loop` says does not predict conversion here
  (9 documented high-CTR non-converters). Recorded as
  `caution_hook_corpus_has_no_lead_outcomes`; annotating those 108 ads is the top unblock.
- Only 55 of 92 rows clear a 500-impression floor. `story_narrative` has the highest
  weighted CTR of any hook type (16.19%) and is correctly gated out at n=2.
- **Three "established learnings" in CLAUDE.md are contested by our own sources** and are
  now stored as `caution` with a `contradicts` field rather than silently resolved:
  "broad targeting beats custom audiences" is explicitly *retracted* as confounded by the
  test summary (Part 5, Error 3); "phone field doesn't hurt lead volume" is directly
  contradicted by `NEXT_CYCLE_AD_PLAN_2026-07-30.md` §3 ($30/lead vs ~$15, same form);
  "OFFSITE_CONVERSIONS is the #1 lever" is rated Medium-High and confounded by its own
  source, whose isolating experiment got **one** website view.
- The brief's Archetype B figure of $4.27 CPL is the most flattering instant it ever had —
  it decayed to ~$28 by the angle's cull. And "4 quality leads" is really 3: one carried a
  fake +93 phone.
**Files:** scripts/build_hook_corpus.py (new), scripts/build_content_learnings.py (new),
16_General_Reinforcement_Learning/CONTENT_LEARNINGS.md (new)
**Recurrence:** First. Related in kind to Rule 8 — a corpus that cannot be queried produces
the same failure as a guessed field name: an absent answer indistinguishable from no answer.

## [ARTICLES-CONTINUOUS-AUTONOMY] Articles domain given a plan, the learning corpora, and the right to chain its own sessions — 18:20 AEST
**Why:** the article generator's self-learning loop was measured at **0% closed** — it had never read an outcome for anything it wrote. Will asked for a comprehensive plan the domain can lead from, and for it to keep working rather than wait a week when there is work in hand.
**Built:**
  - `ARTICLES_PLAN.md` — priorities P1-P6 and a reference list of every data source WITH its honest limitation (GSC is a single snapshot not a series; `ad_daily_metrics` keeps 90 days so pre-07-15 daily detail is gone; the hook corpus measures clicks only). Includes the three claims CLAUDE.md lists as "established, do not re-test" that our own sources contradict.
  - `article_chain.py` + `article_chain_dispatch.sh` (cron */20, 06-21) — the domain calls `--continue --reason "<specific next task>"` or `--stop --reason "..."` at the end of every session.
**Why this is not a repeat of the design that failed.** The previous system let SIX domains self-pace up to 14x/day, each with its own Telegram line to Will, and produced 31 decision items in 48 hours before being switched off. The difference is not the guards, it is what is capped: **the cap that mattered was on Will's attention, and it is untouched** — articles still cannot message Will and still holds at most 2 open recommendations. Chaining raises effort, not demand on him. Guards on top: 6/day, 20/week, 20-min floor, `--reason` must name a specific task (a 15-char minimum refuses "more work"), and — the important one — a **forced stop after 2 consecutive sessions producing no artefact**, where "artefact" is counted from the database (`rl_articles_actions`, `article_pending_approval`, `content_article_revisions`, `rl_recommendations`) and never from the agent's own claim. Working without output is precisely how the old system looked productive while achieving nothing; Rule 7b applied to chaining.
**Verified:** vague reason refused; claim-before-gap skips; cap arithmetic correct; barren streak forces a stop at 2 and says why. Crontab 111 -> 112, backed up first.
**Files:** 16_General_Reinforcement_Learning/{ARTICLES_PLAN.md, article_chain.py, article_chain_dispatch.sh, briefings/articles.md}, crontab
**Recurrence:** First

---

## [RL-VALUATION-DOMAIN-ADDED] Seventh RL domain — the first one watching the product, not the traffic — 18:50 AEST

**Symptom:** Not a bug — a structural blind spot. The General RL fleet rebuilt earlier today
(2026-08-13) ran six domains: geo, seo, ads, articles, onsite, ops. Five of those compete to
bring more people to the valuation; the sixth checks the machine is still running. **Nobody
was checking whether the number itself is any good** — while 59.4% of the live for-sale book
carries no valuation at all, and Burleigh Waters sits at 16.7% coverage.

**Root cause:** The rebuild ported the domain set from the paused daily system unchanged,
and that set was assembled top-of-funnel-first. The data-rich half of the business
(valuation quality, data coverage, editorial freshness, off-market deck integrity) has never
had a watcher. `README.md` named those four as future candidates but nothing scheduled one.

**Fix:** Added `valuation` as the seventh weekly domain, following today's pattern exactly —
sensor + mandate + standing brief + registry row + one cron line, no new machinery:

- **`valuation_signal.py`** (new) — measures coverage, envelope suppression, comp-pool health
  and self-contradiction across the three suburb collections, scoped `{"listing_status":
  "for_sale"}`. Writes `system_monitor.rl_valuation_signal` (`latest` + history). Rule 7b
  assertion: raises if the book is zero (scrape dead / collections moved, *not* an empty
  market) or if listings exist and not one carries a valuation.
- **`valuation_prompt.md`** (new) — mandate, subordinate to `_CYCLE_CONTRACT.md`. Binds the
  agent to `16_Valuation/`'s governance (append-only experiment record before any method
  change) and lists the eight already-rejected experiments so it cannot re-run them.
- **`briefings/valuation.md`** (new) — **week one is deliberately read-only**: sensors,
  backtests, reading and records only. A valuation edit reaches appraisal PDFs and posted
  mail within a day; Will reads one cycle before that door opens.
- **`domains.yaml`** — row + `fix_keywords` (verified against `fix_digest.py`: returns the
  valuation/appraisal/comparable entries and not the marketing ones).
- **`recommendations.py`**, **`briefing_status.py`** — both carried a hardcoded `DOMAINS`
  list; without both edits `propose --domain valuation` would have been rejected at argparse
  and the brief would never have been checked for freshness.
- **`samantha_weekly_prompt.md`**, **`samantha_weekly.sh`**, **`README.md`** — six → seven,
  plus a note to Samantha that this domain is not a marketing domain and will *propose*
  things the others would simply ship.
- **crontab** — one line, `0 12 * * 0` (after onsite 11:00, before Samantha 16:00). Backed
  up to `logs/crontab_backups/crontab_20260813_183821_pre_valuation.txt` first; active lines
  113 → 114 verified before and after.

**⚠ Rule 8 was the whole risk here, and it paid off.** Every path the sensor queries was
verified before use rather than recalled. Three of the obvious guesses were wrong:

- The premise "57% of the for-sale book can't be valued" is **59.4%** (126/212) as measured.
- `directional_only` is not top-level (**0/108**) — it is
  `valuation_data.confidence.directional_only`, and there are **three copies that disagree**
  (`confidence` / `summary` / `metadata`; the `metadata` one is also written `False`, so its
  fill count is not its true count, and it disagreed with the other two on 2 documents).
- `confidence_reason` — which CLAUDE.md's valuation section refers to — **does not exist**
  at that path. Three different reason fields do: `directional_reason`, `exclusion_reason`,
  `suburb_calibration.reason`.
- `valuation_data` is present on **212/212** live listings, so an unvalued property is never
  a missing sub-document, it is `reconciled_valuation: null`. A sensor counting missing
  sub-documents would have reported **zero unvalued** and been believed.

**First run found (baseline, 2026-08-13):** 86/212 valued (40.6% of the raw book).
Integrity contradictions: 5 documents flagged `insufficient_data` while carrying a figure;
2 with disagreeing envelope flags. Comp pools are thinnest in Varsity Lakes (median 4 comps
used, 6 subjects with zero) against Robina's median 8.

**⚠ CORRECTED SAME DAY — see `[RL-VALUATION-SENSOR-READ-GAP]` below.** This entry originally
reported "76 of the 126 unvalued record no reason at all — 60% of failures are silent". That
was **my sensor's defect, not a data defect**, and the domain's own first cycle falsified it
within six minutes.

**Files:** `16_General_Reinforcement_Learning/valuation_signal.py` (new),
`valuation_prompt.md` (new), `briefings/valuation.md` (new), `domains.yaml`,
`recommendations.py`, `briefing_status.py`, `samantha_weekly_prompt.md`,
`samantha_weekly.sh`, `README.md`, crontab.

**Recurrence:** First.

---

## [RL-VALUATION-SENSOR-READ-GAP] The new valuation sensor asserted an absence it had not tested — three bugs — 19:15 AEST

**Symptom:** `valuation_signal.py`, shipped an hour earlier in
`[RL-VALUATION-DOMAIN-ADDED]`, reported that **76 of 126 unvalued properties "record no
reason at all"**, and that envelope suppression was near zero (`above 0` for Robina). Both
were false. The claim propagated into the standing brief, the fix-history entry, a memory
file and my report to Will before anything checked it.

**Root cause:** Four distinct errors, three of them mine, all in the same instrument:

1. **Read gap (Rule 8 against our own tooling).** Three separate writers stamp a refusal —
   `exclusion_reason` (data failure), `directional_reason` (envelope refusal), and the
   `confidence` tier itself (`insufficient_data`). The sensor read the first only and
   labelled the residual "(no reason recorded)" — a phrase asserting an outcome the query
   could not support. The docstring correctly verified that `exclusion_reason` **exists**;
   it never asked whether it is the **only** place a reason lives. Verified: all 76 carry a
   reason (42 `directional` + 34 `insufficient_data`), all 76 have `computed_at`, none is a
   crash.
2. **Envelope counter gated on `has_rv`.** The one population it exists to measure —
   properties the envelope *suppressed*, which by definition have **no**
   `reconciled_valuation` — was the exact population it could not see. Robina printed
   `above 0` against 25 real `above_design_ceiling` flags.
3. **Range check swallowed by an `elif`** while fixing (2), so it counted suppressed
   properties (whose range is stripped deliberately) as contradictions — 42 phantoms.
4. **`"house" in "townhouse"` is True.** A substring test for property type flagged 2
   townhouses as envelope violations. All 4 originally-flagged "valued outside the envelope"
   properties were attached dwellings, correctly valued by the unit method that went live
   2026-08-10 (`[UNITS-VALUATION-LIVE]`).

**Fix:** Reason resolution now falls through `exclusion_reason` → `directional_reason` →
tier, and only calls a document unexplained when **all** are empty (distinguishing
"never computed" from "no reason anywhere"). Envelope suppression is counted from
`directional_reason` on the unvalued. The range check is back inside the valued branch.
Property type is an **exact** match, never a substring. Post-fix the sensor reports the two
genuine contradictions and nothing else, both independently confirmed by direct query.

**What it actually means:** raw coverage of 40.6% is not a defect rate — it counts every
correct refusal as a failure. Against the population the house method serves (classified
House, inside the envelope, n = 69) coverage is **78.3%**, even across suburbs (Robina
77.6%, Varsity Lakes 81.8%, Burleigh Waters 77.8%). **Burleigh Waters' 16.7% is a
composition effect** — 45 of its 54 listings are attached or above the ceiling — so no
targeted data-sourcing effort is warranted there. The real thin surface is attached
dwellings: **28.3% valued (n = 113)**, dominated by empty comparable pools.

**How it was caught:** by the domain itself, on its first run, six minutes in. It was given
a read-only brief and a sensor, and its first act was to falsify the brief it was handed.
That is the system working — but note the sensor and the brief were written the same hour,
from each other, with nothing between them. A sensor's output should not be the sole
evidence for the brief that tells an agent how to read that sensor.

**Files:** `16_General_Reinforcement_Learning/valuation_signal.py`,
`briefings/valuation.md` (§1, §2, §3, §7, §8 rewritten), `logs/fix-history/2026-08-13.md`.

**Recurrence:** First under this ID. Same **class** as `[FIELD-NAME-GUESS-FALSE-ABSENCE]`
(2026-08-09) and `[STATUTORY-ASSERTION-SCOPE]` (2026-08-13) — an empty result reported as an
absence. Third occurrence of that class in five days.

## [HOOK-CORPUS-CLICKS-ONLY] Content-learning corpus could only learn clicks, not sellers — 19:30 AEST
**Symptom:** `content_hook_corpus` held 92 ads, 305,208 impressions, $2,624 spend — and
**not one lead-optimised ad**. Every row sat in OUTCOME_ENGAGEMENT / OUTCOME_TRAFFIC /
OUTCOME_AWARENESS. Zero `fb_leads` joined. The account's 108 OUTCOME_LEADS ads, including all
43 `AN##` homeowner-funnel angles, were unannotated and therefore invisible to the corpus. Any
article/content system optimising on it would have learned to be **interesting rather than
useful** — the exact failure the funnel run already documented (9 high-CTR non-converters;
"Archetype B" bought clicks at $4.27 CPL with **0% qualified intent** before decaying to ~$28).
**Root cause:** three separate gaps.
(a) `ad_annotate.py` had never been run over the lead campaigns.
(b) `ad_annotate.py` could no longer run at all: it `load_dotenv()`s `.env` at import, which
re-injects `ANTHROPIC_API_KEY` even when the caller unsets it, so `claude -p` aborted with
"connectors are disabled because ANTHROPIC_API_KEY … takes precedence" — exit 1 on every ad.
The module's docstring claims "no paid API" but nothing enforced it.
(c) `build_hook_corpus.py` counted leads (`leads_by_ad`) but had no concept of lead **quality**,
so a junk lead and a qualified seller were the same number — and CTR was the only ranked signal.
**Fix:**
- `ad_annotate.py`: strip `ANTHROPIC_API_KEY` / `ANTHROPIC_AUTH_TOKEN` / `ANTHROPIC_BACKEND`
  from the subprocess env in `call_opus`, enforcing subscription-only rather than trusting the
  caller. Annotated the 110 pending ads with the unchanged schema so the `ad_id` join holds.
- `build_hook_corpus.py`: added `grade_leads()` — per-ad lead ledger grading each `fb_leads`
  doc as qualified / unqualified_intent / unqualified_contact / ungraded / excluded.
  Qualification is **derived from the raw lead**, not hard-coded per angle: `selling_intent ==
  yes` AND a contactable AU phone. That reproduces the verified correction to the ledger's
  "4 quality leads" — one carried `+93413572138`, an Afghanistan code on a Gold Coast homeowner
  form — as **3**, and will catch the same defect in future leads automatically. `is_test`
  (Will's own AYH submission) is excluded entirely.
- Rows gained `qualified_leads`, `cost_per_qualified_lead_aud`, `is_lead_optimised`,
  `lead_grades`, `high_ctr_zero_qualified`, `lead_insufficient_evidence`.
- Aggregates gained a lead view scoped to lead-optimised delivery only (so a TRAFFIC ad's spend
  never dilutes a CPL), with `n_lead_optimised_ads`, `total_qualified_leads`,
  `qualified_rate_pct`, `cost_per_qualified_lead_aud`, `lead_rankable`.
- New `content_hook_findings` doc `finding:high_ctr_zero_qualified` — names the ads that bought
  clicks and produced no qualified seller, so the corpus states it rather than leaving it to be
  inferred from an absent column.
- Writes switched from `delete_many` + `insert_many` to `ReplaceOne` upserts (another agent
  reads `content_hook_corpus` concurrently and could not distinguish "mid-rebuild" from "empty").
**Evidence discipline (the point of the task, not a footnote):** the lead sample is ~7 raw / 3
qualified across 34 angles. Gates `MIN_LEADS=10` / `MIN_QUALIFIED_LEADS=5` are deliberately
unreachable, so **every** lead-side aggregate is stamped `lead_insufficient_evidence: true` and
`lead_rankable: false`. `campaign_objective` is on every row because a CTR gap between a TRAFFIC
ad and a LEADS ad is mostly the objective. One near-miss caught in review: the first version of
the named finding flagged the Buyer Brief ads as "zero qualified" when their forms never asked
the selling-intent question — unmeasured is not zero, the same error class as
`[FIELD-NAME-GUESS-FALSE-ABSENCE]`. Those ads are now excluded from the finding and reported
separately as `n_excluded_ungraded_ads`.
**Files:** `scripts/brain2/ad_annotate.py`, `scripts/build_hook_corpus.py`,
`logs/fix-history/2026-08-13.md`.
**Recurrence:** First for the corpus. The unmeasured-vs-zero near-miss is the **fourth**
occurrence in five days of the class behind `[FIELD-NAME-GUESS-FALSE-ABSENCE]` (2026-08-09),
`[STATUTORY-ASSERTION-SCOPE]` and `[VALUATION-ABSENCE-AS-FACT]` (both 2026-08-13).

## [ARTICLE-LICENCE-GENERATOR-SOURCE] Found the thing still emitting the wrong QLD licence number — 19:40 AEST
**Symptom:** 68 articles carried QLD licence `4832971`; the correct number is `4832972`. The corpus was corrected earlier today ([ARTICLE-LICENCE-WRONG-CORPUS]), but 28 of the 68 had been created in August 2026 — so something was still writing it, and the corpus would have re-accumulated the error from the next generated article onward. `ARTICLES_PLAN.md` recorded "not in the automation repo, not in local config" and hypothesised the model was reproducing it from our own older articles.
**Root cause:** It was in the automation repo. `fields-automation/scripts/push_to_ghost.py:125` hardcodes `'Fields Real Estate (Licence No. 4832971) makes no warranty...'` into the disclaimer appended to every article it pushes. The earlier search had looked for a config value; the number was inline in a string literal.
A filesystem-wide search for the literal also found **two live emitters outside the article corpus that nobody had looked for**, because previous passes scoped themselves to articles:
- `01_Website/public/launch/{a,b,c}/index.html` — three seller-facing landing pages, live, carrying the wrong number 3/2/2 times each. One instance reads *"You can verify on the Office of Fair Trading register"* — inviting a check that would fail.
- `01_Website/netlify/functions/launch-form.mjs:133` — sends `QLD #4832971` in the form response.
**Fix:** Corrected all five files to `4832972`. Article corpus verified already-correct live (`/data/articles.json` serves 41× `4832972`, 0× wrong — the local `public/data/articles.json` is a stale build artefact, not a live problem, since the build now pulls from the public endpoint).
**Files:** `fields-automation/scripts/push_to_ghost.py`, `01_Website/netlify/functions/launch-form.mjs`, `01_Website/public/launch/{a,b,c}/index.html`
**Recurrence:** 3rd (2026-06-21 `[APPRAISAL-COMPLIANCE]` corrected 3 website pages; 2026-08-13 `[ARTICLE-LICENCE-WRONG-CORPUS]` corrected 68 articles; this one found the writer). The first two fixed instances; this is the first pass that looked for the *source*, and the first that searched outside the surface where the symptom was noticed.

## [EDITORIAL-GATE-NEVER-READ-BODIES] The Rule 5 checker had never been shown an article body — 19:45 AEST
**Symptom:** `ARTICLES_PLAN.md` recorded "P4 — Fix the two live editorial breaches. Found by the Rule 5 gate across 73 published articles (71 pass)". A body-level scan found **14 of 73 breaching**, including an entire live section of `leading-vs-lagging-indicators` headed "Practical Application: A Simple Monitoring Framework" instructing readers to *"Start your property search now"*, *"consider listing sooner rather than later"*, *"Strong buy signal"*, *"sell into strength"* and *"Make buy/hold/sell decision"*. Rule 5's no-advice rule exists specifically for liability, and this was the clearest breach of it we have had on a public page.
**Root cause:** The only Rule 5 implementation in the article path lived inside `scripts/fb_post_article.py` and was only ever called on the **composed Facebook post** — a title plus a one-line excerpt. It was never handed an article body. So "71 of 73 pass" was a true statement about 73 headlines and told us nothing about 73 articles. The two breaches it did find were in text short enough to fall inside an excerpt.
The same gap reached Will: three of the fifteen drafts proposed in REC-articles-001 were described to him as "Rule 5 clean" on that basis; one (`13 Waitara Place`) carried `$1.5M–$1.65M` shorthand.
**Fix:**
- New `scripts/editorial_gate.py` — runs the rule set over the full body (HTML stripped, base64 chart data URIs excluded), with the `fb_post_article` patterns plus the body-only constructs the first scan surfaced. Prints the surrounding sentence for every hit so a human can judge it.
- Two false-positive classes suppressed structurally rather than by loosening the rules: a prediction inside an explicit disclaimer ("…is a historical relationship, **not a forecast**") and shorthand at project scale (`$450M` tower, `$5.75B` rail — Rule 5's number format is about property prices; the floor is $20,000,000, above any residential sale here).
- Eight remaining false positives are listed individually in `ACKNOWLEDGED` **with the reason each was judged compliant**, so the gate reaches a genuinely clean exit 0. A gate that always reports the same seven breaches is one everyone learns to ignore, and the next real breach lands in that noise.
- Corrected all 14 breaching published articles + 1 draft. Corpus now `scanned 100 · clean 100 · with breaches 0`.
- Wired into `scripts/article_approval.py propose`, which now **refuses** to put a draft in front of Will if its body fails. `--allow-breach` is the deliberate escape hatch and the help text says to record the reason in `ACKNOWLEDGED` when used.
**Files:** `scripts/editorial_gate.py` (new), `scripts/article_approval.py`, 15 documents in `system_monitor.content_articles`
**Recurrence:** Number-format shorthand is 3rd in this class (`[EDITORIAL-NUMBER-FORMAT]` 2026-06, `[PRICE-FORMAT-ABBREV]` 2026-07 — that one also concluded the source was a prompt). The no-advice breach is 1st. The general shape — **a check that reports PASS over a surface it was never given** — is the same failure as CLAUDE.md Rule 7b and Rule 8: a clean result must assert that something was examined, not merely that nothing threw.

## [ARTICLE-GENERATOR-NO-FEEDBACK-PATH] Article generator never read its own outcomes — built the injection, not deployed — 21:40 AEST
**Symptom:** The article generator (`Will954633/fields-automation` @ `pipeline/`) wrote articles and never found out whether any worked. Prompts are static files; the only runtime context is property data plus `market_context_loader`. Grepping the pipeline for any performance collection returns nothing. Self-learning loop closed at 0%.
**Root cause:** Never built. The three collections it reads (`precomputed_market_charts`, `precomputed_indexed_prices`, `precomputed_active_listings`) are all property data, and the 14-angle `scoring_engine.py` rule table scores property attributes only. The outcome corpora (`article_performance`, `content_learnings`, `content_hook_corpus`) were only created 2026-08-13 and nothing consumed them.
**Fix:** Built a bounded feedback path, **not deployed** (changes public copy → Will's call; proposed as REC-articles-003).
  - `scripts/build_learning_snapshot.py` (orchestrator) compiles a curated, diff-reviewable JSON snapshot: article-native headline CTR ≥500 impressions, read-depth rows n≥10, 8 writing-relevant laws, 3 mandatory cautions, and 5 hook-failure mechanics abstracted from the 36 dead angles. Rule 7b assertions raise on a zero-headline or missing-caution build rather than shipping an empty block.
  - `pipeline/learning_context.py` (automation) renders it to ~6,085 chars / ~1,450 tokens. Degrades to `""` when the snapshot is absent, exactly like `market_context_loader`. `_caveat_block()` is emitted unconditionally and cannot be disabled by a caller.
  - Wired into `article_generator.py` (new `{learning_context_block}` placeholder) and `watch_article_generator.py` (injected before `## INSTRUCTIONS`, covering all 14 angle templates without editing them).
  - `article_prompt_template.md`: moved `## The Result` from section 6 of 8 to section 3, because the measured flagship (9.97% CTR) averaged 10.1% scroll over 37 sessions and `law_facebook_scroll_depth_caps_at_50pct` says the payoff cannot live at the bottom.
**Two corrections to the stated prior:** (1) The 36 dead angles are AN1–AN40 — Facebook *lead-form* ad concepts with **zero namespace overlap** with the generator's `BIGGEST_GAIN`…`VIEW_PREMIUM` angles. The generator has never proposed one and cannot, so injecting them verbatim answers a question nobody asked, and several died as *topics this pipeline legitimately covers* (AN13 renovation ROI, AN19 suburb split, AN40 kitchen ROI) on 70–450 impressions, out of market. Only the abstracted failure mechanics ship, scoped in-prompt to hook construction. (2) `content_hook_aggregates` was excluded entirely — ad copy, other market, and superseded by article-native headline CTR at 12,195 and 44,198 impressions.
**Evidence honesty:** the block carries `caution_hook_corpus_has_no_lead_outcomes` into the prompt as a "What this evidence is NOT" section stating that every number measures a click, that nine high-CTR angles converted nobody, and that the editorial rules outrank every line of the block. Prose figures are derived from the snapshot, never hardcoded — an earlier draft cited "44,198 impressions at 0.31%" from `ARTICLES_PLAN.md`, a figure absent from `article_performance`; that was replaced with a computed `max(impressions)` over the sub-1% rows so the model can never be handed a number we cannot show.
**Verification:** `_build_user_prompt` on a real Burleigh Waters sale resolves 0 leftover placeholders; block present, caveat present; watch injection lands market(1416) → learning(2462) → INSTRUCTIONS(8551). No generation run, nothing pushed to `fields-automation`, no GitHub Action triggered.
**Files:** `scripts/build_learning_snapshot.py`, `16_General_Reinforcement_Learning/artifacts/learning_snapshot.json`, `16_General_Reinforcement_Learning/artifacts/fields-automation_learning_context.patch` (the reviewable 5-file diff), `logs/fix-history/2026-08-13.md`
**Recurrence:** First

## [GH-EMPTY-PUSH-RELATIVE-PATH] Four files pushed EMPTY to GitHub while gh api reported success — 19:20 AEST
**Symptom:** A loop pushing 4 files printed `base64: No such file or directory` for every file **and a valid commit SHA for every file**. The relative paths were resolved against a cwd that had moved into `16_General_Reinforcement_Learning/`. `CONTENT` was therefore empty, and `gh api ... --field content=""` overwrote `ARTICLES_PLAN.md`, `01_BUILD_LOG.md`, the cycle doc and the 245KB `logs/fix-history/2026-08-13.md` with **nothing**, returning success each time.
**Root cause:** Two things compounding. (1) `gh api contents` will happily PUT empty content and calls it a commit — the documented behaviour in memory `gh_api_large_file_gotcha` ("PUT returns SUCCESS while pushing wrong bytes; md5-verify"). (2) The shell's cwd is not stable across calls in this environment, so a relative path in a push loop is unsafe by construction.
**Fix:** Re-pushed all four from absolute paths through a JSON payload + `--input`, then **re-fetched each and compared md5** — all four now byte-identical. Also md5-verified the three code files pushed earlier in the session (`editorial_gate.py`, `article_approval.py`, `push_to_ghost.py`); all three were fine, because that loop ran from the correct cwd.
**Files:** re-pushed `16_General_Reinforcement_Learning/{ARTICLES_PLAN.md,01_BUILD_LOG.md,cycles/2026-W33/2026-08-13/articles_cycle_20260813_1856.md}`, `logs/fix-history/2026-08-13.md`
**Recurrence:** Nth — this is the exact failure mode `scripts/push_website_files.py` was written to prevent, and the exact one memory `gh_api_large_file_gotcha` records. I used that script correctly for the website files earlier in this session and then hand-rolled a `gh api` loop for the orchestrator repo. **The lesson is not "verify md5" — it is that the hand-rolled loop should not be written at all.** `push_website_files.py` takes `--repo`/`--root` and covers this case.

## [ARTICLE-PUBLISH-GATE-BYPASS] Two side doors around Will's Telegram review gate — 19:40 AEST
**Symptom:** Will set policy — *"our new method of publishing articles is to send them to me on telegram for review. each article must now go via that path."* The path exists (`article_approval.py`), but an audit of every writer to `content_articles` found two ways to publish without it.
**Root cause:** The gate was built as a *mechanism*, not as an *invariant*. Nothing prevented a direct `status: "published"` write.
  1. `push-ghost-draft.py --publish` — sets published on create (line ~132) and on update (line ~201).
  2. `funnel-publisher.py` — **INSERTS** funnel landing pages into `content_articles` already marked `published`. Not obviously "an article", but it lands in the article store and renders at `/articles/<slug>`, so neither a reader nor Google can tell the difference.
  Neither is on cron, so nothing was auto-publishing — but both are invocable by a person or an agent, which is enough. The point of moving a rule into code is that instructions get skipped under load and code does not.
**Fix:** `scripts/article_publish_guard.py` — `require_approval(caller, slug, override)` called before any write that would make an article public. Wired into both paths. Refuses with the exact `article_approval.py propose` command to run instead.
  Two deliberate design choices: (a) there IS an override flag, because a blanket ban gets worked around by deleting the import, whereas a loud documented escape hatch survives contact with reality; (b) every override is recorded to `system_monitor.article_publish_overrides` with a reason, so an exception is visible rather than silent.
  The refusal message also names a second benefit the direct path never had: approval fires the Netlify deploy, so the article reaches the articles LIST. A raw status write does not — it produces a published-but-unlisted article, the exact failure logged earlier today as `[ARTICLE-PUBLISH-NO-DEPLOY]`.
**Verified:** `--publish` refused and **no document created**; `--draft` still works and writes `status: draft` (the gate blocks publishing, not authoring). Test artefacts cleaned up.
**Not a bypass, checked:** orchestrator step 120 `AUTO_PUBLISH=1` writes `ai_analysis` on PROPERTY documents, a different surface entirely. It does not touch `content_articles`.
**Files:** scripts/article_publish_guard.py (new), scripts/push-ghost-draft.py, scripts/funnel-publisher.py
**Recurrence:** First
