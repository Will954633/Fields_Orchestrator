# Fix History — 2026-06-25 (AEST)

## [MINI-SITE-VAL-PHOTOS] Comparable photos on the mini-site Valuation tab — 11:58 AEST
**Symptom:** The `/your-home` Valuation tab listed each comparable sale the home is being compared to, but showed no photos of those homes — users couldn't see what their property was being measured against. The card had large unused space from centre to right.
**Root cause:** `valuation_evidence_from_engine()` emitted no photo data for comps. The engine only attaches Azure-blob image URLs to each comp (`fieldspropertyimages.blob.core.windows.net`), which return **403 publicly** (same root cause as [HOUSE-MINI-SITE-009] — Azure blob account disabled). The frontend `EvidenceComparable` type had no photo field.
**Fix:**
- **Backend** ([scripts/property_reports/slot_resolver.py](../../scripts/property_reports/slot_resolver.py)): `valuation_evidence_from_engine()` now attaches up to 15 publicly-displayable photos per comparable as `{thumb, full}` pairs. New helpers `_comp_source_doc()` (resolves the comp's Gold_Coast source doc by `_id` → address fallback, suburb parsed from the comp address since comps can be cross-suburb) and `_comp_display_photos()` lift the comp's **Domain CDN** URLs — the same source the for-sale gallery serves: small signed `rimh2` thumbnails for the card strip, full-res `bucket-api` images for the lightbox, paired by Domain image-id stem. The Azure-blob mirror is deliberately excluded.
- **Frontend** (`Website_Version_Feb_2026`): added `photos?: {thumb,full}[]` to `EvidenceComparable`; `ValuationEvidence.tsx` now renders a thumbnail strip in the card's centre-right spare space (up to 4 thumbs + a `+N` overlay) and opens the existing `PhotoLightbox` on click (full-screen, `1/15` counter, prev/next, keyboard + swipe, scrollable thumbnail strip). New `ValuationEvidence.module.css` (responsive: thumbnails stack full-width below the text on mobile).
- **Backfill:** attached photos to the 10 existing `system_monitor.property_reports` reports that carry engine evidence (80/80 comps resolved photos) — surgical update of `valuation.evidence.comparables`, no recomputation of figures.
- Restarted `fields-property-report-poller` so new resolutions pick up the resolver change.
**Verification:** Resolver output checked against a real Robina valuation (8 comps × 15 photos, URLs return 200). Frontend typecheck + production build clean. Local visual verification via a Vite dev render of the template slug (temporary fixture injection, reverted): confirmed desktop centre-right thumbnails, `+11` overlay, lightbox, and mobile stacking.
**Files:** scripts/property_reports/slot_resolver.py; (website) src/pages/YourHomePage/data/homeFixture.ts, src/pages/YourHomePage/components/ValuationEvidence.tsx, src/pages/YourHomePage/components/ValuationEvidence.module.css.
**Recurrence:** First.

## [MINI-SITE-VAL-MIDPOINT-LABEL] Label midpoint value above the dot on the Valuation range bar — 14:20 AEST
**Symptom:** On the `/your-home` Valuation tab the range bar labelled the lower and upper bounds above its ends (e.g. $1,400,000 / $1,800,000) but the midpoint dot on the bar had no printed value — the midpoint figure only appeared in the prose below.
**Root cause:** Not a bug — the bar was built with lo/hi labels and a midpoint dot only; no label element was rendered above the dot.
**Fix:** In `ValuationEvidence.tsx` added a bold midpoint label inside the range-bar container, absolutely positioned at the same `midPct%` (`left`, `transform: translateX(-50%)`) as the dot and raised above the bar (`top: -26`) so it lands on the same line as the lo/hi labels. Bold (`fontWeight: 700`), copper (`var(--fields-copper)`) to tie it to the copper dot, `whiteSpace: nowrap`. Reuses existing `fmt$(mid)` / `midPct` — no new state or layout reflow.
**Files:** (website) src/pages/YourHomePage/components/ValuationEvidence.tsx. Commit 0727c3c → `Will954633/Website_Version_Feb_2026`.
**Recurrence:** First.

## [MINI-SITE-VAL-EVIDENCE-ORDER] Hoist valuation range bar to sit under the appraisal disclaimer — 15:05 AEST
**Symptom:** On the `/your-home` Valuation tab the "Evidence behind your range" block (range bar + "What this means for you") rendered well down the page — below the Statutory CMA and the "What this appraisal assumes" section. Will wanted it near the top, directly under the appraisal disclaimer ("This is a market appraisal … not a sworn or formal valuation …").
**Root cause:** Layout ordering only. In `ValuationTab.tsx` the disclaimer lives in the intro `<section>`; the `{evidence && <ValuationEvidence/>}` block was placed after StatutoryCMA + the assumptions section.
**Fix:** Split the intro section right after the disclaimer paragraph (`.yh-section` is just `margin-bottom: 56px`, so splitting avoids nesting `<section>`s), rendered `{evidence && <ValuationEvidence evidence={evidence} />}` there, then reopened a `<section>` for the SoWhat callout + under-review/recommendation cards. Removed the original evidence block lower down. New order: disclaimer → range-bar evidence → SoWhat/status → StatutoryCMA → assumptions → street-level → working. Final-tier reports (evidence null) only gain a 56px gap between disclaimer and SoWhat; no content reordering.
**Verification:** `tsc --noEmit` clean. Local Vite render (temporary engine-evidence fixture injection, reverted) screenshotted: disclaimer (Y≈1073) immediately followed by the evidence card (Y≈1249) and range bar (Y≈1319) — nothing between them. Pure frontend; reuses the same `evidence` gate, no resolver/backfill needed.
**Files:** (website) src/pages/YourHomePage/tabs/ValuationTab.tsx. Commit 82343e1 → `Will954633/Website_Version_Feb_2026`.
**Recurrence:** First. Same component family as [MINI-SITE-VAL-MIDPOINT-LABEL] (same day).

## [MINI-SITE-VAL-PHOTOS-SELFHOST] Self-host comparable photos from our own blob store — 12:35 AEST
**Symptom:** Comparable photos on the Valuation tab were served from Domain's CDN (`bucket-api.domain.com.au` / `rimh2.domainstatic.com.au`). Will asked why, given we pulled these photos locally.
**Root cause:** Our local blob store (`/data/blobs`, served at `blobs.fieldsestate.com.au`) only kept **150px cadastral thumbnails** for sold homes — full-res was retained only for currently-listed properties (`for_sale/`). So the lightbox needed Domain's full-res, and the whole site already served property photos from Domain CDN after the Azure blob account was disabled (see [IMAGE-OUTAGE-001]). Domain URLs can rotate/404 over time.
**Fix:**
- New `scripts/backfill_sold_fullres_photos.py`: for sold homes in core suburbs, downloads up to 15 full-res images from `property_images_original` (Domain bucket-api → b.domainstatic) and writes BOTH `property-images/sold/<suburb>/<id>/photos/<NN>.jpg` (full-res) and `.../thumbs/<NN>.jpg` (~320px JPEG, generated with PIL) to the local blob store — keyed by Mongo `_id` (== the engine comp `id`). Idempotent; concurrent; flags `sold_fullres_localized` on the doc.
- `slot_resolver.py`: new `_comp_local_photos()` builds `blobs.fieldsestate.com.au` `{thumb,full}` pairs (thumb from `thumbs/`, full from `photos/`) when a home has a localised gallery with both sizes; `valuation_evidence_from_engine()` now prefers it and falls back to the Domain CDN path (`_comp_display_photos`) only for homes not yet localised. Active comps (no `thumbs/`) stay on Domain for now.
- **Phase 1:** localised the 44 distinct sold comps backing the 10 live reports; re-backfilled `system_monitor.property_reports` so 75/80 comps now serve from our blob store (remainder are active listings → Domain).
- **Phase 2 (running):** full core-suburb backfill (robina, burleigh_waters, varsity_lakes, merrimac — ~1,390 sold homes) so future reports are self-hosted.
**Verification:** Live `/your-home/41-olympus-drive-robina` Valuation tab now serves all 8 comps from `blobs.fieldsestate.com.au` (full 805KB jpeg, thumb 11KB jpeg, both 200). Screenshots confirm thumbnails + full-res lightbox render from our own host. Thumbnails 320×222 (~18KB), full-res 1556×1080+ (~0.6MB).
**Files:** scripts/backfill_sold_fullres_photos.py (new); scripts/property_reports/slot_resolver.py.
**Recurrence:** First. Builds on [MINI-SITE-VAL-PHOTOS] (same day).

## [VAL-ASSUMPTIONS-CMA-COMPLIANCE] your-home valuation assumptions panel not written to PO Act CMA standard — 12:40 AEST
**Symptom:** The "The data and assumptions behind your range" panel on `/your-home/:slug#valuation` (rendered by `AssumptionsPanel` in `ValuationEvidence.tsx`) stated comparables were "sold in the last 18 months", carried no statutory framing, and cited no government policy — not aligned with QLD appraisal law/industry standard. Will flagged it against `00_Run_Commands/Industry_Governance`.
**Root cause:** (1) Factual error — the panel hardcoded "18 months" but the valuation engine ([precompute_valuations.py](../../../Feilds_Website/07_Valuation_Comps/precompute_valuations.py) L2848-2885) uses a **12-month** hard cutoff, recency-weighted, time-adjusted for sales >6mo. (2) The panel predated the statutory CMA layer (built 2026-06-21, audit item A) and never described the Property Occupations Act 2014 (Qld) s215 / Schedule 2 CMA requirement or the appraisal-vs-valuation distinction.
**Fix:** Rewrote the "How the comparables were chosen" bullets (12-month window, ±1 bedroom + price band, in-suburb; new bullet noting ≥3 sold/6mo/5km/similar identified separately as the CMA). Added a "How this meets Queensland requirements" block: appraisal-not-sworn-valuation statement, reasonable-grounds + current-as-at framing, and a footnote citing **PO Act 2014 (Qld) s215** (agent must give a CMA), **Schedule 2** (verbatim CMA definition: ≥3 sold/6mo/5km/similar standard), **s209** (reasonable grounds for price representations), the **Valuers Registration Act 1992 (Qld)** (valuation distinction), and the **Australian Consumer Law / Competition and Consumer Act 2010 (Cth) sch 2**. Statutory wording verified from the source PDFs in `00_Run_Commands/Industry_Governance`. Typecheck clean; pushed to `Will954633/Website_Version_Feb_2026` @ 6f2e2aa. Also corrected the same "last 18 months" factual error in the Valuation tab source line (ValuationTab.tsx L587 → 12 months) @ 1ab6312.
**Files:** (website) src/pages/YourHomePage/components/ValuationEvidence.tsx, src/pages/YourHomePage/tabs/ValuationTab.tsx
**Recurrence:** First. Completes the front-of-house copy for the statutory CMA work logged 2026-06-21 (audit item A).

## [MINI-SITE-VAL-EVIDENCE-MAP] Comparable-sales map on the Valuation tab "The Evidence" section — 12:55 AEST
**Symptom:** The `/your-home/:slug#valuation` "The Evidence" section listed the comparable sales behind the working range as cards, but — unlike the Competition tab, which has an interactive map — gave no geographic picture of where those sales sit relative to the home. Will asked for a map of all comparables used in the range, placed directly under "The evidence" with its own subheading + intro, above the existing "The sales your range is built from" cards.
**Root cause:** Feature gap, not a bug. `valuation_evidence_from_engine()` emitted each comp's address/price/weight but no coordinates, and no subject coordinates — so the frontend had nothing to plot. The engine comparable carries only `address`, `id` and `distance_km`; the lat/lng live on the comp's Gold_Coast source doc (`LATITUDE`/`LONGITUDE`).
**Fix:**
- **Backend** ([scripts/property_reports/slot_resolver.py](../../scripts/property_reports/slot_resolver.py)): `valuation_evidence_from_engine()` now adds `lat`/`lng` to each comparable and a `subject:{lat,lng,address}` block. Coordinates are lifted from the comp's source doc via the **existing** `_comp_source_doc()` lookup (already called once per comp for photos — extended `_COMP_PHOTO_PROJECTION` to also project the coordinate fields, so no extra DB cost). Comps whose source doc isn't geocoded get `lat/lng = null` and are simply not plotted (still shown in cards). Subject coords come from `subject_latlng()`.
- **Frontend** (`Will954633/Website_Version_Feb_2026`): new `EvidenceMap.tsx` + `EvidenceMap.module.css` (Mapbox GL, same token/pattern as `CompetitorMap`) — copper pulsing "your home" marker + numbered forest-green pins for each comparable (numbers match the comparable cards), teal for current-listing comps, popups with sold→adjusted price + distance + weight, auto-fit bounds, legend. Added `lat`/`lng` to `EvidenceComparable` and a `subject` block to the `ValuationEvidence` type. `ValuationEvidence.tsx` renders the map under "The evidence" eyebrow behind a new "Where these sales sit" heading + intro, above the existing cards; guarded so it only appears when subject + ≥1 comp are geocoded.
- **Backfill:** patched the 10 existing engine-evidence `system_monitor.property_reports` docs — added comp `lat/lng` (matched by id, prices/weights untouched) + subject block (5–8 of 8 comps geocoded per report; ungeocoded comps stay card-only).
- Restarted `fields-property-report-poller` so new resolutions emit coordinates.
**Verification:** Resolver output checked on `2-longueville-court-robina` (8/8 comps geocoded + subject). Frontend typecheck + production build clean. Live visual verification (headless Chrome w/ software WebGL) of `/your-home/2-longueville-court-robina#valuation`: map renders Mapbox tiles, copper subject marker, 8 numbered green comparable pins, legend "Comparable sales (8)", above the numbered cards. (My commit 504bc53 was later built upon by Will's parallel CMA-compliance commits 6f2e2aa/1ab6312 — map code intact in live HEAD.)
**Files:** scripts/property_reports/slot_resolver.py; (website) src/pages/YourHomePage/components/EvidenceMap.tsx (new), src/pages/YourHomePage/components/EvidenceMap.module.css (new), src/pages/YourHomePage/components/ValuationEvidence.tsx, src/pages/YourHomePage/data/homeFixture.ts.
**Recurrence:** First. Builds on the same `_comp_source_doc` lookup added by [MINI-SITE-VAL-PHOTOS] earlier today; mirrors the Competition-tab [HOUSE-MINI-SITE-004] competitor map.

## [MINI-SITE-VAL-PHOTOS-SELFHOST] Completion — active listings + full coverage — 13:30 AEST
**Follow-up to the entry above.** Extended self-hosting to active-listing comps and completed the full backfill:
- Generalised `backfill_sold_fullres_photos.py` with `--status sold|for_sale` (bucket-aware): writes a clean FLAT `photos/`+`thumbs/` per home (the resolver reads top-level files, ignoring the legacy nested-date `for_sale/` layout).
- **Full backfill done:** sold = 1,390 homes / 19,285 images; active = ~208 listings / 2,714 images. Blob store 249→284GB (nightly GCS rsync mirrors it).
- **Resolver fix:** `_comp_local_photos` now checks BOTH blob buckets (preferring the kind-implied one) — a listing comp may have SOLD since the report was generated, so its gallery moved from `for_sale/` to `sold/`. Without this, such comps wrongly fell back to Domain.
- **Result:** 77/80 live report comps now serve from `blobs.fieldsestate.com.au`. The 3 remaining are one property — "135 Camberwell Circuit, Robina" — which has no `property_images_original` (no full-res source to download); it keeps a graceful Domain fallback.
**Verification:** Live `/your-home/30-huntingdale-crescent-robina` Valuation tab — 12 thumbnails render, **0 failed blob requests**, the now-sold "16 Anglesea Court" listing comp serves from `blobs.fieldsestate.com.au` (resolved via the two-bucket fix).
**Files:** scripts/backfill_sold_fullres_photos.py; scripts/property_reports/slot_resolver.py.
**Recurrence:** First.
