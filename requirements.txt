# Fields Orchestrator Dependencies
# Last Updated: 26/01/2026, 7:59 PM (Brisbane Time)
#
# Install with: pip3 install -r requirements.txt

# YAML configuration parsing
PyYAML>=6.0

# MongoDB driver
pymongo>=4.0

# Azure Blob Storage (process 110 - download_images_to_blob.py)
azure-storage-blob>=12.0

# HTTP image downloads (process 110)
requests>=2.28

# Note: tkinter is included with Python on macOS
# Note: subprocess, threading, json, etc. are standard library

# QR codes on appraisal covers (attributable mini-site links) — process: generate_appraisal_v4
segno>=1.6
