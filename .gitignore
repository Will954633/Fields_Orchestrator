# Logs and runtime state
logs/*
# Exception: human-curated fix history is documentation, not runtime state
# (CLAUDE.md mandatory rule #1: every fix gets a log entry; rule #2: push for disaster recovery)
!logs/fix-history/
state/
*.log
*.pid
*.lock

# Python cache
__pycache__/
*.pyc
*.pyo

# Sensitive config (contains credentials)
config/settings.yaml
settings.yaml
config/config-data

# Old files with hardcoded credentials
mongodb_monitor.py
02_Deployment/scripts/fix_all_mongodb_connections.sh
02_Deployment/scripts/update_systemd_service.sh
02_Deployment/scripts/apply_remaining_fixes.sh
02_Deployment/scripts/list_database_collections.py
02_Deployment/scripts/check_today_data.py

# MD docs with embedded credentials
FLOOR_PLAN_OPENAI_FALLBACK_IMPLEMENTATION.md
VM_NIGHT_RUN_FEB16_FIX_COMPLETE.md
VM_ORCHESTRATOR_FIXES_COMPLETE.md
VM_MONGODB_CONNECTION_FIX_SUMMARY.md
VM_MONGODB_URI_EXPORT_FIX_COMPLETE.md
VM_ORCHESTRATOR_NIGHT_RUN_ANALYSIS.md

# Generated run artifacts
artifacts/
proposals/

# Backup/temp files
*.bak
*.backup
*.backup_*
*.backup_paths
*.BROKEN_LOCAL_PATHS_*
.tmp.driveupload/
.env

# Local Netlify folder
.netlify
node_modules
compliance_exports/

# Credentials — never commit
00_Run_Commands/gh-token-29Mar.txt
*-token-*.txt
*.token
.gcp-vertex-key.json

# Convenience symlinks to sibling projects under /home/fields — VM-local only,
# so every main folder is visible from one tree. Never tracked (a committed
# symlink would break any clone that doesn't have /home/fields).
actions-runner
audio_library
aux
backups
brain1_build
brain3_build
brain3_ops
brain_drive
build-logs
claude-terminal
Feilds_Website
fields-automation
knowledge-base
lab-funnels
offmarket-preview
Property_Data_Scraping
Property_Valuation
quarterly-preview
samantha-accounting
samantha-email-agent
samantha-knowledge-base

# Website visitor feedback — contains PII (emails), never push to public repo
feedback/

# Generated / regenerable per-address data — NOT source (produced by
# emit_json.py / batch.py / appraisal substantiation exporters). These bloat
# the working tree (~16k files) and trip VS Code's "too many active changes"
# limit. Generator scripts at the parent level stay tracked.
15_Off-Market/Page_Redesign_V2/bundles/
15_Off-Market/Page_Redesign_V2/output/
09_Appraisals/Substantiation/
# Local property-photo cache (regenerable from blob storage)
cache/
