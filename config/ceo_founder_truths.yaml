version: 1
updated_at_aest: "2026-03-17"

brand:
  company_name: "Fields Estate"
  tagline: "Smarter with data"
  mission: "Help buyers and sellers make informed real estate decisions through original analysis, local expertise, and transparent methodology."
  stage: "Pre-revenue"

systems:
  primary_database: "Gold_Coast"
  deprecated_databases:
    - "Gold_Coast_Currently_For_Sale"
    - "Gold_Coast_Recently_Sold"
  active_listings_filter:
    listing_status: "for_sale"
  sold_listings_filter:
    listing_status: "sold"
  enriched_listing_requirement: "valuation_data exists"
  website_repo: "Will954633/Website_Version_Feb_2026"
  orchestrator_repo: "Will954633/Fields_Orchestrator"
  context_repo: "Will954633/fields-ceo-context"
  sandbox_repo: "Will954633/fields-ceo-sandbox"

operating_rules:
  - "No production DB writes from the remote CEO VM."
  - "No direct deploys from the remote CEO VM."
  - "No direct ad changes from the remote CEO VM."
  - "Chief of Staff must synthesize only after successful specialist runs or explicitly degraded inputs."
  - "Blank telemetry exports are a blocking context failure, not an acceptable warning."
  - "All proposal findings should state confidence, evidence freshness, blocked_by, and data_gaps."
  - "Tuesday CEO reviews must explicitly check both the latest daily orchestrator run and the most recent weekly all-suburbs orchestrator run."
  - "Every CEO growth review must check both Facebook Ads and Google Ads and alert the founder to anything material."

target_market:
  suburbs:
    - "Robina"
    - "Burleigh Waters"
    - "Varsity Lakes"

website:
  deploy: "GitHub push to website repo triggers Netlify auto-deploy."
  database_env_var: "COSMOS_CONNECTION_STRING"
  health_contracts:
    - "/api/v1/properties/for-sale"
    - "/api/v1/properties/recently-sold"
    - "/api/v1/address-search"
    - "/api/v1/analyse-property"

ads:
  primary_metric: "Cost per website session"
  established_learnings:
    - "Sell-focused Facebook content does not work."
    - "Lifestyle imagery is a poor performer."
    - "Informational tone outperforms aspirational tone."
    - "Property-specific stories outperform generic market content."
    - "OFFSITE_CONVERSIONS is the correct optimisation goal for website traffic."
    - "Broad targeting with Advantage Audience outperforms custom audiences."
  policy_constraints:
    - "No valuation references in Facebook posts."
  ceo_review_rules:
    - "Compare ad performance against established learnings and documented tests before suggesting changes."
    - "Report early results, completed tests, delivery problems, tracking issues, and wasted spend."
    - "Only suggest a new ad test when there is a clear high-upside opportunity that has not already been run."

experimentation:
  minimum_samples:
    for_sale_page_v1_sessions: 800
    discover_mode_v1_sessions: 300
  review_rule: "Do not call an experiment before the minimum sample or when telemetry is degraded."

backup_scraper:
  purpose: "Redundant scraper on property-scraper VM — hedges against Domain.com.au bot detection blocking the primary curl_cffi scraper."
  vm: "property-scraper (35.201.6.222 external, 10.152.0.3 internal VPC)"
  ssh_user: "fields-orchestrator-vm"
  code_path: "/home/projects/scraper/"
  entry_point: "url_tracking_run.py → ContinuousMonitor (continuous_monitor.py)"
  method: "SearXNG meta-search → agency website scraping → GPT verification — does NOT hit Domain.com.au directly"
  suburbs_covered:
    - "robina"
    - "burleigh_waters"
    - "varsity_lakes"
  key_scripts:
    - "url_tracking_run.py — entry point, runs forever"
    - "continuous_monitor.py — main orchestration loop"
    - "hybrid_extraction_poc.py — extraction from agency websites"
    - "robust_extractor.py — fallback extraction"
    - "url_tracker.py — URL dedup and tracking"
    - "direct_agency_scraper.py — direct agency website scraper"
    - "gpt_verifier.py — GPT-4 listing verification"
    - "selfhosted_searxng_search.py — SearXNG search interface"
  status_check: "ssh fields-orchestrator-vm@35.201.6.222 'sudo bash /home/projects/scraper/status.sh'"
  log_check: "ssh fields-orchestrator-vm@35.201.6.222 'sudo tail -50 /home/projects/scraper/scraper.log'"
  operational_risk: "If Domain.com.au blocks our primary scraper, this backup must be ready to take over as the data source."
  review_cadence: "Engineering agent should check backup scraper health weekly and flag if it is down or producing zero new URLs."

cost_awareness:
  stage: "Pre-revenue — every dollar matters"
  monthly_budget_ceiling_aud: 3000
  cost_data_source: "metrics/cost_summary_30d.json"
  platforms:
    facebook_ads: "Variable — main advertising spend"
    google_ads: "Variable — search campaigns"
    google_cloud: "~$76/month fixed (2x e2-medium VMs)"
    azure_cosmos_db: "~$15/month (serverless)"
    azure_blob_storage: "~$8/month (property images)"
    netlify: "~$29/month (Pro plan)"
    openai_api: "~$25/month (GPT-4 Vision enrichment)"
    codex_agents: "Variable — ~$0.20-0.50/agent/run"
  review_rules:
    - "Growth agent must review cost trends in every daily run"
    - "Flag any day where total spend exceeds 2x the 7-day average"
    - "Track cost-per-session for all ad platforms"
    - "Propose budget reallocation when ROI diverges between platforms"
    - "Engineering should flag Cosmos RU spikes or excessive Netlify builds"

telemetry_slas:
  context_export_max_age_hours: 12
  scraper_freshness_max_age_hours: 12
  blank_export_policy: "abort_agent_run"

known_resolved_issues:
  updated_at: "2026-03-19"
  note: "Do NOT re-flag these. Verify fixes are holding instead."
  items:
    - id: "step_106_zombies"
      resolved: "2026-03-19"
      description: "64 orphaned process_runs records with status 'running' caused OPS to show 4 concurrent step 106 runs. Fixed by auto-cleanup in CEO pre-flight export and failed_stale filtering in OPS refresh and broker queries."
    - id: "recently_sold_route"
      resolved: "2026-03-18"
      description: "/api/v1/properties/recently-sold returns 200 with real data. Route paths were added to recently-sold.mjs and properties-for-sale.mjs wildcard was narrowed. Stale contract_issue flag in api_health_checks was also cleared."
    - id: "ops_counts_inflated"
      resolved: "2026-03-18"
      description: "OPS was counting all records instead of listing_status='for_sale'. Fixed in refresh-ops-context.py. OPS now shows correct counts (e.g. Robina 45, not 11,896)."
    - id: "facebook_attribution"
      resolved: "2026-03-18"
      description: "Facebook sessions were classified as 'direct' due to self-referral entry_referrer and missing utm_id check. Fixed in website-metrics-collector.py."
    - id: "google_ads_attribution"
      resolved: "2026-03-18"
      description: "Google Ads clicks invisible because tracker only checked gclid, not gad_source/gbraid. Fixed in tracker.js and website-metrics-collector.py. Note: Google Ads campaigns are currently PAUSED, so 0 Google sessions is expected."
    - id: "experiment_variant_race"
      resolved: "2026-03-18"
      description: "$setOnInsert race condition in visitor-track.mjs caused active_variants to always be null. Fixed by removing active_variants from $setOnInsert. Only affects NEW sessions — old sessions will not retroactively get variant data."

data_context_notes:
  effective_date: "2026-03-19"
  note: "Check these BEFORE flagging any telemetry anomaly as a current bug."
  items:
    - key: "google_ads_paused"
      detail: "All Google Ads campaigns are paused by Will. 0 Google sessions in the 7-day window is expected, not a tracking failure."
    - key: "facebook_attribution_history"
      detail: "Facebook attribution was fixed on 2026-03-18. Days before Mar 18 show incorrect or zero Facebook session counts. The 7-day window will contain pre-fix data until Mar 25. Weight analysis toward Mar 19+ data only."
    - key: "experiment_variant_backfill"
      detail: "Experiment variant fix deployed 2026-03-18. Sessions before that date have null variant data. Do not treat missing variants on pre-Mar-18 sessions as a current problem. Both experiments are now marked early_blocked."
    - key: "metrics_window_caveat"
      detail: "The 7-day metrics window includes pre-fix data from before Mar 18. Treat pre-fix days as unreliable for source attribution, variant analysis, and session counts."
