# Weekly Brief — 2026-W35 (cycle 4)

**You answered all five again — 5 of 5, second clean sweep.** Recorded 08-24 and 08-27, in your
own words. Four shipped inside 72 hours; the fifth (Gmail OAuth) is half-done by design and is
covered below in Health.

Seven of seven domains produced a cycle document. **Nine items open; five below, three wait, one
I fixed myself rather than spending a slot on it.**

Two things you should know before the list. **First: this was a week of self-correction, and I
want that on the record rather than buried.** seo discovered it had been reading 9% of Search
Console for the sensor's entire life and told you plainly that two things it put in front of you
were false. onsite withdrew its own recommendation because the evidence flipped inside seven
days. valuation found that a rule shipped this morning to stop false claims was itself justified
with false figures. Three domains chose to be wrong out loud. That is the system working, and it
is worth more than the findings.

**Second: six of your seven standing briefs expired 17 days ago**, so six domains spent this week
in NARROWED mode — bug fixes only, no new initiatives. Five of them independently ended their
cycle document naming that as their single biggest blocker. It is item 2.

---

## Decisions I need from you  (5 of 9 open)

### 1. Four people asked us to analyse their home, we promised a link, and never sent it
`[ads · S · reversible]`

The Owner-Market ad asks for an address, texts back *"I'm finalising your link and will text it
through shortly"* — and for these four, nothing was ever going to finish it. You hand-followed-up
Marjorie yourself on 08-27; the other three have had silence. The sharp one is **Jennifer Ng Liet
Hing, 6 Campelles Ave Varsity Lakes** — she is **currently listed for sale with Image Property
(agent Ben Ratcliffe)**, which makes her the clearest identified seller this paid channel has ever
produced, and she has been waiting two days.

**Evidence:** `fb_leads {om_needs_manual_link: True}` returns **4** (ads reported 3; a fourth,
Trish McCarthy / 11 Laura Place, landed 38 minutes after ads ran — so this is accruing at roughly
one a day while the campaign spends). n=4 of 4 paid Owner-Market address leads, $105. **All three
outstanding links exist and I verified them live just now, 200 OK:**
`/off-market/2-9-cedar-court-burleigh-heads` · `/property/6-campelles-avenue-varsity-lakes` ·
`/off-market/11-laura-place-varsity-lakes`. Jennifer's listing status confirmed directly:
`Gold_Coast.varsity_lakes` → `listing_status: for_sale`, Image Property Gold Coast. ads found and
fixed the resolver bug behind it (the ad's suburb was overriding the address the person typed) and
correctly did **not** contact anyone — that is never autonomous. Richard's 18 Glauca St is a
genuine data gap: Glauca St runs 1–15 in the index, so number 18 needs a corrected number or an
honest "we couldn't match that address", not more silence.

**→ Send the three links that now resolve? And for Jennifer specifically — she is under an agency
agreement, so POA s216(3) binds anything beyond fulfilling the analysis she personally asked for:
fulfil-only, or something more?**
(full detail: `recommendations.py show --id REC-ads-006`)

### 2. Six of your seven briefs expired 17 days ago. The domains have been narrowed for three weeks
`[all domains · S · reversible]`

A standing brief is an authorisation envelope, not a memo — when it goes stale it does not just
age, it **withdraws permission**. `briefing_status.py` puts geo, seo, ads, articles, ops and
valuation all at `stale` / NARROWED: bug fixes only, start nothing new. onsite alone is current,
and onsite is the only domain that shipped a new experiment this week. That is not a coincidence.

**Evidence:** all six last updated 2026-08-13 against a 7-day cadence. What it cost, in each
domain's own words: **articles** cannot build the onward-routing module — it has now measured that
13 of 13 conversions passed through an address field and **no article links to one**; **seo**
cannot touch `/off-market/`, which its corrected data shows is **68% of all organic clicks at
position 6.7**, a template it has never once worked on because the broken sensor told it it was
the worst on the site; **ads** is forbidden to advise on **$796.08 of live spend across six
campaigns** because its brief still says *"all ads are paused, deliberately"*; **geo** would have
fixed item 5 itself; **valuation** cannot start on attached dwellings (35.6% valued, n=104); **ops**
does not know whether the direct-call chain in item 3 is still wanted.

**→ Can we do the briefing session this week — 30–40 minutes? If you'd rather not sit down for it,
say so and I'll draft all six from your last known direction for you to correct in writing, which
is cheaper for you and slower for them.**
(full detail: `recommendations.py show --id REC-ads-007`)

### 3. Nine jobs have never once run on schedule — they are in no scheduler at all
`[ops · S · reversible]`

Nine of the ten STALE rows on the health board are not decayed jobs. They were built, run once by
hand to seed a heartbeat, declared a cadence, and then never wired into anything. Each has exactly
the run count its author gave it on the day it was written. **I promised you a paste-ready file
last week and ops delivered it** — `artifacts/REC-ops-005_cron_lines_DRAFT.txt`, with the four
direct-call jobs separated out as you'd expect, dependency-ordered, and timed to miss the nightly
pipeline window.

**Evidence:** I re-verified this myself just now rather than take it on trust — all nine scripts
return **0 hits** across `crontab -l`, `/etc/crontab` and `/etc/cron.d/*`; `systemctl list-timers`
lists 19 timers, none of them these. n=9, each checked individually, no sampling. Among them is
`offmarket_report_poller`, which is what dispatches paid report requests, last run 2026-08-13 — the
day it was written.

**→ Paste the nine lines? And confirm the nine are all still WANTED — if the direct-call chain was
shelved rather than forgotten (it sits under the IPA s45 / POA s215 question), say so and ops marks
those four KNOWN-GAP instead of chasing them. Either answer clears half the STALE column.**
(full detail: `recommendations.py show --id REC-ops-005`)

### 4. Your own bio page never says you are a licensed agent — and I need the wording from you
`[geo · S · reversible]`

You approved the `/about` fix on 08-27 and it shipped. This is the other half of the same entity:
`/about/will-simpson` is the page an AI engine reads to answer *"who is Will Simpson"*, and it
contains the words **"licensed" 0 times, "agent" 0 times, "real estate agency" 0 times**. Its
Credentials list in full: BSc, MBA, a Harvard negotiation certificate, 7 years at Blue Oceans,
Founder of Fields — **no licence**. Meanwhile `/about`'s schema declares the founder a *"Licensed
Real Estate Agent"* and links to that page. `/about` asserts the licence, then points at the page
that refutes it.

**Evidence:** counts taken live 2026-08-30 as `OAI-SearchBot/1.0`, exact and reproducible, N=1
page. Three grounded queries the same day: asked about Fields directly, the engine still returns
*"it is not a real estate agency"*; asked about you by name, the top three results are **Phillis
Real Estate — your former agency**. That query set is **n=3 on one surface and directional, not a
frequency estimate**. The mechanism is not directional though: geo proved on 08-23 that when schema
and prose disagree, the model follows the prose. Four exact edits are drafted and Rule 5 checked in
`REC-geo-004_DRAFT_founder_licence_identity.md`.

**→ Approve the four edits — and give me the exact licence wording: individual agent licence,
corporation licence held by Fields, or both, and do you want the number shown? geo refused to
publish a regulated credential on a real person's behalf from a line in a briefing file, which was
the right call. I need it from you.**
(full detail: `recommendations.py show --id REC-geo-004`)

### 5. `/about/<literally anything>` returns 200 — and last week I told seo the wrong fix
`[seo + geo merged · S · reversible]`

Last week I filtered seo's `/about` item and directed it to delete the static files and let the
React routes serve. **geo has since shown that would break things** — the `/about` profile link and
the site footer point at one of those URLs. I am bringing it back as one decision instead of two,
and correcting my own instruction.

**Evidence:** I re-verified live as Googlebot: `/about/fields`, `/about/fields-editorial`,
`/about/xyzzy` and `/about/totally-made-up-slug-12345` **all four return 200**, each with a
canonical pointing at the made-up URL and no noindex — so the entity cluster is unbounded, not four
URLs. Two of the duplicates are real and competing: `/about/fields` (in the sitemap, and the author
URL in **all 100 articles'** JSON-LD) and `/about/fields-editorial` (linked from `/about`), with
different titles and different copy for the same entity. Cause is `about.$authorSlug.tsx` — the
fallback branch sets a self-canonical for any slug with no validation and no 404. Separately, seo
already found **three different organisation names** claiming `fieldsestate.com.au` and fixed the
live pages; `/about` itself took 0 search impressions in 28 days, which is part of the point.

**→ Two answers. (a) Static files or React routes — which copy do we maintain? Either is fine and
the live pages are correct either way. (b) Confirm ONE canonical editorial-team URL and 301 the
other; `/about/fields` is the better keeper since 100 articles' schema already cites it.**
(full detail: `recommendations.py show --id REC-seo-006`, folded with `REC-geo-005`)

---

## Done without asking

- **onsite:** found and fixed a live address-search defect that was **silently zeroing 37% of
  queries typed with an abbreviated street type** (`Ct`, `Pde` — the index stores 10,618 COURT and
  zero CT), measured 246/388 → 388/388 over 400 sampled addresses. One visitor typed the same
  address 41 times and got nothing. This sat on `submitted_address`, the single highest-value
  action on the site.
- **ops:** fixed three separate "one bad record fails the whole batch" outages in one cycle — the
  Living Map building **0 of 51** listings (36 had usable coordinates under a field name the code
  never read), the Live Leads Tracker getting **no rows at all** because one lead ticked two
  suburbs, and step 15 failing the entire nightly run on **one unnamed powder room**. It explicitly
  did not raise the 5% error threshold to turn the board green.
- **seo:** rebuilt the Search Console collector after finding it had been storing 9% of
  impressions; updated all four downstream consumers, added a zero-row assertion and logged the
  coverage ratio so the gap can never be invisible again.
- **articles:** traced live "high-confidence range" copy to our own prompt teaching it, fixed it at
  three layers, and backfilled 19 published documents / 39 strings to zero.
- **geo:** killed a three-cycle-old mystery by testing it and then deciding to stop chasing it.
- **me:** corrected `CLAUDE.md`'s valuation block at the root and the editorial prompt clauses that
  inherited it — see "What I filtered out".

## Last week's answers, and what came of them

- **REC-articles-004** (seller advice on competitors' listings) — you chose option (b): strip the
  advice and the agency ranking, keep the pricing range. Approved 08-24.
- **REC-articles-005** (workflow pile-up) — you granted the PAT the Workflows permission and the
  13-file fix shipped. **Grades 2026-09-03.**
- **REC-ads-005** (Messenger carousel) — you had already stopped the ads yourself. Your key
  learning is recorded and goes into the ads re-brief: *the Facebook **Marketplace** listing of the
  same address succeeded where the paid Messenger carousel failed.* **Grades 2026-09-07.**
- **REC-ops-006** (Gmail OAuth) — you said fix it durably but find the cleanest path first
  (Workspace → Internal vs publish-to-Production, which risks a restricted-scope review).
  Investigation dispatched; you re-authed to unblock Friday. Still approved, not yet closed.
- **REC-geo-003** (agency identity) — *"being described wrongly is worse than obscurity."* Shipped
  08-27. **It must not be graded yet and geo said so unprompted:** the engines are still quoting a
  cache ~3 weeks old, citing a URL that has 301'd since 07-31. Grades 2026-09-20.

## What I filtered out and why

- **REC-valuation-004 (the valuation band's description) — I fixed it instead of briefing it.** The
  rule shipped this morning to *stop* false confidence claims justified itself with four false
  figures ("flat ±12%", "arithmetic not statistics", "contains the sale 61% of the time", "a 90%
  band needs ±26.4%"). All four describe a method retired on 2026-08-07. Production has emitted a
  per-suburb **empirical 80% band** since 08-08 (VL ±11.2%, Robina ±12.2%, BW ±14.0%, n=581 real
  sales) — a **stronger** claim that we were documenting as a weak one. Root cause was
  `CLAUDE.md:455`, never updated, which then infected two other domains' careful work on the same
  day. Corrected at the root plus the two copies, and the adjacent stale accuracy bullet
  (10.5%/8.2%/59% → the governing 8.05%/6.44%/69%, n=581). No reader-facing surface carried the
  stale figures — this was config, not copy. Pushed; fix-history `[VALUATION-BAND-STALE-AT-ROOT]`.
  **Not a decision, so it should not have cost you one.** The envelope question underneath it —
  does valuation get a narrow write on these clauses — goes into the briefing session.
- **REC-ops-007 (the ops heartbeat dies exactly when the tamper guard fires)** — real, mechanism
  proven in isolation, and a genuinely good refusal: ops declined to edit its own tamper guard's
  wrapper on its own judgement. But it is a one-line change and I will not spend a slot on it while
  five bigger things are open. **I also did not apply it myself** — CLAUDE.md forbids autonomous
  edits to monitoring code and `job_runs` absolutely, and "the change makes the alarm louder" is
  exactly the reasoning that rule exists to refuse. It goes in the briefing session as an envelope
  question, or first in the queue next week.
- **REC-seo-007 (`/for-sale-v3` vs `/houses-for-sale/` for general home-search)** — seo corrected
  its own numbers here and deserves credit for it: `/for-sale-v3` is at position **13.0**, not 41.3,
  and both other suburbs do rank. But seo's own honest read is the reason I held it: **the whole
  cluster earned 4 clicks in 90 days**, while `/off-market/` earned 801. It asked to be told whether
  this is a vanity target. That is a briefing question, not a standalone decision, and answering it
  in the wrong order would commit the site's intent to a URL before deciding whether the intent is
  worth owning.
- **The OpenAI account being at zero credits (7 days, every GPT-4o call 429s)** — raised by two
  domains, and it does cost money, which is yours alone. I checked what it actually breaks before
  bringing it: step 106 falls back to Claude, 108 and 117 route via OpenRouter, and the step that
  *is* broken is broken by DNS, not credits. So it is real but currently near-zero-impact. **Say
  the word and I'll add it; I did not want to spend a founder decision on a top-up that nothing is
  currently waiting on.**
- **ads' "$26.20 vs $199.00 per identified seller"** — the most encouraging number this domain has
  measured, and I have not let it travel as a headline. It rests on **n=4 and n=1**. It is
  corroborated by an independent dataset (`submitted_address` at 53× lift) and the mechanism is
  plausible, but it is a hypothesis, and ads said so itself. It belongs in the ads re-brief.

## Health

- **Domains that ran: 7 of 7.** Second consecutive full sweep, all seven cycle documents present.
- **⚠ ops's heartbeat is stale for the second week running** (last `rl_weekly_ops` run 2026-08-15)
  although ops ran and wrote a full document at 06:00 today. It is now diagnosed — that is
  REC-ops-007 above — and the diagnosis is worse than the symptom: the runner dies *before* writing
  its heartbeat precisely when the integrity guard fires, so the board goes **silent** in the one
  case it is built to make loud.
- **⚠ Genuinely broken, not yet a recommendation: pipeline step 105 processes ZERO properties every
  night across all six suburbs, prints "completed successfully" and exits 0.** Six-plus consecutive
  nights; `*.blob.core.windows.net` does not resolve from this VM while `google.com` does, so it
  looks like an egress policy rather than an Azure outage. articles found it and routed it to `all`,
  which means nobody owned it — **I have assigned it to ops as its first job next cycle**, including
  the instruction not to weaken the assertion if the DNS cause turns out to be infrastructure it
  cannot change.
- **Still open from 08-23 and unresolved: an `ops_integrity` ERROR flagging UNEXPECTED SOURCE EDITs**
  to `scripts/fb_approval.py` and `scripts/step117_satellite_analysis.py`, plus two junk `job_runs`
  rows that cycle created. ops declined to delete them on its own authority. Correct call —
  deleting heartbeats is absolute — and it does not stop being true because the mess is its own.
- **Track record:** you have said yes to 20 of 20 items across all domains. That is either very good
  triage or too cautious a filter, and I cannot tell which from inside it. **Nothing has yet been
  graded on whether it actually worked** — the first real gradings land 09-03 (articles), 09-07
  (ads) and 09-12 (seo). The one item graded so far, REC-ops-001, did **not** move its claimed
  metric. That 0/1 is the number I am watching, not the 20/20.
