# Weekly Brief — 2026-W34 (cycle 3)

**You answered all five last week.** Every item carries a verdict and a reason in your own
words, recorded 08-17/08-18. That is the first clean sweep since this cycle started, and it
changed what the domains did — three of the five shipped inside 48 hours.

One did not, and it is the reason this brief is shorter than it could be: **an item you
approved on 08-18 sat five days with nobody assigned to execute it.** I executed it during
this cycle rather than spending a slot asking you who should. Details in Health.

Seven of seven domains produced a cycle document. **Ten items open; five below, four wait,
one I merged and one I closed myself.**

---

## Decisions I need from you  (5 of 10 open)

### 1. We are publishing seller strategy — and an agency ranking — on our competitors' listings
`[articles · S · reversible]`

Three live property pages carry a "gated" tier meant for a seller's own private report. It
ships in the **uncredentialed public API** whether or not anyone clicks to reveal it. I
fetched all three myself. On WHITEFOX Gold Coast's own listing at 8 Trinity Place we publish:
a ranked table placing **McGrath and REMAX above the incumbent agent**, WHITEFOX marked
*"Insufficient Robina-specific data to rank"*, their agent **named** (Elle Anderson), the note
that *"switching agents now would reset DOM and create market stigma"*, and the instruction
**"Do not accept anything below $1,880,000"**. The other two are McGrath Palm Beach's and
AveNew Realty Group's listings.

**Evidence:** 3 of 71 live listings, each verified live today by me, not sampled —
`curl https://fieldsestate.com.au/api/property/<id>` → `positioning_analysis.gated`. This is
the same exposure as `[AGENT-LISTING-DISPARAGEMENT]` (2026-08-20), which cost a written
undertaking to Tyler Benson. **That was three days ago and this is still live.** Rule 5 forbids
advice outright; the agency ranking is a separate and larger problem.

**→ Withhold the gated tier from public property pages entirely (my recommendation), or keep
it minus the advice and the agency ranking?**
(full detail: `recommendations.py show --id REC-articles-004`)

### 2. Article automation cancels itself every Sunday — and tonight is Sunday
`[articles · S · reversible]`

Seven weekly workflows all fire into a 2h05m window on a runner that executes one job at a
time. On 08-16/17 the queue outran GitHub's 24-hour pending limit and **five runs were
cancelled without ever starting** — including How It Sold, the format that ranks page-1 for
exact-address queries. The corrected 13-file fix is written and validated on the VM and
**cannot be deployed**: our fine-grained PAT is refused 403 on `.github/workflows`, which needs
a separate GitHub permission.

**Evidence:** 5 of 13 workflows cancelled at ~24h queue time (`gh run list`), none ever
started — no `Worker_*.log` in `_diag` for 46 hours. No job had `timeout-minutes`, so one hang
held the runner 11h. Cost measured: 14 sold properties with existing Search Console demand
(271 impressions/30d) had no article; articles hand-wrote 7 this cycle to fill the gap.

**→ Grant the fields-automation PAT the GitHub "workflows" permission (one toggle, and the
domain ships its own fixes thereafter), or apply the 13-file diff yourself? Without one, the
same pile-up starts tonight at 20:00 UTC and How It Sold is cancelled again Monday.**
(full detail: `recommendations.py show --id REC-articles-005`)

### 3. The only ad still running is buying accidental taps, and three people have blocked us
`[ads · S · reversible]`

The 93 Burleigh Messenger carousel bought 8 conversations at **$11.96 each — cheaper than any
lead this business has ever bought.** ads read all eight from the Page inbox verbatim. Three
said they tapped by mistake (*"I accidentally tapped the link. Please leave me alone"*,
*"Mistaken button sorry"*, *"nothing i just liked the sunset ..wanted to zoom in"*), one wanted
a rental form, one is buying overseas. **Zero Gold Coast buyers, zero sellers, three Page
blocks.** You hand-answered every one.

**Evidence:** $95.69 / 3,238 impressions / 108 clicks / 8 conversations / 3 `messaging_block`,
2026-08-20→23. **n=8 is the complete census, not a sample** — the cost-per figure is
directional on 3 days, but the zero-qualified count is not an estimate. Sydney adset is the
worst: $28.84 bought 2 conversations and 2 of the 3 blocks. I confirmed all three adsets are
still ACTIVE, though delivery has fallen to near zero since 08-22.

**→ Switch the carousel destination from Messenger to the listing page so a tap costs us a
measurable page view, keep it as-is, or pause it?**
(full detail: `recommendations.py show --id REC-ads-005`)

### 4. All site email is dead, and re-authing it will not keep it alive
`[ops · S · reversible]`

Five Property Friday sent **0 of 4 emails on 08-21**. The Thursday pre-flight caught it exactly
as designed — the chore behind it is what failed. It will die again in 7 days no matter who
re-auths it, because the `fields-estate-ads` OAuth consent screen is still in **Testing mode**,
and Google expires Testing-mode refresh tokens weekly.

**Evidence:** I re-ran the pre-flight myself this afternoon: `dead — invalid_grant`. 4 of 4
recipients failed with `gmail_token_unavailable`. **5th occurrence in a month** — fix-history
entries on 07-24, 07-27, 08-07, 08-12. Part (a) needs your browser and Google account; I
cannot do it. Part (b) is the durable half, and it is the same shape as your own ruling on
REC-ops-004: *"a fix that depends on remembering is not a fix."*

**→ 4a. Re-auth today so Friday 08-28 sends (`python3 scripts/reauth_google_oauth.py`)?**
**→ 4b. Publish the consent screen to stop the 7-day expiry permanently — or is there a reason
it must stay in Testing mode?**
(full detail: `recommendations.py show --id REC-ops-006`)

### 5. Our own /about page tells AI engines we are not a real estate agency
`[geo · S · reversible — sequenced with seo]`

AI surfaces describe Fields as *"primarily a data company rather than a traditional real estate
agency"* — because our copy says so, near-verbatim. I read the live page today: /about carries
an **Independence** section reading *"Fields Research is not affiliated with any real estate
agency, property developer, or financial institution"*, and the word **"licensed" appears zero
times** — directly beneath JSON-LD declaring the page a `RealEstateAgent` whose founder is a
licensed agent. Both cannot be true, and an engine summarising a page follows the prose.

**Evidence:** 3 grounded queries re-run 08-23, reproducing the 08-16 result **3/3**, a week
after the crawler unblock shipped. *"best real estate agent Robina 4226"* returns Image
Property, Coastal, Ray White, RE/MAX — **Fields absent**. The traced quote is an exact
substring match; the query result is **directional, n=3 on one surface**, not a frequency
estimate. Five exact before/after edits are drafted and Rule 5 checked
(`REC-geo-003_DRAFT_about_page_agency_identity.md`).

**This is not purely a bug fix, and geo is right to say so.** That Independence section buys
editorial credibility — it is why our market analysis reads as research rather than agency
marketing, and the Editorial Policy page makes the same promise. The rewrite keeps every
promise that is *true* (no payment for coverage, not the listing agent on third-party listings)
and stops the one that is false. **The trade is yours.**

**→ May the Independence section be reworded so it stops denying we are an agency, keeping the
no-paid-coverage and not-the-listing-agent promises intact?**
(full detail: `recommendations.py show --id REC-geo-003`)

---

## Done without asking

- **ops:** fixed step 117, which had been failing the **entire nightly pipeline** for three
  consecutive nights on a legitimately empty queue.
- **seo:** found four `/about*` URLs served by hand-written static files shadowing the
  maintained React routes, and corrected all three files live — **three different organisation
  names** were claiming `fieldsestate.com.au` as their entity.
- **seo:** widened `brand_serp_signal.py` from 4 sampled pages to 9 after it reported the site
  clean while missing every /about page — it caught the fragmentation on the first re-run.
- **articles:** pre-registered a matched-pair experiment *before* writing a word — 14 sold
  addresses split on impressions and suburb, 7 written, 7 deliberately left unwritten as a
  control, with the property-type imbalance stated rather than buried.
- **ads:** read all 8 Messenger conversations verbatim from the Page inbox rather than
  reporting the $11.96 cost-per that Meta showed it.
- **valuation:** re-derived REC-002's disputed figure independently before re-raising it; it
  held (35.5%, stable against 34.2% a week earlier on the same 38 properties).
- **me:** executed the approved-but-unowned valuation sensor fix — see Health.

## Last week's answers, and what came of them

- **REC-articles-002 / -003, REC-seo-001 / -002** — shipped, none due for grading yet.
- **REC-geo-002** (overseas 403 + noindex) — you said serve a normal 200 with a region notice,
  and explicitly told me **not** to later grade it on Bing impressions alone. Shipped. Recorded
  with that constraint attached so the grader cannot quietly ignore it.
- **REC-ops-004** (rotated secrets) — you chose the **automatic** systemd path unit over the
  manual script, on the grounds that "the manual version is exactly what already failed."
- **REC-ads-001 / -004** — you approved the on-site address submit over Instant Forms, and
  **one** buyer-framed arm carrying the seller qualifier, not all arms. You also instructed
  that n=7 be reported as a hypothesis when it grades. Noted in the ledger.
- **REC-onsite-003** — you ratified `genrl_personalization_v1` live at 100% rather than
  flipping it off, and approved mounting the slot on both the deck and the property page.
- **REC-valuation-002** — approved 08-18, and then **nothing happened for five days.** This is
  the failure of the week and it is mine, not the domain's. See Health.

## What I filtered out and why

- **REC-seo-006 (which /about copy do we maintain)** — seo says "either is fine" and has already
  made the live pages correct, so this is engineering hygiene, not a founder decision. **I have
  directed seo to delete the static files and let the React routes serve** — one source of
  truth, and it stops the next person fixing copy that nobody is served. Sequenced *before*
  item 5, so geo's edits land in the file that actually renders.
- **REC-ops-005 (nine jobs in no scheduler, never once run)** — real, verified nine ways, and
  cheap for you (paste nine cron lines). Filtered on sequencing, not merit: four of the nine are
  the direct-call chain, which sits under a live legal question (IPA s45 / POA s215), and I will
  not ask you to schedule it as a rounding item. **ops will bring a paste-ready file next week
  with the direct-call four separated out.**
- **REC-onsite-005 (`for_sale_page_v1`, all three arms below control)** — the largest N in the
  system (1,530 users) and probably right. But 30 conversions across four arms is 6–10 each, and
  the second half of onsite's own ask — *"what should /for-sale actually be judged on?"* — is the
  real question. Not worth a slot until it is asked on its own. Reverting to control is one
  PostHog change onsite can make under standing authorisation once you have said what the metric
  is.
- **REC-seo-004 (which URL owns home-search intent)** — a genuine call that blocks seo's work,
  and my strongest reserve item. 53 impressions and 0 clicks; seo's own read (option A,
  `/houses-for-sale/:suburb`, which already holds the association) is well-argued and I did not
  want to spend a legal-exposure slot on it. **First in the queue next week.**
- **REC-valuation-003 (name an executor)** — not a decision. I executed it. See Health.
- **ads' "$11.96 is the cheapest lead we have ever bought"** — true and irrelevant, so I have
  not let it travel as a headline. A cheap worthless conversation is not a cheap lead.

## Health

- **Domains that ran: 7 of 7.** First full sweep. The `--max-turns` ceiling that killed three
  domains last week did not recur.
- **⚠ ops's heartbeat is stale (last `rl_weekly_ops` run 2026-08-15) although ops ran and wrote
  a cycle document at 06:12 today.** The domain that audits our self-monitoring is the one whose
  own heartbeat is not recording. Rule 7 says a job must self-report; this one reports a run
  that happened eight days ago. Sent to ops as its first job next cycle.
- **The failure of the week: an approved item with no owner.** You approved REC-valuation-002 on
  08-18. Five days later the file's last commit was still 08-13, the briefing still cited the
  disproved figure, and the rec's own graded metric was still failing. The domain could not do
  it — `valuation_signal.py` carries a Rule 7 heartbeat, which puts it permanently outside
  autonomous scope. **The ledger has `verdict → ship → grade` and nothing in between that names
  who acts**, so an approved item is indistinguishable from a shipped one until someone
  re-derives it. I executed it this cycle (commit `f09c50b4`, pushed): the sensor now joins
  `Gold_Coast.unit_valuations`, and the book is reported split by engine —
  **houses 58/107 (54.2%), attached 38/107 (35.5%)** — with the blended 47.7% retained but
  flagged `blended_do_not_quote`, because its numerator counts 44 house-engine figures written
  on attached dwellings the house method excludes by decision. Five false integrity
  contradictions → zero. Logged as `[RL-APPROVED-BUT-UNOWNED]`.
  **Two corrections fell out of doing it:** REC-002 claimed four outstanding edits; two of them
  were already in the file. A recommendation that miscounts its own remaining work is harder to
  hand to an executor, and that has gone back to valuation.
- **⚠ All seven briefs are 10 days old (`aging`).** Standing authorisations still apply, so
  nothing is blocked *yet* — but the ads brief §2 now states something false ("all ad spend
  paused") while $294.60 ran in the last 7 days, and the valuation brief was carrying a
  disproved figure until this afternoon. **A stale brief stops being a memo and starts being
  wrong.** Your reply to this brief is the natural place to refresh them; I have left the
  specific questions in each `§7`.
- **Not yet a recommendation, third week running:** MongoDB WiredTiger cache still sized for the
  old 8 GB VM. One line. I will stop mentioning it after this week unless it causes something.
