# claude_arm_final — E3_correctness

Opus 5, read-only, identical brief to the GPT arm.

---

## FINDING 1: A second, Domain-only median pipeline still feeds the property-page editorial, and it is told to use the in-progress quarter
CATEGORY: data-integrity
CLAIM: `Gold_Coast.suburb_median_prices` is a parallel median collection, rebuilt every night by orchestrator step 13, and the AI property editorial prompt injects its **last** entry — an in-progress quarter with n=7–11 sales — as "⚡ CURRENT SUBURB MEDIAN … USE THIS FIGURE", overriding the canonical union median the website publishes.
EVIDENCE: Producer `/home/fields/Feilds_Website/08_Market_Narrative_Engine/generate_suburb_medians.py:184` (`int(statistics.median(prices))` over `scraped_data.property_timeline`, Domain-only, any quarter with ≥3 sales, no onthehouse union, no dedupe, no in-progress exclusion). Scheduled: `config/process_commands.yaml:274-284` step 13, and `src/schedule_manager.py:77` `always_run_processes = {11,12,13,…}` → runs nightly; `suburb_median_prices.last_updated = 2026-08-06 22:35`. Consumer: `/home/fields/Fields_Orchestrator/scripts/backend_enrichment/generate_property_ai_analysis.py:374-381` (`get_suburb_medians` reads `suburb_median_prices`) → `:1096-1103` `format_medians()` takes `suburb_medians[-1]` and emits `"⚡ CURRENT SUBURB MEDIAN: ${latest['median']:,} … — USE THIS FIGURE when referencing the suburb median. Do NOT use older quarters."` → `:3080`, `:4055-4059` into the prompt. Step 120 (`config/process_commands.yaml:362-372`) runs nightly with `AUTO_PUBLISH=1` and is in `always_run_processes`. Measured now (my queries): legacy last quarter = Burleigh Waters 2026-Q3 **$2,250,000 (n=11)**, Robina 2026-Q3 **$1,365,000 (n=9)**, Varsity Lakes 2026-Q3 **$1,477,500 (n=7)**. Canonical `Gold_Coast.precomputed_indexed_prices` / live `https://fieldsestate.com.au/api/market-insights?suburb=…`: Burleigh Waters **$1,925,000 (n=167)**, Robina **$1,490,000 (n=265)**, Varsity Lakes **$1,400,000 (n=111)**, `median_source: domain_union_onthehouse`; the canonical doc quarantines Q3 2026 in a separate `in_progress_quarter` field (`{'period':'Q3 2026','median_price':2000000.0,'transaction_count':11,'is_complete':False}` for Burleigh Waters). Same collection also feeds `scripts/fb-page-post.py:763-796` ("The median sale price has moved X% over the past year to Y") and `01_Website/src/lib/db.server.ts:468` `getSuburbPriceHistory` → off-market deck capital-gain chart.
STATUS: VERIFIED — I read the producer, the schedule, the prompt-formatting code, and queried both collections.
IMPACT: Burleigh Waters property-page prose is instructed to call **$2,250,000** the current suburb median while the same site publishes **$1,925,000** — a **+$325,000 (+16.9%)** overstatement, on 11 sales, in a quarter the canonical pipeline explicitly refuses to publish. Robina understates by $125,000 (−8.4%). Every property that passes step 120 tonight inherits it; ~206 for-sale listings and the Facebook median-movement template are in scope. This is exactly the sibling of the owner-article bypass: the guarded union pipeline is bypassed because the consumer reads a different collection.
CONFIDENCE: HIGH
EFFORT: 3-4 hours (point `get_suburb_medians` at `precomputed_indexed_prices`, use `rolling_12m_median_price` for "current", never `[-1]`; then decide whether step 13 should be retired or its output renamed to signal non-canonical).
PROPOSED ACTION: Replace `get_suburb_medians`/`format_medians` with a read of `precomputed_indexed_prices.rolling_12m_median_price` + `median_price_12m_sample_n`, and never surface `in_progress_quarter` as "current". Apply the same swap in `fb-page-post.py` and `db.server.ts:468`. Then either retire step 13 or rename its collection to `suburb_median_prices_domain_only_legacy` so no future consumer mistakes it for canonical.
FALSIFIED BY: `format_medians` not reaching the live prompt (e.g. `--new-listings` path taking a different formatter), or `suburb_median_prices[-1]` being a complete quarter that matches `rolling_12m_median_price` within sampling error.

## FINDING 2: Two live public pages still call the valuation range a "90% confidence interval" and say ~10% fall outside; the measured figure is 61% / 39%
CATEGORY: correctness
CLAIM: `/methodology` states the reconciled range is a 90% confidence interval with ~10% of properties falling outside, and `/accuracy` renders the self-refuting sentence "Our 90% confidence range captured the actual sale price 61% of the time" — the exact wording CLAUDE.md forbids after the 2026-08-06 recalibration.
EVIDENCE: `/home/fields/Feilds_Website/01_Website/src/pages/MethodologyPage/MethodologyPage.tsx:61`. I fetched the live page: `curl https://fieldsestate.com.au/methodology` → HTTP 200, SSR HTML contains verbatim `"90% confidence interval; ~10% of properties will fall outside."`. `/home/fields/Feilds_Website/01_Website/src/pages/ValuationAccuracyPage/ValuationAccuracyPage.tsx:185` interpolates the live metric into the phrase "Our 90% confidence range captured the actual sale price {m.range_accuracy_pct}% of the time." Live source: `system_monitor.valuation_accuracy` latest run 2026-08-06 07:35 → `range_accuracy_pct: 61`, `p90_error: 26.4`; same values from `https://fieldsestate.com.au/api/v1/valuation-accuracy`. Correct framing already exists at `src/pages/YourHomePage/components/ValuationEvidence.tsx:472` ("±12% — an empirically-calibrated margin … not a textbook confidence interval"), so the site describes the same band two contradictory ways. Fix-history `logs/fix-history/2026-08-06.md:586` `[VALUATION-RANGE-MISCALIBRATED]` records the recalibration; these two strings were not updated.
STATUS: VERIFIED — string present in live SSR HTML; live API value read.
IMPACT: A prospective vendor reading `/methodology` is told 90% of estimates land inside the band. 39% do not. On a $1.5M home the honest 90% band is ±$396,000 (±26.4%), not ±$180,000. This is the highest-liability wording on the site and it is two string literals.
CONFIDENCE: HIGH
EFFORT: 30 minutes
PROPOSED ACTION: In `MethodologyPage.tsx:61` and `ValuationAccuracyPage.tsx:185`, replace "90% confidence interval/range" with "a flat ±12% band, not a statistical confidence interval", and state the measured containment (61%, 67% inside the design envelope) and that a true 90% band needs ±26.4%. Add a lint/grep gate for the phrase "90% confidence" near "valuation".
FALSIFIED BY: `range_accuracy_pct` in `system_monitor.valuation_accuracy` reading ≥88, or the ±12% multiplier having been widened in `precompute_valuations.py`.

## FINDING 3: `/review/:slug` serves a fully hardcoded page for a home that sold three months ago, still advertising its auction — and matches any slug
CATEGORY: correctness
CLAIM: `https://fieldsestate.com.au/review/21-indooroopilly-court-robina` returns HTTP 200 with a hardcoded "Auction — 11 April 2026 at 2:00pm on-site" and ~11 stale market statistics, for a property whose DB record is `listing_status: "sold"`, `sale_price: "$1,260,000"`, `sold_date: "2026-05-05"`; the route also serves the identical page for any slug.
EVIDENCE: `/home/fields/Feilds_Website/01_Website/src/pages/PropertyReviewPage/PropertyReviewPage.tsx:26` (`auctionDate: "11 April 2026 at 2:00pm on-site"`), `:75`, `:195`, `:277-284`, `:396`. I fetched it: HTTP 200, 32,882 bytes, contains `11 April 2026 at 2:00pm on-site`, `1.3 months` (×2) and `1,379,000` (×4). `curl https://fieldsestate.com.au/review/not-a-real-address-xyz` → **HTTP 200** (same page). DB: `Gold_Coast.robina` `_id 690bd7de8b8f546592602c96` → sold 2026-05-05 at $1,260,000. Live truths vs hardcoded: Q4 2025 median $1,520,000 vs `$1,450,000`; Q1 2026 described as "a pullback to $1,379,000 on just 40 sales" vs actual Q1 2026 $1,560,000 on 55 sales (a rise); absorption 2.18 months vs `1.3`; DOM 34 days and **+9.5 days YoY** vs `23 days, down from 49`; YoY growth 5.8% vs `+15.9%`; active listings 59 vs `43`. No `noindex` in the served HTML; not in sitemap.xml.
STATUS: VERIFIED — live fetch plus DB read.
IMPACT: A single public URL that a homeowner or agent could reach contradicts the site on eight separate figures, inverts the direction of two of them (DOM, Q1 median), and invites a buyer to an auction that happened four months ago. Because any slug resolves, a shared link like `/review/<their-address>` shows a stranger's home's numbers as if they were theirs. Reputationally the worst single artefact I found.
CONFIDENCE: HIGH
EFFORT: 1 hour to delete the route; 1-2 days if it is to become data-driven.
PROPOSED ACTION: Remove `/review/:slug` from the router (or gate it to a 404 for any slug and add `noindex`) until it reads from `properties-for-sale.mjs`. If it is kept as a sales demo, move it behind an unlisted path and stamp "illustrative, as at <date>" on every figure.
FALSIFIED BY: the route being unreachable in production (my fetch returning 404/redirect), or the page carrying a `noindex` + "example only" banner I missed.

## FINDING 4: `market-narrative.mjs` recomputes a median inline for the public houses-vs-units chart, including the quarter the canonical pipeline quarantines
CATEGORY: data-integrity
CLAIM: One of six chart branches in `market-narrative.mjs` buckets raw sold `property_timeline` events and medians them in the function, so the public `/market-intelligence/:suburb/:category` chart's rightmost point is Q3 2026 — Burleigh Waters **$2,250,000 off 11 sales** — against a canonical published median of $1,925,000.
EVIDENCE: `/home/fields/Feilds_Website/01_Website/netlify/functions/market-narrative.mjs:236-315`, median computed at `:271-274` from `db.collection(suburb)`; the same file correctly reads `precomputed_indexed_prices` at `:186`. Consumer: `src/components/MetricCharts/PropertyTypeRaceChart.tsx` plots absolute dollars (`formatPrice` :38-41, tooltip :60, `latestHouse` summary stat :133), rendered by `src/pages/MarketMetricsPage/MarketMetricsPage.tsx:126` and `src/components/HousesVsUnitsSection/HousesVsUnitsSection.tsx:152` with the caption at `:173` "Current median house prices in absolute dollar terms"; `src/config/marketMetrics.ts:141-150` `available: true`. Measured Q2 2026: Burleigh Waters inline $1,790,000 vs canonical $1,877,775 (−$87,775); Varsity Lakes $1,468,000 vs $1,410,000 (+$58,000); Robina agrees at $1,410,000 but on n=27 vs canonical n=51. Additional defect at `:257`: `rawType === 'House' ? 'House' : 'Unit'`, so the Burleigh Waters "Unit" line is 26 Duplex + 11 Townhouse + 8 Semi-Detached + 4 Apartment + 4 Villa + 1 Land — four actual apartments. `logs/fix-history/2026-07-23.md:724` flagged the symptom ("PropertyTypeRaceChart's own internal 'House Premium' stat differs … flagged for a future pass") and never diagnosed the cause; the chart predates the union pipeline (2026-08-01) and was never migrated.
STATUS: VERIFIED — code read in both the function and the components; both medians computed from the DB.
IMPACT: A public chart tells a Burleigh Waters owner the current median house price is **$2,250,000** while the headline metric on an adjacent page says $1,925,000 — a **+$325,000 (+16.9%)** gap on an 11-sale sample. This is the "Burleigh Waters is accelerating" artefact the business has already published and retracted once. Also mislabels a mixed attached-dwelling basket as "Units".
CONFIDENCE: HIGH
EFFORT: 4-6 hours
PROPOSED ACTION: Replace the `property-type-prices` branch with a read of `precomputed_indexed_prices.indexed_series` (houses) plus a properly-derived attached-dwelling series, exclude `in_progress_quarter` from the plotted line, and label the non-house line by its actual composition or drop it.
FALSIFIED BY: the chart not being reachable (`marketMetrics.ts` `available: false`), or the branch already preferring a precomputed doc on the deployed function.

## FINDING 5: The homepage stat strip publishes a sales-volume percentage and a 2025 turnover rate as current trends, both from the count series the business has ruled unreliable
CATEGORY: correctness
CLAIM: `DataInsightsStrip` renders "Sales Volume 43, −21.8%" (Robina) and "Sales Volume 22, −38.9%" (Varsity Lakes) with red trend arrows, plus "Turnover Rate 2.91%, −0.8pp" where "latest" is calendar **2025** — while the pipeline's own `sales_volume_basis` string forbids exactly this.
EVIDENCE: `/home/fields/Feilds_Website/01_Website/src/components/DataInsightsStrip/DataInsightsStrip.tsx:136-142` (`change: formatPercentage(data.sales_volume_qoq_pct)`, `trend: getTrend(...)`) and `:144-149` (turnover). Mounted unconditionally at `src/pages/MarketIntelligencePage/MarketIntelligencePage.tsx:117`, i.e. `/` and `/news/:suburb`. Source `netlify/functions/market-insights.mjs:247-249` off `indexed_series[].transaction_count`. Live values I fetched today: Robina `sales_volume_quarter: 43, sales_volume_qoq_pct: -21.8, turnover_rate_latest: 2.91, turnover_rate_yoy_change: -0.8`; Burleigh Waters `46, 0, 3.75, -0.9`; Varsity Lakes `22, -38.9, 3.65, -1.6`. The prohibition is in the data itself: `system_monitor.market_pulse.data_snapshot.sales_volume_basis` (all 21 docs) reads "… NO year-on-year comparison is possible … never a count, never a percentage." Turnover source `Gold_Coast.precomputed_market_charts.*_turnover_rate.timeline` latest year = **2025** (Robina 352 sales / 12,088 dwellings), with no year label on the tile. All three suburbs' 2025 sold counts fall ~20% simultaneously (Robina 449→352, Burleigh Waters 331→265, Varsity Lakes 413→290) — a capture-rate signature, not three coincident market events.
STATUS: VERIFIED for the code path and the live numbers. INFERRED that the ~20% simultaneous drop is a capture artefact rather than a real market move.
IMPACT: The most-trafficked surface on the site publishes a −21.8% and a −38.9% volume collapse, with a directional arrow, off a series that misses 40-50% of real sales — the single claim type the business has documented as unusable. A 7-month-closed calendar year is presented as the current turnover trend.
CONFIDENCE: HIGH
EFFORT: 2-4 hours
PROPOSED ACTION: Drop the QoQ percentage from the Sales Volume tile (show the count with an explicit "our tracked sales, ~55-65% of market" footnote, or remove the tile); label the turnover tile with its year ("Turnover 2025") and remove the YoY arrow until the series is capture-corrected.
FALSIFIED BY: `market-insights.mjs` switching the volume series to a settlement-complete source (PropRadar) with a measured capture rate ≥95%, or the strip being feature-flagged off.

## FINDING 6: The public absorption-rate methodology caption describes a computation we abandoned, and the JSON-LD claims an absorption basis it does not have
CATEGORY: correctness
CLAIM: `/market-intelligence/:suburb/absorption-rate` publishes "Methodology: Active listings … divided by the number of confirmed sales in the last 30 days … Updated nightly", but the served figure is PropRadar's 12-month settlement-based houses-only inventory-months — and the two inputs the caption names come back **null** from the live API.
EVIDENCE: `/home/fields/Feilds_Website/01_Website/src/pages/MarketMetricsPage/MarketMetricsPage.tsx:219`. Live `https://fieldsestate.com.au/api/market-insights?suburb=Robina` → `absorption_rate_months: 2.18`, `absorption_rate_source: "propradar_houses"`, `absorption_house_sales_12mo: 386`, `absorption_monthly_sales: 32`, and **`absorption_rate_active: null`, `absorption_rate_sold_30d: null`** — the described numerator and denominator are absent. Served path `netlify/functions/market-insights.mjs:400-409` (PropRadar) vs the legacy 30-day method it still falls back to at `:447-459`. Separately, `src/components/SellNowSection/SellNowSection.tsx:173` emits `schema.org/FAQPage` markup: "`${suburb}` is currently a `${marketType}` market **based on absorption rate data**" — but `marketType` comes from `MARKET_TYPE_FROM_VERDICT[verdict]` (`:56-67`, `:133`), i.e. the LLM's `verdict` string, not from absorption; same misattribution at `:550`, `BuySection.tsx:163`, `:482`. The same payload ships `active_listings_current: 59` and `absorption_monthly_sales: 32` (59/32 = 1.84 months) alongside the published 2.18. The legacy 30-day method is still live for suburbs without PropRadar stats: `Gold_Coast.absorption_rate_snapshots` holds `worongary_2026-08: 14 months (56 active ÷ 4 sold_30d)` and `reedy_creek_2026-07: 10.3 months (41 ÷ 4)` — the manufactured-oversupply artefact.
STATUS: VERIFIED — caption string, live API payload, and the `marketType` derivation all read directly.
IMPACT: We publish a false description of our own method on a page whose whole promise is transparent methodology, and we assert in Google-liftable structured data that a market-type verdict is absorption-derived when it is LLM-derived. A reader who divides the two published inputs gets 1.84 where we print 2.18. For non-PropRadar suburbs the abandoned method is still producing 10-14 month readings from 4-sale denominators.
CONFIDENCE: HIGH
EFFORT: 2-3 hours
PROPOSED ACTION: Rewrite the `absorption-rate` caption to state the actual basis (PropRadar settlement-confirmed house sales, trailing 12 months ÷ 12, houses only, monthly refresh) and surface `absorption_rate_source` on the page. Change `SellNowSection.tsx:173` to say the verdict is a Fields assessment, not "based on absorption rate data". Suppress the legacy 30-day fallback below a minimum sold_30d count.
FALSIFIED BY: `absorption_rate_source` reading `legacy_30d` for the three target suburbs, which would make the caption accurate.

## FINDING 7: The design-envelope backfill filters `property_type: "House"` while the runtime guard does not — non-House pages still serve the suppressed point estimate and ±12% range
CATEGORY: data-integrity
CLAIM: Both passes of `backfill_design_envelope.py` are scoped to `property_type: "House"`, but the envelope check in `precompute_valuations.py` tests price only, so 28 townhouse/apartment/duplex documents — 15 of them live for-sale or under-contract — still carry a point estimate and a ±12% range outside the envelope, and I confirmed two of them rendering on production pages today.
EVIDENCE: `/home/fields/Fields_Orchestrator/scripts/backfill_design_envelope.py:52` and `:96` both open with `{"property_type": "House",`. The runtime guard has no type condition: `/home/fields/Feilds_Website/07_Valuation_Comps/precompute_valuations.py:3567` `if rv and not (_ENVELOPE_MIN <= rv < _ENVELOPE_MAX): directional_only = True`, with suppression of both figure and range at `:3665-3678`. My query over `Gold_Coast.{robina,burleigh_waters,varsity_lakes}` for `reconciled_valuation` outside $1,000,000–$2,000,000 with a non-null value: **28 docs, every one non-House** (Townhouse 12, Apartment/Unit/Flat 14, Duplex 1, untyped 1), `directional_only` unset on all 28, `range` populated on all 28. By status: 14 `for_sale`, 1 `under_contract`, 12 `sold`, 1 null. Live confirmation: `https://fieldsestate.com.au/property/2506-42-laver-drive-robina` (apartment, stored estimate $768,762) serves `$676,511`–`$861,014` in SSR HTML; `/property/26-206-cheltenham-drive-robina` (townhouse, $988,140) serves `$869,563`–`$1,106,717`; neither page contains the word "directional". Separately, 43 non-House docs sit *inside* the price band with a full non-directional estimate and range (23 of them live `for_sale`/`under_contract`) even though the envelope is documented as detached houses only. This is the 2nd blind spot in the script written to fix the 1st (`logs/fix-history/2026-08-06.md:643` `[ENVELOPE-BACKFILL-BLIND-SPOT]`).
STATUS: VERIFIED — both filters read, DB counted, two live pages fetched.
IMPACT: 15 live listing pages and 12 indexed sold pages present a narrow ±12% band around a figure the method has declared unusable — the precise state the 2026-08-06 change was written to eliminate, and the state CLAUDE.md says is "indistinguishable from a correct one in our own output". The for-sale subset self-heals within ~7 days via `VALUATION_MAX_AGE_DAYS`; the 12 sold documents never will, because step 18 only covers active listings. The 43 in-band non-House valuations never heal at all — the runtime guard cannot see them.
CONFIDENCE: HIGH
EFFORT: 2 hours
PROPOSED ACTION: Delete `"property_type": "House"` from both passes of `backfill_design_envelope.py` and re-run, then decide the type question explicitly: either extend the runtime check to `if property_type not in HOUSE_TYPES: directional_only = True`, or state in the envelope note that attached dwellings are in scope and measure accuracy for them separately.
FALSIFIED BY: a deliberate decision on record that attached dwellings are excluded from the envelope, or `precompute_valuations.py` refusing to value non-House stock (it does not — 333 non-House docs hold `valuation_data`).

## FINDING 8: The Q2 2026 quarterly report states price growth of +10.9%–15.5% and medians up to $215,000 below the live site, plus an inverted vendor-discount sign and a Q1 colophon
CATEGORY: correctness
CLAIM: The rendered quarterly report contradicts the live site on its own headline numbers — it publishes medians of $1,450,000 / $1,710,000 / $1,350,000 and growth of +10.9% / +12.2% / +15.5%, against canonical $1,490,000 / $1,925,000 / $1,400,000 and +5.8% / +6.9% / +10.2% — and it inverts the sign on Varsity Lakes' vendor discount.
EVIDENCE: `/home/fields/Fields_Orchestrator/10_Market_Report/pipeline/quarterly/index.html:1522` ("Robina $1,450,000 (+10.9% year-on-year), Burleigh Waters $1,710,000 (+12.2%), Varsity Lakes $1,350,000 (+15.5%) … approximately +12.9%"), repeated as a range at `:1188`, `:1259`, `:1263`, and as stat tiles at `:1898`, `:2216`. Canonical, fetched live today: Robina $1,490,000 / +5.8% / n=265; Burleigh Waters $1,925,000 / +6.9% / n=167; Varsity Lakes $1,400,000 / +10.2% / n=111, all `median_source: domain_union_onthehouse`. The report's low end (+10.9%) exceeds the live high end (+10.2%). Vendor discount: `:1188`, `:1259`, `:1263`, `:2329` say "discounting between 0.4% and 2.3% off their asking price" — actual `vendor_discount_avg_pct` is Robina −0.7, Burleigh Waters −0.4, Varsity Lakes **+2.3**, and by the site's own stated convention (`MarketMetricsPage.tsx:218` "Negative values mean the buyer paid less than asking") Varsity sold **2.3% above** asking, on n=8, in the suburb whose own market_pulse summary says there are too few tracked sales to report an asking-vs-achieved gap. Colophon: `:1080-1088` "Data closed: 31 March 2026. The Q2 2026 quarter is in progress … and is not included", "Next edition: 15 August 2026, covering Q2 2026", "Permanent URL: fieldsestate.com.au/quarterly/**q1-2026**" — inside an issue titled Q2 2026 that reports Q2 volume, FCI and DOM throughout. Volume figures match no live series: `:1393` "85 sales", `:1473` Robina "30 versus 54.2", `:1471` Varsity "15 sales this quarter", against union 51 / 42 / 17 and indexed 43 / 46 / 22. Rendered to `10_Market_Report/issues/q2_2026_quarterly/latest.pdf` (regenerated 2026-08-01, modified in the working tree).
STATUS: VERIFIED for every quoted string and every live comparison. INFERRED that the cause is pre-union Domain-only figures surviving the union promote. NOT ESTABLISHED: whether this PDF has been distributed — I found no `quarterly` route in `src/routes.ts`, `public/_redirects` or `netlify.toml`, so I cannot confirm anyone has received it.
IMPACT: If the PDF has gone out, the flagship editorial product overstates Burleigh Waters' median by $215,000 and its growth by 5.3pp, tells Varsity Lakes vendors they are discounting when they are achieving above asking, and carries a Q1 colophon and permanent URL. A PDF is the hardest artefact to retract. Conditional on distribution, this would rank second.
CONFIDENCE: HIGH on the contradictions; LOW on whether it has been distributed.
EFFORT: 1 day to re-derive every figure from the canonical collections and re-render; 2 hours to add a build-time assertion.
PROPOSED ACTION: Establish whether Issue 02 was sent. Regenerate `index.html` reading medians, YoY and vendor discount from `precomputed_indexed_prices` / `market_pulse` rather than any pre-union snapshot; take vendor discount signed, not absolute; drop the 5-year volume comparison, which crosses the `union_from = Q3 2025` capture boundary; fix the colophon. Add a pre-render check that every headline figure matches the live API within tolerance.
FALSIFIED BY: the report deliberately quoting a different basis (e.g. Domain-only quarterly, disclosed as such in the methodology page of the PDF), or the PDF never having left the VM.

## FINDING 9: The posted appraisal report computes its own suburb median and converts an under-captured sale count into a supply/demand verdict, breaking its own stated rule
CATEGORY: data-integrity
CLAIM: `generate_appraisal_report.py` medians `Gold_Coast[suburb]` inline instead of reading the canonical union median (Varsity Lakes $1,290,000 vs $1,400,000, −7.9%), and then asserts "shows balanced supply and demand" from a raw 12-month Domain sold count — which the same file's own prompt forbids.
EVIDENCE: `/home/fields/Fields_Orchestrator/scripts/generate_appraisal_report.py:599` `median = sorted(prices)[len(prices)//2]` over `{listing_status: "sold", property_type: /house/i}`, 365-day window. Measured: Varsity Lakes $1,290,000 (n=168) vs canonical $1,400,000 (n=111) = **−$110,000 (−7.9%)**; Robina $1,410,000 (n=296) vs $1,490,000 = −$80,000 (−5.4%); Burleigh Waters $1,890,000 (n=163) vs $1,925,000 = −$35,000. `:961` `f"With {market_stats.get('currently_listed')} properties currently listed and {market_stats.get('houses_sold_12m')} sales in the last 12 months, {suburb} shows balanced supply and demand."`, where `houses_sold_12m` is `str(len(prices))` from `get_market_stats()` `:588-600`. The same file states at `:717`: "We do not have a record of every home that sold … You must NEVER write … any wording that implies a complete count of all sales." The median is also injected into the Claude prompt at `:712` and `:952`, so it is baked into prose, not just template slots. Delivery: `appraisal-poller.py:108-151` → `send_report.send_via_graph`, i.e. emailed to a named homeowner. Same pattern in `scripts/appraisal_template/data_pull.py:485` (`<strong>median $X</strong> (n=…)` straight into Section 03) and `scripts/appraisal_data_pull.py:334` (bedroom-cohort median falling back to `listing_price` when no sale price exists).
STATUS: VERIFIED — code read and both medians computed. INFERRED that a specific delivered PDF contains the mismatched figure (I traced the call graph, not an individual sent document).
IMPACT: A Varsity Lakes homeowner receives a document quoting a suburb median $110,000 below the one on our website, alongside a supply/demand verdict built on a count that misses 40-50% of sales — in the one artefact where our credibility is most concentrated and where a discrepancy is most likely to be checked against the site.
CONFIDENCE: HIGH
EFFORT: 4-6 hours across the three appraisal data-pull paths.
PROPOSED ACTION: Replace `:599`, `appraisal_template/data_pull.py:485` and the `appraisal_data_pull.py:334` fallback with a read of `precomputed_indexed_prices.rolling_12m_median_price` + sample size and CI. Delete the verdict at `:961` or restate it as "of the sales we track" with the sample-size caveat the file's own prompt requires.
FALSIFIED BY: the appraisal pipeline being retired/unscheduled (`appraisal-poller` inactive), or `:961` being unreachable in the current template.

## FINDING 10: `offmarket_intel_poller` swallows resolver failures, stamps the document `status: "done"`, clears the error field, and never retries — so a crash and a genuinely storyless home are byte-identical
CATEGORY: correctness
CLAIM: The live systemd poller that builds the public `/off-market/:slug` positioning cards catches every sub-resolver exception into `None`/`[]`, then writes `status: "done"` with `$unset: {error: ""}`; because it only ever selects `{"status": "pending"}` and the result is cached forever, 63 of 731 qualifying decks carry `positioning: null` and 231 carry `narrative: null` with no record of why.
EVIDENCE: `/home/fields/Fields_Orchestrator/scripts/offmarket_intel_poller.py:288-290`, `:305-308`, `:315-319`, `:355-358` (each `except Exception: traceback.print_exc()` then an empty value), returning `err=None`; the write at `:446-449` `{"$set": {"status": "done", …}, "$unset": {"error": ""}}`. Selection is `{"status": "pending"}` at `:398` and `:434`; the docstring at `:259-261` states the result is cached forever and must never regenerate. The honest-degrade branch at `:275-276` writes the identical shape, so the two cases cannot be told apart. Read-only counts on `system_monitor.offmarket_positioning`: 1,184 docs, **all** `status: done`, **zero** with an `error` field; 731 passed the degrade gate (`feature_combination` non-null), of which **63** have `result.positioning: null`, **231** `result.narrative: null`, **44** `result.personas: []`. Example: `41-17-great-southern-drive-robina`, computed 2026-07-23, `feature_combination: "4 bedrooms, 3 bathrooms and a 1387-metre walk to Robina State School"`, `positioning: null`. Service `fields-offmarket-intel-poller.service` active. `_find_subject()` `:91-94` turns a transient Cosmos `find_one` failure into `d = None`, surfacing as the factually wrong permanent verdict `error: "subject_not_found"`. The poller has no `job_run()` heartbeat; `scripts/main_site_health_check.py:1158` only checks the unit is active, never that its output is complete.
STATUS: VERIFIED for the swallow → `done` → never-retried structure and for the null counts. INFERRED that those specific 63/44 nulls came from the exception paths rather than the resolvers' own internal gates — I could not separate them, and that inability is itself the defect.
IMPACT: Up to 231 public deck pages that a homeowner reaches by googling their own address are missing the narrative section, permanently, with the database asserting they completed successfully. A retry would cost nothing; nothing will ever retry them. Rule 7 is unmet for a live daemon.
CONFIDENCE: HIGH on the structure, MEDIUM on the exact attribution of the 63/231.
EFFORT: 3-4 hours
PROPOSED ACTION: Set `err` (or a `partial: [<which resolvers failed>]` list) on any swallowed exception so the doc lands as `status: "partial"`, not `done`; add `{"status": "partial"}` to the poller's selection so it retries with backoff; keep the honest-degrade path writing a distinct `degrade_reason` so absence-by-design is separable from failure. Wrap the loop in `job_run("offmarket_intel_poller", cadence_hours=…)`.
FALSIFIED BY: the 63/231 nulls all tracing to `resolve_positioning_object`'s designed gates (which would still leave the structural defect, but drop the impact figure to zero).

## FINDING 11: The Leads & CRM staleness guard has been watching two log files that stopped being written on 2026-08-01
CATEGORY: process
CLAIM: Two health-board rows key off log-file mtimes that the 2026-08-01 lead-chain consolidation orphaned, so they are a permanent false alarm *and* structurally unable to detect a real failure — the exact "guard that no longer covers its case" pattern.
EVIDENCE: `/home/fields/Fields_Orchestrator/scripts/main_site_health_check.py:365-373` watches `logs/live-leads-sheet.log` (cadence 2 days) and `:384-386` watches `logs/samantha/lead_intelligence.log`. Actual mtimes I checked: `logs/live-leads-sheet.log` = **2026-07-31 23:55**, `logs/samantha/lead_intelligence.log` = **2026-08-01 02:01**, `logs/nightly-lead-chain.log` = **2026-08-07 00:16** (fresh). Cause: the single surviving cron line is `15 0 * * * … scripts/nightly_lead_chain.py >> logs/nightly-lead-chain.log 2>&1`, and `nightly_lead_chain.py:107-108` runs each step with `stdout=sys.stdout, stderr=subprocess.STDOUT`, so all step output now lands in the chain log. Consequence in the latest `system_monitor.mainsite_health_snapshots` (run_at 2026-08-06 15:00Z): `Leads & CRM::Live Leads Tracker` **STALE since 2026-08-02**, `Leads & CRM::Lead-Intelligence Pipeline (worklist build)` **STALE since 2026-08-03**, page tally `{"STALE": 2, "OK": 4, "ERROR": 1}`, `health_pct 57`. Stale documentation in the same file at `:1151` still says the lead-intelligence pipeline runs "nightly 02:00"; it runs at 00:15 inside the chain. The same file warns at `:84` that "permanent false alarms train you to ignore the board".
STATUS: VERIFIED — log mtimes, cron line, subprocess wiring, and the persisted health snapshot all read directly.
IMPACT: Two permanently red rows on the board that is meant to be the single "is everything running?" signal, dragging the Leads & CRM page to 57% health for six days and counting; and if `live_leads_to_sheet` or `lead_intelligence` genuinely broke inside the chain, these rows would look exactly as they look now. Partially mitigated: `job_runs.nightly_lead_chain` = success @ 2026-08-06 14:16Z with "all 5 steps ok", so the chain itself is covered — this is a redundant guard gone bad, not a total blind spot.
CONFIDENCE: HIGH
EFFORT: 1 hour
PROPOSED ACTION: Either point both `log_freshness_check` calls at `logs/nightly-lead-chain.log` with per-step marker patterns, or delete the two rows and rely on the per-step `job_run` heartbeats — but not leave them keyed to files nothing writes. Correct `:1151`.
FALSIFIED BY: something restarting writes to those two log paths, or the two rows already having been removed in a newer revision of the file.

## FINDING 12: The seasonality thesis on the mini-site hardcodes "November peak, 18,978 sales, 2010–2025" while the live dataset on the same page says December, 9,559, 2015–2026
CATEGORY: correctness
CLAIM: A hardcoded paragraph on `/your-home/:slug` and `/off-market/:slug` names the strongest selling month as November off 18,978 matched sales (2010–2025); the precomputed dataset rendered a few sections below it says December in all six suburbs, off 9,559 sales, 2015–2026.
EVIDENCE: `/home/fields/Feilds_Website/01_Website/src/pages/YourHomePage/data/processPlan.ts:190` (repeated `:227`): "Across 18,978 matched sales in the southern Gold Coast catchment (2010–2025, excluding the COVID years), the gap between the strongest month (November, +3.3%) and the weakest (January, −1.4%)…", also hardcoded as `totalSales: 18978` at `homeFixture.ts:2129`. Rendered by `src/pages/YourHomePage/components/ProcessPlanSection.tsx:119` (`thesis.body.map`, no gate) and reached from `OffMarketDeck.tsx:10`. Live source `Gold_Coast.precomputed_seasonality` (6 docs, `last_updated 2026-07-31`), which `SeasonalityStrip.tsx` renders on the same report: `peakMonthIndex = 11` for **all six** suburbs (index convention "0–11, Jan=0" confirmed at `homeFixture.ts:276`), windows 2015–2026 / 2015–2025, `totalSales` summing to 9,559 (robina 2,771, burleigh_waters 2,451, varsity_lakes 1,610, reedy_creek 1,364, merrimac 814, worongary 549).
STATUS: VERIFIED — hardcoded strings and the precomputed collection both read; render path traced.
IMPACT: One report contains two different answers to "when is the best month to list", one of them wrong by a month and attributed to a sample twice the size of the one we actually hold. Timing is a decision a vendor acts on, so it is a materially misleading contradiction rather than cosmetic drift.
CONFIDENCE: HIGH
EFFORT: 2 hours
PROPOSED ACTION: Derive the thesis sentence from `precomputed_seasonality` at render time (peak/trough month, window, summed `totalSales`) instead of a literal, or delete the paragraph and let `SeasonalityStrip` carry the claim alone.
FALSIFIED BY: `processPlan.ts` not being reachable on either live route, or `peakMonthIndex` using a 1-based convention (contradicted by `homeFixture.ts:276`).

## FINDING 13: Three components render fabricated market numbers as fact when their API fails, including a QoQ figure pointing the opposite way to reality
CATEGORY: correctness
CLAIM: `DataInsightsStrip` falls through to a hardcoded placeholder array on fetch error, publishing "Price Growth (QoQ) +3.2%, Q4 2025" when the live value is **−9.6%, Q2 2026**, with nothing marking it as a placeholder; two other components do the same in catch blocks.
EVIDENCE: `/home/fields/Feilds_Website/01_Website/src/components/DataInsightsStrip/DataInsightsStrip.tsx:188-231` — `:76-79` sets `error` but the render still reaches the placeholder array: Median Price `$1,400,000` / `+8.4%` (:193-194), DOM `32 days` (:200), Sales Volume `52` (:207), Turnover `5.1%` (:214), Active Listings `123` (:221), Price Growth QoQ `+3.2%` / `Q4 2025` (:228-229). Live today: `$1,490,000` / `+5.8%`, `34`, `43`, `2.91%`, `59`, **`-9.6%`** / `Q2 2026`. Mounted unconditionally at `MarketIntelligencePage.tsx:117` (`/` and `/news/:suburb`). Also `src/data/currentMarketSignals.ts:41` `lastUpdated: 'Q4 2024'` with wage growth `3.8%` and consumer spending `$8.2B`, used as the catch-block fallback at `MarketSignals.tsx:176-181` and printed as "Last updated: Q4 2024" at `:256` — live API returns `Q3 2026`, `3.4%`, `$22.3B` (a 2.7× error on spending). And `src/components/MarketNarrative/MarketNarrative.tsx:242` fabricates `insight: 'Properties selling 30% faster than historical average'` with invented `confidence: 0.88` / `data_quality_score: 0.90`, while live Robina DOM is *up* 9.5 days YoY.
STATUS: VERIFIED — placeholder literals and live API values both read. INFERRED for frequency: all three APIs returned 200 today, so I could not measure how often the fallback actually renders.
IMPACT: A single Netlify function timeout on the homepage silently publishes six invented statistics as fact, one of them reversing the direction of the market and carrying a quarter label three quarters stale. Memory records `netlify_mongo_eventloop_hang` and CDN-cache gaps, so the trigger is not hypothetical. Zero-quantity today, unbounded on the next outage.
CONFIDENCE: MEDIUM (structure HIGH; exposure unmeasured)
EFFORT: 2-3 hours
PROPOSED ACTION: On error render em-dashes and a "data temporarily unavailable" note rather than values; delete the numeric placeholder arrays in all three components. If a fallback is genuinely wanted, stamp it visibly with its own vintage.
FALSIFIED BY: an early return on `error` that I misread, or the placeholder branch being unreachable because `data` is always seeded server-side.

## FINDING 14: The sold-property editorial computes its own all-time median and will accept an asking price as a sale price
CATEGORY: data-integrity
CLAIM: `generate_sold_analysis.py` medians `Gold_Coast[suburb]` sold houses inline over all time — bypassing the union pipeline — and accepts `listing_price` where no sale price exists, then quotes the result in prose on ~767 public sold-property pages and in their meta descriptions.
EVIDENCE: `/home/fields/Fields_Orchestrator/scripts/backend_enrichment/generate_sold_analysis.py:175` (also `:202`, `:345`) — median over `db[suburb].find({listing_status: "sold", property_type: "House"})` with no date window and `listing_price` in the price-resolution chain. Output lands in `sold_analysis`, rendered by `SoldEditorialBlock` and quoted as "it sold X above the $Y that houses in Robina typically go for"; also used as the page description at `src/routes/property.$id.tsx:412`. Volume from `logs/fix-history/2026-08-05.md:900` `[SOLD-EDITORIAL-NO-RENDER-PATH]`: 767 write-ups (Robina 311, Burleigh Waters 231, Varsity Lakes 225), shipped live 2026-08-05. Canonical comparison: an all-time house median is not the same quantity as the published `rolling_12m_median_price` of $1,490,000 / $1,925,000 / $1,400,000, and the `listing_price` fallback is the same defect class as `logs/fix-history/2026-08-05.md:214` `[HOWITSOLD-ASKING-PRICE-CAMPAIGN]`, which was fixed in the article path but not here.
STATUS: VERIFIED for the code and the render path. INFERRED for the magnitude of the disagreement — I did not compute the all-time median per suburb, so I cannot state the dollar gap.
IMPACT: ~767 indexed public pages benchmark a sale against a median we do not publish anywhere, computed on a different window and a corpus that can include asking prices. It is the highest page-count instance of the bypass pattern, but the per-page error is likely smaller than Findings 1 and 9, which is why it ranks here.
CONFIDENCE: MEDIUM
EFFORT: 3 hours
PROPOSED ACTION: Read `precomputed_indexed_prices.rolling_12m_median_price` (with sample size) for the benchmark, state the window in the prose, and drop `listing_price` from the price-resolution chain entirely — a sold analysis has no business quoting an asking price as a transaction.
FALSIFIED BY: the all-time median coming out within a few percent of the rolling 12-month figure, and no sold document in the three suburbs resolving its price via `listing_price`.

## COVERAGE
CHECKED: `logs/fix-history/` (all 129 dated files enumerated; 2026-08-04 → 08-07 read in full, plus targeted reads of the envelope, backfill, sold-editorial, off-market and health-board entries) and mined for recurrence patterns. `system_monitor.job_runs` — all 80 heartbeats against declared cadence and `stale_hours`, cross-referenced against `_PAUSED_JOBS` in `scripts/main_site_health_check.py:1460-1486`: **29 stale/error rows, every one of them deliberately paused and correctly rendered as a known gap** — I found no dead-but-registered job, which is a genuine null result and a credit to the paused-vs-dead work. All 87 crontab lines cross-referenced against their log-file mtimes (three "missing" logs — `monthly_sold_refresh`, `monthly-market-precompute`, `fridge_home_shot` — traced to cron entries added on/after 2026-08-05, not to failures). Three jobs write heartbeats without `cadence_hours` and are therefore invisible on the Process Registry (`fetch_abs_market_signals`, `fetch_macro_indicators`, `propradar_ingest`) — they have a bespoke "Market Signals Fetch" collector at `main_site_health_check.py:294-302`, so not reported as a finding. The canonical median path (`scripts/precompute_union_prices.py`, `scripts/run_monthly_market_precompute.sh`, `scripts/check_union_median_integrity.py` and its daily log — clean, 3/3 suburbs on the union basis). The design envelope end to end (`precompute_valuations.py`, `on_demand_valuation.py` — correctly reuses the guarded function, `backfill_design_envelope.py`, and the byte-identical V4 prototype fork, which is not a divergence). Reader-facing verification by live HTTP GET: `/methodology`, `/accuracy` API, `/review/:slug` (plus a garbage slug), two envelope-breach property pages, two sold property pages (`SoldOut` correct, zero "auction" — the 2026-08-05 handover is genuinely live), and `/api/market-insights` for all three suburbs. Read-only Cosmos queries across `Gold_Coast.{robina,burleigh_waters,varsity_lakes}`, `suburb_median_prices`, `precomputed_indexed_prices`, `precomputed_market_charts`, `precomputed_seasonality`, `absorption_rate_snapshots`, `system_monitor.{market_pulse, job_runs, mainsite_health_snapshots, valuation_accuracy, offmarket_positioning}`. Inline-metric sweep across `scripts/`, `src/`, `netlify/functions/`. An AST sweep of broad-`except` handlers in `scripts/`, `src/`, `shared/` (358 hits, triaged by liveness and by whether the empty result reaches public output or a DB write). All eight PostHog query helpers compared — **the historical `[]`-on-504 bug is genuinely fixed in every core helper** (`crm_sync.py:86` raises `PostHogQueryError`; `brain2_util.py:19` re-raises; `soft=True` has zero callers); only two wrapper-level swallows remain (`samantha/kpi_rollup.py:80` — unscheduled; `samantha/seller_intent.py:207` — the import guard above a query path whose own comment forbids the behaviour), neither strong enough to rank. `market_pulse.narrative.pillars` — **the 2026-07-23 failure mode is closed: the field no longer exists on any of the 21 docs**, and both consumers guard for it.

NOT CHECKED: whether the Q2 2026 quarterly PDF was actually distributed (the single biggest gap in Finding 8's impact estimate). The absorption sibling-field contradiction inside `data_snapshot` (`generate_market_pulse.py:588-591` overwrites only `absorption_rate_months` with the PropRadar value, leaving `absorption_monthly_sales` and `absorption_active` Domain-derived, so the published inputs do not reproduce the published output — real, verified, but I judged it below the cut). The off-market card-06 dead source (`fact_bundle.py:208` reads `valuation_data.recent_sales`, absent on 99.7% of the 22,489-deck fleet) and the 62.6%-thin reachable-only decks — both are already logged as "FINDING, NOT YET FIXED" in `logs/fix-history/2026-08-05.md:719` and `:738`, so they are known rather than silent. The `sales_volume_prev` / "Most recent complete quarter" off-by-one mislabel in `OverviewSection.tsx:128` and `CrashRiskSection.tsx:137`. The legacy anchored `*_sales_volume.timeline` still feeding the quarterly report's FCI (`10_Market_Report/pipeline/fci_calculator.py:170`, `generate_charts.py:68,180` — neither file references `union_timeline`) while both website paths prefer the union. `/methodology`'s unreproducible cohort figures (458 vs 569 measured; 1,696 vs 1,035) and `CrashRiskSection.tsx:339`'s uncited "7,760 house sales" against a 2,610-house sold corpus. `task_executor.py:227-229` returning `[]` on a config-load failure with no zero-step guard — a malformed `process_commands.yaml` would produce a pipeline that does nothing and reports success. `coverage_check.py:388-392` swallowing a `count_documents` RU failure into `enriched: 0` and publishing `status: "critical"`. No Netlify Function logs, no PostHog session data, no Facebook/Google Ads API state, no mini-site or compliance pipeline, no RU-cost analysis, and no visual/screenshot verification of any page.

MOST LIKELY BLIND SPOT: the LLM-written layer. Findings 1 and 9 prove that wrong numbers are being handed to Claude with instructions to treat them as authoritative, but I inspected the *inputs*, not the *outputs* — I never read a single generated `ai_analysis`, `sold_analysis` or appraisal body to see which wrong figures actually made it into published prose, or whether the fact-check agent caught some of them. There could be an order of magnitude more reader-facing wrongness inside those ~767 sold write-ups and ~206 listing editorials than in all the template code I checked, and it is invisible to grep because it is prose. Second blind spot: the off-market deck fleet (22,489 pages, the largest public surface Fields has) — I checked its positioning-data completeness at the database level and read the deck emitters only glancingly, so a systematic wrong number rendered on 20,000 pages would very likely have escaped me.
