# claude_arm_final — E1_cost

Opus 5, read-only, identical brief to the GPT arm.

---

## FINDING 1: A 400 GB SSD nobody has mounted since 30 July is still billing $76 AUD/month
CATEGORY: cost
CLAIM: `fields-blob-storage` (400 GB pd-balanced) was kept attached-but-unmounted as a rollback for the 30 July blob-disk migration, with an explicit "delete after ~48h" follow-up that is now 8 days overdue, and it is billing $2.51 AUD/day.
EVIDENCE: `gcloud compute disks list --project=fields-estate` → `fields-blob-storage 400 pd-balanced ['fields-orchestrator-vm'] READY`. It is attached as `/dev/sdb` (`/dev/disk/by-id/google-fields-blob-storage -> ../../sdb`, `/sys/block/sdb/size` = 838860800 sectors = 400 GB) but appears in neither `/proc/mounts` nor `/etc/fstab` — the only data mount is `/dev/sdc /data/blobs ext4` (UUID `37b22927-4ca3-414c-adfa-8d393145e009`, the 750 GB HDD, 738 G / 44% used). Per-resource billing: `python3 scripts/gcp_cost_report.py --month 2026-08 --by resource` → `fields-blob-storage 10.34 AUD 18.1%`; daily BigQuery breakdown of `fields-estate.billing_export.gcp_billing_export_resource_v1_015552_D96B77_2718DA` gives $2.51/day on each of the three complete days (Aug 3, 4, 5). The follow-up it was meant to get: `logs/fix-history/2026-07-30.md:224` — "**FOLLOW-UP:** after ~48h stable, `gcloud compute instances detach-disk … --disk=fields-blob-storage` + `gcloud compute disks delete fields-blob-storage` → … stops paying for it."
STATUS: VERIFIED — I inspected it
IMPACT: $76 AUD/month ($2.51/day × 30.4), 18.6% of the $410/month `fields-estate` GCP bill. Rests on actual billing-export rows net of credits, not a price-list model; internally cross-checked against the 100 GB boot disk on the same pd-balanced SKU ($0.63/day — an exact 4:1 ratio for a 4:1 capacity ratio). Note the earlier hand-modelled figure in `logs/fix-history/2026-08-05.md:365` ("~$94 AUD/month") overstated it by 24%.
CONFIDENCE: HIGH
EFFORT: 15 minutes
PROPOSED ACTION: `gcloud compute instances detach-disk fields-orchestrator-vm --disk=fields-blob-storage --zone=australia-southeast1-b` then `gcloud compute disks delete fields-blob-storage`. If Will wants a longer rollback window, snapshot first ($0.04/GB/mo ≈ $16 one-off for 400 GB) and delete the disk — a snapshot costs a fifth of the live disk.
FALSIFIED BY: Finding the disk in `/proc/mounts` or `/etc/fstab`, or finding a process holding it open (`lsof /dev/sdb`), or `fields-blob-storage` absent from the per-resource billing breakdown.

## FINDING 2: Two VMs in a second GCP project have been running ~170 days for a system retired in March
CATEGORY: cost
CLAIM: `property-scraper` (e2-standard-2) and `searxng-server` (e2-micro) in project `property-data-scraping-477306` have run continuously since 19/17 February on a separate billing account, but the CEO/Codex agents they hosted were disabled on 2026-03-31 and nothing they produce reaches the live database.
EVIDENCE: `gcloud compute instances list --project=property-data-scraping-477306` → both `RUNNING`, `LAST_START_TIMESTAMP` 2026-02-19 and 2026-02-17. `gcloud billing projects describe property-data-scraping-477306` → `billingEnabled: true`, account `0154C4-059CFE-8490AD` (separate from `fields-estate`'s `015552-D96B77-2718DA`, so it does not appear in any of the GCP cost tooling built on 2026-08-05). CEO agent crons are commented out: `logs/crontab-backups/crontab.bak-20260805-111152:54-56` — "`# DISABLED 2026-03-31: Codex agents stopped`". The backup scraper's documented output collections are empty in the live DB: `Gold_Coast.property_url_tracking` = 0 docs, `Gold_Coast.new_url_discoveries` = 0 docs, and `{"scrape_source":"backup_scraper"}` = 0 in each of `robina`, `varsity_lakes`, `burleigh_waters`. The business's own audit already says so: `001_Our_Competitive_Advantages/05_WHERE_WE_FALL_SHORT.md:149` — "### 19. Backup scraper VM (property-scraper) is dormant in operating sense / Built. Not exercised." Cloud Monitoring (14 days, hourly ALIGN_MEAN, `compute.googleapis.com/instance/cpu/utilization`): `property-scraper` mean 8.1% / p95 9.0% / max 10.1% (it was 85% sustained when audited in March — `output/backup-scraper-audit-2026-03-31.md:113`); `searxng-server` mean 9.3%, and **0 bytes sent for 7 consecutive days** (`network/sent_bytes_count`, 1.4 MB/day received) — nothing is querying it. `output/backup-scraper-audit-2026-03-31.md:115` also records that SearXNG "exists but has no backing service", and the only repo references to it are stale docs (`AGENTS.md:2110`, `scripts/ceo-context-export.py:789`).
STATUS: VERIFIED — I inspected it (idleness of `searxng-server` verified; `property-scraper` verified running and disconnected from production, its local-only usefulness INFERRED)
IMPACT: ~$113 AUD/month of compute. Derived from the measured rate for this VM's e2-standard-4 ($6.57/day for 4 vCPU + 16 GB): e2-standard-2 is exactly half → ~$3.28/day = $100/mo; e2-micro ≈ $12.7/mo. Their two 30 GB pd-standard disks add ~$4.6/mo (from the measured $1.88/day for 750 GB of the same SKU) and would be retained under the proposal.
CONFIDENCE: MEDIUM (HIGH that they are running and billed and that nothing consumes them; MEDIUM that no one wants the scraper as cold standby)
EFFORT: 1 hour, including confirming with Will that the backup scraper is not a required DR control
PROPOSED ACTION: `gcloud compute instances stop searxng-server --project=property-data-scraping-477306` and delete it (zero egress for 7 days, no code path). **Stop, do not delete,** `property-scraper` — stopping ends all compute charges while keeping the 30 GB boot disk ($2.3/mo) so the DR image survives; per `05_WHERE_WE_FALL_SHORT.md:149` the right operating model is a weekly start-scrape-compare-stop cycle, not a permanently-running instance. Separately, add billing account `0154C4-059CFE-8490AD` to `scripts/gcp_cost_monitor.py` (`BILLING_ACCOUNT` is a single hardcoded constant) or the next audit will miss it again.
FALSIFIED BY: A cron, systemd timer or webhook on either VM that feeds something live; or `property-scraper` output appearing in production (non-zero `scrape_source: "backup_scraper"` docs); or Will stating the standby must stay hot.

## FINDING 3: Every DB byte crosses the public internet uncompressed, on a fresh connection per request — 56% of all VM egress
CATEGORY: cost
CLAIM: 53 of the 54 database-using Netlify functions open a brand-new MongoClient per invocation with no wire compression configured, so mongod→Netlify traffic is 56% of all VM egress (7.1 GB/day) at full BSON size, and each invocation pays a full TCP+TLS+auth handshake Sydney↔US.
EVIDENCE: `netlify/functions/db.mjs:40-48` (cached client) and `:76-83` (`getFreshClient`) — the options objects contain `retryWrites`, timeouts and pool sizes but **no `compressors`** key. 53 functions import `getFreshClient`, 1 uses `getClient` (`grep -rl` across `netlify/functions/*.mjs`), and the per-request-client design is deliberate and documented at `db.mjs:57-70` (SDAM heartbeat timers keep the Netlify event loop alive → `ERR_HTTP2_PROTOCOL_ERROR`). The DB is on this VM, not Azure: `.env` `COSMOS_CONNECTION_STRING="mongodb://***@localhost:27017/?authSource=admin"`, `systemctl is-active mongod` → `active`, `ss -ltn` → `0.0.0.0:27017`, `mongod --version` → v7.0.31 (supports zstd/snappy/zlib). Measured egress split from the 24 h iptables snapshot in `logs/egress_counters.csv` (window 2026-08-05T07:05:50Z → 21:20:51Z, 14.25 h): `acct:mongo-out` 1,441,295,020 → 5,682,337,940 B = 4.24 GB (7.1 GB/day); `acct:all-egress` 3,884,174,144 → 11,433,402,534 B = 7.55 GB (12.7 GB/day) — mongod is 56%, outbound HTTPS 39%, serving the website 4%. Billed egress SKUs for Aug: "Network Internet Data Transfer Out from Sydney to Australia" $2.80 + "…to Americas" $2.75 + "…to Apac" $1.21 ≈ $1.50/day.
STATUS: VERIFIED — I inspected it
IMPACT: ~$26 AUD/month is mongod's share of a measured $46 AUD/month egress bill (56% × $1.50/day × 30.4). BSON typically compresses 4–8× with zstd, so a one-line connection-string change should recover $18–22/month, plus it shortens every API response. This is the residual after the caching work of 2026-08-05 — June was $167.72 ex-GST of egress (`logs/fix-history/2026-08-05.md:825`), so the big win is already banked and I am not double-counting it.
CONFIDENCE: MEDIUM (HIGH on the byte split and the absent compressor; MEDIUM on the compression ratio, which I did not measure on this payload mix)
EFFORT: 1–2 hours (append `&compressors=zstd,snappy` to the Netlify `COSMOS_CONNECTION_STRING`, redeploy, re-read the counters)
PROPOSED ACTION: Add `compressors=zstd,snappy` to the connection string used by the functions and confirm `acct:mongo-out` drops by re-running `scripts/egress_attribution_snapshot.sh`. While in `db.mjs`, delete the stale `retryWrites: false` and its "Azure Cosmos DB does NOT support retryable writes" comment (`:39-41`) — the DB has been self-hosted mongod since April 2026 and disabling retryable writes on mongod costs reliability for no reason.
FALSIFIED BY: A `compressors` parameter already present in the Netlify-side connection string (I could not read the Netlify env var, only the client options); or `acct:mongo-out` not falling after the change, which would mean the counter is dominated by something other than query results.

## FINDING 4: The cost dashboard is a hardcoded constant that understates real infrastructure spend by 3.3×
CATEGORY: cost
CLAIM: `cost_tracking.by_category.infrastructure` has been exactly $4.38/day every single day because it sums a hardcoded dict, not measurements — it books the VM as an "e2-medium" at $38/month (actual: e2-standard-4 at $200/month), invoices a second VM that is not in this project, and still bills $23/month for two Azure services that were decommissioned in April/June.
EVIDENCE: `scripts/cost-collector.py:42-78` `FIXED_MONTHLY_COSTS` — `:43-47` `google_cloud_vm` "GCP e2-medium (fields-orchestrator-vm)" $38 (actual machine type is `e2-standard-4` per `gcloud compute instances list`, billing $6.57/day = $200/mo); `:48-52` `google_cloud_vm_scraper` "$38" for the property-scraper, which lives in a different project on a different billing account (Finding 2); `:58-62` `azure_cosmos_db` $15 — the DB is `mongodb://…@localhost:27017` (Finding 3) and `logs/fix-history/2026-06-06.md:187` records "The live DB was migrated off Azure Cosmos (subscription DISABLED…)"; `:63-67` `azure_blob_storage` $8 — `logs/fix-history/2026-06-08.md:158` "Azure Blob omitted — decommissioned post-GCS"; `:73-77` `openai_api` $25 flat guess. `collect_daily_snapshot():235-238` computes `infrastructure` purely by summing that dict, so it can never move. Confirmed live: every `system_monitor.cost_tracking` doc from 2026-07-25 to 2026-08-07 reads `'infrastructure': 4.38`. Ground truth: July GCP invoice $409.59 AUD (`logs/fix-history/2026-08-05.md:339`); the billing export gives Aug 3/4/5 at $11.63 / $12.37 / $16.46 per day. `CLAUDE.md:109` also still says "e2-standard-2".
STATUS: VERIFIED — I inspected it
IMPACT: $133/month booked versus ~$442/month actual (GCP $410 + Netlify Pro $29 + domain $3) — a 3.3× understatement, and ~$23/month of it is charged for services that no longer exist. This is the mechanism that let Findings 1 and 2 survive: a $76/month disk and a $113/month pair of VMs cannot show up in a dashboard whose infrastructure line is a constant. Numbers rest on the resource-level billing export and the July invoice.
CONFIDENCE: HIGH
EFFORT: 2–3 hours
PROPOSED ACTION: Replace `FIXED_MONTHLY_COSTS`' four GCP/Azure entries with a read from `system_monitor.gcp_costs` (already populated daily by `scripts/gcp_cost_monitor.py`), delete the `azure_cosmos_db` and `azure_blob_storage` entries, delete `google_cloud_vm_scraper` (or move it behind the second billing account), and replace the flat `openai_api` guess with actual usage — or drop it and record $0 with a note, which is at least honest. Also correct `CLAUDE.md:109` to `e2-standard-4`.
FALSIFIED BY: An Azure invoice showing live Cosmos/Blob charges; or a `cost_tracking` document whose `infrastructure` value differs from 4.38.

## FINDING 5: The cost monitor built to catch exactly this waste reports "$0.00, 0 alerts, success"
CATEGORY: process
CLAIM: `gcp_cost_monitor.py` guards the case where the billing export table is missing but not the case where it exists and returns zero rows, so a $410/month cost line renders as MTD $0.00 with zero alerts and a green heartbeat on the Fields Systems Health board.
EVIDENCE: `scripts/gcp_cost_monitor.py:124-125` — `mtd = sum(...) for r in by_service` is 0 when `by_service` is `[]`; the three alert paths cannot fire on an empty result (`:140` spike check requires `len(complete) >= 8`; `:150` needs `forecast > MONTHLY_CEILING` and forecast is 0; `:159` needs Recommender savings > 0, and all four `IdleResourceRecommender`/`MachineTypeRecommender` queries return nothing today). There is no `if not by_service:` guard anywhere. Observed live — both `system_monitor.gcp_costs` documents read `month_to_date: 0, forecast_month_end: 0.0, by_service: [], alerts: [], has_resource_detail: true`, and the `system_monitor.job_runs` heartbeat reads `status: 'success'`, `detail: '2026-08 MTD 0.00 , forecast 0.00, top=n/a, 0 alert(s)'`. The missing-table case, by contrast, *is* guarded and did raise (`logs/gcp-cost-monitor.log` traceback → `RuntimeError: GCP cost data unavailable`), and the author's own comment at `:218-220` states the principle: "A monitor that shrugs when its data source is missing is the silent failure Rule 7 exists to prevent." An empty table is the same failure wearing a different hat. The data is there now — `bq query … WHERE invoice.month='202608'` → 2,998 rows, $57.21 gross, and running the query layer under a cron-like `env -i` returns three services correctly, so the next scheduled run should populate; the blind spot remains regardless.
STATUS: VERIFIED — I inspected it (that the code produces a silent $0 success; I did **not** establish why the two stored runs were zero — the export table was created 2026-08-05 03:37 UTC and the second run was 43 h later, which is consistent with a real mis-report but also with a slow first backfill)
IMPACT: Not a direct dollar saving, but it is the difference between the two findings above being caught in a day and being caught in eight. Guards the whole $410/month line plus the Recommender feed that is meant to surface idle disks automatically.
CONFIDENCE: HIGH
EFFORT: 30 minutes
PROPOSED ACTION: In `analyse()`, raise if `not by_service` (or `mtd == 0`) for a month with at least one elapsed day — the same deliberate-fatal treatment `CostDataUnavailable` already gets. Add `month_to_date` to a floor check so any run reporting less than, say, $50 MTD after 5 days of the month alarms. Also add the second billing account (Finding 2) so idle resources outside `fields-estate` are in scope.
FALSIFIED BY: A guard in the code I missed that alarms on empty `by_service`; or the health board already showing this job as ERROR/STALE rather than OK.

## FINDING 6: crm_sync runs 25×/day and 87% of runs create nothing — it re-upserts the same 67 contacts
CATEGORY: cost
CLAIM: `crm_sync.py` runs hourly on cron *and* again inside the nightly lead chain, issuing four 30-day-lookback PostHog HogQL queries and ~67 identical Mongo upserts each time; across 647 logged runs, 563 (87%) created zero contacts.
EVIDENCE: Two schedules for one job — `logs/crontab-backups/crontab.bak-20260805-111152:100` (`7 * * * * … scripts/crm_sync.py`) and `scripts/nightly_lead_chain.py:74-78` (`STEPS = [Step("crm_sync", [PY, f"{ROOT}/scripts/crm_sync.py"], …)`) at 00:15. Default lookback is 30 days (`crm_sync.py:9`, `:578`, `:581`) with `POSTHOG_DEFAULT_LIMIT = 50000` (`:82`) across four queries (`:178`, `:223`, `:251`, `:281`). `logs/crm-sync.log` tail shows three consecutive runs producing `215`, `216`, `217` distinct visitors → `67 qualify` → `Done: 0 created, 67 updated` each time. Across the whole log: 647 `Done:` lines, of which `563` are `Done: 0 created`; total created across all runs ≈ 168 (≈6/day over ~27 days).
STATUS: VERIFIED — I inspected it
IMPACT: ~1,600 redundant Mongo upserts/day and ~100 PostHog HogQL scans/day over a 30-day event window, for ~6 genuinely new contacts/day. No RU bill (the DB is local), so the cost is VM CPU on the $200/month instance and avoidable load on the PostHog query API — which matters because that API's 504 load-shedding has already silently returned `[]` and lost three nights of leads (`MEMORY.md`, nightly lead chain note). Cutting cron to 4×/day removes ~84% of the calls and delays a new contact by at most 6 h.
CONFIDENCE: HIGH
EFFORT: 15 minutes
PROPOSED ACTION: Change the cron from `7 * * * *` to `7 */6 * * *` (or delete the cron entirely and rely on the 00:15 chain plus an on-demand trigger). Independently, gate the upsert on a content hash so a contact whose engagement fields have not changed is not rewritten.
FALSIFIED BY: A downstream consumer that needs sub-hourly contact freshness — e.g. `hot_lead_responder` (every 10 min) genuinely acting on contacts created in the previous hour. Its log shows 0 successful drafts (Finding 8), so today nothing does.

## FINDING 7: 1,440 DB writes a day buy a one-hour utilisation window, which is why the $200/month VM has never been rightsized
CATEGORY: cost
CLAIM: `write_vm_metrics.py` runs every minute and trims the collection to 60 documents, so the business pays 1,440 writes + 1,440 sorted reads + 1,440 deletes a day for one hour of history — while Cloud Monitoring retains six weeks of the same metrics for free and shows the VM's real profile is mean 22.6% CPU.
EVIDENCE: `write_vm_metrics.py:71` `col.insert_one(doc)`, `:75` `col.find({}, {"_id": 1}).sort("_id", -1).limit(61)`, `:78` `col.delete_many({"_id": {"$lt": cutoff_id}})`; scheduled at `logs/crontab-backups/crontab.bak-20260805-111152:59` (`* * * * *`). Live: `system_monitor.vm_metrics.count_documents({})` = **61**, spanning 2026-08-07 08:50 → 09:50 — exactly one hour. The free alternative already holds the answer: Monitoring API `compute.googleapis.com/instance/cpu/utilization`, 14 days at 3600 s ALIGN_MEAN, instance `5285763293018462683` → n=336, mean **22.6%**, p50 19.4%, p95 **45.8%**, max 99.5%. Memory on the same VM (from the 61 live docs): mean 52.5%, max 76.7% of 16.77 GB. Billing: the instance resource is $6.57/day = ~$200/month, 48.7% of the `fields-estate` bill and its single largest line.
STATUS: VERIFIED — I inspected it
IMPACT: The write volume itself is now nearly free (self-hosted mongod), so the real cost is decision-blindness on a $200/month line. p95 of 45.8% on 4 vCPU = 1.83 vCPU, so an e2-standard-2 would sit at ~92% p95 and I do **not** recommend it — the Aug 1 and Aug 6 disk-thrash lockouts and the long OOM history argue the other way. A 2 vCPU / 16 GB custom E2 keeps all the RAM (memory peaks at 76.7%) and halves the core line: cores are $17.63 of the $27.08 instance cost in the sampled window, so ~$8.8 of every $27 → ~$65/month, at real risk of reintroducing the lockups. The defensible saving is $0 until a proper CPU-headroom test exists; the finding is that the measurement to justify either choice has been thrown away 1,440 times a day.
CONFIDENCE: HIGH (on the retention defect and the measured profile); LOW (on any specific downsize being safe)
EFFORT: 30 minutes to fix retention; 1 day to trial a rightsize safely
PROPOSED ACTION: Raise the trim from 60 to 20,160 documents (14 days at 1 min) or drop the interval to 5 minutes and keep 30 days — either costs less than today and yields a real profile. Then run the rightsizing decision off Cloud Monitoring p99 rather than a guess, and enable `google.compute.instance.MachineTypeRecommender` observation time before touching the machine type.
FALSIFIED BY: A consumer that only ever needs the last hour *and* would break with more documents (`scripts/refresh-ops-context.py` is the likely reader); or Monitoring p99 near 100%, which would settle the sizing question the other way.

## FINDING 8: Two watchdogs that cannot fail — one reads a CDN cache, the other has failed 1,933 times invisibly
CATEGORY: process
CLAIM: `keep_warm_forsale_v3.py` was written to keep a Netlify function warm and detect its cold-start 500s, but the endpoint now carries `s-maxage=300` so the 5-minute cron hits the CDN edge and can neither warm the function nor see it break; and `hot_lead_responder.py` has failed on every run since an auth-precedence change, 1,933 times, with no Rule 7 heartbeat to show it.
EVIDENCE: **(a)** `scripts/keep_warm_forsale_v3.py:23` `URL = "https://fieldsestate.com.au/api/v1/properties/decision-feed-v3"`, `:33` a plain `requests.get` with no cache-buster, `:34` success = `status_code == 200 and len(r.content) > 1000`; scheduled `*/5 * * * *` (crontab backup `:176`). Live `curl -sI` on that URL returns `cache-control: public,max-age=300,s-maxage=300` and **`age: 5`** — i.e. served from cache, function never invoked. A cached 200 also masks a broken function for up to 5 minutes, which is precisely the failure the script exists to catch (`:5-8`, added for the paid `buyerbrief_carousel_v1` funnel — itself paused since 2026-07-30). **(b)** `scripts/hot_lead_responder.py:39-47` shells out to `claude -p` and does not strip `ANTHROPIC_API_KEY`; `logs/hot-lead-responder.log` contains **1,933** `draft failed` lines, all `claude exit 1: ⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login`, always for the same lead address, so it never advances. Ten other files in the repo do strip that variable (`15_Off-Market/Reinforcement_Learning/run_cycle.sh`, `16_General_Reinforcement_Learning/*_cycle.sh`, `scripts/backend_enrichment/claude_max_client.py`, …). `system_monitor.job_runs` tracks 80 jobs; `hot_lead_responder` is not one of them, and neither is `keep_warm_forsale_v3` or `crm_sync`.
STATUS: VERIFIED — I inspected it
IMPACT: 288 + 144 = 432 no-value cron invocations/day, negligible in dollars. The cost is a false green: the paid-funnel uptime alarm reports healthy off a CDN cache, and a lead-response drafter has been dead for ~1,933 runs (~13 days at 144/day) without a single alert, on a system whose CLAUDE.md Rule 7 exists specifically to close this class. 80 jobs are on the health board and these three are not.
CONFIDENCE: HIGH
EFFORT: 1 hour
PROPOSED ACTION: (a) Give the keep-warm a cache-busting query parameter or a `Cache-Control: no-cache` request header so it actually reaches the function — or retire it, since Netlify caching now covers the cold-start problem it was built for and the funnel is paused. (b) Route `hot_lead_responder.py` through `scripts/backend_enrichment/claude_max_client.py` instead of a bare `claude -p`, or wrap the subprocess in `env -u ANTHROPIC_API_KEY` like the ten scripts that already do. (c) Wrap all three in `job_run(name, cadence_hours=…)`.
FALSIFIED BY: Repeated `curl -sI` returning `age: 0` (Netlify not actually serving the keep-warm from cache); or a `hot_lead_responder` heartbeat existing under another job name; or recent `draft ok` lines in its log.

## FINDING 9: An orphaned disk from a non-Fields VM bills to the Fields billing account
CATEGORY: cost
CLAIM: `greyhound-betting-vm` sits TERMINATED in the `fields-estate` project but its 20 GB pd-standard disk is still provisioned and appears as a billed resource.
EVIDENCE: `gcloud compute instances list --project=fields-estate` → `greyhound-betting-vm australia-southeast1-b e2-small TERMINATED`; `gcloud compute disks list` → `greyhound-betting-vm 20 pd-standard ['greyhound-betting-vm'] READY`. Per-resource billing shows `greyhound-betting-vm 0.20 AUD` for the sampled window ($0.04–0.05/day in the daily breakdown). Nothing in the Fields codebase references it.
STATUS: VERIFIED — I inspected it
IMPACT: ~$1.4 AUD/month. Small, and I am ranking it last deliberately — but it is a 5-minute delete and it is a greyhound-betting resource cross-charged to the property business, so it also distorts the (now real) per-resource cost view.
CONFIDENCE: HIGH
EFFORT: 5 minutes
PROPOSED ACTION: Confirm with Will that the greyhound project is dead (there is a separate `greyhound-app` still RUNNING in `greyhound-gb-track-specialist`), then delete the instance and disk, or move them to the greyhound billing account.
FALSIFIED BY: Will intending to restart that VM, or data on the disk not held elsewhere.

## FINDING 10: The repo-wide default LLM backend is an OpenRouter account that is $0.14 overdrawn
CATEGORY: cost
CLAIM: `.env` sets `ANTHROPIC_BACKEND=openrouter` globally — a metered provider whose lifetime spend is $13,748 and whose balance is now exhausted — so any LLM script that does not explicitly clear it targets a dead paid API instead of the credit-free Claude Max subscription.
EVIDENCE: `.env:136` `USE_CLAUDE_MAX=1`, `.env:140` `ANTHROPIC_BACKEND=openrouter`, `.env:143` `VISION_BACKEND=gemini_vertex`. OpenRouter API (`GET /api/v1/credits`): `total_credits: 13748.4816`, `total_usage: 13748.6267` — overdrawn by $0.14; `GET /api/v1/key`: `usage: 268.34`, `usage_monthly: 0.00708`. `config/process_commands.yaml:405` still exports `ANTHROPIC_BACKEND=openrouter` for step 117; that step nonetheless works because `shared/claude_vision.py:215` checks `VISION_BACKEND` **first** and short-circuits to Gemini-via-Vertex — so the OpenRouter export on that line is dead config that reads as live routing (`logs/runs/2026-08-06T20-30-08_failed/13_step_117…` reports `Processed: 1, GPT errors: 0`). Vertex is genuinely cheap: "Gemini 2.5 Flash" SKUs total $1.37 AUD for the sampled window ≈ $8/month.
STATUS: VERIFIED — I inspected it
IMPACT: $0/month today (the account is exhausted, so calls fail rather than bill) but a latent one: the moment anyone tops OpenRouter up, every script that inherits the default resumes metered spend against a subscription that is already paid for — and history says that path has cost $13,748. Only 5 files in the repo call `make_client`, and 4 handle `ANTHROPIC_BACKEND` explicitly, so the blast radius is small today and will grow with each new script that forgets.
CONFIDENCE: MEDIUM (HIGH on the config and the balance; MEDIUM that no in-flight job is silently degraded by it right now)
EFFORT: 30 minutes
PROPOSED ACTION: Invert the default — remove `ANTHROPIC_BACKEND=openrouter` from `.env` so `make_client` honours `USE_CLAUDE_MAX=1` by default, and set `ANTHROPIC_BACKEND=openrouter` only in the few places that genuinely need it. Delete the misleading `export ANTHROPIC_BACKEND=openrouter` from `config/process_commands.yaml:405` since `VISION_BACKEND` already wins. Add a `low balance` probe for OpenRouter to `scripts/api_health_monitor.py` (it covers 19 providers; a $0 balance on the default backend should be a hard alarm).
FALSIFIED BY: `make_client` preferring `use_max` over `ANTHROPIC_BACKEND` (CLAUDE.md says the opposite — "it checks that var *before* `use_max`" — but I read the note, not the function); or OpenRouter being deliberately kept at zero as a hard spend cap.

## COVERAGE
CHECKED: `logs/crontab-backups/crontab.bak-20260805-111152` (all 262 lines, plus the live spool line for `gcp_cost_monitor` via `sudo`), classified by frequency and cross-checked against what each job's data actually changes. GCP inventory across **all 23 projects** the account can see (`instances`, `disks`, `snapshots` — none exist, `addresses` — one, in use, `images` — none) plus billing linkage per project. The BigQuery detailed billing export directly (`fields-estate.billing_export.gcp_billing_export_resource_v1_…`): by service, by SKU, by resource, and a per-day-per-resource pivot; reconciled against the $409.59 July invoice. Cloud Monitoring CPU (14 d) and network bytes (7 d) for `fields-orchestrator-vm`, `property-scraper` and `searxng-server`. Block-device reality on this VM (`/proc/mounts`, `/etc/fstab`, `/dev/disk/by-id`, `/sys/block/*/size`, `df`). `scripts/cost-collector.py` in full, `scripts/gcp_cost_monitor.py` and `scripts/gcp_cost_report.py` in full, `write_vm_metrics.py` in full, `scripts/write_heartbeat.sh`, `scripts/keep_warm_forsale_v3.py`, `scripts/vertex_quota_watch.py`, `netlify/functions/db.mjs`. Live DB reads against the self-hosted mongod: `cost_tracking` (July + August), `ad_daily_metrics` (checked for duplicate-doc inflation on the two $370–573 days — none, 44 and 64 distinct `ad_id`s, spend is real), `gcp_costs`, `job_runs` (80 jobs, 1 in error), `Gold_Coast.property_url_tracking` / `new_url_discoveries` / `{scrape_source:"backup_scraper"}`, `vm_metrics`. Netlify Deploys and Accounts APIs; live cache headers on two API routes. OpenRouter credits + key endpoints. `logs/egress_counters.csv` and `logs/egress_destinations.csv`. `logs/fix-history/` for July–August plus targeted greps back to February.

**Null results worth stating:** (1) **The Cosmos RU premise in the brief no longer applies.** The database was migrated off Azure Cosmos to a self-hosted mongod on this VM in April 2026 (`.env` → `localhost:27017`, `mongod` v7.0.31 active, `logs/fix-history/2026-06-06.md:187`, `2026-07-16.md:207`) — `getLastRequestStatistics` does not exist on it. RU-per-operation is not a billed dimension anywhere in this system, so missing `listing_status` filters and unbounded `find()` calls cost CPU on the $200/month VM, not RU dollars. CLAUDE.md, `SCHEMA_SNAPSHOT.md` and `cost-collector.py` all still describe Cosmos. (2) **Netlify is not a cost problem.** Pro plan, 100 deploys over 4 days at a 148 s median = 175 build minutes; even at the account's own $0.00667/credit top-up rate that is ~$1.17. The 28 of 100 deploys in `error` state are a quality signal, not a money one. (3) **GCS is negligible** — all six buckets together are $0.90 AUD for the sampled window, ~$5.5/month. (4) **Ad spend is currently $0** and has been since 2026-07-31; July's $1,953 was real, not a double-count. (5) **The health board is broadly honest** — 80 jobs tracked, 1 in error (`offmarket_coverage_scraper`, a JSONDecodeError), zero stale.

NOT CHECKED: Azure — I could not confirm whether the Cosmos account `fields-property-cosmos` or the old blob account are *fully* deprovisioned versus merely disabled-and-still-accruing; there is no Azure credential or invoice on this VM, and `cost-collector.py` books $23/month for them on faith. Netlify **function invocation and bandwidth** volume (only builds and plan metadata are exposed by the endpoints I used), so the serverless side of the site's cost is unquantified. PostHog, Anthropic Max, OpenAI, Bright Data, JustCall, Google Maps/Static Maps and Telegram billing — all subscription or metered lines with no local cost record; `api_health_monitor.py` probes 19 providers for liveness but not for spend, so the entire SaaS column of the P&L is invisible from here. Per-endpoint Cosmos-equivalent query shapes in the 54 Netlify functions (I read `db.mjs` and the client options, not the queries) — the brief's "missing `listing_status`" class may still exist as a latency and CPU problem. The daily `gsutil -m rsync` of 1.07 M files at 03:00 and the daily `mongodb-backup.sh` — I confirmed their GCS charges are trivial but did not measure their CPU/IO cost on a VM with a documented disk-thrash lockup history. Whether the 750 GB HDD needed to be 750 GB (44% used; disks cannot shrink, so it is sunk either way, but ~$30/month of that $57 is unused capacity). Other businesses' resources on other billing accounts (`zarleah-dev`, `greyhound-app`, `horse-racing-vm`) — out of scope for Fields but they share this Google identity.

MOST LIKELY BLIND SPOT: The non-GCP recurring lines. I got hard, resource-attributed numbers for GCP because the billing export exists, and that is exactly why every finding above is a GCP finding — the light is there. Anthropic Max, PostHog, Bright Data, JustCall and the residual Azure services have no local cost record at all, and `cost-collector.py` either guesses them or omits them entirely. If there is a $100+/month subscription being paid for a system that was retired months ago — and the pattern of this codebase is *exactly* that (Codex agents, Ghost CMS, Azure Blob, Azure Cosmos, the Home Owner funnel, the RL crons all retired while something kept running) — it would not appear anywhere I looked. Second blind spot: I could not read the Netlify environment variables, so my compression claim in Finding 3 rests on the client options in code rather than the connection string actually in use.
